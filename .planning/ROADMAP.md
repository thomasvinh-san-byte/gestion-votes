# AG-VOTE Roadmap — v1.1

## Milestone: Post-Audit Hardening

**Goal**: Harden the application after UX/UI audit fixes, establish automated testing, and address technical concerns identified in the codebase mapping.

---

### Phase 1 — E2E Test Suite Hardening [REQ-001]
**Status**: done
**Goal**: Harden existing Playwright suite (14 specs) and add audit regression coverage.
- [x] Extracted shared helpers (loginAsOperator/Admin/Voter, constants) to `tests/e2e/helpers.js`
- [x] Updated all 14 original specs to use shared helpers (DRY)
- [x] Added audit regression spec (`audit-regression.spec.js`) — 13 tests covering P1/P2/P3 fixes
- [x] Added mobile viewport regression spec (`mobile-viewport.spec.js`) — 13 tests (375x812 + 768x1024)
- [x] Added `package.json` with Playwright dependency and test scripts
- [x] All 197 tests across 16 files parse correctly in headless mode

### Phase 2 — CI Pipeline Expansion [REQ-002]
**Status**: done
**Goal**: Expand existing CI workflow with linting and E2E.
- [x] Added PHPStan job to validate step (level 5, `app/` directory)
- [x] Added ESLint job (v9 flat config, 0 errors after auto-fix)
- [x] Added Playwright E2E job (chromium, runs after validate + lint)
- [x] Cached Composer and npm dependencies
- [x] Build gates on all quality checks (validate, lint-js, e2e)
- [x] Renamed eslint.config.js → .mjs for ESM/CJS coexistence

### Phase 3 — CDN Hardening [REQ-003, REQ-004]
**Status**: done
**Goal**: Eliminate CDN single points of failure.
- [x] Vendored HTMX 1.9.12 to `public/assets/vendor/htmx.min.js`
- [x] Vendored Chart.js 4.4.1 to `public/assets/vendor/chart.umd.js`
- [x] Updated 4 HTML files to use local vendor paths
- [x] Removed unpkg.com and cdn.jsdelivr.net from CSP script-src
- [x] Google Fonts kept as CDN (CSS only, with display=swap)
- [x] Added `vendor:copy` npm script for reproducible builds

### Phase 4 — App Shell Deduplication [REQ-005]
**Status**: done (no changes needed)
**Goal**: Single source of truth for shared layout.
- [x] Audit found architecture already optimal:
  - Sidebar: JS-injected from single `partials/sidebar.html`
  - Mobile nav: 100% JS-generated by `shell.js`
  - Header: Intentionally inline per-page (unique content)
  - No duplication at component level

### Phase 5 — Frontend Error Handling [REQ-006]
**Status**: done (no changes needed)
**Goal**: No silent fetch failures anywhere.
- [x] Audit found 0 unhandled fetch calls (all have error handling)
- [x] `api()` wrapper used consistently with timeout, CSRF, idempotency
- [x] `setNotif()` toast notifications on failures throughout
- [x] 2 minor partial cases acceptable (demo mode detection, partial loading)

### Phase 6 — Accessibility & Performance [REQ-007, REQ-008]
**Status**: done
**Goal**: WCAG AA compliance and improved load times.
- [x] ag-confirm: Added focus trap for WCAG 2.1 modal compliance
- [x] ag-confirm: Added :focus-visible styles on dialog buttons
- [x] ag-toast: Fixed text contrast (--color-text vs --color-text-muted)
- [x] ag-toast: Added :focus-visible style on close button
- [x] ag-searchable-select: Added aria-label on search input
- [x] Existing strengths confirmed: skip links, aria-live regions, reduced motion, global :focus-visible
