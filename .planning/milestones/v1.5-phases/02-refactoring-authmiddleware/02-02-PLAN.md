---
phase: 02-refactoring-authmiddleware
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tests/Unit/SessionManagerTest.php
  - tests/Unit/RbacEngineTest.php
autonomous: true
requirements: [REFAC-02]

must_haves:
  truths:
    - "SessionManager can be tested in isolation (timeout logic, expiry check)"
    - "RbacEngine can be tested in isolation (role checks, permissions, transitions)"
    - "Full unit test suite passes green"
  artifacts:
    - path: "tests/Unit/SessionManagerTest.php"
      provides: "Unit tests for SessionManager extraction"
      min_lines: 50
    - path: "tests/Unit/RbacEngineTest.php"
      provides: "Unit tests for RbacEngine extraction"
      min_lines: 80
  key_links:
    - from: "tests/Unit/SessionManagerTest.php"
      to: "app/Core/Security/SessionManager.php"
      via: "direct static calls"
      pattern: "SessionManager::"
    - from: "tests/Unit/RbacEngineTest.php"
      to: "app/Core/Security/RbacEngine.php"
      via: "direct static calls"
      pattern: "RbacEngine::"
---

<objective>
Create unit tests for the newly extracted SessionManager and RbacEngine classes, proving they work correctly in isolation.

Purpose: REFAC-02 validation — demonstrate extracted classes are independently testable with nullable DI, confirming the refactoring produced genuinely decoupled components.
Output: Two test files covering core behaviors of each extracted class.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-refactoring-authmiddleware/02-RESEARCH.md
@.planning/phases/02-refactoring-authmiddleware/02-01-SUMMARY.md

@app/Core/Security/SessionManager.php
@app/Core/Security/RbacEngine.php
@app/Core/Security/Permissions.php
@tests/Unit/AuthMiddlewareTest.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SessionManagerTest with timeout and expiry tests</name>
  <files>tests/Unit/SessionManagerTest.php</files>
  <read_first>
    - app/Core/Security/SessionManager.php (the class under test — read full file to know exact method signatures)
    - tests/Unit/AuthMiddlewareTimeoutTest.php (pattern reference — how existing timeout tests work)
    - app/Core/Security/Permissions.php (constants used)
  </read_first>
  <behavior>
    - Test: getSessionTimeout returns DEFAULT_SESSION_TIMEOUT (1800) when no DB config
    - Test: setSessionTimeoutForTest overrides getSessionTimeout for given tenant
    - Test: setSessionTimeoutForTest(tenantId, null) clears override, reverts to default
    - Test: getSessionTimeout clamps values to min 300, max 86400
    - Test: checkExpiry returns true when lastActivity exceeds timeout
    - Test: checkExpiry returns false when lastActivity is within timeout
    - Test: isSessionExpired returns false initially, true after expired session detected
    - Test: consumeSessionExpired returns true once then false on second call
    - Test: reset clears all cached state (timeout cache, test overrides, expiry flag)
  </behavior>
  <action>
    Create `tests/Unit/SessionManagerTest.php` in namespace `Tests\Unit`.

    - `declare(strict_types=1);`
    - `use PHPUnit\Framework\TestCase;`
    - `use AgVote\Core\Security\SessionManager;`
    - Class: `final class SessionManagerTest extends TestCase`
    - `protected function tearDown(): void { SessionManager::reset(); }` — clean static state between tests

    Write tests for each behavior listed above. Use `SessionManager::setSessionTimeoutForTest()` to inject test values. For checkExpiry tests, set a known timeout via setSessionTimeoutForTest then call checkExpiry with timestamps that are within/beyond that timeout.

    Do NOT mock DB — test the public static API with test helpers (same pattern as existing AuthMiddlewareTimeoutTest).

    Run: `timeout 60 php vendor/bin/phpunit tests/Unit/SessionManagerTest.php --no-coverage`
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && timeout 60 php vendor/bin/phpunit tests/Unit/SessionManagerTest.php --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `php -l tests/Unit/SessionManagerTest.php` passes
    - `timeout 60 php vendor/bin/phpunit tests/Unit/SessionManagerTest.php --no-coverage` shows 0 failures
    - `grep -c 'function test' tests/Unit/SessionManagerTest.php` shows at least 7
    - `grep 'SessionManager::' tests/Unit/SessionManagerTest.php` shows direct calls to extracted class
  </acceptance_criteria>
  <done>SessionManagerTest.php passes with 7+ test methods covering timeout, expiry, and reset behaviors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RbacEngineTest with role and permission tests</name>
  <files>tests/Unit/RbacEngineTest.php</files>
  <read_first>
    - app/Core/Security/RbacEngine.php (the class under test — read full file for exact method signatures)
    - app/Core/Security/Permissions.php (HIERARCHY, PERMISSIONS, TRANSITIONS constants)
    - tests/Unit/AuthMiddlewareTest.php (pattern reference — how existing role tests work)
  </read_first>
  <behavior>
    - Test: normalizeRole resolves aliases ('trust' -> 'assessor', 'readonly' -> 'viewer')
    - Test: normalizeRole passes through valid roles unchanged ('admin' -> 'admin')
    - Test: isMeetingRole returns true for 'president', 'assessor', 'voter'
    - Test: isSystemRole returns true for 'admin', 'operator', 'auditor', 'viewer'
    - Test: checkRole returns true when user's role matches required role
    - Test: checkRole returns true when user has higher hierarchy role than required
    - Test: checkRole returns false when user has insufficient role
    - Test: can returns true for permission granted to user's role
    - Test: can returns false for permission not in user's role
    - Test: canTransition returns true for allowed status transition
    - Test: canTransition returns false for disallowed transition
    - Test: availableTransitions returns correct list for given role and status
    - Test: getRoleLevel returns correct hierarchy value from Permissions::HIERARCHY
    - Test: isRoleAtLeast returns true when role >= minimum
    - Test: isRoleAtLeast returns false when role < minimum
    - Test: reset clears meeting context state
  </behavior>
  <action>
    Create `tests/Unit/RbacEngineTest.php` in namespace `Tests\Unit`.

    - `declare(strict_types=1);`
    - `use PHPUnit\Framework\TestCase;`
    - `use AgVote\Core\Security\RbacEngine;`
    - `use AgVote\Core\Security\Permissions;`
    - Class: `final class RbacEngineTest extends TestCase`
    - `protected function tearDown(): void { RbacEngine::reset(); }` — clean static state between tests

    For role/permission tests, construct mock user arrays directly:
    ```php
    $adminUser = ['id' => 'uuid-1', 'role' => 'admin', 'tenant_id' => 'tenant-1', 'is_active' => true];
    $voterUser = ['id' => 'uuid-2', 'role' => 'viewer', 'tenant_id' => 'tenant-1', 'is_active' => true];
    ```

    Pass these user arrays to RbacEngine methods — no need for AuthMiddleware or DB. This proves RbacEngine is testable in isolation.

    For canTransition/availableTransitions, use status values from Permissions::TRANSITIONS ('setup', 'running', 'closed', etc.) and roles from Permissions::HIERARCHY.

    Run: `timeout 60 php vendor/bin/phpunit tests/Unit/RbacEngineTest.php --no-coverage`

    Then run full suite to confirm no regressions:
    `timeout 60 php vendor/bin/phpunit tests/Unit/AuthMiddlewareTest.php tests/Unit/AuthMiddlewareTimeoutTest.php tests/Unit/SessionManagerTest.php tests/Unit/RbacEngineTest.php --no-coverage`
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && timeout 60 php vendor/bin/phpunit tests/Unit/RbacEngineTest.php --no-coverage 2>&1 | tail -5 && timeout 60 php vendor/bin/phpunit tests/Unit/AuthMiddlewareTest.php tests/Unit/AuthMiddlewareTimeoutTest.php --no-coverage 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `php -l tests/Unit/RbacEngineTest.php` passes
    - `timeout 60 php vendor/bin/phpunit tests/Unit/RbacEngineTest.php --no-coverage` shows 0 failures
    - `grep -c 'function test' tests/Unit/RbacEngineTest.php` shows at least 12
    - `grep 'RbacEngine::' tests/Unit/RbacEngineTest.php` shows direct calls (isolation proof)
    - Existing AuthMiddleware tests still pass (regression check)
  </acceptance_criteria>
  <done>RbacEngineTest.php passes with 12+ test methods covering roles, permissions, transitions, hierarchy; all 25 existing AuthMiddleware tests still pass</done>
</task>

</tasks>

<verification>
1. `timeout 60 php vendor/bin/phpunit tests/Unit/SessionManagerTest.php tests/Unit/RbacEngineTest.php --no-coverage` — all new tests pass
2. `timeout 60 php vendor/bin/phpunit tests/Unit/AuthMiddlewareTest.php tests/Unit/AuthMiddlewareTimeoutTest.php --no-coverage` — all 25 existing tests still pass
3. `grep -c 'function test' tests/Unit/SessionManagerTest.php` >= 7
4. `grep -c 'function test' tests/Unit/RbacEngineTest.php` >= 12
</verification>

<success_criteria>
- SessionManagerTest has 7+ test methods, all passing
- RbacEngineTest has 12+ test methods, all passing
- Both test files call extracted classes directly (not through AuthMiddleware) — isolation proof
- All 25 existing AuthMiddleware tests still pass (regression guard)
</success_criteria>

<output>
After completion, create `.planning/phases/02-refactoring-authmiddleware/02-02-SUMMARY.md`
</output>
