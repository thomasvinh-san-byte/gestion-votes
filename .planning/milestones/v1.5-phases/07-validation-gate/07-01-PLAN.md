---
phase: 07-validation-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [GUARD-01, GUARD-02, GUARD-03]

must_haves:
  truths:
    - "routes.php is unchanged since pre-v1.5 baseline (commit 2283062f)"
    - "PHPUnit full suite passes with zero failures and zero errors"
    - "Playwright chromium suite passes with zero failures"
  artifacts: []
  key_links: []
---

<objective>
Validate the v1.5 milestone introduced zero regressions across routes, unit tests, and E2E tests.

Purpose: Confirm all refactoring work (Phases 1-6) preserved existing behavior.
Output: Validation results confirming GUARD-01, GUARD-02, GUARD-03 are satisfied.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-validation-gate/07-CONTEXT.md

<interfaces>
Pre-v1.5 baseline commit: 2283062f (v1.4 milestone completion)
Route file: app/routes.php
PHPUnit config: phpunit.xml
Playwright config: playwright.config.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate route stability (GUARD-01)</name>
  <files>app/routes.php</files>
  <read_first>
    - app/routes.php (current state)
  </read_first>
  <action>
Run `git diff 2283062f..HEAD -- app/routes.php` to confirm zero changes to public URLs since the pre-v1.5 baseline.

If the diff is empty: GUARD-01 is satisfied.
If changes are found: analyze whether they are URL changes (regression) or non-URL changes (formatting, comments). Report findings.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && git diff 2283062f..HEAD -- app/routes.php | wc -l | awk '{if ($1 == 0) print "GUARD-01: PASS — zero route changes"; else print "GUARD-01: FAIL — "$1" lines changed"}'</automated>
  </verify>
  <acceptance_criteria>
    - `git diff 2283062f..HEAD -- app/routes.php` produces empty output (zero changes)
  </acceptance_criteria>
  <done>GUARD-01 validated: routes.php unchanged since v1.4</done>
</task>

<task type="auto">
  <name>Task 2: Validate PHPUnit suite (GUARD-02)</name>
  <files>phpunit.xml</files>
  <read_first>
    - phpunit.xml (test configuration)
  </read_first>
  <action>
Run `timeout 120 php vendor/bin/phpunit --no-coverage` to execute the full PHPUnit suite.

Expected: zero failures, zero errors. Skipped tests are acceptable (they represent known limitations documented in prior phases).

If failures are found: identify which test(s) failed, which service they test, and whether the failure is related to v1.5 refactoring.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && timeout 120 php vendor/bin/phpunit --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PHPUnit output contains "OK" (possibly with "but some tests were skipped")
    - Zero "Failures:" and zero "Errors:" in output
  </acceptance_criteria>
  <done>GUARD-02 validated: PHPUnit suite passes with zero failures</done>
</task>

<task type="auto">
  <name>Task 3: Validate Playwright chromium suite (GUARD-03)</name>
  <files>playwright.config.ts</files>
  <read_first>
    - playwright.config.ts (test configuration)
  </read_first>
  <action>
Run `npx playwright test --project=chromium` to execute the full Playwright E2E suite against the chromium browser.

Expected: zero failures. All existing specs should pass.

If failures are found: identify which spec(s) failed, capture error messages, and determine whether the failure is related to v1.5 refactoring or a pre-existing flake.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && npx playwright test --project=chromium 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Playwright output shows all tests passed (e.g., "X passed")
    - Zero "failed" in the summary line
  </acceptance_criteria>
  <done>GUARD-03 validated: Playwright chromium suite passes with zero failures</done>
</task>

</tasks>

<verification>
1. `git diff 2283062f..HEAD -- app/routes.php | wc -l` shows 0 (GUARD-01)
2. `timeout 120 php vendor/bin/phpunit --no-coverage` shows OK (GUARD-02)
3. `npx playwright test --project=chromium` shows all passed (GUARD-03)
</verification>

<success_criteria>
- GUARD-01: Zero route changes since v1.4 baseline
- GUARD-02: PHPUnit full suite green (zero failures, zero errors)
- GUARD-03: Playwright chromium green (zero failures)
</success_criteria>

<output>
After completion, create `.planning/phases/07-validation-gate/07-01-SUMMARY.md`
</output>
