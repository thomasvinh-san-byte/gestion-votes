---
phase: 34-quality-assurance-final-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/app.css
  - public/assets/css/report.css
  - public/assets/css/admin.css
  - public/assets/css/dashboard.css
  - public/partials/sidebar.html
  - public/dashboard.htmx.html
  - public/archives.htmx.html
  - public/report.htmx.html
  - public/admin.htmx.html
  - public/email-templates.htmx.html
  - public/audit.htmx.html
  - public/validate.htmx.html
  - public/analytics.htmx.html
  - public/meetings.htmx.html
  - public/users.htmx.html
  - public/help.htmx.html
  - public/docs.htmx.html
  - public/trust.htmx.html
  - public/members.htmx.html
  - public/settings.htmx.html
  - public/hub.htmx.html
  - public/operator.htmx.html
  - public/wizard.htmx.html
  - public/postsession.htmx.html
autonomous: true
requirements: [QA-05]

must_haves:
  truths:
    - "Zero layout/typography style='' attributes remain in production HTML — only JS-functional display:none toggles are permitted"
    - "Mobile footer logo, logo-mark, spacer, and link styles are CSS-driven from app.css — not inline on every page"
    - "Onboarding tip lists use a CSS class instead of repeated inline margin/padding/font styles"
  artifacts:
    - path: "public/assets/css/app.css"
      provides: "Mobile footer CSS rules (.mobile-footer .logo, .mobile-footer .logo-mark, .mobile-footer-spacer, .mobile-footer-link), onboarding-tips class"
      contains: "mobile-footer"
    - path: "public/assets/css/report.css"
      provides: "CSS rules for .pv-preview and .pv-empty-state replacing inline styles"
      contains: "pv-empty-state"
    - path: "public/assets/css/admin.css"
      provides: "CSS variant classes for .dash-kpi-icon colors"
      contains: "dash-kpi-icon"
  key_links:
    - from: "app.css mobile-footer rules"
      to: "All 15 .htmx.html files with mobile footer"
      via: "CSS class selectors replace inline styles"
      pattern: "mobile-footer"
    - from: "app.css .onboarding-tips class"
      to: "8 .htmx.html files with onboarding <ul>"
      via: "CSS class replaces inline style"
      pattern: "onboarding-tips"
---

<objective>
Remove all layout/typography inline style="" attributes from production HTML files by moving styles to CSS classes (QA-05).

Purpose: Zero inline styles in production HTML is the QA-05 shipping gate. The mobile footer pattern alone accounts for 60+ inline style attributes across 15 pages.
Output: All .htmx.html and sidebar.html files pass an inline-style audit. CSS classes in app.css, report.css, and admin.css absorb the moved styles.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-quality-assurance-final-audit/34-CONTEXT.md
@.planning/phases/34-quality-assurance-final-audit/34-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CSS classes for shared inline patterns and fix sidebar</name>
  <files>
    public/assets/css/app.css,
    public/assets/css/report.css,
    public/assets/css/admin.css,
    public/partials/sidebar.html
  </files>
  <read_first>
    public/assets/css/app.css (lines 295-310, 410-420 for skeleton-w-* classes),
    public/assets/css/report.css (full file — likely small),
    public/assets/css/admin.css (lines 300-310),
    public/partials/sidebar.html (lines 1-20)
  </read_first>
  <action>
**Step 1: Add mobile footer CSS rules to app.css**

Add these rules at the end of app.css (before any @media print block if one exists):

```css
/* ── Mobile footer overrides ── */
.mobile-footer .logo {
  font-size: 12px;
  gap: 6px;
}
.mobile-footer .logo-mark {
  width: 18px;
  height: 18px;
  border-radius: 3px;
  font-size: 8px;
}
.mobile-footer-spacer {
  flex: 1;
}
.mobile-footer-link {
  text-decoration: none;
}
```

**Step 2: Add onboarding tip list CSS class to app.css**

```css
/* ── Onboarding tip list ── */
.onboarding-tips {
  margin: 0.5rem 0 0;
  padding-left: 1.25rem;
  font-size: 0.8125rem;
  color: var(--color-text-muted);
  line-height: 1.6;
}
```

**Step 3: Add report.css classes for pv-preview and pv-empty-state**

Add to report.css:

```css
/* ── PV Preview panel ── */
.pv-preview {
  position: relative;
  min-height: 200px;
}
.pv-empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 3rem;
  color: var(--color-text-muted);
  text-align: center;
}
.pv-empty-state-icon {
  opacity: 0.5;
  margin-bottom: 1rem;
}
.pv-empty-state-title {
  font-weight: 600;
  font-size: 14px;
}
.pv-empty-state-desc {
  font-size: 13px;
  margin-top: 4px;
}
.pv-empty-state .skeleton {
  margin: 0 auto 0.5rem;
}
.pv-empty-state .skeleton:last-child {
  margin-bottom: 0;
}
```

**Step 4: Add admin.css variant classes for dash-kpi-icon**

Add to admin.css:

```css
/* ── KPI icon color variants ── */
.dash-kpi-icon.kpi-primary {
  background: var(--color-primary-subtle);
  color: var(--color-primary);
}
.dash-kpi-icon.kpi-danger {
  background: var(--color-danger-subtle);
  color: var(--color-danger);
}
.dash-kpi-icon.kpi-warning {
  background: var(--color-warning-subtle);
  color: var(--color-warning);
}
.dash-kpi-icon.kpi-success {
  background: var(--color-success-subtle);
  color: var(--color-success);
}
```

**Step 5: Fix sidebar.html inline styles**

In `public/partials/sidebar.html`:

Line 8: Remove `style="height:48px;padding:0 8px 0 14px;margin-bottom:4px;"` from the `.nav-item` link. Add class `nav-brand` instead.

Line 10: Remove `style="width:22px;height:22px;border-radius:4px;font-size:10px;"` from `.logo-mark`. The sidebar logo-mark gets its own CSS rule.

Line 14: Remove `style="font-family:var(--font-display);font-size:15px;font-weight:700;letter-spacing:-.2px;color:#fff;"`. Add class `nav-brand-label` instead.

Add to app.css:

```css
/* ── Sidebar brand ── */
.nav-brand {
  height: 48px;
  padding: 0 8px 0 14px;
  margin-bottom: 4px;
}
.nav-brand .logo-mark {
  width: 22px;
  height: 22px;
  border-radius: 4px;
  font-size: 10px;
}
.nav-brand-label {
  font-family: var(--font-display);
  font-size: 15px;
  font-weight: 700;
  letter-spacing: -0.2px;
  color: #fff;
}
```

Note: The `color: #fff` on `.nav-brand-label` is acceptable — this is the sidebar brand wordmark which sits on a dark sidebar background in both light and dark modes. It is a brand element, not a content element.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && echo "=== CSS classes added ===" && grep -c "mobile-footer" public/assets/css/app.css && grep -c "onboarding-tips" public/assets/css/app.css && grep -c "pv-empty-state" public/assets/css/report.css && grep -c "kpi-primary" public/assets/css/admin.css && grep -c "nav-brand" public/assets/css/app.css && echo "=== Sidebar inline styles ===" && grep 'style=' public/partials/sidebar.html && echo "FAIL: sidebar still has inline styles" || echo "PASS: sidebar clean"</automated>
  </verify>
  <acceptance_criteria>
    - grep "mobile-footer" public/assets/css/app.css returns 4+ matches (logo, logo-mark, spacer, link)
    - grep "onboarding-tips" public/assets/css/app.css returns 1+ match
    - grep "pv-empty-state" public/assets/css/report.css returns 1+ match
    - grep "kpi-primary" public/assets/css/admin.css returns 1+ match
    - grep 'style=' public/partials/sidebar.html returns zero matches
    - grep "nav-brand" public/assets/css/app.css returns 2+ matches
  </acceptance_criteria>
  <done>CSS classes created for mobile footer (4 rules), onboarding tips (1 rule), pv-preview/empty-state (5 rules), admin KPI icon variants (4 rules), sidebar brand (3 rules). Sidebar.html inline styles removed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove inline styles from all HTML files using the new CSS classes</name>
  <files>
    public/dashboard.htmx.html,
    public/archives.htmx.html,
    public/report.htmx.html,
    public/admin.htmx.html,
    public/email-templates.htmx.html,
    public/audit.htmx.html,
    public/validate.htmx.html,
    public/analytics.htmx.html,
    public/meetings.htmx.html,
    public/users.htmx.html,
    public/help.htmx.html,
    public/docs.htmx.html,
    public/trust.htmx.html,
    public/members.htmx.html,
    public/settings.htmx.html,
    public/hub.htmx.html,
    public/operator.htmx.html,
    public/wizard.htmx.html,
    public/postsession.htmx.html
  </files>
  <read_first>
    public/dashboard.htmx.html (lines 50-60, 118-170, 190-205),
    public/archives.htmx.html (lines 115-165),
    public/report.htmx.html (lines 100-115, 195-210),
    public/admin.htmx.html (lines 89-115, 128-160, 1120-1140)
  </read_first>
  <action>
**Pattern A: Mobile footer — apply to ALL 15 files that have the footer pattern**

Files: email-templates, dashboard, archives, audit, validate, analytics, meetings, users, report, help, docs, trust, members, admin, settings

For each file, find the mobile footer block (search for `class="logo" style="font-size:12px`) and make these changes:

1. On the `<a class="logo" style="font-size:12px;gap:6px;">` element: Remove the `style` attribute entirely (CSS class `.mobile-footer .logo` handles it).

2. On the `<span class="logo-mark" style="width:18px;height:18px;border-radius:3px;font-size:8px;">` element: Remove the `style` attribute (CSS class `.mobile-footer .logo-mark` handles it).

3. On `<span style="flex:1"></span>`: Replace with `<span class="mobile-footer-spacer"></span>`.

4. On `<a ... style="text-decoration:none;">Aide</a>` and `<a ... style="text-decoration:none;">Accessibilite</a>`: Remove `style="text-decoration:none;"` and add class `mobile-footer-link` (so: `class="text-muted mobile-footer-link"`).

**IMPORTANT:** Verify each file's footer is wrapped in an element with class `mobile-footer`. If not, add `class="mobile-footer"` to the parent `<footer>` or wrapping div. The CSS selectors depend on this parent class.

**Pattern B: Onboarding tip lists — apply to 8 files**

Files: dashboard, hub, wizard, meetings, members, operator, analytics, postsession

Find: `<ul style="margin:.5rem 0 0;padding-left:1.25rem;font-size:.8125rem;color:var(--color-text-muted);line-height:1.6">`
Replace with: `<ul class="onboarding-tips">`

**Pattern C: Dashboard-specific inline styles**

In dashboard.htmx.html:
- Line 121: `style="margin-bottom: var(--space-card);"` on `.card.dashboard-panel` — remove style, add this rule to dashboard page CSS or use the gap pattern on parent. If no dashboard.css exists, add a `<style>` block or use app.css. Better: if the parent has `display:flex;flex-direction:column`, add `gap: var(--space-card)` to parent instead.
- Line 158: `style="margin-bottom: var(--space-card);"` on `.card-title` — remove, use CSS class `.dashboard-shortcuts-title { margin-bottom: var(--space-card); }`.
- Lines 159, 168: `style="margin-bottom: var(--space-3);"` on `.shortcut-card` — remove, add `.shortcut-card { margin-bottom: var(--space-3); }` to dashboard section of app.css or create `.dashboard-shortcuts .card + .card { margin-top: var(--space-3); }`.

**Pattern D: Archives inline styles**

In archives.htmx.html:
- Line 118: `style="padding: var(--space-4) var(--space-card);"` on `#archiveStats` — remove, add `.archive-stats { padding: var(--space-4) var(--space-card); }` to archives.css.
- Line 136: `style="padding: 0 var(--space-card);"` on `#archivesList` — remove, add `#archivesList { padding: 0 var(--space-card); }` to archives.css.

**Pattern E: Report inline styles**

In report.htmx.html:
- Line 102: Remove `style="position:relative;min-height:200px;"` (now `.pv-preview` class from Task 1).
- Line 103: Remove entire `style="display:flex;flex-direction:column;..."` (now `.pv-empty-state` class from Task 1).
- Line 104: Add `class="pv-empty-state-icon"` to the SVG, remove `style="opacity:.5;margin-bottom:1rem;"`.
- Line 105: Remove `style="font-weight:600;font-size:14px;"`, add `class="pv-empty-state-title"`.
- Line 106: Remove `style="font-size:13px;margin-top:4px;"`, add `class="pv-empty-state-desc"`.
- Lines 109-111: Remove `style="margin:0 auto 0.5rem"` and `style="margin:0 auto"` from skeleton divs (now handled by `.pv-empty-state .skeleton` rule from Task 1).

**Pattern F: Admin inline styles**

In admin.htmx.html:
- Lines 92, 99, 106, 113: Replace `style="background: var(--color-X-subtle); color: var(--color-X);"` with variant classes:
  - Line 92 (primary): Add class `kpi-primary`, remove style
  - Line 99 (danger): Add class `kpi-danger`, remove style
  - Line 106 (warning): Add class `kpi-warning`, remove style
  - Line 113 (success): Add class `kpi-success`, remove style

- Lines 131-158 (skeleton widths): Replace `style="width:60%"` with `class="skeleton-w-60"`, etc. Map: 60%->skeleton-w-60, 40%->skeleton-w-40, 50%->skeleton-w-50, 35%->skeleton-w-40 (closest), 55%->skeleton-w-60 (closest), 30%->skeleton-w-40 (closest), 65%->skeleton-w-60 (closest), 45%->skeleton-w-50 (closest). For margin-top:4px on second lines, add a `.skeleton-line + .skeleton-line { margin-top: 4px; }` rule to admin.css.

- Line 196: `style="border-color: var(--color-border);"` on `<hr>` — this should already be the default from design-system.css. Check if it is; if so, just remove the style. If not, add `hr { border-color: var(--color-border); }` to design-system.css.

- Line 538: `style="display:flex;align-items:center;gap:0.5rem"` on `.alert.alert-warning` — check if alert already has flex layout from design-system.css. If not, add to admin.css: `.alert-inline { display:flex; align-items:center; gap:0.5rem; }` and add class `alert-inline`.

**Acceptable exceptions (DO NOT remove):**
- All `style="display:none"` / `style="display: none;"` — JS show/hide toggles
- `public.htmx.html` progress bars with `style="width: 0%"` — JS-animated
- `analytics.htmx.html` donut legend dots with `style="background:var(--color-X)"` — these are data-driven dots, acceptable as inline
- `settings.htmx.html` text-size labels with `style="font-size:1.15em"` / `style="font-size:1.3em"` — these are demonstration labels showing the actual text sizes, inline is semantically correct
- `meetings.htmx.html` line 75 `style="position: relative;"` on onboarding banner — check if already in CSS; if not, move to CSS
- Email template PHP files (`app/Templates/*.php`) — email clients require inline CSS
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && echo "=== Layout inline styles remaining ===" && grep -rn 'style="' public/*.htmx.html public/partials/*.html | grep -v 'display:none\|display: none\|width: 0%\|width:0%' | grep -v 'font-size:1\.\(15\|3\)em' | grep -v 'background:var(--color-' | grep -c '[a-z]' && echo "(count of remaining style= attributes — target: 0 or very few JS-functional ones)"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'style="font-size:12px' public/*.htmx.html returns zero matches (mobile footer fixed)
    - grep 'style="width:18px' public/*.htmx.html public/partials/*.html returns zero matches (logo-mark fixed)
    - grep 'style="flex:1"' public/*.htmx.html returns zero matches (spacer fixed)
    - grep 'style="text-decoration:none' public/*.htmx.html returns zero matches (footer links fixed)
    - grep 'style="margin:\.5rem' public/*.htmx.html returns zero matches (onboarding tips fixed)
    - grep 'style=' public/report.htmx.html | grep -v 'display:none' returns zero matches
    - grep 'style="background:' public/admin.htmx.html returns zero matches (KPI icons fixed)
    - grep 'style="width:' public/admin.htmx.html | grep -v 'display' returns zero matches (skeletons fixed)
  </acceptance_criteria>
  <done>All layout/typography inline styles removed from 19 HTML files. Mobile footer pattern (15 pages, 60+ attributes) replaced with CSS classes. Onboarding tips (8 pages) replaced with CSS class. Report pv-preview, admin KPI/skeletons, dashboard margins, archives padding all moved to CSS.</done>
</task>

</tasks>

<verification>
After both tasks:
1. Run full inline-style audit: `grep -rn 'style="' public/*.htmx.html public/partials/*.html | grep -v 'display:none\|display: none\|width: 0%\|width:0%' | grep -v 'font-size:1\.\(15\|3\)em' | grep -v 'background:var(--color-'`
   Should return zero or near-zero results (only JS-functional toggles and acceptable data-driven styles)
2. Verify CSS classes exist: `grep -c "mobile-footer\|onboarding-tips\|pv-empty-state\|kpi-primary\|nav-brand" public/assets/css/app.css public/assets/css/report.css public/assets/css/admin.css`
</verification>

<success_criteria>
- Zero layout/typography inline styles in production HTML (only JS display:none, progress bar widths, and data-driven backgrounds remain)
- 5 CSS class families created: mobile-footer (4 rules), onboarding-tips (1 rule), pv-preview/empty-state (6 rules), admin KPI variants (4 rules), sidebar brand (3 rules)
- 19 HTML files cleaned up
</success_criteria>

<output>
After completion, create `.planning/phases/34-quality-assurance-final-audit/34-02-SUMMARY.md`
</output>
