---
phase: 31-component-refresh
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
autonomous: true
requirements: [CMP-01, CMP-02, CMP-03, CMP-04, CMP-05, CMP-06, CMP-07, CMP-08]

must_haves:
  truths:
    - "Every component type has a visually distinct border-radius — buttons 6px, cards 12px, badges pill, modals 12px, toasts 8px"
    - "Cards show shadow-sm at rest, shadow-md on hover with border-color change"
    - "Clickable cards lift translateY(-1px) on hover — buttons do NOT lift"
    - "All focusable components use the same double-ring focus pattern via var(--shadow-focus)"
    - "Tables have 48px rows, 40px headers, and a .col-num utility for right-aligned monospace numbers"
    - "Form inputs are 36px height with double-ring focus and red border on error"
    - "Toasts use inset box-shadow for left accent stripe instead of border-left"
    - "Stepper CSS uses connector-line pattern with 28px circles"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "All 8 component CSS specs updated with token-based values"
      contains: "--toast-width"
  key_links:
    - from: "design-system.css :root COMPONENT ALIASES"
      to: "design-system.css @layer components"
      via: "var(--token) references in component classes"
      pattern: "var\\(--btn-height|--input-height|--toast-width|--stepper-dot-size\\)"
---

<objective>
Update all 8 component CSS class specs in design-system.css to use Phase 30 tokens correctly, add missing component alias tokens, apply the locked radius/hover/focus decisions, and ensure every component type is visually differentiated.

Purpose: Establish the canonical CSS spec for each component type before Web Component Shadow DOM reconciliation in plan 02.
Output: Updated design-system.css with correct component classes for buttons, cards, tables, forms, modals, toasts, badges, and steppers.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-component-refresh/31-CONTEXT.md
@.planning/phases/31-component-refresh/31-RESEARCH.md
@.planning/phases/30-token-foundation/30-04-SUMMARY.md

<interfaces>
<!-- Current token system in design-system.css :root -->

COMPONENT ALIASES (already exist from Phase 30):
  --radius-btn:     var(--radius-md);   /* 6px */
  --radius-input:   var(--radius-md);   /* 6px */
  --radius-badge:   var(--radius-sm);   /* 4px — NOTE: badges use --radius-full per CONTEXT.md */
  --radius-card:    var(--radius-xl);   /* 12px */
  --radius-modal:   var(--radius-xl);   /* 12px */
  --radius-toast:   var(--radius-lg);   /* 8px */

SEMANTIC tokens (already exist):
  --shadow-focus: 0 0 0 2px var(--color-surface-raised), 0 0 0 4px var(--color-border-focus);
  --shadow-focus-danger: 0 0 0 2px var(--color-surface-raised), 0 0 0 4px rgba(196, 40, 40, 0.35);
  --shadow-sm, --shadow-md, --shadow-lg, --shadow-xl (all exist)
  --space-4: 1rem (16px), --space-8: 2rem (32px), --space-10: 2.5rem (40px), --space-12: 3rem (48px)
  --duration-fast, --ease-default, --transition-ui (all exist)
  --font-mono: 'JetBrains Mono' (exists)
  --text-xs, --text-sm, --text-base (exist)

NOTE: --space-9 (36px) does NOT exist. Scale jumps --space-8 (32px) to --space-10 (40px).
NOTE: --radius-badge is currently var(--radius-sm) = 4px, but CONTEXT.md locks badges at var(--radius-full) (pill). The alias needs updating.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add component alias tokens and update buttons, cards, tables, forms</name>
  <files>public/assets/css/design-system.css</files>
  <read_first>
    - public/assets/css/design-system.css (lines 520-560 for COMPONENT ALIASES, lines 1388-1870 for buttons/cards/forms/tables)
    - .planning/phases/31-component-refresh/31-CONTEXT.md (locked decisions)
    - .planning/phases/31-component-refresh/31-RESEARCH.md (exact changes needed)
  </read_first>
  <action>
**Step 1: Add new component alias tokens to :root COMPONENT ALIASES section (after line ~536)**

Add these tokens after the existing radius aliases block:

```css
  /* Component dimension aliases */
  --btn-height: 36px;
  --input-height: 36px;
  --toast-width: 356px;
  --toast-accent-width: 3px;
  --stepper-dot-size: 28px;
  --stepper-connector-height: 2px;
```

Also fix `--radius-badge` from `var(--radius-sm)` to `var(--radius-full)` (pill shape per locked CONTEXT.md decision).

**Step 2: Update .btn base (line ~1390)**

Change these properties on `.btn`:
- `min-height: 40px` -> `min-height: var(--btn-height)` (36px)
- `border-radius: var(--radius-md)` -> `border-radius: var(--radius-btn)` (still 6px, but via component alias)
- `font-weight: var(--font-bold)` -> `font-weight: var(--font-semibold)` (600 — shadcn standard)
- `transition` value: replace literal `.18s` references with `var(--duration-fast) var(--ease-default)` for each property

**Step 3: Update .btn:focus-visible (line ~1410)**

Replace the current formula:
```css
box-shadow: 0 0 0 var(--ring-offset) var(--color-surface),
            0 0 0 calc(var(--ring-offset) + var(--ring-width)) var(--ring-color);
```
With:
```css
box-shadow: var(--shadow-focus);
```

**Step 4: Remove translateY(-1px) from button hover variants**

Per locked decision: "translateY(-1px) lift reserved for clickable cards only — not buttons".

On `.btn-primary:hover:not(:disabled)` (line ~1435):
- Remove `transform: translateY(-1px);`
- Replace hardcoded `box-shadow: inset 0 1px 0 rgba(255,255,255,.15), 0 4px 8px rgba(22,80,224,.35)` with `box-shadow: inset 0 1px 0 rgba(255,255,255,.15), var(--shadow-md)`

On `.btn-success:hover:not(:disabled)` (line ~1465):
- Remove `transform: translateY(-1px);`
- Replace hardcoded box-shadow with `box-shadow: inset 0 1px 0 rgba(255,255,255,.15), var(--shadow-md)`

On `.btn-danger:hover:not(:disabled)` (line ~1483):
- Remove `transform: translateY(-1px);`
- Replace hardcoded box-shadow with `box-shadow: inset 0 1px 0 rgba(255,255,255,.15), var(--shadow-md)`

On `.btn-primary:active`, `.btn-success:active`, `.btn-danger:active` — keep `scale(.98) translateY(0)` as-is (active press feedback is fine).

**Step 5: Update button size variants to use --radius-btn**

- `.btn-sm` (line ~1534): `border-radius: var(--radius)` -> `border-radius: var(--radius-btn)`
- `.btn-lg` (line ~1540): `border-radius: var(--radius-lg)` -> keep as-is (large buttons get larger radius)
- `.btn-xl` (line ~1546): keep as-is

**Step 6: Update .card (line ~1582)**

- `border-radius: var(--radius-lg)` -> `border-radius: var(--radius-card)` (12px via component alias)
- `transition: box-shadow .18s ease, ...` -> `transition: box-shadow var(--duration-fast) var(--ease-default), border-color var(--duration-fast) var(--ease-default), transform var(--duration-fast) var(--ease-default), background var(--duration-fast) var(--ease-default)`

On `.card:hover` (line ~1590):
- Add `border-color: var(--color-border-strong);` (per locked decision: card hover shows lighter border)

On `.card-clickable:hover` (line ~1598):
- `transform: translateY(-2px)` -> `transform: translateY(-1px)` (locked: -1px not -2px)

On `.card-footer` (line ~1613):
- `border-radius: 0 0 var(--radius-lg) var(--radius-lg)` -> `border-radius: 0 0 var(--radius-card) var(--radius-card)`

On `.card-title` (line ~1620):
- `font-size: 13px` -> `font-size: var(--text-sm)` (use token, not literal)
- `font-weight: 700` -> `font-weight: var(--font-bold)` (use token)

**Step 7: Update form inputs (line ~1706)**

On `.form-input, .form-select, .form-textarea`:
- `border-radius: var(--radius-md)` -> `border-radius: var(--radius-input)` (still 6px, via alias)
- `min-height: 42px` -> `min-height: var(--input-height)` (36px)

On `.form-label` (line ~1693):
- `font-size: 13px` -> `font-size: var(--text-sm)`
- `font-weight: 700` -> `font-weight: var(--font-semibold)` (600 per CMP-04: "label at 14px/600")

On `.form-input:focus, .form-select:focus, .form-textarea:focus` (line ~1737):
- Replace `box-shadow: 0 0 0 3px var(--ring-color)` with `box-shadow: var(--shadow-focus)`

On `.form-input.is-error:focus, .form-select.is-error:focus` (line ~1756):
- Replace `box-shadow: 0 0 0 3px rgba(220, 38, 38, 0.15)` with `box-shadow: var(--shadow-focus-danger)`

On `.form-input.is-invalid:focus, .form-select.is-invalid:focus` (line ~1769):
- Replace `box-shadow: 0 0 0 3px rgba(220, 38, 38, 0.15)` with `box-shadow: var(--shadow-focus-danger)`

On `.form-input.is-valid:focus, .form-select.is-valid:focus` (line ~1781):
- Replace `box-shadow: 0 0 0 3px rgba(22, 163, 74, 0.15)` with `box-shadow: var(--shadow-focus)` (valid = standard focus ring, not green)

**Step 8: Update tables (line ~1825)**

On `.table th` (line ~1838):
- Add `height: var(--space-10);` (40px)
- `font-size` is inherited from `.table` which has `var(--text-sm)` — keep

On `.table td` (line ~1851):
- Add `height: var(--space-12);` (48px)

On `.table tbody tr:nth-child(even) td` (line ~1864):
- Replace `background: rgba(21,21,16,.022)` with `background: color-mix(in srgb, var(--color-text) 2%, transparent)` (token-based, dark-mode compatible)

Add new utility class after the table section (after line ~1870):
```css
.col-num {
  text-align: right;
  font-family: var(--font-mono);
  font-size: var(--text-sm);
  font-variant-numeric: tabular-nums;
}
```
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "var(--radius-card)" public/assets/css/design-system.css | grep -q "[3-9]" && grep -c "var(--shadow-focus)" public/assets/css/design-system.css | grep -q "[4-9]" && grep -c "var(--btn-height)" public/assets/css/design-system.css | grep -q "[1-9]" && grep -c "var(--input-height)" public/assets/css/design-system.css | grep -q "[1-9]" && grep "col-num" public/assets/css/design-system.css | grep -q "text-align: right" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "--btn-height: 36px" design-system.css returns a match (new token exists)
    - grep "--input-height: 36px" design-system.css returns a match
    - grep "--toast-width: 356px" design-system.css returns a match
    - grep "--stepper-dot-size: 28px" design-system.css returns a match
    - grep "radius-badge.*radius-full" design-system.css returns a match (badge is now pill)
    - grep "min-height: var(--btn-height)" design-system.css returns a match on .btn
    - grep "var(--radius-card)" design-system.css returns 3+ matches (.card, .card-footer, .card-clickable)
    - grep "translateY(-1px)" design-system.css returns matches ONLY for .card-clickable:hover, NOT for any .btn variant
    - grep "var(--shadow-focus)" design-system.css returns 4+ matches (btn, input, select, textarea focus rules)
    - grep "min-height: 42px" design-system.css returns zero matches (old input height removed)
    - grep "height: var(--space-10)" design-system.css returns a match (table th 40px)
    - grep "height: var(--space-12)" design-system.css returns a match (table td 48px)
    - grep "col-num" design-system.css returns a match with "font-family: var(--font-mono)"
    - grep "font-weight: var(--font-semibold)" in .form-label section exists (600, not 700)
    - grep "rgba(21,21,16" design-system.css returns zero matches (hardcoded table bg removed)
  </acceptance_criteria>
  <done>All 4 component types (buttons, cards, forms, tables) in design-system.css use component alias tokens for radius/height/focus. Buttons do not lift on hover. Cards lift -1px (not -2px). Form inputs are 36px with double-ring focus. Tables have 40px headers and 48px rows. New component tokens exist in :root.</done>
</task>

<task type="auto">
  <name>Task 2: Update modal, toast, badge, stepper CSS in design-system.css</name>
  <files>public/assets/css/design-system.css</files>
  <read_first>
    - public/assets/css/design-system.css (lines 1634-1680 for badges, 1941-2007 for modals, 2063-2141 for toasts, 2508-2562 for steppers)
    - .planning/phases/31-component-refresh/31-CONTEXT.md (locked decisions on radius mapping and hover model)
    - .planning/phases/31-component-refresh/31-RESEARCH.md (toast inset pattern, stepper reconciliation)
  </read_first>
  <action>
**Step 1: Update .badge (line ~1635)**

The CSS `.badge` class currently uses `border-radius: var(--radius-full)` (correct per CONTEXT.md pill shape). Verify it matches. If `--radius-badge` was fixed in Task 1 to `var(--radius-full)`, then update:
- `border-radius: var(--radius-full)` -> `border-radius: var(--radius-badge)` (now resolves to pill via updated alias)

The `.badge` font-size `var(--text-xs)` and font-weight `var(--font-medium)` are already correct. No changes needed to badge variants.

**Step 2: Update .modal-backdrop (line ~1942)**

- `background: rgba(0,0,0,.35)` -> `background: var(--color-backdrop)` (use the token; it resolves to rgba(0,0,0,0.5) — single source of truth)

The `.modal` class (line ~1963) already uses `border-radius: var(--radius-xl)` and `box-shadow: var(--shadow-xl)` — these are correct. No changes needed there.

**Step 3: Update .toast CSS (line ~2075)**

On `.toast`:
- `border-radius: var(--radius-md)` -> `border-radius: var(--radius-toast)` (8px via component alias)
- Add `width: var(--toast-width)` (356px — new token from Task 1)

On `.toast-success` (line ~2088):
- Replace `border-left: 4px solid var(--color-success)` with `box-shadow: var(--shadow-lg), inset var(--toast-accent-width) 0 0 var(--color-success)` (inset box-shadow for proper radius behavior)

On `.toast-warning` (line ~2092):
- Replace `border-left: 4px solid var(--color-warning)` with `box-shadow: var(--shadow-lg), inset var(--toast-accent-width) 0 0 var(--color-warning)`

On `.toast-danger` (line ~2096):
- Replace `border-left: 4px solid var(--color-danger)` with `box-shadow: var(--shadow-lg), inset var(--toast-accent-width) 0 0 var(--color-danger)`

On `.toast-info` (line ~2100):
- Replace `border-left: 4px solid var(--color-info)` with `box-shadow: var(--shadow-lg), inset var(--toast-accent-width) 0 0 var(--color-info)`

On `.toast-title` (line ~2113):
- `font-weight: 700` -> `font-weight: var(--font-bold)`
- `font-size: 14px` -> `font-size: var(--text-sm)`

On `.toast-msg` (line ~2119):
- `font-size: 13px` -> `font-size: var(--text-xs)`
- `margin-top: 2px` -> `margin-top: var(--space-0-5)`

On `.toast-close` (line ~2125):
- `padding: 4px` -> `padding: var(--space-1)`
- `transition: color .15s ease, background .15s ease` -> `transition: color var(--duration-fast) var(--ease-default), background var(--duration-fast) var(--ease-default)`

**Step 4: Update stepper CSS to connector-line pattern (line ~2508)**

Replace the entire stepper section (lines ~2508-2561) with the connector-line pattern. `.stepper-item` is NOT used in any PHP/HTML files (confirmed by grep), so it is safe to restyle completely.

New stepper CSS:
```css
/* --- Stepper --- */
.stepper {
  display: flex;
  align-items: center;
  gap: 0;
}

.stepper-item {
  flex: 1;
  display: flex;
  align-items: center;
  gap: var(--space-2);
  padding: var(--space-1) 0;
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
  transition: color var(--duration-fast) var(--ease-default);
}

.stepper-item:not(:last-child)::after {
  content: '';
  flex: 1;
  height: var(--stepper-connector-height);
  background: var(--color-border);
  margin: 0 var(--space-2);
  transition: background var(--duration-fast) var(--ease-default);
}

.stepper-number {
  width: var(--stepper-dot-size);
  height: var(--stepper-dot-size);
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--color-bg-subtle);
  border-radius: var(--radius-full);
  font-weight: var(--font-semibold);
  font-size: var(--text-sm);
  flex-shrink: 0;
  transition: background var(--duration-fast) var(--ease-default), color var(--duration-fast) var(--ease-default);
}

.stepper-item.active {
  color: var(--color-primary);
}

.stepper-item.active .stepper-number {
  background: var(--color-primary);
  color: var(--color-primary-text);
}

.stepper-item.done {
  color: var(--color-success-text);
}

.stepper-item.done .stepper-number {
  background: var(--color-success);
  color: var(--color-text-inverse);
}

.stepper-item.done:not(:last-child)::after {
  background: var(--color-success);
}
```

Note: Remove the old card-box styles (background, border, border-radius on .stepper-item; the background/border on .stepper-item.active and .stepper-item.done). The stepper now uses the industry-standard dot-connector pattern with 28px circles.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep "var(--radius-toast)" public/assets/css/design-system.css | grep -q "border-radius" && grep "var(--toast-width)" public/assets/css/design-system.css | grep -q "width" && grep "inset var(--toast-accent-width)" public/assets/css/design-system.css | grep -q "box-shadow" && grep "var(--stepper-dot-size)" public/assets/css/design-system.css | grep -q "width" && grep "var(--stepper-connector-height)" public/assets/css/design-system.css | grep -q "height" && grep "var(--color-backdrop)" public/assets/css/design-system.css | grep -q "modal-backdrop" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "border-radius: var(--radius-toast)" design-system.css returns a match on .toast
    - grep "width: var(--toast-width)" design-system.css returns a match
    - grep "border-left: 4px solid" design-system.css returns zero matches (toast accents converted to inset box-shadow)
    - grep "inset var(--toast-accent-width)" design-system.css returns 4 matches (success/warning/danger/info)
    - grep "var(--color-backdrop)" design-system.css returns a match in .modal-backdrop
    - grep "rgba(0,0,0,.35)" design-system.css returns zero matches (hardcoded backdrop removed)
    - grep "var(--stepper-dot-size)" design-system.css returns at least 2 matches (width + height)
    - grep "var(--stepper-connector-height)" design-system.css returns a match
    - grep "border: 1px solid var(--color-border)" in stepper section returns zero matches (old card-box removed)
    - grep "font-weight: 700" in toast section returns zero matches (replaced with token)
    - grep "font-size: 14px" in toast section returns zero matches (replaced with token)
    - grep "font-size: 13px" in toast section returns zero matches (replaced with token)
  </acceptance_criteria>
  <done>Modals use var(--color-backdrop) token. Toasts have 356px width, inset box-shadow accent, and tokenized typography. Badges use var(--radius-badge) which resolves to pill. Stepper CSS uses connector-line pattern with 28px dots and connector lines between steps. All hardcoded literals in these 4 component sections are replaced with design-system tokens.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -cP "font-size: \d+px" public/assets/css/design-system.css` — count remaining hardcoded font-size literals in the file (target: significantly reduced from baseline; remaining ones are outside the 8 component sections)
2. `grep -n "translateY(-1px)" public/assets/css/design-system.css` — should only appear on `.card-clickable:hover`, never on `.btn-*`
3. `grep -c "var(--shadow-focus)" public/assets/css/design-system.css` — should be 4+ (buttons + 3 form element focus rules)
4. `grep "border-left:.*solid" public/assets/css/design-system.css` — zero matches in toast section
</verification>

<success_criteria>
- All 8 component CSS specs in design-system.css reference component alias tokens (--radius-btn, --radius-card, --radius-toast, --radius-badge, --btn-height, --input-height, --toast-width, --stepper-dot-size)
- Focus ring is unified via var(--shadow-focus) across buttons, inputs, selects, textareas
- Button hover does not use translateY; card-clickable hover uses translateY(-1px)
- Toast accent uses inset box-shadow, not border-left
- Stepper uses connector-line visual pattern with 28px circles
- No hardcoded px literals remain in the 8 component sections for font-size, font-weight, padding, border-radius, or height
</success_criteria>

<output>
After completion, create `.planning/phases/31-component-refresh/31-01-SUMMARY.md`
</output>
