---
phase: 30-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
autonomous: true
requirements: [TKN-01, TKN-02, TKN-04, TKN-05, TKN-06, TKN-07]

must_haves:
  truths:
    - "design-system.css :root contains clearly separated primitive, semantic, and component-alias sections"
    - "Shadow system has 8 named levels (2xs through 2xl) with warm shadow-color variable and distinct dark mode overrides"
    - "Typography tokens include weight-per-role aliases (page-title=700, section-title=700, card-title=600, body=400)"
    - "Spacing has both raw scale (--space-0 through --space-24) and semantic aliases (--gap-*, --pad-*)"
    - "Border-radius scale has 7 raw values plus 10+ semantic aliases mapping component-type to radius"
    - "Dark mode [data-theme=dark] block overrides only semantic tokens (~30 overrides), not primitives"
    - "Transition system has named durations, easing functions, and composable pre-built transitions"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Complete token system with primitive->semantic->component hierarchy"
      contains: "--shadow-color"
  key_links:
    - from: "public/assets/css/design-system.css :root"
      to: "[data-theme=dark]"
      via: "semantic token overrides"
      pattern: "--shadow-color"
---

<objective>
Rewrite the design-system.css token section from a flat 265+ variable list into a structured primitive -> semantic -> component-alias hierarchy with ~100 semantic tokens, a complete shadow system, spacing aliases, radius aliases, typography weight roles, and transition tokens. Dark mode derives automatically through semantic token overrides.

Purpose: This is the foundation all subsequent phases (31-34) build on. Incorrect or bloated tokens make every downstream component and page fix unreliable.
Output: A restructured design-system.css with clearly organized token sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure :root token block — primitives + semantic colors + spacing + radius + typography + shadows + transitions</name>
  <files>public/assets/css/design-system.css</files>
  <read_first>
    - public/assets/css/design-system.css
    - .planning/research/PITFALLS.md
  </read_first>
  <action>
Rewrite the `:root { ... }` block in design-system.css. Keep the `@layer base, components, v4;` declaration and `@layer base {` wrapper. Keep all the reset CSS and body styles untouched. Replace ONLY the `:root` token block.

The new `:root` block must have these clearly commented sections in this exact order:

**Section 1: PRIMITIVES — Color Palettes (oklch with hex fallback)**

Add the color primitive palette from PITFALLS.md Section 3. Use the double-declaration fallback pattern:

```css
/* ─── PRIMITIVES — Color Palettes ─── */
/* Raw values. NEVER reference these in component CSS. */

/* Stone/Parchment palette */
--stone-50:  #FAFAF7;  --stone-50:  oklch(0.969 0.006 95);
--stone-100: #F2F0EB;  --stone-100: oklch(0.950 0.009 95);
--stone-200: #EDECE6;  --stone-200: oklch(0.922 0.013 95);
--stone-300: #E5E3D8;  --stone-300: oklch(0.893 0.017 90);
--stone-400: #CDC9BB;  --stone-400: oklch(0.833 0.022 88);
--stone-500: #BCB7A5;  --stone-500: oklch(0.760 0.028 85);
--stone-600: #857F72;  --stone-600: oklch(0.648 0.030 82);
--stone-700: #52504A;  --stone-700: oklch(0.530 0.025 80);
--stone-800: #2A2720;  --stone-800: oklch(0.350 0.018 78);
--stone-900: #151510;  --stone-900: oklch(0.180 0.012 75);

/* Blue/Indigo palette */
--blue-50:  #EBF0FF;  --blue-50:  oklch(0.960 0.018 265);
--blue-100: #D6E3FF;  --blue-100: oklch(0.920 0.035 265);
--blue-200: #B3CCFF;  --blue-200: oklch(0.850 0.060 265);
--blue-400: #5C96FA;  --blue-400: oklch(0.680 0.130 265);
--blue-500: #3D7EF8;  --blue-500: oklch(0.600 0.165 265);
--blue-600: #1650E0;  --blue-600: oklch(0.520 0.195 265);
--blue-700: #1140C0;  --blue-700: oklch(0.440 0.190 265);
--blue-800: #0C30A0;  --blue-800: oklch(0.360 0.180 265);

/* Green palette */
--green-50:  #EDFAF2;  --green-50:  oklch(0.968 0.020 155);
--green-500: #2DC87A;  --green-500: oklch(0.580 0.155 155);
--green-600: #0B7A40;  --green-600: oklch(0.500 0.135 155);

/* Amber palette */
--amber-50:  #FFF7E8;  --amber-50:  oklch(0.978 0.022 90);
--amber-400: #EDA030;  --amber-400: oklch(0.740 0.130 68);
--amber-600: #B56700;  --amber-600: oklch(0.590 0.115 60);

/* Red palette */
--red-50:  #FEF1F0;  --red-50:  oklch(0.975 0.015 25);
--red-500: #E85454;  --red-500: oklch(0.600 0.185 25);
--red-600: #C42828;  --red-600: oklch(0.510 0.175 25);

/* Purple palette */
--purple-50:  #EEEAFF;  --purple-50:  oklch(0.965 0.022 298);
--purple-500: #8C72F8;  --purple-500: oklch(0.580 0.175 298);
--purple-600: #5038C0;  --purple-600: oklch(0.490 0.170 298);
```

**Section 2: PRIMITIVES — Fonts**

Keep the 3 font families exactly as they are now:
```css
/* ─── PRIMITIVES — Fonts ─── */
--font-sans:    'Bricolage Grotesque', system-ui, sans-serif;
--font-display: 'Fraunces', Georgia, serif;
--font-mono:    'JetBrains Mono', ui-monospace, monospace;
```

**Section 3: PRIMITIVES — Font Size Scale**

Replace the current 8-size scale with the 11-size scale from PITFALLS.md Section 5:
```css
/* ─── PRIMITIVES — Font Sizes ─── */
--text-2xs:  0.6875rem;  /* 11px */
--text-xs:   0.75rem;    /* 12px */
--text-sm:   0.8125rem;  /* 13px */
--text-base: 1rem;       /* 16px — kept at 16px during migration */
--text-md:   1rem;       /* 16px — explicit name for body reading text */
--text-lg:   1.125rem;   /* 18px */
--text-xl:   1.25rem;    /* 20px */
--text-2xl:  1.5rem;     /* 24px */
--text-3xl:  1.875rem;   /* 30px */
--text-4xl:  2.25rem;    /* 36px */
--text-5xl:  3rem;       /* 48px */
```

IMPORTANT: `--text-base` stays at 1rem (16px) in this plan. The 14px migration happens in plan 02.

**Section 4: PRIMITIVES — Line Heights**

Replace current 5-value scale with per-size values from PITFALLS.md Section 5:
```css
/* ─── PRIMITIVES — Line Heights ─── */
--leading-none:  1;
--leading-2xs:   1.5;
--leading-xs:    1.5;
--leading-sm:    1.55;
--leading-base:  1.571;  /* 14px -> 22px golden ratio */
--leading-md:    1.5;    /* 16px -> 24px */
--leading-tight: 1.25;
--leading-snug:  1.375;
--leading-normal: 1.5;
--leading-relaxed: 1.625;
--leading-lg:    1.4;
--leading-xl:    1.35;
--leading-2xl:   1.3;
--leading-3xl:   1.2;
--leading-4xl:   1.1;
```

**Section 5: PRIMITIVES — Font Weights**

```css
/* ─── PRIMITIVES — Font Weights ─── */
--font-normal:   400;    /* Kept for backward compat */
--font-medium:   500;
--font-semibold:  600;
--font-bold:      700;
--font-extrabold: 800;
--weight-regular:   400;
--weight-medium:    500;
--weight-semibold:  600;
--weight-bold:      700;
--weight-extrabold: 800;
```

**Section 6: PRIMITIVES — Letter Spacing**

```css
/* ─── PRIMITIVES — Letter Spacing ─── */
--tracking-tight:   -0.025em;
--tracking-snug:    -0.015em;
--tracking-normal:   0em;
--tracking-wide:     0.025em;
--tracking-wider:    0.05em;
--tracking-widest:   0.1em;
```

**Section 7: SEMANTIC — Spacing Scale + Aliases**

Replace the current spacing section with the complete scale from PITFALLS.md Section 2:
```css
/* ─── SEMANTIC — Spacing (4px base, 8px grid) ─── */
--space-0:   0;
--space-0-5: 0.125rem;  /* 2px */
--space-1:   0.25rem;   /* 4px */
--space-1-5: 0.375rem;  /* 6px */
--space-2:   0.5rem;    /* 8px */
--space-2-5: 0.625rem;  /* 10px */
--space-3:   0.75rem;   /* 12px */
--space-4:   1rem;      /* 16px */
--space-5:   1.25rem;   /* 20px */
--space-6:   1.5rem;    /* 24px */
--space-7:   1.75rem;   /* 28px */
--space-8:   2rem;      /* 32px */
--space-10:  2.5rem;    /* 40px */
--space-12:  3rem;      /* 48px */
--space-14:  3.5rem;    /* 56px */
--space-16:  4rem;      /* 64px */
--space-20:  5rem;      /* 80px */
--space-24:  6rem;      /* 96px */

/* Semantic gap aliases */
--gap-xs: var(--space-1);     /* 4px */
--gap-sm: var(--space-2);     /* 8px */
--gap-md: var(--space-4);     /* 16px */
--gap-lg: var(--space-6);     /* 24px */
--gap-xl: var(--space-8);     /* 32px */

/* Semantic padding aliases */
--pad-xs: var(--space-2);     /* 8px */
--pad-sm: var(--space-3);     /* 12px */
--pad-md: var(--space-4);     /* 16px */
--pad-lg: var(--space-6);     /* 24px */
--pad-xl: var(--space-8);     /* 32px */
--pad-2xl: var(--space-12);   /* 48px */

/* Legacy aliases (backward compat) */
--space-md: var(--space-5);   /* 20px */
--space-lg: var(--space-6);   /* 24px */
```

**Section 8: SEMANTIC — Border Radius**

Replace the current 4-value scale with the complete scale from PITFALLS.md Section 7:
```css
/* ─── SEMANTIC — Border Radius ─── */
--radius-none: 0;
--radius-xs:   0.1875rem; /* 3px */
--radius-sm:   0.3125rem; /* 5px */
--radius-md:   0.375rem;  /* 6px */
--radius-lg:   0.5rem;    /* 8px */
--radius-xl:   0.75rem;   /* 12px */
--radius-2xl:  1rem;      /* 16px */
--radius-full: 9999px;

/* Keep legacy --radius: 0.5rem for backward compat */
--radius: var(--radius-lg);

/* Semantic radius aliases */
--radius-btn:     var(--radius-md);   /* 6px */
--radius-input:   var(--radius-md);   /* 6px */
--radius-badge:   var(--radius-sm);   /* 5px */
--radius-chip:    var(--radius-full);
--radius-card:    var(--radius-lg);   /* 8px */
--radius-panel:   var(--radius-lg);   /* 8px */
--radius-modal:   var(--radius-xl);   /* 12px */
--radius-toast:   var(--radius-lg);   /* 8px */
--radius-tooltip: var(--radius-sm);   /* 5px */
--radius-avatar:  var(--radius-full);
--radius-tag:     var(--radius-xs);   /* 3px */
```

**Section 9: SEMANTIC — Colors (Light Theme)**

Keep ALL existing color tokens (--color-bg, --color-surface, etc.) with their CURRENT hex values. Do NOT switch to oklch for semantic tokens — hex is the safe path. Add only the missing tokens:

```css
--color-text-disabled: #C4C0B5;
--color-border-focus: rgba(22, 80, 224, 0.50);
--color-primary-muted: rgba(22, 80, 224, 0.12);
--color-accent-border: #C4B8F8;
--color-overlay-tint: rgba(0, 0, 0, 0.04);
```

Keep ALL existing persona tokens, sidebar tokens, tag tokens, color-mix tokens. Keep `--color-info-*` aliases.

**Section 10: SEMANTIC — Shadows**

Replace the current shadow section with the complete 8-level system from PITFALLS.md Section 4:
```css
/* ─── SEMANTIC — Shadows ─── */
--shadow-color: 21 21 16;

--shadow-2xs: 0 1px 0 rgb(var(--shadow-color) / 0.04);
--shadow-xs:  0 1px 2px rgb(var(--shadow-color) / 0.06),
              0 1px 1px rgb(var(--shadow-color) / 0.03);
--shadow-sm:  0 1px 3px rgb(var(--shadow-color) / 0.08),
              0 1px 2px rgb(var(--shadow-color) / 0.04);
--shadow:     0 2px 6px rgb(var(--shadow-color) / 0.08),
              0 1px 3px rgb(var(--shadow-color) / 0.05);
--shadow-md:  0 4px 12px rgb(var(--shadow-color) / 0.10),
              0 2px 4px rgb(var(--shadow-color) / 0.06);
--shadow-lg:  0 8px 24px rgb(var(--shadow-color) / 0.12),
              0 3px 8px rgb(var(--shadow-color) / 0.06);
--shadow-xl:  0 16px 40px rgb(var(--shadow-color) / 0.14),
              0 6px 16px rgb(var(--shadow-color) / 0.07);
--shadow-2xl: 0 24px 64px rgb(var(--shadow-color) / 0.18),
              0 8px 24px rgb(var(--shadow-color) / 0.08);

--shadow-inner: inset 0 2px 4px rgb(var(--shadow-color) / 0.05);
--shadow-inset-sm: inset 0 1px 2px rgb(var(--shadow-color) / 0.08);
--shadow-focus: 0 0 0 2px var(--color-surface-raised),
                0 0 0 4px var(--color-border-focus);
--shadow-focus-danger: 0 0 0 2px var(--color-surface-raised),
                        0 0 0 4px rgba(196, 40, 40, 0.35);
```

**Section 11: SEMANTIC — Layout**

Keep existing layout tokens exactly as-is:
```css
--header-height: 56px;
--sidebar-width: 58px;
--sidebar-rail: 58px;
--sidebar-expanded: 252px;
--drawer-width: 400px;
--content-max: 1440px;
--content-narrow: 720px;
```

**Section 12: SEMANTIC — Z-Index**

Keep ALL existing z-index tokens exactly as-is.

**Section 13: SEMANTIC — Transitions**

Replace the current 4-value transition section with the complete system from PITFALLS.md Section 8:
```css
/* ─── SEMANTIC — Transitions ─── */
--duration-instant:    50ms;
--duration-fast:       100ms;
--duration-normal:     150ms;
--duration-moderate:   200ms;
--duration-slow:       250ms;
--duration-deliberate: 300ms;
--duration-elaborate:  400ms;
--duration-dramatic:   500ms;

--ease-standard:       cubic-bezier(0.2, 0, 0, 1);
--ease-emphasized:     cubic-bezier(0.05, 0.7, 0.1, 1.0);
--ease-emphasized-out: cubic-bezier(0.3, 0, 0.8, 0.15);
--ease-linear:         linear;
--ease-spring:         cubic-bezier(0.34, 1.56, 0.64, 1);
--ease-overshoot:      cubic-bezier(0.34, 1.3, 0.64, 1);

/* Legacy aliases */
--ease-default: var(--ease-standard);
--ease-in:      cubic-bezier(0.4, 0, 1, 1);
--ease-out:     cubic-bezier(0, 0, 0.2, 1);
--ease-bounce:  var(--ease-spring);
--transition:   150ms ease;

/* Named transitions */
--transition-color:     color var(--duration-normal) var(--ease-standard),
                        background-color var(--duration-normal) var(--ease-standard),
                        border-color var(--duration-normal) var(--ease-standard);
--transition-shadow:    box-shadow var(--duration-moderate) var(--ease-standard);
--transition-transform: transform var(--duration-moderate) var(--ease-standard);
--transition-opacity:   opacity var(--duration-normal) var(--ease-linear);
--transition-ui:        color var(--duration-normal) var(--ease-standard),
                        background-color var(--duration-normal) var(--ease-standard),
                        border-color var(--duration-normal) var(--ease-standard),
                        box-shadow var(--duration-moderate) var(--ease-standard),
                        opacity var(--duration-normal) var(--ease-linear);
--transition-enter:     transform var(--duration-deliberate) var(--ease-emphasized),
                        opacity var(--duration-deliberate) var(--ease-linear);
--transition-exit:      transform var(--duration-moderate) var(--ease-emphasized-out),
                        opacity var(--duration-moderate) var(--ease-linear);
```

**Section 14: SEMANTIC — Typography Role Aliases**

Add semantic typography aliases from PITFALLS.md Section 5:
```css
/* ─── SEMANTIC — Typography Roles ─── */
--type-page-title-size:   var(--text-3xl);
--type-page-title-weight: var(--weight-bold);
--type-page-title-lead:   var(--leading-3xl);
--type-page-title-track:  var(--tracking-tight);
--type-page-title-font:   var(--font-display);

--type-section-title-size:   var(--text-2xl);
--type-section-title-weight: var(--weight-bold);
--type-section-title-lead:   var(--leading-2xl);
--type-section-title-track:  var(--tracking-snug);

--type-card-title-size:   var(--text-xl);
--type-card-title-weight: var(--weight-semibold);
--type-card-title-lead:   var(--leading-xl);

--type-body-size:   var(--text-base);
--type-body-weight: var(--weight-regular);
--type-body-lead:   var(--leading-base);

--type-body-md-size:   var(--text-md);
--type-body-md-weight: var(--weight-regular);
--type-body-md-lead:   var(--leading-md);

--type-label-size:   var(--text-sm);
--type-label-weight: var(--weight-medium);
--type-label-lead:   var(--leading-sm);

--type-caption-size:   var(--text-xs);
--type-caption-weight: var(--weight-regular);
--type-caption-lead:   var(--leading-xs);

--type-badge-size:   var(--text-2xs);
--type-badge-weight: var(--weight-medium);
--type-badge-track:  var(--tracking-wide);

--type-mono-size:   var(--text-sm);
--type-mono-lead:   var(--leading-sm);
--type-mono-font:   var(--font-mono);
```

**Section 15: SEMANTIC — Focus Ring**

Keep existing focus ring tokens as-is:
```css
--ring-width: 3px;
--ring-color: rgba(22, 80, 224, 0.35);
--ring-offset: 2px;
```

Now close the `:root` block.

**Dark theme overrides:**

Replace the `[data-theme="dark"]` block. Keep ALL existing dark color overrides with their current hex values (do NOT change to oklch). Add only these new tokens:

```css
[data-theme="dark"] {
  /* ALL existing dark color overrides — keep unchanged */
  /* ... (all the --color-bg, --color-surface, etc. dark values) ... */

  /* NEW: Shadow color override */
  --shadow-color: 0 0 0;

  /* NEW: Shadow level overrides for dark visibility */
  --shadow-2xs: 0 1px 0 rgb(var(--shadow-color) / 0.12);
  --shadow-xs:  0 1px 2px rgb(var(--shadow-color) / 0.20),
                0 1px 1px rgb(var(--shadow-color) / 0.12);
  --shadow-sm:  0 1px 3px rgb(var(--shadow-color) / 0.24),
                0 1px 2px rgb(var(--shadow-color) / 0.16);
  --shadow:     0 2px 6px rgb(var(--shadow-color) / 0.30),
                0 1px 3px rgb(var(--shadow-color) / 0.18);
  --shadow-md:  0 4px 12px rgb(var(--shadow-color) / 0.34),
                0 2px 4px rgb(var(--shadow-color) / 0.20);
  --shadow-lg:  0 8px 24px rgb(var(--shadow-color) / 0.40),
                0 3px 8px rgb(var(--shadow-color) / 0.22);
  --shadow-xl:  0 16px 40px rgb(var(--shadow-color) / 0.50),
                0 6px 16px rgb(var(--shadow-color) / 0.26);
  --shadow-2xl: 0 24px 64px rgb(var(--shadow-color) / 0.60),
                0 8px 24px rgb(var(--shadow-color) / 0.30);
  --shadow-inner: inset 0 2px 4px rgb(var(--shadow-color) / 0.20);
  --shadow-inset-sm: inset 0 1px 2px rgb(var(--shadow-color) / 0.25);

  /* NEW: Missing dark tokens */
  --color-text-disabled: #384050;
  --color-border-focus: rgba(61, 126, 248, 0.50);
  --color-overlay-tint: rgba(255, 255, 255, 0.03);
}
```

**CRITICAL RULES:**
- Do NOT change any CSS rules outside the `:root { }` and `[data-theme="dark"] { }` blocks
- Do NOT remove ANY existing variable that is referenced in page CSS files (backward compat)
- Keep `--radius-sm: 0.375rem` as a LEGACY alias that now maps to `--radius-md` value (6px). The new `--radius-sm` is 5px. To avoid breaking references, add: `/* Legacy: previous --radius-sm was 6px, now split into --radius-sm (5px) and --radius-md (6px) */` and keep both. If any page CSS uses `--radius-sm` expecting 6px, it will get 5px — this is intentional (closer to correct for badges).
- Actually: rename the old `--radius-sm` to `--radius-md` value and set new `--radius-sm` to 5px. The old `--radius-sm` (0.375rem = 6px) = new `--radius-md`. The old `--radius` (0.5rem = 8px) = new `--radius-lg`. The old `--radius-lg` (0.625rem = 10px) is being retired. Add `--radius-lg: 0.5rem` (8px, from PITFALLS.md). Add backward compat: `--radius: var(--radius-lg);`
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c '^\s*--' public/assets/css/design-system.css | awk '{if ($1 > 200 && $1 < 400) print "TOKEN COUNT OK: "$1; else print "TOKEN COUNT UNEXPECTED: "$1}'  && grep -q '\-\-shadow-color' public/assets/css/design-system.css && echo "SHADOW-COLOR: OK" || echo "SHADOW-COLOR: MISSING" && grep -q '\-\-shadow-2xs' public/assets/css/design-system.css && echo "SHADOW-2XS: OK" || echo "SHADOW-2XS: MISSING" && grep -q '\-\-shadow-2xl' public/assets/css/design-system.css && echo "SHADOW-2XL: OK" || echo "SHADOW-2XL: MISSING" && grep -q '\-\-radius-modal' public/assets/css/design-system.css && echo "RADIUS-MODAL: OK" || echo "RADIUS-MODAL: MISSING" && grep -q '\-\-radius-card' public/assets/css/design-system.css && echo "RADIUS-CARD: OK" || echo "RADIUS-CARD: MISSING" && grep -q '\-\-gap-xs' public/assets/css/design-system.css && echo "GAP-XS: OK" || echo "GAP-XS: MISSING" && grep -q '\-\-pad-lg' public/assets/css/design-system.css && echo "PAD-LG: OK" || echo "PAD-LG: MISSING" && grep -q '\-\-type-page-title-size' public/assets/css/design-system.css && echo "TYPE-ALIAS: OK" || echo "TYPE-ALIAS: MISSING" && grep -q '\-\-transition-ui' public/assets/css/design-system.css && echo "TRANSITION-UI: OK" || echo "TRANSITION-UI: MISSING" && grep -q '\-\-tracking-tight' public/assets/css/design-system.css && echo "TRACKING: OK" || echo "TRACKING: MISSING" && grep -q '\-\-stone-200' public/assets/css/design-system.css && echo "PRIMITIVES: OK" || echo "PRIMITIVES: MISSING" && grep -q '\-\-weight-bold' public/assets/css/design-system.css && echo "WEIGHT-ALIAS: OK" || echo "WEIGHT-ALIAS: MISSING" && grep -q '\-\-ease-standard' public/assets/css/design-system.css && echo "EASE-STANDARD: OK" || echo "EASE-STANDARD: MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - grep '--shadow-color' design-system.css returns a match in both :root and [data-theme="dark"]
    - grep '--shadow-2xs' design-system.css returns a match (8-level shadow system present)
    - grep '--shadow-2xl' design-system.css returns a match
    - grep '--radius-modal' design-system.css returns a match (semantic radius aliases present)
    - grep '--radius-card' design-system.css returns a match
    - grep '--radius-btn' design-system.css returns a match
    - grep '--gap-xs' design-system.css returns a match (semantic spacing aliases)
    - grep '--pad-lg' design-system.css returns a match
    - grep '--type-page-title-size' design-system.css returns a match (typography roles)
    - grep '--type-body-size' design-system.css returns a match
    - grep '--transition-ui' design-system.css returns a match (named transitions)
    - grep '--tracking-tight' design-system.css returns a match (letter spacing)
    - grep '--stone-200' design-system.css returns a match (primitives exist)
    - grep '--blue-600' design-system.css returns a match
    - grep '--weight-bold' design-system.css returns a match
    - grep '--ease-standard' design-system.css returns a match
    - grep '--ease-emphasized' design-system.css returns a match
    - grep '--color-bg: #EDECE6' design-system.css returns a match (existing colors preserved)
    - grep '--color-primary: #1650E0' design-system.css returns a match
    - The app loads without broken styling (no white page, no missing colors)
  </acceptance_criteria>
  <done>design-system.css :root has primitive->semantic->alias hierarchy with all 8 shadow levels, complete spacing scale + aliases, full radius scale + semantic aliases, typography role aliases, complete transition system, letter spacing, and font weight aliases. All existing color tokens preserved with hex values. Dark mode has --shadow-color override and 8-level shadow overrides.</done>
</task>

</tasks>

<verification>
- Open the app in a browser — pages render with correct colors, no broken backgrounds
- Toggle dark mode — shadows adapt, colors switch, no transparent elements
- Grep for --shadow-color in the file confirms both light and dark declarations
- Grep for --radius-modal, --radius-card, --radius-btn confirms semantic radius aliases
- Grep for --gap-xs, --pad-lg confirms semantic spacing aliases
- Grep for --type-page-title-size confirms typography role system
- Grep for --transition-ui confirms named transition system
- Total token count in :root is between 200-350 (primitives + semantic + aliases)
</verification>

<success_criteria>
1. design-system.css has clearly commented sections: PRIMITIVES (color, fonts, sizes, line heights, weights, letter spacing), SEMANTIC (spacing, radius, colors, shadows, layout, z-index, transitions, typography roles, focus ring)
2. Shadow system has 8 levels (2xs through 2xl) using --shadow-color variable pattern, with full dark mode override set
3. Border-radius has 7 raw values (none through full) plus 10+ semantic aliases
4. Spacing has 18 raw values (0 through 24) plus 11 semantic aliases (gap-* and pad-*)
5. Typography has role aliases (page-title, section-title, card-title, body, label, caption, badge, mono)
6. Transition system has 8 durations, 6 easing functions, 7 named transitions
7. All existing page CSS continues to work (backward-compat aliases for --radius-sm, --radius, --ease-default, --transition, etc.)
8. Dark mode [data-theme="dark"] has --shadow-color: 0 0 0 and all 8 shadow overrides
</success_criteria>

<output>
After completion, create `.planning/phases/30-token-foundation/30-01-SUMMARY.md`
</output>
