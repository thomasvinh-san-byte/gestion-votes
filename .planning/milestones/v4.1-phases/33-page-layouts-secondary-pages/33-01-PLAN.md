---
phase: 33-page-layouts-secondary-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/hub.css
  - public/assets/css/postsession.css
autonomous: true
requirements: [LAY-07, LAY-08]

must_haves:
  truths:
    - "Hub page shows a 220px sidebar stepper beside main content via CSS Grid"
    - "Hub quorum section uses var(--color-surface-raised) and stands out visually with accent border"
    - "Hub collapses to single column at 768px with stepper becoming static"
    - "Post-session content is centered at max-width 900px"
    - "Post-session stepper is sticky at top:80px while scrolling"
    - "Post-session panels have var(--space-section) (48px) spacing between them"
  artifacts:
    - path: "public/assets/css/hub.css"
      provides: "Hub two-column CSS Grid layout, quorum prominence"
      contains: "grid-template-columns: 220px 1fr"
    - path: "public/assets/css/postsession.css"
      provides: "Post-session centered layout, sticky stepper, section spacing"
      contains: "max-width: 900px"
  key_links:
    - from: "public/assets/css/hub.css"
      to: "design-system.css tokens"
      via: "var(--space-card), var(--color-surface-raised)"
      pattern: "var\\(--space-card\\)"
    - from: "public/assets/css/postsession.css"
      to: "design-system.css tokens"
      via: "var(--space-section)"
      pattern: "var\\(--space-section\\)"
---

<objective>
Apply Phase 32 layout language to the hub and post-session pages — CSS Grid conversion for hub, centered max-width + sticky stepper + section spacing for post-session.

Purpose: These two stepper-based pages are the most complex secondary layouts. They establish the sidebar-stepper pattern (hub) and single-column guided flow pattern (post-session) that Phase 34 will audit.
Output: Updated hub.css and postsession.css with correct grid specs, spacing tokens, and responsive breakpoints.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-page-layouts-secondary-pages/33-CONTEXT.md
@.planning/phases/33-page-layouts-secondary-pages/33-RESEARCH.md
@public/assets/css/hub.css
@public/assets/css/postsession.css
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hub layout — CSS Grid conversion + quorum prominence (LAY-07)</name>
  <files>public/assets/css/hub.css</files>
  <read_first>public/assets/css/hub.css, public/assets/css/design-system.css</read_first>
  <action>
Update hub.css with the following specific changes:

1. **Convert `.hub-layout-body` from flexbox to CSS Grid:**
   - Replace `display: flex; gap: 2rem; align-items: flex-start; flex-wrap: wrap;` with:
     ```css
     display: grid;
     grid-template-columns: 220px 1fr;
     gap: var(--space-card);
     align-items: start;
     ```

2. **Update `.hub-stepper-col`:**
   - Remove `width: 260px;` and `flex-shrink: 0;`
   - Keep `position: sticky; top: 80px;`
   - Add `align-self: start;`

3. **Update `.hub-main-col`:**
   - Remove `flex: 1;`
   - Keep `min-width: 0;`

4. **Elevate `.hub-quorum-section` for visual prominence:**
   - Change `background: var(--color-surface)` to `background: var(--color-surface-raised)`
   - Add `border-left: 3px solid var(--color-primary);`
   - Change `padding: 1.5rem` to `padding: var(--space-card);`
   - Change `margin-bottom: 1.5rem` to `margin-bottom: var(--space-card);`

5. **Update responsive breakpoints:**
   - In the `@media (max-width: 1024px)` block, update `.hub-layout-body` rule:
     Remove `flex-direction: column;` and add `grid-template-columns: 1fr;`
   - In existing `@media (max-width: 1024px)`, update `.hub-stepper-col`:
     Remove `width: 100%;` (grid handles it)
   - In the `@media (max-width: 768px)` block, update `.hub-layout-body` rule:
     Replace `flex-direction: column;` with `grid-template-columns: 1fr;`
   - In `@media (max-width: 768px)`, update `.hub-stepper-col`:
     Remove `width: 100%;`

6. **Also update the legacy `.hub-layout` grid** (line ~1013):
   - Change `grid-template-columns: 260px 1fr` to `grid-template-columns: 220px 1fr`
   - Change `gap: 1.5rem` to `gap: var(--space-card)`

7. **Tokenize remaining hardcoded spacing** in hub sections:
   - `.hub-checklist`: change `padding: 1.5rem` to `padding: var(--space-card)`; `margin-bottom: 1.5rem` to `margin-bottom: var(--space-card)`
   - `.hub-motions-section`: same padding/margin tokenization
   - `.hub-convocation-section`: same
   - `.hub-action`: change `padding: 1.75rem` to `padding: var(--space-card)`; `margin-bottom: 1.5rem` to `margin-bottom: var(--space-card)`

Do NOT touch JavaScript. Do NOT modify HTML structure. Only CSS changes.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "grid-template-columns: 220px 1fr" public/assets/css/hub.css && grep -c "color-surface-raised" public/assets/css/hub.css && grep -c "border-left: 3px solid var(--color-primary)" public/assets/css/hub.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "grid-template-columns: 220px 1fr" hub.css returns at least 2 matches (hub-layout-body + hub-layout)
    - grep "color-surface-raised" hub.css returns at least 1 match (quorum section)
    - grep "border-left: 3px solid var(--color-primary)" hub.css returns match in .hub-quorum-section
    - grep "width: 260px" hub.css returns 0 matches (removed from hub-stepper-col)
    - grep "display: flex" hub.css does NOT match .hub-layout-body (converted to grid)
    - No hardcoded hex values added
  </acceptance_criteria>
  <done>Hub uses CSS Grid 220px+1fr layout, quorum section elevated with raised surface and accent border, responsive breakpoints collapse to single column, all spacing uses design tokens</done>
</task>

<task type="auto">
  <name>Task 2: Post-session layout — centered max-width + sticky stepper + section spacing (LAY-08)</name>
  <files>public/assets/css/postsession.css</files>
  <read_first>public/assets/css/postsession.css, public/assets/css/design-system.css</read_first>
  <action>
Update postsession.css with the following specific changes:

1. **Add centered content constraint** — add a new rule (or update existing `.container` within postsession context):
   ```css
   .postsession-main .container,
   .postsession-main .page-content {
     max-width: 900px;
     margin: 0 auto;
     padding: 0 var(--space-card);
   }
   ```
   First check the HTML file to confirm which wrapper class is used. The research says `<div class="container">` at line 78. Target that class within the postsession context.

2. **Make stepper sticky:**
   ```css
   .ps-stepper {
     position: sticky;
     top: 80px;
     z-index: 10;
     background: var(--color-bg);
     padding-top: var(--space-3);
     padding-bottom: var(--space-3);
     margin-bottom: var(--space-card);
   }
   ```
   Update the existing `.ps-stepper` rule: keep `display: flex; gap: 4px;`, replace `margin-bottom: 18px` with `margin-bottom: var(--space-card)`, add the sticky properties listed above.

3. **Add inter-section spacing between panels:**
   ```css
   .ps-panel + .ps-panel {
     margin-top: var(--space-section);
   }
   ```

4. **Tokenize existing hardcoded spacing:**
   - `.ps-complete-banner`: change `padding: 14px` to `padding: var(--space-4)`; `margin-bottom: 14px` to `margin-bottom: var(--space-4)`
   - `.ps-irreversible-warning`: change `padding: 14px` to `padding: var(--space-4)`; `margin-bottom: 14px` to `margin-bottom: var(--space-4)`
   - `.ps-validation-kpis`: change `margin-bottom: 14px` to `margin-bottom: var(--space-4)`

5. **Ensure result cards use proper surface token:**
   - `.result-card` already uses `background: var(--color-surface)` — confirm and keep
   - `.result-card[open]` already has `box-shadow: var(--shadow-md)` — confirm and keep

Do NOT touch JavaScript. Do NOT modify HTML structure. Only CSS changes.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "max-width: 900px" public/assets/css/postsession.css && grep -c "position: sticky" public/assets/css/postsession.css && grep -c "var(--space-section)" public/assets/css/postsession.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "max-width: 900px" postsession.css returns at least 1 match
    - grep "position: sticky" postsession.css returns at least 2 matches (ps-stepper + ps-footer-nav)
    - grep "var(--space-section)" postsession.css returns at least 1 match (panel spacing)
    - grep "top: 80px" postsession.css returns at least 1 match (ps-stepper)
    - grep "z-index: 10" postsession.css returns at least 1 match (ps-stepper)
    - No hardcoded hex values added
  </acceptance_criteria>
  <done>Post-session content centered at 900px max-width, stepper sticky at top:80px with z-index:10, panels separated by var(--space-section) (48px), all spacing tokenized</done>
</task>

</tasks>

<verification>
- Open hub page in browser: sidebar stepper should be 220px wide on left, main content fills remaining space
- Hub quorum section should visually stand out with raised background and blue left accent border
- At 768px viewport, hub layout should collapse to single column with stepper above content
- Open post-session page: content should be centered with visible max-width constraint
- Post-session stepper should remain visible when scrolling down through panels
- Spacing between post-session panels should be visibly larger (48px) than within-panel spacing
</verification>

<success_criteria>
- Hub layout uses CSS Grid with 220px sidebar column
- Hub quorum section uses var(--color-surface-raised) background
- Post-session content constrained to 900px max-width
- Post-session stepper is sticky
- Post-session panel spacing uses var(--space-section)
- Zero hardcoded hex/rgb values introduced
- All spacing uses design-system tokens
</success_criteria>

<output>
After completion, create `.planning/phases/33-page-layouts-secondary-pages/33-01-SUMMARY.md`
</output>
