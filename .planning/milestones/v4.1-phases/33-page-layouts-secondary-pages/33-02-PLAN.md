---
phase: 33-page-layouts-secondary-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/analytics.css
  - public/assets/css/help.css
autonomous: true
requirements: [LAY-09, LAY-10]

must_haves:
  truths:
    - "Analytics page shows min 2 columns at 1024px viewport — never collapses to single column on tablet"
    - "Analytics content is constrained to max-width 1400px"
    - "Analytics overview cards use var(--color-surface-raised) background"
    - "Help/FAQ page content is constrained to max-width 800px"
    - "FAQ accordion questions have padding var(--space-4) var(--space-card) (16px 24px)"
    - "FAQ accordion answers have padding 0 var(--space-card) var(--space-card)"
  artifacts:
    - path: "public/assets/css/analytics.css"
      provides: "Analytics responsive grid with 2-col floor, KPI card elevation, max-width"
      contains: "max-width: 1400px"
    - path: "public/assets/css/help.css"
      provides: "Help page max-width 800px, accordion padding tokenization"
      contains: "max-width: 800px"
  key_links:
    - from: "public/assets/css/analytics.css"
      to: "design-system.css tokens"
      via: "var(--color-surface-raised), var(--space-card)"
      pattern: "var\\(--color-surface-raised\\)"
    - from: "public/assets/css/help.css"
      to: "design-system.css tokens"
      via: "var(--space-4), var(--space-card), var(--font-semibold)"
      pattern: "var\\(--space-card\\)"
---

<objective>
Apply Phase 32 layout language to analytics and help/FAQ pages — responsive grid with 2-column floor for analytics, proper accordion padding and max-width for help.

Purpose: Analytics must never collapse to single-column on tablet (explicit requirement). Help page needs consistent accordion padding aligned to design tokens instead of browser-default `1rem`.
Output: Updated analytics.css and help.css with correct grid constraints, max-widths, and tokenized spacing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-page-layouts-secondary-pages/33-CONTEXT.md
@.planning/phases/33-page-layouts-secondary-pages/33-RESEARCH.md
@public/assets/css/analytics.css
@public/assets/css/help.css
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics layout — 2-column grid floor + KPI elevation + max-width (LAY-09)</name>
  <files>public/assets/css/analytics.css</files>
  <read_first>public/assets/css/analytics.css, public/assets/css/design-system.css</read_first>
  <action>
Update analytics.css with the following specific changes:

1. **Ensure content max-width constraint** — the existing `.analytics-content` already has `max-width: 1400px; margin: 0 auto;` (line 53-56). Verify this is correct and keep it. If `.page-content` is used in HTML instead, add:
   ```css
   .analytics-main .page-content {
     max-width: 1400px;
     margin: 0 auto;
   }
   ```

2. **Update `.overview-grid` for proper KPI card styling:**
   - Keep existing `display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));`
   - Change `gap: 10px` to `gap: var(--space-card);`
   - Change `margin-bottom: 14px` to `margin-bottom: var(--space-card);`

3. **Elevate `.overview-card` to raised surface:**
   - Change `background: var(--color-surface)` to `background: var(--color-surface-raised)`
   - Change `padding: 14px` to `padding: var(--space-card);`
   - Add `box-shadow: var(--shadow-sm);`

4. **Fix `.charts-grid` to guarantee 2-column minimum at tablet:**
   - Change `grid-template-columns: repeat(auto-fit, minmax(400px, 1fr))` to `grid-template-columns: repeat(auto-fit, minmax(320px, 1fr))`
   - Change `gap: 10px` to `gap: var(--space-card);`
   - Change `margin-bottom: 14px` to `margin-bottom: var(--space-section);`
   - Update the existing `@media (max-width: 768px)` rule for `.charts-grid` to enforce 2-col floor above 768px. Add a NEW media query:
     ```css
     @media (min-width: 768px) {
       .charts-grid {
         grid-template-columns: repeat(2, minmax(0, 1fr));
       }
     }
     ```
   - Keep the existing `@media (max-width: 768px) { .charts-grid { grid-template-columns: 1fr; } }` for mobile

5. **Tokenize `.chart-card` spacing:**
   - Change `border-radius: 8px` to `border-radius: var(--radius-lg);`
   - `.chart-card-header`: change `padding: 10px 14px` to `padding: var(--space-3) var(--space-4);`
   - `.chart-card-body`: change `padding: 14px` to `padding: var(--space-4);`

6. **Tokenize other hardcoded spacing:**
   - `.analytics-content`: change `padding: 14px` to `padding: var(--space-4);`
   - `.analytics-page-header`: change `padding: 10px 14px` to `padding: var(--space-3) var(--space-4);`

Do NOT touch JavaScript. Do NOT modify HTML structure. Only CSS changes.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "color-surface-raised" public/assets/css/analytics.css && grep -c "var(--space-card)" public/assets/css/analytics.css && grep "repeat(2, minmax(0, 1fr))" public/assets/css/analytics.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "color-surface-raised" analytics.css returns at least 1 match (overview-card)
    - grep "repeat(2, minmax(0, 1fr))" analytics.css returns match (2-col floor)
    - grep "minmax(320px, 1fr)" analytics.css returns match (charts-grid default)
    - grep "max-width: 1400px" analytics.css returns at least 1 match
    - grep "var(--space-card)" analytics.css returns multiple matches
    - grep "var(--space-section)" analytics.css returns at least 1 match
    - grep "gap: 10px" analytics.css returns 0 matches (all tokenized)
    - No hardcoded hex values added
  </acceptance_criteria>
  <done>Analytics grid guarantees 2 columns at 1024px, KPI cards use raised surface with shadow, content constrained to 1400px, all spacing tokenized</done>
</task>

<task type="auto">
  <name>Task 2: Help/FAQ layout — max-width 800px + accordion padding tokenization (LAY-10)</name>
  <files>public/assets/css/help.css</files>
  <read_first>public/assets/css/help.css, public/assets/css/design-system.css</read_first>
  <action>
Update help.css with the following specific changes:

1. **Fix `.help-content` max-width:**
   - Change `max-width: 900px` to `max-width: 800px`
   - Change `padding: 14px` to `padding: var(--space-card);`

2. **Update `.faq-question` padding to token spec:**
   - Change `padding: 1rem` to `padding: var(--space-4) var(--space-card);` (16px top/bottom, 24px left/right)
   - Change `font-weight: 500` to `font-weight: var(--font-semibold);`
   - Add `font-size: var(--text-base);` for consistent sizing

3. **Update `.faq-answer` padding to token spec:**
   - Change `padding: 0 1rem 1rem 1rem` to `padding: 0 var(--space-card) var(--space-card);` (0 top, 24px left/right/bottom)

4. **Add smooth reveal animation for accordion expand** (no layout shift):
   ```css
   .faq-item.open .faq-answer {
     display: block;
     animation: faqReveal 150ms ease-out;
   }

   @keyframes faqReveal {
     from { opacity: 0; transform: translateY(-4px); }
     to   { opacity: 1; transform: translateY(0); }
   }
   ```
   Update the existing `.faq-item.open .faq-answer` rule (which currently just sets `display: block`) to add the animation.

5. **Tokenize `.faq-section` spacing:**
   - Change `margin-bottom: 14px` to `margin-bottom: var(--space-4);`

6. **Tokenize `.faq-item` spacing:**
   - Change `margin-bottom: 0.75rem` to `margin-bottom: var(--space-3);`
   - Change `border-radius: 8px` to `border-radius: var(--radius);`

7. **Tokenize `.help-header` spacing:**
   - Change `padding: 10px 14px` to `padding: var(--space-3) var(--space-4);`

8. **Tokenize `.doc-links` and other sections:**
   - `.doc-links`: change `padding: 14px` to `padding: var(--space-4);`; `margin-top: 14px` to `margin-top: var(--space-4);`
   - `.help-support`: change `padding: 14px` to `padding: var(--space-4);`; `margin-top: 14px` to `margin-top: var(--space-4);`
   - `.tour-grid`: change `margin-bottom: 14px` to `margin-bottom: var(--space-4);`
   - `.quick-links`: change `margin-bottom: 14px` to `margin-bottom: var(--space-4);`

Do NOT touch JavaScript. Do NOT modify the FAQ JS accordion toggle. Only CSS changes.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "max-width: 800px" public/assets/css/help.css && grep "var(--space-4) var(--space-card)" public/assets/css/help.css && grep -c "faqReveal" public/assets/css/help.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "max-width: 800px" help.css returns 1 match (was 900px)
    - grep "max-width: 900px" help.css returns 0 matches (replaced)
    - grep "var(--space-4) var(--space-card)" help.css returns match (faq-question padding)
    - grep "var(--font-semibold)" help.css returns match (faq-question font-weight)
    - grep "padding: 0 var(--space-card) var(--space-card)" help.css returns match (faq-answer)
    - grep "faqReveal" help.css returns at least 2 matches (animation name + keyframes)
    - grep "padding: 1rem" help.css returns 0 matches (all tokenized)
    - No hardcoded hex values added
  </acceptance_criteria>
  <done>Help page max-width reduced to 800px, FAQ accordion uses tokenized padding (16px 24px question, 0 24px 24px answer), smooth 150ms reveal animation, all spacing tokenized</done>
</task>

</tasks>

<verification>
- Open analytics page: KPI cards should have raised background (lighter than page background)
- At 1024px viewport, analytics charts grid must show 2 columns side by side — never 1 column
- Open help/FAQ page: content should be visually narrower than before (800px vs 900px)
- Expanding a FAQ item should show a subtle fade-in animation with no layout shift on surrounding items
- FAQ question text should be semibold with 24px left/right padding
</verification>

<success_criteria>
- Analytics charts show minimum 2 columns at 1024px viewport
- Analytics KPI cards use var(--color-surface-raised) background
- Analytics content constrained to 1400px
- Help content constrained to 800px (not 900px)
- FAQ accordion padding uses var(--space-4) and var(--space-card) tokens
- FAQ expand has 150ms fade animation
- Zero hardcoded px spacing values remaining in help.css
</success_criteria>

<output>
After completion, create `.planning/phases/33-page-layouts-secondary-pages/33-02-SUMMARY.md`
</output>
