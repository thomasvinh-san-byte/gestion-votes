---
phase: 04-tests-et-decoupage-controllers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Unit/ImportServiceTest.php
autonomous: true
requirements:
  - TEST-04
must_haves:
  truths:
    - "Tests verify that all accented aliases in getMembersColumnMap resolve correctly (ponderation/ponderation, prenom/prenom, tantiemes/tantiemes)"
    - "Tests verify that all accented aliases in getMotionsColumnMap resolve correctly (intitule/intitule, resolution/resolution, detail/detail)"
    - "Tests verify that all accented aliases in getAttendancesColumnMap resolve correctly (presence/presence, etat/etat)"
    - "Tests verify that all accented aliases in getProxiesColumnMap resolve correctly"
    - "Tests verify that whitespace-padded headers are handled by readCsvFile normalization"
    - "Tests verify that mixed-case raw CSV headers are lowercased by readCsvFile before mapColumns"
  artifacts:
    - path: "tests/Unit/ImportServiceTest.php"
      provides: "Fuzzy matching tests for column alias resolution"
      contains: "testMapColumnsAllVotingPowerAliases"
  key_links:
    - from: "tests/Unit/ImportServiceTest.php"
      to: "app/Services/ImportService.php"
      via: "static calls mapColumns(), getMembersColumnMap(), getMotionsColumnMap(), etc."
      pattern: "ImportService::(mapColumns|get.*ColumnMap)"
---

<objective>
Add fuzzy matching and multi-language alias tests to ImportServiceTest.php covering column mapping edge cases (TEST-04).

Purpose: Close the TEST-04 gap — existing tests exercise mapColumns with lower-case unaccented headers only. No tests verify accented aliases (ponderation, prenom, tantiemes, resolution, etc.), multi-language variants, whitespace padding, or mixed-case raw CSV headers.

Output: ImportServiceTest.php extended with 8+ new test methods covering all column map alias lists comprehensively.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tests-et-decoupage-controllers/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fuzzy matching and multi-language alias tests to ImportServiceTest</name>
  <files>tests/Unit/ImportServiceTest.php</files>
  <read_first>
    - tests/Unit/ImportServiceTest.php (current tests, mapColumns section, column map getter tests)
    - app/Services/ImportService.php (mapColumns method at line 163, all 4 column map methods at lines 265-311, readCsvFile header normalization at line 236)
  </read_first>
  <action>
Add the following test methods to `ImportServiceTest.php` in a new section `// =========================================================================` / `// mapColumns — fuzzy matching and alias coverage (TEST-04)` / `// =========================================================================` after the existing `mapColumns tests` section.

**Test 1: `testMapColumnsAllVotingPowerAliases()`**
- Define array: `['voting_power', 'ponderation', 'pondération', 'weight', 'tantiemes', 'tantièmes', 'poids']`
- For each alias, call `ImportService::mapColumns([$alias], ImportService::getMembersColumnMap())`
- Assert each returns `['voting_power' => 0]`
- This proves all 7 aliases for voting_power resolve correctly

**Test 2: `testMapColumnsAllFirstNameAliases()`**
- Define array: `['first_name', 'prenom', 'prénom']`
- For each alias, call mapColumns with getMembersColumnMap
- Assert each returns `['first_name' => 0]`

**Test 3: `testMapColumnsAllGroupsAliases()`**
- Define array: `['groups', 'groupes', 'group', 'groupe', 'college', 'collège', 'categorie', 'catégorie']`
- For each alias, call mapColumns with getMembersColumnMap
- Assert each returns `['groups' => 0]`

**Test 4: `testMapColumnsAllMotionTitleAliases()`**
- Define array: `['title', 'titre', 'intitule', 'intitulé', 'resolution', 'résolution']`
- For each, call mapColumns with getMotionsColumnMap
- Assert each returns `['title' => 0]`

**Test 5: `testMapColumnsAllMotionDescriptionAliases()`**
- Define array: `['description', 'texte', 'content', 'contenu', 'detail', 'détail']`
- For each, call mapColumns with getMotionsColumnMap
- Assert each returns `['description' => 0]`

**Test 6: `testMapColumnsAllAttendanceModeAliases()`**
- Define array: `['mode', 'statut', 'status', 'presence', 'présence', 'etat', 'état']`
- For each, call mapColumns with getAttendancesColumnMap
- Assert each returns `['mode' => 0]`

**Test 7: `testMapColumnsAllProxyGiverAliases()`**
- Define array: `['giver_name', 'mandant_nom', 'mandant', 'donneur', 'donneur_nom', 'from_name', 'de']`
- For each, call mapColumns with getProxiesColumnMap
- Assert each returns `['giver_name' => 0]`

**Test 8: `testMapColumnsAllProxyReceiverAliases()`**
- Define array: `['receiver_name', 'mandataire_nom', 'mandataire', 'receveur', 'receveur_nom', 'to_name', 'vers']`
- For each, call mapColumns with getProxiesColumnMap
- Assert each returns `['receiver_name' => 0]`

**Test 9: `testReadCsvFileNormalizesUppercaseHeaders()`**
- Create temp CSV file with content: `"NOM,EMAIL,PONDERATION\nAlice,alice@test.com,1\n"`
- Call `ImportService::readCsvFile($path)`
- Assert `$result['headers'] === ['nom', 'email', 'ponderation']` (all lowercased)
- Then call `ImportService::mapColumns($result['headers'], ImportService::getMembersColumnMap())`
- Assert result has keys `name`, `email`, `voting_power`

**Test 10: `testReadCsvFileNormalizesMixedCaseAccentedHeaders()`**
- Create temp CSV file: `"Nom,Email,Pondération,Prénom\nDupont,d@test.com,2,Jean\n"`
- Call readCsvFile, assert headers are `['nom', 'email', 'pondération', 'prénom']`
- Call mapColumns with getMembersColumnMap
- Assert result has keys `name`, `email`, `voting_power`, `first_name`

**Test 11: `testReadCsvFileTrimsWhitespacePaddedHeaders()`**
- Create temp CSV file: `"  nom  , email ,ponderation \nAlice,a@test.com,1\n"`
- Call readCsvFile, assert headers are `['nom', 'email', 'ponderation']` (trimmed)
- Call mapColumns with getMembersColumnMap
- Assert result has keys `name`, `email`, `voting_power`

All tests use `$this->tmpDir` for temp CSV files (matching existing pattern). Use `$this->assertArrayHasKey` for column presence checks and provide descriptive failure messages including the alias being tested: `"Alias '{$alias}' must map to {field}"`.
  </action>
  <verify>
    <automated>timeout 60 php vendor/bin/phpunit tests/Unit/ImportServiceTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - ImportServiceTest.php contains method `testMapColumnsAllVotingPowerAliases`
    - ImportServiceTest.php contains method `testMapColumnsAllFirstNameAliases`
    - ImportServiceTest.php contains method `testMapColumnsAllGroupsAliases`
    - ImportServiceTest.php contains method `testMapColumnsAllMotionTitleAliases`
    - ImportServiceTest.php contains method `testMapColumnsAllMotionDescriptionAliases`
    - ImportServiceTest.php contains method `testMapColumnsAllAttendanceModeAliases`
    - ImportServiceTest.php contains method `testMapColumnsAllProxyGiverAliases`
    - ImportServiceTest.php contains method `testMapColumnsAllProxyReceiverAliases`
    - ImportServiceTest.php contains method `testReadCsvFileNormalizesUppercaseHeaders`
    - ImportServiceTest.php contains method `testReadCsvFileNormalizesMixedCaseAccentedHeaders`
    - ImportServiceTest.php contains method `testReadCsvFileTrimsWhitespacePaddedHeaders`
    - All tests pass with exit code 0
    - Total test count in ImportServiceTest.php is 35+ (24 existing + 11 new)
  </acceptance_criteria>
  <done>ImportServiceTest has 35+ passing tests, with 11 new tests covering all accented alias variants across all 4 column maps, mixed-case headers, and whitespace-padded headers — satisfying TEST-04 fuzzy matching requirements.</done>
</task>

</tasks>

<verification>
timeout 60 php vendor/bin/phpunit tests/Unit/ImportServiceTest.php --no-coverage
All tests green, 35+ tests total (24 existing + 11 new alias/fuzzy matching tests).
</verification>

<success_criteria>
- TEST-04 closed: ImportService has tests for all accented aliases (ponderation, tantiemes, prenom, resolution, etc.), all multi-language column maps (members, attendances, motions, proxies), header case normalization, and whitespace trimming
- All existing tests continue to pass (no regressions)
- Tests run without database or Redis (pure unit tests on static methods + CSV file I/O)
</success_criteria>

<output>
After completion, create `.planning/phases/04-tests-et-decoupage-controllers/04-02-SUMMARY.md`
</output>
