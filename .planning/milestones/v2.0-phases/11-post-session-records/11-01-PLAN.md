---
phase: 11-post-session-records
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/postsession.htmx.html
  - public/assets/css/postsession.css
  - public/assets/js/pages/postsession.js
autonomous: true
requirements: [POST-01, POST-02, POST-03]

must_haves:
  truths:
    - "Post-session page shows a 4-step stepper (Verification, Validation, PV, Envoi) with clickable done segments"
    - "A shared pinned footer nav bar shows 'Etape X / 4' with Precedent/Suivant buttons below all panels"
    - "Step 1 shows a success alert banner and a simplified 5-column results table (no stats grid, no checklist card, no alerts card)"
    - "Step 3 signataires use inline readonly inputs in 2-per-row layout instead of card grid"
    - "Step 3 observations split into two fields: Observations du president + Reserves et contestations"
    - "Step 3 eIDAS uses chip selector with per-role sign buttons and signature counter"
    - "Step 4 has a green PV summary card at top and 2-column exports/archivage layout"
    - "Document download (PV PDF), sign buttons, and send-to-all functionality are accessible"
  artifacts:
    - path: "public/postsession.htmx.html"
      provides: "Restructured post-session HTML with shared footer nav, simplified Step 1, wireframe Step 3, and 2-col Step 4"
      contains: "ps-footer-nav"
    - path: "public/assets/css/postsession.css"
      provides: "CSS for shared footer nav, chip selector, signataire inputs, PV summary card, 2-col exports"
      contains: "ps-footer-nav"
    - path: "public/assets/js/pages/postsession.js"
      provides: "JS updates shared footer nav counter/buttons on step change, chip toggle, sign button handlers"
      contains: "updateFooterNav"
  key_links:
    - from: "public/assets/js/pages/postsession.js"
      to: "public/postsession.htmx.html"
      via: "goToStep updates .ps-footer-nav counter and button labels"
      pattern: "ps-footer-nav"
    - from: "public/postsession.htmx.html"
      to: "public/assets/css/postsession.css"
      via: "New CSS classes for chips, signataire inputs, PV summary, footer nav"
      pattern: "ps-chip|ps-signataire-input|ps-pv-summary|ps-footer-nav"
---

<objective>
Restructure the post-session page to match wireframe v3.19.2 per CONTEXT.md decisions.

Purpose: The existing post-session page has structural differences from the wireframe -- a 6-stat grid and checklist in Step 1 that should be a simple success alert + 5-col table, card-based signataires that should be inline inputs, radio-based eIDAS that should be chips, and per-panel nav buttons that should be a shared footer bar.

Output: Reworked postsession.htmx.html, postsession.css, postsession.js matching wireframe layout with shared footer navigation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-post-session-records/11-CONTEXT.md

@public/postsession.htmx.html
@public/assets/css/postsession.css
@public/assets/js/pages/postsession.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure post-session HTML and CSS to match wireframe</name>
  <files>public/postsession.htmx.html, public/assets/css/postsession.css</files>
  <action>
Restructure postsession.htmx.html with these changes (per CONTEXT.md locked decisions):

**Shared footer nav (replaces all per-panel .ps-actions blocks):**
- Add a pinned footer bar after all panels (before closing `</div>` of `.container`): `<div class="ps-footer-nav" id="psFooterNav">` containing a step counter span ("Etape 1 / 4") and Precedent/Suivant buttons
- Remove ALL existing `.ps-actions` divs from panels 1-4 (4 instances total)
- CSS: `.ps-footer-nav` is sticky at bottom with `position: sticky; bottom: 0; z-index: 10;` background surface, border-top, flex layout with space-between, padding

**Step 1 (Verification) — simplify:**
- Remove the `.stats-grid#summaryStats` div (6-stat grid) entirely
- Remove the checklist card (`#verifyChecklist` parent card) entirely
- Remove the alerts card (`#alertsCard`) entirely
- Add a success alert banner before the results table: `<div class="alert alert-success mb-4" id="verifyAlert"><svg icon check-circle>Tous les votes sont clos. Aucune anomalie detectee.</div>`
- Change results table from 7 columns to 5 columns: #, Resolution, Resultat (tag), Pour/Contre/Abst (single combined column), Majorite
- The Resultat column uses `.tag.tag-success` with check icon for Adoptee and `.tag.tag-danger` with x icon for Rejetee
- Pour/Contre/Abst combined into one column with slash-separated values

**Step 2 (Validation) — keep as-is** (already close to wireframe per CONTEXT.md)

**Step 3 (PV) — signataires, observations, eIDAS:**
- Replace signataire card grid with 2-row inline readonly input layout:
  ```html
  <div class="ps-signataire-row">
    <div class="form-group"><label class="form-label">President(e)</label><input class="form-input" id="sigPresident" readonly value="—"></div>
    <div class="form-group"><label class="form-label">Secretaire</label><input class="form-input" id="sigSecretary" readonly value="—"></div>
  </div>
  <div class="ps-signataire-row">
    <div class="form-group"><label class="form-label">Scrutateur 1</label><input class="form-input" id="sigScrutateur1" readonly value="—"></div>
    <div class="form-group"><label class="form-label">Scrutateur 2</label><input class="form-input" id="sigScrutateur2" readonly value="—"></div>
  </div>
  ```
- Split observations textarea into two separate fields: "Observations du president" textarea + "Reserves et contestations" textarea with `.alert.alert-warn` between them: "Delai de contestation : le PV doit etre notifie dans le delai prevu par vos statuts."
- The reserves textarea wrapper gets a warn-colored left border (`.ps-reserves-field`)
- Replace eIDAS radio cards with chip selector: 3 chips with exact labels "Signature avancee (eIDAS)", "Signature qualifiee (eIDAS+)", "Manuscrite (impression)" using `.chip` / `.chip.active` pattern
- Below chips, add per-role sign buttons: "Signer — President", "Signer — Secretaire" as `.btn.btn-primary.btn-sm` + a signature counter tag "0/2 signatures" using `.tag.tag-accent`
- Keep PV preview card with Generate button, PDF link, hash bar (not in wireframe but useful — per CONTEXT.md)

**Step 4 (Envoi & Archivage):**
- Add prominent PV summary card at top with green border: `.ps-pv-summary` card showing title, key stats (resolutions, adoption rate), and a PDF download button
- Replace `.ps-exports-grid` (single grid) with 2-column layout: left column = exports list (7 items as vertical list with download buttons), right column = archivage card
- Keep recipient selector and completion banner
- CSS: `.ps-exports-layout` as 2-col grid (2fr 1fr), `.ps-export-item` as flex row with icon + label + download button

**CSS updates to postsession.css:**
- Add `.ps-footer-nav` styles (sticky, flex, border-top, padding, bg surface)
- Add `.ps-signataire-row` (display: grid, grid-template-columns: 1fr 1fr, gap)
- Add `.ps-reserves-field` (border-left: 3px solid var(--color-warning))
- Replace `.ps-eidas-modes` / `.ps-eidas-mode` with `.ps-chip-group` (flex wrap gap) — reuse existing `.chip` / `.chip.active` from design-system
- Add `.ps-sign-actions` (flex, gap, align-items center)
- Add `.ps-pv-summary` (border: 2px solid var(--color-success), border-radius, padding, flex between)
- Add `.ps-exports-layout` (grid 2-col) and `.ps-export-item` (flex row)
- Remove old `.ps-signataire-card`, `.ps-signataires` grid, `.ps-eidas-mode*`, `.stats-grid` CSS (dead code after HTML changes)
- Remove `.ps-actions` CSS (replaced by `.ps-footer-nav`)
- Responsive: `.ps-exports-layout` stacks to 1 column on mobile, `.ps-signataire-row` stacks to 1 column on mobile, `.ps-footer-nav` stays visible
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && node -e "
      var fs = require('fs');
      var html = fs.readFileSync('public/postsession.htmx.html','utf8');
      var css = fs.readFileSync('public/assets/css/postsession.css','utf8');
      var checks = [
        ['footer nav exists', html.includes('ps-footer-nav')],
        ['no ps-actions in HTML', !html.includes('class=\"ps-actions\"')],
        ['no summaryStats', !html.includes('id=\"summaryStats\"')],
        ['no verifyChecklist', !html.includes('id=\"verifyChecklist\"')],
        ['no alertsCard', !html.includes('id=\"alertsCard\"')],
        ['verify alert exists', html.includes('verifyAlert')],
        ['signataire row exists', html.includes('ps-signataire-row')],
        ['chip pattern used', html.includes('chip')],
        ['pv summary card', html.includes('ps-pv-summary')],
        ['exports layout', html.includes('ps-exports-layout')],
        ['footer nav CSS', css.includes('ps-footer-nav')],
        ['no old ps-actions CSS', !css.includes('.ps-actions')],
        ['reserves field CSS', css.includes('ps-reserves-field')],
      ];
      var pass = checks.filter(c=>c[1]).length;
      checks.forEach(function(c){console.log((c[1]?'PASS':'FAIL')+': '+c[0])});
      console.log(pass+'/'+checks.length+' checks passed');
      process.exit(pass===checks.length?0:1);
    "
    </automated>
  </verify>
  <done>Post-session HTML restructured: shared footer nav replaces per-panel actions, Step 1 simplified to alert+5-col table, Step 3 has inline signataire inputs + chip eIDAS + sign buttons, Step 4 has PV summary card + 2-col exports layout. All old dead CSS removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update post-session JS for shared footer nav and new UI elements</name>
  <files>public/assets/js/pages/postsession.js</files>
  <action>
Update postsession.js to support the restructured HTML:

**Shared footer nav logic:**
- Add `updateFooterNav(step)` function called from `goToStep()`:
  - Updates step counter text: "Etape {step} / 4"
  - Precedent button: hidden on step 1, visible on steps 2-4, label "Precedent"
  - Suivant button: visible on steps 1-3 with label "Suivant", hidden on step 4
  - Suivant button disabled state: on step 1 tied to verification success (same logic as old btnToStep2), on step 2 tied to validation state (same as old btnToStep3)
- Wire Precedent button click to `goToStep(_currentStep - 1)` and Suivant to `goToStep(_currentStep + 1)` in `bindNavigation()`
- Remove all old per-panel button bindings (btnToStep2, btnToStep3, btnToStep4, btnBackToStep1, btnBackToStep2, btnBackToStep3) — these elements no longer exist

**Step 1 verification rewrite:**
- Remove `loadVerification()` references to summaryStats (setStatValue calls for statMembers, statPresent, etc.)
- Remove checklist loading logic (Shared.withRetry block for verifyChecklist)
- Remove anomalies loading logic (alertsCard/alertsList)
- Keep API call to meeting_summary.php but use it to populate the results table with 5-column layout
- Add `loadResultsTable()` function: fetches `/api/v1/meeting_motions.php?meeting_id=...`, renders rows with: `#` (index), resolution title, result tag (`.tag.tag-success` "Adoptee" or `.tag.tag-danger` "Rejetee"), combined "Pour/Contre/Abst" as "X/Y/Z", and majority type
- If all votes closed successfully, show the verify alert banner; if issues detected, change alert to warn variant
- Suivant button (in footer nav) enabled once results table loads without critical issues

**Step 3 chip toggle + sign buttons:**
- Add chip toggle handler: clicking a `.chip` in the eIDAS group sets `.active` on clicked chip and removes from siblings
- Add sign button click handlers for "Signer — President" and "Signer — Secretaire" buttons: call API or show confirmation, update signature counter tag text ("1/2 signatures", "2/2 signatures")
- Load signataire names into readonly input values from meeting data

**Step 4 PV summary card:**
- Populate `.ps-pv-summary` card with meeting title, resolution count, adoption rate from summary API data
- Set PDF download link href on the summary card's download button

**Keep all existing working logic** for: Step 2 validation/workflow, Step 3 PV generation/preview/hash, Step 4 send email/archive/completion banner.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && node -e "
      var fs = require('fs');
      var js = fs.readFileSync('public/assets/js/pages/postsession.js','utf8');
      var checks = [
        ['updateFooterNav exists', js.includes('updateFooterNav')],
        ['no btnToStep2 binding', !js.includes('btnToStep2')],
        ['no btnBackToStep1 binding', !js.includes('btnBackToStep1')],
        ['no summaryStats refs', !js.includes('statMembers')],
        ['no verifyChecklist refs', !js.includes('verifyChecklist')],
        ['no alertsCard refs', !js.includes('alertsCard')],
        ['chip toggle logic', js.includes('chip')],
        ['sign button handler', js.includes('Signer') || js.includes('sign')],
        ['IIFE pattern preserved', js.includes('(function')],
        ['goToStep calls updateFooterNav', js.includes('updateFooterNav')],
      ];
      var pass = checks.filter(c=>c[1]).length;
      checks.forEach(function(c){console.log((c[1]?'PASS':'FAIL')+': '+c[0])});
      console.log(pass+'/'+checks.length+' checks passed');
      process.exit(pass===checks.length?0:1);
    "
    </automated>
  </verify>
  <done>Post-session JS updated: shared footer nav driven by updateFooterNav(), Step 1 simplified to results table only, Step 3 has chip toggle and sign button handlers, all old per-panel nav button references removed, existing Step 2/3/4 functionality preserved.</done>
</task>

</tasks>

<verification>
- Open postsession.htmx.html in browser — stepper visible with 4 segments, shared footer nav pinned at bottom with step counter
- Clicking Suivant in footer advances steps, Precedent goes back
- Step 1 shows alert banner + 5-column table (no stats grid, no checklist, no alerts)
- Step 3 shows inline signataire inputs, split observations fields, chip eIDAS selector, sign buttons
- Step 4 shows green PV summary card, 2-column exports/archivage layout
- No JS console errors
</verification>

<success_criteria>
Post-session page restructured to match wireframe v3.19.2: shared footer nav replaces per-panel buttons, Step 1 simplified, Step 3 uses chips and inline inputs, Step 4 has 2-col layout with PV summary. All JS functionality preserved for existing API calls.
</success_criteria>

<output>
After completion, create `.planning/phases/11-post-session-records/11-01-SUMMARY.md`
</output>
