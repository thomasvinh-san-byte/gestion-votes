---
phase: 06-layout-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/partials/sidebar.html
  - public/assets/js/core/shell.js
  - public/assets/css/design-system.css
autonomous: true
requirements:
  - NAV-01
  - NAV-02

must_haves:
  truths:
    - "Sidebar shows 58px icon rail by default and expands to 252px on hover as overlay without layout shift"
    - "Sidebar can be pinned open and pin state persists across page loads"
    - "Sidebar has exactly 5 labeled sections: Preparation, Seance en direct, Apres la seance, Controle, Systeme"
    - "Each section contains the correct nav items per wireframe specification"
    - "Sections are collapsible with collapse state saved to localStorage"
    - "Unauthorized nav items are hidden entirely, not grayed out"
  artifacts:
    - path: "public/partials/sidebar.html"
      provides: "Sidebar partial with 5-section nav structure"
      contains: "nav-group"
    - path: "public/assets/js/core/shell.js"
      provides: "Sidebar rail/expand/pin behavior with nav-group collapse persistence"
      contains: "togglePin"
    - path: "public/assets/css/design-system.css"
      provides: "Sidebar CSS with rail and expanded widths"
      contains: "sidebar-rail"
  key_links:
    - from: "public/partials/sidebar.html"
      to: "public/assets/js/core/shell.js"
      via: "data-include-sidebar dynamic loading + MutationObserver"
      pattern: "data-include-sidebar"
    - from: "public/assets/js/core/shell.js"
      to: "localStorage"
      via: "ag-vote-sidebar-groups key for collapse persistence"
      pattern: "ag-vote-sidebar-groups"
---

<objective>
Align sidebar structure and behavior with wireframe v3.19.2 specifications.

Purpose: The sidebar is the primary navigation for the desktop experience. It must match the wireframe's 5-section organization exactly, with correct items in each section, proper rail/expand behavior, and notification badge support.

Output: Updated sidebar.html partial with correct 5-section structure, aligned shell.js behavior, and verified CSS dimensions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-layout-navigation/06-CONTEXT.md
@public/partials/sidebar.html
@public/assets/js/core/shell.js
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure sidebar.html to wireframe 5-section layout</name>
  <files>public/partials/sidebar.html</files>
  <action>
Restructure the sidebar.html partial to match the wireframe's exact 5-section organization. Current state has 6 groups (Preparation, En direct, Apres, Controle, Systeme, Aide). Changes needed:

1. **Section labels** -- Update nav-group-label text to match wireframe exactly:
   - "Preparation" stays as "Preparation" (already correct)
   - "En direct" becomes "Seance en direct" (data-group="live" stays)
   - "Apres" becomes "Apres la seance" (data-group="post" stays)
   - "Controle" stays as "Controle" (already correct)
   - "Systeme" stays as "Systeme" (already correct)

2. **Merge "Aide" group into "Systeme"** -- Move the "Guide & FAQ" nav-item from the Aide group into the Systeme group as the last item. Delete the Aide group button entirely. This produces exactly 5 section groups.

3. **Verify nav items per section** match CONTEXT.md decisions:
   - Preparation: Dashboard, Seances, Creer une seance (keep Members -- it exists in the app and is valid even though not explicitly listed in wireframe sections; removing it would break existing functionality)
   - Seance en direct: Fiche seance, Operateur, Affichage salle
   - Apres la seance: Cloture & PV, Archives
   - Controle: Audit, Statistiques
   - Systeme: Utilisateurs, Parametres, Guide & FAQ

4. **Add notification badge placeholder spans** to nav items that will show pending action counts. Add a `<span class="nav-badge" style="display:none"></span>` inside the following nav-items (after the nav-label span):
   - Seances (data-page="meetings") -- for pending convocations
   - Cloture & PV (data-page="postsession") -- for PV to send
   These will be populated by the notification system in a future phase.

5. Keep all existing attributes: data-page, data-requires-role, href paths, icon SVG references. Do NOT change URLs or role requirements.

6. Keep the existing pin button, brand logo, sidebar-fade/scroll structure, device preview section, and sidebar-footer (theme toggle + version) exactly as they are.
  </action>
  <verify>
    <automated>grep -c 'nav-group-label' public/partials/sidebar.html | grep -q '^5$' && grep -q 'Seance en direct' public/partials/sidebar.html && grep -q 'Apres la seance' public/partials/sidebar.html && ! grep -q 'data-group="help"' public/partials/sidebar.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Sidebar has exactly 5 nav-group sections with correct wireframe labels, Aide merged into Systeme, notification badge placeholders on key items</done>
</task>

<task type="auto">
  <name>Task 2: Align sidebar CSS dimensions and add nav-badge styles</name>
  <files>public/assets/css/design-system.css</files>
  <action>
Verify and align sidebar CSS custom properties and add notification badge styles:

1. **Verify CSS custom properties** in :root -- confirm these values exist (they should from Phase 4):
   - `--sidebar-rail: 58px` (collapsed width)
   - `--sidebar-expanded: 252px` (expanded width)
   If they exist with correct values, no change needed. If missing or different, add/fix them.

2. **Add .nav-badge styles** for the notification count dots on nav items:
```css
.nav-badge {
  display: none;
  min-width: 18px;
  height: 18px;
  padding: 0 5px;
  border-radius: 9px;
  background: var(--color-danger);
  color: #fff;
  font-size: 11px;
  font-weight: 700;
  line-height: 18px;
  text-align: center;
  margin-left: auto;
  flex-shrink: 0;
}
.nav-badge[data-count]:not([data-count="0"]) {
  display: inline-flex;
  align-items: center;
  justify-content: center;
}
/* In collapsed rail, show badge as a small dot positioned on the icon */
.app-sidebar:not(:hover):not(.pinned) .nav-badge[data-count]:not([data-count="0"]) {
  position: absolute;
  top: 4px;
  right: 8px;
  min-width: 8px;
  height: 8px;
  padding: 0;
  font-size: 0;
  border-radius: 50%;
}
```

3. **Ensure .nav-item has position: relative** (needed for the absolute badge dot in rail mode). Check if it already has it; add only if missing.

NOTE: Do NOT add mobile bottom nav CSS (.mobile-bnav, .mobile-bnav-btn) in this task. Those styles are authored in Plan 06-02 alongside the JS that uses them, to avoid cross-plan dependency issues.
  </action>
  <verify>
    <automated>grep -q 'nav-badge' public/assets/css/design-system.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Sidebar CSS uses correct 58px/252px dimensions, nav-badge styles added for notification dots</done>
</task>

</tasks>

<verification>
1. `grep -c 'nav-group-label' public/partials/sidebar.html` returns exactly 5
2. `grep 'nav-group-label' public/partials/sidebar.html` shows: Preparation, Seance en direct, Apres la seance, Controle, Systeme
3. `grep 'data-group="help"' public/partials/sidebar.html` returns no matches (Aide group removed)
4. `grep 'nav-badge' public/assets/css/design-system.css` returns matches for badge styles
</verification>

<success_criteria>
- Sidebar partial has exactly 5 nav-group sections matching wireframe labels
- Guide & FAQ moved from standalone Aide group into Systeme section
- Notification badge CSS ready for future population
- No existing sidebar behavior broken (pin, collapse, active state, role filtering)
</success_criteria>

<output>
After completion, create `.planning/phases/06-layout-navigation/06-01-SUMMARY.md`
</output>
