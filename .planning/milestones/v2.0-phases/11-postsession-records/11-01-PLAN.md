---
phase: 11-postsession-records
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/postsession.htmx.html
  - public/assets/css/postsession.css
  - public/archives.htmx.html
  - public/assets/css/archives.css
autonomous: true
requirements: [POST-01, POST-02, POST-03, ARCH-01, ARCH-02]

must_haves:
  truths:
    - "Post-session page shows a 4-step stepper with done/active/pending visual states and clickable completed steps"
    - "Post-session steps have per-step checklist items with action buttons and status indicators"
    - "Post-session provides PV download, eIDAS signature request (3 levels), and send-to-all functionality"
    - "Archives page displays searchable archive cards with title, date, type badge, resolution summary, attendance"
    - "Archives page has pagination (5 per page) and detail view on click"
  artifacts:
    - path: "public/postsession.htmx.html"
      provides: "Post-session stepper page with no inline styles (except footer pattern)"
      contains: "ps-stepper"
    - path: "public/assets/css/postsession.css"
      provides: "Post-session styles using design tokens"
      contains: "ps-step.done"
    - path: "public/archives.htmx.html"
      provides: "Archives page with no inline styles (except footer pattern)"
      contains: "ag-pagination"
    - path: "public/assets/css/archives.css"
      provides: "Archives styles using design tokens"
      contains: "archive-card"
  key_links:
    - from: "public/postsession.htmx.html"
      to: "public/assets/css/postsession.css"
      via: "link stylesheet"
      pattern: "postsession\\.css"
    - from: "public/archives.htmx.html"
      to: "public/assets/css/archives.css"
      via: "link stylesheet"
      pattern: "archives\\.css"
---

<objective>
Align post-session stepper and archives pages with wireframe v3.19.2 design language.

Purpose: Clean up remaining inline styles on both pages, ensure stepper navigation shows proper done/active/pending states with clickable completed steps, and verify component integration (ag-pagination, ag-badge) on archives page.
Output: Updated postsession.htmx.html, postsession.css, archives.htmx.html, archives.css with full design token usage and zero non-structural inline styles.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-postsession-records/11-CONTEXT.md
@public/postsession.htmx.html
@public/assets/css/postsession.css
@public/assets/js/pages/postsession.js
@public/archives.htmx.html
@public/assets/css/archives.css
@public/assets/js/pages/archives.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Post-session stepper wireframe alignment and inline cleanup</name>
  <files>public/postsession.htmx.html, public/assets/css/postsession.css</files>
  <action>
1. **Inline styles cleanup on postsession.htmx.html:**
   - Find all `style="..."` attributes in the HTML file
   - Footer-level inline styles (logo mark width/height/border-radius, flex:1 spacer, text-decoration:none on links) are acceptable — these match the established footer pattern from Phase 6. Leave them as-is.
   - Any other inline styles (visibility, display, colors, spacing) must be replaced:
     - `style="display:none"` -> `hidden` attribute
     - Other inline styles -> new CSS classes in postsession.css using design tokens
   - Verify no `style=` attributes remain outside the `<footer>` element

2. **Stepper navigation alignment (POST-01):**
   - Verify the `.ps-stepper` renders 4 steps: Verification, Validation, PV, Envoi
   - Each `.ps-step` must have `.done`, `.active`, or default (pending) class states
   - Completed steps must be clickable (wrapped in `<button>` or have `cursor: pointer` + click handler in JS)
   - Add `cursor: pointer` to `.ps-step.done` in CSS if not present
   - Verify the stepper has a pinned navigation footer with back/forward buttons per wireframe

3. **Per-step checklist items (POST-02):**
   - Verify each step panel has checklist items with action buttons and status indicators
   - If checklist items use inline styles for status colors, replace with CSS classes:
     - `.ps-check-done` with color: var(--color-success)
     - `.ps-check-pending` with color: var(--color-warning)
     - `.ps-check-error` with color: var(--color-danger)

4. **Document actions (POST-03):**
   - Verify PV download button exists in Step 3 panel
   - Verify eIDAS signature section renders 3 modes (simple, advanced, qualified) with `.ps-eidas-modes` grid — CSS already exists for this
   - Verify Step 4 has send-to-all functionality
   - If any action buttons use inline styles, move to postsession.css classes
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -c 'style=' public/postsession.htmx.html | xargs -I{} test {} -le 6 && echo "PASS: inline styles within footer limit" || echo "FAIL: too many inline styles"</automated>
  </verify>
  <done>Post-session page has zero inline styles outside footer pattern. Stepper shows 4 steps with done/active/pending states. Completed steps are clickable. Each step has checklist items with status indicators. PV download, eIDAS 3-level signature, and send-to-all buttons are present and styled via CSS classes.</done>
</task>

<task type="auto">
  <name>Task 2: Archives page inline cleanup and component adoption</name>
  <files>public/archives.htmx.html, public/assets/css/archives.css</files>
  <action>
1. **Inline styles cleanup on archives.htmx.html:**
   - Find all `style="..."` attributes
   - Footer-level inline styles (same pattern as postsession) — leave as-is
   - Modal `style="display: none;"` on `.modal-backdrop` and `.modal` elements -> replace with `hidden` attribute
   - Any other inline styles -> CSS classes in archives.css

2. **Component adoption verification (ARCH-01, ARCH-02):**
   - Verify `<ag-pagination>` web component is used for pagination (not raw HTML buttons) — if raw pagination exists, check if it can remain as-is since CONTEXT.md says "alignment only, no new features"
   - Verify archive cards display: title, date, type (with ag-badge for type badges), resolution summary, attendance count
   - Verify search bar exists and is functional
   - Verify pagination is set to 5 per page (check archives.js for page size constant)
   - Verify detail view modal opens on card click

3. **CSS token audit on archives.css:**
   - Scan for any hardcoded colors (hex values, rgb, rgba) — replace with design tokens
   - Scan for any hardcoded spacing (px values not in custom properties) — replace with --space-* tokens
   - Scan for any hardcoded fonts — replace with --font-* tokens
   - Leave `0`, `1px`, `50%`, `100%` as-is (these are structural, not token-worthy)

4. **Dark theme check:**
   - Verify no explicit color values that would break in dark theme
   - All backgrounds should use --color-surface* tokens
   - All text should use --color-text* tokens
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -c 'style=' public/archives.htmx.html | xargs -I{} test {} -le 5 && echo "PASS: inline styles within footer limit" || echo "FAIL: too many inline styles"</automated>
  </verify>
  <done>Archives page has zero inline styles outside footer pattern (modals use hidden attribute). Archive cards show title, date, type badge, resolution summary, attendance. Pagination present with 5/page. Detail modal opens on click. All CSS uses design tokens with no hardcoded colors.</done>
</task>

</tasks>

<verification>
- `grep -c 'style=' public/postsession.htmx.html` returns <= 6 (footer pattern only)
- `grep -c 'style=' public/archives.htmx.html` returns <= 5 (footer pattern only)
- `grep -E '#[0-9a-fA-F]{3,8}' public/assets/css/postsession.css` returns 0 (no hardcoded hex colors)
- `grep -E '#[0-9a-fA-F]{3,8}' public/assets/css/archives.css` returns 0 (no hardcoded hex colors)
- Open postsession.htmx.html in browser: stepper renders 4 labeled steps with visual state indicators
- Open archives.htmx.html in browser: cards display with type badges, pagination works
</verification>

<success_criteria>
- Post-session stepper shows 4 steps (Verification, Validation, PV, Envoi) with done/active/pending states
- Completed steps are clickable for back-navigation
- Each step has checklist items with status indicators
- PV download, eIDAS signature (3 levels), and send-to-all are present
- Archives cards show title, date, type, resolution summary, attendance
- Archives pagination set to 5/page with detail modal on click
- Zero non-structural inline styles on both pages
- All CSS values use design tokens
</success_criteria>

<output>
After completion, create `.planning/phases/11-postsession-records/11-01-SUMMARY.md`
</output>
