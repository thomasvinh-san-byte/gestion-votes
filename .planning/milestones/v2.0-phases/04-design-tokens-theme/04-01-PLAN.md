---
phase: 04-design-tokens-theme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
  - .planning/phases/04-design-tokens-theme/validate-tokens.sh
autonomous: true
requirements: [DS-01, DS-03, DS-05]

must_haves:
  truths:
    - "All :root color tokens match wireframe v3.19.2 hex/rgba values exactly"
    - "Surface elevation tokens follow bg < surface < surface-alt < surface-raised < glass hierarchy"
    - "Border radius values match wireframe (6px/8px/10px/999px)"
    - "Shadow tokens match wireframe values"
    - "Font stacks use Bricolage Grotesque (body), Fraunces (display), JetBrains Mono (data)"
    - "Validation script confirms all light theme tokens match wireframe reference"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Light theme design tokens aligned with wireframe"
      contains: "--color-surface-alt"
    - path: ".planning/phases/04-design-tokens-theme/validate-tokens.sh"
      provides: "Automated token comparison script"
  key_links:
    - from: "public/assets/css/design-system.css"
      to: "ag_vote_wireframe.html"
      via: "Token values must match"
      pattern: "--color-bg.*#EDECE6"
---

<objective>
Align all light theme (:root) CSS custom properties in design-system.css with the exact values from wireframe v3.19.2, and create a validation script to verify correctness.

Purpose: Foundation for the entire v2.0 UI redesign -- every subsequent phase depends on correct design tokens.
Output: Updated design-system.css light theme tokens + validate-tokens.sh script.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-tokens-theme/04-CONTEXT.md
@.planning/phases/04-design-tokens-theme/04-VALIDATION.md
@ag_vote_wireframe.html (lines 13-78: light theme tokens)
@public/assets/css/design-system.css (lines 79-305: :root block)

<interfaces>
<!-- The wireframe uses short token names (--bg, --accent, --border) while the codebase uses --color-* prefix.
     Per CONTEXT.md locked decision: KEEP the --color-* prefix convention. Do NOT rename tokens.
     Map wireframe names to codebase names when comparing values. -->

Wireframe-to-codebase token name mapping:
  --bg             -> --color-bg
  --surface        -> --color-surface
  --surface-alt    -> --color-bg-subtle (RENAME VALUE to match wireframe's --surface-alt: #E5E3D8)
  --surface-raised -> --color-surface-raised
  --glass          -> --color-glass
  --border         -> --color-border
  --border-soft    -> --color-border-subtle
  --border-dash    -> --color-border-dash
  --accent         -> --color-primary (per locked decision: keep --color-primary name)
  --accent-hover   -> --color-primary-hover
  --accent-light   -> --color-primary-subtle
  --accent-dark    -> --color-primary-active
  --accent-glow    -> --color-primary-glow
  --text-dark      -> --color-text-dark
  --text           -> --color-text
  --text-muted     -> --color-text-muted
  --text-light     -> --color-text-light
  --danger         -> --color-danger
  --danger-bg      -> --color-danger-subtle
  --danger-border  -> --color-danger-border
  --success        -> --color-success
  --success-bg     -> --color-success-subtle
  --success-border -> --color-success-border
  --warn           -> --color-warning
  --warn-bg        -> --color-warning-subtle
  --warn-border    -> --color-warning-border
  --purple         -> --color-purple
  --purple-bg      -> --color-purple-subtle
  --purple-border  -> --color-purple-border
  --font           -> --font-sans
  --font-display   -> --font-display
  --font-mono      -> --font-mono
  --radius-lg      -> --radius-lg (CHANGE from 16px to 10px per wireframe)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create token validation script</name>
  <files>.planning/phases/04-design-tokens-theme/validate-tokens.sh</files>
  <action>
Create a bash script that extracts token values from design-system.css and compares them against wireframe reference values. The script should:

1. Define two associative arrays of expected values: one for `:root` (light theme) and one for `[data-theme="dark"]`.
2. For each expected token-value pair, grep the relevant CSS block in design-system.css and compare.
3. Report PASS/FAIL per token with expected vs actual values on failure.
4. Support `--full` flag (checks all tokens) vs default quick mode (checks 10 key tokens: --color-bg, --color-surface, --color-primary, --color-danger, --color-text, --shadow-xs, --radius-lg, --font-sans, --sidebar-bg, --tag-bg).
5. Exit 0 if all pass, exit 1 if any fail.

Key light theme reference values (from wireframe):
- --color-bg: #EDECE6
- --color-surface: #FAFAF7
- --color-surface-raised: #FFFFFF
- --color-glass: rgba(250, 250, 247, 0.95) or rgba(250,250,247,.95)
- --color-border: #CDC9BB
- --color-primary: #1650E0
- --color-danger: #C42828
- --color-success: #0B7A40
- --color-warning: #B56700
- --color-purple: #5038C0
- --color-text: #52504A
- --color-text-dark: #151510
- --shadow-xs: contains rgba(21,21,16,
- --radius-lg: contains 10px (wireframe) -- NOTE: will fail until Task 2 updates it
- --font-sans: contains Bricolage Grotesque
- --sidebar-bg: #0C1018

Key dark theme reference values:
- --color-bg: #0B0D10
- --color-surface: #141820
- --color-primary: #3D7EF8
- --color-danger: #E85454
- --color-text: #7A8499
- --shadow-xs: contains rgba(0,0,0,

The script must handle whitespace variations (the CSS file uses spaces while wireframe is minified). Use pattern matching (grep) rather than exact string comparison for rgba values. Target file path: public/assets/css/design-system.css (relative to repo root).
  </action>
  <verify>
    <automated>bash .planning/phases/04-design-tokens-theme/validate-tokens.sh 2>&1 | head -30</automated>
  </verify>
  <done>Script runs without errors, reports token comparison results (some may fail before Task 2 runs)</done>
</task>

<task type="auto">
  <name>Task 2: Align light theme tokens with wireframe v3.19.2</name>
  <files>public/assets/css/design-system.css</files>
  <action>
Read the full `:root` block in design-system.css (lines 79-305) and compare every value against the wireframe reference (ag_vote_wireframe.html lines 13-78). Apply the --color-* prefix mapping from the interfaces block above. Make these specific corrections:

**Surface elevation (DS-03):**
- Add `--color-surface-alt: #E5E3D8;` as a NEW token (wireframe defines --surface-alt but codebase only has --color-bg-subtle with the same value -- add the explicit surface-alt token for semantic clarity, keep --color-bg-subtle as-is)
- Verify --color-bg: #EDECE6 (should already match)
- Verify --color-surface: #FAFAF7 (should already match)
- Verify --color-surface-raised: #FFFFFF (should already match)
- Verify --color-glass: rgba(250, 250, 247, 0.95) (should already match)

**Border radius (DS-01):**
- Change `--radius-lg: 1rem;` (16px) to `--radius-lg: 0.625rem;` (10px) to match wireframe's `--radius-lg:10px`
- Remove `--radius-xl: 1.25rem;` (20px) -- wireframe does not define this token
- Remove `--radius-md: 0.75rem;` (12px) -- wireframe only has sm(6px), default(8px), lg(10px), full(999px)
- Verify --radius-sm: 0.375rem (6px) matches wireframe
- Verify --radius: 0.5rem (8px) matches wireframe
- Change --radius-full from 9999px to 999px to match wireframe

**Shadows (DS-01):**
- Light theme shadows already match wireframe. Verify no differences.
- The codebase has `--shadow` (without suffix) and `--shadow-xl` which wireframe doesn't define -- keep these as useful extras, they don't conflict.

**Transitions (DS-01):**
- Wireframe uses `--tr:.15s ease` as a single shorthand. The codebase uses separate duration/easing tokens. Per Claude's discretion: keep the granular tokens (--duration-fast, --duration-normal, etc.) since they're more flexible, BUT add `--transition: 150ms ease` as an alias matching the wireframe's `--tr` shorthand. This lets components use either approach.

**Typography (DS-05):**
- Font stacks already match. Verify:
  - --font-sans contains 'Bricolage Grotesque'
  - --font-display contains 'Fraunces'
  - --font-mono contains 'JetBrains Mono'

**Colors already matching (verify, do not change):**
- --color-bg, --color-surface, --color-surface-raised, --color-glass
- --color-text (#52504A), --color-text-dark (#151510), --color-text-muted (#857F72), --color-text-light (#B5B0A0)
- --color-border (#CDC9BB), --color-border-subtle (#DEDAD0), --color-border-dash (#BCB7A5)
- --color-primary (#1650E0), hover (#1140C0), active (#0C30A0), subtle (#EBF0FF), glow (rgba(22,80,224,0.12))
- All semantic colors (danger, success, warning, purple) and sidebar tokens

**Sidebar tokens (already match wireframe):** Verify no changes needed.

**Tag tokens (already match wireframe):** Verify no changes needed.

**Primary/Accent aliasing (Claude's discretion from CONTEXT.md):**
- Keep both --color-primary and --color-accent as separate tokens
- --color-primary = #1650E0 (the wireframe's "accent" blue)
- --color-accent = #5038C0 (currently aliased to purple -- keep as-is since it's used in components)
- Do NOT alias one to the other -- they serve different semantic purposes in the existing codebase

**IMPORTANT: Do NOT change any tokens outside the :root block. Dark theme is Plan 02.**
**IMPORTANT: Do NOT touch anything below the `:root` closing brace except the dark theme block adjustments in Plan 02.**

After making changes, run the validation script to confirm light theme tokens pass.
  </action>
  <verify>
    <automated>bash .planning/phases/04-design-tokens-theme/validate-tokens.sh 2>&1 | tail -5</automated>
  </verify>
  <done>All light theme tokens match wireframe v3.19.2 values. --color-surface-alt added. Border radius aligned to 6/8/10/999. Validation script reports all light theme tokens PASS.</done>
</task>

</tasks>

<verification>
- `bash .planning/phases/04-design-tokens-theme/validate-tokens.sh` reports all light theme tokens PASS
- `grep "radius-lg" public/assets/css/design-system.css` shows 10px (0.625rem)
- `grep "color-surface-alt" public/assets/css/design-system.css` finds the new token
- `grep "radius-xl\|radius-md" public/assets/css/design-system.css` in the :root block returns nothing (removed)
- CSS file still parses correctly (no syntax errors)
</verification>

<success_criteria>
1. All :root CSS custom property values in design-system.css match wireframe v3.19.2 reference
2. --color-surface-alt token exists for elevation hierarchy
3. Border radius scale is 6px/8px/10px/999px (matching wireframe)
4. Validation script exists and confirms all light theme tokens pass
5. No changes to dark theme block, typography section, or component styles
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-tokens-theme/04-01-SUMMARY.md`
</output>
