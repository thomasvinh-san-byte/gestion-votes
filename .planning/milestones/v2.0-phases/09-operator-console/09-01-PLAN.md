---
phase: 09-operator-console
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/operator.htmx.html
  - public/assets/css/operator.css
  - public/partials/operator-exec.html
autonomous: true
requirements: [OPR-01, OPR-02, OPR-03]

must_haves:
  truths:
    - "Execution mode header shows live dot, session title, running timer (HH:MM:SS), room display button, and close session button"
    - "KPI strip displays Presents (x/y), Quorum (% + check), Ont vote (x/y), Resolution (x/y) with compact monospace values and uppercase labels"
    - "Progress track shows 5 horizontal color-coded segments (green=voted, blue=voting, gray=pending) and segments are clickable for voted/voting resolutions"
    - "All inline styles in operator.htmx.html are replaced with CSS classes"
  artifacts:
    - path: "public/operator.htmx.html"
      provides: "Operator page with zero inline styles, execution header, KPI strip, progress track HTML"
      contains: "op-exec-header"
    - path: "public/assets/css/operator.css"
      provides: "CSS classes for execution header, op-kpi-strip, progress track, inline style replacements"
      contains: "op-kpi-strip"
    - path: "public/partials/operator-exec.html"
      provides: "Updated execution partial with new KPI strip HTML structure"
      contains: "op-kpi-strip"
  key_links:
    - from: "public/operator.htmx.html"
      to: "public/assets/css/operator.css"
      via: "CSS class references"
      pattern: "op-kpi-strip|op-exec-header"
---

<objective>
Refactor operator page inline styles to CSS classes, redesign execution mode header with live indicators, create dedicated op-kpi-strip with compact density, and make progress track segments clickable.

Purpose: Establish the visual foundation for the operator execution console -- compact header, live KPI strip, and resolution progress track per wireframe v3.19.2.
Output: Clean HTML (zero inline styles), new execution header, KPI strip, and progress track CSS/HTML.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-operator-console/09-CONTEXT.md
@public/operator.htmx.html
@public/assets/css/operator.css
@public/partials/operator-exec.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline styles cleanup + execution header HTML/CSS</name>
  <files>public/operator.htmx.html, public/assets/css/operator.css</files>
  <action>
**Part A: Inline style cleanup (entire file)**

Replace ALL `style="..."` attributes in operator.htmx.html with CSS classes in operator.css. There are ~19 inline styles to replace:

1. `nav.tabs-nav style="display: none;"` -- already handled by JS toggle, remove the inline style (JS sets hidden attribute or display)
2. `.hub-checklist-bar-fill style="width: 0%"` -- keep as-is (dynamic width set by JS, initial 0% is fine as inline)
3. `#addResolutionForm style="display: none;"` -- replace with `hidden` attribute
4. `#launchBanner style="display:none;"` -- replace with `hidden` attribute
5. `#invEngagement style="display:none;"` -- replace with `hidden` attribute
6. `#invitationsOptions style="display:none;"` -- replace with `hidden` attribute
7. `#scheduleGroup style="display:none;"` -- replace with `hidden` attribute
8. `#dashboardCard style="display:none;"` -- replace with `hidden` attribute
9. `#dashCurrentMotion style="display:none;"` -- replace with `hidden` attribute
10. `#dashReadySign style="display:none;"` -- replace with `hidden` attribute
11. `#dashNotReadySign style="display:none;"` -- replace with `hidden` attribute
12. `#devicesCard style="display:none;"` -- replace with `hidden` attribute
13. Footer logo `style="font-size:12px;gap:6px;"` -- add CSS class `.footer-logo`
14. Footer logo-mark `style="width:18px;height:18px;border-radius:3px;font-size:8px;"` -- add CSS class `.footer-logo-mark`
15. Footer spacer `style="flex:1"` -- replace with CSS class `.flex-spacer` (add to operator.css)
16. Footer help/accessibility links `style="text-decoration:none;"` -- add CSS class `.footer-link`
17. `#launchModal style="display:none;"` -- replace with `hidden` attribute
18. Tour progress bar `style="width: 12.5%"` -- keep as-is (dynamic width)
19. `#execCloseBanner style="display:none;"` in operator-exec.html -- replace with `hidden` attribute
20. Avance tab paper count inputs `style="flex:1;margin:0"` -- add CSS class `.op-paper-field`
21. Vote bar labels `style="color:var(--color-success)"` etc -- these use semantic colors inline, replace with existing bar-label classes: `.op-bar-label.for`, `.op-bar-label.against`, `.op-bar-label.abstain`
22. `#activeSpeakerState style="display:none;"` in operator-live-tabs.html -- replace with `hidden` attribute
23. `#activeVotePanel style="display: none;"` in operator-live-tabs.html -- replace with `hidden` attribute
24. `#execSpeechActions style="display:none;"` in operator-exec.html -- replace with `hidden` attribute

For elements currently toggled by JS using `.style.display`, verify the JS uses `hidden` attribute or `Shared.show()/Shared.hide()` which already work with the `hidden` attribute. If JS sets `.style.display` directly, the `hidden` attribute approach still works because `Shared.show()` removes `hidden` and `Shared.hide()` sets it.

Add to operator.css:
```css
.footer-logo { font-size: 12px; gap: 6px; }
.footer-logo-mark { width: 18px; height: 18px; border-radius: 3px; font-size: 8px; }
.flex-spacer { flex: 1; }
.footer-link { text-decoration: none; }
.op-paper-field { flex: 1; margin: 0; }
.op-bar-label.for { color: var(--color-success); }
.op-bar-label.against { color: var(--color-danger); }
.op-bar-label.abstain { color: var(--color-text-muted); }
```

**Part B: Execution header redesign (OPR-01)**

In operator.htmx.html, add a new execution header section inside `#viewExec` (or at the top of the execution area) that replaces the meeting-bar when in execution mode. Structure:

```html
<div class="op-exec-header" id="opExecHeader" hidden>
  <div class="op-exec-header-left">
    <span class="op-live-dot" aria-label="Session en cours"></span>
    <h1 class="op-exec-title" id="opExecTitle">--</h1>
  </div>
  <div class="op-exec-header-right">
    <span class="op-live-chrono" id="opExecTimer">00:00:00</span>
    <a class="btn btn-sm btn-secondary" id="opBtnRoomDisplay" href="/public.htmx.html" target="_blank" data-meeting-link>
      <svg class="icon icon-text" aria-hidden="true"><use href="/assets/icons.svg#icon-monitor"></use></svg> Projection
    </a>
    <button class="btn btn-sm btn-danger" id="opBtnCloseSession">
      <svg class="icon icon-text" aria-hidden="true"><use href="/assets/icons.svg#icon-square"></use></svg> Cloturer
    </button>
  </div>
</div>
```

Add CSS for `.op-exec-header`:
- `display: flex; align-items: center; justify-content: space-between; padding: 0.5rem 1.5rem;`
- `background: var(--color-surface); border-bottom: 2px solid var(--color-success);` (green border = live state per wireframe)
- `.op-exec-title`: `font-size: var(--text-lg); font-weight: var(--font-bold); margin: 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;`
- `.op-live-dot` already exists in CSS (pulsing green animation) -- reuse it
- `.op-live-chrono` already exists -- it uses `font-family: var(--font-mono); font-size: var(--text-xl);`
- `.op-exec-header-right`: `display: flex; align-items: center; gap: 0.75rem;`
- `.op-exec-header-left`: `display: flex; align-items: center; gap: 0.75rem; min-width: 0; flex: 1;`

Per CONTEXT.md decision: session selector visibility in execution mode is Claude's discretion -- hide the meeting-bar select in execution mode for a focused view.
  </action>
  <verify>
    <automated>grep -c 'style="' public/operator.htmx.html | xargs -I{} test {} -le 3 && echo "PASS: inline styles reduced" || echo "FAIL: too many inline styles remain"</automated>
  </verify>
  <done>All inline styles in operator.htmx.html replaced with CSS classes (except dynamic width values set by JS). Execution header HTML and CSS exists with live dot, title, timer, room display button, close session button.</done>
</task>

<task type="auto">
  <name>Task 2: KPI strip redesign + progress track clickable segments</name>
  <files>public/partials/operator-exec.html, public/assets/css/operator.css</files>
  <action>
**Part A: KPI strip redesign (OPR-02)**

Replace the existing `.exec-kpi-strip` HTML in operator-exec.html with a new `.op-kpi-strip` structure per CONTEXT.md decision (dedicated class, compact density, monospace values, uppercase labels -- NOT reusing .hub-kpi or .result-card):

```html
<div class="op-kpi-strip" id="opKpiStrip">
  <div class="op-kpi-item">
    <span class="op-kpi-label">PRESENTS</span>
    <span class="op-kpi-value" id="opKpiPresent">0<span class="op-kpi-total">/0</span></span>
  </div>
  <div class="op-kpi-item">
    <span class="op-kpi-label">QUORUM</span>
    <span class="op-kpi-value" id="opKpiQuorum">0%</span>
    <span class="op-kpi-check" id="opKpiQuorumCheck" hidden>
      <svg class="icon icon-xs" aria-hidden="true"><use href="/assets/icons.svg#icon-check-circle"></use></svg>
    </span>
  </div>
  <div class="op-kpi-item">
    <span class="op-kpi-label">ONT VOTE</span>
    <span class="op-kpi-value" id="opKpiVoted">0<span class="op-kpi-total">/0</span></span>
  </div>
  <div class="op-kpi-item">
    <span class="op-kpi-label">RESOLUTION</span>
    <span class="op-kpi-value" id="opKpiResolution">0<span class="op-kpi-total">/0</span></span>
  </div>
</div>
```

Add CSS for `.op-kpi-strip` (new dedicated class, NOT extending .exec-kpi-strip):
```css
.op-kpi-strip {
  display: flex;
  align-items: center;
  gap: 0;
  padding: 0.5rem 1.5rem;
  background: var(--color-bg-subtle);
  border-bottom: 1px solid var(--color-border);
}
.op-kpi-item {
  display: flex;
  align-items: center;
  gap: 0.375rem;
  padding: 0.25rem 1rem;
  border-right: 1px solid var(--color-border);
}
.op-kpi-item:last-child { border-right: none; }
.op-kpi-label {
  font-size: var(--text-xs);
  font-weight: var(--font-semibold);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--color-text-muted);
}
.op-kpi-value {
  font-family: var(--font-mono);
  font-size: var(--text-base);
  font-weight: var(--font-bold);
  font-variant-numeric: tabular-nums;
}
.op-kpi-total {
  font-weight: var(--font-normal);
  color: var(--color-text-muted);
}
.op-kpi-check {
  color: var(--color-success);
}
```

Also keep the existing `.exec-kpi-strip` CSS for backward compatibility (other JS references may still use the old IDs temporarily). The old exec-kpi-strip HTML will be replaced by op-kpi-strip.

Keep the existing `#opTags` section (quorum, correspondance, procurations tags) below the KPI strip -- per CONTEXT.md: "Tags row always visible below KPI strip".

**Part B: Progress track clickable segments (OPR-03)**

The `.op-resolution-progress` and `.op-progress-segment` CSS already exists with color-coding (voted=green, active=blue, skipped=gray). Update the HTML to:

1. Ensure segments have `role="button"` and `tabindex="0"` for keyboard accessibility
2. Add `data-motion-id` attribute to each segment for click navigation
3. Add `cursor: pointer` for voted/voting segments (already has `:hover` styles)
4. Add `aria-label` with resolution title on each segment

The segments are already rendered dynamically by JS. The CSS is already present. Verify the `.op-progress-segment` has:
- `.voted { background: var(--color-success); }`
- `.active { background: var(--color-primary); }` (blue = voting)
- Default gray for pending
- `cursor: pointer` on `.voted` and `.active`
- `pointer-events: none` on pending segments (non-clickable)

Add to CSS if not present:
```css
.op-progress-segment.voted,
.op-progress-segment.active {
  cursor: pointer;
}
.op-progress-segment:not(.voted):not(.active) {
  cursor: default;
  pointer-events: none;
}
```

Remove the `#execCloseBanner style="display:none;"` inline style and replace with `hidden` attribute.

Mobile responsive: at `max-width: 768px`, `.op-kpi-strip` wraps to 2x2 grid.
  </action>
  <verify>
    <automated>grep -c 'op-kpi-strip' public/partials/operator-exec.html | xargs -I{} test {} -ge 1 && grep -c 'op-kpi-strip' public/assets/css/operator.css | xargs -I{} test {} -ge 1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>KPI strip uses new .op-kpi-strip with compact monospace values and uppercase labels. Progress track segments are clickable for voted/voting resolutions with proper accessibility attributes. Tags row remains below KPI strip.</done>
</task>

</tasks>

<verification>
- `grep -c 'style="' public/operator.htmx.html` returns 3 or fewer (only dynamic width values)
- `.op-exec-header` class exists in operator.css with green border
- `.op-kpi-strip` class exists in operator.css with monospace font family
- `.op-progress-segment.voted` and `.op-progress-segment.active` have cursor:pointer in CSS
- No `style="display:none"` remains in operator.htmx.html (replaced with `hidden` attribute)
</verification>

<success_criteria>
Operator page HTML has zero unnecessary inline styles. Execution header shows live dot + title + timer + buttons. KPI strip uses dedicated compact design with monospace values. Progress track segments are clickable.
</success_criteria>

<output>
After completion, create `.planning/phases/09-operator-console/09-01-SUMMARY.md`
</output>
