---
phase: 15-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/icons.svg
  - public/assets/js/core/shell.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "All SVG icon references render a visible icon (no blank/invisible placeholders)"
    - "Notification panel 'Voir tout' link navigates to admin page without dead ?tab=notifications parameter"
  artifacts:
    - path: "public/assets/icons.svg"
      provides: "4 missing Lucide icon symbols"
      contains: "icon-help-circle"
    - path: "public/assets/icons.svg"
      provides: "icon-pause symbol"
      contains: "icon-pause"
    - path: "public/assets/icons.svg"
      provides: "icon-smartphone symbol"
      contains: "icon-smartphone"
    - path: "public/assets/icons.svg"
      provides: "icon-plus-circle symbol"
      contains: "icon-plus-circle"
  key_links:
    - from: "public/*.htmx.html"
      to: "public/assets/icons.svg"
      via: "SVG use href"
      pattern: "icons\\.svg#icon-(help-circle|pause|smartphone|plus-circle)"
    - from: "public/assets/js/core/shell.js"
      to: "public/admin.htmx.html"
      via: "notification link href"
      pattern: "admin\\.htmx\\.html"
---

<objective>
Add 4 missing Lucide SVG icons to the icon sprite and fix the notification panel query parameter mismatch.

Purpose: Eliminate blank icon placeholders across 12+ icon usages on 10 pages, and fix the dead ?tab=notifications query param in shell.js.
Output: Updated icons.svg with 4 new symbols; shell.js notification link cleaned up.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tech-debt-cleanup/15-CONTEXT.md
@public/assets/icons.svg
@public/assets/js/core/shell.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 missing Lucide icons to SVG sprite</name>
  <files>public/assets/icons.svg</files>
  <action>
Add 4 new `<symbol>` elements to public/assets/icons.svg, sourcing paths from the Lucide icon set. Each symbol must use the same attributes as existing icons: viewBox="0 0 24 24", fill="none", stroke="currentColor", stroke-width="2", stroke-linecap="round", stroke-linejoin="round".

Icons to add:

1. `icon-help-circle` — Lucide "help-circle": circle cx=12 cy=12 r=10, path "M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3", line x1=12 x2=12.01 y1=17 y2=17

2. `icon-pause` — Lucide "pause" (outline/stroke style, two vertical bars): rect x=6 y=4 width=4 height=16 rx=1, rect x=14 y=4 width=4 height=16 rx=1

3. `icon-smartphone` — Lucide "smartphone": rect width=14 height=20 x=5 y=2 rx=2, line x1=12 x2=12.01 y1=18 y2=18

4. `icon-plus-circle` — Lucide "plus-circle": circle cx=12 cy=12 r=10, path "M8 12h8", path "M12 8v8"

Place the new symbols in a logical section. Add a comment `<!-- Tech Debt Cleanup -->` before the group. Verify the Lucide SVG paths are accurate by cross-referencing https://lucide.dev.
  </action>
  <verify>
    <automated>grep -c 'id="icon-help-circle"\|id="icon-pause"\|id="icon-smartphone"\|id="icon-plus-circle"' public/assets/icons.svg | grep -q '^4$' && echo "PASS: 4 icons found" || echo "FAIL: missing icons"</automated>
  </verify>
  <done>icons.svg contains all 4 new symbols with correct Lucide SVG paths. grep confirms 4 matches for the 4 icon IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Fix notification panel query parameter</name>
  <files>public/assets/js/core/shell.js</files>
  <action>
In shell.js, the notification panel "Voir tout" link points to `/admin.htmx.html?tab=notifications` but admin.js never reads the `tab` query parameter, making it dead code.

Fix: Remove the `?tab=notifications` query parameter from the href in shell.js (line ~604), changing the link to just `/admin.htmx.html`. This is the simpler approach — the parameter is unused and removing it avoids adding unnecessary URL-parsing code to admin.js.

The link is inside the notifPanel innerHTML assignment, in the anchor tag with class "notif-footer". Change:
`href="/admin.htmx.html?tab=notifications"` to `href="/admin.htmx.html"`
  </action>
  <verify>
    <automated>grep -c 'tab=notifications' public/assets/js/core/shell.js | grep -q '^0$' && echo "PASS: query param removed" || echo "FAIL: query param still present"</automated>
  </verify>
  <done>shell.js notification "Voir tout" link no longer includes the dead ?tab=notifications query parameter.</done>
</task>

</tasks>

<verification>
- All 4 icon IDs exist in icons.svg: `grep -E 'id="icon-(help-circle|pause|smartphone|plus-circle)"' public/assets/icons.svg | wc -l` returns 4
- No remaining ?tab=notifications in shell.js: `grep 'tab=notifications' public/assets/js/core/shell.js` returns nothing
- SVG sprite file is valid XML (no unclosed tags): `python3 -c "import xml.etree.ElementTree as ET; ET.parse('public/assets/icons.svg'); print('Valid XML')"` succeeds
</verification>

<success_criteria>
- 4 Lucide icon symbols (help-circle, pause, smartphone, plus-circle) present in icons.svg with correct SVG paths
- No dead query parameter in shell.js notification link
- icons.svg remains valid XML
</success_criteria>

<output>
After completion, create `.planning/phases/15-tech-debt-cleanup/15-01-SUMMARY.md`
</output>
