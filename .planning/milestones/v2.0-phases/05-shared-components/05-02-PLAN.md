---
phase: 05-shared-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/js/components/ag-badge.js
  - public/assets/js/components/ag-mini-bar.js
  - public/assets/js/components/ag-popover.js
  - public/assets/css/design-system.css
  - public/assets/js/core/shared.js
autonomous: true
requirements: [COMP-04, COMP-05, COMP-06, COMP-07]

must_haves:
  truths:
    - "Tag/badge renders correctly in danger, success, warn, accent, and purple variants with semantic design tokens"
    - "Empty state displays icon + title + subtitle + CTA button with wireframe-aligned styling"
    - "Progress bar and mini bar chart render with tokenized colors and correct border-radius"
    - "Popover menus display with wireframe shadow, radius, and background tokens"
  artifacts:
    - path: "public/assets/js/components/ag-badge.js"
      provides: "Badge with all wireframe variants using design tokens"
      contains: "color-danger-subtle"
    - path: "public/assets/js/components/ag-mini-bar.js"
      provides: "Mini bar chart with tokenized styles"
      contains: "color-bg-subtle"
    - path: "public/assets/js/components/ag-popover.js"
      provides: "Popover with wireframe-aligned shadow and radius tokens"
      contains: "color-surface-raised"
    - path: "public/assets/css/design-system.css"
      provides: "Empty state CSS with wireframe-aligned tokens"
      contains: "empty-state"
  key_links:
    - from: "ag-badge.js"
      to: "design-system.css tokens"
      via: "CSS custom properties in shadow DOM"
      pattern: "--color-.*-subtle"
    - from: "shared.js emptyState()"
      to: "design-system.css .empty-state"
      via: "HTML class names"
      pattern: "empty-state-icon|empty-state-title"
---

<objective>
Align ag-badge, ag-mini-bar, ag-popover, and empty state components with wireframe v3.19.2 design tokens.

Purpose: These utility components appear across nearly every page (badges in tables, empty states in lists, popovers for help, progress bars in operator). Tokenizing them ensures visual consistency across all consuming pages.

Output: Four updated component files + empty-state CSS with wireframe-aligned tokens and dark theme compatibility.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-shared-components/05-CONTEXT.md
@.planning/phases/04-design-tokens-theme/04-01-SUMMARY.md

<interfaces>
<!-- Existing component APIs (do NOT change) -->

ag-badge: <ag-badge variant="success|danger|warning|info|primary|accent|purple|live|draft|scheduled|closed|validated" icon="name" pulse size="sm|md">text</ag-badge>

ag-mini-bar: <ag-mini-bar values='[80,20]' colors='["var(--color-success)","var(--color-danger)"]' height="8"></ag-mini-bar>

ag-popover: <ag-popover title="..." content="..." position="top|bottom|left|right" trigger="hover|click|focus" width="280">
  <button slot="trigger">?</button>
  <div slot="content">...</div>
</ag-popover>

Shared.emptyState({ icon: 'name', title: 'Title', description: 'Desc', actionHtml: '<button>CTA</button>' })
Returns HTML string using classes: .empty-state, .empty-state-icon, .empty-state-title, .empty-state-description

<!-- Key design tokens -->
Surfaces: --color-surface, --color-surface-raised, --color-bg-subtle
Semantic: --color-danger-subtle, --color-success-subtle, --color-warning-subtle, --color-primary-subtle, --color-purple-subtle
Radius: --radius-sm (0.375rem), --radius (0.5rem), --radius-lg (0.625rem), --radius-full (999px)
Shadows: --shadow-md, --shadow-lg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align ag-badge and ag-popover on wireframe tokens</name>
  <files>public/assets/js/components/ag-badge.js, public/assets/js/components/ag-popover.js</files>
  <action>
**ag-badge.js** — Mostly well-tokenized already. Targeted fixes:
1. Replace `--tag-bg, #e8e7e2` with `var(--color-bg-subtle, #e8e7e2)` in the default `.badge` and `draft` variant (replace proprietary --tag-bg with design system token)
2. Replace `--tag-text, #6b6b6b` with `var(--color-text-muted, #95a3a4)` in the default `.badge` and `draft` variant
3. Ensure "warn" works as an alias — add `:host([variant="warn"]) .badge` rule pointing to same tokens as `warning` variant
4. Verify `--radius-full` usage (line 60) — already correct at `var(--radius-full, 9999px)`, update fallback to `999px` to match Phase 4 value
5. No JS logic changes needed — variants and API are already correct

**ag-popover.js** — Style-only changes per user decision (do NOT touch JS positioning logic):
1. Replace `.popover` `border-radius: 8px` with `var(--radius-lg, 0.625rem)`
2. Replace `.popover` `background: var(--color-surface, #fff)` with `var(--color-surface-raised, #fff)` (popover is elevated)
3. Replace `box-shadow: var(--shadow-lg, ...)` — keep using `--shadow-lg` (correct for popovers)
4. Replace arrow `background: var(--color-surface, #fff)` with `var(--color-surface-raised, #fff)` to match popover body
5. Replace `.default-trigger` `background: var(--color-bg-subtle, #f3f4f6)` fallback to `#e8e7e2` (Phase 4 value)
6. Replace `.default-trigger` `border: 1px solid var(--color-border, #d1d5db)` fallback to `#d5dbd2` (Phase 4 value)
7. Replace `.popover-text` `color: var(--color-text-muted, #6b7280)` fallback to `#95a3a4` (Phase 4 value)
8. Replace all `#1f2937` fallbacks with `#1a1a1a` and `#6b7280` with `#95a3a4` to match Phase 4 tokens

Dark theme: Both components use CSS custom properties exclusively — dark theme inherits automatically.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -c "tag-bg\|tag-text" public/assets/js/components/ag-badge.js && grep -c "color-surface-raised" public/assets/js/components/ag-popover.js && grep "radius-lg" public/assets/js/components/ag-popover.js | head -2</automated>
  </verify>
  <done>ag-badge uses design system tokens (no --tag-bg/--tag-text), has "warn" alias variant, --radius-full fallback is 999px. ag-popover uses --color-surface-raised, --radius-lg, and all fallback hex values match Phase 4 tokens. No JS positioning logic changed in ag-popover.</done>
</task>

<task type="auto">
  <name>Task 2: Align empty state CSS, ag-mini-bar, and add standard progress bar</name>
  <files>public/assets/css/design-system.css, public/assets/js/components/ag-mini-bar.js</files>
  <action>
**Empty state in design-system.css** (lines ~1702-1730) — COMP-04:
1. Replace `var(--space-12)` / `var(--space-4)` etc. with explicit values if those space tokens don't exist in :root — check first with `grep "space-12\|space-4\|space-2\|space-6" design-system.css | head`. If they exist, leave them. If not, replace with: `--space-12` -> `3rem`, `--space-4` -> `1rem`, `--space-2` -> `0.5rem`, `--space-6` -> `1.5rem`
2. Replace `var(--text-lg)` and `var(--text-sm)` with explicit wireframe values if those typography tokens don't exist. Check first: `grep "text-lg\|text-sm\b" design-system.css | head`. If not in :root, use: `--text-lg` -> `1.125rem`, `--text-sm` -> `0.875rem`
3. Replace `var(--font-semibold)` with `600` if token not in :root
4. Replace `.empty-state-description color: var(--color-text-secondary)` with `var(--color-text-muted)` (--color-text-secondary may not be a Phase 4 token; --color-text-muted is canonical)
5. Add `.empty-state .btn` styling for CTA button: `margin-top: 1rem; padding: 8px 20px; border-radius: var(--radius); background: var(--color-primary); color: var(--color-text-inverse); font-weight: 600; font-size: 13px; border: none; cursor: pointer; transition: background var(--transition);`
6. Add `.empty-state .btn:hover` with `background: var(--color-primary-hover);`

Note: Per Claude's Discretion, keep Shared.emptyState() as utility function (no web component conversion) — it's used in 12+ files and works well.

**ag-mini-bar.js** — COMP-06:
1. Already well-tokenized. Replace `background: var(--color-bg-subtle, #e8e7e2)` — verify fallback matches Phase 4 (it does, keep as-is)
2. Replace hardcoded `.seg` transition `width .4s ease` with `width var(--duration-normal, 300ms) ease` for token consistency
3. Add `gap: 1px` to `.mini-bar` flex to create subtle segment separation (wireframe shows thin gaps between bar segments)

**Standard progress bar** — COMP-06 also calls for a "progress bar" component for quorum display. Evaluate: ag-mini-bar already handles multi-segment bars. For single-value progress (quorum %), add a CSS-only progress bar pattern in design-system.css:
```css
.progress-bar {
  height: 8px;
  border-radius: var(--radius-full);
  background: var(--color-bg-subtle);
  overflow: hidden;
}
.progress-bar-fill {
  height: 100%;
  border-radius: var(--radius-full);
  background: var(--color-primary);
  transition: width var(--duration-normal, 300ms) ease;
}
.progress-bar-fill.success { background: var(--color-success); }
.progress-bar-fill.danger { background: var(--color-danger); }
.progress-bar-fill.warning { background: var(--color-warning); }
```
Add this after the empty state section in design-system.css (around line 1731).
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -c "color-text-muted" public/assets/css/design-system.css | head -1 && grep "progress-bar" public/assets/css/design-system.css | head -3 && grep "gap: 1px\|duration-normal" public/assets/js/components/ag-mini-bar.js | head -2</automated>
  </verify>
  <done>Empty state CSS uses canonical design tokens (--color-text-muted, not --color-text-secondary), has CTA button styling. ag-mini-bar uses tokenized transition timing and segment gaps. Standard .progress-bar CSS pattern exists in design-system.css with success/danger/warning variants. Dark theme compatible via CSS custom properties.</done>
</task>

</tasks>

<verification>
Verify all tokenized — no proprietary tokens remain:
```bash
grep -n "tag-bg\|tag-text\|color-text-secondary" public/assets/js/components/ag-badge.js public/assets/css/design-system.css | head -10
```

Check component registration still works:
```bash
grep -c "customElements.define" public/assets/js/components/ag-badge.js public/assets/js/components/ag-mini-bar.js public/assets/js/components/ag-popover.js
```

E2E regression check:
```bash
npx playwright test tests/e2e/specs/ux-interactions.spec.js --reporter=list 2>&1 | tail -20
```
</verification>

<success_criteria>
- ag-badge uses --color-bg-subtle/--color-text-muted instead of --tag-bg/--tag-text, "warn" variant alias works
- ag-popover uses --color-surface-raised, --radius-lg, Phase 4-aligned fallback values
- Empty state CSS uses canonical tokens, has CTA button styling
- ag-mini-bar has tokenized transitions and segment gaps
- .progress-bar CSS pattern added to design-system.css
- No --tag-bg, --tag-text, or --color-text-secondary references remain
</success_criteria>

<output>
After completion, create `.planning/phases/05-shared-components/05-02-SUMMARY.md`
</output>
