---
phase: 05-shared-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/js/components/ag-modal.js
  - public/assets/js/components/ag-confirm.js
  - public/assets/js/components/ag-toast.js
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03]

must_haves:
  truths:
    - "Modal displays centered dialog with header/body/footer over backdrop, closes on Escape or backdrop click"
    - "Confirm dialog renders in danger/warn/info variants with matching icon and semantic colors"
    - "Toast notifications appear top-right, stacked downward, auto-dismiss at correct intervals, max 3 visible"
  artifacts:
    - path: "public/assets/js/components/ag-modal.js"
      provides: "Modal with wireframe-aligned tokens and 150ms animation"
      contains: "color-surface-raised"
    - path: "public/assets/js/components/ag-confirm.js"
      provides: "Confirm dialog with danger/warn/info variants"
      contains: "variant.*danger"
    - path: "public/assets/js/components/ag-toast.js"
      provides: "Toast with top-right positioning and differentiated auto-dismiss"
      contains: "top: 20px"
  key_links:
    - from: "ag-modal.js"
      to: "design-system.css tokens"
      via: "CSS custom properties in shadow DOM"
      pattern: "--color-surface|--radius-lg|--shadow-lg"
    - from: "ag-toast.js"
      to: "ag-toast-container"
      via: "static show() method creates container"
      pattern: "top.*right"
---

<objective>
Align ag-modal, ag-confirm, and ag-toast web components with wireframe v3.19.2 design tokens.

Purpose: These three overlay components are the most frequently used shared UI elements. Aligning them on design tokens ensures every modal, confirmation, and notification across the app matches the Acte Officiel visual spec in both light and dark themes.

Output: Three updated web component files with tokenized styles, correct animations, and wireframe-compliant behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-shared-components/05-CONTEXT.md
@.planning/phases/04-design-tokens-theme/04-01-SUMMARY.md

<interfaces>
<!-- Design tokens available from Phase 4 (in :root and [data-theme="dark"]) -->
<!-- All components use shadow DOM — they inherit CSS custom properties from :root -->

Key tokens to use:
- Surfaces: --color-surface, --color-surface-raised, --color-surface-alt, --color-bg-subtle
- Text: --color-text-dark, --color-text, --color-text-muted, --color-text-inverse
- Borders: --color-border, --color-border-subtle
- Semantic: --color-danger, --color-danger-subtle, --color-success, --color-success-subtle, --color-warning, --color-warning-subtle
- Primary: --color-primary, --color-primary-subtle
- Radius: --radius-sm (0.375rem), --radius (0.5rem), --radius-lg (0.625rem), --radius-full (999px)
- Shadows: --shadow-sm, --shadow-md, --shadow-lg, --shadow-xl
- Typography: --font-display (Fraunces), --font-body (Bricolage Grotesque)
- Transition: --transition (150ms ease), --duration-fast (150ms)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align ag-modal and ag-confirm on wireframe tokens and animation</name>
  <files>public/assets/js/components/ag-modal.js, public/assets/js/components/ag-confirm.js</files>
  <action>
**ag-modal.js** — Style-only changes inside the shadow DOM template:
1. Replace `backdrop-filter: blur(3px)` backdrop with `rgba(0,0,0,.45)` (slightly darker for Acte Officiel look) — keep blur as subtle enhancement
2. Change animation timing from `.2s` to `var(--duration-fast, 150ms)` for both the fadeIn and modalIn keyframes
3. Replace hardcoded `border-radius: 6px` on `.modal-close` with `var(--radius-sm, 0.375rem)`
4. Ensure `.modal` uses `background: var(--color-surface-raised, #fff)` (raised surface for elevated modals, not flat surface)
5. Replace hardcoded padding values with consistent spacing: header/footer `padding: 14px 20px`, body `padding: 18px 20px`
6. Ensure all color fallbacks match current Phase 4 token values (--color-border fallback: #d5dbd2 is correct)

**ag-confirm.js** — Style + variant alignment:
1. Same backdrop/animation timing changes as modal (150ms, rgba(0,0,0,.45))
2. Ensure "warn" variant name works as alias for "warning" — add `warn` key to `variantColors` map pointing to same config as `warning`
3. Use distinct SVG icons per variant:
   - danger: shield-alert path (a shield with exclamation) — inline SVG `<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><path d="M12 8v4"/><path d="M12 16h.01"/>`
   - warn: alert-triangle path (already correct) — `<path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3"/><path d="M12 9v4"/><path d="M12 17h.01"/>`
   - info: info circle — `<circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/>`
4. Replace the `<use href="/assets/icons.svg#...">` pattern with inline SVG paths so confirm works regardless of icon sprite availability (confirm is critical UI)
5. Ensure `.modal` in confirm uses `background: var(--color-surface-raised, #fff)` for elevation consistency
6. Replace `.btn` border-radius `8px` with `var(--radius, 0.5rem)`

Both files: Verify dark theme compatibility — all colors come from CSS custom properties which Phase 4 defined in both :root and [data-theme="dark"], so no extra work needed as long as no hardcoded hex colors remain in the styles.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -c "color-surface-raised" public/assets/js/components/ag-modal.js && grep -c "duration-fast\|150ms" public/assets/js/components/ag-modal.js && grep -c "warn.*bg\|warn.*color" public/assets/js/components/ag-confirm.js && grep "radius-sm\|radius\b" public/assets/js/components/ag-confirm.js | head -3</automated>
  </verify>
  <done>ag-modal uses --color-surface-raised background, 150ms animation, tokenized radius. ag-confirm has danger/warn/info variants with inline SVG icons and tokenized styles. No hardcoded hex colors remain in either component's style block.</done>
</task>

<task type="auto">
  <name>Task 2: Align ag-toast on wireframe — top-right position, differentiated auto-dismiss, max 3 stacking</name>
  <files>public/assets/js/components/ag-toast.js</files>
  <action>
**Container positioning** — In the `static show()` method:
1. Change container position from `bottom: 20px; right: 20px` to `top: 20px; right: 20px` (wireframe places toasts top-right)
2. Change `flex-direction: column` to `flex-direction: column` (keep same, toasts stack downward from top)
3. The max 3 limit is already implemented (lines 219-222) — verify it removes the OLDEST toast (index 0) which is correct

**Auto-dismiss timing** — Per user decision:
1. Change default duration from `4200` to differentiated values based on type
2. In `static show()`, if caller doesn't pass explicit duration, set:
   - success/info: `5000` (5 seconds)
   - warning/error: `8000` (8 seconds)
3. Update `connectedCallback` to read type attribute and apply correct default if no explicit duration:
   ```
   const type = this.getAttribute('type') || 'info';
   const defaultDurations = { success: 5000, info: 5000, warning: 8000, error: 8000 };
   const duration = this.hasAttribute('duration')
     ? parseInt(this.getAttribute('duration'), 10)
     : (defaultDurations[type] || 5000);
   ```

**Style tokenization:**
1. Replace `.toast` border-radius `var(--radius-md, 12px)` with `var(--radius-lg, 0.625rem)` (--radius-md was removed from :root in Phase 4)
2. Ensure `.toast` uses `background: var(--color-surface-raised, #ffffff)` (already correct)
3. Change animation from `cubic-bezier(.34, 1.1, .64, 1)` to `var(--duration-fast, 150ms) ease` for consistency
4. Verify left-border design pattern matches wireframe: colored left border bar + icon + text + close button (already implemented)

**Close button:** Already has close button (lines 173-175). Verify it uses tokenized colors (already does).

Dark theme: All colors reference CSS custom properties — inherits dark values automatically.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep "top:" public/assets/js/components/ag-toast.js | head -3 && grep -c "8000\|5000" public/assets/js/components/ag-toast.js && grep "radius-lg\|radius-md" public/assets/js/components/ag-toast.js | head -2</automated>
  </verify>
  <done>Toast container positioned top-right. Success/info auto-dismiss at 5s, warning/error at 8s. Max 3 visible with oldest dismissed. Border-radius uses --radius-lg token. No hardcoded hex in style block.</done>
</task>

</tasks>

<verification>
Run grep to verify no hardcoded hex colors remain in shadow DOM style blocks of all three components:
```bash
grep -n '#[0-9a-fA-F]\{3,6\}' public/assets/js/components/ag-modal.js public/assets/js/components/ag-confirm.js public/assets/js/components/ag-toast.js | grep -v 'fallback\|icons.svg\|//' | head -20
```
Note: Hardcoded hex in CSS var() fallbacks (e.g., `var(--color-x, #fff)`) is acceptable. Only standalone hardcoded hex is a problem.

Run E2E to verify no regressions:
```bash
npx playwright test tests/e2e/specs/ux-interactions.spec.js --reporter=list 2>&1 | tail -20
```
</verification>

<success_criteria>
- ag-modal uses --color-surface-raised, 150ms animation, tokenized radius/spacing
- ag-confirm supports danger/warn/info variants with inline SVG icons, no icon sprite dependency
- ag-toast container is top-right, auto-dismiss is 5s (success/info) and 8s (warning/error), max 3 visible
- All three components have zero standalone hardcoded hex colors in their style blocks
- E2E tests pass without regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-shared-components/05-01-SUMMARY.md`
</output>
