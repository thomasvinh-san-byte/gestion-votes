---
phase: 62-smtp-template-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/js/pages/settings.js
  - public/settings.htmx.html
autonomous: true
requirements: [EMAIL-04]

must_haves:
  truths:
    - "Template save from settings page sends body_html (not body) to the API"
    - "Template load from settings page reads tpl.body_html (not tpl.body)"
    - "Template preview uses server-side API call with canonical variable names"
    - "Variable tags in the template editor use canonical names from EmailTemplateService::AVAILABLE_VARIABLES"
  artifacts:
    - path: "public/assets/js/pages/settings.js"
      provides: "Fixed template editor with correct field names and server-side preview"
      contains: "body_html"
    - path: "public/settings.htmx.html"
      provides: "Variable tag buttons with canonical variable names and preview container"
      contains: "member_name"
  key_links:
    - from: "public/assets/js/pages/settings.js"
      to: "/api/v1/email_templates"
      via: "PUT request with body_html field"
      pattern: "body_html.*value"
    - from: "public/assets/js/pages/settings.js"
      to: "/api/v1/email_templates_preview"
      via: "POST request for server-side preview rendering"
      pattern: "email_templates_preview"
---

<objective>
Fix the template editor in the settings page so it correctly communicates with the backend API (field name alignment) and uses server-side preview with canonical variable names.

Purpose: EMAIL-04 requires email templates to be editable from the admin UI with variable placeholders. The template editor UI exists but has field name mismatches (`body` vs `body_html`) and uses stale variable names (`{{nom}}` vs `{{member_name}}`). This plan fixes those disconnects.

Output: Working template editing flow: select template -> edit subject/body -> preview with canonical variables via server API -> save with correct field names.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-smtp-template-engine/62-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/Controller/EmailTemplatesController.php:
```php
// update() reads these fields from PUT body:
$name = isset($input['name']) ? trim((string) $input['name']) : $existing['name'];
$subject = isset($input['subject']) ? trim((string) $input['subject']) : $existing['subject'];
$bodyHtml = isset($input['body_html']) ? trim((string) $input['body_html']) : $existing['body_html'];
// Returns: { template: { id, name, subject, body_html, body_text, ... } }
```

From app/Controller/EmailTemplatesController.php list():
```php
// GET response: { items: [{ id, name, subject, body_html, body_text, template_type, ... }] }
```

From app/Controller/EmailController.php preview():
```php
// POST /api/v1/email_templates_preview.php
// Input: { body_html: string, subject: string, custom_variables?: object }
// Returns: { preview_html, preview_subject, validation, available_variables }
```

From app/Services/EmailTemplateService.php:
```php
public const AVAILABLE_VARIABLES = [
    '{{member_name}}' => 'Nom complet du membre',
    '{{member_first_name}}' => 'Prenom du membre',
    '{{member_email}}' => 'Email du membre',
    '{{member_voting_power}}' => 'Pouvoir de vote',
    '{{meeting_title}}' => 'Titre de la seance',
    '{{meeting_date}}' => 'Date formatee (15 janvier 2024)',
    '{{meeting_time}}' => 'Heure (14h30)',
    '{{meeting_location}}' => 'Lieu de la seance',
    '{{meeting_status}}' => 'Statut de la seance',
    '{{vote_url}}' => 'Lien de vote complet',
    '{{app_url}}' => 'URL de l\'application',
    '{{token}}' => 'Token d\'authentification',
    '{{motions_count}}' => 'Nombre de resolutions',
    '{{tenant_name}}' => 'Nom de l\'organisation',
    '{{current_date}}' => 'Date d\'envoi',
    '{{current_time}}' => 'Heure d\'envoi',
];
```

Current settings.js bugs:
- Line ~434: sends `body: body.value` instead of `body_html: body.value`
- Line ~405: reads `tpl.body` instead of `tpl.body_html`
- Line ~430: sends `type:` instead of `template_type:` (controller create() reads template_type, but update() does not use it — check)
- Lines 566-573: hardcoded preview samples use {{nom}}, {{date}}, {{heure}} instead of canonical names
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix template editor field names and add server-side preview</name>
  <read_first>
    public/assets/js/pages/settings.js
    app/Controller/EmailTemplatesController.php
    app/Controller/EmailController.php
    app/Services/EmailTemplateService.php
  </read_first>
  <files>
    public/assets/js/pages/settings.js
  </files>
  <action>
In `public/assets/js/pages/settings.js`, make the following targeted fixes:

**Fix 1: Template load — `tpl.body` to `tpl.body_html`** (around line 405)

Change:
```javascript
if (bodyEl) bodyEl.value = tpl.body || '';
```
To:
```javascript
if (bodyEl) bodyEl.value = tpl.body_html || '';
```

**Fix 2: Template save — `body` to `body_html`** (around line 430-434)

Change the PUT payload from:
```javascript
api('/api/v1/email_templates', {
  id: _currentTemplateId,
  type: _currentTemplate,
  subject: subject ? subject.value : '',
  body: body ? body.value : ''
}, 'PUT')
```
To:
```javascript
api('/api/v1/email_templates', {
  id: _currentTemplateId,
  template_type: _currentTemplate,
  subject: subject ? subject.value : '',
  body_html: body ? body.value : ''
}, 'PUT')
```

Note: `type` changed to `template_type` to match `EmailTemplatesController::create()` convention. The `update()` method does not read template_type (it preserves the existing type), so this is harmless but consistent.

**Fix 3: Replace client-side preview with server-side API call**

Replace the entire `updateTemplatePreview()` function (lines ~556-578) with a version that calls the server preview API:

```javascript
var _previewDebounce = null;
function updateTemplatePreview() {
  var body = document.getElementById('templateBody');
  var subject = document.getElementById('templateSubject');
  var preview = document.getElementById('templatePreviewRender');
  if (!body || !preview) return;
  var text = body.value || '';
  if (!text.trim()) {
    preview.innerHTML = '<span style="color: var(--color-text-muted); font-style: italic; font-family: var(--font-sans);">Commencez \u00e0 saisir le corps du message pour voir l\'aper\u00e7u ici.</span>';
    return;
  }
  // Debounce server calls 400ms
  clearTimeout(_previewDebounce);
  _previewDebounce = setTimeout(function() {
    api('/api/v1/email_templates_preview', {
      body_html: text,
      subject: subject ? subject.value : ''
    })
      .then(function(r) {
        if (r.body && r.body.ok && r.body.data) {
          preview.innerHTML = r.body.data.preview_html || text;
        } else {
          // Fallback: show raw text
          preview.innerHTML = text.replace(/\n/g, '<br>');
        }
      })
      .catch(function() {
        preview.innerHTML = text.replace(/\n/g, '<br>');
      });
  }, 400);
}
```

This uses the existing `EmailController::preview()` which calls `EmailTemplateService::preview()` with canonical sample data. The preview is debounced at 400ms to avoid excessive API calls.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -q 'body_html' public/assets/js/pages/settings.js && grep -q 'tpl.body_html' public/assets/js/pages/settings.js && grep -q 'email_templates_preview' public/assets/js/pages/settings.js && ! grep -q "tpl\.body ||" public/assets/js/pages/settings.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'body_html' public/assets/js/pages/settings.js` returns at least 3 (load, save, preview)
    - `grep 'tpl.body_html' public/assets/js/pages/settings.js` finds the fixed load line
    - `grep -c "body: body" public/assets/js/pages/settings.js` returns 0 (old field name removed from template save)
    - `grep 'email_templates_preview' public/assets/js/pages/settings.js` finds the server-side preview call
    - The old hardcoded `{{nom}}`, `{{date}}`, `{{heure}}` sample map no longer exists in the file
  </acceptance_criteria>
  <done>Template editor in settings.js uses correct `body_html` field name for both load and save. Preview calls server-side API with canonical variable substitution. Old hardcoded variable map removed.</done>
</task>

<task type="auto">
  <name>Task 2: Add variable tag buttons and preview container to template editor HTML</name>
  <read_first>
    public/settings.htmx.html
    app/Services/EmailTemplateService.php
  </read_first>
  <files>
    public/settings.htmx.html
  </files>
  <action>
In `public/settings.htmx.html`, enhance the "Modeles d'email" card (id="templateEditor") with:

**A) Variable tags row** — Add a row of clickable variable tag buttons between the "Corps" textarea and the card's closing `</div>`. Insert AFTER the `templateBody` textarea's `field-group-h` div (after line ~221), BEFORE the card-body closing `</div>`:

```html
                <div class="field-group-h field-group-h--top" style="margin-top:0.5rem;">
                  <label>Variables</label>
                  <div style="display:flex; flex-wrap:wrap; gap:0.375rem;">
                    <button type="button" class="btn btn-xs btn-ghost variable-tag" data-var="{{member_name}}">{{member_name}}</button>
                    <button type="button" class="btn btn-xs btn-ghost variable-tag" data-var="{{member_first_name}}">{{member_first_name}}</button>
                    <button type="button" class="btn btn-xs btn-ghost variable-tag" data-var="{{member_email}}">{{member_email}}</button>
                    <button type="button" class="btn btn-xs btn-ghost variable-tag" data-var="{{meeting_title}}">{{meeting_title}}</button>
                    <button type="button" class="btn btn-xs btn-ghost variable-tag" data-var="{{meeting_date}}">{{meeting_date}}</button>
                    <button type="button" class="btn btn-xs btn-ghost variable-tag" data-var="{{meeting_time}}">{{meeting_time}}</button>
                    <button type="button" class="btn btn-xs btn-ghost variable-tag" data-var="{{meeting_location}}">{{meeting_location}}</button>
                    <button type="button" class="btn btn-xs btn-ghost variable-tag" data-var="{{vote_url}}">{{vote_url}}</button>
                    <button type="button" class="btn btn-xs btn-ghost variable-tag" data-var="{{app_url}}">{{app_url}}</button>
                    <button type="button" class="btn btn-xs btn-ghost variable-tag" data-var="{{tenant_name}}">{{tenant_name}}</button>
                    <button type="button" class="btn btn-xs btn-ghost variable-tag" data-var="{{current_date}}">{{current_date}}</button>
                  </div>
                </div>
```

These use the canonical variable names from `EmailTemplateService::AVAILABLE_VARIABLES`. The click-to-insert handler already exists in `settings.js:initTemplatePreview()` (line ~588-601) and targets `.variable-tag` elements.

**B) Preview container** — Add a preview render area after the variable tags row:

```html
                <div class="field-group-h field-group-h--top" style="margin-top:0.75rem;">
                  <label>Aper&ccedil;u</label>
                  <div id="templatePreviewRender" style="border:1px solid var(--color-border); border-radius:var(--radius-md); padding:1rem; min-height:120px; background:var(--color-surface); font:14px/1.6 var(--font-sans);">
                    <span style="color: var(--color-text-muted); font-style: italic;">Commencez &agrave; saisir le corps du message pour voir l'aper&ccedil;u ici.</span>
                  </div>
                </div>
```
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -q 'member_name' public/settings.htmx.html && grep -q 'templatePreviewRender' public/settings.htmx.html && grep -q 'variable-tag' public/settings.htmx.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'variable-tag' public/settings.htmx.html` returns at least 5 (multiple variable tag buttons)
    - `grep -c 'member_name' public/settings.htmx.html` returns at least 1
    - `grep -c 'meeting_title' public/settings.htmx.html` returns at least 1
    - `grep -q 'templatePreviewRender' public/settings.htmx.html` succeeds (preview container exists)
    - No `{{nom}}` or `{{date}}` or `{{heure}}` old variable names appear in the template editor section
  </acceptance_criteria>
  <done>Template editor HTML has clickable variable tag buttons using canonical names and a preview render container. Click-to-insert (from existing JS) works with the new buttons. Server-side preview (from Task 1) renders into the preview container.</done>
</task>

</tasks>

<verification>
```bash
# Field name fixes verified
grep 'body_html' public/assets/js/pages/settings.js | head -5
grep 'tpl.body_html' public/assets/js/pages/settings.js

# Old bugs gone
! grep -q "tpl\.body ||" public/assets/js/pages/settings.js
! grep -q "'{{nom}}'" public/assets/js/pages/settings.js

# HTML enhancements
grep 'variable-tag' public/settings.htmx.html | head -3
grep 'templatePreviewRender' public/settings.htmx.html

# Full test suite still passes
./vendor/bin/phpunit --no-coverage
```
</verification>

<success_criteria>
1. Template save sends `body_html` (not `body`) to the API
2. Template load reads `tpl.body_html` (not `tpl.body`) from API response
3. Preview uses server-side API call (POST to email_templates_preview) with canonical variables
4. Variable tag buttons in HTML use canonical names (member_name, meeting_title, etc.)
5. Preview container exists in settings HTML for rendered output
6. No stale variable names ({{nom}}, {{date}}, {{heure}}) remain in settings.js
</success_criteria>

<output>
After completion, create `.planning/phases/62-smtp-template-engine/62-02-SUMMARY.md`
</output>
