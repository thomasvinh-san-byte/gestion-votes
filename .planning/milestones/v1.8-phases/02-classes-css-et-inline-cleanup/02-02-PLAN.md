---
phase: 02-classes-css-et-inline-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/docs.htmx.html
  - public/email-templates.htmx.html
  - public/members.htmx.html
  - public/report.htmx.html
  - public/validate.htmx.html
  - public/wizard.htmx.html
  - public/settings.htmx.html
  - public/analytics.htmx.html
  - public/hub.htmx.html
  - public/operator.htmx.html
  - public/assets/css/design-system.css
autonomous: true
requirements:
  - UI-05
must_haves:
  truths:
    - "Zero style='display:none' dans les fichiers HTML (remplace par hidden)"
    - "Le preview template dans settings utilise une classe CSS, pas inline styles"
    - "Les donut legend dots dans analytics utilisent des classes CSS"
    - "Le popover explicatif dans hub utilise une classe CSS"
    - "Les styles inline de cursor:pointer dans operator sont remplaces"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Nouvelles classes utilitaires pour remplacer inline styles"
      contains: "template-preview"
    - path: "public/settings.htmx.html"
      provides: "Settings sans inline styles complexes"
    - path: "public/members.htmx.html"
      provides: "Members avec hidden au lieu de display:none"
  key_links:
    - from: "public/settings.htmx.html"
      to: "public/assets/css/design-system.css"
      via: "template-preview class"
      pattern: "template-preview"
    - from: "public/analytics.htmx.html"
      to: "public/assets/css/design-system.css"
      via: "donut legend color classes"
      pattern: "donut-legend-dot--(success|danger|muted)"
---

<objective>
Replace all static inline styles across 10 HTML files with CSS classes or HTML attributes.

Purpose: Eliminate inline styles so the design-system CSS is the single source of truth for visual styling. This also prepares for future CSP enforcement (no inline styles needed).
Output: Zero static inline styles in HTML files, new utility classes in design-system.css.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-classes-css-et-inline-cleanup/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace display:none with hidden attribute across all HTML files</name>
  <files>public/docs.htmx.html, public/email-templates.htmx.html, public/members.htmx.html, public/report.htmx.html, public/validate.htmx.html, public/wizard.htmx.html</files>
  <action>
In each file, replace `style="display:none"` and `style="display: none;"` and `style="display: none"` with the `hidden` attribute. Remove the style attribute entirely and add `hidden` as a standalone attribute.

Specific occurrences (18 total):

**docs.htmx.html** (3):
- Line 37: breadcrumbDir span
- Line 38: breadcrumbDirSep svg
- Line 59: docTocRail aside

**email-templates.htmx.html** (1):
- Line 83: emptyState div

**members.htmx.html** (5):
- Line 205: groupFiltersField div
- Line 260: groupsPanel div
- Line 282: importPanel div
- Line 298: csvFile input
- Line 312: importOut div

**report.htmx.html** (4):
- Line 60: meetingContext div
- Line 66: notValidatedWarning div
- Line 87: emailMsg div
- Line 113: pvFrame iframe

**validate.htmx.html** (2):
- Line 60: meetingContext div
- Line 162: validateMsg div

**wizard.htmx.html** (2):
- Line 261: csvFileInput input
- Line 369: reso-add-panel div

For each: remove `style="display:none"` (or variant with spaces), add `hidden` attribute. Example:
- Before: `<div id="foo" style="display: none;">`
- After: `<div id="foo" hidden>`

Per D-08: Do NOT touch JS-managed runtime styles (public.htmx.html width percentages).
  </action>
  <verify>
    <automated>grep -rn 'style="display:none\|style="display: none' public/docs.htmx.html public/email-templates.htmx.html public/members.htmx.html public/report.htmx.html public/validate.htmx.html public/wizard.htmx.html; echo "EXIT:$?"</automated>
  </verify>
  <done>Zero style="display:none" in the 6 target files. All replaced with hidden attribute.</done>
</task>

<task type="auto">
  <name>Task 2: Replace complex inline styles with CSS classes</name>
  <files>public/assets/css/design-system.css, public/settings.htmx.html, public/analytics.htmx.html, public/hub.htmx.html, public/operator.htmx.html</files>
  <action>
**Step 1: Add utility classes to design-system.css** (at the end, before dark mode section or after utility classes):

```css
/* --- Inline style replacements (Phase 02) --- */

/* Settings: email template preview container */
.template-preview {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 1rem;
  min-height: 120px;
  background: var(--color-surface);
  font: 14px/1.6 var(--font-sans);
}

.template-preview-placeholder {
  color: var(--color-text-muted);
  font-style: italic;
}

/* Settings: hint text below fields */
.field-hint-sm {
  font-size: 12px;
  color: var(--color-text-muted);
  margin-top: 2px;
}

/* Settings: font-size radio card labels */
.settings-radio-card__label--sm { font-size: 1.2em; }
.settings-radio-card__label--lg { font-size: 1.5em; }

/* Settings: flex wrap for template variables */
.template-vars-wrap {
  display: flex;
  flex-wrap: wrap;
  gap: 0.375rem;
}

/* Analytics: donut legend dot colors */
.donut-legend-dot--success { background: var(--color-success); }
.donut-legend-dot--danger { background: var(--color-danger); }
.donut-legend-dot--muted { background: var(--color-text-muted); }

/* Hub: popover help text */
.popover-help-text {
  margin: 6px 0 0;
  font-size: 13px;
  color: var(--color-text-muted);
}
```

**Step 2: Update settings.htmx.html:**
- Line ~240: Replace `style="display:flex; flex-wrap:wrap; gap:0.375rem;"` with `class="template-vars-wrap"`
- Line ~257: Replace the inline style on templatePreviewRender div with `class="template-preview"`
- Line ~258: Replace inline style on the placeholder span with `class="template-preview-placeholder"`
- Line ~327: Replace inline style on the hint span with `class="field-hint-sm"`
- Line ~357: Replace `style="font-size:1.2em"` with adding class `settings-radio-card__label--sm` (keep existing class)
- Line ~363: Replace `style="font-size:1.5em"` with adding class `settings-radio-card__label--lg` (keep existing class)
- Lines ~238,~255: Remove `style="margin-top:0.5rem"` and `style="margin-top:0.75rem"` — use existing spacing utilities `mt-2` and `mt-3` if available, or add them

**Step 3: Update analytics.htmx.html:**
- Line ~275: Replace `style="background:var(--color-success)"` with add class `donut-legend-dot--success`
- Line ~276: Replace `style="background:var(--color-danger)"` with add class `donut-legend-dot--danger`
- Line ~277: Replace `style="background:var(--color-text-muted)"` with add class `donut-legend-dot--muted`

**Step 4: Update hub.htmx.html:**
- Line ~191: Replace inline style on the `<p>` with `class="popover-help-text"`

**Step 5: Update operator.htmx.html:**
- Line ~528: Replace `style="cursor:pointer"` with `class="cursor-pointer"` (add `.cursor-pointer { cursor: pointer; }` to design-system.css if not present)
- Line ~1225: Remove `style="position:relative;"` if the element already has a class that sets position, or add a utility class
  </action>
  <verify>
    <automated>grep -c 'style=' public/settings.htmx.html public/analytics.htmx.html public/hub.htmx.html && echo "---" && grep -c 'template-preview\|donut-legend-dot--\|popover-help-text' public/assets/css/design-system.css</automated>
  </verify>
  <done>settings.htmx.html has zero inline styles (was 8). analytics.htmx.html has zero inline styles (was 3). hub.htmx.html has zero inline styles (was 1). operator.htmx.html has zero static inline styles. New classes defined in design-system.css.</done>
</task>

</tasks>

<verification>
- `grep -rn 'style="display:none\|style="display: none' public/*.html public/*.htmx.html` returns only public.htmx.html (runtime styles)
- `grep -c 'style=' public/settings.htmx.html` returns 0
- `grep -c 'style=' public/analytics.htmx.html` returns 0
- `grep -c 'style=' public/hub.htmx.html` returns 0
</verification>

<success_criteria>
All 42 static inline styles across HTML files are replaced with CSS classes or hidden attributes. Runtime JS-managed styles (public.htmx.html progress bars) are preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/02-classes-css-et-inline-cleanup/02-02-SUMMARY.md`
</output>
