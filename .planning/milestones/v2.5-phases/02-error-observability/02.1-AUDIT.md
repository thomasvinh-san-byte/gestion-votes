---
plan_id: 02.1
audit: business_error callers
gathered: 2026-05-04
total_callers: 11
buckets: 3
generic_fallback: 1
---

# Audit `business_error` callers — 02.1

## Méthodologie

Grep exhaustif sur `app/` + `public/api/` :

```bash
grep -rnE "api_fail\(\s*'business_error'|'business_error'\s*=>" app/ public/api/
```

11 callers `api_fail('business_error', ...)` + 1 entrée dictionnaire (`ErrorDictionary.php:109` — préservée par D-03).

## Classification 3 buckets sémantiques + fallback

### Bucket A — Workflow / transition failure

Échec d'une opération de transition d'état (transition/launch/validate/resetDemo). RuntimeException propagée par `MeetingTransitionService` ou `MeetingLifecycleService::validateMeeting` après les cas spécifiques (meeting_not_found, archived, status_not_resettable…) déjà mappés.

| # | File:line | Méthode | Source RuntimeException |
|---|---|---|---|
| 1 | `app/Controller/MeetingWorkflowController.php:39` | `transition()` | `MeetingTransitionService::transition()` (tail catch après `meeting_not_found` + `archivée`) |
| 2 | `app/Controller/MeetingWorkflowController.php:171` | `readyCheck()` | `MeetingTransitionService::readyCheck()` |
| 3 | `app/Controller/MeetingWorkflowController.php:252` | `resetDemo()` | `MeetingTransitionService::resetDemo()` (tail catch après `validée` + `meeting_status_not_resettable`) |
| 4 | `app/Controller/MeetingsController.php:286` | `validate()` | `MeetingLifecycleService::validateMeeting()` (validate = transition vers `validated`) |

**Code proposé** : `meeting_transition_failed`
**Message FR** : `« La transition de séance a échoué, vérifiez le statut courant et les pré-requis du workflow ou consultez le détail affiché. »`

### Bucket B — Lifecycle CRUD failure

Échec d'une mutation CRUD sur la séance (update/delete) hors transition. RuntimeException propagée par `MeetingLifecycleService` après les cas spécifiques (meeting_not_found, archivée, meeting_live_cannot_delete, meeting_not_draft…) déjà mappés.

| # | File:line | Méthode | Source RuntimeException |
|---|---|---|---|
| 5 | `app/Controller/MeetingsController.php:64` | `update()` | `MeetingLifecycleService::updateMeeting()` (HTTP 409 conflict) |
| 6 | `app/Controller/MeetingsController.php:211` | `deleteMeeting()` | `MeetingLifecycleService::deleteDraft()` |

**Code proposé** : `meeting_operation_failed`
**Message FR** : `« L'opération sur la séance a échoué, vérifiez les pré-requis affichés ou actualisez la page avant de relancer l'action. »`

### Bucket C — State / read query failure

Échec de lecture d'état/résumé/stats. RuntimeException propagée par `MeetingLifecycleService::getStatus/getStatusForMeeting/getSummary/getStats` après les cas spécifiques (meeting_not_found, no_live_meeting) déjà mappés.

| # | File:line | Méthode | Source RuntimeException |
|---|---|---|---|
| 7 | `app/Controller/MeetingsController.php:93` | `status()` | `MeetingLifecycleService::getStatus()` |
| 8 | `app/Controller/MeetingsController.php:107` | `statusForMeeting()` | `MeetingLifecycleService::getStatusForMeeting()` |
| 9 | `app/Controller/MeetingsController.php:122` | `summary()` | `MeetingLifecycleService::getSummary()` |
| 10 | `app/Controller/MeetingsController.php:136` | `stats()` | `MeetingLifecycleService::getStats()` |

**Code proposé** : `meeting_state_read_failed`
**Message FR** : `« Lecture de l'état de séance impossible, actualisez la page ou vérifiez auprès de l'opérateur que la séance est encore active. »`

### Fallback légitime — à conserver

| # | File:line | Méthode | Justification conservation |
|---|---|---|---|
| 11 | `app/Controller/AbstractController.php:50` | `handle()` (catch-all) | Catch global générique sur tout `RuntimeException` non capturé par les controllers spécifiques. Sémantique authentiquement « business error inconnu ». **Conformément à D-03**, `business_error` reste comme fallback ici — c'est exactement son cas d'usage. |

## Synthèse

- **11 callers** `business_error` identifiés
- **10 callers migrables** → 3 buckets (A: 4, B: 2, C: 4)
- **1 fallback préservé** dans `AbstractController::handle()` (catch-all global)
- **Hypothèses CONTEXT D-02** (`quorum_not_met`, `meeting_state_invalid`, `vote_already_closed`) **rejetées** :
  - `quorum_not_met` existe déjà ligne 89 du dictionnaire (jamais émis comme `business_error`)
  - Aucun caller `business_error` n'est lié au quorum (aucun appel depuis `BallotsController`/`VoteEngine`/`QuorumEngine`)
  - Aucun caller lié à un scrutin clos — tous concernent la séance, pas les motions/ballots
- **3 codes définitifs** :
  1. `meeting_transition_failed` (Bucket A)
  2. `meeting_operation_failed` (Bucket B)
  3. `meeting_state_read_failed` (Bucket C)

## Validation conformité ERR-02 + ERR-03

Chaque message proposé respecte :

- **ERR-02** : virgule présente + verbe d'action de la liste blanche `UxConventionsTest::ACTION_VERBS`
  - A : virgule + `vérifiez` + `consultez`
  - B : virgule + `vérifiez` + `actualisez`
  - C : virgule + `actualisez` + `vérifiez`
- **ERR-03** : 0 phrase bannie (`réessayer.`, `contactez l'admin`, `erreur survenue`, `une erreur est survenue`, `veuillez réessayer plus tard`)

## Baseline post-migration

| Métrique | Avant | Après cible |
|---|---|---|
| Callers `api_fail('business_error', …)` | 11 | 1 (catch-all `AbstractController::handle()`) |
| Codes dans `ErrorDictionary::MESSAGES` | 206 | 209 (+3) |
| Test gardien `testBusinessErrorCallersBaseline` borne | — | ≤ 3 (marge sécurité) |

## Hors scope (déféré)

- **Suppression complète `business_error`** (déféré v2.5+) — la conservation du catch-all `AbstractController` est intentionnelle et conforme D-03.
- **Migration logger context** sur ces sites (Plan 02.3) — zone disjointe.
