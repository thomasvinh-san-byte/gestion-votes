---
phase: 04-lexique-ux-critique
plan: 6
subsystem: docs
tags: [audit, prevention, error-handling, milestone-gate]

# Dependency graph
requires:
  - plan: 04.1 (lexique migration — context for ErrorDictionary messages)
  - plan: 04.4 (top 50 enrichment — `meeting_not_found` and `invalid_meeting_id` messages already actionable thanks to 04.4)
  - plan: 04.5 (UxConventionsTest — guards the next-steps that 04.6 audit references)
provides:
  - "ERR-04 audit complete : 5/5 codes documented (audit Norman prevention > detection > recovery)"
  - "2/5 codes 'prévenu par design v2.3' (missing_meeting_id + invalid_meeting_id — by-construction prevention via MeetingContext singleton + zero UUID manual input UI)"
  - "3/5 codes 'déféré v2.4' avec rationale écrite (meeting_not_found + motion_not_found = race fondamentale ; business_error = refonte structurelle)"
  - "Followups v2.4 capturés (refactor business_error en codes spécifiques par site d'émission, audit logging, structured tracking)"
  - "Milestone Gate Status section : screenshot panel gate à exécuter manuellement post-merge"

affects: [phase 04 verifier, /gsd:ship gate manual screenshot panel]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cadre Norman 'prevention > detection > recovery' appliqué : pour chaque erreur fréquente, demander 'peut-on prévenir par contrainte UI ?' avant 'comment guider la récupération ?' (cf 04.4 next-steps)"
    - "Audit by-design pattern : 2/5 codes prévenus par architecture existante (MeetingContext, pas de saisie UUID manuelle) — pas de fix livré car prévention déjà cristallisée. Garde-fou serveur reste défensif."
    - "Stream-timeout recovery pattern (3e fois cette phase) : executor commit AUDIT.md avant timeout ; orchestrator finalise SUMMARY + state updates inline post-spot-check"

key-files:
  created:
    - .planning/phases/04-lexique-ux-critique/04.6-AUDIT.md (16599 bytes, 5 sections code + synthèse + critère ERR-04 + fixes + followups + méthodologie)
  modified: []

key-decisions:
  - "5/5 codes audités explicitement (le PLAN exigeait ≥2/5 — overdelivery)"
  - "2/5 prévenus par design v2.3 (missing_meeting_id, invalid_meeting_id) : architecture MeetingContext + absence totale de saisie UUID manuelle suffisent, pas de fix livré (prévention par construction)"
  - "3/5 déférés v2.4 avec rationale : meeting_not_found (race avec suppression côté serveur), motion_not_found (race idem), business_error (refonte en codes spécifiques par site d'émission)"
  - "Aucun fix UI nouveau livré v2.3 — les 'prévenus' le sont par construction historique. Les messages enrichis par 04.4 guident correctement la récupération pour les 3 'déférés'"
  - "Followups v2.4 capturés : refactor business_error en codes spécifiques (~30 sites d'émission), structured logging si volume justifie"

# Critère ERR-04 (REQUIREMENTS)
err04_compliance:
  required_minimum: "≥ 2/5 codes marqués 'prévenu en v2.3' ou 'déféré v2.4 avec rationale'"
  delivered:
    - code: "meeting_not_found"
      decision: "Déféré v2.4 avec rationale (race fondamentale, message 04.4 guide la récupération)"
    - code: "missing_meeting_id"
      decision: "Prévenu par design v2.3 (rationale + audit codebase MeetingContext)"
    - code: "motion_not_found"
      decision: "Déféré v2.4 avec rationale (race fondamentale, message 04.4 guide la récupération)"
    - code: "invalid_meeting_id"
      decision: "Prévenu par design v2.3 (rationale + audit codebase — pas de saisie UUID manuelle)"
    - code: "business_error"
      decision: "Déféré v2.4 avec rationale (refonte structurelle en codes spécifiques)"
  status: "5/5 décidés (largement au-dessus du minimum ≥2/5). ERR-04 satisfait."

# Verification
verification:
  acceptance:
    - "`.planning/phases/04-lexique-ux-critique/04.6-AUDIT.md` existe (16599 bytes, ~5 sections + synthèse) ✓"
    - "5/5 codes traités explicitement avec décision ✓"
    - "2/5 marqués 'prévenu par design v2.3' (au-dessus du seuil ≥2) ✓"
    - "Section Synthèse + tableau comparatif présents ✓"
    - "Followups v2.4 capturés (3 codes déférés + méthodologie) ✓"
    - "0 occurrence de `copropriété` ou `syndic` ✓"

# Milestone Gate Status (F4 fix iter 1)
milestone_gate_status:
  state: "Pending"
  gate_name: "Screenshot Panel Gate"
  source: "ROADMAP v2.3 §Milestone Success Gate"
  protocol:
    - "Captures avant (snapshot v2.2-merge) et après (snapshot v2.3-merge) sur 5 écrans : `/login`, `/dashboard`, `/operator` (mode exec), `/audit` (PV), un modal d'erreur"
    - "3 observateurs internes notent chaque paire sur 5 dimensions (échelle 1-5) : confiance, compréhension, charge, action, sérieux"
    - "Critère : médiane gagne ≥ +1 point sur chaque dimension entre v2.2-merge et v2.3-merge"
  blocking_for: "/gsd:ship Phase 4 final + milestone v2.3 ship"
  next_action: "Le développeur prépare les captures sur sa machine dev une fois la branche feat/v2.3-cockpit-operateur mergée. La gate est par nature humaine — non automatisable."
  rationale: "Phase 4 verifier passing ≠ milestone gate passing. Le screenshot panel test l'objectif ultime 'celui-là est plus rassurant' du milestone — irreductible à l'instrumentation."

# Deviations
deviations:
  - rule: "Stream idle timeout (infrastructure)"
    type: "executor agent timeout post-AUDIT-commit"
    rationale: "L'executor a réussi à écrire et committer AUDIT.md (commit `bbe69c7`, 16599 bytes) avant le timeout API stream idle (51 tool uses, 503s). Les wrap-up tasks (SUMMARY.md + STATE.md + ROADMAP.md + REQUIREMENTS.md updates) n'ont pas été complétées par l'executor — l'orchestrator (Claude main thread) les fait manuellement post-spot-check (working tree clean, AUDIT structure validée par grep, 5/5 décisions présentes)."
    impact: "Aucun. Le livrable principal (AUDIT.md) est en place et committé. Les artefacts métadonnées sont produits inline par l'orchestrator avec une référence explicite à cette déviation."

# Tests
tests:
  added: []
  modified: []
  removed: []

# Followups
followups:
  - id: "04.6-FOLLOWUP-1"
    type: "milestone gate (manual, blocking)"
    description: "Exécuter le screenshot panel gate avant `/gsd:ship` final v2.3. Cf section milestone_gate_status ci-dessus."
    owner: "developer"
    blocking_for: "milestone v2.3 ship"

  - id: "04.6-FOLLOWUP-2"
    type: "v2.4 backlog"
    description: "Refactor `business_error` en codes spécifiques par site d'émission (~30 sites grep). Le code générique masque les vraies causes en logs et empêche un guide utilisateur précis. Architecture proposée : enum BusinessError ou codes textuels par domaine (`vote_already_cast`, `meeting_not_open`, etc.)."
    owner: "v2.4 milestone"
    blocking_for: "audit logging granularité"

  - id: "04.6-FOLLOWUP-3"
    type: "v2.4 backlog"
    description: "`meeting_not_found` + `motion_not_found` race conditions : audit volume production (combien d'occurrences sur 30j ?). Si volume justifie, ajouter HTMX swap qui détecte 404 + replace UI avec empty-state amical au lieu de toast d'erreur."
    owner: "v2.4 milestone"
    blocking_for: "user-perceived reliability sur sessions cliquées tardivement"

# Commits
commits:
  - hash: bbe69c7
    type: docs
    description: "ERR-04 prevention audit on top 5 emitted codes"
  - hash: TBD
    type: docs
    description: "complete plan 04.6 (this SUMMARY.md + STATE/ROADMAP/REQUIREMENTS updates) — orchestrator-completed post-stream-timeout"
