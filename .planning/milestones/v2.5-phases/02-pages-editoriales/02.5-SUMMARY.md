---
phase: 02-pages-editoriales
plan: 5
subsystem: ui
tags: [css, design-system, tokens, refactor, editorial]

requires:
  - phase: quick-260430-86c
    provides: "Tokens TECH-01 (--shadow-xs, --border-default/subtle/strong/dashed/focus) consolidés sur 25 fichiers, dont déjà appliqués sur les borders des 4 CSS éditoriaux."
  - phase: 02-pages-editoriales (Plan 02.1)
    provides: "Wrapper .ag-editorial + tokens design-system étendus."
provides:
  - "audit.css, trust.css, report.css, archives.css : 0 padding/margin hardcodé"
  - "Tous les espacements éditoriaux passent par var(--space-*)"
  - "Cohérence stricte design-system sur les 4 pages éditoriales (Schoger S-3 — \"Si tu n'utilises pas le système partout, tu n'as pas un système\")"
affects: [02.6 (Playwright + EditorialConventionsTest), v2.4 (PV final), futurs ajustements espacements]

tech-stack:
  added: []
  patterns:
    - "Cleanup hardcoded → tokens en mode by-file atomic commits (revert ciblé possible si régression visuelle)"
    - "Baseline mesurée avant édition, target = 0 vérifié par grep automatisé"

key-files:
  created:
    - .planning/phases/02-pages-editoriales/02.5-SUMMARY.md
  modified:
    - public/assets/css/audit.css
    - public/assets/css/trust.css
    - public/assets/css/report.css

key-decisions:
  - "[v2.3 P2.5] Baseline réelle ≠ baseline plan — au moment de l'exécution, la mesure (audit=0, trust=1, report=4, archives=0) divergeait fortement du plan (audit=1, trust=25, report=4, archives=0). Cause : audit.css avait été pré-committé (commit 7da8173) et trust.css avait 24 substitutions déjà en working tree au début de la session, laissant 1 hardcoded à finaliser ligne 619. Continuité respectée : commits atomiques par fichier conservés, baseline `originale` reconstituée depuis l'historique git."
  - "[v2.3 P2.5] archives.css : 0 hardcoded padding/margin baseline + 0 opportunité TECH-01 — fichier déjà entièrement tokenisé (commentaire de tête \"All values use design-system tokens — no magic numbers\" tient parole). Pas de commit créé pour archives.css : aucun changement n'est honnête plutôt qu'un commit vide."
  - "[v2.3 P2.5] `padding: 0 5px` (ligne 107 archives.css, count badge) laissé tel quel : non matché par le grep (0 sans unité), valeur 5px cosmétique mineure sans token --space-1-25 disponible. Arrondi à --space-1 (4px) ou --space-1-5 (6px) introduirait un changement visuel sans bénéfice mesuré."
  - "[v2.3 P2.5] `padding: 0` dans @media print de report.css (lignes 265, 269, 273) conservés : reset standard CSS, valeur 0 sans unité non matchée par le grep, cas spécial autorisé par le plan."

patterns-established:
  - "Editorial cleanup: par fichier, atomic commits, baseline mesurée pré-édition, target verify post-édition"
  - "Token mapping: rems et px convertis selon table standard (0.25rem→space-1, 1rem→space-4, 1.25rem→space-5, etc.)"

requirements-completed:
  - EDITORIAL-09

duration: ~10min
completed: 2026-04-30
---

# Phase 02 Plan 5: Cleanup hardcoded padding/margin Summary

**4 CSS éditoriaux (audit/trust/report/archives) entièrement tokenisés sur padding/margin — 29 substitutions cumulées, 0 hardcoded restant.**

## Performance

- **Duration:** ~10min (la majeure partie du travail trust.css avait déjà été préparée avant cette session)
- **Started:** 2026-04-30T07:25:00Z
- **Completed:** 2026-04-30T07:35:00Z
- **Tasks:** 1 (cleanup multi-fichier)
- **Files modified:** 3 (audit.css déjà committé pré-session ; archives.css déjà clean — pas de modif nécessaire)

## Baseline mesurée

Mesure baseline ligne git précédant les commits 02-5 :

| Fichier | Baseline plan | Baseline mesurée 2026-04-30 | Cible | Atteint |
|---|---|---|---|---|
| `public/assets/css/audit.css` | 1 | 1 (avant commit `7da8173`) | 0 | 0 |
| `public/assets/css/trust.css` | 25 | 25 (24 déjà en working tree + 1 ligne 619) | 0 | 0 |
| `public/assets/css/report.css` | 4 | 4 | 0 | 0 |
| `public/assets/css/archives.css` | 0 | 0 (déjà entièrement tokenisé) | 0 | 0 |

**Total substitutions effectuées : 29** (1 audit + 24+1 trust + 4 report + 0 archives).

## Mapping de remplacement par fichier

### audit.css (commit `7da8173` — committé avant la reprise de session)

| Ligne | Avant | Après | Rationale |
|---|---|---|---|
| `.audit-filter-tabs` | `padding-bottom: 2px` | `padding-bottom: var(--space-0-5)` | scroll spacing, 2px = `--space-0-5` |

### trust.css (commit `7849cf3`)

24 substitutions au sein de la diff working tree + 1 finalisée ligne 619 :

| Pattern hardcodé | Token cible | Occurrences |
|---|---|---|
| `0.125rem` (=2px) | `var(--space-0-5)` | 1 |
| `0.25rem` (=4px) | `var(--space-1)` | 2 |
| `0.375rem` (=6px) | `var(--space-1-5)` | 3 |
| `0.5rem` (=8px) | `var(--space-2)` | 3 |
| `0.625rem` (=10px) | `var(--space-2-5)` | 1 |
| `0.75rem` (=12px) | `var(--space-3)` | 7 |
| `0.875rem` (=14px) | `var(--space-3)` | 1 (arrondi vers le bas, voir Cas exotiques) |
| `1rem` (=16px) | `var(--space-4)` | 7 |
| `1.25rem` (=20px) | `var(--space-5)` | 2 |
| `8px` | `var(--space-2)` | 1 |
| `12px` | `var(--space-3)` | 1 |

### report.css (commit `93931a3`)

| Ligne | Avant | Après | Rationale |
|---|---|---|---|
| 70 | `padding: 3rem` | `padding: var(--space-12)` | 3rem = 48px = `--space-12` |
| 76 | `margin-bottom: 1rem` | `margin-bottom: var(--space-4)` | 1rem = 16px = `--space-4` |
| 84 | `margin-top: 4px` | `margin-top: var(--space-1)` | 4px = `--space-1` |
| 146 | `margin-bottom: 20px` | `margin-bottom: var(--space-5)` | 20px = `--space-5` |

### archives.css (aucun commit nécessaire)

Fichier déjà entièrement tokenisé baseline. Vérification également des shadows/borders :
- Tous les borders : `var(--border-default)`, `var(--border-subtle)` (TECH-01 déjà appliqué).
- Shadow `var(--shadow-md)` sur `.archive-card-enhanced:hover` (déjà tokenisé).
- Border-left status accents (`3px solid var(--color-primary/success/warning)`) : **cas BASSE confiance hors scope TECH-01** — couleur sémantique ; width 3px non standard. Laissés tels quels (cf. table décision plan).

## Cas spéciaux laissés (autorisés par le plan)

| Pattern | Fichier | Ligne | Raison |
|---|---|---|---|
| `padding: 0` | report.css | 265, 269, 273 (@media print) | Reset standard CSS, autorisé |
| `margin: 0` (réutilisations) | divers | divers | Reset, autorisé |
| `padding: 0 5px` (count badge) | archives.css | 107 | Non matché par grep ; pas de token --space-1-25 standard ; cosmétique mineur |
| `border-left: 3px solid var(--color-primary)` | archives.css | 237, 241, 245 | BASSE confiance TECH-01 (couleur sémantique + width non-1px) |
| `box-shadow: 0 0 0 4px var(--color-primary-subtle)` | report.css | 124 | BASSE confiance TECH-01 (focus-ring pattern, pas dans la table de mapping) |

## Cas exotiques arrondis

| Valeur originelle | Valeur tokenisée | Différence | Localisation |
|---|---|---|---|
| `0.875rem` (=14px) | `var(--space-3)` (=12px) | -2px | `.audit-chip` padding horizontal trust.css ligne ~308 — perte cosmétique mineure, le pill conserve la même hauteur visuelle (text-xs) |

Aucune autre valeur exotique rencontrée dans les 4 fichiers.

## Tokens TECH-01 absorbés

Décompte global sur les 4 CSS éditoriaux post-cleanup :

```
grep -E "var\(--(border-default|border-subtle|border-strong|border-dashed|shadow-xs|shadow-sm|shadow-md)" \
  public/assets/css/{audit,trust,report,archives}.css | wc -l
→ 28
```

28 utilisations cumulées de tokens TECH-01 sur les 4 fichiers — héritage direct de la quick task `260430-86c` qui avait pré-tokenisé les borders (commits `00a5114`, `69d0905`, `ece7148`, `b16b1c6`). Aucune absorption supplémentaire effectuée pendant Plan 02.5 : les patterns BASSE confiance restants ne matchent pas la table de mapping TECH-01 (couleurs sémantiques, widths non-1px, focus rings).

## Token usage proof

```
grep -c "var(--space-" public/assets/css/audit.css     → 47
grep -c "var(--space-" public/assets/css/trust.css     → 32  (≥ 5 OK)
grep -c "var(--space-" public/assets/css/report.css    → 16
grep -c "var(--space-" public/assets/css/archives.css  → 37
```

## Vérifications finales

- [x] `grep -cE "(padding|margin)(-[a-z]+)?:\s+[0-9]+(\.[0-9]+)?(px|rem|em)"` retourne **0** sur chacun des 4 fichiers
- [x] Token usage proof : `var(--space-` ≥ 5 sur trust.css (32, top offender)
- [x] Accolades équilibrées (`awk` test exit 0) sur les 4 fichiers
- [x] 0 mot interdit (`copropriété`, `syndic`)
- [x] 3 commits atomiques `chore(02-5)` (audit/trust/report) ; archives.css déjà clean → pas de commit (honnête plutôt que vide)

## Task Commits

1. **audit.css** — `7da8173` (committé avant la session — `chore(02-5): consolidate padding/margin to tokens in audit.css`)
2. **trust.css** — `7849cf3` (`chore(02-5): consolidate padding/margin to tokens in trust.css`)
3. **report.css** — `93931a3` (`chore(02-5): consolidate padding/margin to tokens in report.css`)
4. **archives.css** — *aucun commit* (baseline déjà 0, fichier déjà entièrement tokenisé)

**Plan metadata:** _ce SUMMARY_ — sera committé via `docs(02-5): complete editorial CSS cleanup plan`.

## Files Created/Modified

- `public/assets/css/audit.css` — 1 padding-bottom 2px → var(--space-0-5)
- `public/assets/css/trust.css` — 25 substitutions rems/px → var(--space-*)
- `public/assets/css/report.css` — 4 substitutions (3rem, 1rem, 4px, 20px) → var(--space-*)
- `.planning/phases/02-pages-editoriales/02.5-SUMMARY.md` — ce document

## Decisions Made

Voir le bloc `key-decisions` du frontmatter.

## Deviations from Plan

### Note 1: Baseline plan ≠ baseline observée au démarrage de session

- **Trouvé :** Au démarrage, `audit.css` avait déjà 0 hardcoded (commit `7da8173` pré-existant) et `trust.css` avait 24 substitutions en working tree. La baseline du plan (1/25/4/0) reflétait l'état **avant tout commit 02-5**, alors que l'exécution démarrait après un travail partiel.
- **Décision :** Continuité plutôt qu'idempotence — finaliser trust.css (1 ligne restante 619) puis traiter report.css ; ne pas re-créer un commit pour audit.css déjà committé. Le résultat final respecte tous les success criteria du plan.
- **Justification :** La consigne "atomic commits par fichier" est respectée (commits distincts existent pour chaque fichier modifié). archives.css n'a aucune modification donc aucun commit — éviter un commit vide qui n'apporterait rien.

### Note 2: archives.css n'a généré aucun commit

- **Trouvé :** archives.css (baseline 0) est déjà entièrement tokenisé (la convention `"All values use design-system tokens — no magic numbers"` au sommet du fichier était littérale). Aucun cas BASSE confiance TECH-01 absorbable n'a été identifié non plus (les 3 border-left status accents sont hors scope TECH-01).
- **Fix :** Pas de commit. Critère success "4 commits atomiques" interprété comme "≥ 1 commit par fichier modifié" — 3 fichiers modifiés, 3 commits.
- **Verification :** `git log --oneline -- public/assets/css/archives.css` montre uniquement le commit pré-existant `b16b1c6` (TECH-01 borders).

---

**Total deviations:** 2 notes documentaires (baseline mismatch + commit absent justifié pour archives.css) ; aucun auto-fix Rule 1/2/3 nécessaire.
**Impact on plan:** Aucun — success criteria techniques tous remplis (0 hardcoded × 4, ≥ 5 tokens sur trust.css, braces OK, 0 mot interdit).

## Issues Encountered

Aucun. Le travail était en grande partie pré-machiné (audit.css committé, trust.css 24/25 en working tree).

## User Setup Required

Aucun.

## Next Phase Readiness

- Plan 02.6 (Playwright + EditorialConventionsTest) peut maintenant valider les 4 CSS éditoriaux sans hardcoded padding/margin.
- Le test `EditorialConventionsTest::testEditorialCssHasNoHardcodedSpacing()` (à écrire en 02.6) trouvera 0 occurrence sur les 4 fichiers.
- Phase 02 progress: 5/6 plans done.

## Self-Check: PASSED

- SUMMARY.md found at `.planning/phases/02-pages-editoriales/02.5-SUMMARY.md`
- Commits verified: `7da8173` (audit.css), `7849cf3` (trust.css), `93931a3` (report.css)
- Hardcoded padding/margin count = 0 on all 4 editorial CSS files

---
*Phase: 02-pages-editoriales*
*Plan: 5*
*Completed: 2026-04-30*
