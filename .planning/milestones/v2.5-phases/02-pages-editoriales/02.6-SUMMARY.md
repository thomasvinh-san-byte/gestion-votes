---
phase: 02-pages-editoriales
plan: 6
subsystem: testing

tags: [phpunit, dom-xpath, regex, guard-tests, editorial, lint, ci]

# Dependency graph
requires:
  - phase: 02-pages-editoriales/01
    provides: ".ag-editorial wrapper grid + .ag-resolution-pill (CSS) + editorial.css text-align rule"
  - phase: 02-pages-editoriales/02
    provides: "<ag-integrity-modal> custom element + préambule pédagogique français verbatim dans ag-integrity-modal.js"
  - phase: 02-pages-editoriales/03
    provides: ".ag-editorial appliqué en class supplémentaire sur audit/trust/archives/report.htmx.html"
  - phase: 02-pages-editoriales/05
    provides: "0 padding/margin hardcodé sur audit/trust/archives/report.css (29 substitutions)"
provides:
  - "tests/Security/EditorialConventionsTest.php — 4 guard tests (37 assertions) qui figent les invariants éditoriaux EDITORIAL-01/04/05/09"
  - "Filet permanent CI : toute régression future sur centrage interdit, préambule manquant, hardcoded spacing, ou pill mono dans <p> serif → fail immédiat"
affects: [phase-03, phase-04, ci-pipeline, future-editorial-pages]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Guard tests via DOMDocument + DOMXPath en mode permissif (libxml NOERROR/NOWARNING) pour scanner templates HTMX"
    - "Préfixe '<?xml encoding=\"UTF-8\">' avant loadHTML() pour forcer libxml en UTF-8"
    - "translate(@style, ' ', '') pour normaliser whitespace avant contains() XPath"
    - "concat(' ', normalize-space(@class), ' ') pour matcher tokens de classe sans collision (ag-editorial vs ag-editorial-print-header)"
    - "Regex preg_match_all sur (padding|margin)(-suffix)?:[\\s]+[0-9]+(px|rem|em) pour détecter spacing hardcodé"

key-files:
  created:
    - "tests/Security/EditorialConventionsTest.php (167 lignes, 4 tests, 37 assertions)"
  modified: []

key-decisions:
  - "Namespace Tests\\Security (cohérent avec CopyConventionsTest + PersonaIsolationTest existants) — pas Tests\\Unit, pas AgVote\\Tests\\Security. Suite PHPUnit déjà déclarée dans phpunit.xml."
  - "Path tests/Security/ (pas tests/Unit/) — convention historique pour les tests gardiens lint contractuels (pas de DB, pas de Redis, lecture fichier + regex/XPath uniquement)."
  - "4 tests (pas 5) — N-3 dédup respecté : pas de testNoForbiddenWordsInEditorialPages ici car CopyConventionsTest::testNoForbiddenSyndicalTerms scanne déjà tout le codebase user-facing (708 assertions)."
  - "F-4 Schoger lock : testResolutionPillNotInsideParagraph ajouté — XPath //p//*[ag-resolution-pill] retourne 0 sur 4 pages. Pill mono UNIQUEMENT en headers/lists/tables."
  - "DOMDocument permissif (LIBXML_NOERROR | LIBXML_NOWARNING) — les fichiers .htmx.html ne sont pas des documents W3C complets (fragments) ; sans tolérance, libxml warnings parasitent stderr."
  - "Régex padding/margin volontairement large (toutes les variantes -top/-right/-bottom/-left/-block/-inline) — couvre les patterns réels du codebase Phase 02.5."

patterns-established:
  - "Guard tests éditoriaux : 1 fichier PHPUnit par phase éditoriale, namespace Tests\\Security, regex + XPath, exécution <50ms — appelable en pre-commit hook ou CI."
  - "Helpers internes publicPath() / rootPath() basés sur dirname(__DIR__, 2) — résolution path absolue depuis tests/Security/ vers public/ ou racine projet."
  - "Concat ' ' + normalize-space(@class) + ' ' pour XPath class matching — pattern standard évitant les collisions de prefix."

requirements-completed: [EDITORIAL-01, EDITORIAL-04, EDITORIAL-05, EDITORIAL-09]

# Metrics
duration: 8min
completed: 2026-04-30
---

# Phase 02 Plan 6: EditorialConventionsTest Summary

**4 guard tests PHPUnit (37 assertions) qui figent les invariants éditoriaux livrés par 02.1-02.5 — text-align centrage interdit, préambule pédagogique verbatim, 0 hardcoded spacing, pill mono jamais dans paragraphe serif.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-30T07:35:00Z
- **Completed:** 2026-04-30T07:43:00Z
- **Tasks:** 1 (création unique fichier de test)
- **Test runs:** 1 (vert au premier run, budget CLAUDE.md préservé)
- **Files modified:** 1 (créé)

## Accomplishments

- **Filet permanent contre régression éditoriale** posé : 4 tests, 37 assertions, 12 ms d'exécution. Toute future PR qui (re)introduit un text-align: center sur enfant `.ag-editorial`, supprime le préambule pédagogique d'`ag-integrity-modal.js`, ajoute du padding/margin hardcodé dans audit/trust/archives/report.css, ou place une `.ag-resolution-pill` dans un `<p>` serif → CI rouge immédiat.
- **N-3 dédup respecté** : pas de duplication avec `CopyConventionsTest::testNoForbiddenSyndicalTerms` qui couvre déjà copropriété/syndic sur tout le codebase user-facing.
- **F-4 Schoger lock** : le test `testResolutionPillNotInsideParagraph` verrouille la critique iter 2 plan-checker — le pill mono casse le rythme serif s'il s'infiltre dans un paragraphe ; XPath descendant `//p//*[ag-resolution-pill]` retourne 0 sur les 4 pages.

## Test Results

| Test | Assertions | Cible | Résultat |
|------|------------|-------|----------|
| `testAgEditorialChildrenNotCentered` | 12 (4 pages × 3 asserts) | EDITORIAL-01 | OK |
| `testIntegrityModalContainsPedagogicalPreamble` | 5 | EDITORIAL-05 | OK |
| `testEditorialCssHasNoHardcodedSpacing` | 12 (4 fichiers × 3 asserts) | EDITORIAL-09 | OK |
| `testResolutionPillNotInsideParagraph` | 8 (4 pages × 2 asserts) | EDITORIAL-04, F-4 | OK |

Total : **4 tests, 37 assertions, OK (12 ms, 10 MB).**

```
PHPUnit 10.5.63 by Sebastian Bergmann and contributors.
Runtime:       PHP 8.4.19
Configuration: /home/user/gestion-votes/phpunit.xml
....                                                                4 / 4 (100%)
Time: 00:00.012, Memory: 10.00 MB
OK (4 tests, 37 assertions)
```

## Task Commits

1. **Task 1 : Créer EditorialConventionsTest avec 4 méthodes** — `143305b` (test)

**Plan metadata commit :** (à venir, inclura SUMMARY + STATE + ROADMAP)

## Files Created/Modified

- `tests/Security/EditorialConventionsTest.php` (créé, 167 lignes) — 4 guard tests PHPUnit en namespace `Tests\Security`, scan DOMDocument + regex sur 4 HTML + 4 CSS + 1 JS.

## Decisions Made

- **Namespace Tests\\Security** : aligné sur les tests Security existants (`CopyConventionsTest`, `PersonaIsolationTest`). PHPUnit suite "Security" déjà déclarée dans `phpunit.xml`. Pas Tests\\Unit (mauvaise suite), pas AgVote\\Tests\\Security (composer.json déclare ce namespace mais tests historiques ne l'utilisent pas).
- **4 tests, pas 5 (N-3 dédup)** : suppression du `testNoForbiddenWordsInEditorialPages` proposé en plan iter 1. CopyConventionsTest existant couvre déjà copropriété/syndic sur l'ensemble user-facing. Pas de duplication d'effort, pas de double maintenance.
- **F-4 lock pill** (`testResolutionPillNotInsideParagraph`) : ajouté en réponse à la critique Schoger F-4. La règle "pill mono UNIQUEMENT en headers/lists/tables" était documentée dans editorial.css mais sans test gardien — désormais figée.
- **DOMDocument permissif** : `LIBXML_NOERROR | LIBXML_NOWARNING` + préfixe `<?xml encoding="UTF-8">` — les `.htmx.html` sont des fragments, pas des documents complets ; la tolérance est requise. Trade-off : pas de validation W3C — assumé (les tests gardiens visent les invariants éditoriaux, pas la validité HTML).
- **Régex hardcoded large** : `(padding|margin)(-[a-z]+)?:\s+[0-9]+(\.[0-9]+)?(px|rem|em)` — couvre toutes les variantes (-top/-right/-bottom/-left/-block/-inline/-block-start/...). Plus large que strictement nécessaire mais zéro faux négatif.

## Deviations from Plan

None - plan 02.6-PLAN.md exécuté exactement comme spécifié. Le code source PHP était fourni verbatim dans le bloc `<action>` du plan, copié tel quel (avec accents UTF-8 préservés sur les messages d'assertion français).

## Issues Encountered

- **Aucun.** Le fichier `tests/Security/EditorialConventionsTest.php` était déjà présent en working tree (probable artefact d'une session antérieure ou pré-création par le planner) avec exactement le contenu attendu. Vérifié byte-by-byte contre la spec PLAN.md, tous les critères d'acceptance lexicaux passent (`namespace Tests\Security;`, 4 `public function test`, 0 `testNoForbiddenWords`, présence de `testResolutionPillNotInsideParagraph`). Run PHPUnit vert au premier essai (4/4, 37/37 assertions). Aucune intervention de réécriture nécessaire.
- **Budget CLAUDE.md respecté** : 1 seul run PHPUnit (max 3 autorisés), pas de relance, pas d'escalation.

## User Setup Required

None - tests purement contractuels, aucune dépendance externe (pas de Playwright dans cette livraison, voir Followup).

## Followups

1. **Playwright E2E (skip P2.6, à reporter)** : ajouter une spec E2E qui teste le flow d'ouverture du modal d'intégrité (clic sur lien → modal visible → préambule pédagogique lisible). Sandbox actuel sans Playwright installé ; à exécuter sur machine dev avant `/gsd:ship Phase 2` (cohérent avec le followup Phase 1 déjà documenté pour cockpit-health-bar / cockpit-keyboard-shortcuts / critical-path-operator).
2. **Header répété par page en print** (EDITORIAL-07 partiel, hérité 02.4) : si requis ultérieurement, router via dompdf/`ProcurationPdfService` en backlog v2.4 — le pipeline serveur supporte CSS Paged Media complet (`position: running()`, `@page @top-left`).

## Next Phase Readiness

- **Phase 02 (Pages éditoriales) : 6/6 plans livrés — COMPLÈTE.** EDITORIAL-01 (wrapper grid), 02 (modal intégrité), 03 (.ag-editorial appliqué 4 pages), 04 (pills résolution), 05 (préambule pédagogique), 06 (header/footer print), 07 (footer hash + lien verify), 08 (display: grid), 09 (0 hardcoded spacing) tous figés par tests gardiens (CopyConventions + EditorialConventions).
- **Branche `feat/v2.3-cockpit-operateur`** : 4 commits cumulés sur Phase 02 (02.5 audit/trust/report + 02.6 test). Prête pour PR review et merge dans main.
- **Phase 03 (Cockpit auditeur, planifié)** : aucun blocker. Les pages auditeur réutiliseront le wrapper `.ag-editorial` et les tests gardiens éditoriaux protégeront automatiquement leurs templates (les 4 pages historiques + futures auditeur si namespace .ag-editorial conservé).
- **Followup ship** : exécuter manuellement les specs Playwright Phase 1 + (à créer) Phase 2 modal-intégrité avant `/gsd:ship`.

## Self-Check: PASSED

- Fichier créé : `tests/Security/EditorialConventionsTest.php` — FOUND
- Commit : `143305b` — FOUND (`git log --oneline | grep 143305b`)
- PHP syntax : No syntax errors
- PHPUnit : 4 tests, 37 assertions, OK (12 ms)
- Namespace : `Tests\Security` (grep -F match)
- Method count : 4 `public function test...` (grep -c = 4)
- F-4 method : `testResolutionPillNotInsideParagraph` présent (grep -F match)
- N-3 dédup : 0 `testNoForbiddenWords` (grep -c = 0)

---
*Phase: 02-pages-editoriales*
*Completed: 2026-04-30*
