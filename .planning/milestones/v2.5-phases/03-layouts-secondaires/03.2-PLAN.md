---
phase: 03-layouts-secondaires
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified:
  - public/dashboard.htmx.html
  - public/assets/css/dashboard.css
autonomous: true
requirements:
  - DASHBOARD-05
user_setup: []

must_haves:
  truths:
    - "Les 15 shortcut-cards sont préservées (count inchangé) — aucune supprimée"
    - "5 shortcut-cards sont visibles en grille principale (flux quotidien fréquent)"
    - "10 shortcut-cards sont repliées dans <details><summary>Toutes les actions (15)</summary>...</details>"
    - "Le SUMMARY documente les 5 cards retenues et la justification produit (flux quotidien vs config one-shot)"
    - "Le critère de tri est figé : flux quotidien fréquent (créer séance, importer membres, voir résultats, /audit, /report) vs config/admin one-shot"
  artifacts:
    - path: "public/dashboard.htmx.html"
      provides: "Markup avec 5 shortcut-cards en grille principale + 10 wrappées dans <details>"
      contains: "Toutes les actions"
    - path: "public/assets/css/dashboard.css"
      provides: "Styles optionnels pour <summary> et l'état open/closed du <details>"
      contains: "dashboard-shortcuts__more"
  key_links:
    - from: "public/dashboard.htmx.html"
      to: "public/assets/css/dashboard.css"
      via: "classe .dashboard-shortcuts__more sur le <details>"
      pattern: "dashboard-shortcuts__more"
---

<objective>
Auditer les 15 shortcut-cards actuelles du dashboard, en garder 5 visibles selon un critère figé (flux quotidien fréquent), et replier les 10 autres dans un disclosure `<details><summary>Toutes les actions (15)</summary>...</details>`. Aucune card supprimée — le total reste 15.

Purpose: Réduire la charge visuelle initiale du dashboard sans perdre d'accès. Refactoring UI Schoger : *"Reduce by half, see what breaks. Almost nothing breaks."*
Output: `public/dashboard.htmx.html` modifié (5 cards en grille + 10 dans `<details>`) + styles optionnels pour le disclosure dans `public/assets/css/dashboard.css`.
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/03-layouts-secondaires/03-01-SUMMARY.md

<read_first>
- public/dashboard.htmx.html (entier, focus sur la section shortcut-cards : grep `shortcut-card` pour repérer les 15 occurrences et leur contenu — titre, lien, icône)
- public/assets/css/dashboard.css (rapide, juste pour repérer le sélecteur de la grille shortcut et son style)
</read_first>

<product_decision_locked>
**DASHBOARD-05 (figé) :** 5 shortcut-cards visibles + 10 pliées dans `<details><summary>Toutes les actions (15)</summary>...</details>`. **L'exécutant choisit les 5 retenus** en lisant `dashboard.htmx.html` selon ce critère figé :

> **Conserver** les actions du flux quotidien fréquent : créer une séance, importer des membres, voir les résultats, accéder à `/audit`, accéder à `/report`.
> **Replier** les actions de configuration / administration consultées une seule fois ou rarement (paramètres, gestion utilisateurs, exports backoffice, etc.).

Les 5 cards exactement retenues doivent être **nommées dans le SUMMARY** avec une phrase de justification produit chacune.
</product_decision_locked>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit des 15 shortcut-cards et sélection des 5 retenues</name>
  <files>public/dashboard.htmx.html (lecture seule — pas de mutation cette tâche)</files>
  <read_first>
    - public/dashboard.htmx.html : grep `shortcut-card` pour lister les 15 cards, extraire pour chacune (titre, lien, icône)
  </read_first>
  <acceptance_criteria>
    - Une liste numérotée des 15 cards est produite dans la note d'exécution (pas dans le code)
    - 5 cards sont sélectionnées comme "flux quotidien fréquent" avec justification 1 ligne
    - 10 cards sont marquées "à replier" (config/admin one-shot)
    - L'ordre des 5 conservées est intentionnel (priorité du plus utilisé au moins utilisé)
  </acceptance_criteria>
  <action>
1. Lire `public/dashboard.htmx.html` et lister les 15 occurrences de `shortcut-card` (titre + lien + icône pour chacune).
2. Appliquer le critère figé :
   - **Garder (5)** : actions du flux quotidien fréquent. Cibles probables : "Créer une séance", "Importer membres", "Voir résultats", lien vers `/audit`, lien vers `/report`. Adapter selon le markup réel — si une de ces 5 n'existe pas comme shortcut, prendre la card la plus proche dans le critère (ex: "Liste des séances" si "Créer séance" est ailleurs).
   - **Replier (10)** : config/admin one-shot. Typiquement : paramètres, gestion utilisateurs, templates email, exports backoffice, intégrations, etc.
3. Écrire la sélection dans une note de travail (sera reportée dans le SUMMARY de plan en Task 3) :
   ```
   Cards conservées (5, ordre du plus fréquent au moins fréquent) :
   1. {Titre} — {lien} — Justification : {1 ligne}
   2. ...
   Cards repliées (10) :
   1. {Titre} — {lien}
   2. ...
   ```
4. **Aucune mutation de fichier dans cette tâche** : c'est un audit en lecture seule pour cadrer Task 2.
  </action>
  <verify>
    <automated>[ "$(grep -c 'shortcut-card' public/dashboard.htmx.html)" -ge "15" ]</automated>
  </verify>
  <done>15 cards listées ; 5 sélectionnées avec justification ; 10 marquées pour repliement ; aucune mutation de fichier.</done>
</task>

<task type="auto">
  <name>Task 2: Wrapper les 10 cards repliées dans &lt;details&gt; et garder les 5 en grille principale</name>
  <files>public/dashboard.htmx.html, public/assets/css/dashboard.css</files>
  <read_first>
    - Note de travail issue de Task 1 (liste 5 conservées + 10 repliées)
    - public/dashboard.htmx.html (section shortcut-cards complète avec contexte parent)
  </read_first>
  <acceptance_criteria>
    - `grep -c "shortcut-card" public/dashboard.htmx.html` retourne **15** (count inchangé — aucune supprimée)
    - `grep -F "Toutes les actions" public/dashboard.htmx.html` matche
    - `grep -F "<details" public/dashboard.htmx.html` matche au moins une fois dans la zone shortcut
    - `grep -F "<summary>Toutes les actions (15)</summary>" public/dashboard.htmx.html` matche exactement (libellé figé)
    - Les 5 cards conservées sont **hors** du `<details>`, dans la grille principale
    - Les 10 cards repliées sont **dans** le `<details>` (ordre préservé ou regroupé logiquement)
  </acceptance_criteria>
  <action>
1. Réorganiser la section shortcut-cards dans `public/dashboard.htmx.html` :
   - Placer en premier les 5 cards conservées (Task 1) dans leur grille existante.
   - Ajouter à la suite, dans la même section, le bloc :
     ```html
     <details class="dashboard-shortcuts__more">
       <summary>Toutes les actions (15)</summary>
       <div class="dashboard-shortcuts__more-grid">
         <!-- les 10 cards repliées ici, conservant leur markup shortcut-card -->
       </div>
     </details>
     ```
   - **Préserver intégralement** le markup interne de chaque card (classes, liens, attributs HTMX, icônes, libellés français). Seul le wrapping change.
2. Vérifier le compte total après édition : `grep -c "shortcut-card" public/dashboard.htmx.html` doit retourner **15**.
3. Dans `public/assets/css/dashboard.css`, ajouter des styles minimalistes pour le disclosure :
   ```css
   .dashboard-shortcuts__more {
     margin-top: var(--space-6);
   }

   .dashboard-shortcuts__more > summary {
     cursor: pointer;
     font-family: var(--font-sans);
     font-weight: 500;
     padding: var(--space-2) 0;
     list-style: none;
   }

   .dashboard-shortcuts__more > summary::-webkit-details-marker {
     display: none;
   }

   .dashboard-shortcuts__more > summary::before {
     content: "▸ ";
     display: inline-block;
     transition: transform 0.2s ease;
   }

   .dashboard-shortcuts__more[open] > summary::before {
     transform: rotate(90deg);
   }

   .dashboard-shortcuts__more-grid {
     display: grid;
     grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
     gap: var(--space-3);
     margin-top: var(--space-3);
   }

   @media (prefers-reduced-motion: reduce) {
     .dashboard-shortcuts__more > summary::before { transition: none; }
   }
   ```
   Adapter `--space-*` aux tokens réels présents dans `design-system.css`.
4. **Aucun padding/margin/gap numérique** : tokens uniquement.
5. Aucun texte français modifié dans les cards.
  </action>
  <verify>
    <automated>[ "$(grep -c 'shortcut-card' public/dashboard.htmx.html)" = "15" ] && grep -F "Toutes les actions (15)" public/dashboard.htmx.html >/dev/null && grep -F "<details" public/dashboard.htmx.html >/dev/null && grep -F "dashboard-shortcuts__more" public/dashboard.htmx.html >/dev/null && grep -F "dashboard-shortcuts__more" public/assets/css/dashboard.css >/dev/null</automated>
  </verify>
  <done>15 shortcut-cards préservées ; 5 visibles en grille principale, 10 dans `<details>` ; libellé `<summary>Toutes les actions (15)</summary>` exact ; CSS disclosure ajouté avec tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Documenter les 5 cards retenues dans le SUMMARY de plan</name>
  <files>.planning/phases/03-layouts-secondaires/03-02-SUMMARY.md (créé par execute-plan, pas par cette tâche)</files>
  <read_first>
    - Note de travail Task 1
  </read_first>
  <acceptance_criteria>
    - Lors du SUMMARY, lister les 5 cards conservées avec libellé exact et 1 phrase de justification produit chacune
    - Lister les 10 cards repliées (libellés courts suffisent)
    - Référencer le critère figé "flux quotidien fréquent vs config one-shot"
  </acceptance_criteria>
  <action>
Tâche de documentation à produire au moment du SUMMARY (étape `<output>` ci-dessous). Inclure dans le 03-02-SUMMARY.md :

```markdown
## Sélection des 5 shortcut-cards retenues (DASHBOARD-05)

**Critère appliqué :** flux quotidien fréquent (action récurrente du président/secrétaire) vs config/admin one-shot.

### Cards visibles (5)
1. **{Titre exact}** (`{lien}`) — {1 ligne de justification produit}
2. ...

### Cards repliées dans <details> (10)
- {Titre} (`{lien}`)
- ...
```

Aucune action sur le code dans cette tâche : c'est uniquement le contenu attendu du SUMMARY.
  </action>
  <verify>
    <automated>echo "Documentation task — vérifié manuellement à la création du SUMMARY"</automated>
  </verify>
  <done>Le SUMMARY post-exécution contient la liste nommée des 5 + 10 cards et la justification.</done>
</task>

</tasks>

<verification>
- `[ "$(grep -c 'shortcut-card' public/dashboard.htmx.html)" = "15" ]`
- `grep -F "Toutes les actions (15)" public/dashboard.htmx.html`
- `grep -F "<details" public/dashboard.htmx.html`
- `grep -F "dashboard-shortcuts__more" public/assets/css/dashboard.css`
- Aucun texte français de card modifié (lecture diff)
- Aucun string "copropriété" ou "syndic" introduit
</verification>

<success_criteria>
Le dashboard expose 5 shortcut-cards en grille principale (les plus fréquemment utilisées du flux quotidien) et replie les 10 autres derrière un disclosure `<details><summary>Toutes les actions (15)</summary>...</details>`. Le total de 15 cards est préservé. Le SUMMARY nomme explicitement les 5 retenues avec justification.
</success_criteria>

<output>
Créer `.planning/phases/03-layouts-secondaires/03-02-SUMMARY.md` après exécution avec la liste nommée des 5 cards retenues + 10 repliées + justification produit.
</output>
