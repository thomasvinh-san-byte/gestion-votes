---
phase: 03-tokens-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
  - public/assets/css/login.css
  - public/assets/css/postsession.css
  - public/assets/css/analytics.css
  - public/assets/css/vote.css
  - public/assets/css/pages.css
  - public/assets/css/validate.css
  - public/assets/css/wizard.css
  - public/assets/css/meetings.css
  - public/assets/css/components/ag-health-bar.css
autonomous: true
requirements:
  - TOKENS-V26-01
  - TOKENS-V26-02
  - TOKENS-V26-03
tags:
  - css
  - design-tokens
  - refactor

must_haves:
  truths:
    - "Phase 7.2: --border-thick-3 et --border-thin-1-5 disparaissent du codebase (0 hit grep)"
    - "Phase 7.2: --border-thick existe et est utilisé par les anciens callers de --border-thick-3"
    - "Phase 7.2: les 2 callers de --border-thin-1-5 (login.css ligne 253, postsession.css ligne 59) utilisent --border-default ou --border-subtle"
    - "Phase 7.3: les 4 variants --border-emphasis-{primary,warning,danger-dashed,text-inverse-alpha-30} disparaissent du codebase (0 hit grep)"
    - "Phase 7.3: les 4 callers respectifs (validate.css x2, pages.css x1, vote.css x1) utilisent --border-emphasis base + override border-color/border-style inline"
    - "Phase 7.4: les 4 ring variants (--shadow-ring-1px-primary, --shadow-ring-3px-primary-strong, --shadow-ring-inset-1px-danger, --shadow-ring-inset-2px-primary) disparaissent ou sont consolidés"
    - "Phase 7.4: les 4 callers (wizard.css, vote.css, ag-health-bar.css, meetings.css) utilisent un token canonique --shadow-ring-2px-primary OU une valeur inline (0 0 0 2px var(--color-X))"
    - "Aucun var(--border-thick-3), var(--border-thin-1-5), var(--border-emphasis-primary|warning|danger-dashed|text-inverse-alpha-30), var(--shadow-ring-1px-primary), var(--shadow-ring-3px-primary-strong), var(--shadow-ring-inset-1px-danger), var(--shadow-ring-inset-2px-primary) n'est référencé hors design-system.css"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Tokens consolidés post-7.2/7.3/7.4 (10 tokens 1-site retirés ou consolidés)"
      contains: "--border-thick:"
    - path: "public/assets/css/login.css"
      provides: "Migration --border-thin-1-5 → --border-default"
    - path: "public/assets/css/validate.css"
      provides: "Inline border-color override pour emphasis-primary et emphasis-danger-dashed"
  key_links:
    - from: "public/assets/css/design-system.css"
      to: "consumer CSS files"
      via: "var(--token-name)"
      pattern: "var\\(--border-thick\\)|var\\(--border-emphasis\\)"
---

<objective>
Exécuter les Phases 7.2, 7.3, 7.4 du plan de remediation `.planning/v2.5-TOKENS-AUDIT.md` :
- **7.2 Width cleanup** (-2 à -4 tokens) : renommer `--border-thick-3` → `--border-thick`, retirer `--border-thin-1-5` (callers migrent vers `--border-default`).
- **7.3 Emphasis flatten** (-4 tokens) : retirer les 4 variants `--border-emphasis-{primary,warning,danger-dashed,text-inverse-alpha-30}`, callers utilisent `--border-emphasis` base + override inline.
- **7.4 Ring unification** (-3 à -4 tokens) : retirer `--shadow-ring-1px-primary`, `--shadow-ring-3px-primary-strong`, `--shadow-ring-inset-1px-danger`, consolider `--shadow-ring-inset-2px-primary` ou le rendre inline.

**Note**: l'audit v2.5 mentionne `--border-soft` et `--border-none` ; ces tokens **n'existent déjà plus** dans `design-system.css` (vérifié pré-planification, grep retourne 0 hit). Phase 7.2 est donc allégée d'autant.

Purpose: passer sous le seuil de **30 tokens 1-site** (cible TOKENS-V26-04). État pré-phase ~40 (post-7.1 alpha unification déjà shippée v2.5).

Output: design-system.css allégé, 10 callers migrés sur 7 fichiers CSS, 3 commits atomiques (un par sub-phase).
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/gestion-votes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v2.5-TOKENS-AUDIT.md
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 7.2 — Width cleanup (--border-thick-3, --border-thin-1-5)</name>
  <files>
    public/assets/css/design-system.css,
    public/assets/css/login.css,
    public/assets/css/postsession.css,
    public/assets/css/analytics.css,
    public/assets/css/vote.css
  </files>
  <read_first>
    - public/assets/css/design-system.css lignes 360-395 (bloc des bordures, contient --border-emphasis-* et --border-thick-3 ligne 373, --border-thin-1-5 ligne 372)
    - public/assets/css/login.css ligne 253 (caller --border-thin-1-5)
    - public/assets/css/postsession.css ligne 59 (caller --border-thin-1-5)
    - public/assets/css/analytics.css ligne 463 (caller --border-thick-3)
    - public/assets/css/vote.css ligne 307 (caller --border-thick-3)
    - .planning/v2.5-TOKENS-AUDIT.md section "Phase 7.2 (later) — width / soft / none cleanup"
  </read_first>
  <action>
    Trois opérations atomiques sur design-system.css + migration des 4 callers.

    **Étape A — design-system.css (lignes 372-373) :**

    Remplacer les 2 lignes :
    ```
      --border-thin-1-5:                1.5px solid var(--color-border);                       /* width 1.5px (login, postsession) */
      --border-thick-3:                 3px solid var(--color-border);                         /* width 3px (analytics, vote) */
    ```
    Par 1 ligne (rename `--border-thick-3` → `--border-thick`, suppression de `--border-thin-1-5`) :
    ```
      --border-thick:                   3px solid var(--color-border);                         /* width 3px (analytics, vote) */
    ```

    **Étape B — Migration callers `--border-thick-3` → `--border-thick` :**
    - public/assets/css/analytics.css ligne 463 : `border: var(--border-thick-3);` → `border: var(--border-thick);`
    - public/assets/css/vote.css ligne 307 : `border: var(--border-thick-3);` → `border: var(--border-thick);`

    **Étape C — Migration callers `--border-thin-1-5` → `--border-default` :**

    `--border-default` existe déjà dans design-system.css (1px solid var(--color-border)). Le passage de 1.5px à 1px est imperceptible visuellement (perte de 0.5px sur des bordures simples non-décoratives — login form input, postsession card divider). Si la régression visuelle est jugée trop forte au moment de l'inspection finale (Plan 03-02), on inlinera `border: 1.5px solid var(--color-border);` cas-par-cas.

    - public/assets/css/login.css ligne 253 : `border: var(--border-thin-1-5);` → `border: var(--border-default);`
    - public/assets/css/postsession.css ligne 59 : `border: var(--border-thin-1-5);` → `border: var(--border-default);`

    **Étape D — Commit atomique :**
    ```
    git add public/assets/css/design-system.css public/assets/css/login.css public/assets/css/postsession.css public/assets/css/analytics.css public/assets/css/vote.css
    git commit -m "refactor(03-01): consolidate border width tokens (Phase 7.2)" -m "- rename --border-thick-3 → --border-thick (analytics, vote)
- retire --border-thin-1-5 (login, postsession) → use --border-default
- Phase 7.2 of v2.5-TOKENS-AUDIT.md remediation plan"
    ```

    **Pas de tests automatisés à lancer** (pas de PHP impacté). Verification = grep gates Étape E.

    **Étape E — Grep gates obligatoires** (lancer avant le commit, si non-zéro corriger) :
    ```
    grep -rn "border-thick-3\|border-thin-1-5" public/assets/css/ app/ public/*.html
    # Doit retourner 0 ligne
    grep -c "^\s*--border-thick:" public/assets/css/design-system.css
    # Doit retourner 1
    ```
  </action>
  <verify>
    <automated>bash -c 'set -e; test "$(grep -rEc "border-thick-3|border-thin-1-5" public/assets/css/ app/ public/*.html 2>/dev/null | grep -v ":0$" | wc -l)" = "0" && grep -E "^\s*--border-thick:\s" public/assets/css/design-system.css | grep -c "^" | grep -q "^1$" && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "border-thick-3" public/assets/css/ app/ public/*.html` retourne 0 hit
    - `grep -rn "border-thin-1-5" public/assets/css/ app/ public/*.html` retourne 0 hit
    - `grep -E "^\s*--border-thick:\s" public/assets/css/design-system.css` retourne exactement 1 hit (ligne 372 nouveau bloc)
    - `grep -n "var(--border-thick)" public/assets/css/analytics.css public/assets/css/vote.css` retourne 2 hits (1 par fichier)
    - `grep -n "var(--border-default)" public/assets/css/login.css public/assets/css/postsession.css` retourne au moins 2 hits cumulés (les anciens var(--border-thin-1-5) migrés)
    - 1 commit atomique avec message `refactor(03-01): consolidate border width tokens (Phase 7.2)`
    - `php -l` n'est pas applicable (pas de PHP touché)
  </acceptance_criteria>
  <done>5 fichiers modifiés, 1 commit atomique Phase 7.2 shippé, 2 tokens 1-site retirés (--border-thick-3 et --border-thin-1-5).</done>
</task>

<task type="auto">
  <name>Task 2: Phase 7.3 — Emphasis flatten (4 variants → inline override)</name>
  <files>
    public/assets/css/design-system.css,
    public/assets/css/pages.css,
    public/assets/css/validate.css,
    public/assets/css/vote.css
  </files>
  <read_first>
    - public/assets/css/design-system.css lignes 363-388 (bloc emphasis : --border-emphasis base ligne 363, variants 365-367 + 379-380 + 386)
    - public/assets/css/pages.css ligne 274 (caller --border-emphasis-warning)
    - public/assets/css/validate.css lignes 47-56 (callers --border-emphasis-primary ligne 48, --border-emphasis-danger-dashed ligne 55)
    - public/assets/css/vote.css ligne 819 (caller --border-emphasis-text-inverse-alpha-30)
    - .planning/v2.5-TOKENS-AUDIT.md section "Phase 7.3 (later) — emphasis flatten" et tableau "Emphasis variants → consolider"
  </read_first>
  <action>
    Retirer les 4 variants emphasis de design-system.css et migrer les 4 callers vers `--border-emphasis` base + override inline.

    **Conservés** (multi-callers, restent en tokens) :
    - `--border-emphasis: 2px solid var(--color-border);` (ligne 363 — base)
    - `--border-emphasis-dashed: 2px dashed var(--color-border);` (ligne 364)
    - `--border-emphasis-success: 2px solid var(--color-success);` (ligne 365)
    - `--border-emphasis-danger: 2px solid var(--color-danger);` (ligne 366)

    **Retirés** (1-site chacun) :
    - `--border-emphasis-primary` (ligne 367)
    - `--border-emphasis-warning` (ligne 379)
    - `--border-emphasis-danger-dashed` (ligne 380)
    - `--border-emphasis-text-inverse-alpha-30` (ligne 386)

    **Étape A — design-system.css :**
    Supprimer ligne 367 (`--border-emphasis-primary: ...`), ligne 379 (`--border-emphasis-warning: ...`), ligne 380 (`--border-emphasis-danger-dashed: ...`), ligne 386 (`--border-emphasis-text-inverse-alpha-30: ...`).

    **Étape B — Migration callers :**

    - public/assets/css/validate.css ligne 48 : remplacer
      ```
      border: var(--border-emphasis-primary);
      ```
      par les 2 lignes :
      ```
      border: var(--border-emphasis);
      border-color: var(--color-primary);
      ```

    - public/assets/css/validate.css ligne 55 : remplacer
      ```
      border: var(--border-emphasis-danger-dashed);
      ```
      par les 3 lignes :
      ```
      border: var(--border-emphasis);
      border-style: dashed;
      border-color: var(--color-danger);
      ```

    - public/assets/css/pages.css ligne 274 : remplacer
      ```
      border: var(--border-emphasis-warning);
      ```
      par les 2 lignes :
      ```
      border: var(--border-emphasis);
      border-color: var(--color-warning);
      ```

    - public/assets/css/vote.css ligne 819 : remplacer
      ```
      border: var(--border-emphasis-text-inverse-alpha-30);
      ```
      par les 2 lignes :
      ```
      border: var(--border-emphasis);
      border-color: color-mix(in oklch, var(--color-text-inverse) 30%, transparent);
      ```

    **Étape C — Commit atomique :**
    ```
    git add public/assets/css/design-system.css public/assets/css/validate.css public/assets/css/pages.css public/assets/css/vote.css
    git commit -m "refactor(03-01): flatten emphasis variants (Phase 7.3)" -m "- retire --border-emphasis-{primary,warning,danger-dashed,text-inverse-alpha-30}
- callers use --border-emphasis base + inline border-color/border-style override
- Phase 7.3 of v2.5-TOKENS-AUDIT.md remediation plan
- 4 tokens 1-site removed"
    ```

    **Étape D — Grep gates** (lancer avant commit) :
    ```
    grep -rEn "border-emphasis-(primary|warning|danger-dashed|text-inverse-alpha-30)" public/assets/css/ app/ public/*.html
    # Doit retourner 0 ligne (callers migrés ET tokens retirés du design-system)
    ```
  </action>
  <verify>
    <automated>bash -c 'set -e; HITS=$(grep -rEc "border-emphasis-(primary|warning|danger-dashed|text-inverse-alpha-30)" public/assets/css/ app/ public/*.html 2>/dev/null | grep -v ":0$" | wc -l); test "$HITS" = "0" && grep -cE "^\s*--border-emphasis:\s" public/assets/css/design-system.css | grep -q "^1$" && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rEn "border-emphasis-(primary|warning|danger-dashed|text-inverse-alpha-30)" public/assets/css/ app/ public/*.html` retourne 0 hit
    - `grep -E "^\s*--border-emphasis:\s" public/assets/css/design-system.css` retourne 1 hit (base conservée)
    - `grep -E "^\s*--border-emphasis-(success|danger|dashed):\s" public/assets/css/design-system.css` retourne 3 hits (les 3 multi-callers conservés)
    - validate.css ligne ~48 contient `border: var(--border-emphasis);` suivi de `border-color: var(--color-primary);`
    - validate.css ligne ~55-58 contient `border: var(--border-emphasis);` suivi de `border-style: dashed;` et `border-color: var(--color-danger);`
    - pages.css ligne ~274 contient `border: var(--border-emphasis);` suivi de `border-color: var(--color-warning);`
    - vote.css ligne ~819 contient `border: var(--border-emphasis);` suivi de `border-color: color-mix(in oklch, var(--color-text-inverse) 30%, transparent);`
    - 1 commit atomique avec message `refactor(03-01): flatten emphasis variants (Phase 7.3)`
  </acceptance_criteria>
  <done>4 fichiers modifiés, 1 commit atomique Phase 7.3 shippé, 4 tokens 1-site retirés.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 7.4 — Ring variants unification</name>
  <files>
    public/assets/css/design-system.css,
    public/assets/css/wizard.css,
    public/assets/css/vote.css,
    public/assets/css/meetings.css,
    public/assets/css/components/ag-health-bar.css
  </files>
  <read_first>
    - public/assets/css/design-system.css lignes 542-549 (bloc des shadow-ring : --shadow-ring-1px-primary ligne 542, ring-2px-* lignes 543-546, ring-3px-primary-strong ligne 547, ring-inset-2px-primary ligne 548, ring-inset-1px-danger ligne 549)
    - public/assets/css/wizard.css ligne 656 (caller --shadow-ring-1px-primary)
    - public/assets/css/vote.css ligne 1741 (caller --shadow-ring-3px-primary-strong avec !important)
    - public/assets/css/meetings.css ligne 470 (caller --shadow-ring-inset-2px-primary)
    - public/assets/css/components/ag-health-bar.css ligne 106 (caller --shadow-ring-inset-1px-danger)
    - .planning/v2.5-TOKENS-AUDIT.md section "Phase 7.4 (later) — ring variants unification" et tableau "Shadow ring variants"
  </read_first>
  <action>
    Unifier toutes les rings à 2px width par défaut. Conserver les 4 ring-2px-* sémantiques (multi-callers ou kept). Retirer les 4 variants 1-site (1px, 3px, inset-1px, inset-2px) et migrer leurs callers.

    **Stratégie de consolidation :**
    - `--shadow-ring-1px-primary` (wizard hover, subtil) → migration vers `--shadow-ring-2px-border` (déjà existant). 2px sur surface neutre = équivalent perceptuel à 1px primary pour un hover wizard, et le token `border` est plus neutre que `primary` pour un état hover non-sélection.
      *Rationale:* le caller wizard.css:656 est un hover sur card template — l'audit suggère "unifier 1px→2px primary" mais l'effet visuel d'un 2px primary serait trop fort pour un hover ; on choisit `--shadow-ring-2px-border` qui rend l'effet aussi subtil que l'original 1px primary tout en utilisant un token canonique. Décision documentée ici, à valider lors de l'inspection visuelle Plan 03-02.
    - `--shadow-ring-3px-primary-strong` (vote sélection avec `!important`) → inline `0 0 0 3px var(--color-primary)` car c'est un cas d'emphasis intentionnel unique (la sélection de bulletin de vote).
    - `--shadow-ring-inset-1px-danger` (health bar quorum missed) → inline `inset 0 0 0 1px var(--color-danger)` car cas spécifique au composant health-bar.
    - `--shadow-ring-inset-2px-primary` (calendar today) → inline `inset 0 0 0 2px var(--color-primary)` car cas spécifique calendar today indicator.

    **Étape A — design-system.css :**

    Supprimer 4 lignes :
    - ligne 542 : `--shadow-ring-1px-primary: 0 0 0 1px var(--color-primary);`
    - ligne 547 : `--shadow-ring-3px-primary-strong: 0 0 0 3px var(--color-primary);`
    - ligne 548 : `--shadow-ring-inset-2px-primary: inset 0 0 0 2px var(--color-primary);`
    - ligne 549 : `--shadow-ring-inset-1px-danger: inset 0 0 0 1px var(--color-danger);`

    Conserver les 4 ring-2px-{surface,border,danger,success} (lignes 543-546) — *keep* per audit (variants sémantiques uniques par rôle, justifiables comme tokens canoniques).

    **Étape B — Migration callers :**

    - public/assets/css/wizard.css ligne 656 :
      ```
      box-shadow: var(--shadow-ring-1px-primary);
      ```
      →
      ```
      box-shadow: var(--shadow-ring-2px-border);
      ```

    - public/assets/css/vote.css ligne 1741 :
      ```
      box-shadow: var(--shadow-ring-3px-primary-strong) !important;
      ```
      →
      ```
      box-shadow: 0 0 0 3px var(--color-primary) !important;
      ```

    - public/assets/css/meetings.css ligne 470 :
      ```
      box-shadow: var(--shadow-ring-inset-2px-primary);
      ```
      →
      ```
      box-shadow: inset 0 0 0 2px var(--color-primary);
      ```

    - public/assets/css/components/ag-health-bar.css ligne 106 :
      ```
      box-shadow: var(--shadow-ring-inset-1px-danger);
      ```
      →
      ```
      box-shadow: inset 0 0 0 1px var(--color-danger);
      ```

    **Étape C — Commit atomique :**
    ```
    git add public/assets/css/design-system.css public/assets/css/wizard.css public/assets/css/vote.css public/assets/css/meetings.css public/assets/css/components/ag-health-bar.css
    git commit -m "refactor(03-01): unify ring variants (Phase 7.4)" -m "- retire --shadow-ring-1px-primary (wizard → ring-2px-border)
- retire --shadow-ring-3px-primary-strong (vote → inline 3px primary !important)
- retire --shadow-ring-inset-2px-primary (meetings calendar → inline)
- retire --shadow-ring-inset-1px-danger (ag-health-bar → inline)
- keep --shadow-ring-2px-{surface,border,danger,success} as canonical sem rings
- Phase 7.4 of v2.5-TOKENS-AUDIT.md remediation plan
- 4 tokens 1-site removed"
    ```

    **Étape D — Grep gates :**
    ```
    grep -rEn "shadow-ring-1px-primary|shadow-ring-3px-primary-strong|shadow-ring-inset-1px-danger|shadow-ring-inset-2px-primary" public/assets/css/ app/ public/*.html
    # Doit retourner 0 ligne
    grep -cE "^\s*--shadow-ring-2px-(surface|border|danger|success):" public/assets/css/design-system.css
    # Doit retourner 4
    ```
  </action>
  <verify>
    <automated>bash -c 'set -e; HITS=$(grep -rEc "shadow-ring-1px-primary|shadow-ring-3px-primary-strong|shadow-ring-inset-1px-danger|shadow-ring-inset-2px-primary" public/assets/css/ app/ public/*.html 2>/dev/null | grep -v ":0$" | wc -l); test "$HITS" = "0" && KEPT=$(grep -cE "^\s*--shadow-ring-2px-(surface|border|danger|success):" public/assets/css/design-system.css); test "$KEPT" = "4" && echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rEn "shadow-ring-1px-primary|shadow-ring-3px-primary-strong|shadow-ring-inset-1px-danger|shadow-ring-inset-2px-primary" public/assets/css/ app/ public/*.html` retourne 0 hit
    - `grep -cE "^\s*--shadow-ring-2px-(surface|border|danger|success):" public/assets/css/design-system.css` retourne 4 (les 4 ring-2px-* sémantiques conservées)
    - wizard.css ligne ~656 contient `box-shadow: var(--shadow-ring-2px-border);`
    - vote.css ligne ~1741 contient `box-shadow: 0 0 0 3px var(--color-primary) !important;`
    - meetings.css ligne ~470 contient `box-shadow: inset 0 0 0 2px var(--color-primary);`
    - components/ag-health-bar.css ligne ~106 contient `box-shadow: inset 0 0 0 1px var(--color-danger);`
    - 1 commit atomique avec message `refactor(03-01): unify ring variants (Phase 7.4)`
    - 4 tokens 1-site retirés
  </acceptance_criteria>
  <done>5 fichiers modifiés, 1 commit atomique Phase 7.4 shippé. Total Plan 03-01: 3 commits, 10 tokens 1-site retirés (2 width + 4 emphasis + 4 ring).</done>
</task>

</tasks>

<verification>
Plan-level grep gates (à lancer après les 3 commits) :

```
# Aucun token retiré ne doit subsister
grep -rEn "border-thick-3|border-thin-1-5|border-emphasis-(primary|warning|danger-dashed|text-inverse-alpha-30)|shadow-ring-1px-primary|shadow-ring-3px-primary-strong|shadow-ring-inset-(1px-danger|2px-primary)" public/assets/css/ app/ public/*.html
# → 0 hit attendu

# Token --border-thick (renommé) doit exister 1 fois en définition
grep -cE "^\s*--border-thick:\s" public/assets/css/design-system.css
# → 1 attendu

# 3 commits atomiques shippés sur la branche courante
git log --oneline -3 | grep -E "Phase 7\.[234]"
# → 3 lignes attendues
```

**Régression visuelle :** déférée à Plan 03-02 (note dev-machine — inspection 5 pages échantillon login/dashboard/operator/audit/settings).
</verification>

<success_criteria>
- 3 commits atomiques shippés (un par sub-phase 7.2 / 7.3 / 7.4)
- 10 tokens 1-site retirés ou consolidés du design-system.css
- 0 référence aux 10 tokens retirés dans le codebase (grep -r retourne 0)
- 7 fichiers CSS callers migrés sans casser leur sélecteur ou propriété adjacente
- Plan 03-02 peut commencer (audit final + smoke regression note)
</success_criteria>

<output>
After completion, create `.planning/phases/03-tokens-cleanup/03-01-SUMMARY.md` listing :
- 3 commits SHAs (un par sub-phase)
- Décompte tokens 1-site avant/après chaque sub-phase
- Liste des 10 tokens retirés
- Liste des 7 fichiers callers migrés avec ligne approximative
- Note pour Plan 03-02 : régression visuelle à inspecter sur 5 pages échantillon (login, dashboard, operator, audit, settings)
</output>
