---
phase: 51-utility-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/trust.htmx.html
  - public/assets/css/trust.css
  - public/assets/js/pages/trust.js
  - public/validate.htmx.html
  - public/assets/css/validate.css
  - public/assets/js/pages/validate.js
  - public/docs.htmx.html
  - public/assets/css/doc.css
  - public/assets/js/pages/docs-viewer.js
autonomous: true
requirements: [UTL-05, WIRE-01, WIRE-02]

must_haves:
  truths:
    - "Trust page displays integrity dashboard with KPI stats fetched from backend — no hardcoded values"
    - "Trust page anomalies list filters by severity (all, danger, warning, info) and coherence checks render"
    - "Trust page motions table shows resolution data with coherence column"
    - "Trust page audit log shows table and timeline views with category chip filters and search"
    - "Validate page loads session summary stats from backend API"
    - "Validate page pre-validation checklist renders check items with pass/fail status"
    - "Validate page validation modal requires checkbox + typing VALIDER to enable confirm button"
    - "Validate page validation submits to real API with president name"
    - "Docs page renders markdown documentation from API with sidebar index, content area, and table of contents"
    - "No JS console errors on any of the 3 pages — all DOM IDs preserved for trust.js, validate.js, docs-viewer.js"
  artifacts:
    - path: "public/trust.htmx.html"
      provides: "Complete trust/audit page — integrity dashboard, anomalies, checks, motions table, audit log with table+timeline"
      contains: "integrityStatus"
    - path: "public/assets/css/trust.css"
      provides: "Trust page styles — integrity stats, severity pills, anomaly cards, audit table+timeline, modal"
      contains: "integrity-summary"
    - path: "public/validate.htmx.html"
      provides: "Complete validation page — summary stats, checklist, signature form, validation modal"
      contains: "validateModal"
    - path: "public/assets/css/validate.css"
      provides: "Validate page styles — summary grid, checklist, validation zone, modal"
      contains: "validation-zone"
    - path: "public/docs.htmx.html"
      provides: "Complete docs viewer page — sidebar index, markdown content area, table of contents rail"
      contains: "docContent"
    - path: "public/assets/css/doc.css"
      provides: "Docs page styles — three-column layout, sidebar index, prose content, TOC rail"
      contains: "doc-layout"
  key_links:
    - from: "public/trust.htmx.html"
      to: "public/assets/js/pages/trust.js"
      via: "DOM IDs preserved for JS binding"
      pattern: "meetingSelect|meetingTitle|statusBox|integrityStatus|kpiIntegrity|kpiEvents|kpiAnomalies|kpiLastSession|kpiMotions|kpiPresent|kpiBallots|integrityHash|integrityHashValue|btnCopyHash|badgeAnomalies|countDanger|countWarning|countInfo|anomaliesList|severityFilters|checksList|motionsTbody|auditTableBody|auditLogFilter|auditTableView|auditTimelineView|auditViewToggle|auditCategoryChips|auditSelectAll|auditExportBar|auditSelectedCount|auditEventModal|auditModalTitle|auditModalBody|modalEventId|modalEventTime|modalEventAction|modalEventCategory|modalEventUser|modalEventDetail|modalEventHash|auditModalClose|btnExportAudit|btnExportSelection|btnExportSelectedCsv|btnExportSelectedJson|btnRefresh|btnRecheck|btnExportTrust"
    - from: "public/trust.htmx.html"
      to: "/api/v1/trust_*.php"
      via: "fetch calls for trust data"
      pattern: "api.*trust"
    - from: "public/validate.htmx.html"
      to: "public/assets/js/pages/validate.js"
      via: "DOM IDs preserved for JS binding"
      pattern: "meetingTitle|meetingName|meetingDateCtx|meetingContext|sumMembers|sumPresent|sumMotions|sumAdopted|sumRejected|sumBallots|sumQuorum|sumDuration|summaryGrid|checksList|readyBadge|btnValidate|presidentName|validationZone|validateMsg|validateModal|confirmIrreversible|btnModalConfirm|btnModalCancel|confirmText|btnRefresh|btnRecheck|notif_box"
    - from: "public/validate.htmx.html"
      to: "/api/v1/validate_meeting.php"
      via: "fetch call for validation"
      pattern: "api.*validate"
    - from: "public/docs.htmx.html"
      to: "public/assets/js/pages/docs-viewer.js"
      via: "DOM IDs preserved for JS binding"
      pattern: "docIndex|docContent|docTitle|breadcrumbCurrent|breadcrumbDir|breadcrumbDirSep|docTocRail|tocList"
    - from: "public/assets/js/pages/docs-viewer.js"
      to: "/api/v1/docs.php"
      via: "fetch call for documentation content"
      pattern: "api.*docs"
---

<objective>
Ground-up rebuild of the Trust (audit/control), Validate (session validation), and Docs (documentation viewer) pages — complete HTML+CSS rewrite from scratch with v4.3 design language. These are the three verification/informational pages. Preserve all DOM IDs that trust.js, validate.js, and docs-viewer.js target. Verify integrity dashboard, checklist, validation modal, and docs rendering all wire correctly.

Purpose: Trust provides legal compliance verification. Validate is the irreversible session archival gate. Docs is the self-service documentation browser. All three must display backend data correctly with no hardcoded values.
Output: New trust.htmx.html + trust.css, new validate.htmx.html + validate.css, new docs.htmx.html + doc.css, with all three JS files verified working.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-utility-pages/51-CONTEXT.md

@public/trust.htmx.html
@public/assets/css/trust.css
@public/assets/js/pages/trust.js
@public/validate.htmx.html
@public/assets/css/validate.css
@public/assets/js/pages/validate.js
@public/docs.htmx.html
@public/assets/css/doc.css
@public/assets/js/pages/docs-viewer.js
@public/assets/css/app.css
@public/dashboard.htmx.html
@public/assets/js/services/meeting-context.js

<interfaces>
<!-- Critical DOM IDs that trust.js targets — ALL must exist in new HTML -->

Meeting selector:
- #meetingSelect — meeting dropdown select
- #meetingTitle — subtitle in header
- #statusBox — status display box

Integrity dashboard:
- #integrityStatus — integrity status stat (has .success/.warning/.danger class toggled by JS)
- #kpiIntegrity — integrity percentage value
- #kpiEvents — events count value
- #kpiAnomalies — anomalies count value
- #kpiLastSession — last session value
- #kpiMotions — motions count value
- #kpiPresent — present count value
- #kpiBallots — ballots count value
- #integrityHash — hash container (hidden attr toggled)
- #integrityHashValue — hash value code element
- #btnCopyHash — copy hash button

Anomalies:
- #badgeAnomalies — anomaly count badge
- #countDanger / #countWarning / #countInfo — severity count spans
- #anomaliesList — anomalies list container (rendered by JS)
- #severityFilters — severity filter buttons container
- .severity-pill — severity filter buttons with data-severity attribute
- [data-severity="all|danger|warning|info"] — severity filter values

Checks:
- #checksList — coherence checks container (rendered by JS)

Motions table:
- #motionsTbody — motions table body (rendered by JS)

Audit log:
- #auditTableBody — audit table body (rendered by JS)
- #auditLogFilter — audit search input
- #auditTableView — table view container
- #auditTimelineView — timeline view container (hidden attr toggled)
- #auditViewToggle — view toggle container
- .audit-view-btn — view toggle buttons with data-view attribute
- [data-view="table|timeline"] — view toggle values
- #auditCategoryChips — category chip filters container
- .audit-chip — category chip buttons with data-category attribute
- [data-category="all|votes|presences|security|system"] — category values
- #auditSelectAll — select all checkbox
- #auditExportBar — export bar (hidden attr toggled)
- #auditSelectedCount — selected count text
- #btnExportSelectedCsv / #btnExportSelectedJson — selected export buttons
- #btnExportAudit — full export button
- #btnExportSelection — selection export button (disabled attr)

Event detail modal:
- #auditEventModal — modal overlay (hidden attr toggled)
- #auditModalTitle — modal title
- #auditModalBody — modal body
- #modalEventId / #modalEventTime / #modalEventAction / #modalEventCategory / #modalEventUser / #modalEventDetail / #modalEventHash — modal field values
- #auditModalClose — modal close button

Actions:
- #btnRefresh — refresh button
- #btnRecheck — recheck button
- #btnExportTrust — export trust report button

Drawer:
- [data-drawer="anomalies|infos|context"] — drawer triggers
- #drawer / #drawerTitle / #drawerBody

<!-- Critical DOM IDs that validate.js targets — ALL must exist in new HTML -->

Context:
- #meetingTitle — header subtitle
- #meetingName — meeting name in alert
- #meetingDateCtx — meeting date in alert
- #meetingContext — context alert (display toggled)
- #notif_box — notification box

Summary stats:
- #summaryGrid — summary grid container
- #sumMembers / #sumPresent / #sumMotions / #sumAdopted / #sumRejected / #sumBallots / #sumQuorum / #sumDuration — stat values

Checklist:
- #checksList — checks list container (rendered by JS)
- #readyBadge — ready status badge
- #btnRecheck — recheck button

Validation:
- #btnValidate — validate button (disabled attr toggled)
- #presidentName — president name input (required, with pattern)
- #validationZone — validation zone container
- #validateMsg — validation message alert (display toggled)

Modal:
- #validateModal — modal backdrop (hidden attr toggled)
- #confirmIrreversible — irreversibility checkbox
- #btnModalConfirm — confirm button (disabled attr toggled)
- #btnModalCancel — cancel button
- #confirmText — confirmation text input ("VALIDER")

Actions:
- #btnRefresh — refresh button

Drawer:
- [data-drawer="infos|context"] — drawer triggers
- #drawer / #drawerTitle / #drawerBody

<!-- Critical DOM IDs that docs-viewer.js targets — ALL must exist in new HTML -->

Navigation:
- #docIndex — sidebar document index nav
- #docTitle — page title h1
- #breadcrumbCurrent — current breadcrumb span
- #breadcrumbDir — directory breadcrumb span (display toggled)
- #breadcrumbDirSep — directory separator icon (display toggled)

Content:
- #docContent — main content article (markdown rendered here)
- #docTocRail — table of contents sidebar (display toggled)
- #tocList — TOC list ul

Drawer:
- #drawer / #drawerTitle / #drawerBody
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Trust page (HTML+CSS)</name>
  <read_first>
    - public/assets/js/pages/trust.js (full read — every getElementById, querySelector, data-attribute, event listener, class manipulation)
    - public/trust.htmx.html (current HTML structure — all sections)
    - public/assets/css/trust.css (current styles)
    - public/assets/js/services/meeting-context.js (meeting context pattern)
    - public/dashboard.htmx.html (reference v4.3 page-title pattern)
  </read_first>
  <files>public/trust.htmx.html, public/assets/css/trust.css</files>
  <action>
    **HTML rewrite (trust.htmx.html):**
    Rewrite from scratch using v4.3 design language. Structure:
    1. Standard head with critical-tokens, theme-init.js, fonts, app.css + trust.css. NOTE: Remove htmx vendor script if present (not needed for rebuilt page)
    2. data-page-role="auditor,assessor" on html element
    3. app-shell with app-sidebar (data-include-sidebar data-page="trust")
    4. app-header: left side with icon-shield-check "Controle & Audit" + p#meetingTitle "Verification de coherence". Right side: #btnExportTrust btn-sm btn-secondary (icon-download + "Exporter le rapport"), #btnRefresh btn-secondary (icon-refresh-cw + "Actualiser"), drawer buttons for anomalies, infos, context
    5. app-main#main-content > div.container:
       - Page header zone: v4.3 gradient accent bar (4px), icon-shield-check, breadcrumb
       - div#notif_box.hidden.mb-4
       - Context selection card.mb-6: grid 2-col with form-group select#meetingSelect + form-group div#statusBox
       - Integrity dashboard card.mb-6: card-header "Tableau de bord d'integrite", card-body with .integrity-summary grid (7 stat items: #integrityStatus.success, #integrityEvents.primary, #integrityAnomalies.danger, lastSession, #integrityMotions, #integrityPresent, #integrityBallots — each with .integrity-stat-value and .integrity-stat-label). Plus #integrityHash hidden div with label + code#integrityHashValue + #btnCopyHash
       - Grid 2-col.mb-6:
         Left: Anomalies card — header with icon-alert-circle "Anomalies detectees" + span#badgeAnomalies.badge.badge-danger, body with #severityFilters (button.severity-pill.active[data-severity="all"] "Tous", .danger[data-severity="danger"] with #countDanger, .warning[data-severity="warning"] with #countWarning, .info[data-severity="info"] with #countInfo), div#anomaliesList (default empty state)
         Right: Checks card — header with icon-check-circle "Controles de coherence" + #btnRecheck, body div#checksList (default spinner)
       - Motions table card.mb-6: card-header icon-clipboard-list "Resolutions & Decisions" + description. card-body.p-0 table with thead (Resolution, Statut, Pour, Contre, Abstention, Total, Decision, Coherence) + tbody#motionsTbody
       - Audit log card: card-header with icon-scroll "Journal d'audit" + #auditViewToggle (table/timeline buttons with data-view), #btnExportSelection disabled, #btnExportAudit. card-body with #auditCategoryChips (all/votes/presences/security/system chips with data-category), input#auditLogFilter search. card-body.p-0 with #auditTableView (table with thead: checkbox#auditSelectAll, #, Horodatage, Evenement, Utilisateur, SHA-256 + tbody#auditTableBody) and #auditTimelineView.audit-timeline hidden. #auditExportBar hidden with #auditSelectedCount + export buttons
       - Event detail modal #auditEventModal hidden: .audit-modal with header (#auditModalTitle + #auditModalClose), body #auditModalBody with rows for each field (#modalEventId, #modalEventTime, #modalEventAction, #modalEventCategory, #modalEventUser, #modalEventDetail, #modalEventHash)
    6. Standard mobile-footer
    7. Drawer: backdrop + aside#drawer with #drawerTitle + #drawerBody
    8. Scripts: utils.js, components/index.js, shared.js, shell.js, meeting-context.js, trust.js

    **CSS rewrite (trust.css):**
    Rewrite from scratch:
    - .integrity-summary: display grid, grid-template-columns repeat(auto-fill, minmax(120px, 1fr)), gap var(--space-4)
    - .integrity-stat: text-align center, padding var(--space-4), bg surface, radius-lg, border-left 3px solid transparent. .success: border-color var(--color-success). .primary: border-color var(--color-accent). .danger: border-color var(--color-danger)
    - .integrity-stat-value: font-size 1.75rem, font-weight 800, line-height 1.2
    - .integrity-stat-label: font-size 0.75rem, text-secondary, margin-top var(--space-1)
    - .integrity-hash: display flex, align-items center, gap var(--space-3), padding var(--space-3), bg surface-alt, radius-md, margin-top var(--space-4), font-size 0.75rem
    - .integrity-hash-value: font-family var(--font-mono), word-break break-all, flex 1
    - .severity-filters: display flex, gap var(--space-2), flex-wrap wrap, margin-bottom var(--space-4)
    - .severity-pill: padding var(--space-1) var(--space-3), border-radius 9999px, font-size 0.8rem, border 1px solid var(--color-border), cursor pointer, display flex items-center gap-1. .active: bg accent, color white. .danger: border-color var(--color-danger). .warning: border-color var(--color-warning). .info: border-color var(--color-info)
    - .audit-view-toggle: display flex, bg surface, radius-md, overflow hidden. .audit-view-btn: padding var(--space-2) var(--space-3), border none, bg transparent, cursor pointer, font-size 0.8rem. .active: bg accent, color white
    - .audit-category-chips: display flex, gap var(--space-2), flex-wrap wrap, margin-bottom var(--space-3)
    - .audit-chip: padding var(--space-1) var(--space-3), border-radius 9999px, font-size 0.8rem, border 1px solid var(--color-border), cursor pointer. .active: bg accent, color white
    - .audit-table: width 100%. .audit-checkbox: width 32px. .audit-num: width 50px. .audit-hash-col: max-width 120px, font-family mono, font-size 0.7rem, overflow hidden, text-overflow ellipsis
    - .audit-timeline: padding var(--space-4). Timeline item styling with left border, dot, content
    - .audit-modal-overlay: position fixed, inset 0, bg rgba(0,0,0,0.5), display flex center, z-index 1000
    - .audit-modal: bg surface, radius-xl, max-width 600px, width 90vw, shadow-2xl
    - .audit-modal-header: padding var(--space-4) var(--space-6), border-bottom, flex justify-between
    - .audit-modal-body: padding var(--space-6)
    - .audit-modal-row: display flex, gap var(--space-4), padding var(--space-2) 0, border-bottom 1px solid var(--color-border). .audit-modal-label: width 100px, text-secondary, font-size 0.8rem. .audit-modal-value: flex 1
    - .audit-modal-hash: font-family mono, font-size 0.75rem, word-break break-all
    - .audit-export-bar: display flex, align-items center, gap var(--space-3), padding var(--space-3) var(--space-6), bg surface-alt, border-top
    - Dark mode: via tokens

    CRITICAL: Do NOT change trust.js. The HTML must match every selector the JS uses.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && echo "=== Trust page IDs ===" && for id in meetingSelect meetingTitle statusBox integrityStatus kpiIntegrity kpiEvents kpiAnomalies kpiLastSession kpiMotions kpiPresent kpiBallots integrityHash integrityHashValue btnCopyHash badgeAnomalies countDanger countWarning countInfo anomaliesList severityFilters checksList motionsTbody auditTableBody auditLogFilter auditTableView auditTimelineView auditViewToggle auditCategoryChips auditSelectAll auditExportBar auditSelectedCount auditEventModal auditModalTitle auditModalBody modalEventId modalEventTime modalEventAction modalEventCategory modalEventUser modalEventDetail modalEventHash auditModalClose btnExportAudit btnExportSelection btnExportSelectedCsv btnExportSelectedJson btnRefresh btnRecheck btnExportTrust notif_box; do grep -q "id=\"$id\"" public/trust.htmx.html && echo "OK: $id" || echo "MISSING: $id"; done && echo "=== Trust classes ===" && for cls in severity-pill audit-view-btn audit-chip integrity-summary; do grep -q "$cls" public/trust.htmx.html && echo "OK: $cls" || echo "MISSING: $cls"; done && echo "=== Data attrs ===" && grep -c 'data-severity=' public/trust.htmx.html && grep -c 'data-view=' public/trust.htmx.html && grep -c 'data-category=' public/trust.htmx.html</automated>
  </verify>
  <acceptance_criteria>
    - All 49 trust.js DOM IDs present in trust.htmx.html (zero MISSING lines)
    - grep 'data-severity="all"' trust.htmx.html returns match
    - grep 'data-severity="danger"' trust.htmx.html returns match
    - grep 'data-view="table"' trust.htmx.html returns match
    - grep 'data-view="timeline"' trust.htmx.html returns match
    - grep 'data-category="all"' trust.htmx.html returns match
    - grep 'data-category="votes"' trust.htmx.html returns match
    - grep 'integrity-summary' trust.css returns match
    - grep 'severity-pill' trust.css returns match
    - grep 'audit-modal' trust.css returns match
    - grep 'trust.js' trust.htmx.html returns match
    - grep 'meeting-context.js' trust.htmx.html returns match
    - No htmx vendor script in rebuilt page (removed)
  </acceptance_criteria>
  <done>Trust/audit page fully rebuilt with v4.3 design language. Integrity dashboard, anomalies with severity filters, coherence checks, motions table, audit log with table+timeline views, event detail modal. All 49 DOM IDs preserved for trust.js.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild Validate + Docs pages (HTML+CSS) + verify all 3 pages</name>
  <read_first>
    - public/assets/js/pages/validate.js (full read — every getElementById, querySelector, event listener)
    - public/validate.htmx.html (current HTML structure)
    - public/assets/css/validate.css (current styles)
    - public/assets/js/pages/docs-viewer.js (full read — every getElementById, querySelector, event listener)
    - public/docs.htmx.html (current HTML structure)
    - public/assets/css/doc.css (current styles)
  </read_first>
  <files>public/validate.htmx.html, public/assets/css/validate.css, public/docs.htmx.html, public/assets/css/doc.css</files>
  <action>
    **HTML rewrite (validate.htmx.html):**
    Rewrite from scratch using v4.3 design language. Structure:
    1. Standard head with critical-tokens, theme-init.js, fonts, app.css + validate.css
    2. data-page-role="operator" on html element
    3. app-shell with app-sidebar (data-include-sidebar data-page="operator")
    4. app-header: left with icon-check-circle "Validation finale" + p#meetingTitle "Etape finale : verifier puis valider". Right: #btnRefresh btn-secondary (icon-refresh-cw + "Actualiser"), drawer buttons infos + context
    5. app-main#main-content > div.container:
       - v4.3 gradient accent bar, breadcrumb
       - div#notif_box.hidden.mb-4
       - Meeting context: div#meetingContext.alert.alert-info.mb-6 display:none with icon-pin, strong#meetingName, span#meetingDateCtx
       - Summary stats card.mb-6: card-header "Resume de la seance" icon-bar-chart. card-body with div#summaryGrid.summary-grid (8 items: #sumMembers, #sumPresent, #sumMotions, #sumAdopted text-success, #sumRejected text-danger, #sumBallots, #sumQuorum, #sumDuration — each .summary-item with .summary-value + .summary-label)
       - Grid 2-col.mb-6:
         Left: Checklist card — header "Checklist pre-validation" icon-clipboard-list + span#readyBadge.badge. body div#checksList (default spinner). button#btnRecheck.btn.btn-secondary.w-full.mt-4
         Right: Validation card — header "Signature & Validation" icon-signature. body: form-group #presidentName input (required, pattern, placeholder "Ex : Jean Dupont", form-helper text). div#validationZone.validation-zone.validation-zone-danger.mb-4 with warning icon+text + button#btnValidate.btn.btn-lg.btn-danger disabled. div#validateMsg.alert.alert-info display:none
       - Notes card: important notes with warning/info/success items (keep exact content from current HTML)
    6. Standard mobile-footer
    7. Validation modal #validateModal hidden: .validate-modal-backdrop with .validate-modal containing header (#validateModalTitle), body (.validate-modal-warning, label.validate-modal-checkbox with checkbox#confirmIrreversible, form-group input#confirmText placeholder "VALIDER"), actions (#btnModalCancel + #btnModalConfirm.btn.btn-danger.btn-lg disabled)
    8. Drawer: backdrop + aside#drawer with #drawerTitle + #drawerBody
    9. Scripts: utils.js, components/index.js, shared.js, shell.js, meeting-context.js, validate.js

    **CSS rewrite (validate.css):**
    Rewrite from scratch:
    - .summary-grid: display grid, grid-template-columns repeat(auto-fill, minmax(120px, 1fr)), gap var(--space-4)
    - .summary-item: text-align center, padding var(--space-3). .summary-value: font-size 1.75rem, font-weight 800. .summary-label: font-size 0.75rem, text-secondary
    - .validation-zone: padding var(--space-6), border-radius var(--radius-lg), text-align center. .validation-zone-danger: bg var(--color-danger)/5, border 1px solid var(--color-danger)/20
    - .validate-modal-backdrop: position fixed, inset 0, bg rgba(0,0,0,0.5), display flex center, z-index 1000
    - .validate-modal: bg surface, radius-xl, max-width 500px, width 90vw, shadow-2xl
    - .validate-modal-header: padding var(--space-4) var(--space-6), border-bottom
    - .validate-modal-body: padding var(--space-6)
    - .validate-modal-warning: bg var(--color-danger)/5, border 1px solid var(--color-danger)/20, padding var(--space-4), radius-md, margin-bottom var(--space-4)
    - .validate-modal-checkbox: display flex, gap var(--space-3), cursor pointer, padding var(--space-3), bg surface-alt, radius-md, margin-bottom var(--space-4)
    - .validate-modal-actions: padding var(--space-4) var(--space-6), border-top, display flex, justify-content flex-end, gap var(--space-3)
    - Dark mode: via tokens

    **HTML rewrite (docs.htmx.html):**
    Rewrite from scratch. Structure:
    1. Standard head with critical-tokens, theme-init.js, fonts, app.css + doc.css. Include marked.min.js vendor script
    2. data-page-role="viewer" on html element
    3. app-shell with app-sidebar (data-include-sidebar data-page="help")
    4. NO app-header — viewer page, header is inline
    5. app-main.doc-main#main-content:
       - .doc-page-header: .doc-breadcrumb (link to /help.htmx.html "Aide", chevron-right icon, span#breadcrumbDir display:none, svg#breadcrumbDirSep display:none, span#breadcrumbCurrent "Documentation"), h1#docTitle "Documentation"
       - .doc-layout (3 column):
         aside.doc-sidebar: nav#docIndex.doc-index with h4 "Documentation" + skeleton placeholder
         article#docContent.doc-content.prose: spinner placeholder
         aside#docTocRail.doc-toc-rail display:none: nav.doc-toc with h4 "Sommaire" + ul#tocList
    6. Standard mobile-footer
    7. Drawer: backdrop + aside#drawer with #drawerTitle + #drawerBody
    8. Scripts: components/index.js, utils.js, shared.js, shell.js, meeting-context.js, docs-viewer.js

    **CSS rewrite (doc.css):**
    Rewrite from scratch:
    - .doc-main: padding 0 (layout handles padding)
    - .doc-page-header: padding var(--space-6), border-bottom 1px solid var(--color-border)
    - .doc-breadcrumb: display flex, align-items center, gap var(--space-2), font-size 0.8rem, text-secondary, margin-bottom var(--space-2). a: color var(--color-accent), text-decoration none
    - .doc-layout: display grid, grid-template-columns 240px 1fr 200px, gap 0, min-height calc(100vh - 200px)
    - .doc-sidebar: border-right 1px solid var(--color-border), padding var(--space-4), overflow-y auto, position sticky, top 0, max-height 100vh
    - .doc-index h4: font-size 0.75rem, text-transform uppercase, letter-spacing 0.05em, text-secondary, margin-bottom var(--space-3)
    - .doc-index a: display block, padding var(--space-2) var(--space-3), font-size 0.875rem, color var(--color-text), text-decoration none, radius-md. hover: bg surface-hover. .active: bg accent/10, color accent, font-weight 600
    - .doc-content: padding var(--space-8), max-width 800px, overflow-x auto
    - .prose: standard typography — h2 margin-top var(--space-8) font-size 1.5rem, h3 margin-top var(--space-6) font-size 1.25rem, p margin var(--space-4) 0, code bg surface-alt padding 2px 6px radius-sm font-mono, pre bg surface-alt padding var(--space-4) radius-md overflow-x auto, ul/ol margin var(--space-4) 0 padding-left var(--space-6), blockquote border-left 3px solid accent padding-left var(--space-4) text-secondary
    - .doc-toc-rail: border-left 1px solid var(--color-border), padding var(--space-4), position sticky, top 0, max-height 100vh, overflow-y auto
    - .doc-toc h4: font-size 0.75rem, text-transform uppercase, letter-spacing 0.05em, text-secondary
    - .doc-toc a: display block, padding var(--space-1) var(--space-2), font-size 0.8rem, color text-secondary, text-decoration none, border-left 2px solid transparent. hover: color text. .active: border-left-color accent, color accent
    - Responsive: below 1024px hide doc-toc-rail, doc-layout 2-col (240px 1fr). Below 768px: doc-layout 1-col, doc-sidebar not sticky, collapsible
    - Dark mode: via tokens

    CRITICAL: Do NOT change trust.js, validate.js, or docs-viewer.js.

    **After all rebuilds, verify wiring for all 3 pages.**
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && echo "=== Validate page IDs ===" && for id in meetingTitle meetingName meetingDateCtx meetingContext sumMembers sumPresent sumMotions sumAdopted sumRejected sumBallots sumQuorum sumDuration summaryGrid checksList readyBadge btnValidate presidentName validationZone validateMsg validateModal confirmIrreversible btnModalConfirm btnModalCancel confirmText btnRefresh btnRecheck notif_box; do grep -q "id=\"$id\"" public/validate.htmx.html && echo "OK: $id" || echo "MISSING: $id"; done && echo "=== Docs page IDs ===" && for id in docIndex docContent docTitle breadcrumbCurrent breadcrumbDir breadcrumbDirSep docTocRail tocList; do grep -q "id=\"$id\"" public/docs.htmx.html && echo "OK: $id" || echo "MISSING: $id"; done && echo "=== Validate CSS ===" && grep -c 'validation-zone' public/assets/css/validate.css && grep -c 'validate-modal' public/assets/css/validate.css && grep -c 'summary-grid' public/assets/css/validate.css && echo "=== Doc CSS ===" && grep -c 'doc-layout' public/assets/css/doc.css && grep -c 'doc-sidebar' public/assets/css/doc.css && grep -c 'doc-toc' public/assets/css/doc.css && echo "=== JS wired ===" && grep -q 'validate.js' public/validate.htmx.html && echo "OK: validate.js" || echo "MISSING: validate.js" && grep -q 'docs-viewer.js' public/docs.htmx.html && echo "OK: docs-viewer.js" || echo "MISSING: docs-viewer.js" && grep -q 'marked.min.js' public/docs.htmx.html && echo "OK: marked.min.js" || echo "MISSING: marked.min.js"</automated>
  </verify>
  <acceptance_criteria>
    - All 28 validate.js DOM IDs present in validate.htmx.html (zero MISSING)
    - All 8 docs-viewer.js DOM IDs present in docs.htmx.html (zero MISSING)
    - grep 'validation-zone' validate.css returns match
    - grep 'validate-modal' validate.css returns match
    - grep 'summary-grid' validate.css returns match
    - grep 'doc-layout' doc.css returns match
    - grep 'doc-sidebar' doc.css returns match
    - grep 'doc-toc' doc.css returns match
    - grep 'validate.js' validate.htmx.html returns match
    - grep 'docs-viewer.js' docs.htmx.html returns match
    - grep 'marked.min.js' docs.htmx.html returns match (markdown renderer vendor)
    - grep 'meeting-context.js' validate.htmx.html returns match
    - Validate modal has both checkbox and text confirmation inputs
    - Docs page has 3-column layout (sidebar, content, TOC)
    - All trust page IDs from Task 1 still pass verification
  </acceptance_criteria>
  <done>Validate and Docs pages fully rebuilt with v4.3 design language. Validate has summary stats, checklist, signature form, irreversible validation modal with dual confirmation. Docs has 3-column layout with sidebar index, prose content, and sticky TOC. All DOM IDs preserved for validate.js and docs-viewer.js. All 3 pages in plan pass full selector verification.</done>
</task>

</tasks>

<verification>
1. All DOM IDs from trust.js exist in trust.htmx.html (49 IDs)
2. All DOM IDs from validate.js exist in validate.htmx.html (28 IDs)
3. All DOM IDs from docs-viewer.js exist in docs.htmx.html (8 IDs)
4. All class selectors (.severity-pill, .audit-view-btn, .audit-chip, .summary-item, .doc-index a) match between HTML and JS
5. All data attributes (data-severity, data-view, data-category, data-drawer) present with correct values
6. All CSS files use design tokens
7. All 3 pages include correct script tags
8. Validate modal has dual confirmation (checkbox + text)
9. Docs includes marked.min.js vendor for markdown rendering
10. Trust page no longer includes htmx vendor script
</verification>

<success_criteria>
- Trust: integrity dashboard renders, anomalies filter by severity, checks render, motions table populates, audit log with table+timeline views and search
- Validate: summary stats load, checklist renders, validation requires signature + dual confirmation, submits to API
- Docs: sidebar index loads from API, markdown renders in content area, TOC generates from headings
- Zero JS selector mismatches between HTML and JS files across all 3 pages
</success_criteria>

<output>
After completion, create `.planning/phases/51-utility-pages/51-03-SUMMARY.md`
</output>
