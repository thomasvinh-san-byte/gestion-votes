---
phase: 49-secondary-pages-part-1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/meetings.htmx.html
  - public/assets/css/meetings.css
  - public/assets/js/pages/meetings.js
  - public/archives.htmx.html
  - public/assets/css/archives.css
  - public/assets/js/pages/archives.js
autonomous: true
requirements: [REB-03, REB-04, WIRE-02]

must_haves:
  truths:
    - "Meetings page has filter pills (Toutes, A venir, En cours, Terminees) with counts"
    - "Meetings page has search input, sort dropdown, list/calendar view toggle"
    - "Session cards render with status badges, type labels, and action menus"
    - "Calendar view shows month grid with session dots on dates"
    - "Edit and delete modals work for draft sessions"
    - "Archives page has KPI row (5 cards: archived, with PV, this year, participation, period)"
    - "Archives has type filter chips, status filter chips, year dropdown, search input"
    - "Archives has card/list view toggle with pagination"
    - "Archives exports modal with 7 export buttons (PV, attendance, votes, motions, members, audit, ZIP)"
    - "No JS console errors on either page — all DOM IDs preserved"
  artifacts:
    - path: "public/meetings.htmx.html"
      provides: "Complete meetings page with filter pills, toolbar, session list, calendar, modals"
      contains: "meetingsList"
    - path: "public/assets/css/meetings.css"
      provides: "Meetings styles — filter pills, toolbar, session items, calendar grid, modals, responsive"
      contains: "filter-pill"
    - path: "public/archives.htmx.html"
      provides: "Complete archives page with KPI row, filters, archive list, exports modal"
      contains: "archivesList"
    - path: "public/assets/css/archives.css"
      provides: "Archives styles — KPI grid, filter tabs, archive cards, table view, exports modal, responsive"
      contains: "archive-card"
  key_links:
    - from: "public/meetings.htmx.html"
      to: "public/assets/js/pages/meetings.js"
      via: "DOM IDs and class selectors for list rendering, filters, modals"
      pattern: "meetingsList|meetingsSearch|meetingsSort|meetingsCount|meetingsPagination|filterPills|filter-pill|calendarContainer|calendarGrid|calendarTitle|calendarPrev|calendarNext|calendarToday|editMeetingModal|deleteMeetingModal|onboardingBanner|view-toggle-btn"
    - from: "public/archives.htmx.html"
      to: "public/assets/js/pages/archives.js"
      via: "DOM IDs for list rendering, filters, KPIs, exports"
      pattern: "archivesList|searchInput|yearFilter|archiveTypeFilter|archiveStatusFilter|archivesPagination|kpiTotal|kpiWithPV|kpiThisYear|kpiAvgParticipation|kpiDateRange|exportsModal|exportsBackdrop|exportMeetingSelect|btnExportsModal|btnRefresh|view-toggle-btn"
    - from: "public/assets/js/pages/meetings.js"
      to: "/api/meetings.php"
      via: "fetch for session list"
      pattern: "api.*meetings"
    - from: "public/assets/js/pages/archives.js"
      to: "/api/meetings.php"
      via: "fetch for archived sessions"
      pattern: "api.*meetings|api.*export"
---

<objective>
Ground-up rebuild of meetings list and archives pages — both are list/filter/search pages with similar patterns. Complete HTML+CSS rewrite for each with v4.3 design language. Preserve all DOM IDs for their respective JS files.

Purpose: Meetings and archives share structural patterns (filter bars, card lists, pagination, view toggles) making them efficient to rebuild together. Both are list-oriented pages.
Output: New HTML+CSS for both pages, with JS verified working.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-secondary-pages-part-1/49-CONTEXT.md

@public/meetings.htmx.html
@public/assets/css/meetings.css
@public/assets/js/pages/meetings.js
@public/archives.htmx.html
@public/assets/css/archives.css
@public/assets/js/pages/archives.js
@public/assets/css/app.css
@public/dashboard.htmx.html

<interfaces>
<!-- DOM IDs that meetings.js targets — ALL must exist in new HTML -->

Filter pills:
- #filterPills — container with role="tablist"
- .filter-pill[data-filter] — filter buttons (all, upcoming, live, completed)
- #countAll, #countUpcoming, #countLive, #countCompleted — count badges

Toolbar:
- #meetingsSearch — search input
- #meetingsSort — sort select dropdown
- #meetingsCount — result count text
- .view-toggle-btn[data-view] — list/calendar toggle buttons

Session list:
- #meetingsList — sessions container (JS renders session cards here)
- #meetingsPagination — pagination container

Calendar:
- #calendarContainer — calendar view container
- #calendarGrid — month grid (generated by JS)
- #calendarTitle — month/year title
- #calendarPrev, #calendarNext — navigation buttons
- #calendarToday — today button

Edit modal:
- #editMeetingModal — modal backdrop
- #editMeetingId — hidden id input
- #editMeetingTitle — title input
- #editMeetingDate — date input
- input[name="editMeetingType"] — radio buttons for meeting type
- #editMeetingSaveBtn — save button
- [data-close-modal] — close triggers

Delete modal:
- #deleteMeetingModal — modal backdrop
- #deleteMeetingId — hidden id input
- #deleteMeetingName — meeting name display
- #deleteMeetingConfirmBtn — confirm delete button

Onboarding:
- #onboardingBanner — dismissible banner
- #btnStartTour, #btnDismissOnboarding, #btnCloseOnboarding — banner buttons

<!-- DOM IDs that archives.js targets — ALL must exist in new HTML -->

KPIs:
- #kpiTotal — total archived sessions
- #kpiWithPV — sessions with PV
- #kpiThisYear — archived this year
- #kpiAvgParticipation — average participation rate
- #kpiDateRange — date period range

Filters:
- #archiveTypeFilter — type filter container with .filter-tab[data-type] buttons
- #archiveStatusFilter — status filter container with .filter-tab[data-status] buttons
- #yearFilter — year select dropdown
- #searchInput — search text input
- .view-toggle-btn[data-view] — cards/list toggle (values: "cards", "list")

List:
- #archivesList — archives container (JS renders cards/rows here)
- #archivesPagination — pagination container
- #archivesPager — ag-pagination component (per-page="5")

Exports modal:
- #exportsModal — modal element
- #exportsBackdrop — modal backdrop
- #btnExportsModal — open modal button
- #btnCloseExports, #btnCloseExports2 — close buttons
- #exportMeetingSelect — meeting select dropdown inside modal
- #btnExportPV — PV export button
- #btnExportAttendance — attendance CSV button
- #btnExportVotes — votes CSV button
- #btnExportMotions — motions CSV button
- #btnExportMembers — members CSV button
- #btnExportAudit — audit CSV button
- #btnExportZip — ZIP archive button

Other:
- #btnRefresh — refresh button in header
- #notif_box — notification area
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild meetings HTML+CSS from scratch</name>
  <files>public/meetings.htmx.html, public/assets/css/meetings.css</files>
  <read_first>
    - public/meetings.htmx.html (current HTML — all DOM IDs)
    - public/assets/js/pages/meetings.js (ALL getElementById/querySelector calls)
    - public/assets/css/meetings.css (current CSS)
    - public/assets/css/app.css (design system tokens)
    - public/dashboard.htmx.html (reference for v4.3 session list pattern)
  </read_first>
  <action>
    Rewrite meetings.htmx.html from scratch with v4.3 design language:

    1. **HTML structure**: app-shell (sidebar data-page="meetings", header with breadcrumb + page-title with bar+icon, main, footer). Header right: help popover, "Nouvelle seance" primary button linking to /wizard.htmx.html, drawer toggle.

    2. **Onboarding banner**: #onboardingBanner with icon, title "Bienvenue sur AG-VOTE", text, action buttons (#btnStartTour, #btnDismissOnboarding), close button (#btnCloseOnboarding). Dismissible via localStorage.

    3. **Filter pills**: #filterPills (role="tablist") with 4 .filter-pill[data-filter] buttons (all, upcoming, live, completed). Each has .filter-pill-count span (#countAll, #countUpcoming, #countLive, #countCompleted).

    4. **Toolbar**: .meetings-toolbar with search input (#meetingsSearch), sort select (#meetingsSort with 4 options), result count (#meetingsCount), view toggle (.view-toggle-btn[data-view="list"] and [data-view="calendar"]).

    5. **Sessions list**: #meetingsList container (JS dynamically renders session cards here). 3 skeleton placeholders for loading state.

    6. **Pagination**: #meetingsPagination container.

    7. **Calendar view**: #calendarContainer with header (nav buttons #calendarPrev/#calendarNext, title #calendarTitle, today button #calendarToday) and #calendarGrid.

    8. **Edit modal**: #editMeetingModal backdrop with modal containing: #editMeetingId hidden input, #editMeetingTitle text input, #editMeetingDate date input, meeting type radio chips (name="editMeetingType", values: ag_ordinaire, ag_extraordinaire, conseil, bureau, autre), #editMeetingSaveBtn, [data-close-modal] triggers.

    9. **Delete modal**: #deleteMeetingModal backdrop with small modal: #deleteMeetingId hidden, #deleteMeetingName text, #deleteMeetingConfirmBtn danger button, [data-close-modal] triggers.

    10. **Drawer**: Standard structure (#drawer, #drawerTitle, #drawerBody).

    11. **Scripts**: utils, components/index.js (module), shared, shell, meeting-context, page-components, meetings.js.

    Rewrite meetings.css from scratch:
    - Page max-width: 1200px centered
    - Filter pills: horizontal flex row, pill-shaped with border, active uses primary bg, count badges
    - Toolbar: flex row with search, sort, count, view toggle. Search input takes available space
    - Sessions list: vertical stack with gap. Individual session items styled by meetings.js (cards)
    - Calendar: month grid (7 columns), day cells, session dots, popover for day details
    - Modals: backdrop + centered card, header/body/footer sections. Type chips: inline radio labels
    - Onboarding banner: horizontal layout with icon, content, actions. Dismissible
    - View toggle: button group with icon buttons
    - Pagination: centered row
    - Skeleton loading states
    - All via design tokens
    - Responsive: toolbar wraps, calendar scrolls horizontally at mobile
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c 'meetingsList\|meetingsSearch\|meetingsSort\|filterPills\|calendarContainer\|calendarGrid\|editMeetingModal\|deleteMeetingModal' public/meetings.htmx.html && grep -c 'countAll\|countUpcoming\|countLive\|countCompleted' public/meetings.htmx.html && grep -c 'var(--' public/assets/css/meetings.css</automated>
  </verify>
  <acceptance_criteria>
    - grep 'id="meetingsList"' public/meetings.htmx.html returns match
    - grep 'id="meetingsSearch"' public/meetings.htmx.html returns match
    - grep 'id="filterPills"' public/meetings.htmx.html returns match
    - grep 'id="calendarContainer"' public/meetings.htmx.html returns match
    - grep 'id="calendarGrid"' public/meetings.htmx.html returns match
    - grep 'id="editMeetingModal"' public/meetings.htmx.html returns match
    - grep 'id="deleteMeetingModal"' public/meetings.htmx.html returns match
    - grep 'id="countAll"' public/meetings.htmx.html returns match
    - grep 'id="onboardingBanner"' public/meetings.htmx.html returns match
    - grep 'data-filter="all"' public/meetings.htmx.html returns match
    - grep 'data-filter="live"' public/meetings.htmx.html returns match
    - grep 'name="editMeetingType"' public/meetings.htmx.html returns match
    - grep 'meetings.js' public/meetings.htmx.html returns match
    - grep 'var(--' public/assets/css/meetings.css returns 15+ matches
  </acceptance_criteria>
  <done>Meetings HTML completely rewritten with v4.3 design language. Filter pills, toolbar, session list, calendar, edit/delete modals all have correct IDs. CSS uses design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild archives HTML+CSS from scratch</name>
  <files>public/archives.htmx.html, public/assets/css/archives.css</files>
  <read_first>
    - public/archives.htmx.html (current HTML — all DOM IDs)
    - public/assets/js/pages/archives.js (ALL getElementById/querySelector calls)
    - public/assets/css/archives.css (current CSS)
    - public/assets/css/app.css (design system tokens)
  </read_first>
  <action>
    Rewrite archives.htmx.html from scratch with v4.3 design language:

    1. **HTML structure**: app-shell (sidebar data-page="archives", header, main, footer). Header: h1 with archive icon "Archives", subtitle "Seances validees et proces-verbaux". Header right: #btnRefresh, #btnExportsModal, drawer info button, drawer context button.

    2. **KPI grid**: 5 .kpi-card cards in horizontal grid:
       - #kpiTotal (archive icon, primary color) — "Seances archivees"
       - #kpiWithPV (clipboard icon, success) — "Avec PV"
       - #kpiThisYear (calendar icon) — "Cette annee"
       - #kpiAvgParticipation (users icon, success) — "Participation moy."
       - #kpiDateRange (clock icon) — "Periode"
       Each with ag-tooltip for description.

    3. **Type filter chips**: #archiveTypeFilter container with .filter-tab[data-type] buttons ("" for Toutes, ag_ordinaire, ag_extraordinaire, conseil).

    4. **Status filter chips**: #archiveStatusFilter container with .filter-tab[data-status] buttons ("" for Tous statuts, validated, archived, pv_sent).

    5. **Toolbar**: Header row with title "Seances archivees" + description, right side: #yearFilter select, #searchInput text input, .view-toggle (cards/list buttons with data-view="cards" and data-view="list").

    6. **Archives list**: .table-card container with #archivesList inside (JS renders card/list views here). Spinner loading state.

    7. **Pagination**: #archivesPagination container with ag-pagination#archivesPager (per-page="5").

    8. **Drawer**: Standard structure.

    9. **Exports modal**: #exportsBackdrop + #exportsModal with:
       - Header: #exportsModalTitle, close button #btnCloseExports
       - Body: #exportMeetingSelect dropdown, info alert, export-grid with 7 buttons (#btnExportPV, #btnExportAttendance, #btnExportVotes, #btnExportMotions, #btnExportMembers, #btnExportAudit, #btnExportZip)
       - Footer: #btnCloseExports2

    10. **Scripts**: utils, components/index.js (module), shared, shell, meeting-context, archives.js.

    Rewrite archives.css from scratch:
    - KPI grid: 5-column at xl, 3-column at lg, 2-column at md, 1-column at sm
    - Filter tabs: horizontal row, pill-shaped, active state
    - Toolbar: flex row with title, filters, search, view toggle
    - Archive cards: shadow-md, border-radius, hover lift effect, structured header/meta/actions
    - Table/list view alternative
    - Exports modal: fixed overlay, centered card, export-grid as 2-col or 3-col grid of buttons
    - Pagination: centered
    - Year select: compact
    - All via design tokens
    - Responsive: KPI grid wraps, toolbar stacks, cards single column at mobile
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c 'archivesList\|archiveTypeFilter\|archiveStatusFilter\|yearFilter\|searchInput\|exportsModal\|exportMeetingSelect' public/archives.htmx.html && grep -c 'kpiTotal\|kpiWithPV\|kpiThisYear\|kpiAvgParticipation\|kpiDateRange' public/archives.htmx.html && grep -c 'btnExportPV\|btnExportAttendance\|btnExportVotes\|btnExportMotions\|btnExportMembers\|btnExportAudit\|btnExportZip' public/archives.htmx.html && grep -c 'var(--' public/assets/css/archives.css</automated>
  </verify>
  <acceptance_criteria>
    - grep 'id="archivesList"' public/archives.htmx.html returns match
    - grep 'id="searchInput"' public/archives.htmx.html returns match
    - grep 'id="yearFilter"' public/archives.htmx.html returns match
    - grep 'id="archiveTypeFilter"' public/archives.htmx.html returns match
    - grep 'id="archiveStatusFilter"' public/archives.htmx.html returns match
    - grep 'id="archivesPagination"' public/archives.htmx.html returns match
    - grep 'id="exportsModal"' public/archives.htmx.html returns match
    - grep 'id="exportMeetingSelect"' public/archives.htmx.html returns match
    - grep 'id="kpiTotal"' public/archives.htmx.html returns match
    - grep 'id="kpiWithPV"' public/archives.htmx.html returns match
    - grep 'id="kpiAvgParticipation"' public/archives.htmx.html returns match
    - grep 'id="btnExportPV"' public/archives.htmx.html returns match
    - grep 'id="btnExportZip"' public/archives.htmx.html returns match
    - grep 'id="btnRefresh"' public/archives.htmx.html returns match
    - grep 'data-type="ag_ordinaire"' public/archives.htmx.html returns match
    - grep 'data-status="validated"' public/archives.htmx.html returns match
    - grep 'archives.js' public/archives.htmx.html returns match
    - grep 'var(--' public/assets/css/archives.css returns 15+ matches
  </acceptance_criteria>
  <done>Archives HTML completely rewritten with v4.3 design language. KPI row, type/status filters, archive list, exports modal all have correct IDs. CSS uses design tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Verify meetings+archives JS wiring</name>
  <files>public/assets/js/pages/meetings.js, public/assets/js/pages/archives.js</files>
  <read_first>
    - public/meetings.htmx.html (rebuilt HTML from Task 1)
    - public/archives.htmx.html (rebuilt HTML from Task 2)
    - public/assets/js/pages/meetings.js (check all selectors)
    - public/assets/js/pages/archives.js (check all selectors)
  </read_first>
  <action>
    Verify both JS files wire correctly to their rebuilt HTML:

    **meetings.js verification:**
    1. Cross-reference all getElementById calls: meetingsList, meetingsCount, meetingsPagination, calendarContainer, calendarGrid, calendarTitle, countAll, countUpcoming, countLive, countCompleted, meetingsSearch, meetingsSort, editMeetingModal, editMeetingId, editMeetingTitle, editMeetingDate, editMeetingSaveBtn, deleteMeetingModal, deleteMeetingId, deleteMeetingName, deleteMeetingConfirmBtn, calendarPrev, calendarNext, calendarToday, onboardingBanner, btnCloseOnboarding, btnDismissOnboarding.
    2. Check querySelectorAll selectors: '.filter-pill', '.view-toggle-btn', 'input[name="editMeetingType"]', '[data-close-modal]'.
    3. Fix any broken selectors due to HTML restructuring.
    4. Verify API endpoint: /api/meetings.php or equivalent.

    **archives.js verification:**
    1. Cross-reference all getElementById calls: archivesList, searchInput, yearFilter, archivesPagination, archivesPager, kpiTotal, kpiWithPV, kpiThisYear, kpiAvgParticipation, kpiDateRange, exportsModal, exportsBackdrop, exportMeetingSelect, btnExportsModal, btnCloseExports, btnCloseExports2, btnRefresh, btnExportPV, btnExportAttendance, btnExportVotes, btnExportMotions, btnExportMembers, btnExportAudit, btnExportZip.
    2. Check querySelectorAll selectors: '#archiveTypeFilter .filter-tab', '#archiveStatusFilter .filter-tab', '.view-toggle-btn'.
    3. Fix any broken selectors.
    4. Verify API endpoints for exports.

    For both: add error banners (setNotif) on API failure if not already present. Ensure no dead code or references to removed elements.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && node -e "const fs=require('fs');const html=fs.readFileSync('public/meetings.htmx.html','utf8');const js=fs.readFileSync('public/assets/js/pages/meetings.js','utf8');const ids=[...js.matchAll(/getElementById\(['\"]([^'\"]+)['\"]\)/g)].map(m=>m[1]);const missing=ids.filter(id=>!html.includes('id=\"'+id+'\"'));if(missing.length){console.error('MEETINGS MISSING IDs:',missing);process.exit(1)}else{console.log('Meetings: all',ids.length,'IDs found')}" && node -e "const fs=require('fs');const html=fs.readFileSync('public/archives.htmx.html','utf8');const js=fs.readFileSync('public/assets/js/pages/archives.js','utf8');const ids=[...js.matchAll(/getElementById\(['\"]([^'\"]+)['\"]\)/g)].map(m=>m[1]);const missing=ids.filter(id=>!html.includes('id=\"'+id+'\"'));if(missing.length){console.error('ARCHIVES MISSING IDs:',missing);process.exit(1)}else{console.log('Archives: all',ids.length,'IDs found')}"</automated>
  </verify>
  <acceptance_criteria>
    - Both node verification scripts exit 0 (all getElementById targets exist in HTML)
    - node -c public/assets/js/pages/meetings.js exits 0
    - node -c public/assets/js/pages/archives.js exits 0
    - grep 'loadMeetings\|fetchMeetings\|api' public/assets/js/pages/meetings.js returns matches
    - grep 'loadArchives\|api' public/assets/js/pages/archives.js returns matches
  </acceptance_criteria>
  <done>Both meetings.js and archives.js selectors match their rebuilt HTML. No broken DOM references. API calls verified. Filter/search/view-toggle wiring intact.</done>
</task>

</tasks>

<verification>
1. Open meetings page — filter pills show with counts, session list renders
2. Search filters sessions in real time
3. Sort dropdown reorders sessions
4. Calendar view shows month grid with session markers
5. Edit modal opens and saves changes
6. Delete modal confirms and removes session
7. Open archives page — KPI cards show values
8. Type and status filter chips filter the list
9. Search and year filter work
10. Card/list view toggle switches layout
11. Exports modal opens and export buttons trigger downloads
12. No JS console errors on either page
13. Dark mode works on both pages
</verification>

<success_criteria>
- Meetings page fully rebuilt with v4.3 design language
- Archives page fully rebuilt with v4.3 design language
- Filter pills, search, sort, calendar, modals all functional (meetings)
- KPI row, type/status filters, exports modal, pagination all functional (archives)
- All DOM IDs preserved for both JS files
- No JS console errors on either page
</success_criteria>

<output>
After completion, create `.planning/phases/49-secondary-pages-part-1/49-03-SUMMARY.md`
</output>
