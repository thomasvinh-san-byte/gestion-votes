---
phase: 50-secondary-pages-part-2
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - public/vote.htmx.html
  - public/assets/css/vote.css
  - public/assets/js/pages/vote.js
  - public/assets/js/pages/vote-ui.js
autonomous: true
requirements: [REB-08, WIRE-01, WIRE-02]

must_haves:
  truths:
    - "Vote page renders full-screen mobile ballot with identity banner"
    - "Voter can read the motion text and resolution counter"
    - "Voter can cast a vote (Pour/Contre/Abstention) with optimistic feedback"
    - "Confirmation overlay shows chosen vote with meeting/member/motion context"
    - "Vote receipt appears after confirmed cast"
    - "Connection status dot shows real-time SSE/polling state"
    - "Offline banner appears when connection lost"
    - "Blocked overlay shows when voting is locked"
    - "Speech request box shows queue position and hand-raise button"
    - "PDF document consultation opens resolution documents"
    - "Meeting/member selection works when not in token-based flow"
    - "Bottom nav with status label and navigation dots"
    - "No JS console errors — all DOM IDs preserved for vote.js and vote-ui.js"
  artifacts:
    - path: "public/vote.htmx.html"
      provides: "Complete vote/ballot page — full-screen mobile layout, identity, voting panel, confirmation, receipt"
      contains: "voteApp"
    - path: "public/assets/css/vote.css"
      provides: "Vote styles — ballot layout, vote buttons, confirmation overlay, receipt, speech box, responsive"
      contains: "vote-btn"
    - path: "public/assets/js/pages/vote.js"
      provides: "Vote JS — token redemption, motion polling, ballot cast, presence, PDF viewer"
      contains: "ballots_cast"
    - path: "public/assets/js/pages/vote-ui.js"
      provides: "Vote UI JS — UI rendering, confirmation flow, bottom nav, speech, connection status"
      contains: "confirmationOverlay"
  key_links:
    - from: "public/vote.htmx.html"
      to: "public/assets/js/pages/vote.js"
      via: "DOM IDs preserved for vote.js binding"
      pattern: "voteApp|voteLoadingState|blockedOverlay|blockedMsg|identityAvatar|identityName|identityMeeting|voteHint|btnPresence|btnConsultDocument"
    - from: "public/vote.htmx.html"
      to: "public/assets/js/pages/vote-ui.js"
      via: "DOM IDs preserved for vote-ui.js binding"
      pattern: "motionTitle|resoText|voteResolutionCounter|motionProgressText|voteSubtitle|voteStatus|voteReceipt|confirmationOverlay|confirmChoice|confirmedChoice|confirmResoText|confirmResoDetails|btnConfirm|btnCancel|btnConfirmInline|connectionDot|connectionStatus|offlineBanner|bottomNavDot|bottomNavStatusLabel|memberAvatar|memberName|memberWeight|memberSelect|meetingSelect|identityBanner|speechBox|speechLabel|speechHint|speechHandIcon|speechQueuePos|speechPosNumber|btnHand|currentSpeakerBanner|currentSpeakerName|currentSpeakerTimer|currentSpeakerQueue|currentSpeakerQueueCount|cMeeting|cMember|cMotion"
    - from: "public/assets/js/pages/vote.js"
      to: "/api/v1/ballots_cast.php"
      via: "fetch call to cast vote"
      pattern: "api.*ballots_cast|api.*current_motion|api.*attendances|api.*invitations_redeem|api.*vote_policies|api.*resolution_document"
---

<objective>
Ground-up rebuild of the vote/ballot page — complete HTML+CSS rewrite from scratch with v4.3 design language. This is the voter-facing page: full-screen mobile ballot with large vote buttons, confirmation flow, and receipt. Preserve all DOM IDs for both vote.js and vote-ui.js (50+ IDs combined). Verify the complete voting flow end-to-end.

Purpose: The ballot page is the most critical voter-facing interface — it must be clear, fast, and reliable. Large buttons, optimistic feedback, and a confirmation step prevent accidental votes.
Output: New vote.htmx.html + vote.css, with vote.js and vote-ui.js verified working.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-secondary-pages-part-2/50-CONTEXT.md

@public/vote.htmx.html
@public/assets/css/vote.css
@public/assets/js/pages/vote.js
@public/assets/js/pages/vote-ui.js
@public/assets/css/app.css
@public/dashboard.htmx.html

<interfaces>
<!-- Critical DOM IDs from vote.js — ALL must exist in new HTML -->

App container:
- #voteApp — main app container (JS shows/hides sections within)
- #voteLoadingState — loading spinner/state shown during init

Blocked state:
- #blockedOverlay — overlay when voting is locked
- #blockedMsg — blocked message text

Identity:
- #identityAvatar — voter avatar display
- #identityName — voter name display
- #identityMeeting — meeting name display
- #voteHint — hint text below identity

Presence:
- #btnPresence — presence toggle button
- .presence-toggle-icon — icon inside presence button
- .presence-toggle-label — label inside presence button

Document:
- #btnConsultDocument — button to open PDF resolution document

Vote buttons:
- .vote-btn[data-choice] — vote buttons (Pour, Contre, Abstention) with data-choice attribute
- ag-pdf-viewer — PDF viewer web component

<!-- Critical DOM IDs from vote-ui.js — ALL must exist in new HTML -->

Motion display:
- #motionTitle — current motion title
- #resoText — resolution text
- #voteResolutionCounter — resolution counter (e.g., "3/8")
- #motionProgressText — progress text
- #voteSubtitle — subtitle text
- #voteStatus — vote status display
- #voteReceipt — receipt display after voting

Confirmation overlay:
- #confirmationOverlay — confirmation dialog overlay
- #confirmChoice — chosen vote display in confirmation
- #confirmedChoice — post-confirmation choice display
- #confirmResoText — resolution text in confirmation
- #confirmResoDetails — resolution details in confirmation
- #btnConfirm — confirm vote button
- #btnCancel — cancel vote button
- #btnConfirmInline — inline confirm button

Connection status:
- #connectionDot — SSE connection dot
- #connectionStatus — connection status text
- #offlineBanner — offline warning banner

Bottom navigation:
- #bottomNavDot — nav dot indicator
- #bottomNavStatusLabel — nav status label
- .bottom-nav-item[data-nav] — bottom nav items

Identity banner:
- #identityBanner — identity banner container
- #memberAvatar — member avatar
- #memberName — member name
- #memberWeight — member voting weight

Meeting/member selection (non-token flow):
- #meetingSelect — meeting dropdown
- #memberSelect — member dropdown

Speech:
- #speechBox — speech request box
- #speechLabel — speech label
- #speechHint — speech hint text
- #speechHandIcon — hand raise icon
- #speechQueuePos — queue position container
- #speechPosNumber — position number
- #btnHand — hand raise/lower button
- #currentSpeakerBanner — current speaker banner
- #currentSpeakerName — speaker name
- #currentSpeakerTimer — speaker timer
- #currentSpeakerQueue — speaker queue info
- #currentSpeakerQueueCount — queue count

Confirmation context:
- #cMeeting — meeting name in confirmation
- #cMember — member name in confirmation
- #cMotion — motion name in confirmation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild vote/ballot HTML+CSS from scratch</name>
  <files>public/vote.htmx.html, public/assets/css/vote.css</files>
  <read_first>
    - public/vote.htmx.html (current HTML — extract all DOM IDs and structure)
    - public/assets/js/pages/vote.js (JS — understand ALL getElementById/querySelector calls)
    - public/assets/js/pages/vote-ui.js (JS — understand ALL getElementById/querySelector calls)
    - public/assets/css/vote.css (current CSS — understand structure)
    - public/assets/css/app.css (design system tokens)
    - public/dashboard.htmx.html (reference for v4.3 head pattern)
  </read_first>
  <action>
    Rewrite vote.htmx.html from scratch with v4.3 design language. This is a VOTER page — no sidebar, no admin shell. Full-screen mobile-first ballot layout.

    1. **HTML structure**: NO app-shell/sidebar. Minimal head with critical-tokens, fonts, app.css, vote.css. Body is a full-screen ballot container. data-page-role="voter".

    2. **Loading state**: #voteLoadingState — centered spinner shown during initialization.

    3. **Blocked overlay**: #blockedOverlay with #blockedMsg — full-screen overlay when voting is locked.

    4. **Main app container**: #voteApp wraps all voting UI.

    5. **Identity banner**: #identityBanner at top with #memberAvatar, #memberName, #memberWeight. Below: #identityAvatar, #identityName, #identityMeeting (used by vote.js). #voteHint text.

    6. **Connection status**: #connectionDot + #connectionStatus in top-right corner. #offlineBanner below header (hidden by default).

    7. **Meeting/member selection** (non-token flow): #meetingSelect dropdown, #memberSelect dropdown — shown when no token in URL.

    8. **Motion display**: Card with #motionTitle, #resoText (resolution text), #voteResolutionCounter (e.g., "Resolution 3/8"), #motionProgressText, #voteSubtitle.

    9. **Vote buttons**: Three large .vote-btn buttons with data-choice="pour", data-choice="contre", data-choice="abstention". Each button: icon + label. Full-width on mobile. Use distinct colors per choice (green/red/amber via tokens).

    10. **Presence toggle**: #btnPresence button with .presence-toggle-icon and .presence-toggle-label inside.

    11. **Document consultation**: #btnConsultDocument button. Below or in modal: ag-pdf-viewer web component.

    12. **Vote status and receipt**: #voteStatus area, #voteReceipt (shown after vote confirmed).

    13. **Confirmation overlay**: #confirmationOverlay — centered modal/overlay. Contains: #confirmResoText, #confirmResoDetails, #confirmChoice (shows selected vote with color), #cMeeting, #cMember, #cMotion context fields, #btnConfirm + #btnCancel buttons, #btnConfirmInline alternative. #confirmedChoice post-confirmation display.

    14. **Speech box**: #speechBox card with #speechLabel, #speechHint, #speechHandIcon, #btnHand, #speechQueuePos with #speechPosNumber. #currentSpeakerBanner with #currentSpeakerName, #currentSpeakerTimer, #currentSpeakerQueue, #currentSpeakerQueueCount.

    15. **Bottom navigation**: Fixed bottom bar with .bottom-nav-item[data-nav] items, #bottomNavDot, #bottomNavStatusLabel.

    16. **Scripts**: htmx, utils, shared, vote.js, vote-ui.js (NO shell.js — voter page has no sidebar).

    Rewrite vote.css from scratch:
    - All values via design tokens
    - Full-screen layout: 100vh, centered content, mobile-first
    - Vote buttons: large (min 64px height), rounded, distinct colors via tokens, touch-friendly with active/pressed states, disabled state
    - Confirmation overlay: centered card with backdrop, large choice indicator
    - Identity banner: horizontal bar with avatar, name, weight badge
    - Connection dot: small circle with pulse animation for connected state
    - Offline banner: warning bar at top
    - Speech box: card with hand icon, queue position badge
    - Bottom nav: fixed bottom bar with dot indicators
    - Receipt: success card with check icon
    - Blocked overlay: full-screen dimmed with centered message
    - Loading state: centered spinner
    - Meeting/member selects: full-width on mobile
    - Responsive: designed mobile-first, stacks well on larger screens too
    - No hardcoded hex colors — all via var() tokens
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && node -e "const fs=require('fs');const html=fs.readFileSync('public/vote.htmx.html','utf8');const ids=['voteApp','voteLoadingState','blockedOverlay','blockedMsg','identityAvatar','identityName','identityMeeting','voteHint','btnPresence','btnConsultDocument','motionTitle','resoText','voteResolutionCounter','motionProgressText','voteSubtitle','voteStatus','voteReceipt','confirmationOverlay','confirmChoice','confirmedChoice','confirmResoText','confirmResoDetails','btnConfirm','btnCancel','btnConfirmInline','connectionDot','connectionStatus','offlineBanner','bottomNavDot','bottomNavStatusLabel','identityBanner','memberAvatar','memberName','memberWeight','memberSelect','meetingSelect','speechBox','speechLabel','speechHint','speechHandIcon','speechQueuePos','speechPosNumber','btnHand','currentSpeakerBanner','currentSpeakerName','currentSpeakerTimer','currentSpeakerQueue','currentSpeakerQueueCount','cMeeting','cMember','cMotion'];const missing=ids.filter(id=>!html.includes('id=\"'+id+'\"'));if(missing.length){console.error('MISSING IDs:',missing);process.exit(1)}else{console.log('All',ids.length,'IDs found')}" && grep -c 'var(--' public/assets/css/vote.css</automated>
  </verify>
  <acceptance_criteria>
    - The node ID verification script exits 0 (all 49 IDs found)
    - grep 'data-choice="pour"' public/vote.htmx.html returns match
    - grep 'data-choice="contre"' public/vote.htmx.html returns match
    - grep 'data-choice="abstention"' public/vote.htmx.html returns match
    - grep 'vote-btn' public/vote.htmx.html returns match
    - grep 'bottom-nav-item' public/vote.htmx.html returns match
    - grep 'data-nav' public/vote.htmx.html returns match
    - grep 'presence-toggle-icon' public/vote.htmx.html returns match
    - grep 'presence-toggle-label' public/vote.htmx.html returns match
    - grep 'ag-pdf-viewer' public/vote.htmx.html returns match
    - grep 'var(--' public/assets/css/vote.css returns 40+ matches
    - No hardcoded hex colors in vote.css outside comments
    - grep 'vote.js' public/vote.htmx.html returns match
    - grep 'vote-ui.js' public/vote.htmx.html returns match
    - NO grep 'shell.js' match (voter page has no sidebar shell)
  </acceptance_criteria>
  <done>Vote/ballot HTML completely rewritten with v4.3 design language. Full-screen mobile ballot layout. All 49+ DOM IDs preserved for vote.js and vote-ui.js. Large vote buttons, confirmation overlay, receipt, speech box, bottom nav all present. CSS uses tokens exclusively.</done>
</task>

<task type="auto">
  <name>Task 2: Verify vote JS wiring and fix broken selectors</name>
  <files>public/assets/js/pages/vote.js, public/assets/js/pages/vote-ui.js</files>
  <read_first>
    - public/vote.htmx.html (the NEW rebuilt HTML from Task 1)
    - public/assets/js/pages/vote.js (check all selectors against new HTML)
    - public/assets/js/pages/vote-ui.js (check all selectors against new HTML)
  </read_first>
  <action>
    After the HTML rebuild, verify every getElementById/querySelector call in BOTH vote.js and vote-ui.js matches the new HTML:

    1. Cross-reference all getElementById calls in vote.js (10 IDs): blockedMsg, blockedOverlay, btnConsultDocument, btnPresence, identityAvatar, identityMeeting, identityName, voteApp, voteHint, voteLoadingState.

    2. Cross-reference all getElementById calls in vote-ui.js (39 IDs): bottomNavDot, bottomNavStatusLabel, btnCancel, btnConfirm, btnConfirmInline, btnHand, cMeeting, cMember, cMotion, confirmationOverlay, confirmChoice, confirmedChoice, confirmResoDetails, confirmResoText, connectionDot, connectionStatus, currentSpeakerBanner, currentSpeakerName, currentSpeakerQueue, currentSpeakerQueueCount, currentSpeakerTimer, identityBanner, meetingSelect, memberAvatar, memberName, memberSelect, memberWeight, motionProgressText, motionTitle, offlineBanner, resoText, speechBox, speechHandIcon, speechHint, speechLabel, speechPosNumber, speechQueuePos, voteApp, voteReceipt, voteResolutionCounter, voteStatus, voteSubtitle.

    3. Cross-reference class-based selectors: .vote-btn, [data-choice], .presence-toggle-icon, .presence-toggle-label, ag-pdf-viewer, .bottom-nav-item[data-nav].

    4. If any selectors are broken, update the JS files to match.

    5. Verify API endpoints exist and are correct:
       - /api/v1/ballots_cast.php (cast vote)
       - /api/v1/current_motion.php (get current motion)
       - /api/v1/attendances.php (attendance check)
       - /api/v1/attendances_upsert.php (mark presence)
       - /api/v1/invitations_redeem.php (token redemption)
       - /api/v1/meetings_index.php (meeting list)
       - /api/v1/members.php (member list)
       - /api/v1/vote_policies.php (vote policies)
       - /api/v1/meeting_vote_settings.php (vote settings)
       - /api/v1/resolution_documents (PDF documents)
       - /api/v1/resolution_document_serve (PDF serve)
       - /api/v1/device_heartbeat.php (heartbeat)
       - /api/v1/speech_current.php (current speaker)
       - /api/v1/speech_my_status.php (my speech status)
       - /api/v1/speech_request.php (request to speak)

    6. Verify the optimistic feedback pattern: on vote button click, immediately show visual feedback (button state change) before waiting for API response.

    7. Verify confirmation flow: click vote button -> show #confirmationOverlay -> #btnConfirm sends to API -> show #voteReceipt.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && node -e "const fs=require('fs');const html=fs.readFileSync('public/vote.htmx.html','utf8');const js1=fs.readFileSync('public/assets/js/pages/vote.js','utf8');const js2=fs.readFileSync('public/assets/js/pages/vote-ui.js','utf8');const js=js1+js2;const ids=[...js.matchAll(/getElementById\(['\"]([^'\"]+)['\"]\)/g)].map(m=>m[1]);const unique=[...new Set(ids)];const missing=unique.filter(id=>!html.includes('id=\"'+id+'\"'));if(missing.length){console.error('MISSING IDs:',missing);process.exit(1)}else{console.log('All',unique.length,'unique IDs found in HTML')}"</automated>
  </verify>
  <acceptance_criteria>
    - The node verification script exits 0 (all unique IDs from both JS files found in HTML)
    - grep 'ballots_cast' public/assets/js/pages/vote.js returns match (vote cast endpoint)
    - grep 'current_motion' public/assets/js/pages/vote.js returns match (motion polling)
    - grep 'speech_request' public/assets/js/pages/vote-ui.js returns match (speech endpoint)
    - No syntax errors: node -c public/assets/js/pages/vote.js exits 0
    - No syntax errors: node -c public/assets/js/pages/vote-ui.js exits 0
  </acceptance_criteria>
  <done>All vote.js and vote-ui.js selectors match rebuilt HTML. No broken DOM references. Complete voting flow wired: token redemption, motion display, vote cast with confirmation, receipt, speech requests, connection status. All 15+ API endpoints verified.</done>
</task>

</tasks>

<verification>
1. Open vote page in browser — loading state appears, then ballot UI
2. Meeting/member selection works when no token provided
3. Motion title and resolution text display
4. Three vote buttons render with correct labels and colors
5. Click a vote button shows confirmation overlay
6. Confirm sends vote, receipt appears
7. Presence toggle works
8. Connection dot shows status
9. Speech box shows with hand-raise button
10. Bottom nav renders with status label
11. No JS console errors on page load
12. Dark mode works via tokens
</verification>

<success_criteria>
- Vote/ballot page fully rebuilt with v4.3 design language
- Full-screen mobile ballot with large touch-friendly buttons
- Complete voting flow: select -> confirm -> receipt
- Optimistic feedback on vote cast
- Connection status and offline banner functional
- Speech request box wired
- PDF document consultation available
- All 49+ DOM IDs preserved — both vote.js and vote-ui.js bind without errors
- Dark mode works via tokens
- No JS console errors
</success_criteria>

<output>
After completion, create `.planning/phases/50-secondary-pages-part-2/50-04-SUMMARY.md`
</output>
