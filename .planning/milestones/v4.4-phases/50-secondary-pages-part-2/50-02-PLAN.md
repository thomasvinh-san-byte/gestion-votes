---
phase: 50-secondary-pages-part-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/members.htmx.html
  - public/assets/css/members.css
  - public/assets/js/pages/members.js
autonomous: true
requirements: [REB-06, WIRE-01, WIRE-02]

must_haves:
  truths:
    - "Members page shows KPIs: total, active, inactive, voting power, avg power, email coverage"
    - "Members list renders in card or table view with toggle"
    - "CSV import uploads a file, shows result count, and creates real member records via /api/v1/members_import_csv.php"
    - "Member CRUD works: create, edit (name, email, power, active), delete — persists after reload"
    - "Groups management tab: create group with name+color, assign members to groups, edit/delete groups"
    - "Search filters members, sort dropdown reorders, filter chips filter by group/status"
    - "Pagination navigates through member list"
    - "Onboarding state shown when no members exist"
    - "No JS console errors — all DOM IDs preserved for members.js"
  artifacts:
    - path: "public/members.htmx.html"
      provides: "Complete members page with list, groups, import tabs, KPI bar, detail modal"
      contains: "membersList"
    - path: "public/assets/css/members.css"
      provides: "Members styles — cards, table, groups, import zone, modals, responsive"
      contains: "members-list"
    - path: "public/assets/js/pages/members.js"
      provides: "Members JS — CRUD, groups, CSV import, filter, sort, paginate"
      contains: "members_import_csv"
  key_links:
    - from: "public/members.htmx.html"
      to: "public/assets/js/pages/members.js"
      via: "DOM IDs preserved for JS binding"
      pattern: "membersList|membersCount|searchInput|sortSelect|groupFilters|csvFile|btnImport|btnCreate|btnCreateGroup|groupsList|kpiTotal|kpiActive|kpiInactive|memberDetailDialog|paginPrev|paginNext|uploadZone"
    - from: "public/assets/js/pages/members.js"
      to: "/api/v1/members.php"
      via: "fetch calls for CRUD"
      pattern: "api.*members\\.php|api.*member_groups|api.*members_import_csv"
---

<objective>
Ground-up rebuild of the members page — complete HTML+CSS rewrite from scratch with v4.3 design language. Preserve all DOM IDs that members.js targets. Verify CRUD, CSV import, groups management, and card/table toggle all wire correctly.

Purpose: Members management is central to the voting system — must support card/table views, bulk CSV import, group management, and individual CRUD.
Output: New members.htmx.html + members.css, with members.js verified working.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-secondary-pages-part-2/50-CONTEXT.md

@public/members.htmx.html
@public/assets/css/members.css
@public/assets/js/pages/members.js
@public/assets/css/app.css
@public/dashboard.htmx.html

<interfaces>
<!-- Critical DOM IDs that members.js targets — ALL must exist in new HTML -->

KPI bar:
- #kpiTotal — total members count
- #kpiActive — active members
- #kpiInactive — inactive members
- #kpiPower — total voting power
- #kpiAvgPower — average power
- #kpiEmailCoverage — email coverage %

Tab navigation (management tabs):
- .mgmt-tab[data-mgmt-tab] — tab buttons (members, groups, import)
- .mgmt-tab-panel[data-mgmt-panel] — tab panels

Members list:
- #membersList — members card/table container
- #membersCount — count display with .count child
- #membersOnboarding — onboarding state (shown when 0 members)
- #searchInput — search input
- #sortSelect — sort dropdown
- #resultsSubtitle — results subtitle text
- #activeFiltersHint — active filters hint
- .filter-chip[data-filter] — filter chips for group/status

Pagination:
- #paginPrev — previous page button
- #paginNext — next page button
- #paginInfo — page info text
- #paginSize — page size dropdown

Create/Edit member:
- #btnCreate — create member button
- #mName, #mEmail, #mPower, #mActive — create form fields

Member detail dialog:
- #memberDetailDialog — detail/edit dialog
- #memberDetailBody — dialog body content
- #closeMemberDetail — close button
- #editMemberName, #editMemberEmail, #editMemberPower, #editMemberActive — edit fields (querySelector inside dialog)

Groups tab:
- #btnCreateGroup — create group button
- #groupName, #groupColor — create group form fields
- #groupsList — groups list container
- #groupFiltersField — group filter field
- #groupFilters — group filter container
- #editGroupName, #editGroupDesc, #editGroupColor — edit group fields (querySelector)

Import tab:
- #uploadZone — drag-and-drop upload zone
- #csvFile — file input
- #fileName — selected file name display
- #btnImport — import button
- #importOut — import results output

Onboarding:
- #onbStepMembers, #onbStepWeights, #onbStepGroups, #onbStepMeeting — onboarding step indicators
- .onboarding-step-num, .onboarding-optional — onboarding classes

Seed (dev):
- #btnSeed — dev seed button
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild members HTML+CSS from scratch</name>
  <files>public/members.htmx.html, public/assets/css/members.css</files>
  <read_first>
    - public/members.htmx.html (current HTML — extract all DOM IDs and structure)
    - public/assets/js/pages/members.js (JS — understand ALL getElementById/querySelector calls)
    - public/assets/css/members.css (current CSS — understand structure)
    - public/assets/css/app.css (design system tokens and utility classes)
    - public/dashboard.htmx.html (reference for v4.3 app-shell, card layout, KPI pattern)
  </read_first>
  <action>
    Rewrite members.htmx.html from scratch with v4.3 design language:

    1. **HTML structure**: app-shell layout (sidebar, header with gradient accent bar using members persona color, main). Same head pattern as dashboard (critical-tokens, fonts, app.css, members.css).

    2. **Page title**: v4.3 pattern — .page-title with gradient bar, members icon, title "Membres", breadcrumb.

    3. **KPI bar**: Horizontal 6-card grid — #kpiTotal, #kpiActive, #kpiInactive, #kpiPower, #kpiAvgPower, #kpiEmailCoverage. Use shadow-sm cards with icon+value+label.

    4. **Management tabs**: .mgmt-tab buttons with data-mgmt-tab attributes for "members", "groups", "import". Below: .mgmt-tab-panel divs with data-mgmt-panel attributes.

    5. **Members panel**: Toolbar with #searchInput, #sortSelect, #btnCreate, .filter-chip[data-filter] chips. #activeFiltersHint, #resultsSubtitle. #membersList container (JS renders cards/rows). #membersOnboarding (hidden when members exist) with onboarding steps (#onbStepMembers, #onbStepWeights, #onbStepGroups, #onbStepMeeting). Pagination bar: #paginPrev, #paginInfo, #paginNext, #paginSize. #membersCount with .count span.

    6. **Groups panel**: #btnCreateGroup button. Create form: #groupName input, #groupColor color input. #groupsList container. #groupFilters with #groupFiltersField.

    7. **Import panel**: #uploadZone drag-drop area. Hidden #csvFile file input. #fileName display. #btnImport button. #importOut results container. #btnSeed dev button (hidden in production).

    8. **Member detail dialog**: #memberDetailDialog overlay with #memberDetailBody, #closeMemberDetail button. Edit fields: #editMemberName, #editMemberEmail, #editMemberPower, #editMemberActive.

    9. **Create member form**: inline or modal with #mName, #mEmail, #mPower, #mActive fields.

    10. **Scripts**: htmx, utils, shared, shell, meeting-context, ag-searchable-select, members.js.

    Rewrite members.css from scratch:
    - All values via design tokens
    - KPI grid: 6 columns desktop, 3 tablet, 2 mobile
    - Management tabs: horizontal tab bar with active underline
    - Members list: card grid (3 cols desktop) or table depending on view
    - Member cards: shadow-sm, avatar placeholder, name, email, power badge, status dot
    - Groups: color dot + name + member count, inline edit
    - Import zone: dashed border, drag-over highlight
    - Detail dialog: slide-in panel or centered modal
    - Onboarding: step indicators with check marks
    - Pagination: centered nav bar
    - Responsive: single column below 768px
    - No hardcoded hex colors
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c 'membersList\|membersCount\|searchInput\|sortSelect\|btnCreate\|btnCreateGroup\|groupsList\|csvFile\|btnImport\|uploadZone\|memberDetailDialog\|kpiTotal\|kpiActive\|kpiInactive\|kpiPower\|paginPrev\|paginNext\|membersOnboarding' public/members.htmx.html && grep -c 'var(--' public/assets/css/members.css</automated>
  </verify>
  <acceptance_criteria>
    - grep 'id="membersList"' public/members.htmx.html returns match
    - grep 'id="membersCount"' public/members.htmx.html returns match
    - grep 'id="searchInput"' public/members.htmx.html returns match
    - grep 'id="sortSelect"' public/members.htmx.html returns match
    - grep 'id="btnCreate"' public/members.htmx.html returns match
    - grep 'id="btnCreateGroup"' public/members.htmx.html returns match
    - grep 'id="groupsList"' public/members.htmx.html returns match
    - grep 'id="groupName"' public/members.htmx.html returns match
    - grep 'id="groupColor"' public/members.htmx.html returns match
    - grep 'id="csvFile"' public/members.htmx.html returns match
    - grep 'id="btnImport"' public/members.htmx.html returns match
    - grep 'id="uploadZone"' public/members.htmx.html returns match
    - grep 'id="fileName"' public/members.htmx.html returns match
    - grep 'id="importOut"' public/members.htmx.html returns match
    - grep 'id="memberDetailDialog"' public/members.htmx.html returns match
    - grep 'id="memberDetailBody"' public/members.htmx.html returns match
    - grep 'id="closeMemberDetail"' public/members.htmx.html returns match
    - grep 'id="kpiTotal"' public/members.htmx.html returns match
    - grep 'id="kpiActive"' public/members.htmx.html returns match
    - grep 'id="kpiInactive"' public/members.htmx.html returns match
    - grep 'id="kpiPower"' public/members.htmx.html returns match
    - grep 'id="kpiAvgPower"' public/members.htmx.html returns match
    - grep 'id="kpiEmailCoverage"' public/members.htmx.html returns match
    - grep 'id="mName"' public/members.htmx.html returns match
    - grep 'id="mEmail"' public/members.htmx.html returns match
    - grep 'id="mPower"' public/members.htmx.html returns match
    - grep 'id="mActive"' public/members.htmx.html returns match
    - grep 'id="paginPrev"' public/members.htmx.html returns match
    - grep 'id="paginNext"' public/members.htmx.html returns match
    - grep 'id="paginInfo"' public/members.htmx.html returns match
    - grep 'id="paginSize"' public/members.htmx.html returns match
    - grep 'id="membersOnboarding"' public/members.htmx.html returns match
    - grep 'id="onbStepMembers"' public/members.htmx.html returns match
    - grep 'id="onbStepWeights"' public/members.htmx.html returns match
    - grep 'id="onbStepGroups"' public/members.htmx.html returns match
    - grep 'id="onbStepMeeting"' public/members.htmx.html returns match
    - grep 'id="btnSeed"' public/members.htmx.html returns match
    - grep 'data-mgmt-tab' public/members.htmx.html returns match
    - grep 'data-mgmt-panel' public/members.htmx.html returns match
    - grep 'filter-chip' public/members.htmx.html returns match
    - grep 'var(--' public/assets/css/members.css returns 30+ matches
    - grep 'members.js' public/members.htmx.html returns match
  </acceptance_criteria>
  <done>Members HTML completely rewritten with v4.3 design language. Three management tabs (members, groups, import) all present. KPI bar, member list, groups, import zone, detail dialog all have correct IDs. CSS uses tokens exclusively.</done>
</task>

<task type="auto">
  <name>Task 2: Verify members JS wiring and fix broken selectors</name>
  <files>public/assets/js/pages/members.js</files>
  <read_first>
    - public/members.htmx.html (the NEW rebuilt HTML from Task 1)
    - public/assets/js/pages/members.js (current JS — check all selectors against new HTML)
  </read_first>
  <action>
    After the HTML rebuild, verify every getElementById/querySelector call in members.js matches the new HTML:

    1. Cross-reference all 37+ getElementById calls against new HTML. Create a checklist and confirm each exists.

    2. Cross-reference class-based selectors: .filter-chip[data-filter], .mgmt-tab, .mgmt-tab-panel, .count, #editGroupColor, #editGroupDesc, #editGroupName, #editMemberActive, #editMemberEmail, #editMemberName, #editMemberPower, .onboarding-optional, .onboarding-step-num.

    3. If any selectors are broken, update members.js to match. Common fixes: class name changes, new wrapper elements.

    4. Verify API endpoint paths exist:
       - /api/v1/members.php (CRUD)
       - /api/v1/members.php?include_groups=1 (list with groups)
       - /api/v1/member_groups.php (groups CRUD)
       - /api/v1/member_group_assignments.php (assign members to groups)
       - /api/v1/members_import_csv.php (CSV import)
       - /api/v1/dev_seed_members.php (dev seed)

    5. Ensure error handling uses toast (setNotif from utils.js) on API failures.

    6. Verify the upload zone drag-and-drop events wire correctly to new HTML structure.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && node -e "const fs=require('fs');const html=fs.readFileSync('public/members.htmx.html','utf8');const js=fs.readFileSync('public/assets/js/pages/members.js','utf8');const ids=[...js.matchAll(/getElementById\(['\"]([^'\"]+)['\"]\)/g)].map(m=>m[1]);const missing=ids.filter(id=>!html.includes('id=\"'+id+'\"'));if(missing.length){console.error('MISSING IDs:',missing);process.exit(1)}else{console.log('All',ids.length,'IDs found in HTML')}"</automated>
  </verify>
  <acceptance_criteria>
    - The node verification script exits 0 (all getElementById targets exist in HTML)
    - grep 'members_import_csv' public/assets/js/pages/members.js returns match (import endpoint)
    - grep 'member_groups' public/assets/js/pages/members.js returns match (groups endpoint)
    - grep 'members.php' public/assets/js/pages/members.js returns match (CRUD endpoint)
    - No syntax errors: node -c public/assets/js/pages/members.js exits 0
  </acceptance_criteria>
  <done>All members.js selectors match rebuilt HTML. No broken DOM references. CRUD, CSV import, groups management, filter/sort all wired. API endpoints verified.</done>
</task>

</tasks>

<verification>
1. Open members page in browser — KPI bar shows 6 metrics
2. Members tab shows member cards (or onboarding if empty)
3. Groups tab shows group list, can create/edit groups
4. Import tab shows upload zone, can select CSV file
5. Search filters members, sort reorders
6. Pagination works
7. Click member opens detail dialog for editing
8. Tab switching works between members/groups/import
9. No JS console errors on page load
10. Dark mode toggle works
</verification>

<success_criteria>
- Members page fully rebuilt with v4.3 design language
- Three management tabs functional (members, groups, import)
- CRUD operations work for members and groups
- CSV import wired to real endpoint
- KPI bar renders with real data
- Card/table toggle works
- All DOM IDs preserved — members.js binds without errors
- Dark mode works via tokens
- No JS console errors
</success_criteria>

<output>
After completion, create `.planning/phases/50-secondary-pages-part-2/50-02-SUMMARY.md`
</output>
