---
phase: 50-secondary-pages-part-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/audit.htmx.html
  - public/assets/css/audit.css
  - public/assets/js/pages/audit.js
autonomous: true
requirements: [REB-05, WIRE-01]

must_haves:
  truths:
    - "Audit page shows events in timeline view with chronological items"
    - "Audit page shows events in table view with sortable columns and row checkboxes"
    - "View toggle switches between timeline and table without page reload"
    - "KPI bar shows event count, anomalies, integrity status, last session"
    - "CSV export downloads a valid file for all events or selected events"
    - "Detail modal opens on row click showing timestamp, user, category, severity, description, hash"
    - "Search filters events in real time, sort dropdown reorders, type filter tabs work"
    - "Pagination navigates through large event sets"
    - "No JS console errors — all DOM IDs preserved for audit.js"
  artifacts:
    - path: "public/audit.htmx.html"
      provides: "Complete audit page with timeline+table views, KPI bar, detail modal"
      contains: "auditTableBody"
    - path: "public/assets/css/audit.css"
      provides: "Audit styles — timeline, table, KPIs, modal, responsive"
      contains: "audit-timeline"
    - path: "public/assets/js/pages/audit.js"
      provides: "Audit JS — fetch, render, filter, sort, paginate, export CSV, detail modal"
      contains: "generateCSV"
  key_links:
    - from: "public/audit.htmx.html"
      to: "public/assets/js/pages/audit.js"
      via: "DOM IDs preserved for JS binding"
      pattern: "auditTableBody|auditTimeline|auditSearch|auditSort|auditPagination|btnExportAll|btnExportSelection|auditDetailModal|kpiEvents|kpiAnomalies|kpiIntegrity|kpiLastSession|selectAll|auditTableView|auditTimelineView"
    - from: "public/assets/js/pages/audit.js"
      to: "/api/v1/audit_log.php"
      via: "fetch call for audit events"
      pattern: "api.*audit_log"
---

<objective>
Ground-up rebuild of the audit page — complete HTML+CSS rewrite from scratch with v4.3 design language (gradient accent bar, shadow-md cards, token-based dark mode). Preserve all DOM IDs that audit.js targets. Verify timeline view, table view, CSV export, and detail modal all wire correctly.

Purpose: The audit page is the compliance backbone — must display events clearly in both timeline and table views with working export for legal traceability.
Output: New audit.htmx.html + audit.css, with audit.js verified working.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-secondary-pages-part-2/50-CONTEXT.md

@public/audit.htmx.html
@public/assets/css/audit.css
@public/assets/js/pages/audit.js
@public/assets/css/app.css
@public/dashboard.htmx.html

<interfaces>
<!-- Critical DOM IDs that audit.js targets — ALL must exist in new HTML -->

KPI bar:
- #kpiEvents — total events count
- #kpiAnomalies — anomaly count
- #kpiIntegrity — integrity status text
- #kpiLastSession — last session date

View toggle + filters:
- .view-toggle-btn — view toggle buttons (timeline/table)
- #auditTimelineView — timeline container
- #auditTableView — table container
- #auditSearch — search input
- #auditSort — sort dropdown
- #auditTypeFilter .filter-tab — type filter tabs
- #auditRetryBtn — retry button on error

Timeline:
- #auditTimeline — timeline items container
- .audit-timeline-item — individual timeline items (rendered by JS)

Table:
- #auditTableBody — table body for rows
- #selectAll — select-all checkbox
- .audit-row-check — individual row checkboxes (rendered by JS)
- .audit-table-row — table rows (rendered by JS)
- tr.audit-detail-inline — inline detail rows (rendered by JS)

Pagination:
- #auditPagination — pagination container
- button[data-page] — page buttons (rendered by JS)

Export:
- #btnExportAll — export all events CSV
- #btnExportSelection — export selected events CSV (hidden by default)
- #btnExportDetail — export single event from detail modal

Detail modal:
- #auditDetailModal — modal container
- #auditDetailBackdrop — modal backdrop
- #btnCloseAuditDetail — close button (X)
- #btnCloseAuditDetail2 — close button (footer)
- #detailTimestamp, #detailUser, #detailCategory, #detailSeverity, #detailDescription, #detailHash — detail fields
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild audit HTML+CSS from scratch</name>
  <files>public/audit.htmx.html, public/assets/css/audit.css</files>
  <read_first>
    - public/audit.htmx.html (current HTML — extract all DOM IDs and structure)
    - public/assets/js/pages/audit.js (JS — understand ALL getElementById/querySelector calls)
    - public/assets/css/audit.css (current CSS — understand structure)
    - public/assets/css/app.css (design system tokens and utility classes)
    - public/dashboard.htmx.html (reference for v4.3 app-shell, card layout, KPI pattern)
  </read_first>
  <action>
    Rewrite audit.htmx.html from scratch with v4.3 design language:

    1. **HTML structure**: app-shell layout (sidebar, header with gradient accent bar using audit persona color, main, footer). Same head pattern as dashboard.htmx.html (critical-tokens, fonts, app.css, page CSS).

    2. **Page title**: v4.3 pattern — .page-title section with gradient bar, icon (shield-check or similar audit icon), title "Audit et Conformite", breadcrumb.

    3. **KPI bar**: Horizontal 4-card grid — #kpiEvents (total events), #kpiAnomalies (anomalies), #kpiIntegrity (integrity), #kpiLastSession (last session). Use shadow-sm cards with icon+value+label pattern matching dashboard KPIs.

    4. **Toolbar**: Search input #auditSearch, sort dropdown #auditSort, type filter tabs inside #auditTypeFilter with .filter-tab buttons, view toggle .view-toggle-btn buttons (timeline/table icons), export buttons #btnExportAll and #btnExportSelection (hidden by default).

    5. **Timeline view**: #auditTimelineView container with #auditTimeline inside. JS renders .audit-timeline-item elements. Use card wrapper.

    6. **Table view**: #auditTableView container with a proper table. Header row with #selectAll checkbox, columns: checkbox, date, user, category, event, severity. Body: #auditTableBody (JS renders rows). Use card wrapper with overflow-x-auto.

    7. **Pagination**: #auditPagination container below views.

    8. **Retry button**: #auditRetryBtn hidden by default, shown on API error.

    9. **Detail modal**: #auditDetailModal with #auditDetailBackdrop. Card inside with header (#btnCloseAuditDetail), body with detail fields (#detailTimestamp, #detailUser, #detailCategory, #detailSeverity, #detailDescription, #detailHash), footer with #btnExportDetail and #btnCloseAuditDetail2.

    10. **Scripts**: htmx, utils, shared, shell, meeting-context, audit.js — same order as other pages.

    Rewrite audit.css from scratch:
    - All values via design tokens (--space-*, --shadow-*, --radius-*, --color-*)
    - KPI grid: 4 columns on desktop, 2 on tablet, 1 on mobile
    - Timeline items: left border with dot marker, card-like items
    - Table: sticky header, zebra rows via tokens, hover state
    - Modal: centered overlay with backdrop blur
    - View toggle: pill-shaped button group with active state
    - Filter tabs: horizontal scrollable tab bar
    - Responsive: single column below 768px
    - No hardcoded hex colors — all via var() tokens
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c 'auditTableBody\|auditTimeline\|auditSearch\|auditSort\|auditPagination\|btnExportAll\|auditDetailModal\|kpiEvents\|kpiAnomalies\|selectAll\|auditTimelineView\|auditTableView' public/audit.htmx.html && grep -c 'var(--' public/assets/css/audit.css</automated>
  </verify>
  <acceptance_criteria>
    - grep 'id="auditTableBody"' public/audit.htmx.html returns match
    - grep 'id="auditTimeline"' public/audit.htmx.html returns match
    - grep 'id="auditTimelineView"' public/audit.htmx.html returns match
    - grep 'id="auditTableView"' public/audit.htmx.html returns match
    - grep 'id="auditSearch"' public/audit.htmx.html returns match
    - grep 'id="auditSort"' public/audit.htmx.html returns match
    - grep 'id="auditPagination"' public/audit.htmx.html returns match
    - grep 'id="btnExportAll"' public/audit.htmx.html returns match
    - grep 'id="btnExportSelection"' public/audit.htmx.html returns match
    - grep 'id="auditDetailModal"' public/audit.htmx.html returns match
    - grep 'id="auditDetailBackdrop"' public/audit.htmx.html returns match
    - grep 'id="btnCloseAuditDetail"' public/audit.htmx.html returns match
    - grep 'id="btnCloseAuditDetail2"' public/audit.htmx.html returns match
    - grep 'id="detailTimestamp"' public/audit.htmx.html returns match
    - grep 'id="detailUser"' public/audit.htmx.html returns match
    - grep 'id="detailCategory"' public/audit.htmx.html returns match
    - grep 'id="detailSeverity"' public/audit.htmx.html returns match
    - grep 'id="detailDescription"' public/audit.htmx.html returns match
    - grep 'id="detailHash"' public/audit.htmx.html returns match
    - grep 'id="btnExportDetail"' public/audit.htmx.html returns match
    - grep 'id="kpiEvents"' public/audit.htmx.html returns match
    - grep 'id="kpiAnomalies"' public/audit.htmx.html returns match
    - grep 'id="kpiIntegrity"' public/audit.htmx.html returns match
    - grep 'id="kpiLastSession"' public/audit.htmx.html returns match
    - grep 'id="selectAll"' public/audit.htmx.html returns match
    - grep 'id="auditRetryBtn"' public/audit.htmx.html returns match
    - grep 'id="auditTypeFilter"' public/audit.htmx.html returns match
    - grep 'view-toggle-btn' public/audit.htmx.html returns match
    - grep 'filter-tab' public/audit.htmx.html returns match
    - grep 'var(--' public/assets/css/audit.css returns 20+ matches
    - No hardcoded hex colors in audit.css outside comments
    - grep 'audit.js' public/audit.htmx.html returns match
  </acceptance_criteria>
  <done>Audit HTML completely rewritten with v4.3 design language. Both timeline and table views present. KPI bar, toolbar, pagination, detail modal all have correct IDs. CSS uses tokens exclusively.</done>
</task>

<task type="auto">
  <name>Task 2: Verify audit JS wiring and fix broken selectors</name>
  <files>public/assets/js/pages/audit.js</files>
  <read_first>
    - public/audit.htmx.html (the NEW rebuilt HTML from Task 1)
    - public/assets/js/pages/audit.js (current JS — check all selectors against new HTML)
  </read_first>
  <action>
    After the HTML rebuild, verify every getElementById/querySelector call in audit.js matches the new HTML:

    1. Cross-reference all 25 getElementById calls: auditDetailBackdrop, auditDetailModal, auditPagination, auditRetryBtn, auditSearch, auditSort, auditTableBody, auditTableView, auditTimeline, auditTimelineView, btnCloseAuditDetail, btnCloseAuditDetail2, btnExportAll, btnExportDetail, btnExportSelection, detailCategory, detailDescription, detailHash, detailSeverity, detailTimestamp, detailUser, kpiAnomalies, kpiEvents, kpiIntegrity, kpiLastSession, selectAll.

    2. Cross-reference class-based selectors: .audit-row-check, .audit-timeline-item, #auditTypeFilter .filter-tab, button[data-page], tr.audit-detail-inline, tr.audit-table-row, .view-toggle-btn, .count.

    3. If any selectors are broken, update audit.js to match new HTML. Common fixes: class name changes, new wrapper elements, attribute changes.

    4. Verify API endpoint: /api/v1/audit_log.php?meeting_id= is correct and the endpoint file exists.

    5. Ensure CSV export functions (generateCSV, downloadCSV) work with the data structure returned by the API.

    6. Verify the detail modal open/close logic matches new HTML structure (backdrop click, close buttons, escape key).
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && node -e "const fs=require('fs');const html=fs.readFileSync('public/audit.htmx.html','utf8');const js=fs.readFileSync('public/assets/js/pages/audit.js','utf8');const ids=[...js.matchAll(/getElementById\(['\"]([^'\"]+)['\"]\)/g)].map(m=>m[1]);const missing=ids.filter(id=>!html.includes('id=\"'+id+'\"'));if(missing.length){console.error('MISSING IDs:',missing);process.exit(1)}else{console.log('All',ids.length,'IDs found in HTML')}"</automated>
  </verify>
  <acceptance_criteria>
    - The node verification script exits 0 (all getElementById targets exist in HTML)
    - grep 'generateCSV' public/assets/js/pages/audit.js returns match (CSV export present)
    - grep 'audit_log.php' public/assets/js/pages/audit.js returns match (API endpoint correct)
    - No syntax errors: node -c public/assets/js/pages/audit.js exits 0
    - grep 'view-toggle-btn' public/assets/js/pages/audit.js returns match (view toggle wired)
  </acceptance_criteria>
  <done>All audit.js selectors match the rebuilt HTML. No broken DOM references. CSV export functional. API endpoint verified. Detail modal wired correctly.</done>
</task>

</tasks>

<verification>
1. Open audit page in browser — KPI bar shows 4 metrics
2. Timeline view renders chronological events
3. Table view renders sortable rows with checkboxes
4. View toggle switches between timeline and table
5. Search filters events, sort reorders, type tabs filter
6. Click row opens detail modal with event info
7. CSV export downloads valid file
8. Pagination works with large datasets
9. No JS console errors on page load
10. Dark mode toggle works — no hardcoded colors
</verification>

<success_criteria>
- Audit page fully rebuilt with v4.3 design language
- Timeline and table views both functional
- KPI bar renders with real data from audit_log API
- CSV export works for all and selected events
- Detail modal shows event details
- All DOM IDs preserved — audit.js binds without errors
- Dark mode works via tokens
- No JS console errors
</success_criteria>

<output>
After completion, create `.planning/phases/50-secondary-pages-part-2/50-01-SUMMARY.md`
</output>
