---
phase: 41.3-horizontal-first-layout-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - "41.3-01"
files_modified:
  - public/wizard.htmx.html
  - public/assets/css/wizard.css
  - public/assets/js/pages/wizard.js
  - public/hub.htmx.html
  - public/assets/css/hub.css
autonomous: false
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "Wizard Step 1 shows Type, Date, and Heure on one row (3-column grid) at 1366px+"
    - "Wizard Step 4 recap displays session info and participants+resolutions in 2 side-by-side columns"
    - "Hub page shows quorum bar and motions list side by side at 1024px+"
    - "Resolution add panel shows Titre and Majorite side by side"
    - "All multi-column layouts collapse to single column at 768px"
  artifacts:
    - path: "public/wizard.htmx.html"
      provides: "3-column step 1 identity row, 2-col reso add panel"
    - path: "public/assets/js/pages/wizard.js"
      provides: "2-column review-grid wrapper in recap HTML"
    - path: "public/assets/css/wizard.css"
      provides: ".review-grid CSS for step 4 2-column recap"
    - path: "public/hub.htmx.html"
      provides: ".hub-live-pair wrapper around quorum + motions"
    - path: "public/assets/css/hub.css"
      provides: ".hub-live-pair grid layout"
  key_links:
    - from: "public/wizard.htmx.html"
      to: "public/assets/css/design-system.css"
      via: ".form-grid-3 class on step 1 identity fields"
      pattern: "form-grid-3"
    - from: "public/assets/js/pages/wizard.js"
      to: "public/assets/css/wizard.css"
      via: ".review-grid class wrapping recap sections"
      pattern: "review-grid"
    - from: "public/hub.htmx.html"
      to: "public/assets/css/hub.css"
      via: ".hub-live-pair class wrapping quorum + motions"
      pattern: "hub-live-pair"
---

<objective>
Restructure wizard step 1 fields into a 3-column grid (Type | Date | Heure), convert step 4 recap to 2-column layout, place hub quorum+motions side by side, and pair resolution add panel fields.

Purpose: With the width constraints removed in plan 01, forms now fill available space. This plan uses that space effectively with multi-column layouts.
Output: Modified wizard HTML/JS/CSS and hub HTML/CSS.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.3-horizontal-first-layout-redesign/41.3-CONTEXT.md
@.planning/phases/41.3-horizontal-first-layout-redesign/41.3-RESEARCH.md
@.planning/phases/41.3-horizontal-first-layout-redesign/41.3-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wizard step 1 three-column identity grid + resolution add panel pairing</name>
  <files>public/wizard.htmx.html</files>
  <read_first>public/wizard.htmx.html (lines 105-140 — Identite section, lines 362-390 — reso-add-panel)</read_first>
  <action>
**Step 1 Identity section (lines 106-140) — convert Type+Date form-grid-2 and standalone Heure into a single form-grid-3:**

Current structure (lines 116-139):
```html
<div class="form-grid-2">
  <div class="field">Type</div>
  <div class="field">Date</div>
</div>
<div class="field" style="max-width: 200px;">
  <label>Heure</label>
  <div class="time-input-wrap">...</div>
</div>
```

Replace with a single `.form-grid-3` wrapping all three:
```html
<div class="form-grid-3">
  <div class="field">
    <label class="field-label" for="wizType">Type de s&eacute;ance <span class="req">*</span></label>
    <select class="field-input" id="wizType" aria-required="true">
      <option>AG Ordinaire</option>
      <option>AG Extraordinaire</option>
      <option>Conseil syndical</option>
    </select>
  </div>
  <div class="field">
    <label class="field-label" for="wizDate">Date <span class="req">*</span></label>
    <input class="field-input" id="wizDate" aria-required="true" type="date">
    <div class="field-error-msg" id="errWizDate">La date est obligatoire.</div>
  </div>
  <div class="field">
    <label class="field-label" for="wizTimeHH">Heure <span class="req">*</span></label>
    <div class="time-input-wrap">
      <input class="field-input time-input-hh" id="wizTimeHH" placeholder="HH" inputmode="numeric" maxlength="2" autocomplete="off" aria-label="Heures">
      <span class="time-sep">:</span>
      <input class="field-input time-input-mm" id="wizTimeMM" placeholder="MM" inputmode="numeric" maxlength="2" autocomplete="off" aria-label="Minutes">
    </div>
    <div class="field-error-msg" id="errWizTime">L'heure est obligatoire (HH:MM).</div>
  </div>
</div>
```

IMPORTANT: Remove `style="max-width: 200px;"` from the Heure field div — the grid cell handles sizing now. The `.time-input-wrap` internal constraint (max-width: 130px) is preserved in wizard.css and keeps the HH:MM inputs narrow within the cell. This is acceptable — the label aligns with other columns and the input sits naturally at its compact width.

Keep the Title field (`wizTitle`) above the grid as a standalone full-width field — it benefits from full width.

**Resolution add panel (lines 363-390) — pair Titre + Majorite:**

Current: Titre field, Description textarea, then a `.row` div containing Majorite select.

Restructure the form fields inside `.reso-add-panel` (after the `.reso-add-title` div):
```html
<div class="form-grid-2">
  <div class="field">
    <label class="field-label" for="resoTitle">Titre <span class="req">*</span></label>
    <input class="field-input" id="resoTitle" aria-required="true" placeholder="Ex : Approbation des comptes 2025" maxlength="200" autocomplete="off">
  </div>
  <div class="field">
    <label class="field-label" for="resoMaj">Majorit&eacute; <span class="req">*</span></label>
    <select class="field-input" id="resoMaj" aria-required="true">
      <option value="art24">Majorit&eacute; simple (art. 24) &mdash; D&Eacute;FAUT</option>
      <option value="art25">Majorit&eacute; absolue (art. 25)</option>
      <option value="art26">Double majorit&eacute; (art. 26)</option>
      <option value="unanimite">Unanimit&eacute;</option>
    </select>
  </div>
</div>
<div class="field">
  <label class="field-label" for="resoDesc">Description</label>
  <textarea class="form-textarea" id="resoDesc" rows="3" placeholder="D&eacute;tail optionnel..."></textarea>
</div>
```
Then keep the existing Add button row below.

IMPORTANT: Preserve all element IDs exactly (resoTitle, resoMaj, resoDesc) — wizard.js references them. Also preserve the Majorite option values (art24, art25, art26, unanimite) exactly.

Read the current reso-add-panel content carefully to identify the exact existing option values and add button markup — preserve them.
  </action>
  <verify>
    <automated>grep -c "form-grid-3" public/wizard.htmx.html && grep "form-grid-2" public/wizard.htmx.html | grep -c "reso\|Titre" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
- Step 1 Identite section: Type, Date, and Heure fields are inside a single `.form-grid-3` div
- Title (wizTitle) remains above the grid as a standalone full-width field
- Heure field no longer has `style="max-width: 200px;"` on the outer div
- Time-input-wrap internal structure is unchanged (HH:MM inputs)
- Lieu section (Place + Adresse) remains in its existing `.form-grid-2` — not modified
- Resolution add panel: Titre and Majorite are side by side in `.form-grid-2`
- Description textarea is below the grid, full width
- All element IDs preserved: wizType, wizDate, wizTimeHH, wizTimeMM, resoTitle, resoMaj, resoDesc
- All error-msg divs preserved: errWizDate, errWizTime
  </acceptance_criteria>
  <done>Wizard step 1 uses 3-column grid for Type|Date|Heure and resolution panel pairs Titre+Majorite</done>
</task>

<task type="auto">
  <name>Task 2: Wizard step 4 two-column recap + hub quorum/motions side-by-side</name>
  <files>public/assets/js/pages/wizard.js, public/assets/css/wizard.css, public/hub.htmx.html, public/assets/css/hub.css</files>
  <read_first>public/assets/js/pages/wizard.js (lines 783-855 — recap builder), public/assets/css/wizard.css (full file for placement), public/hub.htmx.html (lines 155-195 — quorum + motions sections), public/assets/css/hub.css (full file for placement)</read_first>
  <action>
**wizard.js — Wrap recap HTML in `.review-grid` container:**

In the recap builder function (around line 783), the JS builds a string of `.review-section` blocks and sets `recap.innerHTML`. Wrap the output in a `.review-grid` div:

Find the line that concatenates the review-section blocks (around line 819-851). The current pattern is:
```js
recap.innerHTML =
  '<div class="review-section">...(Informations)...</div>' +
  '<div class="review-section">...(Membres)...</div>' +
  '<div class="review-section">...(Resolutions)...</div>' +
  '<div class="review-section">...(Documents)...</div>' +
  warnings;
```

Change to wrap in a `.review-grid` with 2 columns — left column gets Informations + Documents, right column gets Membres + Resolutions:
```js
recap.innerHTML =
  '<div class="review-grid">' +
    '<div>' +
      '<div class="review-section">...(Informations)...</div>' +
      '<div class="review-section">...(Documents)...</div>' +
    '</div>' +
    '<div>' +
      '<div class="review-section">...(Membres)...</div>' +
      '<div class="review-section">...(Resolutions)...</div>' +
    '</div>' +
  '</div>' +
  warnings;
```

The `warnings` string stays OUTSIDE the review-grid (it spans full width below).

**wizard.css — Add `.review-grid` styles:**

Add after the existing `.wiz-content` block:
```css
/* Step 4 — 2-column recap grid */
.review-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-card);
  align-items: start;
}

@media (max-width: 900px) {
  .review-grid { grid-template-columns: 1fr; }
}
```

**hub.htmx.html — Add `.hub-live-pair` wrapper:**

Wrap the quorum hero and motions section in a layout-only div. Find lines 159-187 (the quorum + motions blocks).

Current:
```html
          <!-- Quorum progress bar hero (HUB-04) -->
          <div class="hub-quorum-hero" id="hubQuorumSection" style="display:none;">
            ...
          </div>

          <!-- Motions list with document badges (WIZ-08) -->
          <div class="hub-motions-section" id="hubMotionsSection" style="display:none;">
            ...
          </div>
```

After:
```html
          <div class="hub-live-pair">
            <!-- Quorum progress bar hero (HUB-04) -->
            <div class="hub-quorum-hero" id="hubQuorumSection" style="display:none;">
              ...
            </div>

            <!-- Motions list with document badges (WIZ-08) -->
            <div class="hub-motions-section" id="hubMotionsSection" style="display:none;">
              ...
            </div>
          </div>
```

CRITICAL (Pitfall 3): The `.hub-live-pair` wrapper is LAYOUT-ONLY. Do NOT add an id, do NOT add `style="display:none"`, do NOT modify hub.js. The JS continues to show/hide `hubQuorumSection` and `hubMotionsSection` individually. When one child is hidden, the grid naturally reflows to show the visible child at full width. When both are visible, they sit side by side.

**hub.css — Add `.hub-live-pair` styles:**

Add after the `.hub-layout-body` block:
```css
/* Quorum + Motions side-by-side layout */
.hub-live-pair {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-card);
  align-items: start;
}

@media (max-width: 1024px) {
  .hub-live-pair { grid-template-columns: 1fr; }
}
```
  </action>
  <verify>
    <automated>grep -c "review-grid" public/assets/js/pages/wizard.js && grep -c "review-grid" public/assets/css/wizard.css && grep -c "hub-live-pair" public/hub.htmx.html && grep -c "hub-live-pair" public/assets/css/hub.css && echo "ALL OK"</automated>
  </verify>
  <acceptance_criteria>
- wizard.js: recap HTML wrapped in `.review-grid` div with 2 child divs (left: Informations+Documents, right: Membres+Resolutions)
- wizard.js: warnings string remains outside .review-grid (full width below)
- wizard.css: `.review-grid` defined with `grid-template-columns: 1fr 1fr`, collapses at 900px
- hub.htmx.html: `.hub-live-pair` div wraps both `#hubQuorumSection` and `#hubMotionsSection`
- hub.htmx.html: `.hub-live-pair` has NO id, NO style attribute, NO display:none
- hub.css: `.hub-live-pair` defined with `grid-template-columns: 1fr 1fr`, collapses at 1024px
- hub.js: NOT modified — JS continues to control individual children's display
- All existing element IDs unchanged (hubQuorumSection, hubMotionsSection, hubQuorumBar, hubQuorumPct, hubMotionsList)
  </acceptance_criteria>
  <done>Step 4 recap shows 2-column layout; hub shows quorum and motions side by side at desktop</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification at 1366px viewport</name>
  <files>public/wizard.htmx.html, public/settings.htmx.html, public/hub.htmx.html</files>
  <action>
    Visual verification of horizontal-first layout changes across wizard, settings, and hub pages.
  </action>
  <what-built>
    Horizontal-first layout redesign across wizard and hub:
    1. Wizard fields fill full content width (680px cap removed)
    2. Step 1: Type | Date | Heure on one row (3-column grid)
    3. Step 3: Resolution Titre + Majorite side by side
    4. Step 4: 2-column recap (Info+Docs left, Members+Resolutions right)
    5. Settings content fills sidenav remainder (720px cap removed)
    6. Hub: Quorum bar + Motions list side by side
  </what-built>
  <how-to-verify>
    1. Open wizard at 1366px viewport — verify Type/Date/Heure appear on ONE row in step 1
    2. Open wizard step 3 — click "Ajouter" and verify Titre + Majorite are side by side
    3. Fill wizard steps 1-3 and advance to step 4 — verify recap is 2 columns
    4. Open settings at 1366px — verify form content fills the full width (no narrow column)
    5. Open hub at 1366px with a session that has quorum + motions — verify they sit side by side
    6. Resize to 768px — verify all layouts collapse to single column
  </how-to-verify>
  <verify>Human visual inspection at 1366px and 768px viewports</verify>
  <done>All horizontal-first layouts confirmed at desktop and mobile viewports</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Wizard page at 1366px: fields span full content area, no narrow 680px column
- Wizard step 1: 3-column row for Type | Date | Heure
- Wizard step 4: 2-column recap grid
- Settings page at 1366px: content fills sidenav remainder, no 720px cap
- Hub page at 1366px: quorum + motions side by side when both visible
- All pages at 768px: single-column fallback works correctly
- No JS errors on wizard (recap builder), hub (quorum/motions toggle)
</verification>

<success_criteria>
1. Every form exploits horizontal space — no page wastes >50% of viewport at 1366px
2. Wizard step 1 Type|Date|Heure are on one row at desktop
3. Step 4 recap is 2-column at desktop, 1-column at mobile
4. Hub quorum+motions sit side by side at desktop
5. All responsive breakpoints collapse gracefully to single column
</success_criteria>

<output>
After completion, create `.planning/phases/41.3-horizontal-first-layout-redesign/41.3-02-SUMMARY.md`
</output>
