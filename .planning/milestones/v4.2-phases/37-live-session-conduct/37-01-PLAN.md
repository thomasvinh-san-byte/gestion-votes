---
phase: 37-live-session-conduct
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/operator.css
  - public/partials/operator-exec.html
  - public/operator.htmx.html
  - public/assets/js/pages/operator-exec.js
autonomous: true
requirements: [CORE-03]

must_haves:
  truths:
    - "Operator status bar is a compact 40px strip showing session status, connected count, elapsed time, and SSE dot"
    - "Every agenda item shows a number badge and status indicator dot (pending/en cours/vote)"
    - "Every operator action button has an ag-tooltip explaining what it does"
    - "Live vote panel shows motion title prominently with tally numbers in JetBrains Mono"
    - "Guidance panels are styled as info cards, not alerts"
  artifacts:
    - path: "public/assets/css/operator.css"
      provides: "Compact status bar, agenda card items, action tooltip support, live panel enhancements, guidance card styling"
      contains: "op-exec-status-bar"
    - path: "public/partials/operator-exec.html"
      provides: "Exec status bar HTML, live vote panel badge, KPI strip enhancements"
      contains: "op-exec-status-bar"
    - path: "public/operator.htmx.html"
      provides: "ag-tooltip wrappers on action buttons"
      contains: "ag-tooltip"
    - path: "public/assets/js/pages/operator-exec.js"
      provides: "Updated renderAgendaList template with number badge and status dot"
      contains: "op-agenda-num"
  key_links:
    - from: "public/assets/js/pages/operator-exec.js"
      to: "public/assets/css/operator.css"
      via: "renderAgendaList() emits .op-agenda-num + .op-agenda-status-dot classes styled in CSS"
      pattern: "op-agenda-num"
    - from: "public/operator.htmx.html"
      to: "public/assets/js/components/ag-tooltip.js"
      via: "ag-tooltip wrapping action buttons"
      pattern: "ag-tooltip.*opBtn"
---

<objective>
Visually redesign the Operator Console page for dense, mission-control ergonomics during live sessions.

Purpose: Operators manage high-stakes live voting sessions — the console must communicate status, agenda position, and available actions at a glance. Bloomberg Terminal density with clear visual hierarchy.
Output: Compact status bar, card-style agenda items with number badges, ag-tooltip action buttons, enhanced live vote panel, info-card guidance panels.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-live-session-conduct/37-CONTEXT.md
@.planning/phases/37-live-session-conduct/37-RESEARCH.md

@public/assets/css/operator.css
@public/partials/operator-exec.html
@public/operator.htmx.html
@public/assets/js/pages/operator-exec.js
@public/assets/js/components/ag-tooltip.js
@public/assets/js/components/ag-badge.js
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compact status bar, agenda card items, and guidance panels</name>
  <files>public/assets/css/operator.css, public/partials/operator-exec.html, public/assets/js/pages/operator-exec.js</files>
  <read_first>
    - public/assets/css/operator.css (full file — 4485 lines, focus on .meeting-bar, .op-agenda-item, .op-agenda-circle, .op-kpi-strip, .op-guidance, .op-sse-indicator sections)
    - public/partials/operator-exec.html (full file — 261 lines, the exec panel HTML)
    - public/assets/js/pages/operator-exec.js (focus on renderAgendaList function around lines 313-319, and any status bar rendering)
    - public/assets/css/design-system.css (--persona-operateur token at line 381, dark mode override at line 678)
    - .planning/phases/37-live-session-conduct/37-RESEARCH.md (Before/After specs, Pattern 6 compact status bar, Pattern 1 agenda items)
  </read_first>
  <action>
**Status Bar (exec mode only):**
- In operator-exec.html, add a new `<div class="op-exec-status-bar">` at the very top of the exec panel (before op-kpi-strip). Contents as a flex row: session title span (truncated, flex:1), SSE indicator (move existing `.op-sse-indicator` here or clone), `<ag-badge variant="live" pulse>En direct</ag-badge>`, connected members count span (use `.op-status-members` class, value set by JS), elapsed timer span (`.op-status-elapsed`, font-family: var(--font-mono), value set by JS).
- In operator.css, add `.op-exec-status-bar` rules: `display: flex; align-items: center; gap: 1rem; padding: 0 1.25rem; height: 40px; background: var(--color-surface); border-bottom: 1px solid var(--color-border); border-top: 3px solid var(--persona-operateur); flex-shrink: 0; font-size: 0.8125rem;`
- The existing `.meeting-bar` in setup mode stays UNCHANGED — only exec mode gets the compact strip.
- Add dark mode overrides for `.op-exec-status-bar` using existing dark token patterns.

**Agenda Items (card style with number badge + status dot):**
- In operator-exec.js, update `renderAgendaList()` template string. Replace `<span class="op-agenda-circle"></span>` with `<span class="op-agenda-num">` + (i+1) + `</span>`. Remove the number prefix from `op-agenda-title` text. Add `<span class="op-agenda-status-dot"></span>` after the title.
- New template:
  ```
  '<div class="op-agenda-item ' + status + '" data-motion-id="' + escapeHtml(m.id) + '" role="button" tabindex="0">' +
    '<span class="op-agenda-num">' + (i + 1) + '</span>' +
    '<span class="op-agenda-title">' + escapeHtml(m.title || '') + '</span>' +
    '<span class="op-agenda-status-dot"></span>' +
  '</div>'
  ```
- In operator.css, add `.op-agenda-num` styles: `display: inline-flex; align-items: center; justify-content: center; min-width: 24px; height: 24px; border-radius: 6px; font-size: 0.75rem; font-weight: 600; font-family: var(--font-mono); background: var(--color-surface-alt); color: var(--color-text-muted); flex-shrink: 0;`
- `.op-agenda-item.current .op-agenda-num`: `background: var(--color-primary); color: var(--color-text-inverse);`
- `.op-agenda-item.voted .op-agenda-num`: `background: var(--color-success-subtle); color: var(--color-success);`
- `.op-agenda-status-dot`: `width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; margin-left: auto;`
- Status dot colors: `.pending .op-agenda-status-dot { background: var(--color-border); }`, `.current .op-agenda-status-dot { background: var(--color-primary); animation: ssePulse 2s ease-in-out infinite; }`, `.voted .op-agenda-status-dot { background: var(--color-success); }`
- Remove old `.op-agenda-circle` styles (or leave them — they won't be rendered).
- Ensure `.op-agenda-item` has: `display: flex; align-items: center; gap: 0.625rem; padding: 0.5rem 0.75rem; border-radius: 8px; cursor: pointer; transition: background 0.15s;`
- Keep existing `.current` highlight (primary-subtle bg + 3px left border).

**KPI Strip Enhancement:**
- In operator.css, ensure `.op-kpi-value` has `font-family: var(--font-mono, 'JetBrains Mono', monospace); font-variant-numeric: tabular-nums;`
- Increase `.op-kpi-label` from `0.6875rem` to `0.75rem`, remove uppercase if present, use `font-weight: 500; color: var(--color-text-muted);`

**Guidance Panels (info card style):**
- In operator.css, restyle `.op-guidance` (or `.op-guidance-card` if that exists) as: `background: var(--color-info-subtle, var(--color-surface-alt)); border: 1px solid var(--color-info, var(--color-border)); border-radius: 10px; padding: 1rem 1.25rem; margin: 0.75rem 0;`
- Ensure guidance panels do NOT use `.alert` or `.toast` styling — they are calm info cards.

**Density:**
- Add a scoped rule for exec mode density: `[data-page-role="operator"] .app-main { font-size: 0.8125rem; }` (13px base).
- Tighten `.op-split` gap to `var(--space-3)` if not already.

**Dark mode:** Add dark-mode overrides for all new classes following existing patterns (`.dark .op-exec-status-bar`, `.dark .op-agenda-num`, `.dark .op-agenda-status-dot`).
  </action>
  <verify>
    <automated>grep -c "op-exec-status-bar\|op-agenda-num\|op-agenda-status-dot" public/assets/css/operator.css public/partials/operator-exec.html public/assets/js/pages/operator-exec.js</automated>
  </verify>
  <acceptance_criteria>
    - operator-exec.html contains a .op-exec-status-bar div with session title, SSE indicator, ag-badge, members count, and elapsed timer
    - operator.css has .op-exec-status-bar at 40px height with cyan persona top border
    - renderAgendaList() in operator-exec.js emits .op-agenda-num and .op-agenda-status-dot elements
    - .op-agenda-num is a 24px rounded square with positional styling (primary for current, success for voted)
    - .op-kpi-value uses JetBrains Mono with tabular-nums
    - .op-guidance styled as info card with border-radius, subtle background, not alert style
    - Dark mode overrides exist for all new classes
  </acceptance_criteria>
  <done>Status bar is 40px compact strip with session info. Agenda items render as cards with number badge and status dot. KPI values use mono font. Guidance panels are info cards. 13px base font in exec mode.</done>
</task>

<task type="auto">
  <name>Task 2: Action button tooltips and live vote panel enhancement</name>
  <files>public/operator.htmx.html, public/assets/css/operator.css, public/partials/operator-exec.html</files>
  <read_first>
    - public/operator.htmx.html (focus on exec-mode action buttons: opBtnToggleVote, opBtnProclaim, opBtnUnanimity, opBtnPasserelle, opBtnProxy, opBtnSuspend — search for these IDs)
    - public/partials/operator-exec.html (the exec panel HTML — exec-vote-title, op-vote-bars, exec-kpi sections)
    - public/assets/css/operator.css (focus on .op-action-bar, .exec-vote-title, .exec-kpi, .op-vote-bars sections)
    - public/assets/js/components/ag-tooltip.js (confirm shadow DOM :host(:hover) pattern works on disabled buttons)
    - .planning/phases/37-live-session-conduct/37-RESEARCH.md (Pattern 1 ag-tooltip, Before/After action bar buttons, live vote panel)
  </read_first>
  <action>
**Action Button Tooltips (operator.htmx.html):**
- Wrap each action button in `<ag-tooltip>` following the Hub pattern from Phase 36. For each button:
  - `opBtnToggleVote`: `<ag-tooltip text="Ouvrir le vote pour cette resolution (raccourci : F)" position="top">` when in open state. Note: the tooltip text is static — the button label changes via JS. Set a reasonable default tooltip. The disabled state tooltip "Selectionnez une resolution d'abord" should be the default `text` attribute since the button starts disabled.
  - `opBtnProclaim`: `<ag-tooltip text="Proclamer le resultat du vote (raccourci : P)" position="top">`. Disabled tooltip: "Vote non encore cloture".
  - `opBtnUnanimity`: `<ag-tooltip text="Declarer adopte a l'unanimite" position="top">`
  - `opBtnPasserelle`: `<ag-tooltip text="Reporter cette resolution" position="top">`
  - `opBtnProxy`: `<ag-tooltip text="Gerer les pouvoirs de vote" position="top">`
  - `opBtnSuspend`: `<ag-tooltip text="Suspendre la seance" position="top">`
- IMPORTANT: Do NOT change button IDs, classes, or the `hidden` attribute behavior. Only wrap with `<ag-tooltip>`.
- The `ag-tooltip` host element receives hover even when slotted button is disabled — this is verified behavior (Pitfall 1 from RESEARCH.md).

**Live Vote Panel Enhancement (operator-exec.html + operator.css):**
- In operator-exec.html, add above `.exec-vote-title`: `<ag-badge variant="live" pulse class="exec-live-badge">VOTE EN COURS</ag-badge>` (this badge is visible only when a vote is open — JS already controls the exec-activeVote container visibility).
- In operator.css, enhance `.exec-vote-title`: `font-size: var(--text-xl); font-weight: 700;` (increase from current var(--text-lg)).
- Ensure `.exec-kpi .exec-kpi-value` (or equivalent tally value class) has `font-family: var(--font-mono, 'JetBrains Mono', monospace); font-variant-numeric: tabular-nums; font-size: var(--text-2xl);`
- Add `.exec-live-badge { margin-bottom: 0.5rem; }` to give spacing below the badge before the title.
- Keep `.op-vote-bars` 24px track height and animated fill transitions (already present).

**Toggle Vote Button Dynamic Styling:**
- In operator.css, add a rule for when vote is open: `.op-action-bar .btn-warning` is the current "Fermer le vote" styling. Per CONTEXT.md, when vote is open, the close button should be danger (red). Add: `.op-action-bar [data-vote-open] { }` — but check if JS toggles a class. If the button already changes class via JS, just ensure `btn-danger` styling exists. If not, add a CSS rule: `.op-action-bar .vote-close-mode { background: var(--color-danger); color: white; }` and note that operator-exec.js already toggles the button text/class.
- When no vote is open, the "Ouvrir le vote" should be `btn-primary` (prominent green CTA). Verify current state and adjust if needed.

**Dark mode:** Ensure `.exec-live-badge` and tooltip-wrapped buttons look correct in dark mode (ag-tooltip and ag-badge already handle dark mode internally).
  </action>
  <verify>
    <automated>grep -c "ag-tooltip" public/operator.htmx.html public/partials/operator-exec.html</automated>
  </verify>
  <acceptance_criteria>
    - Every operator action button (opBtnToggleVote, opBtnProclaim, opBtnUnanimity, opBtnPasserelle, opBtnProxy, opBtnSuspend) is wrapped in ag-tooltip with descriptive French text
    - operator-exec.html has an ag-badge "VOTE EN COURS" with variant="live" and pulse attribute above the exec-vote-title
    - .exec-vote-title is var(--text-xl) weight 700
    - Tally values use JetBrains Mono with tabular-nums
    - No button IDs, classes, or hidden attribute behavior is changed — only ag-tooltip wrapping added
  </acceptance_criteria>
  <done>All operator action buttons have contextual tooltips. Live vote panel has a prominent pulsing "VOTE EN COURS" badge. Tally numbers use JetBrains Mono. Button styling differentiates open/close vote states.</done>
</task>

</tasks>

<verification>
- Open operator console in browser (exec mode with a test session)
- Verify status bar is 40px compact strip with session info
- Verify agenda items show number badges and status dots
- Hover each action button to confirm ag-tooltip appears with French text
- Hover disabled buttons to confirm tooltip still shows (ag-tooltip :host(:hover) pattern)
- Verify KPI values and tally numbers render in JetBrains Mono
- Check dark mode for all new elements
</verification>

<success_criteria>
- Operator console has a compact 40px status bar with session info, SSE dot, and elapsed timer
- Agenda items are card-style with number badge and status indicator
- Every action button has an ag-tooltip with contextual explanation
- Live vote panel shows a pulsing "VOTE EN COURS" badge and prominent motion title
- Guidance panels styled as calm info cards
- 13px base font density in exec mode
- Dark mode works for all new elements
</success_criteria>

<output>
After completion, create `.planning/phases/37-live-session-conduct/37-01-SUMMARY.md`
</output>
