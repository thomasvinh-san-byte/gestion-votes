---
phase: 36-session-creation-flow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/wizard.htmx.html
  - public/assets/css/wizard.css
  - public/assets/js/pages/wizard.js
autonomous: true
requirements: [CORE-02]

must_haves:
  truths:
    - "Wizard stepper shows clear active/done/pending states with connector lines between steps"
    - "Every complex field label (quorum, majority) has a tooltip trigger icon explaining its purpose"
    - "Step-nav footer shows gradient CTA next button and 'Etape X sur 4' counter"
    - "Motion template selector shows card grid with icon + title + description, not inline buttons"
    - "Form sections use divider lines instead of nested card backgrounds"
    - "Field labels are 14px semibold (not 12px uppercase) with helper text below complex fields"
  artifacts:
    - path: "public/assets/css/wizard.css"
      provides: "All wizard visual redesign CSS"
      contains: "step-nav-counter"
    - path: "public/wizard.htmx.html"
      provides: "Restructured HTML with tooltips, template cards, step counter"
      contains: "wiz-template-card"
    - path: "public/assets/js/pages/wizard.js"
      provides: "Step counter update logic and step subtitle"
      contains: "wizStepSubtitle"
  key_links:
    - from: "public/assets/js/pages/wizard.js"
      to: "#wizStepSubtitle"
      via: "updateStepper() sets subtitle text"
      pattern: "wizStepSubtitle"
    - from: "public/assets/js/pages/wizard.js"
      to: ".step-nav-counter"
      via: "showStep() updates counter text"
      pattern: "step-nav-counter"
---

<objective>
Complete visual redesign of the Wizard page (session creation) to Linear-quality polish.

Purpose: Transform the wizard from a functional but crowded 4-step form into a premium-feeling creation flow with clear visual hierarchy, generous whitespace, field-level guidance tooltips, and micro-interactions. CORE-02 requirement.
Output: Visually redesigned wizard.htmx.html, wizard.css, and wizard.js with dramatic before/after contrast.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-session-creation-flow/36-CONTEXT.md
@.planning/phases/36-session-creation-flow/36-RESEARCH.md
@public/wizard.htmx.html
@public/assets/css/wizard.css
@public/assets/js/pages/wizard.js
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wizard stepper hierarchy, form field labels, sections, and field tooltips</name>
  <files>public/wizard.htmx.html, public/assets/css/wizard.css</files>
  <read_first>
    - public/wizard.htmx.html (full HTML structure, stepper, form sections, field labels)
    - public/assets/css/wizard.css (current stepper styles, .wiz-step-item, .field-label, .wiz-section)
    - public/assets/css/design-system.css (available tokens: --color-*, --space-*, --radius-*, --font-*)
    - .planning/phases/36-session-creation-flow/36-RESEARCH.md (sections: WIZARD-01 stepper hierarchy, WIZARD-02 field labels/tooltips, Code Examples)
  </read_first>
  <action>
**Stepper visual hierarchy (WIZARD-01 from RESEARCH.md):**
- In wizard.css, increase active `.wiz-snum` to 32x32px with `font-size: 0.8125rem`
- Add pending state: `.wiz-step-item:not(.active):not(.done) { opacity: 0.6; }` to clearly dim non-active/non-done steps
- Add connector line between steps: `.wiz-step-item:not(:last-child) { box-shadow: inset -1px 0 0 var(--color-border); margin-right: 0; }` -- use box-shadow NOT ::after (::after is already used for active underline bar)
- In wizard.htmx.html, add step subtitle element inside the page header area: `<p class="page-sub wiz-step-subtitle" id="wizStepSubtitle">Informations generales</p>` below the page title

**Form field labels and tooltips (WIZARD-02 from RESEARCH.md):**
- Override `.wiz-step-body .field-label` to: `font-size: 0.875rem; font-weight: 600; color: var(--color-text-dark); text-transform: none; letter-spacing: 0; display: flex; align-items: center; gap: 6px; margin-bottom: 8px;`
- Override `.field-label .req` to subtle style: `color: var(--color-text-muted); font-size: 0.65rem; font-weight: 700;` (remove the red asterisk look)
- Add `.field-hint` class in wizard.css: `font-size: 0.75rem; color: var(--color-text-muted); margin-top: 4px; line-height: 1.4;`
- Add `.field-info-btn` class (from RESEARCH Code Examples): 18x18px circular transparent button, hover shows primary color + primary-subtle bg
- In wizard.htmx.html, add `ag-popover` tooltips with `.field-info-btn` trigger to these fields in Step 1: "Politique de quorum" (explain what quorum means legally), "Type de majorite" (explain majority types). Use the exact HTML pattern from RESEARCH.md WIZARD-02 section
- Add `<div class="field-hint">` below the quorum select and majority select fields with short explanatory text

**Form section redesign (remove nested card feel):**
- Override `.wiz-section` to: `background: transparent; border: none; border-top: 1px solid var(--color-border-subtle); border-radius: 0; padding: 1.5rem 0; margin-bottom: 0;`
- Add `.wiz-section:first-of-type { border-top: none; }`
- Increase `.wiz-section-title` margin-bottom to `1.25rem`

**Remove redundant in-card step header:**
- Add `.wf-step { display: none; }` in wizard.css -- the stepper above makes this redundant, frees ~80px vertical space

**Override cramped .row layout in wizard:**
- Override `.wiz-step-body .row` to `gap: var(--space-4)` (16px min instead of 12px)

**Micro-interactions:**
- Add focus ring animation on `.wiz-step-body .field-input:focus`: `box-shadow: 0 0 0 3px var(--color-primary-glow); border-color: var(--color-primary); transition: box-shadow 0.2s ease, border-color 0.2s ease;`
- Add smooth validation state: `.wiz-step-body .field-error .field-input { border-color: var(--color-danger); transition: border-color 0.3s ease; }`
  </action>
  <verify>
    <automated>grep -c "field-info-btn\|field-hint\|wiz-step-subtitle\|wf-step.*display.*none" public/assets/css/wizard.css | xargs test 4 -le</automated>
  </verify>
  <acceptance_criteria>
- Wizard stepper circles are 32px on active, pending steps dimmed at 0.6 opacity, connector lines visible between steps
- Field labels are 14px semibold (not uppercase), required indicators are subtle (not red)
- Quorum and majority fields have ag-popover tooltip triggers with explanatory content
- .wiz-section uses transparent background with divider lines (no nested card look)
- .wf-step header inside card is hidden
- Focus ring animation on field inputs
  </acceptance_criteria>
  <done>Wizard stepper shows clear visual hierarchy, form fields have tooltips and helper text, sections use clean dividers</done>
</task>

<task type="auto">
  <name>Task 2: Step-nav gradient CTA with counter, template cards, review step, and JS wiring</name>
  <files>public/wizard.htmx.html, public/assets/css/wizard.css, public/assets/js/pages/wizard.js</files>
  <read_first>
    - public/wizard.htmx.html (step-nav sections, template buttons in step 3, review step structure)
    - public/assets/css/wizard.css (current .step-nav styles, .wiz-templates-row)
    - public/assets/js/pages/wizard.js (showStep() function, updateStepper() function, buildReviewCard() function, template click handlers)
    - .planning/phases/36-session-creation-flow/36-RESEARCH.md (sections: WIZARD-03 step nav, WIZARD-04 template cards, Pitfall 6 step counter JS)
  </read_first>
  <action>
**Step navigation footer (WIZARD-03 from RESEARCH.md):**
- In wizard.htmx.html, add `<span class="step-nav-counter" id="stepNavCounter">Etape 1 sur 4</span>` to the step-nav div (between Back and Next buttons)
- Add `btn-ghost` class to all Back buttons (replace plain `btn`)
- Add `step-nav-next` class to all Next/Submit buttons
- In wizard.css, add `.step-nav { position: relative; }` for counter positioning
- Add `.step-nav-counter` CSS: `font-size: 0.8125rem; font-weight: 600; color: var(--color-text-muted); position: absolute; left: 50%; transform: translateX(-50%);`
- Add `.step-nav-next` CSS with gradient: `background: linear-gradient(135deg, var(--color-primary), var(--color-primary-hover)); box-shadow: 0 2px 8px var(--color-primary-glow);` and hover lift: `transform: translateY(-1px); box-shadow: 0 4px 16px var(--color-primary-glow);`
- Add `.btn-ghost` if not already defined: `background: transparent; color: var(--color-text); border: 1px solid var(--color-border);`

**Motion template cards (WIZARD-04 from RESEARCH.md):**
- In wizard.htmx.html step 3, replace the `.wiz-templates-row` with inline buttons with a `.wiz-template-grid` div containing 3 `.wiz-template-card` buttons. Each card has: `.wiz-template-card-icon` (32x32px rounded square with primary-subtle bg, containing an SVG icon), `.wiz-template-card-title` (14px bold), `.wiz-template-card-desc` (12px muted, one-line description)
- Keep the existing `data-template` attributes and click handler wiring (wizard.js already handles these)
- Add all CSS from RESEARCH WIZARD-04: grid 3-column, card border + hover with primary ring + lift, icon/title/desc styles
- At 768px breakpoint, change grid to 1-column

**Review step polish:**
- In wizard.css, increase `.review-section-title` to `font-size: 0.875rem` (from 11px)
- Style `.review-modifier` buttons as small pill buttons: `font-size: 0.75rem; font-weight: 600; padding: 4px 12px; border-radius: var(--radius-sm); background: var(--color-primary-subtle); color: var(--color-primary); border: none; cursor: pointer;`
- Add hover: `.review-modifier:hover { background: var(--color-primary); color: white; }`

**JS wiring (CRITICAL -- without this, step counter shows static text):**
- In wizard.js, find the `showStep()` or `updateStepper()` function
- Add line to update step counter: `var counter = document.getElementById('stepNavCounter'); if (counter) counter.textContent = 'Etape ' + (n+1) + ' sur 4';` where n is the current step index (0-based)
- Add line to update step subtitle: `var sub = document.getElementById('wizStepSubtitle'); if (sub) sub.textContent = STEP_LABELS[n];` where STEP_LABELS is an array like `['Informations generales', 'Participants', 'Resolutions', 'Verification']` (derive exact labels from the existing step names in the HTML stepper)
  </action>
  <verify>
    <automated>grep -c "step-nav-counter\|wiz-template-grid\|wiz-template-card\|wizStepSubtitle\|stepNavCounter" public/assets/css/wizard.css public/wizard.htmx.html public/assets/js/pages/wizard.js | grep -v ":0$" | wc -l | xargs test 3 -le</automated>
  </verify>
  <acceptance_criteria>
- Step-nav footer shows gradient CTA next button with hover lift
- "Etape X sur 4" counter is visible centered in the step-nav, updates on each step change
- Step subtitle below page title updates to show current step name
- Motion templates display as 3 visual cards in a grid with icon, title, description
- Template card click still triggers existing JS behavior (form fill)
- Review step section titles are larger, "Modifier" buttons are styled pill buttons
  </acceptance_criteria>
  <done>Wizard step navigation has gradient CTA with live counter, templates are card-style selectors, review step is polished</done>
</task>

</tasks>

<verification>
- Open wizard page in browser, navigate through all 4 steps
- Verify stepper shows distinct active/done/pending states with connectors
- Verify step-nav counter updates on each step
- Verify field tooltips appear on quorum and majority fields
- Verify template cards in step 3 show icon + title + description
- Verify review step has larger section titles and styled modifier buttons
- Compare before/after screenshots for visible improvement
</verification>

<success_criteria>
The wizard page feels like Linear's issue creation flow: clear progression, generous whitespace, field-level guidance, and a premium gradient CTA. Every field has purpose-explaining tooltips, and the step counter eliminates "how many steps left?" anxiety.
</success_criteria>

<output>
After completion, create `.planning/phases/36-session-creation-flow/36-01-SUMMARY.md`
</output>
