---
phase: 41.2-wizard-ux-and-general-flow-improvements
plan: 02
type: execute
wave: 2
depends_on: ["41.2-01"]
files_modified: []
autonomous: false
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "All 4 wizard steps fit on 1080p viewport without scrolling"
    - "Collapsed sections expand and collapse correctly"
    - "Smart defaults apply on fresh visit only"
    - "Draft persistence works across page reload"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of wizard viewport fitting and UX improvements.

Purpose: Confirm all wizard steps fit within 1080p viewport, collapsed sections work correctly, smart defaults apply, and no regressions in existing functionality.
Output: Human-verified wizard UX at 1080p.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41.2-wizard-ux-and-general-flow-improvements/41.2-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify wizard viewport fitting at 1080p</name>
  <files>public/wizard.htmx.html, public/assets/css/wizard.css</files>
  <action>
Present the wizard to the user for visual verification at 1080p viewport. Guide them through the 9-step verification checklist below. No code changes in this task — purely human confirmation.

What was built in Plan 01:
- Step 1: Reduced padding, form-grid-2 for Type+Date and Place+Addr, Regles de vote collapsed in details/summary with chevron, alert-warn inside collapsed section
- Step 2: Compact horizontal upload zone, ctx-panel removed
- Step 3: Resolution add panel collapsed behind button, ctx-panel removed
- Smart defaults: today's date + 18:00 time on fresh visits

Verification steps:
1. Open browser at exactly 1080px viewport height (DevTools > Toggle Device Toolbar > set 1920x1080)
2. Navigate to /wizard.htmx.html
3. Step 1: Verify all fields visible without scrolling. Click "Regles de vote" toggle — section expands with quorum + majorite side by side. Chevron rotates. Close it.
4. Step 2: Click Next. Verify upload zone is compact (single horizontal line), member form visible, no Guide panel at bottom. No scrollbar.
5. Step 3: Click Next. Verify template cards visible, "Ajouter une resolution" button visible, no large form panel. Click the button — panel appears. Fill title "Test" and click Ajouter — panel collapses, resolution appears in list.
6. Step 4: Click Next. Verify recap fits without scrolling.
7. Smart defaults: Clear localStorage (DevTools > Application > Local Storage > delete ag-vote-wizard-draft). Reload page. Verify date shows today and time shows 18:00.
8. Draft persistence: Fill Step 1 fields, reload page — fields should restore.
9. Dark mode: Toggle dark mode. Verify all new elements (toggle, compact upload, trigger button) look correct.
  </action>
  <verify>
    <automated>echo "Human verification required — no automated test"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 steps fit within 1080p viewport without scrollbar appearing
    - Regles de vote collapses/expands with chevron animation
    - Type + Date are side by side in 2 columns
    - Quorum + Majorite are side by side when expanded
    - Upload zone is compact horizontal layout
    - Resolution add panel collapses after adding
    - Smart defaults work on fresh visit
    - Draft persistence unbroken
    - Dark mode looks correct
  </acceptance_criteria>
  <done>User confirms all wizard steps fit on 1080p viewport and all interactive elements work correctly in both light and dark mode.</done>
</task>

</tasks>

<verification>
Human visual verification at 1080p viewport confirms all steps fit without scrolling.
</verification>

<success_criteria>
User confirms all wizard steps fit on 1080p viewport and all UX improvements work correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/41.2-wizard-ux-and-general-flow-improvements/41.2-02-SUMMARY.md`
</output>
