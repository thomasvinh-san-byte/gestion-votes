---
phase: 41.1-form-fields-modernization
plan: 02
type: execute
wave: 2
depends_on:
  - 41.1-01
files_modified:
  - public/settings.htmx.html
  - public/assets/css/settings.css
  - public/admin.htmx.html
  - public/assets/css/admin.css
  - public/assets/css/members.css
autonomous: true
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "Settings SMTP fields (host+port, user+pass) are in a 2-column grid — not stacked full-width"
    - "Settings templateBody textarea uses form-textarea class for proper min-height and resize"
    - "Admin user-create form fields (name+email, password+role) are in a 2-column grid"
    - "Admin number-type settings inputs are constrained to max-width: 120px — not full-width stretch"
    - "Members create-form has consistent 12px column gap"
  artifacts:
    - path: "public/assets/css/settings.css"
      provides: "SMTP 2-column grid layout"
      contains: "form-grid-2"
    - path: "public/settings.htmx.html"
      provides: "form-textarea class on templateBody, form-grid-2 wrapper on SMTP fields"
      contains: "form-textarea"
    - path: "public/assets/css/admin.css"
      provides: "User-create 2-column grid, number input max-width"
      contains: "form-grid-2"
    - path: "public/admin.htmx.html"
      provides: "form-grid-2 wrapper on user-create form fields"
      contains: "form-grid-2"
    - path: "public/assets/css/members.css"
      provides: "Consistent create-form column gap"
      contains: "12px"
  key_links:
    - from: "public/settings.htmx.html"
      to: "public/assets/css/design-system.css"
      via: ".form-grid-2 utility class from plan 01"
      pattern: "form-grid-2"
    - from: "public/admin.htmx.html"
      to: "public/assets/css/design-system.css"
      via: ".form-grid-2 utility class from plan 01"
      pattern: "form-grid-2"
---

<objective>
Apply the design-system form utilities (form-grid-2, form-stack) to page-specific layouts: settings SMTP 2-column grid, admin user-create 2-column grid, number input constraints, textarea class fixes, and members gap harmonization.

Purpose: Short fields stop stretching full-width. Related field pairs sit side-by-side. Every page's form layout matches Stripe-quality field density.
Output: Updated HTML templates and page CSS for settings, admin, and members.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-form-fields-modernization/41.1-CONTEXT.md
@.planning/phases/41.1-form-fields-modernization/41.1-RESEARCH.md
@.planning/phases/41.1-form-fields-modernization/41.1-01-SUMMARY.md

<interfaces>
<!-- From plan 01: design-system.css utilities now available -->
.form-grid-2 — 2-column grid, 16px vertical gap, 12px horizontal gap, collapses to 1-col on mobile
.form-grid-2 .form-group-full — spans both columns
.form-stack — flex column with 16px gap between form-groups
textarea.form-input — gets min-height: 100px, resize: vertical, height: auto
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings SMTP 2-column grid + textarea class fix</name>
  <files>public/settings.htmx.html, public/assets/css/settings.css</files>
  <read_first>
    - public/settings.htmx.html (search for "smtp" and "templateBody" sections)
    - public/assets/css/settings.css (lines 335-340 for .settings-form-grid)
  </read_first>
  <action>
**Change 1 — SMTP fields 2-column grid** in settings.htmx.html:
Find the 4 SMTP form-groups (smtp_host, smtp_port, smtp_user, smtp_pass). They are currently stacked vertically inside a settings-form-grid or card-body. Wrap them in a `div class="form-grid-2"`:
- smtp_host and smtp_port should be side-by-side (row 1)
- smtp_user and smtp_pass should be side-by-side (row 2)
Each form-group stays as-is, just wrapped in the grid container.

**Change 2 — templateBody textarea class** in settings.htmx.html:
Find `<textarea class="form-input" id="templateBody"` (line ~415).
BEFORE: `class="form-input"`
AFTER: `class="form-textarea"`
This picks up the existing design-system .form-textarea rule (min-height: 100px, resize: vertical) instead of relying on the new textarea.form-input fallback.

**Change 3 — Number input max-width** in settings.css:
Add a rule to constrain number-type inputs in settings to prevent them stretching full-width:
```css
.settings-card input[type="number"] {
  max-width: 120px;
}
```
This applies to session timeout, proxy limits, and similar numeric settings fields.

Do NOT change:
- .settings-form-grid (auto-fill grid for toggle rows — different purpose, keep as-is)
- Toggle switch layout (Phase 40 — already correct)
- Any JavaScript or data attributes
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "form-grid-2" public/settings.htmx.html && grep "form-textarea" public/settings.htmx.html | grep -c "templateBody" && grep -c 'input\[type="number"\]' public/assets/css/settings.css</automated>
  </verify>
  <acceptance_criteria>
    - settings.htmx.html contains at least 1 "form-grid-2" wrapper around SMTP fields
    - templateBody textarea has class="form-textarea" (not form-input)
    - settings.css contains input[type="number"] max-width: 120px rule
    - No JS changes — all class names preserved except textarea class swap
  </acceptance_criteria>
  <done>Settings SMTP fields display in a 2-column grid (host+port, user+pass). Number inputs are compact (120px max). Template textarea resizes vertically with proper min-height.</done>
</task>

<task type="auto">
  <name>Task 2: Admin user-create 2-column grid + number constraints</name>
  <files>public/admin.htmx.html, public/assets/css/admin.css, public/assets/css/members.css</files>
  <read_first>
    - public/admin.htmx.html (search for user create form — modalUserName, modalUserEmail, modalUserPassword, modalUserRole fields)
    - public/assets/css/admin.css (lines 194-200 for form-group-search/filter)
    - public/assets/css/members.css (lines 113-125 for .create-form)
    - public/admin.htmx.html (search for templateBody textarea)
  </read_first>
  <action>
**Change 1 — Admin user-create form 2-column grid** in admin.htmx.html:
Find the user creation modal/form with fields modalUserName, modalUserEmail, modalUserPassword, modalUserRole. These are currently stacked form-groups. Wrap them in a `div class="form-grid-2"`:
- modalUserName and modalUserEmail side-by-side (row 1)
- modalUserPassword and modalUserRole side-by-side (row 2)

**Change 2 — Admin templateBody textarea** in admin.htmx.html:
Find `<textarea class="form-input" id="templateBody"` (line ~821).
BEFORE: `class="form-input"`
AFTER: `class="form-textarea"`

**Change 3 — Admin number input constraint** in admin.css:
Add:
```css
.admin-card input[type="number"],
.settings-card input[type="number"] {
  max-width: 120px;
}
```
For numeric proxy/delay settings in admin panel. Use `.admin-card` or whatever the parent container class is (verify by reading the HTML context around numeric inputs).

**Change 4 — Members create-form gap** in members.css:
Find `.create-form` (line ~113). It uses `grid-template-columns: 2fr 1.5fr 1fr auto auto`.
Verify the `gap` property. If it is not `12px` or `0 12px`, update to:
```css
.create-form {
  /* existing properties... */
  column-gap: 12px;
  row-gap: var(--space-field);
}
```
This ensures consistent 12px horizontal gap between inline fields.

Do NOT change:
- Any JavaScript selectors or data attributes
- Members table/card structure (Phase 39 — already correct)
- Admin audit/archive sections (different pages)
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "form-grid-2" public/admin.htmx.html && grep "form-textarea" public/admin.htmx.html | grep -c "templateBody" && grep -c 'type="number"' public/assets/css/admin.css && grep "gap" public/assets/css/members.css | grep "create-form" -A2 | head -3</automated>
  </verify>
  <acceptance_criteria>
    - admin.htmx.html contains at least 1 "form-grid-2" wrapper around user-create fields
    - admin.htmx.html templateBody textarea has class="form-textarea"
    - admin.css contains number input max-width constraint
    - members.css .create-form has 12px column gap
    - No JS changes — all IDs and data attributes preserved
  </acceptance_criteria>
  <done>Admin user-create form displays name+email and password+role in 2-column grids. Number inputs are compact. Members create-form has consistent 12px column spacing. All textareas use form-textarea class.</done>
</task>

</tasks>

<verification>
Open settings page -> Communication tab:
- SMTP fields should be in 2-col grid (host+port on one row, user+pass on next)
- Template textarea should have drag handle for vertical resize
- Number inputs (timeout etc) should be compact, not full-width

Open admin page -> Users tab -> create user modal:
- Name+email side-by-side, password+role side-by-side
- Number settings inputs compact

Open members page:
- Inline create form has consistent spacing between columns
</verification>

<success_criteria>
- Settings SMTP fields render in 2-column grid on desktop, collapse to 1-col on mobile
- All templateBody textareas use form-textarea class (settings + admin)
- Number-type inputs constrained to 120px max-width
- Members create-form has 12px column gap
- No JavaScript breakage — all form submissions and auto-save still work
</success_criteria>

<output>
After completion, create `.planning/phases/41.1-form-fields-modernization/41.1-02-SUMMARY.md`
</output>
