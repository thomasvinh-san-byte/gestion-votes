---
phase: 38-results-and-history
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/analytics.css
  - public/analytics.htmx.html
  - public/assets/js/pages/analytics-dashboard.js
  - public/assets/css/meetings.css
  - public/meetings.htmx.html
  - public/assets/js/pages/meetings.js
autonomous: true
requirements: [DATA-05, DATA-06]

must_haves:
  truths:
    - "Analytics KPI cards use the established .kpi-card pattern with JetBrains Mono large numbers and colored icons"
    - "Each analytics KPI has an ag-tooltip explaining what the metric measures"
    - "Chart cards have a subtitle explaining the data below the title"
    - "Period filter shows 7j/30j/90j/1an/Tout pill buttons in a rounded container"
    - "Meetings list uses .session-card pattern with hover-reveal CTA buttons"
    - "Each meeting card shows type badge, status badge, date in mono, and state-based left border"
    - "Meeting CTA label changes by status (Ouvrir/Reprendre/Voir resultats)"
  artifacts:
    - path: "public/assets/css/analytics.css"
      provides: "Analytics KPI grid modifier, period pills, chart subtitle"
      contains: "analytics-kpi-grid"
    - path: "public/analytics.htmx.html"
      provides: "KPI card HTML with ag-tooltip wrappers, chart subtitle markup"
      contains: "kpi-card kpi-card--1"
    - path: "public/assets/css/meetings.css"
      provides: "Session card density override, type badge, date mono, left border accents"
      contains: "meeting-type-badge"
    - path: "public/assets/js/pages/meetings.js"
      provides: "renderSessionItem emitting .session-card HTML"
      contains: "session-card"
  key_links:
    - from: "public/analytics.htmx.html"
      to: "public/assets/css/analytics.css"
      via: "kpi-card classes + analytics-kpi-grid modifier"
      pattern: "analytics-kpi-grid"
    - from: "public/assets/js/pages/meetings.js"
      to: "public/assets/css/meetings.css"
      via: "renderSessionItem emits session-card classes consumed by CSS"
      pattern: "session-card"
    - from: "public/assets/js/pages/analytics-dashboard.js"
      to: "public/analytics.htmx.html"
      via: "KPI IDs (kpiMeetings etc) and trend class references"
      pattern: "kpiMeetings|overview-card-trend"
---

<objective>
Redesign the analytics dashboard and meetings list pages. Analytics gets Stripe-quality KPI cards, chart subtitles, and modern period filter pills. Meetings list migrates from .session-item rows to .session-card cards with hover-reveal CTAs and type/status badges.

Purpose: DATA-05 (analytics visual redesign) + DATA-06 (meetings list visual redesign)
Output: Both pages visually transformed to match Phase 35 design system quality
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-results-and-history/38-CONTEXT.md
@.planning/phases/38-results-and-history/38-RESEARCH.md

@public/assets/css/analytics.css
@public/analytics.htmx.html
@public/assets/js/pages/analytics-dashboard.js
@public/assets/css/meetings.css
@public/meetings.htmx.html
@public/assets/js/pages/meetings.js
@public/assets/css/pages.css
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics KPI migration, chart subtitles, and period filter pills</name>
  <files>public/assets/css/analytics.css, public/analytics.htmx.html, public/assets/js/pages/analytics-dashboard.js</files>
  <read_first>
    - public/assets/css/analytics.css (full file — understand .overview-card, .overview-grid, .period-selector, .chart-card rules)
    - public/analytics.htmx.html (full file — find overview-grid markup, chart-card-header markup, period-selector markup)
    - public/assets/js/pages/analytics-dashboard.js (search for: kpiMeetings, overview-card-trend, period-btn, updatePeriod — understand JS references to HTML IDs and classes)
    - public/assets/css/pages.css (lines 1015-1170 — .kpi-grid and .kpi-card pattern reference)
    - .planning/phases/38-results-and-history/38-RESEARCH.md (Patterns 3, 4, 6 and Pitfalls 1, 2)
  </read_first>
  <action>
**HTML — analytics.htmx.html: KPI card migration**

Replace the `.overview-grid` / `.overview-card` block with the established `.kpi-card` / `.kpi-grid` pattern from pages.css. Use RESEARCH.md Pattern 3 as the exact HTML template.

1. Replace `<div class="overview-grid" id="overviewCards">` with `<div class="kpi-grid analytics-kpi-grid" id="overviewCards" aria-live="polite">`
2. Each `.overview-card` becomes a `.kpi-card` wrapped in `<ag-tooltip>`:
   - KPI 1: Seances (icon: calendar) — tooltip: "Nombre total de seances cloturees sur la periode selectionnee"
   - KPI 2: Resolutions votees (icon: check-square) — tooltip: "Total des votes exprimes sur toutes les resolutions"
   - KPI 3: Taux d'adoption (icon: trending-up) — tooltip: "% de resolutions adoptees parmi toutes les resolutions mises au vote"
   - KPI 4: Participation (icon: users) — tooltip: "Taux de participation moyen (membres presents / membres inscrits)"
3. Each card: `.kpi-card .kpi-card--N` > `.kpi-icon` (svg) + `.kpi-value#kpiXxx` + `.kpi-label` + `.overview-card-trend#kpiXxxTrend`
4. CRITICAL: Keep the existing `id` attributes (`kpiMeetings`, `kpiResolutions`, `kpiAdoptionRate`, `kpiParticipation`, `kpiMeetingsTrend`, etc.) — the JS references them by ID.
5. CRITICAL: Keep `.overview-card-trend` class on trend divs — analytics-dashboard.js updates `.overview-card-trend.up/.down` CSS classes for green/red coloring. Keep those CSS rules in analytics.css.

Use proper French accented characters in tooltip text attributes.

**HTML — analytics.htmx.html: Chart card subtitles**

For each `.chart-card-header`, wrap the `<h3>` in a `<div>` and add a `<p class="chart-card-subtitle">` below it explaining what the chart measures. Add an ag-tooltip info button next to each chart title (see RESEARCH.md Pattern 6).

Chart subtitle texts:
- "Taux de participation par seance" -> subtitle "% membres presents / membres inscrits"
- "Seances par mois" -> subtitle "Nombre de seances organisees chaque mois"
- "Repartition des votes" -> subtitle "Proportion pour / contre / abstention sur l'ensemble"
- (add subtitles for all chart cards present)

**HTML — analytics.htmx.html: Period filter pills**

Replace the `.charts-controls` / `.period-selector` / `.period-btn` markup with the redesigned `.analytics-filter-bar` / `.analytics-period-pills` / `.analytics-period-pill` markup from RESEARCH.md Pattern 4. Period options: 7j, 30j, 90j, 1 an, Tout.

CRITICAL: Keep `data-period` attribute values that the JS uses. Map the new values to what analytics-dashboard.js expects. If the JS uses `data-period="month"`, either keep the old values or update the JS event listener to handle the new values (`7j`, `30j`, `90j`, `1an`, `all`).

**CSS — analytics.css: KPI grid modifier + period pills + chart subtitle**

1. Add `.analytics-kpi-grid { margin-bottom: var(--space-section); }` — 48px breathing room before charts
2. Add `.analytics-kpi-grid ag-tooltip { display: contents; }` — prevent flex breakage (may already be inherited from pages.css `.kpi-grid ag-tooltip`, but add explicitly for safety)
3. Add `.analytics-filter-bar` flex layout (see RESEARCH.md Pattern 4 CSS)
4. Add `.analytics-period-pills` container with rounded background
5. Add `.analytics-period-pill` base + `.active` state styles
6. Add `.chart-card-subtitle` styles (text-xs, muted color, 2px top margin)
7. Keep ALL existing `.overview-card-trend` CSS rules (`.up`, `.down`, colors) — do not remove them even though the parent HTML class changed from overview-card to kpi-card
8. Remove or comment out old `.overview-card` / `.overview-grid` styles that are no longer referenced (but only after confirming no JS references remain)

**JS — analytics-dashboard.js: Period handler update (if needed)**

If the JS has event listeners on `.period-btn` class, update them to `.analytics-period-pill`. If it uses `data-period` values like `month`/`year`/`all`, update the mapping to handle `7j`/`30j`/`90j`/`1an`/`all`. The period filter click handler must:
1. Remove `.active` from all pills
2. Add `.active` to clicked pill
3. Call the data refresh function with the selected period
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "kpi-card" public/analytics.htmx.html && grep -c "ag-tooltip" public/analytics.htmx.html && grep -c "analytics-period-pill" public/assets/css/analytics.css && grep -c "chart-card-subtitle" public/assets/css/analytics.css</automated>
  </verify>
  <acceptance_criteria>
    - analytics.htmx.html uses .kpi-grid .kpi-card pattern (not .overview-card)
    - All 4 KPI cards wrapped in ag-tooltip with French metric explanations
    - KPI IDs (kpiMeetings etc) preserved for JS compatibility
    - .overview-card-trend CSS rules preserved for trend arrow coloring
    - Chart cards have .chart-card-subtitle elements with explanatory text
    - Period filter uses .analytics-period-pill pills with 7j/30j/90j/1an/Tout options
    - Period pill active state has white background + shadow + primary color
    - analytics-dashboard.js handles new period pill class and data-period values
    - .analytics-kpi-grid ag-tooltip uses display:contents
  </acceptance_criteria>
  <done>Analytics page has Stripe-quality KPI cards with tooltips, chart subtitles explain each metric, period filter is a modern pill group</done>
</task>

<task type="auto">
  <name>Task 2: Meetings list session-card migration with hover-reveal CTA</name>
  <files>public/assets/css/meetings.css, public/meetings.htmx.html, public/assets/js/pages/meetings.js</files>
  <read_first>
    - public/assets/css/meetings.css (full file — understand .session-item, .meeting-card-status, filter-pill rules)
    - public/meetings.htmx.html (full file — understand page structure, filter toolbar, session list container)
    - public/assets/js/pages/meetings.js (full file — find renderSessionItem function, STATUS_LABELS, event handlers)
    - public/assets/css/design-system.css (lines 4962-5070 — .session-card hover-reveal CTA pattern)
    - .planning/phases/38-results-and-history/38-RESEARCH.md (Pattern 5 and Pitfall 4)
  </read_first>
  <action>
**JS — meetings.js: Update renderSessionItem() to emit .session-card HTML**

This is the critical coordination point (RESEARCH Pitfall 4): CSS and JS must change together.

1. Replace the `renderSessionItem()` function body to emit `.session-card` HTML instead of `.session-item` HTML. Follow the exact pattern from RESEARCH.md Pattern 5 code example:
   - Root element: `<div class="session-card" data-id="..." data-status="...">`
   - Info block: `.session-card-info` > `.session-card-title` + `.session-card-meta`
   - Meta items: `.session-card-date` (date in formatted form), meta separators, participant count, resolution count
   - Type badge: `<span class="meeting-type-badge">` with meeting type label
   - Status: keep `.meeting-card-status` with status class and status-dot (these CSS rules already exist)
   - CTA: `<a class="btn btn-sm btn-primary session-card-cta" href="...">` with status-dependent label
   - Menu: keep existing 3-dot menu button unchanged

2. Add helper functions if not already present:
   - `getCtaLabel(status)`: 'Reprendre' for live/paused, 'Voir resultats' for closed/validated/archived, 'Ouvrir' for others
   - `getCtaHref(status, id)`: postsession URL for closed/validated/archived, hub URL for others

3. CRITICAL: Use `Utils.escapeHtml()` for title output (maintain XSS safety). Use `Utils.formatDate()` for date formatting if available, otherwise keep existing date formatting logic.

4. Keep the click handler on the session card (if one exists for navigation). Ensure `data-id` attribute is set for any JS that references it.

**CSS — meetings.css: Session card density + type badge + left border accents**

1. Add `.sessions-list .session-card { gap: var(--space-3); }` — tighter gap for meetings density (overrides design-system default)

2. Add `.meeting-type-badge` styles:
   - `font-size: 0.6875rem; padding: 2px 8px`
   - `border-radius: var(--radius-full)`
   - `background: var(--color-neutral-subtle, var(--color-bg-subtle))`
   - `color: var(--color-text-muted); border: 1px solid var(--color-border)`
   - `font-weight: 600; text-transform: uppercase; letter-spacing: 0.04em`
   - `flex-shrink: 0; white-space: nowrap`

3. Add `.sessions-list .session-card-date { font-family: var(--font-mono); font-size: var(--text-xs); }` — mono date

4. Add state-based left border accents:
   - `.sessions-list .session-card[data-status="live"] { border-left: 3px solid var(--color-danger); }`
   - `.sessions-list .session-card[data-status="closed"]`, `[data-status="validated"]`, `[data-status="archived"]` → `border-left: 3px solid var(--color-success);`

5. Remove or supersede old `.session-item` CSS rules that are no longer referenced. Keep `.meeting-card-status` rules (still used for status badge display).

6. Ensure hover-reveal CTA works: the `.session-card:hover .session-card-cta { opacity: 1 }` rule from design-system.css should apply automatically. Verify it is not overridden.

**HTML — meetings.htmx.html: Minor alignment (if needed)**

If filter pill markup uses old class names, align to `.filter-pill` or similar consistent naming. Ensure the sessions list container has a class that matches the CSS selector `.sessions-list`. If the container currently uses a different class, add `.sessions-list` as an additional class.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "session-card" public/assets/js/pages/meetings.js && grep -c "meeting-type-badge" public/assets/css/meetings.css && grep "session-card\[data-status" public/assets/css/meetings.css | head -3</automated>
  </verify>
  <acceptance_criteria>
    - meetings.js renderSessionItem() emits .session-card (not .session-item)
    - Each card shows title, mono date, participant count, resolution count
    - Type badge (AG ordinaire etc) rendered as .meeting-type-badge pill
    - Status badge uses existing .meeting-card-status classes
    - Hover-reveal CTA button appears on card hover (opacity 0->1 transition)
    - CTA label changes by status: Ouvrir / Reprendre / Voir resultats
    - CTA href points to hub or postsession based on status
    - State-based left border: red for live, green for closed/validated/archived
    - Dates use JetBrains Mono font
    - No JS console errors when meetings list renders
  </acceptance_criteria>
  <done>Meetings list shows polished session cards with hover-reveal CTAs, type/status badges, mono dates, and state-colored left borders — an admin can find a session in under 3 seconds</done>
</task>

</tasks>

<verification>
- Open analytics.htmx.html at 1080p: 4 KPI cards with large mono numbers, colored icons, tooltips on hover
- Period filter pills (7j/30j/90j/1an/Tout) render as rounded pill group with active state highlighting
- Chart cards have subtitle text below titles
- Chart data still loads and renders (no JS errors from ID or class mismatches)
- Open meetings.htmx.html at 1080p: session cards (not flat rows), hover-reveal CTA buttons
- Each meeting card shows type badge and status badge
- Dates in JetBrains Mono
- Left border colored by session status
- CTA labels are contextual (Ouvrir/Reprendre/Voir resultats)
- Dark mode: both pages maintain contrast and readability
- 768px viewport: KPIs go 2-col, charts maintain readable layout, meeting cards stack properly
</verification>

<success_criteria>
Analytics page matches Stripe dashboard clarity with proper KPI cards, chart explanations, and modern filter pills. Meetings list uses the design system session-card pattern with hover-reveal CTAs and scannable badges. Both pages show dramatic before/after improvement.
</success_criteria>

<output>
After completion, create `.planning/phases/38-results-and-history/38-02-SUMMARY.md`
</output>
