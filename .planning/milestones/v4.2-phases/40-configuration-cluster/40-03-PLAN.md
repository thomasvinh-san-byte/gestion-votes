---
phase: 40-configuration-cluster
plan: "03"
type: execute
wave: 2
depends_on: ["40-01", "40-02"]
files_modified: []
autonomous: false
requirements: [CORE-06, SEC-04, SEC-03]

must_haves:
  truths:
    - "All three configuration pages (Settings, Admin, Help) pass visual inspection in light and dark mode"
    - "Every tooltip is readable and correctly positioned"
    - "Toggle switches animate smoothly and auto-save still works"
    - "Template editor preview updates live"
    - "Help tab switching filters sections correctly"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint for all three configuration cluster pages — Settings/Admin, Email templates (within Settings), and Help/FAQ.

Purpose: Confirm that the dramatic visual improvements from plans 01 and 02 render correctly in the browser, tooltips are readable, toggles animate, template preview works, and dark mode is consistent.
Output: User approval of all configuration pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-configuration-cluster/40-01-SUMMARY.md
@.planning/phases/40-configuration-cluster/40-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Visual verification: Settings, Admin, Email templates, Help/FAQ</name>
  <files></files>
  <action>
  Present all three configuration pages to the user for visual verification. No code changes — this is a human review checkpoint.

  Pages to verify:
  - settings.htmx.html (including Communication tab for email templates)
  - admin.htmx.html
  - help.htmx.html

  What was built (plans 01 and 02):

  **Settings page:** Sidenav with 16px icons and left 3px primary border accent on active. Visual 36x20px toggle switches (CSS-only). ag-tooltip info icons on complex settings. Per-section card footer with "Enregistrer" + unsaved-dot. CNIL/security cards with colored left-border accents.

  **Email templates (Communication tab):** Two-pane editor (form + 400px preview). Clickable variable tags with ag-tooltip. Live preview with sample data. Template list with icons.

  **Admin page:** KPI labels with ag-tooltip. 4-KPI users strip. Tab icons with tooltips. State machine node tooltips.

  **Help/FAQ:** Filter-tab pill categories. FAQ section left-border accents. 48px search. Doc-links card. Support CTA card with icon.

  Verification steps:
  1. Open settings.htmx.html — sidenav icons, left border accent, toggle switches
  2. Switch sidenav sections — smooth transition, no layout shift
  3. Toggle a switch — visual pill animation, auto-save still works
  4. Hover info icons — ag-tooltip with French impact explanation
  5. Change a value — unsaved-dot appears; click Enregistrer — dot hides
  6. Communication tab — template list icons, two-pane editor, live preview, variable tag insertion
  7. Open admin.htmx.html — KPI tooltips, tab icons+tooltips, users KPI strip
  8. Open help.htmx.html — pill tabs, left-border section titles, search, doc card, support CTA
  9. Dark mode on all three — no broken contrast or invisible elements
  </action>
  <verify>User visual inspection of all three pages in light and dark mode</verify>
  <acceptance_criteria>
  - Settings sidenav has icons and active accent
  - Toggle switches render as visual pills
  - Tooltips appear on hover for all annotated elements
  - Template editor shows two-pane with live preview
  - Admin KPIs and tabs have tooltips and icons
  - Help tabs are pills, section titles have left-border
  - Dark mode renders correctly on all pages
  </acceptance_criteria>
  <done>User approves all configuration cluster pages — CORE-06, SEC-04, SEC-03 confirmed</done>
</task>

</tasks>

<verification>
User confirms all configuration cluster pages pass visual inspection.
</verification>

<success_criteria>
All three configuration pages approved — CORE-06, SEC-04, SEC-03 requirements confirmed complete.
</success_criteria>

<output>
After completion, create `.planning/phases/40-configuration-cluster/40-03-SUMMARY.md`
</output>
