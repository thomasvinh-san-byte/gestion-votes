---
phase: 35-entry-points
plan: 03
type: execute
wave: 2
depends_on: [35-01, 35-02]
files_modified: []
autonomous: false
requirements: [UX-02]

must_haves:
  truths:
    - "Both dashboard and login pages are visually transformed — a developer can point to 5+ improvements per page without prompting"
    - "Dark mode on both pages is visually equivalent in quality to light mode"
    - "Every complex dashboard element has a tooltip explaining itself on hover"
  artifacts: []
  key_links: []
---

<objective>
Visual verification checkpoint — confirm both entry point pages meet top 1% design quality

Purpose: The user explicitly stated that v4.1's CSS infrastructure work produced zero visible impact. This checkpoint ensures both pages show dramatic, immediately visible changes before proceeding to Phase 36. UX-02 (top 1% quality) is verified here via human visual inspection.

Output: User confirmation that both pages meet the visual quality bar.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-entry-points/35-CONTEXT.md
@.planning/phases/35-entry-points/35-01-SUMMARY.md
@.planning/phases/35-entry-points/35-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Dashboard and Login visual redesign meets top 1% quality</name>
  <files>public/dashboard.htmx.html, public/login.html</files>
  <action>
Present the user with verification steps for both pages. No code changes — this is a human visual inspection checkpoint.

**What was built:**

Dashboard changes:
- KPI cards: left-aligned layout, JetBrains Mono numbers at 4xl, colored icon backgrounds differentiating each card, ag-tooltip on hover
- Session cards: ag-badge status labels replacing tiny dots, structured meta with bold date and dot separators, hover-reveal CTA buttons
- Aside shortcuts: bg-change hover (not lift), ag-tooltip on each card
- Panel headers: 24px padding, border separator between header and content

Login changes:
- Background: radial gradient creating depth behind the card
- Brand: Fraunces display font heading with 40px colored logo mark
- Button: gradient background, 44px height, lift on hover
- Trust signal: lock icon + "Plateforme securisee" below footer
- Entrance animation: fade-up via @starting-style
- Error states: red border on affected input fields
- Dark mode: adapted gradient, elevated inputs, solid button color

**How to verify:**

Dashboard (light mode):
1. Open http://localhost:8080/dashboard.htmx.html
2. KPI cards should be LEFT-aligned (not centered) with large mono-spaced numbers
3. Each KPI card should have a DIFFERENT colored icon background (blue, red, yellow, accent)
4. Hover a KPI card — tooltip should appear explaining the metric
5. Session cards should show colored BADGE labels (not tiny dots) for status
6. Hover a session card — CTA button should slide in from right
7. Aside shortcut cards should change BACKGROUND on hover (no lift effect)
8. Hover a shortcut card — tooltip should appear

Login (light mode):
9. Open http://localhost:8080/login.html
10. Should see a GRADIENT BACKGROUND (soft glow at top, not flat grey)
11. Card should have ANIMATED IN (fade-up on load — refresh to see again)
12. "AG-VOTE" heading should be in FRAUNCES font (serif display) with a colored square logo above
13. Submit button should have GRADIENT background and LIFT on hover
14. "Plateforme securisee" trust signal visible below footer
15. Submit with wrong credentials — input fields should get RED BORDER

Dark mode (both pages):
16. Toggle to dark mode on both pages
17. Dashboard should maintain visual quality — no washed-out elements
18. Login gradient should adapt — card on dark surface, inputs elevated

Side-by-side test:
19. Can you point to at least 5 visible improvements on each page versus before?

Resume signal: Type "approved" to proceed to Phase 36, or describe specific issues to fix.
  </action>
  <verify>
    <automated>echo "CHECKPOINT — awaiting human verification"</automated>
  </verify>
  <done>User confirms both dashboard and login pages meet top 1% visual quality in both light and dark mode, with 5+ visible improvements identifiable per page</done>
</task>

</tasks>

<verification>
User confirms visual quality meets top 1% standard on both pages in both light and dark mode.
</verification>

<success_criteria>
- User approves both pages as meeting the visual quality bar
- User can identify 5+ visible improvements per page
- Dark mode is confirmed as equally polished
- No regressions reported in either page's functionality
</success_criteria>

<output>
After completion, create `.planning/phases/35-entry-points/35-03-SUMMARY.md`
</output>
