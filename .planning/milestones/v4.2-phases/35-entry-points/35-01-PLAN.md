---
phase: 35-entry-points
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/dashboard.htmx.html
  - public/assets/css/pages.css
  - public/assets/css/design-system.css
  - public/assets/js/pages/dashboard.js
autonomous: true
requirements: [CORE-01, UX-01]

must_haves:
  truths:
    - "KPI cards show numbers in JetBrains Mono, left-aligned, with differentiated colored icon backgrounds per card"
    - "KPI cards have ag-tooltip wrappers explaining what each metric means on hover"
    - "Session cards use ag-badge for status instead of tiny 8px dot"
    - "Session card meta has date in semibold separated by dot separators, not flat dash string"
    - "Session card CTA is hidden at rest, appears on hover (always visible on touch devices)"
    - "Aside shortcut cards have ag-tooltip wrappers and bg-change hover instead of lift"
    - "Dashboard panels have 24px padding and header/content separation"
  artifacts:
    - path: "public/assets/css/pages.css"
      provides: "KPI card visual redesign, aside shortcut redesign, panel spacing"
      contains: "font-family: var(--font-mono)"
    - path: "public/assets/css/design-system.css"
      provides: "Session card redesign with hover-reveal CTA and badge layout"
      contains: "session-card-cta"
    - path: "public/dashboard.htmx.html"
      provides: "ag-tooltip wrappers on KPI cards and shortcut cards, kpi-icon divs"
      contains: "ag-tooltip"
    - path: "public/assets/js/pages/dashboard.js"
      provides: "Session card renderer using ag-badge and structured meta"
      contains: "ag-badge"
  key_links:
    - from: "public/assets/js/pages/dashboard.js"
      to: "public/assets/css/design-system.css"
      via: "JS renders ag-badge and session-card-cta classes that CSS styles"
      pattern: "ag-badge.*variant"
    - from: "public/dashboard.htmx.html"
      to: "public/assets/js/components/ag-tooltip.js"
      via: "ag-tooltip custom elements wrapping KPI and shortcut cards"
      pattern: "<ag-tooltip"
---

<objective>
Dashboard visual redesign — transform the generic dashboard into a Stripe/Linear-quality data interface

Purpose: The dashboard is every user's first screen. KPI cards, session cards, and aside shortcuts must communicate information hierarchy through typography, color differentiation, and interaction patterns — not just through text labels. This plan addresses CORE-01 (dashboard redesign) and UX-01 (self-explanatory tooltips).

Output: Visually transformed dashboard with left-aligned mono KPI numbers, ag-badge session statuses, hover-reveal CTAs, bg-change shortcut hover, and ag-tooltip guidance on all complex elements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-entry-points/35-CONTEXT.md
@.planning/phases/35-entry-points/35-RESEARCH.md
@public/dashboard.htmx.html
@public/assets/css/pages.css
@public/assets/css/design-system.css
@public/assets/js/pages/dashboard.js
@public/assets/js/components/ag-tooltip.js
@public/assets/js/components/ag-badge.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: KPI cards — left-aligned mono numbers, differentiated icons, tooltips</name>
  <files>public/dashboard.htmx.html, public/assets/css/pages.css</files>
  <read_first>
    - public/dashboard.htmx.html (lines 90-112 — KPI grid)
    - public/assets/css/pages.css (lines 1006-1130 — dashboard styles, kpi overrides)
    - public/assets/css/design-system.css (lines 2444-2474 — kpi-card base)
    - public/assets/js/components/ag-tooltip.js (understand wrapper usage)
    - .planning/phases/35-entry-points/35-RESEARCH.md (Pattern 1: KPI Card Redesign, Pitfall 1, Pitfall 2, Pitfall 5)
  </read_first>
  <action>
**HTML changes in dashboard.htmx.html (KPI grid, lines 90-112):**

1. Wrap each `<a class="kpi-card">` in `<ag-tooltip text="..." position="bottom">`:
   - Card 1 (kpiSeances): text="Sessions AG planifiees ce mois-ci"
   - Card 2 (kpiEnCours): text="Sessions en cours de vote en direct"
   - Card 3 (kpiConvoc): text="Convocations en attente d'envoi"
   - Card 4 (kpiPV): text="Proces-verbaux a finaliser et envoyer"

2. Inside each kpi-card, add a `<div class="kpi-icon">` wrapper around the existing SVG icon. Change SVG sizes from width="18" height="18" to width="20" height="20". Keep the existing SVG paths and strokes but change `stroke` attribute to `currentColor` (color will come from CSS nth-child rules).

3. The structure of each card becomes:
```html
<ag-tooltip text="..." position="bottom">
  <a href="..." class="kpi-card" aria-label="...">
    <div class="kpi-icon">
      <svg width="20" height="20" ...stroke="currentColor"...>...</svg>
    </div>
    <div class="kpi-value primary" id="kpiSeances">-</div>
    <div class="kpi-label">AG a venir</div>
  </a>
</ag-tooltip>
```

**CSS changes in pages.css (dashboard section, after existing kpi overrides):**

Replace the existing `.kpi-card` styles in pages.css with the following. IMPORTANT: Edit pages.css, NOT design-system.css (per Pitfall 1 — pages.css overrides design-system.css):

```css
/* KPI Card Redesign — Phase 35 */
.kpi-card {
  text-align: left;
  padding: var(--space-6);
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
  background: var(--color-surface-raised);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  transition: var(--transition-shadow), transform var(--duration-moderate) var(--ease-standard);
}
.kpi-card:hover {
  transform: translateY(-3px);
  box-shadow: var(--shadow-lg);
  border-color: var(--color-border-strong);
}

/* Icon container with per-card color differentiation */
.kpi-card .kpi-icon {
  width: 36px;
  height: 36px;
  border-radius: var(--radius-lg);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.kpi-card:nth-child(1) .kpi-icon { background: var(--color-primary-subtle); color: var(--color-primary); }
.kpi-card:nth-child(2) .kpi-icon { background: var(--color-danger-subtle); color: var(--color-danger); }
.kpi-card:nth-child(3) .kpi-icon { background: var(--color-warning-subtle); color: var(--color-warning); }
.kpi-card:nth-child(4) .kpi-icon { background: var(--color-accent-subtle, var(--color-primary-subtle)); color: var(--color-accent, var(--color-primary)); }

/* KPI numbers — JetBrains Mono for scannable data */
.kpi-card .kpi-value {
  font-family: var(--font-mono);
  font-size: var(--text-4xl);
  font-weight: 700;
  line-height: 1;
  letter-spacing: var(--tracking-tight);
  font-variant-numeric: tabular-nums;
  min-width: 0;
}

/* KPI labels — uppercase small caps */
.kpi-card .kpi-label {
  font-size: var(--text-xs);
  color: var(--color-text-muted);
  font-weight: var(--font-medium);
  text-transform: uppercase;
  letter-spacing: var(--tracking-wider, 0.05em);
}
```

Also update dashboard panel padding:
```css
.dashboard-panel {
  padding: var(--space-card, 24px);
}
.dashboard-panel .flex-between {
  margin-bottom: var(--space-4, 16px);
  padding-bottom: var(--space-3, 12px);
  border-bottom: 1px solid var(--color-border-subtle);
}
```

Verify ag-tooltip click pass-through per Pitfall 2: ag-tooltip.js already has `pointer-events: none` on the tooltip body. The inner `<a>` should receive clicks. If testing reveals issues, add `display: contents` to ag-tooltip's host style.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "ag-tooltip" public/dashboard.htmx.html | grep -q "[4-9]" && grep -q "font-family: var(--font-mono)" public/assets/css/pages.css && grep -q "kpi-icon" public/dashboard.htmx.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "ag-tooltip" public/dashboard.htmx.html returns 4+ matches (one per KPI card)
    - grep "kpi-icon" public/dashboard.htmx.html returns 4 matches
    - grep "font-family: var(--font-mono)" public/assets/css/pages.css returns match
    - grep "text-align: left" public/assets/css/pages.css returns match (within kpi-card block)
    - grep "nth-child" public/assets/css/pages.css returns 4 matches for kpi icon colors
    - grep "border-bottom" public/assets/css/pages.css returns match for panel header separator
    - SVG icons inside kpi-icon use stroke="currentColor" not hardcoded color values
  </acceptance_criteria>
  <done>KPI cards are left-aligned with JetBrains Mono numbers at 4xl size, each card has a colored icon background differentiating it from the others, all 4 cards have ag-tooltip wrappers with descriptive text, and dashboard panels have proper padding with header/content separation</done>
</task>

<task type="auto">
  <name>Task 2: Session cards with ag-badge + hover CTA, aside shortcut tooltips</name>
  <files>public/assets/js/pages/dashboard.js, public/assets/css/design-system.css, public/assets/css/pages.css, public/dashboard.htmx.html</files>
  <read_first>
    - public/assets/js/pages/dashboard.js (full file — find renderSessionCard function)
    - public/assets/css/design-system.css (lines 4960-5041 — session-card styles)
    - public/assets/js/components/ag-badge.js (understand variant attribute and available variants)
    - public/dashboard.htmx.html (lines 157-186 — aside shortcut cards)
    - .planning/phases/35-entry-points/35-RESEARCH.md (Pattern 2: Session Card, Pattern 3: Aside Shortcut, Pitfall 3, Pitfall 7)
  </read_first>
  <action>
**JS changes in dashboard.js — renderSessionCard() function:**

CRITICAL: JS and CSS changes must ship together (Pitfall 7). Do NOT add opacity:0 CSS for CTA before updating the JS renderer.

1. Replace the status dot `<div class="session-card-status-dot">` with `<ag-badge>`:
```javascript
// Map session status to ag-badge variant
var badgeVariant = { draft: 'draft', scheduled: 'primary', frozen: 'info',
                     live: 'live', paused: 'live', closed: 'success',
                     validated: 'primary', archived: 'draft' }[s.status] || 'draft';
var badgePulse = (s.status === 'live' || s.status === 'paused') ? ' pulse' : '';
// Replace the status-dot line with:
h += '<ag-badge variant="' + badgeVariant + '"' + badgePulse + '>' + escapeHtml(statusLabel[s.status] || s.status) + '</ag-badge>';
```

2. Restructure the meta section from flat dash-separated string to structured spans:
```javascript
// BEFORE: h += '<div class="session-card-meta">' + dateStr + ' — ' + count + ' membres — ' + motions + ' resolutions</div>';
// AFTER:
h += '<div class="session-card-meta">';
h += '<span class="session-card-date">' + escapeHtml(dateStr) + '</span>';
h += '<span class="session-card-meta-sep">\u00B7</span>';
h += '<span>' + (s.participant_count || 0) + ' membres</span>';
h += '<span class="session-card-meta-sep">\u00B7</span>';
h += '<span>' + (s.motion_count || 0) + ' r\u00e9solutions</span>';
h += '</div>';
```

Note: Adapt the variable names to match what actually exists in the renderSessionCard function. The key data fields are the session date, member/participant count, and motion/resolution count. Read the actual function to find the correct property names.

3. Remove the `<div class="session-card-status-dot"...>` line entirely from the renderer.

**CSS changes in design-system.css — session-card block (around lines 4960-5041):**

Add/modify these rules in the existing session-card section:

```css
/* Session card redesign — Phase 35 */
.session-card {
  padding: 14px 16px;
  border-radius: var(--radius-lg);
  gap: 14px;
  transition: var(--transition-ui);
  position: relative;
}
.session-card:hover {
  border-color: var(--color-border-strong);
  box-shadow: var(--shadow-md);
  background: var(--color-surface-raised);
}

/* Hover-reveal CTA */
.session-card .session-card-cta {
  opacity: 0;
  transform: translateX(4px);
  transition: opacity var(--duration-normal) var(--ease-standard),
              transform var(--duration-normal) var(--ease-standard);
}
.session-card:hover .session-card-cta {
  opacity: 1;
  transform: translateX(0);
}

/* Touch devices: always show CTA (Pitfall 3) */
@media (hover: none) {
  .session-card .session-card-cta {
    opacity: 1;
    transform: none;
  }
}

/* Date prominence in meta */
.session-card-date {
  font-weight: var(--font-semibold);
  color: var(--color-text);
}

/* Dot separator between meta items */
.session-card-meta-sep {
  color: var(--color-border-strong);
  margin: 0 4px;
}
```

**CSS changes in pages.css — aside shortcut cards:**

Replace shortcut-card hover rules:
```css
/* Shortcut card redesign — Phase 35 */
.shortcut-card {
  padding: var(--space-4);
  border-radius: var(--radius-lg);
  border: 1px solid transparent;
  transition: var(--transition-ui);
}
.shortcut-card:hover {
  transform: none;
  box-shadow: none;
  background: var(--color-bg-subtle, var(--color-bg));
  border-color: var(--color-border);
}
.shortcut-card-title {
  font-size: var(--text-md, 1rem);
  font-weight: var(--font-semibold);
}
```

**HTML changes in dashboard.htmx.html — aside section (lines 157-186):**

Wrap each `<a class="card shortcut-card">` in `<ag-tooltip>`:
- Card 1 (Creer une seance): `<ag-tooltip text="Creer une nouvelle session en 4 etapes" position="left">`
- Card 2 (Piloter un vote): `<ag-tooltip text="Acceder a la console operateur pour piloter un vote en direct" position="left">`
- Card 3 (Consulter le suivi): `<ag-tooltip text="Voir le journal de tracabilite et l'historique des actions" position="left">`

Use position="left" since the aside is on the right side of the page.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -q "ag-badge" public/assets/js/pages/dashboard.js && grep -q "session-card-date" public/assets/css/design-system.css && grep -q "session-card-meta-sep" public/assets/css/design-system.css && grep -q "hover: none" public/assets/css/design-system.css && grep -c "ag-tooltip" public/dashboard.htmx.html | grep -q "[7-9]\|1[0-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "ag-badge" public/assets/js/pages/dashboard.js returns match (badge replaces status dot)
    - grep "session-card-status-dot" public/assets/js/pages/dashboard.js returns NO match (dot removed)
    - grep "session-card-date" public/assets/css/design-system.css returns match
    - grep "session-card-meta-sep" public/assets/css/design-system.css returns match
    - grep "opacity: 0" public/assets/css/design-system.css returns match within session-card-cta context
    - grep "hover: none" public/assets/css/design-system.css returns match (touch device fallback)
    - grep "ag-tooltip" public/dashboard.htmx.html returns 7+ matches (4 KPI from Task 1 + 3 shortcuts)
    - grep "shortcut-card:hover" pages.css shows background change, NOT transform/box-shadow
  </acceptance_criteria>
  <done>Session cards show ag-badge status labels instead of 8px dots, meta has structured date/members/resolutions with dot separators, CTA buttons hide at rest and appear on hover (always visible on touch), aside shortcut cards use bg-change hover and have ag-tooltip wrappers with descriptive text</done>
</task>

</tasks>

<verification>
1. Open dashboard in browser — KPI cards should be left-aligned with large mono numbers and colored icon backgrounds
2. Hover over KPI cards — tooltip appears explaining the metric
3. Session cards show colored status badges (not tiny dots) with structured meta
4. Hover session card — CTA button slides in from right
5. Aside shortcut cards change background on hover (no lift effect)
6. Hover aside cards — tooltip appears with description
7. Test on mobile viewport — session CTAs always visible (no hover dependency)
8. Check dark mode — all elements maintain visual quality
</verification>

<success_criteria>
- Dashboard KPI cards use JetBrains Mono numbers, left-aligned, with 4 differentiated colored icon backgrounds
- All 4 KPI cards and 3 shortcut cards have ag-tooltip wrappers (7 total tooltips on dashboard)
- Session cards use ag-badge component for status display instead of status-dot
- Session card CTA is hover-reveal on desktop, always-visible on touch devices
- Shortcut cards use bg-change hover pattern instead of lift/shadow
- Dashboard panels have proper 24px padding with header border separator
</success_criteria>

<output>
After completion, create `.planning/phases/35-entry-points/35-01-SUMMARY.md`
</output>
