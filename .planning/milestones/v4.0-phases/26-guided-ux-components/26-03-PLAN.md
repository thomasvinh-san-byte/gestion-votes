---
phase: 26-guided-ux-components
plan: 03
type: execute
wave: 2
depends_on: [26-02]
files_modified:
  - public/dashboard.htmx.html
  - public/hub.htmx.html
  - public/wizard.htmx.html
  - public/operator.htmx.html
  - public/members.htmx.html
  - public/postsession.htmx.html
  - public/analytics.htmx.html
  - public/meetings.htmx.html
  - public/assets/js/pages/operator-tabs.js
autonomous: true
requirements: [GUX-04, GUX-05, GUX-07, GUX-08, GUX-03]

must_haves:
  truths:
    - "Clicking the help button on any of the 8 pages opens a popover with page-relevant tips"
    - "Help button text reads 'Aide' (not 'Visite guidee')"
    - "Help panel content is contextual per page (different tips on wizard vs dashboard)"
    - "Every disabled primary action button displays a tooltip explaining why it is locked"
    - "Tooltip text follows the pattern 'Disponible apres [condition]'"
    - "Tooltip text clears when the button becomes enabled"
    - "Technical terms (majorite absolue, quorum, scrutin secret) have (?) ag-popover click popovers"
  artifacts:
    - path: "public/dashboard.htmx.html"
      provides: "Help panel popover replacing btnTour"
      contains: "ag-popover"
    - path: "public/operator.htmx.html"
      provides: "Help panel + disabled button tooltips"
      contains: "ag-tooltip"
    - path: "public/postsession.htmx.html"
      provides: "Help panel + disabled button tooltips"
      contains: "ag-tooltip"
  key_links:
    - from: "8 .htmx.html pages"
      to: "ag-popover component"
      via: "HTML markup replacing btnTour"
      pattern: "ag-popover.*trigger.*click"
    - from: "operator.htmx.html"
      to: "ag-tooltip"
      via: "wrapping disabled buttons"
      pattern: "ag-tooltip.*Disponible"
    - from: "operator-tabs.js"
      to: "ag-tooltip text attribute"
      via: "clearing tooltip on enable"
      pattern: "closest.*ag-tooltip.*setAttribute.*text"
---

<objective>
Replace all 8 btnTour buttons with contextual help panel popovers, wrap all disabled primary buttons in ag-tooltip with "Disponible apres..." explanations, and add (?) ag-popover hints on technical voting terms.

Purpose: GUX-04 replaces sequential tours with contextual help; GUX-03 makes disabled buttons self-explanatory; GUX-05/GUX-07 adds inline term definitions; GUX-08 ensures help panels use localStorage for dismissal state.
Output: 8 pages updated with help popovers, disabled button tooltips on operator/postsession/members, technical term popovers where applicable.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-guided-ux-components/26-CONTEXT.md
@.planning/phases/26-guided-ux-components/26-RESEARCH.md

<interfaces>
From public/assets/js/components/ag-popover.js:
```javascript
// Attributes: title, content, position (top|bottom|left|right), trigger (hover|click|focus), width
// Slots: trigger (button), content (rich HTML)
// Default trigger: renders a "?" button if no slot="trigger" provided
// trigger="click" mode: toggle on click, close on click-outside or Escape
```

From public/assets/js/components/ag-tooltip.js:
```javascript
// Attributes: text, position (top|bottom)
// Wrap any element: <ag-tooltip text="..."><button>...</button></ag-tooltip>
// CSS-only show on :host(:hover) and :host(:focus-within)
// _esc() escapes HTML entities
```

btnTour pattern (same on all 8 pages):
```html
<button class="tour-trigger-btn" id="btnTour" aria-label="Lancer la visite guidee">
  <svg class="icon icon-xs" aria-hidden="true"><use href="/assets/icons.svg#icon-help-circle"></use></svg>
  Visite guidee  <!-- text varies: "Visite guidee", "Guide", "Visite" -->
</button>
```

Disabled buttons to wrap (from Research):
- operator.htmx.html: #btnPrimary (disabled by updatePrimaryButton() when score < 3)
- postsession.htmx.html: #btnSuivant, #btnValidate, #btnReject
- members.htmx.html: #btnImport (disabled until CSV file selected)
- operator.htmx.html: #hubSendConvocation, #hubSend2ndConvocation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace btnTour with help panel popovers on all 8 pages</name>
  <files>
    public/dashboard.htmx.html
    public/hub.htmx.html
    public/wizard.htmx.html
    public/operator.htmx.html
    public/members.htmx.html
    public/postsession.htmx.html
    public/analytics.htmx.html
    public/meetings.htmx.html
  </files>
  <read_first>
    public/dashboard.htmx.html
    public/hub.htmx.html
    public/wizard.htmx.html
    public/operator.htmx.html
    public/members.htmx.html
    public/postsession.htmx.html
    public/analytics.htmx.html
    public/meetings.htmx.html
    public/assets/js/components/ag-popover.js
  </read_first>
  <action>
On each of the 8 pages, find the `<button class="tour-trigger-btn" id="btnTour" ...>` element and replace it with an `<ag-popover>` help panel.

**Replacement pattern (same structure on all 8 pages, only the content varies):**
```html
<ag-popover trigger="click" position="bottom" width="320">
  <button slot="trigger" class="tour-trigger-btn" aria-label="Aide sur cette page">
    <svg class="icon icon-xs" aria-hidden="true"><use href="/assets/icons.svg#icon-help-circle"></use></svg>
    Aide
  </button>
  <div slot="content">
    <strong>[Page Title] &mdash; Comment &ccedil;a marche</strong>
    <ul style="margin:.5rem 0 0;padding-left:1.25rem;font-size:.8125rem;color:var(--color-text-muted);line-height:1.6">
      <li>[Tip 1]</li>
      <li>[Tip 2]</li>
      <li>[Tip 3]</li>
    </ul>
  </div>
</ag-popover>
```

**Per-page help panel content:**

**1. dashboard.htmx.html:**
Title: "Tableau de bord"
Tips:
- Cliquez sur une s&eacute;ance pour ouvrir sa fiche
- Le bouton d'action change selon l'&eacute;tat de la s&eacute;ance
- Cr&eacute;ez une nouvelle s&eacute;ance avec le bouton en haut &agrave; droite

**2. meetings.htmx.html:**
Title: "S&eacute;ances"
Tips:
- Filtrez par &eacute;tat avec les onglets
- Cliquez sur une s&eacute;ance pour ouvrir sa fiche hub
- Utilisez la barre de recherche pour retrouver une s&eacute;ance par nom

**3. hub.htmx.html:**
Title: "Fiche s&eacute;ance"
Tips:
- Suivez les &eacute;tapes dans l'ordre
- Chaque &eacute;tape d&eacute;bloque la suivante
- La console op&eacute;rateur s'ouvre depuis l'&eacute;tape Pr&eacute;sences

**4. wizard.htmx.html:**
Title: "Assistant de cr&eacute;ation"
Tips:
- Compl&eacute;tez les 4 &eacute;tapes dans l'ordre
- &Eacute;tapes 2 et 3 sont optionnelles &mdash; vous pouvez les compl&eacute;ter depuis la fiche s&eacute;ance
- Vos donn&eacute;es sont sauvegard&eacute;es &agrave; chaque &eacute;tape

**5. operator.htmx.html:**
Title: "Console op&eacute;rateur"
Tips:
- Mode Pr&eacute;paration : g&eacute;rez membres et r&eacute;solutions
- Mode Ex&eacute;cution : pilotez les votes en direct
- Le bouton principal change selon l'&eacute;tat de la s&eacute;ance

**6. members.htmx.html:**
Title: "Membres"
Tips:
- Importez un CSV pour ajouter des membres en masse
- Chaque membre re&ccedil;oit un lien de vote unique
- La colonne Poids d&eacute;finit le droit de vote pond&eacute;r&eacute;

**7. postsession.htmx.html:**
Title: "Post-session"
Tips:
- Validez les r&eacute;sultats avant de g&eacute;n&eacute;rer le PV
- La g&eacute;n&eacute;ration PDF prend quelques secondes
- Le PV archiv&eacute; est accessible depuis la page Archives

**8. analytics.htmx.html:**
Title: "Statistiques"
Tips:
- Les statistiques sont calcul&eacute;es sur les s&eacute;ances cl&ocirc;tur&eacute;es
- Exportez en CSV pour vos archives
- Filtrez par p&eacute;riode pour comparer les s&eacute;ances

**IMPORTANT:** Remove the `id="btnTour"` from the button since it is now inside an ag-popover slot. The old ID is not referenced by any JS (the tour system is being removed).

**Do NOT delete** the tour CSS in design-system.css (lines 3741-3940). Leave it as dead code per Research anti-pattern guidance.

**Ensure ag-popover.js is loaded** on each page. Check if shell.js or a shared include already loads it. If not, add `<script src="/assets/js/components/ag-popover.js"></script>` before the page script on each page.

**GUX-08 localStorage dismissal:** Since help panels are click-triggered (user opens them on demand), localStorage dismissal is NOT needed for the help panels — they don't auto-show. The GUX-08 requirement is satisfied by the fact that help panels are user-initiated and close on click-outside. If any page has a proactive hint banner, THAT would need localStorage — but this phase has none.
  </action>
  <verify>
    <automated>grep -l "ag-popover.*trigger.*click" /home/user/gestion_votes_php/public/dashboard.htmx.html /home/user/gestion_votes_php/public/hub.htmx.html /home/user/gestion_votes_php/public/wizard.htmx.html /home/user/gestion_votes_php/public/operator.htmx.html /home/user/gestion_votes_php/public/members.htmx.html /home/user/gestion_votes_php/public/postsession.htmx.html /home/user/gestion_votes_php/public/analytics.htmx.html /home/user/gestion_votes_php/public/meetings.htmx.html 2>/dev/null | wc -l | xargs test 8 -eq && echo "PASS (all 8 pages)" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 pages contain `<ag-popover trigger="click"` replacing the old btnTour button
    - Button text reads "Aide" (not "Visite guidee" or "Guide")
    - Each page has unique tip content in the popover (not generic)
    - No `id="btnTour"` remains on any of the 8 pages
    - The `tour-trigger-btn` CSS class is preserved on the button (for existing styling)
    - ag-popover.js component is loaded on each page (via shared include or individual script tag)
    - Tour CSS in design-system.css is NOT deleted
  </acceptance_criteria>
  <done>All 8 btnTour buttons replaced with contextual help panel ag-popovers showing page-relevant tips</done>
</task>

<task type="auto">
  <name>Task 2: Add disabled button tooltips and technical term popovers</name>
  <files>
    public/operator.htmx.html
    public/postsession.htmx.html
    public/members.htmx.html
    public/assets/js/pages/operator-tabs.js
  </files>
  <read_first>
    public/operator.htmx.html
    public/postsession.htmx.html
    public/members.htmx.html
    public/assets/js/pages/operator-tabs.js
    public/assets/js/components/ag-tooltip.js
    public/assets/js/components/ag-popover.js
  </read_first>
  <action>
**Part A: Disabled button tooltips (GUX-03)**

Wrap each identified disabled button in an `<ag-tooltip>` element. The tooltip shows WHY the button is locked. When JS enables the button, clear the tooltip text.

**operator.htmx.html:**
1. Find `#btnPrimary` (the main action button — "Figer la seance" / "Ouvrir la seance" etc.). Wrap it:
```html
<ag-tooltip text="Disponible apr&egrave;s ajout des membres, enregistrement des pr&eacute;sences et configuration du vote" position="bottom">
  <button class="btn btn-sm btn-primary" id="btnPrimary" disabled>...</button>
</ag-tooltip>
```

2. Find `#hubSendConvocation` — Wrap it:
```html
<ag-tooltip text="Disponible apr&egrave;s compl&eacute;tion de la fiche s&eacute;ance" position="bottom">
  <button ... id="hubSendConvocation" disabled>...</button>
</ag-tooltip>
```

3. Find `#hubSend2ndConvocation` — Wrap it:
```html
<ag-tooltip text="Disponible apr&egrave;s envoi de la premi&egrave;re convocation" position="bottom">
  <button ... id="hubSend2ndConvocation" disabled>...</button>
</ag-tooltip>
```

**postsession.htmx.html:**
1. Find `#btnSuivant` — Wrap it:
```html
<ag-tooltip text="Disponible apr&egrave;s validation des r&eacute;sultats" position="bottom">
  <button ... id="btnSuivant" disabled>...</button>
</ag-tooltip>
```

2. Find `#btnValidate` — Wrap it:
```html
<ag-tooltip text="Disponible apr&egrave;s v&eacute;rification de tous les r&eacute;sultats" position="bottom">
  <button ... id="btnValidate" disabled>...</button>
</ag-tooltip>
```

3. Find `#btnReject` — Wrap it:
```html
<ag-tooltip text="Disponible apr&egrave;s v&eacute;rification de tous les r&eacute;sultats" position="bottom">
  <button ... id="btnReject" disabled>...</button>
</ag-tooltip>
```

**members.htmx.html:**
1. Find `#btnImport` — Wrap it:
```html
<ag-tooltip text="Disponible apr&egrave;s s&eacute;lection d'un fichier CSV" position="bottom">
  <button ... id="btnImport" disabled>...</button>
</ag-tooltip>
```

**Ensure ag-tooltip.js is loaded** on each page. Check existing script tags.

**Part B: Update operator-tabs.js to clear tooltip on enable (GUX-03 pitfall prevention)**

In operator-tabs.js, find the `updatePrimaryButton()` function (around line 2126-2192). Wherever it sets `btnPrimary.disabled = false`, also clear the parent ag-tooltip:
```javascript
var tipEl = btnPrimary.closest('ag-tooltip');
if (tipEl) tipEl.setAttribute('text', '');
```

And wherever it sets `btnPrimary.disabled = true`, restore the tooltip text:
```javascript
var tipEl = btnPrimary.closest('ag-tooltip');
if (tipEl) tipEl.setAttribute('text', 'Disponible apr\u00e8s ajout des membres, enregistrement des pr\u00e9sences et configuration du vote');
```

Similarly for `#hubSendConvocation` and `#hubSend2ndConvocation` — find the JS that enables/disables them and add the same `closest('ag-tooltip')` pattern.

**Part C: Technical term popovers (GUX-05, GUX-07)**

Add `<ag-popover>` inline hints next to technical voting terms. Use the default trigger (renders a "?" button automatically — no slot="trigger" needed).

These go in the HTML pages where the terms appear:
- **operator.htmx.html** — next to "Quorum" label if present:
  `<ag-popover title="Quorum" content="Nombre minimum de participants requis pour que les votes soient valides." position="top" trigger="click"></ag-popover>`

- **hub.htmx.html** — next to "Quorum" or "Majorite" labels if present:
  `<ag-popover title="Majorit&eacute; absolue" content="Plus de la moiti&eacute; des votes exprim&eacute;s. Les abstentions ne sont pas compt&eacute;es." position="top" trigger="click"></ag-popover>`

- **wizard.htmx.html** — next to "Scrutin secret" option if present:
  `<ag-popover title="Scrutin secret" content="Les bulletins de vote sont anonymes. Le r&eacute;sultat individuel n'est pas consultable." position="top" trigger="click"></ag-popover>`

**IMPORTANT:** Only add term popovers where the terms ACTUALLY appear in the page markup. Read each page first. If a term does not appear, do NOT force it in. Use sparingly per CONTEXT.md locked decision.

**Part D: operator-tabs.js empty state migration**

While editing operator-tabs.js, also replace any `Shared.emptyState()` calls in div contexts (not table cells) with `<ag-empty-state>` tags. Use:
`<ag-empty-state icon="generic" title="Aucun \u00e9l\u00e9ment" description="..."></ag-empty-state>`
with appropriate French content for the context.
  </action>
  <verify>
    <automated>grep -q "ag-tooltip.*Disponible" /home/user/gestion_votes_php/public/operator.htmx.html && grep -q "ag-tooltip.*Disponible" /home/user/gestion_votes_php/public/postsession.htmx.html && grep -q "ag-tooltip.*Disponible" /home/user/gestion_votes_php/public/members.htmx.html && grep -q "closest.*ag-tooltip" /home/user/gestion_votes_php/public/assets/js/pages/operator-tabs.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - operator.htmx.html has ag-tooltip wrapping #btnPrimary with "Disponible apres" text
    - operator.htmx.html has ag-tooltip wrapping #hubSendConvocation with "Disponible apres" text
    - operator.htmx.html has ag-tooltip wrapping #hubSend2ndConvocation with "Disponible apres" text
    - postsession.htmx.html has ag-tooltip wrapping #btnSuivant, #btnValidate, #btnReject
    - members.htmx.html has ag-tooltip wrapping #btnImport
    - operator-tabs.js clears ag-tooltip text when enabling buttons via `closest('ag-tooltip').setAttribute('text', '')`
    - operator-tabs.js restores ag-tooltip text when disabling buttons
    - At least 2 technical term ag-popover hints exist across the pages (quorum, majorite absolue, scrutin secret)
    - ag-tooltip.js component is loaded on each modified page
    - operator-tabs.js empty state calls in div contexts use ag-empty-state
  </acceptance_criteria>
  <done>All disabled primary buttons have ag-tooltip explanations, tooltip text syncs with enabled/disabled state, technical terms have (?) popover definitions, operator empty states migrated</done>
</task>

</tasks>

<verification>
- All 8 pages: `grep -c "ag-popover.*trigger.*click" public/*.htmx.html` shows 8 matches
- Disabled buttons: `grep -c "ag-tooltip.*Disponible" public/operator.htmx.html public/postsession.htmx.html public/members.htmx.html` shows 7 total
- Tooltip sync: `grep -c "closest.*ag-tooltip" public/assets/js/pages/operator-tabs.js` shows at least 2
- No btnTour: `grep -c "id=\"btnTour\"" public/*.htmx.html` shows 0
- Open operator page, hover over disabled Figer button — tooltip appears
- Click Aide button on any page — popover opens with relevant tips
</verification>

<success_criteria>
- Every page's help button reads "Aide" and opens a click-triggered popover with page-relevant tips
- No sequential tour or Driver.js remains (per CONTEXT.md locked decision)
- Every disabled primary action button shows "Disponible apres [condition]" on hover
- Tooltip text clears when button becomes enabled (no stale tooltip on active buttons)
- Technical terms have (?) click popovers with clear French definitions
- GUX-08 satisfied: help panels are user-initiated (no auto-show), no localStorage needed
</success_criteria>

<output>
After completion, create `.planning/phases/26-guided-ux-components/26-03-SUMMARY.md`
</output>
