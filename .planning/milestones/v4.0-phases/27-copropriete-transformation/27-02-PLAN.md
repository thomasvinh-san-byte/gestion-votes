---
phase: 27-copropriete-transformation
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - database/seeds/01_minimal.sql
  - database/seeds/03_demo.sql
  - database/seeds/06_test_weighted.sql
  - database/seeds/08_demo_az.sql
  - docs/FAQ.md
  - docs/GUIDE_FONCTIONNEL.md
  - docs/directive-projet.md
  - docs/wireframe/ag_vote_v3_19_2.html
autonomous: true
requirements: [CPR-01, CPR-04]

must_haves:
  truths:
    - "Seed data comments use generic terminology (no copropriete-specific language in comments)"
    - "Documentation files use generic AG terminology instead of copropriete-specific language"
    - "Wireframe file uses generic vocabulary"
    - "Full PHPUnit test suite passes after all changes"
    - "Full grep audit across entire codebase confirms only backend-preserved aliases remain"
  artifacts:
    - path: "database/seeds/06_test_weighted.sql"
      provides: "Weighted test seed with generic comment"
    - path: "docs/directive-projet.md"
      provides: "Project directive with generic vocabulary"
    - path: "docs/FAQ.md"
      provides: "FAQ with generic vocabulary"
  key_links:
    - from: "database/seeds/03_demo.sql"
      to: "database/seeds/01_minimal.sql"
      via: "Both use consistent terminology in comments"
      pattern: "poids de vote|ponderation"
---

<objective>
Update seed data comments and documentation files to use generic AG vocabulary, completing the copropriete-to-generic transformation across the entire codebase. Run final verification audit.

Purpose: Ensure consistency across all project files — not just UI but also developer-facing docs and seed data.
Output: 8 modified files (4 seed SQL, 4 docs) with consistent generic terminology. Full codebase audit confirming transformation is complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-copropriete-transformation/27-CONTEXT.md
@.planning/phases/27-copropriete-transformation/27-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update seed data comments and documentation vocabulary</name>
  <files>
    database/seeds/01_minimal.sql,
    database/seeds/03_demo.sql,
    database/seeds/06_test_weighted.sql,
    database/seeds/08_demo_az.sql,
    docs/FAQ.md,
    docs/GUIDE_FONCTIONNEL.md,
    docs/directive-projet.md,
    docs/wireframe/ag_vote_v3_19_2.html
  </files>
  <read_first>
    - database/seeds/06_test_weighted.sql (line 1 — header comment)
    - database/seeds/03_demo.sql (lines 107-118 — LOT- member references, lines 142, 238, 254 — copro comments)
    - database/seeds/08_demo_az.sql (lines 104, 223 — tantiemes comments)
    - database/seeds/01_minimal.sql (line 25 — policy name)
    - docs/FAQ.md (line 6 — copropriete mention)
    - docs/GUIDE_FONCTIONNEL.md (line 238 — copropriete mention)
    - docs/directive-projet.md (lines 38, 58-60, 586 — copropriete mentions)
    - docs/wireframe/ag_vote_v3_19_2.html (search for copropri and tantiem occurrences)
  </read_first>
  <action>
**A. database/seeds/06_test_weighted.sql**

Line 1: Update header comment:
`-- Dataset PONDÉRÉ : Copro 100 membres avec tantièmes` ->
`-- Dataset PONDÉRÉ : 100 membres avec poids de vote`

**B. database/seeds/03_demo.sql**

1. Lines 107-118: The member_reference values like 'LOT-001' through 'LOT-012' are DATA values in SQL INSERT statements, not UI labels. These are stored in the database `member_reference` column. PRESERVE them as-is — they represent historical imported data that existing databases may contain. Changing seed data values would break existing test expectations.

2. Line 142: Update comment:
`-- LOT-011 (Thomas) et LOT-012 (Lambert) sont absents` ->
`-- Thomas et Lambert sont absents`

3. Line 238: Update motion description string:
`'Vote sur la resolution de changement de syndic de copropriete.'` ->
`'Vote sur la resolution de changement de syndic.'`

4. Line 254: Update motion description string:
`'Discussion et vote sur les questions diverses soulevees par les coproprietaires.'` ->
`'Discussion et vote sur les questions diverses soulevees par les membres.'`

**C. database/seeds/08_demo_az.sql**

1. Line 104: Update comment:
`-- 10 Membres votants (poids varies : total = 1000 tantiemes)` ->
`-- 10 Membres votants (poids varies : total = 1000 poids de vote)`

2. Line 223: Update comment:
`-- 10 MEMBRES (total 1000 tantiemes) :` ->
`-- 10 MEMBRES (total 1000 poids de vote) :`

**D. database/seeds/01_minimal.sql**

Line 25: The policy NAME 'Quorum 50% (pondéré)' is already generic — it says "pondéré" not "tantièmes". No change needed. Verify and skip.

**E. docs/FAQ.md**

Line 6: Replace `reunions de copropriete` with `reunions d'organisation`:
`assemblees generales, conseils syndicaux, reunions de copropriete` ->
`assemblees generales, conseils syndicaux, reunions d'organisation`

**F. docs/GUIDE_FONCTIONNEL.md**

Line 238: Replace `assemblees de copropietaires ages` with `assemblees de membres ages`:
`bouton pour augmenter la taille de police (assemblees de copropietaires ages)` ->
`bouton pour augmenter la taille de police (assemblees de membres ages)`

**G. docs/directive-projet.md**

1. Line 38: Replace `vote electronique pour assemblees generales de copropriete` with `vote electronique pour assemblees generales`:
`Application de **vote electronique pour assemblees generales de copropriete**, conforme a la legislation francaise.` ->
`Application de **vote electronique pour assemblees generales**, conforme a la legislation francaise.`

2. Lines 58-60: Replace `copropriétaires` with `membres` in majority descriptions:
   - Line 58: `>50% des voix de tous les copropriétaires` -> `>50% des voix de tous les membres`
   - Line 59: `≥2/3 des voix de tous les copropriétaires` -> `≥2/3 des voix de tous les membres`
   - Line 60: `100% des voix de tous les copropriétaires` -> `100% des voix de tous les membres`

3. Line 586: Replace `Multi-coproprietes` with `Multi-organisations`:
   `Multi-coproprietes | Une seule entite visible | V1 mono-copropriete, preparer multi` ->
   `Multi-organisations | Une seule entite visible | V1 mono-organisation, preparer multi`

**H. docs/wireframe/ag_vote_v3_19_2.html**

This is the legacy wireframe file (retired as reference for v4.0 per STATE.md). Apply vocabulary updates for consistency:

1. Replace all instances of `copropriete` / `copropriétaires` with `organisation` / `membres` as appropriate
2. Replace `tantiemes` display labels with `poids de vote` (preserve in technical contexts like "tantiemes, etc." examples)
3. Replace `Copropriétaire A/B/C/D` with `Membre A/B/C/D`
4. Replace `AG Copro` with `AG` in notification text
5. Replace `copropriete` in quorum policy option with generic text
6. Replace `AG de copropriete` with `assemblees generales` in descriptions

Use careful search-and-replace. The wireframe is a JSX-like HTML file — do not break any JavaScript expressions or React-like syntax.

**IMPORTANT PRESERVATIONS (CPR-04):**
- DO NOT change LOT-001 through LOT-012 data values in seed SQL (these are member_reference column values)
- DO NOT touch app/Services/ImportService.php tantieme/tantiemes CSV aliases
- DO NOT touch any backend PHP service files
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && vendor/bin/phpunit --no-coverage && grep -rni "copropri" database/seeds/ docs/FAQ.md docs/GUIDE_FONCTIONNEL.md docs/directive-projet.md | grep -vi "LOT-" | grep -vi "member_reference" || echo "CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "copropri" database/seeds/06_test_weighted.sql returns empty
    - grep -n "copropri" database/seeds/03_demo.sql returns only LOT-xxx data values (member_reference column), no comments or motion descriptions
    - grep -n "tantiemes" database/seeds/08_demo_az.sql returns empty
    - grep -n "copropri" docs/FAQ.md returns empty
    - grep -n "copropri" docs/GUIDE_FONCTIONNEL.md returns empty
    - grep -n "copropri" docs/directive-projet.md returns empty
    - vendor/bin/phpunit --no-coverage passes (full suite, 0 failures)
  </acceptance_criteria>
  <done>All seed data comments and documentation files use generic AG terminology. No copropriete-specific language in comments or user-facing docs.</done>
</task>

<task type="auto">
  <name>Task 2: Full codebase audit and final regression verification</name>
  <files></files>
  <read_first>
    - app/Services/ImportService.php (line 237 — confirm tantieme alias preserved)
    - app/Services/BallotsService.php (lines 100-110 — confirm voting_power untouched)
  </read_first>
  <action>
Run a comprehensive codebase audit to confirm the transformation is complete and nothing was broken.

**Step 1: Full PHPUnit test suite**
```bash
vendor/bin/phpunit --no-coverage
```
Must pass with 0 failures, 0 errors.

**Step 2: Weighted vote regression test specifically**
```bash
vendor/bin/phpunit tests/Unit/WeightedVoteRegressionTest.php --no-coverage -v
```
Must show 3 tests, 3 assertions, 0 failures.

**Step 3: ESLint check**
```bash
npm run lint:ci
```
Must not introduce new errors beyond the existing baseline.

**Step 4: Full codebase vocabulary audit**
Run grep across ALL project files (excluding vendor/, node_modules/, .git/):
```bash
grep -rni "copropri" --include="*.php" --include="*.js" --include="*.css" --include="*.html" --include="*.sql" --include="*.md" . | grep -v vendor/ | grep -v node_modules/ | grep -v ".git/" | grep -v ".planning/"
```

The ONLY acceptable remaining matches are:
- `app/Services/ImportService.php` line ~237: tantieme/tantiemes CSV import aliases (CPR-04 preserve)
- `database/seeds/03_demo.sql` LOT-xxx member_reference data values (historical data)
- `docs/wireframe/` may retain minor instances in code-level comments if they reference legal articles

Any other match is a bug that must be fixed.

**Step 5: Dead code verification**
```bash
grep -rn "openKeyModal\|initDistributionKeys\|member-lot\|distributionKeysTable\|btnAddKey\|distributionKeysBody" public/
```
Must return zero matches.

**Step 6: Preserved values verification**
```bash
grep -n 'value="tantiemes"' public/settings.htmx.html
```
Must return exactly one match (the select option value is preserved).

```bash
grep -n "tantieme\|tantiemes" app/Services/ImportService.php
```
Must return matches (the CSV aliases are preserved).

If any audit step fails, fix the issue before completing this task.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && vendor/bin/phpunit --no-coverage && grep -rni "copropri" --include="*.php" --include="*.js" --include="*.css" --include="*.html" --include="*.md" . | grep -v vendor/ | grep -v node_modules/ | grep -v ".git/" | grep -v ".planning/" | grep -v "ImportService.php" | grep -v "LOT-" | grep -v "docs/wireframe/" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - vendor/bin/phpunit --no-coverage exits with code 0
    - vendor/bin/phpunit tests/Unit/WeightedVoteRegressionTest.php --no-coverage shows 3 tests, 0 failures
    - grep -rn "openKeyModal\|initDistributionKeys\|member-lot\|distributionKeysTable\|btnAddKey" public/ returns empty
    - grep -n 'value="tantiemes"' public/settings.htmx.html returns exactly 1 match
    - grep -n "tantieme" app/Services/ImportService.php returns at least 1 match (preserved)
    - The full codebase audit grep (excluding ImportService.php, LOT- data, wireframe, .planning/) returns 0 matches for "copropri"
  </acceptance_criteria>
  <done>Full codebase audit confirms: zero user-facing copropriete vocabulary, all backend aliases preserved, all tests passing, no dead code remaining. Phase 27 transformation is complete.</done>
</task>

</tasks>

<verification>
1. Full PHPUnit suite: `vendor/bin/phpunit --no-coverage` — all tests pass
2. Weighted vote regression: `vendor/bin/phpunit tests/Unit/WeightedVoteRegressionTest.php --no-coverage` — 3/3 pass
3. ESLint: `npm run lint:ci` — no new errors
4. Vocabulary audit: `grep -rni "copropri" --include="*.php" --include="*.js" --include="*.css" --include="*.html" --include="*.md" . | grep -v vendor/ | grep -v node_modules/ | grep -v ".git/" | grep -v ".planning/" | grep -v "ImportService.php" | grep -v "LOT-"` — only wireframe remnants acceptable
5. Dead code audit: `grep -rn "openKeyModal\|initDistributionKeys\|member-lot\|distributionKeysTable\|btnAddKey" public/` — zero matches
6. Preserved aliases: `grep "tantieme" app/Services/ImportService.php` — matches exist (preserved per CPR-04)
</verification>

<success_criteria>
- All seed data comments use generic terminology
- All documentation files use generic terminology
- Full PHPUnit test suite passes with 0 failures
- Codebase audit confirms transformation complete (only preserved backend aliases remain)
- No dead code references remain in public/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/27-copropriete-transformation/27-02-SUMMARY.md`
</output>
