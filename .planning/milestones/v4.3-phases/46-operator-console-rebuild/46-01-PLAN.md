---
phase: 46-operator-console-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/operator.htmx.html
  - public/assets/css/operator.css
autonomous: true
requirements: [REB-04]

must_haves:
  truths:
    - "Operator page loads with two-panel layout: 280px agenda sidebar + fluid main content"
    - "Meeting bar displays meeting selector, status badge, SSE indicator, clock, and refresh button in a single compact row"
    - "Tab navigation bar shows all 7 tabs (4 prep + separator + 3 live) with icons and count badges"
    - "Vote panel card exists with large Pour/Contre/Abstention counters, progress bars, and delta badge placeholder"
    - "Agenda sidebar has container for motion list with proper structure for JS rendering"
    - "All ~80 DOM IDs referenced by the 6 JS modules exist in the new HTML"
    - "Dark mode renders correctly via CSS design tokens"
  artifacts:
    - path: "public/operator.htmx.html"
      provides: "Complete operator console HTML with two-panel layout, inlined exec content (no partials)"
      contains: "op-body"
    - path: "public/assets/css/operator.css"
      provides: "All operator-specific styles: two-panel grid, meeting bar, vote card, agenda sidebar, KPI strip, tabs"
      contains: "op-vote-card"
  key_links:
    - from: "public/operator.htmx.html"
      to: "public/assets/css/operator.css"
      via: "class names on HTML elements"
      pattern: "op-meeting-bar|op-body|op-sidebar|op-vote-card"
    - from: "public/operator.htmx.html"
      to: "public/assets/js/pages/operator-tabs.js"
      via: "DOM IDs preserved for JS modules"
      pattern: "id=\"meetingSelect\"|id=\"opSseIndicator\"|id=\"tabsNav\""
---

<objective>
Complete ground-up rewrite of operator console HTML and CSS. New two-panel split layout with 280px agenda sidebar + fluid main content. Meeting bar compact at top. All exec partial content inlined (no lazy-loading). Every DOM ID that the 6 JS modules reference is preserved in the new structure.

Purpose: Replace the current 1072-line HTML and 4679-line CSS with a clean, mission-control-quality operator console following the locked design decisions.
Output: New operator.htmx.html and operator.css ready for JS selector updates in Plan 02.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-operator-console-rebuild/46-CONTEXT.md
@.planning/phases/46-operator-console-rebuild/46-RESEARCH.md

<interfaces>
<!-- Critical DOM IDs that MUST exist in new HTML — JS modules depend on these -->

From operator-tabs.js (window.OpS bridge + tab management):
- meetingSelect, meetingStatusBadge, meetingBarActions, tabsNav, noMeetingState, srAnnounce
- tabCountResolutions, tabCountPresences, tabCountAlerts, tabSeparator
- healthChip, healthScore, healthHint, contextHint
- btnModeSetup, btnModeExec, btnPrimary, modeSwitch, modeIndicator
- lifecycleBar, btnProjector, viewSetup, viewExec, viewExecContent
- liveTabs (container with data-partial and data-loaded — BUT rebuild inlines content, so remove partial loading)
- advancedMode, hubLayout, onboardingBanner, onboardingDismiss
- tab-seance, tab-participants, tab-ordre-du-jour, tab-controle, tab-parole, tab-vote, tab-resultats

From operator-exec.js (vote lifecycle + KPIs):
- opAgendaList, execActiveVote, execNoVote, execVoteTitle
- execVoteFor, execVoteAgainst, execVoteAbstain
- execVoteParticipationBar, execVoteParticipationPct
- opBarFor, opBarAgainst, opBarAbstain, opPctFor, opPctAgainst, opPctAbstain
- opVoteDeltaBadge, opKpiPresent, opKpiQuorum, opKpiQuorumCheck, opKpiVoted, opKpiResolution
- opResTitle, opResTags, opResLiveDot
- opBtnProclaim, opBtnToggleVote, opBtnCloseSession, opBtnEndSession, execBtnCloseSession, execBtnCloseVote
- opBtnNextVote, opTransitionCard, opTransitionText
- opPostVoteGuidance, opEndOfAgenda
- opQuorumOverlay, opQuorumStats, opQuorumRiskNote, opQuorumContinuer, opQuorumReporter, opQuorumSuspendre
- opResolutionProgress, opExecTitle, opExecTimer, opActionBar
- opSubTabs, opPanelResultat, execLiveBadge
- execParticipation, execMotionsDone, execMotionsTotal
- execManualVoteList, execManualSearch
- execSpeakerInfo, execSpeechActions, execSpeechQueue, execSpeakerTimer
- devOnline, devStale, execDevOnline, execDevStale, execQuickOpenList
- noActiveVote, activeVotePanel, activeVoteTitle

From operator-realtime.js (SSE):
- opSseIndicator (with data-sse-state attr), opSseLabel

From operator-attendance.js:
- attendanceGrid, presenceSearch, presenceToolbar
- membersCount, presStatPresent, presStatRemote, proxyStatActive, presStatAbsent
- btnAddMember, btnMarkAllPresent, btnImportCSV, btnImportAttendanceCSV
- proxyList, proxySearch, btnAddProxy, btnImportProxiesCSV
- noMembersGuide, membersList

From operator-motions.js:
- Various IDs within tab-ordre-du-jour

From operator-speech.js:
- execSpeakerInfo, execSpeechActions, execSpeechQueue, execSpeakerTimer

From operator-exec.html partial (to be inlined):
- opExecStatusBar, opStatusTitle, opSseIndicatorBar, opStatusMembers, opStatusElapsed
- opKpiStrip, opTags, opTagQuorum, opTagNoQuorum, opTagCorrespondance, opTagProcurations, opProcCount
- opResolutionProgress, opGuidance

Layout CSS pattern (from existing operator.css):
```css
[data-page-role="operator"] .app-shell { display: grid; }
[data-page-role="operator"] .op-body { display: grid; grid-template-columns: 280px 1fr; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite operator.htmx.html with two-panel layout and inlined exec content</name>
  <files>public/operator.htmx.html</files>
  <read_first>
    public/operator.htmx.html
    public/partials/operator-exec.html
    public/partials/operator-live-tabs.html
    public/assets/js/pages/operator-tabs.js
    public/assets/js/pages/operator-exec.js
    public/assets/js/pages/operator-realtime.js
    public/assets/js/pages/operator-motions.js
    public/assets/js/pages/operator-attendance.js
    public/assets/js/pages/operator-speech.js
  </read_first>
  <action>
    Complete ground-up rewrite of operator.htmx.html. The new structure follows the locked decisions from CONTEXT.md.

    **CRITICAL FIRST STEP:** Before writing ANY HTML, grep all 6 JS files for `getElementById`, `querySelector`, `id="`, and `dataset.` to compile the FULL list of DOM IDs and data attributes that JS depends on. Every single one MUST exist in the new HTML.

    **New HTML structure:**
    1. Same head section (critical-tokens, fonts, app.css, operator.css)
    2. Skip links preserved
    3. `<div class="app-shell">` with `data-page-role="operator"` on `<html>`
    4. `<aside class="app-sidebar" data-include-sidebar data-page="operator">` — preserved as-is
    5. **Meeting bar** (`<header class="op-meeting-bar" id="opMeetingBar">`):
       - Left: meeting selector (#meetingSelect) + status badge (#meetingStatusBadge) + role badge (#userRoleBadge) + health chip (#healthChip, #healthScore, #healthHint)
       - Center: tab navigation (#tabsNav) — move the tab nav INTO the meeting bar center area for compact layout
       - Right: SSE indicator (#opSseIndicator with data-sse-state="offline", #opSseLabel) + clock (#barClock) + refresh button (#btnBarRefresh) + help popover
       - Second row: meeting bar actions (#meetingBarActions) with context hint (#contextHint), mode switch (#modeSwitch with #btnModeSetup/#btnModeExec — keep in DOM even if visually de-emphasized), primary button (#btnPrimary wrapped in ag-tooltip), projector link (#btnProjector)
    6. Mode indicator (#modeIndicator) — preserved
    7. Lifecycle bar (#lifecycleBar) — preserved with all lifecycle steps
    8. **Two-panel body** (`<div class="op-body">`):
       - `<aside class="op-sidebar" id="opSidebar">`: agenda heading + empty text (#opAgendaEmpty) + ordered list (#opAgendaList)
       - `<main class="op-main" id="opMain">`: contains all content below
    9. Inside op-main: SR announce (#srAnnounce), no-meeting state (#noMeetingState)
    10. **viewSetup section** (#viewSetup) — preserve ALL existing content:
        - Onboarding banner (#onboardingBanner)
        - Hub layout (#hubLayout) with stepper, identity, action card, checklist, accordion — ALL IDs preserved
        - Advanced mode (#advancedMode) with ALL 7 tab panels:
          - tab-seance: all form fields, settings grid, policies, roles — ALL IDs preserved
          - tab-participants: stats cards, members list, attendance grid, proxy list — ALL IDs preserved
          - tab-ordre-du-jour: motion CRUD — ALL IDs preserved
          - tab-controle: alerts and device management — ALL IDs preserved
          - tab-parole: INLINE content (was lazy-loaded) — speech queue IDs
          - tab-vote: INLINE content (was lazy-loaded) — vote panel IDs
          - tab-resultats: INLINE content (was lazy-loaded) — results IDs
    11. **viewExec section** (#viewExec) — INLINE the content from operator-exec.html partial:
        - Exec header (#opExecHeader with #opExecTitle, #opExecTimer, #opBtnRoomDisplay, #opBtnCloseSession)
        - INLINE #viewExecContent directly (remove data-partial="/partials/operator-exec.html"):
          - Exec status bar (#opExecStatusBar)
          - KPI strip (#opKpiStrip with all KPI IDs)
          - Tags (#opTags)
          - Resolution progress (#opResolutionProgress)
          - Guidance bar (#opGuidance)
          - Resolution detail panel (opResTitle, opResTags, opResLiveDot)
          - Action bar (#opActionBar with opBtnToggleVote, opBtnProclaim, etc.)
          - Sub-tabs (#opSubTabs with data-op-tab tabs and opPanel* panels)
          - Active vote display (execActiveVote, vote counters, progress bars)
          - Post-vote guidance (#opPostVoteGuidance, #opEndOfAgenda)
          - Manual vote section (execManualVoteList, execManualSearch)
          - Quick open list (#execQuickOpenList)
        - Transition card (#opTransitionCard, #opTransitionText)
    12. Quorum overlay (#opQuorumOverlay) — preserved with all action buttons
    13. Drawer, launch modal, tour overlay — ALL preserved as-is
    14. Same script tags at bottom in same order

    **Vote panel visual centerpiece (new):**
    - Inside viewExec, add `class="op-vote-card op-vote-card--active"` wrapper around the active vote display
    - Large counters for Pour (#execVoteFor) / Contre (#execVoteAgainst) / Abstention (#execVoteAbstain)
    - Progress bars (#opBarFor, #opBarAgainst, #opBarAbstain) with percentage labels
    - Delta badge (#opVoteDeltaBadge) prominently placed
    - Open/close vote buttons prominently placed in this card

    **ag-tooltip wrapping on all disabled action buttons:**
    - #btnPrimary: "Disponible apres ajout des membres, enregistrement des presences et configuration du vote"
    - #opBtnCloseSession: wrap if it can be disabled
    - #opBtnToggleVote: wrap with contextual tooltip
    - #opBtnProclaim: wrap with contextual tooltip
    - Other action buttons as appropriate

    **Responsive:** Add `data-page-role="operator"` on html element (already exists). Sidebar collapses at 1024px via CSS (handled in Task 2).

    **What NOT to change:**
    - Do NOT modify any JS files
    - Do NOT modify any PHP/API files
    - Do NOT remove any DOM ID that exists in the current HTML or partials
    - Keep all data-tour attributes for the guided tour
    - Keep all aria-* attributes for accessibility
  </action>
  <verify>
    <automated>grep -c 'id="meetingSelect"' public/operator.htmx.html && grep -c 'id="opSseIndicator"' public/operator.htmx.html && grep -c 'id="opAgendaList"' public/operator.htmx.html && grep -c 'id="execVoteFor"' public/operator.htmx.html && grep -c 'id="opVoteDeltaBadge"' public/operator.htmx.html && grep -c 'id="opSubTabs"' public/operator.htmx.html && grep -c 'op-body' public/operator.htmx.html && grep -c 'op-sidebar' public/operator.htmx.html && grep -c 'op-vote-card' public/operator.htmx.html && echo "All critical IDs and classes present"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'id="meetingSelect"' public/operator.htmx.html returns match
    - grep 'id="opSseIndicator"' public/operator.htmx.html returns match
    - grep 'id="opAgendaList"' public/operator.htmx.html returns match
    - grep 'id="execVoteFor"' public/operator.htmx.html returns match
    - grep 'id="opVoteDeltaBadge"' public/operator.htmx.html returns match
    - grep 'id="opSubTabs"' public/operator.htmx.html returns match
    - grep 'id="opKpiPresent"' public/operator.htmx.html returns match
    - grep 'id="opQuorumOverlay"' public/operator.htmx.html returns match
    - grep 'class="op-body"' public/operator.htmx.html returns match
    - grep 'class="op-sidebar"' public/operator.htmx.html returns match
    - grep 'op-vote-card' public/operator.htmx.html returns match
    - grep 'data-partial' public/operator.htmx.html returns NO match (partials inlined)
    - All ~80 DOM IDs from research document exist in new HTML
    - No data-partial attributes remain (all content inlined)
  </acceptance_criteria>
  <done>operator.htmx.html is fully rewritten with two-panel layout, all exec content inlined, every JS-referenced DOM ID preserved, ag-tooltip on disabled buttons, vote card as visual centerpiece</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite operator.css for two-panel layout and refined components</name>
  <files>public/assets/css/operator.css</files>
  <read_first>
    public/assets/css/operator.css
    public/assets/css/app.css
    public/operator.htmx.html
  </read_first>
  <action>
    Complete ground-up rewrite of operator.css. The new file implements the locked design decisions from CONTEXT.md.

    **Layout foundation:**
    ```css
    /* Override app-shell flex with grid for operator page */
    [data-page-role="operator"] .app-shell {
      display: grid;
      grid-template-rows: auto auto auto 1fr; /* meeting-bar, meeting-bar-actions, lifecycle-bar, body */
      grid-template-columns: 1fr;
      height: 100vh;
      overflow: hidden;
    }

    /* Two-panel body */
    .op-body {
      display: grid;
      grid-template-columns: 280px 1fr;
      overflow: hidden;
      height: 100%;
    }
    ```

    **Meeting bar (compact, full-width):**
    - .op-meeting-bar: flex row, align-items center, padding 8px 16px, background var(--color-surface-raised), border-bottom
    - .op-meeting-bar-left: flex row, gap 8px, align-items center — selector + badge + health chip
    - .op-meeting-bar-right: flex row, gap 12px, align-items center, margin-left auto — SSE indicator + clock + refresh + help

    **SSE indicator (always visible, vivid colors):**
    - .op-sse-indicator: inline-flex, align-items center, gap 4px, font-size 0.75rem, font-weight 500
    - .op-sse-dot: 8px circle, border-radius 50%
    - [data-sse-state="live"] .op-sse-dot: background #22c55e (green), box-shadow 0 0 6px #22c55e
    - [data-sse-state="reconnecting"] .op-sse-dot: background #f59e0b (amber), animation pulse
    - [data-sse-state="offline"] .op-sse-dot: background #ef4444 (red)
    - Dark mode: same vivid colors (green/red stay vivid per CONTEXT decision)

    **Agenda sidebar (280px):**
    - .op-sidebar: width 280px, overflow-y auto, border-right 1px solid var(--color-border), padding var(--space-4), background var(--color-surface)
    - .op-agenda-list: list-style none, padding 0
    - .op-agenda-item: padding 10px 12px, border-radius var(--radius-md), cursor pointer, transition background 0.15s
    - .op-agenda-item:hover: background var(--color-surface-hover)
    - .op-agenda-item.current: background var(--color-primary-subtle), border-left 3px solid var(--color-primary)
    - .op-agenda-item.voted: opacity 0.6
    - .op-agenda-item .op-agenda-num: font-weight 600, margin-right 8px
    - .op-agenda-item .op-agenda-status-dot: 6px circle for status

    **Vote panel card (visual centerpiece):**
    - .op-vote-card: padding 24px, border-radius var(--radius-lg), background var(--color-surface), border 1px solid var(--color-border)
    - .op-vote-card--active: border-color var(--color-primary), box-shadow 0 0 0 2px var(--color-primary-subtle), transform-origin center
    - Vote counters: font-size 2.5rem, font-weight 700, font-family var(--font-mono), letter-spacing -0.02em
    - .op-vote-for: color #22c55e
    - .op-vote-against: color #ef4444
    - .op-vote-abstain: color #f59e0b
    - Progress bars: height 8px, border-radius 4px, background var(--color-surface-raised)
    - --bar-pct CSS custom property for width: var(--bar-pct, 0%)

    **Delta badge (animated):**
    - .op-vote-delta-badge: position absolute, top -8px, right -8px, background var(--color-primary), color white, font-size 0.7rem, font-weight 700, padding 2px 6px, border-radius 999px, animation deltaPopIn 0.3s ease-out
    - @keyframes deltaPopIn: from { transform: scale(0.5); opacity: 0; } to { transform: scale(1); opacity: 1; }

    **KPI strip:**
    - .op-kpi-strip: flex row, gap 24px, padding 8px 16px, background var(--color-surface-raised), border-radius var(--radius-md)
    - .op-kpi-item: flex column, align-items center
    - .op-kpi-label: font-size 0.65rem, text-transform uppercase, letter-spacing 0.05em, color var(--color-text-muted)
    - .op-kpi-value: font-family var(--font-mono), font-size 1.1rem, font-weight 600

    **Tab navigation (refined):**
    - Keep existing .tabs-nav and .tab-btn patterns but refine spacing and active indicator
    - .tab-btn-live: add subtle colored indicator when visible

    **Exec view (execution mode):**
    - .op-exec-header: flex row, justify-content space-between, align-items center, padding 12px 16px, background var(--color-primary-subtle)
    - .op-live-dot: 10px pulsing green circle
    - .op-live-chrono: font-family var(--font-mono), font-size 1rem

    **Sub-tabs (opSubTabs):**
    - Preserve data-op-tab click delegation pattern
    - Small underline-style tabs within exec view

    **Resolution progress track:**
    - .op-resolution-progress: flex row, height 6px, border-radius 3px, overflow hidden, gap 2px
    - Segment colors for pending/current/voted states

    **Quorum overlay:**
    - .op-quorum-overlay: fixed inset 0, background rgba(0,0,0,0.5), z-index 1000, flex center
    - .op-quorum-modal: max-width 480px, padding 32px, border-radius var(--radius-lg), background var(--color-surface)

    **Transition card:**
    - .op-transition-card: absolute center, flex column, align-items center, gap 16px, animation fadeIn 0.3s

    **Responsive (1024px):**
    ```css
    @media (max-width: 1024px) {
      .op-body { grid-template-columns: 1fr; }
      .op-sidebar {
        width: 100%;
        max-height: 120px;
        overflow-x: auto;
        overflow-y: hidden;
        border-right: none;
        border-bottom: 1px solid var(--color-border);
      }
      .op-agenda-list { display: flex; flex-direction: row; gap: 8px; }
      .op-agenda-item { white-space: nowrap; min-width: max-content; }
    }
    ```

    **Dark mode:** ALL colors use var(--color-*) tokens. SSE indicator green (#22c55e) and red (#ef4444) stay vivid (not token-dimmed). Use [data-theme="dark"] only for SSE indicator override if needed.

    **What to preserve from existing CSS:**
    - All existing hub-*, settings-*, attendance-*, proxy-*, tab-content, tab-toolbar, drawer, modal, tour patterns — these are used by the preserved HTML sections
    - readiness-strip, onboarding-banner, alert, form-group, badge styles — from app.css, no need to duplicate

    **What to remove:**
    - Old grid-template-areas (statusbar/tabnav/main) — replaced by new grid rows
    - Old bimodal setup/exec CSS complexity
    - Redundant vendor prefixes
    - Unused classes from old structure
  </action>
  <verify>
    <automated>grep -c 'op-body' public/assets/css/operator.css && grep -c 'op-sidebar' public/assets/css/operator.css && grep -c 'op-vote-card' public/assets/css/operator.css && grep -c 'op-sse-dot' public/assets/css/operator.css && grep -c 'op-vote-delta-badge' public/assets/css/operator.css && grep -c 'grid-template-columns: 280px' public/assets/css/operator.css && grep -c '1024px' public/assets/css/operator.css && echo "All critical CSS classes present"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'op-body' public/assets/css/operator.css returns match
    - grep 'op-sidebar' public/assets/css/operator.css returns match
    - grep 'op-vote-card' public/assets/css/operator.css returns match
    - grep 'op-sse-dot' public/assets/css/operator.css returns match
    - grep 'deltaPopIn' public/assets/css/operator.css returns match
    - grep 'grid-template-columns: 280px' public/assets/css/operator.css returns match
    - grep '1024px' public/assets/css/operator.css returns match (responsive breakpoint)
    - grep 'data-sse-state' public/assets/css/operator.css returns match (SSE indicator states)
    - All colors use var(--color-*) tokens except SSE indicator vivid green/red
  </acceptance_criteria>
  <done>operator.css is fully rewritten with two-panel grid layout, 280px sidebar, vote card centerpiece styling, SSE indicator with vivid colors, delta badge animation, KPI strip, responsive collapse at 1024px, full dark mode token parity</done>
</task>

</tasks>

<verification>
- Open operator.htmx.html in browser — page loads without CSS errors
- Two-panel layout visible: sidebar on left (280px), main content fills remaining space
- Meeting bar spans full width at top
- No broken layout at 1024px+ viewport
- Dark mode toggle preserves layout integrity
- No console errors from missing CSS classes
</verification>

<success_criteria>
- operator.htmx.html contains new two-panel HTML structure with all ~80 JS-dependent DOM IDs
- operator.css implements 280px sidebar + fluid main grid layout
- No data-partial attributes remain (exec content inlined)
- Vote card has op-vote-card--active class with prominent counter styling
- SSE indicator has vivid green/red styling
- Responsive breakpoint at 1024px collapses sidebar
- All content from operator-exec.html and operator-live-tabs.html partials is inlined
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-console-rebuild/46-01-SUMMARY.md`
</output>
