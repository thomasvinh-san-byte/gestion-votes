---
phase: 46-operator-console-rebuild
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - public/assets/js/pages/operator-tabs.js
  - public/assets/js/pages/operator-exec.js
  - public/assets/js/pages/operator-realtime.js
  - public/assets/js/pages/operator-motions.js
  - public/assets/js/pages/operator-attendance.js
  - public/assets/js/pages/operator-speech.js
autonomous: false
requirements: [REB-04, WIRE-01, WIRE-02]

must_haves:
  truths:
    - "SSE connection establishes on page load when meeting is selected — indicator shows green 'Connecte'"
    - "Operator can open a vote — live vote counts update via SSE without manual refresh"
    - "Operator can close a vote — result is recorded and agenda sidebar updates motion status"
    - "Delta badge shows +N when new votes arrive and auto-clears after 3 seconds"
    - "All disabled action buttons show ag-tooltip explaining WHY they are disabled"
    - "Tab navigation works across all 7 tabs without errors"
    - "Agenda sidebar items are clickable and load the motion in the main panel"
  artifacts:
    - path: "public/assets/js/pages/operator-tabs.js"
      provides: "Tab management, mode switch, OpS bridge init, lazy-load removal"
      contains: "window.OpS"
    - path: "public/assets/js/pages/operator-exec.js"
      provides: "Vote lifecycle, KPI rendering, delta badge with 3s clear, agenda list rendering"
      contains: "opVoteDeltaBadge"
    - path: "public/assets/js/pages/operator-realtime.js"
      provides: "SSE connection lifecycle, indicator state management"
      contains: "setSseIndicator"
  key_links:
    - from: "public/assets/js/pages/operator-realtime.js"
      to: "public/operator.htmx.html"
      via: "getElementById for SSE indicator"
      pattern: "getElementById.*opSseIndicator"
    - from: "public/assets/js/pages/operator-exec.js"
      to: "public/operator.htmx.html"
      via: "getElementById for vote counters and KPIs"
      pattern: "getElementById.*execVoteFor|opKpiPresent"
    - from: "public/assets/js/pages/operator-tabs.js"
      to: "public/operator.htmx.html"
      via: "getElementById for tab navigation"
      pattern: "getElementById.*tabsNav|meetingSelect"
---

<objective>
Update all 6 operator JS modules to work with the new HTML structure from Plan 01. Remove lazy partial loading (content is now inlined). Update delta badge timer from 10s to 3s. Verify SSE connection, vote lifecycle, and all tab navigation in browser.

Purpose: Wire the rebuilt HTML to the existing JS logic — the final step that makes the operator console fully functional.
Output: All 6 JS files updated with correct selectors, SSE verified, vote flow verified, browser checkpoint passed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-operator-console-rebuild/46-CONTEXT.md
@.planning/phases/46-operator-console-rebuild/46-RESEARCH.md
@.planning/phases/46-operator-console-rebuild/46-01-SUMMARY.md

<interfaces>
<!-- Key patterns from RESEARCH that govern JS updates -->

window.OpS bridge (operator-tabs.js line 11):
```javascript
window.OpS = { fn: {} };
// State: O.currentMeetingId, O.currentMeeting, O.currentMeetingStatus, O.currentMode,
//   O.currentOpenMotion, O.previousOpenMotionId, O.motionsCache, O.ballotsCache,
//   O.attendanceCache, O.membersCache, O.proxiesCache, O.policiesCache,
//   O.speechQueueCache, O.currentSpeakerCache, O.quorumWarningShown,
//   O.execSpeechTimerInterval
```

SSE indicator function (operator-realtime.js):
```javascript
function setSseIndicator(state) {
  var el = document.getElementById('opSseIndicator');
  var lb = document.getElementById('opSseLabel');
  if (el) el.setAttribute('data-sse-state', state);
  if (lb) lb.textContent = SSE_LABELS[state] || state;
}
```

Delta badge (operator-exec.js — current 10s, change to 3s):
```javascript
_deltaFadeTimer = setTimeout(function() { badge.hidden = true; }, 10000); // CHANGE TO 3000
```

Lazy-load pattern to REMOVE (operator-tabs.js):
```javascript
// ensureExecViewLoaded() — REMOVE: content is now inlined
// ensureLiveTabsLoaded() — REMOVE: content is now inlined
// loadPartial() calls — REMOVE or make no-ops
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update operator-tabs.js — remove lazy loading, update selectors for new HTML</name>
  <files>public/assets/js/pages/operator-tabs.js</files>
  <read_first>
    public/assets/js/pages/operator-tabs.js
    public/operator.htmx.html
  </read_first>
  <action>
    Update operator-tabs.js to work with the new inlined HTML structure. Key changes:

    1. **Remove lazy partial loading:** Find `ensureExecViewLoaded()`, `ensureLiveTabsLoaded()`, and any `loadPartial()` calls. Since exec content and live-tabs content are now inlined in the HTML, these functions should either be removed or converted to no-ops that just return immediately. Check every call site — `setMode('exec')` calls `ensureExecViewLoaded()`, for instance.

    2. **Remove data-loaded checks:** Find any `container.dataset.loaded` or `container.dataset.partial` references and remove them. The content is always present now.

    3. **Preserve window.OpS bridge init:** `window.OpS = { fn: {} }` on line 11 MUST remain. All OpS state properties MUST remain.

    4. **Preserve setMode() internal state machine:** Keep `O.currentMode` and `setMode()` function. Even though the mode switch UI may be de-emphasized, the internal state branching is used by operator-exec.js and operator-realtime.js. `setMode('exec')` should still show viewExec and hide viewSetup. `setMode('setup')` vice versa.

    5. **Update any querySelector/getElementById calls** that reference elements whose class or structure changed in Plan 01. The IDs are preserved, so most selectors should still work. Focus on:
       - Any `.app-main` references — may now be `.op-main` or within `.op-body`
       - Any `.op-main-content` references — may have changed
       - The `#liveTabs` container — no longer has data-partial/data-loaded

    6. **Tab click handlers:** Verify tab button selectors still match. The tab buttons (data-tab="seance", etc.) and tab panels (id="tab-seance", etc.) should be unchanged in ID, so handlers should work.

    7. **Preserve all OpS.fn registrations:** Every `O.fn.xxx = xxx` line must remain.

    **Do NOT change:**
    - OpS bridge initialization
    - Meeting selector logic
    - Tab switching logic (unless selectors broke)
    - Mode state machine internals
    - Any API calls
  </action>
  <verify>
    <automated>grep -c 'window.OpS' public/assets/js/pages/operator-tabs.js && grep -c 'setMode' public/assets/js/pages/operator-tabs.js && grep 'loadPartial\|ensureExecViewLoaded\|ensureLiveTabsLoaded' public/assets/js/pages/operator-tabs.js | wc -l && echo "Verify lazy-load removed and core preserved"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'window.OpS' public/assets/js/pages/operator-tabs.js returns match (bridge preserved)
    - grep 'setMode' public/assets/js/pages/operator-tabs.js returns match (mode state preserved)
    - grep 'loadPartial' public/assets/js/pages/operator-tabs.js returns 0 matches (removed)
    - grep 'ensureExecViewLoaded' public/assets/js/pages/operator-tabs.js returns 0 matches OR returns only a no-op stub
    - grep 'dataset.loaded' public/assets/js/pages/operator-tabs.js returns 0 matches (removed)
    - All O.fn.* registrations preserved
  </acceptance_criteria>
  <done>operator-tabs.js works with inlined HTML — no lazy loading, OpS bridge and mode state preserved, all tab switching functional</done>
</task>

<task type="auto">
  <name>Task 2: Update operator-exec.js delta badge timer and remaining JS files</name>
  <files>
    public/assets/js/pages/operator-exec.js
    public/assets/js/pages/operator-realtime.js
    public/assets/js/pages/operator-motions.js
    public/assets/js/pages/operator-attendance.js
    public/assets/js/pages/operator-speech.js
  </files>
  <read_first>
    public/assets/js/pages/operator-exec.js
    public/assets/js/pages/operator-realtime.js
    public/assets/js/pages/operator-motions.js
    public/assets/js/pages/operator-attendance.js
    public/assets/js/pages/operator-speech.js
    public/operator.htmx.html
  </read_first>
  <action>
    Update the remaining 5 JS files for the new HTML structure.

    **operator-exec.js:**
    1. Change delta badge clear timeout from 10000 (10s) to 3000 (3s). Find `_deltaFadeTimer = setTimeout(` and change the milliseconds value. There may be multiple occurrences.
    2. Verify all getElementById calls still find their targets in the new HTML. Key IDs: execActiveVote, execVoteFor, execVoteAgainst, execVoteAbstain, opBarFor, opBarAgainst, opBarAbstain, opVoteDeltaBadge, opKpiPresent, opKpiQuorum, opKpiVoted, opKpiResolution, opResolutionProgress, opActionBar, opSubTabs, etc.
    3. Verify `renderAgendaList()` still targets `#opAgendaList` and renders `.op-agenda-item` with correct classes (pending/current/voted) and `data-motion-id` attribute.
    4. Verify `bindProgressSegmentClicks()` still finds `#opResolutionProgress`.
    5. Verify `bindExecSubTabs()` still delegates on `#opSubTabs`.
    6. Remove any `ensureExecViewLoaded()` calls if they exist in this file.
    7. Preserve all O.fn.* registrations.

    **operator-realtime.js:**
    1. Verify `setSseIndicator()` still targets `#opSseIndicator` (data-sse-state) and `#opSseLabel`. These IDs are preserved — minimal or no changes expected.
    2. Verify `EventStream.connect()` and `MeetingContext.EVENT_NAME` listener are untouched.
    3. If there is a reference to `#opSseIndicatorBar` (the exec-mode duplicate SSE indicator from the partial), verify it still exists in new HTML.

    **operator-motions.js:**
    1. Verify all getElementById calls for the motions/ordre-du-jour tab still resolve. The tab-ordre-du-jour panel and its contents should be preserved.
    2. Verify `loadResolutions()` and motion CRUD functions still work.
    3. No expected changes unless class names shifted.

    **operator-attendance.js:**
    1. Verify all getElementById calls for attendance grid, presence toolbar, proxy list still resolve.
    2. IDs (attendanceGrid, presenceSearch, btnAddMember, btnMarkAllPresent, etc.) are preserved — minimal changes expected.

    **operator-speech.js:**
    1. Verify speech queue IDs (execSpeakerInfo, execSpeechActions, execSpeechQueue, execSpeakerTimer) still resolve.
    2. Tab-parole content is now inlined — no lazy-load dependency.

    **For ALL 5 files:**
    - Read the new operator.htmx.html first to confirm all target IDs exist
    - If any selector uses a class that changed (e.g., `.op-main-content` became `.op-main`), update it
    - Do NOT change any API calls, business logic, or OpS state management
    - Preserve all O.fn.* registrations
  </action>
  <verify>
    <automated>grep -c '3000' public/assets/js/pages/operator-exec.js && grep '10000.*deltaFade\|deltaFade.*10000' public/assets/js/pages/operator-exec.js | wc -l && grep -c 'setSseIndicator' public/assets/js/pages/operator-realtime.js && echo "Delta badge timer and SSE indicator verified"</automated>
  </verify>
  <acceptance_criteria>
    - grep '3000' public/assets/js/pages/operator-exec.js returns match (delta badge 3s timer)
    - grep '10000' near deltaFadeTimer in operator-exec.js returns 0 matches (old 10s removed)
    - grep 'setSseIndicator' public/assets/js/pages/operator-realtime.js returns match (SSE function preserved)
    - grep 'renderAgendaList' public/assets/js/pages/operator-exec.js returns match (agenda rendering preserved)
    - grep 'O.fn.' public/assets/js/pages/operator-exec.js returns matches (OpS registrations preserved)
    - grep 'loadResolutions' public/assets/js/pages/operator-motions.js returns match (preserved)
    - No references to ensureExecViewLoaded or loadPartial remain in any of the 5 files
  </acceptance_criteria>
  <done>All 5 JS files updated for new HTML structure: delta badge timer changed to 3s, SSE indicator wiring verified, all DOM selectors match new HTML, no lazy-load references remain</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser verification of complete operator console</name>
  <files>public/operator.htmx.html</files>
  <action>
    Present the rebuilt operator console for browser verification. All HTML+CSS+JS changes from Tasks 1-2 are complete. The user verifies the full operator flow in the browser following the steps below.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human browser verification"</automated>
  </verify>
  <what-built>Complete operator console rebuild — new HTML+CSS with two-panel layout, all 6 JS modules updated, SSE wired, vote panel functional</what-built>
  <how-to-verify>
    1. Open http://localhost/operator.htmx.html in browser
    2. Verify two-panel layout: 280px agenda sidebar on left, main content fills right
    3. Verify meeting bar at top: selector, status badge, SSE indicator (red "Hors ligne"), clock, refresh button
    4. Select a meeting from the dropdown
    5. Verify tab navigation works: click through Seance, Participants, Ordre du jour, Controle tabs
    6. Verify agenda sidebar populates with motions (if meeting has motions)
    7. Click a motion in sidebar — verify it loads in main panel
    8. If meeting can go live: open the session, verify SSE indicator turns green "Connecte"
    9. Open a vote — verify vote panel card appears as visual centerpiece with large Pour/Contre/Abstention counters
    10. Cast votes (from voter page in another tab) — verify:
        - Live vote counts update via SSE
        - Delta badge shows +N and clears after ~3 seconds
    11. Close the vote — verify result recorded, agenda sidebar updates motion status
    12. Hover disabled buttons — verify ag-tooltip shows explanation text
    13. Toggle dark mode — verify all elements render correctly with vivid SSE indicator colors
    14. Open browser console — verify no JS errors
    15. Resize to under 1024px — verify sidebar collapses to horizontal strip
  </how-to-verify>
  <done>User has approved the operator console rebuild in browser — all 15 verification steps passed</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- SSE connection establishes and indicator shows live state
- Vote open/close flow works end-to-end
- Delta badge shows +N and clears after 3 seconds
- Agenda sidebar lists motions and supports click navigation
- All disabled buttons show tooltips
- No JS console errors on page load or during interactions
- Dark mode renders correctly
- Responsive collapse at 1024px works
</verification>

<success_criteria>
- All 6 JS modules work with new HTML — no console errors
- SSE indicator shows correct state (live/reconnecting/offline)
- Vote lifecycle (open/count/close) works end-to-end
- Delta badge auto-clears after 3s (not 10s)
- Tab navigation works for all 7 tabs
- Agenda sidebar items are clickable
- Disabled buttons have ag-tooltip wrappers with explanation text
- Browser checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-console-rebuild/46-02-SUMMARY.md`
</output>
