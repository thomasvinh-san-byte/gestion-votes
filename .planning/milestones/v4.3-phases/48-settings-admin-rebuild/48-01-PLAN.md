---
phase: 48-settings-admin-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/settings.htmx.html
  - public/assets/css/settings.css
  - public/admin.htmx.html
  - public/assets/css/admin.css
autonomous: true
requirements: [REB-06]

must_haves:
  truths:
    - "Settings page has vertical sidebar tabs (200px) with 4 tabs: Regles, Communication, Securite, Accessibilite"
    - "Settings tab panels use hidden attribute for show/hide, not CSS classes"
    - "Settings fields use horizontal layout: label left, input right"
    - "Admin page has horizontal KPI row of 4 cards at top (members, sessions, votes, active users)"
    - "Admin page has full-width user management table below KPIs with search, filter, create button"
    - "Both pages use app-shell layout with sidebar, header, main structure"
    - "Dark mode works via design-system tokens — no hardcoded colors"
    - "Responsive: settings sidebar stacks horizontal at 768px, admin table scrolls horizontally"
  artifacts:
    - path: "public/settings.htmx.html"
      provides: "Complete settings page HTML with sidebar tabs, 4 tab panels, horizontal field groups"
      contains: "settings-layout"
    - path: "public/assets/css/settings.css"
      provides: "Settings page styles — sidebar nav, tab panels, field groups, responsive breakpoints"
      contains: "settings-layout"
    - path: "public/admin.htmx.html"
      provides: "Complete admin page HTML with KPI row, user management table, create form, pagination"
      contains: "admin-kpis"
    - path: "public/assets/css/admin.css"
      provides: "Admin page styles — KPI grid, user table, user row, pagination, responsive"
      contains: "admin-kpis"
  key_links:
    - from: "public/settings.htmx.html"
      to: "public/assets/js/pages/settings.js"
      via: "DOM IDs and selectors preserved for JS binding"
      pattern: "data-stab|stab-regles|stab-communication|stab-securite|stab-accessibilite|settings-sidenav-item|settings-panel|btn-save-section|unsaved-dot|settingsQuorumList|btnAddQuorumPolicy"
    - from: "public/admin.htmx.html"
      to: "public/assets/js/pages/admin.js"
      via: "DOM IDs preserved for JS binding at parse time"
      pattern: "searchUser|filterRole|btnCreateUser|usersListContainer|usersPrevPage|usersNextPage|usersPaginationPages|usersPaginationInfo|newName|newEmail|newPassword|newRole|passwordStrength|passwordStrengthFill|passwordStrengthText"
---

<objective>
Rewrite both settings.htmx.html and admin.htmx.html from scratch with new CSS. Settings gets vertical sidebar tabs (200px) with horizontal field groups. Admin gets KPI card row + full-width user management table.

Purpose: Ground-up rebuild of the final two pages, matching the card-based design established in Phases 43-47.
Output: New HTML+CSS for both pages, ready for JS wiring verification in Plan 02.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-settings-admin-rebuild/48-CONTEXT.md
@.planning/phases/48-settings-admin-rebuild/48-RESEARCH.md

@public/settings.htmx.html
@public/assets/css/settings.css
@public/assets/js/pages/settings.js
@public/admin.htmx.html
@public/assets/css/admin.css
@public/assets/js/pages/admin.js
@public/assets/css/app.css
@public/dashboard.htmx.html
@public/hub.htmx.html

<interfaces>
<!-- DOM IDs that settings.js targets — ALL must exist in new HTML -->

Tab switching:
- .settings-sidenav-item[data-stab] — tab buttons (regles, communication, securite, accessibilite)
- #stab-regles, #stab-communication, #stab-securite, #stab-accessibilite — tab panels (use hidden attr)
- .settings-panel — all tab panel sections

Auto-save:
- #main-content input/select/checkbox — auto-save targets (any control with an id inside main-content)
- .settings-level-card — CNIL level cards with radio[name="cnilLevel"]

Per-tab save:
- .card — save section container
- .btn-save-section[data-section] — save trigger
- .unsaved-dot — change indicator

Quorum policies (inside regles tab):
- #settingsQuorumList — quorum policy list container
- #btnAddQuorumPolicy — add policy button

Email templates (inside communication tab):
- #templateEditor — template editor section (skip auto-save for fields inside this)
- #templateSelect — template dropdown
- #templateSubject, #templateBody — template edit fields
- #btnSaveTemplate, #btnResetTemplates, #btnTestSmtp — template action buttons

Accessibility (inside accessibilite tab):
- input[name="textSize"] — radio buttons for text size
- #settHighContrast — high contrast toggle

<!-- DOM IDs that admin.js targets — ALL must exist in new HTML at parse time -->

KPI cards (NEW IDs — admin.js will be updated in Plan 02):
- #adminKpiMembers — total members count (replaces old #adminKpiUsers)
- #adminKpiSessions — total sessions count (replaces old #adminKpiAdmins)
- #adminKpiVotes — total votes count (replaces old #adminKpiOperators)
- #adminKpiActive — active users count (kept from old)

Onboarding (REMOVE — not in new design):
- #obBanner, #obClose — onboarding banner (admin.js null-guards these)

User management (MUST exist at parse time):
- #searchUser — search input (addEventListener at parse time, line 222)
- #filterRole — role filter select (addEventListener at parse time, line 344)
- #btnCreateUser — create button (addEventListener at parse time, line 392)
- #usersListContainer — render target (addEventListener at parse time, line 429)
- #newName, #newEmail, #newPassword, #newRole — create form fields (Shared.liveValidate)
- #passwordStrength, #passwordStrengthFill, #passwordStrengthText — password strength indicator
- #usersPrevPage, #usersNextPage, #usersPaginationPages, #usersPaginationInfo — pagination
- #usersCount — total users count display
- #btnRefresh — refresh button
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite settings.htmx.html and settings.css from scratch</name>
  <files>public/settings.htmx.html, public/assets/css/settings.css</files>
  <read_first>
    - public/settings.htmx.html (current 888-line HTML — understand all DOM IDs and tab panel structure)
    - public/assets/js/pages/settings.js (ALL querySelectorAll and getElementById calls — must preserve selectors)
    - public/assets/css/settings.css (current 631-line CSS — understand existing patterns to improve upon)
    - public/assets/css/app.css (design tokens, app-shell grid)
    - public/hub.htmx.html (reference for 1200px max-width centered card layout from Phase 47)
    - public/assets/css/pages.css (kpi-card pattern, form input styles)
  </read_first>
  <action>
Rewrite settings.htmx.html completely. Keep head section (critical-tokens, fonts, theme-init, app.css, settings.css), keep script tags at bottom, keep app-shell + sidebar + header structure.

**Header**: breadcrumb "Parametres" (bc-current), page-title with gear SVG icon "Parametres", page-sub "Configuration de l'application". No header action buttons needed.

**Main content** inside `<main class="app-main" id="main-content" role="main">`:

1. **Settings layout** (class `settings-layout`, CSS grid `200px 1fr`, `max-width: 1200px`, `margin: 0 auto`):

   **Left: Sidebar nav** (class `settings-sidenav`, role="tablist", aria-label="Onglets parametres"):
   - 4 tab buttons, each with class `settings-sidenav-item`, `data-stab="{id}"`, role="tab", aria-controls="stab-{id}":
     - `data-stab="regles"` — SVG icon (scale/balance) + "Regles" (active by default, aria-selected="true")
     - `data-stab="communication"` — SVG icon (mail) + "Communication"
     - `data-stab="securite"` — SVG icon (shield) + "Securite"
     - `data-stab="accessibilite"` — SVG icon (eye) + "Accessibilite"
   - Sidebar is `position: sticky; top: 80px; align-self: start`

   **Right: Panels container** (class `settings-panels`):

   **Panel: Regles** (`id="stab-regles"`, role="tabpanel", class `settings-panel`):
   - Card 1: "Regles de vote" header. Horizontal field groups (class `field-group-h`):
     - Vote mode: label "Mode de vote par defaut" + select#settVoteMode (options: secret, public)
     - Quorum threshold: label "Seuil de quorum (%)" + input#settQuorumThreshold type="number" min="0" max="100"
     - Majority rule: label "Regle de majorite" + select#settMajority (simple, absolute, qualified)
   - Card 2: "Politiques de quorum" header + button#btnAddQuorumPolicy (btn btn-sm btn-secondary "Ajouter"). Container div#settingsQuorumList for policy items (populated by JS).

   **Panel: Communication** (`id="stab-communication"`, hidden, role="tabpanel", class `settings-panel`):
   - Card 1: "Configuration SMTP" header. Horizontal field groups:
     - SMTP host: input#settSmtpHost type="text"
     - SMTP port: input#settSmtpPort type="number"
     - SMTP user: input#settSmtpUser type="email"
     - SMTP password: input#settSmtpPass type="password"
     - Sender name: input#settSenderName type="text"
     - Sender email: input#settSenderEmail type="email"
     - Button#btnTestSmtp (btn btn-sm btn-secondary "Tester SMTP")
   - Card 2: "Modeles d'email" header (id="templateEditor"):
     - Template selector: select#templateSelect
     - Subject: input#templateSubject type="text"
     - Body: textarea#templateBody (rows="8")
     - Actions row: button#btnSaveTemplate (btn btn-sm btn-primary "Enregistrer") + button#btnResetTemplates (btn btn-sm btn-ghost "Reinitialiser les modeles")

   **Panel: Securite** (`id="stab-securite"`, hidden, role="tabpanel", class `settings-panel`):
   - Card 1: "Niveau de conformite CNIL" header. Three `.settings-level-card` items, each containing:
     - radio input[name="cnilLevel"] (values: standard, renforce, maximal)
     - Label and description text
     - Selected state via `.selected` class toggled by JS
   - Card 2: "Parametres de securite" header. Horizontal field groups:
     - Session timeout: input#settSessionTimeout type="number" (minutes)
     - Max login attempts: input#settMaxLoginAttempts type="number"
     - Password min length: input#settPasswordMinLength type="number"

   **Panel: Accessibilite** (`id="stab-accessibilite"`, hidden, role="tabpanel", class `settings-panel`):
   - Card 1: "Taille du texte" header. Three radio inputs[name="textSize"] (values: normal, large, xlarge) with labels.
   - Card 2: "Contraste eleve" header. Toggle checkbox#settHighContrast with label.

2. **CSS rewrite** (settings.css — target ~250 lines):
   - `.settings-layout` — grid: `200px 1fr`, gap `var(--space-6)`, max-width `1200px`, margin `0 auto`, padding `var(--space-6)`
   - `.settings-sidenav` — sticky, flex column, gap `var(--space-1)`, bg `var(--color-surface)`, border-radius `var(--radius-xl)`, padding `var(--space-3)`, border `1px solid var(--color-border)`
   - `.settings-sidenav-item` — flex, align-items center, gap `var(--space-2)`, padding `var(--space-3) var(--space-4)`, border-radius `var(--radius-lg)`, font-size `var(--text-sm)`, color `var(--color-text-secondary)`, transition, cursor pointer, border none, background none, width 100%, text-align left
   - `.settings-sidenav-item.active` — background `var(--color-primary-subtle)`, color `var(--color-primary)`, font-weight 500
   - `.settings-sidenav-item:hover:not(.active)` — background `var(--color-surface-hover)`
   - `.settings-panel` — display flex, flex-direction column, gap `var(--space-6)`
   - `.settings-panel .card` — standard card pattern: bg `var(--color-surface)`, border `1px solid var(--color-border)`, border-radius `var(--radius-xl)`, padding `var(--space-6)`
   - `.field-group-h` — display grid, grid-template-columns `200px 1fr`, gap `var(--space-3)`, align-items center, padding `var(--space-3) 0`, border-bottom `1px solid var(--color-border-subtle)`
   - `.field-group-h:last-child` — no border-bottom
   - `.field-group-h label` — font-size `var(--text-sm)`, color `var(--color-text-secondary)`, font-weight 500
   - `.field-group-h input, .field-group-h select, .field-group-h textarea` — standard form-input style from design-system
   - `.settings-level-card` — border `1px solid var(--color-border)`, border-radius `var(--radius-lg)`, padding `var(--space-4)`, cursor pointer, transition
   - `.settings-level-card.selected` — border-color `var(--color-primary)`, background `var(--color-primary-subtle)`
   - Responsive `@media (max-width: 768px)`: `.settings-layout` grid to `1fr`, `.settings-sidenav` flex-direction row, overflow-x auto, sticky off
   - Dark mode: all colors via tokens, no overrides needed

Do NOT add any inline `style="display:none"` — use `hidden` attribute on inactive panels (panels 2-4). The first panel `stab-regles` has no hidden attribute.
  </action>
  <verify>
    <automated>grep -c 'settings-layout\|settings-sidenav-item\|settings-panel\|field-group-h\|stab-regles\|stab-communication\|stab-securite\|stab-accessibilite\|settingsQuorumList\|btnAddQuorumPolicy\|templateEditor\|btnTestSmtp\|settHighContrast' public/settings.htmx.html | xargs test 10 -le</automated>
  </verify>
  <acceptance_criteria>
    - grep 'settings-layout' public/settings.htmx.html returns at least 1 match
    - grep 'data-stab="regles"' public/settings.htmx.html returns 1 match
    - grep 'data-stab="communication"' public/settings.htmx.html returns 1 match
    - grep 'data-stab="securite"' public/settings.htmx.html returns 1 match
    - grep 'data-stab="accessibilite"' public/settings.htmx.html returns 1 match
    - grep 'id="stab-regles"' public/settings.htmx.html returns 1 match
    - grep 'id="stab-communication"' public/settings.htmx.html returns 1 match
    - grep 'id="settingsQuorumList"' public/settings.htmx.html returns 1 match
    - grep 'id="btnAddQuorumPolicy"' public/settings.htmx.html returns 1 match
    - grep 'id="templateEditor"' public/settings.htmx.html returns 1 match
    - grep 'id="btnTestSmtp"' public/settings.htmx.html returns 1 match
    - grep 'id="settHighContrast"' public/settings.htmx.html returns 1 match
    - grep 'field-group-h' public/assets/css/settings.css returns at least 1 match
    - grep 'settings-sidenav' public/assets/css/settings.css returns at least 1 match
    - grep '768px' public/assets/css/settings.css returns at least 1 match (responsive breakpoint)
    - settings.htmx.html is under 400 lines (was 888)
    - settings.css is under 300 lines (was 631)
  </acceptance_criteria>
  <done>Settings page has complete new HTML with 4 sidebar tabs, horizontal field groups, all JS-required DOM IDs preserved. CSS is clean with design tokens, responsive at 768px. File sizes reduced by ~50%+.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite admin.htmx.html and admin.css from scratch</name>
  <files>public/admin.htmx.html, public/assets/css/admin.css</files>
  <read_first>
    - public/admin.htmx.html (current 1218-line HTML — understand all DOM IDs)
    - public/assets/js/pages/admin.js (ALL getElementById calls — critical: some run at parse time with no null guard)
    - public/assets/css/admin.css (current 1175-line CSS)
    - public/assets/css/pages.css (kpi-card, kpi-card--N variant patterns — lines 1034-1081)
    - public/dashboard.htmx.html (KPI card HTML reference pattern)
    - public/assets/css/app.css (design tokens, app-shell)
  </read_first>
  <action>
Rewrite admin.htmx.html completely. Keep head section (critical-tokens, fonts, theme-init, app.css, admin.css), keep script tags at bottom, keep app-shell + sidebar + header structure.

**Header**: Use the consistent rebuilt header pattern:
- Left: breadcrumb "Administration" (bc-current), page-title with shield SVG icon "Administration", page-sub "Utilisateurs et parametres systeme"
- Right: button#btnRefresh (btn btn-secondary btn-sm "Actualiser")

**Main content** inside `<main class="app-main" id="main-content" role="main">`:

Single column layout, max-width 1200px centered:

1. **KPI row** (class `admin-kpis`, CSS grid 4 equal columns):
   - Card 1 (kpi-card kpi-card--1): icon (users SVG), value `#adminKpiMembers` (text "-"), label "Membres"
   - Card 2 (kpi-card kpi-card--2): icon (calendar SVG), value `#adminKpiSessions` (text "-"), label "Seances"
   - Card 3 (kpi-card kpi-card--3): icon (check-square SVG), value `#adminKpiVotes` (text "-"), label "Votes"
   - Card 4 (kpi-card kpi-card--4): icon (activity SVG), value `#adminKpiActive` (text "-"), label "Actifs (7j)"
   - Use the exact same kpi-card/kpi-icon/kpi-value/kpi-label structure from dashboard.htmx.html

2. **User management section** (class `admin-users-section`, card style):
   - **Section header** (class `section-header`, flex row, space-between):
     - Left: h2 "Utilisateurs" + span#usersCount (text "- utilisateurs")
     - Right actions row (class `section-actions`, flex row, gap):
       - input#searchUser type="search" placeholder="Rechercher..." (class `form-input form-input-sm`)
       - select#filterRole (class `form-input form-input-sm`): options "Tous les roles", admin, operator, auditor, viewer
       - button#btnCreateUser (btn btn-primary btn-sm "Ajouter un utilisateur")

   - **Create user form** (class `admin-create-form`, display grid, 4 columns, gap, card-like bg):
     - input#newName placeholder="Nom" (class `form-input`)
     - input#newEmail type="email" placeholder="Email" (class `form-input`)
     - div containing: input#newPassword type="password" placeholder="Mot de passe" (class `form-input`) + password strength indicator (div#passwordStrength hidden, div#passwordStrengthFill, span#passwordStrengthText)
     - select#newRole (class `form-input`): options "Role", admin, operator, auditor, viewer

   - **Users list container** div#usersListContainer — empty, populated by admin.js renderUsersTable()

   - **Pagination bar** (class `admin-pagination`, flex row, justify-between):
     - Left: span#usersPaginationInfo (text "- utilisateurs")
     - Right: button#usersPrevPage (btn btn-ghost btn-sm "Precedent") + div#usersPaginationPages (inline, page buttons injected by JS) + button#usersNextPage (btn btn-ghost btn-sm "Suivant")

3. **REMOVE from new HTML** (not in new simplified design):
   - Onboarding banner (#obBanner, #obClose) — admin.js null-guards these, safe to remove
   - Dashboard overview KPIs (#kpiUpcomingVal, #kpiLiveVal etc.) — replaced by new KPI row
   - Upcoming sessions list (#dashUpcomingSessions) — not in admin page scope
   - Urgent card (#urgentCard) — not in admin page scope
   - Policies tabs and content (meeting role assignments, vote policies, permissions matrix, state machine)
   - System status section
   - Demo reset section
   - Guide/context drawers
   - All tab switching between "dashboard", "users", "policies" sections

4. **CSS rewrite** (admin.css — target ~350 lines):
   - `.admin-kpis` — display grid, grid-template-columns `repeat(4, 1fr)`, gap `var(--space-4)`, margin-bottom `var(--space-6)`
   - `.admin-users-section` — bg `var(--color-surface)`, border `1px solid var(--color-border)`, border-radius `var(--radius-xl)`, overflow hidden
   - `.section-header` — display flex, align-items center, justify-content space-between, padding `var(--space-5) var(--space-6)`, border-bottom `1px solid var(--color-border-subtle)`
   - `.section-header h2` — font-size `var(--text-lg)`, font-weight 600
   - `.section-actions` — display flex, align-items center, gap `var(--space-3)`
   - `.admin-create-form` — display grid, grid-template-columns `repeat(4, 1fr)`, gap `var(--space-3)`, padding `var(--space-4) var(--space-6)`, background `var(--color-surface-sunken)`, border-bottom `1px solid var(--color-border-subtle)`
   - `.user-row` — display flex, align-items center, gap `var(--space-4)`, padding `var(--space-4) var(--space-6)`, border-bottom `1px solid var(--color-border-subtle)`, transition
   - `.user-row:hover` — background `var(--color-surface-hover)`
   - `.user-row:last-child` — no border-bottom
   - `.user-row.is-inactive` — opacity 0.5
   - `.user-avatar` — width 36px, height 36px, border-radius 50%, display flex, align-items center, justify-content center, font-size `var(--text-xs)`, font-weight 600, flex-shrink 0
   - `.user-row-body` — flex 1, display flex, align-items center, justify-content space-between, min-width 0
   - `.user-row-main` — display flex, flex-direction column, gap `var(--space-1)`, min-width 0
   - `.user-row-name` — font-weight 500, font-size `var(--text-sm)`, truncate
   - `.user-row-email` — font-size `var(--text-xs)`, color `var(--color-text-secondary)`, truncate
   - `.user-row-meta` — display flex, gap `var(--space-2)`, align-items center, flex-shrink 0
   - `.user-row-actions` — display flex, gap `var(--space-1)`, flex-shrink 0
   - `.user-status-badge` — font-size `var(--text-xs)`, padding `2px 8px`, border-radius `var(--radius-full)`
   - `.user-status-badge.active` — bg `var(--color-success-subtle)`, color `var(--color-success)`
   - `.admin-pagination` — display flex, align-items center, justify-content space-between, padding `var(--space-4) var(--space-6)`, border-top `1px solid var(--color-border-subtle)`
   - `.pagination-page` — all pagination button styles
   - `.pagination-page.active` — bg `var(--color-primary)`, color white
   - Password strength: `.password-strength-fill`, `.password-strength-fill.weak/fair/good/strong` with color transitions
   - Responsive `@media (max-width: 768px)`: `.admin-kpis` grid to `2fr 2fr`, `.admin-create-form` grid to `1fr 1fr`, `.section-actions` wrap, `.user-row-meta` display none on small screens, table container overflow-x auto
   - Dark mode: all via tokens

Keep admin.js script tags at bottom. The old admin.js has a large IIFE that initializes immediately — all DOM IDs listed above MUST exist in the HTML before the script tag.
  </action>
  <verify>
    <automated>grep -c 'admin-kpis\|adminKpiMembers\|adminKpiSessions\|adminKpiVotes\|adminKpiActive\|searchUser\|filterRole\|btnCreateUser\|usersListContainer\|usersPrevPage\|usersNextPage\|usersPaginationPages\|usersPaginationInfo\|newName\|newEmail\|newPassword\|newRole\|passwordStrength' public/admin.htmx.html | xargs test 15 -le</automated>
  </verify>
  <acceptance_criteria>
    - grep 'admin-kpis' public/admin.htmx.html returns at least 1 match
    - grep 'id="adminKpiMembers"' public/admin.htmx.html returns 1 match
    - grep 'id="adminKpiSessions"' public/admin.htmx.html returns 1 match
    - grep 'id="adminKpiVotes"' public/admin.htmx.html returns 1 match
    - grep 'id="adminKpiActive"' public/admin.htmx.html returns 1 match
    - grep 'id="searchUser"' public/admin.htmx.html returns 1 match
    - grep 'id="filterRole"' public/admin.htmx.html returns 1 match
    - grep 'id="btnCreateUser"' public/admin.htmx.html returns 1 match
    - grep 'id="usersListContainer"' public/admin.htmx.html returns 1 match
    - grep 'id="newName"' public/admin.htmx.html returns 1 match
    - grep 'id="newEmail"' public/admin.htmx.html returns 1 match
    - grep 'id="newPassword"' public/admin.htmx.html returns 1 match
    - grep 'id="newRole"' public/admin.htmx.html returns 1 match
    - grep 'id="usersPrevPage"' public/admin.htmx.html returns 1 match
    - grep 'id="usersNextPage"' public/admin.htmx.html returns 1 match
    - grep 'id="passwordStrength"' public/admin.htmx.html returns 1 match
    - grep 'admin-kpis' public/assets/css/admin.css returns at least 1 match
    - grep 'user-row' public/assets/css/admin.css returns at least 1 match
    - grep '768px' public/assets/css/admin.css returns at least 1 match (responsive)
    - admin.htmx.html is under 450 lines (was 1218)
    - admin.css is under 400 lines (was 1175)
  </acceptance_criteria>
  <done>Admin page has complete new HTML with 4 KPI cards (members, sessions, votes, active), full user management section with search/filter/create/pagination, all JS-required DOM IDs preserved. CSS is clean with tokens, responsive at 768px. File sizes reduced by ~60%+.</done>
</task>

</tasks>

<verification>
- Both pages load in browser without layout errors (HTML/CSS only — JS errors expected until Plan 02)
- Settings sidebar tabs are visible and panel structure uses hidden attribute
- Admin KPI row shows 4 cards in horizontal grid
- Admin user management section has search, filter, create form, list container, pagination
- All critical DOM IDs from the interfaces block exist in the new HTML
- Dark mode toggle produces correct colors (all via tokens)
- Responsive: at 768px, settings sidebar stacks horizontal, admin KPIs go to 2x2 grid
</verification>

<success_criteria>
Both settings.htmx.html and admin.htmx.html are fully rewritten with clean HTML structure. Both CSS files are rewritten with design tokens. All DOM IDs required by settings.js and admin.js exist in the HTML. File sizes are reduced by 50%+. Pages are ready for JS wiring verification in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/48-settings-admin-rebuild/48-01-SUMMARY.md`
</output>
