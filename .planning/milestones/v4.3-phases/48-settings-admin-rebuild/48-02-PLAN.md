---
phase: 48-settings-admin-rebuild
plan: 02
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - public/api/v1/admin_settings.php
  - app/Controller/SettingsController.php
  - app/Repository/SettingsRepository.php
  - app/Core/Providers/RepositoryFactory.php
  - database/migrations/20260322_tenant_settings.sql
  - public/assets/js/pages/admin.js
  - public/assets/js/pages/settings.js
autonomous: false
requirements: [REB-06, WIRE-01]

must_haves:
  truths:
    - "Settings page saves field values via admin_settings.php and values persist after page reload"
    - "Admin KPI cards display real counts: members from /api/v1/members, sessions+votes from /api/v1/meetings.php, active users from /api/v1/admin_users.php"
    - "Admin user management table loads, search/filter works, create/edit/deactivate users works"
    - "No JS console errors on settings page load"
    - "No JS console errors on admin page load"
    - "Settings tab switching works (4 tabs)"
    - "All toast notifications appear on save success/error"
  artifacts:
    - path: "public/api/v1/admin_settings.php"
      provides: "Settings API endpoint proxy"
      contains: "SettingsController"
    - path: "app/Controller/SettingsController.php"
      provides: "Settings controller with list, update, get_template, save_template, test_smtp, reset_templates actions"
      contains: "class SettingsController"
    - path: "app/Repository/SettingsRepository.php"
      provides: "Settings repository for tenant_settings table CRUD"
      contains: "class SettingsRepository"
    - path: "database/migrations/20260322_tenant_settings.sql"
      provides: "tenant_settings table migration"
      contains: "CREATE TABLE"
    - path: "public/assets/js/pages/admin.js"
      provides: "Updated admin JS with new KPI DOM IDs and multi-endpoint KPI fetching"
      contains: "adminKpiMembers"
    - path: "public/assets/js/pages/settings.js"
      provides: "Settings JS with verified selectors matching new HTML"
      contains: "admin_settings.php"
  key_links:
    - from: "public/assets/js/pages/settings.js"
      to: "public/api/v1/admin_settings.php"
      via: "api() fetch calls"
      pattern: "api.*admin_settings"
    - from: "public/assets/js/pages/admin.js"
      to: "public/api/v1/admin_users.php"
      via: "api() fetch calls for user CRUD + active users KPI"
      pattern: "api.*admin_users"
    - from: "public/assets/js/pages/admin.js"
      to: "public/api/v1/members"
      via: "api() fetch call for members KPI"
      pattern: "api.*members"
    - from: "public/assets/js/pages/admin.js"
      to: "public/api/v1/meetings.php"
      via: "api() fetch call for sessions+votes KPI"
      pattern: "api.*meetings"
---

<objective>
Create the missing admin_settings.php backend endpoint for settings persistence, update admin.js to fetch KPIs from correct endpoints with new DOM IDs, verify all JS selectors match the new HTML from Plan 01, and browser-verify both pages end-to-end.

Purpose: Complete backend wiring (WIRE-01) and JS selector alignment for both settings and admin pages — the final step of v4.3 Ground-Up Rebuild.
Output: Working settings save/load, working admin KPIs and user CRUD, zero console errors on both pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-settings-admin-rebuild/48-CONTEXT.md
@.planning/phases/48-settings-admin-rebuild/48-RESEARCH.md
@.planning/phases/48-settings-admin-rebuild/48-01-SUMMARY.md

@public/assets/js/pages/settings.js
@public/assets/js/pages/admin.js
@app/Controller/AbstractController.php
@app/Controller/AdminController.php
@app/Repository/AbstractRepository.php
@app/Core/Providers/RepositoryFactory.php
@public/api/v1/admin_quorum_policies.php
@public/api/v1/admin_users.php

<interfaces>
<!-- AbstractController pattern -->
From app/Controller/AbstractController.php:
- Extend AbstractController, implement method matching handle() call
- Use $this->repo() for RepositoryFactory access
- Use api_method(), api_request(), api_ok(), api_fail() helpers

<!-- API proxy pattern (from admin_quorum_policies.php) -->
```php
<?php
declare(strict_types=1);
require __DIR__ . '/../../../app/api.php';
(new \AgVote\Controller\SettingsController())->handle('settings');
```

<!-- RepositoryFactory pattern -->
From app/Core/Providers/RepositoryFactory.php:
- Add: public function settings(): SettingsRepository { return $this->get(SettingsRepository::class); }
- Add use statement for SettingsRepository

<!-- AbstractRepository pattern -->
From app/Repository/AbstractRepository.php:
- Extend AbstractRepository
- Use $this->pdo for DB access
- Use selectOne(), selectAll() helpers
- Use $this->pdo->prepare() for INSERT/UPDATE

<!-- admin.js KPI IDs that changed (Plan 01 new HTML) -->
Old IDs removed: #adminKpiUsers, #adminKpiAdmins, #adminKpiOperators
New IDs: #adminKpiMembers, #adminKpiSessions, #adminKpiVotes, #adminKpiActive

<!-- admin.js sections to REMOVE (not in new HTML) -->
- initOnboarding() — #obBanner, #obClose removed
- initDashboard() — #kpiUpcomingVal, #kpiLiveVal, etc. removed
- All policies/roles/permissions/state-machine/system-status code
- Tab switching between dashboard/users/policies sections
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin_settings.php backend endpoint with database table</name>
  <files>database/migrations/20260322_tenant_settings.sql, app/Repository/SettingsRepository.php, app/Controller/SettingsController.php, app/Core/Providers/RepositoryFactory.php, public/api/v1/admin_settings.php</files>
  <read_first>
    - app/Controller/AbstractController.php (base controller pattern — handle(), repo(), api helpers)
    - app/Repository/AbstractRepository.php (base repository — selectOne, selectAll, pdo access)
    - app/Core/Providers/RepositoryFactory.php (full file — need to add settings() method + use statement)
    - app/Controller/AdminController.php (reference for api_method, api_request, api_ok, api_fail, api_current_tenant_id patterns)
    - public/api/v1/admin_quorum_policies.php (reference for API proxy file pattern)
    - public/assets/js/pages/settings.js (all admin_settings.php calls — actions: list, update, get_template, save_template, test_smtp, reset_templates)
    - app/Controller/EmailTemplatesController.php (reference for how email templates are already handled — rewire communication tab to use this)
  </read_first>
  <action>
1. **Create migration** `database/migrations/20260322_tenant_settings.sql`:
```sql
CREATE TABLE IF NOT EXISTS tenant_settings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    tenant_id TEXT NOT NULL,
    key TEXT NOT NULL,
    value TEXT,
    updated_at TEXT DEFAULT (datetime('now')),
    UNIQUE(tenant_id, key)
);
```

2. **Create SettingsRepository** `app/Repository/SettingsRepository.php`:
   - Namespace `AgVote\Repository`
   - Extend `AbstractRepository`
   - Method `listByTenant(string $tenantId): array` — SELECT all rows WHERE tenant_id = ?, return associative array key => value
   - Method `upsert(string $tenantId, string $key, $value): void` — INSERT OR REPLACE into tenant_settings
   - Method `get(string $tenantId, string $key): ?string` — SELECT value WHERE tenant_id AND key
   - Ensure table creation: in constructor, after parent::__construct(), run the CREATE TABLE IF NOT EXISTS SQL directly (SQLite, no migration runner)

3. **Create SettingsController** `app/Controller/SettingsController.php`:
   - Namespace `AgVote\Controller`
   - Extend `AbstractController`
   - Method `settings(): void` that handles POST requests with action field:
     - `action: list` — return `api_ok(['data' => $this->repo()->settings()->listByTenant(api_current_tenant_id())])`
     - `action: update` — expects `key` and `value` fields, calls `$this->repo()->settings()->upsert(...)`, returns `api_ok(['saved' => true])`
     - `action: get_template` — proxy to EmailTemplatesController or return the template from settings (for now, return `api_ok(['data' => ['subject' => '', 'body' => '']])` as stub — real email templates are at /api/v1/email_templates)
     - `action: save_template` — same stub pattern, return `api_ok(['saved' => true])`
     - `action: test_smtp` — return `api_ok(['sent' => true, 'message' => 'Test SMTP non configure'])` (stub for now)
     - `action: reset_templates` — return `api_ok(['reset' => true])`
     - Default/unknown action: `api_fail('unknown_action', 400)`

4. **Update RepositoryFactory** `app/Core/Providers/RepositoryFactory.php`:
   - Add `use AgVote\Repository\SettingsRepository;` at top
   - Add method: `public function settings(): SettingsRepository { return $this->get(SettingsRepository::class); }`

5. **Create API proxy** `public/api/v1/admin_settings.php`:
```php
<?php
declare(strict_types=1);
require __DIR__ . '/../../../app/api.php';
(new \AgVote\Controller\SettingsController())->handle('settings');
```

After creating all files, test the endpoint is reachable by checking PHP syntax:
```bash
php -l public/api/v1/admin_settings.php
php -l app/Controller/SettingsController.php
php -l app/Repository/SettingsRepository.php
```
  </action>
  <verify>
    <automated>php -l public/api/v1/admin_settings.php && php -l app/Controller/SettingsController.php && php -l app/Repository/SettingsRepository.php && grep -q 'settings()' app/Core/Providers/RepositoryFactory.php && echo "ALL OK"</automated>
  </verify>
  <acceptance_criteria>
    - file public/api/v1/admin_settings.php exists
    - file app/Controller/SettingsController.php exists and contains "class SettingsController"
    - file app/Repository/SettingsRepository.php exists and contains "class SettingsRepository"
    - grep 'settings()' app/Core/Providers/RepositoryFactory.php returns 1 match
    - grep 'SettingsRepository' app/Core/Providers/RepositoryFactory.php returns at least 1 match
    - file database/migrations/20260322_tenant_settings.sql exists and contains "CREATE TABLE"
    - php -l passes on all 3 PHP files (no syntax errors)
  </acceptance_criteria>
  <done>admin_settings.php endpoint exists, responds to action:list and action:update, settings are persisted in tenant_settings table. Template and SMTP actions return stubs. PHP lint passes on all files.</done>
</task>

<task type="auto">
  <name>Task 2: Update admin.js KPI logic and trim unused sections</name>
  <files>public/assets/js/pages/admin.js</files>
  <read_first>
    - public/assets/js/pages/admin.js (full file — understand all sections, identify what to keep vs remove)
    - public/admin.htmx.html (new HTML from Plan 01 — verify which DOM IDs exist)
    - public/assets/js/core/shared.js (Shared.liveValidate, Shared.validateAll, Shared.openModal, Shared.btnLoading signatures)
    - public/assets/js/core/utils.js (api() signature, escapeHtml, Utils.fuzzyFilter)
  </read_first>
  <action>
Update admin.js to match the new simplified admin page HTML. The new admin page has ONLY KPIs + user management — no dashboard overview, no policies tabs, no permissions matrix, no system status, no demo reset.

**KPI section rewrite:**

Replace the entire `initDashboard()` function with a new `loadAdminKpis()` function that:
1. Fetches from 3 endpoints in parallel using `Promise.all([...])`:
   - `api('/api/v1/members')` — for total members count
   - `api('/api/v1/meetings.php')` — for total sessions count + total votes count
   - `api('/api/v1/admin_users.php')` — for active users count (filter is_active)
2. Updates new DOM IDs:
   - `#adminKpiMembers` — set to members items length (or total from response)
   - `#adminKpiSessions` — set to meetings items length
   - `#adminKpiVotes` — sum of all motions across meetings, or total votes from analytics if available. Simplest: count meetings with status 'closed' or 'validated' as completed sessions. For votes, use the total motions count or just set to meetings count as placeholder. Best approach: fetch `/api/v1/analytics.php` if it has a total votes count, otherwise use meetings.length as sessions count and mark votes as "-" until a better endpoint exists.
   - `#adminKpiActive` — filter admin_users items where is_active !== false, count them
3. Call `loadAdminKpis()` on DOMContentLoaded (not at parse time — wrap in DOMContentLoaded or use null-guard pattern).

**Sections to REMOVE from admin.js:**
- `initOnboarding()` IIFE — #obBanner, #obClose no longer exist (null-guarded, so safe to keep, but cleaner to remove)
- `initDashboard()` function — old KPI IDs removed, replaced by new loadAdminKpis
- All policies/roles/permissions/state-machine/system-status code blocks (search for these section headers in the IIFE)
- Tab switching code between "dashboard"/"users"/"policies" sections
- Demo reset functionality

**Sections to KEEP (user management):**
- User loading, filtering, rendering (loadUsers, filterAndRenderUsers, renderUsersTable, updateUsersPagination)
- Search input handler (#searchUser)
- Filter role handler (#filterRole) — WRAP in null-guard: `var filterEl = document.getElementById('filterRole'); if (filterEl) filterEl.addEventListener(...)`
- Pagination handlers (#usersPrevPage, #usersNextPage, #usersPaginationPages)
- Password strength indicator (#newPassword input handler)
- Shared.liveValidate on #newName, #newEmail, #newPassword — WRAP each in null-guard
- Create user handler (#btnCreateUser) — WRAP in null-guard
- Edit user, delete user, toggle user, set password click handlers on #usersListContainer
- All role label constants (Shared.ROLE_LABELS_SYSTEM etc.)
- All helper functions (getInitials, getAvatarColor, escapeHtml)

**Critical fix for parse-time crashes:**
Line 344 `document.getElementById('filterRole').addEventListener('change', loadUsers)` runs at parse time with NO null guard. Wrap it:
```javascript
var filterRoleEl = document.getElementById('filterRole');
if (filterRoleEl) filterRoleEl.addEventListener('change', loadUsers);
```

Apply the same null-guard pattern to ALL other getElementById + addEventListener calls that run at parse time (searchUser, btnCreateUser, usersListContainer delegation, pagination buttons).

Wrap Shared.liveValidate calls in null-guards too:
```javascript
var newNameEl = document.getElementById('newName');
if (newNameEl) Shared.liveValidate(newNameEl, [...]);
```

**Call order at bottom of IIFE:**
1. `loadAdminKpis()` — fetch and display KPIs
2. `loadUsers()` — fetch and display user table
3. `btnRefresh` click handler — calls both loadAdminKpis() + loadUsers()
  </action>
  <verify>
    <automated>grep -c 'adminKpiMembers\|adminKpiSessions\|adminKpiVotes\|adminKpiActive\|loadAdminKpis\|api.*members\|api.*meetings\|api.*admin_users' public/assets/js/pages/admin.js | xargs test 5 -le</automated>
  </verify>
  <acceptance_criteria>
    - grep 'adminKpiMembers' public/assets/js/pages/admin.js returns at least 1 match
    - grep 'adminKpiSessions' public/assets/js/pages/admin.js returns at least 1 match
    - grep 'adminKpiVotes' public/assets/js/pages/admin.js returns at least 1 match
    - grep 'adminKpiActive' public/assets/js/pages/admin.js returns at least 1 match
    - grep 'loadAdminKpis' public/assets/js/pages/admin.js returns at least 1 match
    - grep -c 'obBanner\|initDashboard\|kpiUpcomingVal\|kpiLiveVal' public/assets/js/pages/admin.js returns 0 (removed)
    - grep 'filterRole' public/assets/js/pages/admin.js returns at least 1 match (kept with null-guard)
    - grep 'loadUsers' public/assets/js/pages/admin.js returns at least 1 match (kept)
    - admin.js is under 800 lines (was 1449 — removed ~50% unused code)
  </acceptance_criteria>
  <done>Admin.js fetches KPIs from members + meetings + admin_users endpoints, populates new DOM IDs, all parse-time getElementById calls are null-guarded, unused dashboard/policies/system code removed. File size reduced by ~40%+.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser verification of both pages</name>
  <files>public/settings.htmx.html, public/admin.htmx.html, public/assets/js/pages/settings.js, public/assets/js/pages/admin.js</files>
  <action>
Open both pages in browser and verify the complete rebuild works end-to-end. This is a visual and functional verification checkpoint — no code changes, only browser testing.

Steps:
1. Open /settings.htmx.html in browser
2. Verify: 4 sidebar tabs visible (Regles, Communication, Securite, Accessibilite)
3. Click each tab — panel content switches correctly
4. Change a setting value (e.g. quorum threshold) — toast "Parametre enregistre" appears
5. Reload the page — the changed value persists
6. Open browser DevTools Console — verify NO JS errors
7. Open /admin.htmx.html in browser
8. Verify: 4 KPI cards at top show real counts (not all "-")
9. Verify: user table loads with real users
10. Try search — filters users by name/email
11. Try role filter dropdown — filters by role
12. Try "Ajouter un utilisateur" — fill form, create user, verify appears in list
13. Try edit/deactivate/delete on a user — verify actions work
14. Open browser DevTools Console — verify NO JS errors
15. Toggle dark mode — verify both pages render correctly
16. Resize to 768px — verify settings sidebar becomes horizontal, admin KPIs go 2x2
  </action>
  <verify>
    <automated>echo "Manual browser checkpoint — user must approve"</automated>
  </verify>
  <acceptance_criteria>
    - Settings tabs switch correctly (4 tabs, panel visibility toggles)
    - Settings save persists after page reload
    - Admin KPI cards show real non-zero counts
    - Admin user table loads with real users
    - Admin user CRUD works (create, edit, deactivate)
    - No JS console errors on either page
    - Dark mode renders correctly on both pages
    - Responsive layout works at 768px on both pages
  </acceptance_criteria>
  <done>Both settings and admin pages pass full browser verification: tab switching, settings persistence, KPI display, user CRUD, dark mode, responsive layout. Phase 48 complete.</done>
</task>

</tasks>

<verification>
- Settings page: all 4 tabs switch correctly, settings save and persist on reload
- Admin page: KPI cards show real data, user CRUD works end-to-end
- No JS console errors on either page
- Both pages render correctly in dark mode
- Both pages respond correctly at 768px breakpoint
- admin_settings.php endpoint responds to action:list and action:update
</verification>

<success_criteria>
Settings page saves/loads settings from real database. Admin KPIs display live counts from backend. User management CRUD is fully functional. Zero JS console errors on both pages. Both pages pass visual verification in browser including dark mode and responsive behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/48-settings-admin-rebuild/48-02-SUMMARY.md`
</output>
