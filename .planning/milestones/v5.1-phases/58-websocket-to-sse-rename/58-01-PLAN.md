---
phase: 58-websocket-to-sse-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/WebSocket/EventBroadcaster.php
  - app/SSE/EventBroadcaster.php
  - app/Event/Listener/WebSocketListener.php
  - app/Event/Listener/SseListener.php
  - app/bootstrap.php
  - app/Core/Application.php
autonomous: true
requirements:
  - SSE-01
  - SSE-02

must_haves:
  truths:
    - "The directory app/SSE/ exists and app/WebSocket/ does not exist"
    - "app/SSE/EventBroadcaster.php declares namespace AgVote\\SSE"
    - "app/Event/Listener/SseListener.php exists and WebSocketListener.php does not"
    - "app/bootstrap.php autoloader maps AgVote\\SSE\\ to app/SSE/"
    - "app/Core/Application.php uses SseListener, not WebSocketListener"
  artifacts:
    - path: "app/SSE/EventBroadcaster.php"
      provides: "EventBroadcaster class under AgVote\\SSE namespace"
      exports: ["EventBroadcaster"]
    - path: "app/Event/Listener/SseListener.php"
      provides: "SseListener class (formerly WebSocketListener)"
      exports: ["SseListener"]
    - path: "app/bootstrap.php"
      provides: "PSR-4 autoloader with AgVote\\SSE\\ prefix"
      contains: "AgVote\\\\SSE\\\\"
    - path: "app/Core/Application.php"
      provides: "Application bootstrap using SseListener"
      contains: "SseListener"
  key_links:
    - from: "app/bootstrap.php"
      to: "app/SSE/"
      via: "PSR-4 autoloader registration"
      pattern: "AgVote\\\\SSE\\\\"
    - from: "app/Core/Application.php"
      to: "app/Event/Listener/SseListener.php"
      via: "use statement + subscribe() call"
      pattern: "SseListener"
---

<objective>
Rename the core SSE infrastructure files: move app/WebSocket/ to app/SSE/, rename WebSocketListener.php to SseListener.php, update namespaces, and rewire the autoloader and Application bootstrap.

Purpose: Establish the correct terminology foundation. All downstream reference updates in Plan 02 depend on these renamed files existing first.
Output: app/SSE/EventBroadcaster.php (namespace AgVote\SSE), app/Event/Listener/SseListener.php, updated bootstrap.php autoloader, updated Application.php.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-websocket-to-sse-rename/58-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename app/WebSocket/ directory and update EventBroadcaster namespace</name>
  <files>app/SSE/EventBroadcaster.php</files>
  <read_first>
    app/WebSocket/EventBroadcaster.php — read entire file before touching anything
  </read_first>
  <action>
    1. Copy app/WebSocket/EventBroadcaster.php to app/SSE/EventBroadcaster.php (creating the app/SSE/ directory).
    2. In app/SSE/EventBroadcaster.php, change line 5 from:
         `namespace AgVote\WebSocket;`
       to:
         `namespace AgVote\SSE;`
    3. In app/SSE/EventBroadcaster.php, update the class docblock comment on line 13-16:
       Change "Service pour envoyer des evenements au serveur WebSocket." to
       "Service pour envoyer des evenements SSE (Server-Sent Events) via Redis."
    4. Update the QUEUE_KEY constant value from 'ws:event_queue' to 'sse:event_queue'.
    5. Update QUEUE_FILE from '/tmp/agvote-ws-queue.json' to '/tmp/agvote-sse-queue.json'.
    6. Update LOCK_FILE from '/tmp/agvote-ws-queue.lock' to '/tmp/agvote-sse-queue.lock'.
    7. Scan the rest of EventBroadcaster.php for any comment lines containing "WebSocket" and replace with "SSE". Key occurrences: error_log calls that say '[WebSocket]' should become '[SSE]'.
    8. Delete the original file and directory: remove app/WebSocket/EventBroadcaster.php then rmdir app/WebSocket/.

    Do NOT change any method signatures, logic, or the class name EventBroadcaster itself — only the namespace and terminology in strings/comments.
  </action>
  <verify>
    <automated>
      grep -n "namespace" /home/user/gestion_votes_php/app/SSE/EventBroadcaster.php | grep "AgVote\\\\SSE" &&
      test ! -d /home/user/gestion_votes_php/app/WebSocket/ &&
      grep -c "WebSocket" /home/user/gestion_votes_php/app/SSE/EventBroadcaster.php | grep -q "^0$" || echo "FAIL: WebSocket refs remain in EventBroadcaster.php"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "namespace" app/SSE/EventBroadcaster.php` returns `namespace AgVote\SSE;`
    - `test -f app/SSE/EventBroadcaster.php` exits 0
    - `test -d app/WebSocket/` exits 1 (directory gone)
    - `grep -c "WebSocket" app/SSE/EventBroadcaster.php` returns 0
  </acceptance_criteria>
  <done>app/SSE/EventBroadcaster.php exists with namespace AgVote\SSE; app/WebSocket/ directory is deleted; no "WebSocket" string remains in the file</done>
</task>

<task type="auto">
  <name>Task 2: Rename WebSocketListener to SseListener and update its internals</name>
  <files>app/Event/Listener/SseListener.php</files>
  <read_first>
    app/Event/Listener/WebSocketListener.php — read entire file before touching anything
  </read_first>
  <action>
    1. Copy app/Event/Listener/WebSocketListener.php to app/Event/Listener/SseListener.php.
    2. In SseListener.php, update the `use` statement on line 9:
       Change: `use AgVote\WebSocket\EventBroadcaster;`
       To:     `use AgVote\SSE\EventBroadcaster;`
    3. In SseListener.php, rename the class on line 17:
       Change: `final class WebSocketListener {`
       To:     `final class SseListener {`
    4. Update the docblock comment on lines 11-16: change all "WebSocket" occurrences to "SSE".
       Original: "Listens to domain events and broadcasts them via WebSocket."
       Updated:  "Listens to domain events and broadcasts them via SSE."
       Original: "controllers dispatch events, this listener handles the broadcast."
       (Second line of docblock is fine as-is.)
       The "decouples controllers from the WebSocket layer" line:
       Change: "decouples controllers from the WebSocket layer"
       To:     "decouples controllers from the SSE layer"
    5. Delete app/Event/Listener/WebSocketListener.php.

    All method bodies (onVoteCast, onVoteUpdated, etc.) are unchanged — only the class name, use statement, and docblock.
  </action>
  <verify>
    <automated>
      grep -n "class SseListener" /home/user/gestion_votes_php/app/Event/Listener/SseListener.php &&
      grep -n "use AgVote\\\\SSE\\\\EventBroadcaster" /home/user/gestion_votes_php/app/Event/Listener/SseListener.php &&
      test ! -f /home/user/gestion_votes_php/app/Event/Listener/WebSocketListener.php &&
      grep -c "WebSocket" /home/user/gestion_votes_php/app/Event/Listener/SseListener.php | grep -q "^0$" || echo "FAIL: WebSocket refs remain"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "class SseListener" app/Event/Listener/SseListener.php` matches
    - `grep "use AgVote\\SSE\\EventBroadcaster" app/Event/Listener/SseListener.php` matches
    - `test -f app/Event/Listener/WebSocketListener.php` exits 1 (deleted)
    - `grep -c "WebSocket" app/Event/Listener/SseListener.php` returns 0
  </acceptance_criteria>
  <done>SseListener.php exists with correct class name and use statement; WebSocketListener.php is deleted; zero "WebSocket" strings in the file</done>
</task>

<task type="auto">
  <name>Task 3: Update autoloader (bootstrap.php) and Application.php bootstrap wiring</name>
  <files>app/bootstrap.php, app/Core/Application.php</files>
  <read_first>
    app/bootstrap.php — lines 25-45 (PSR-4 autoloader section)
    app/Core/Application.php — lines 1-20 (use statements) and lines 190-210 (initEventDispatcher)
  </read_first>
  <action>
    In app/bootstrap.php:
    1. Line 32 — change the autoloader registration:
       From: `'AgVote\\WebSocket\\' => __DIR__ . '/WebSocket/',`
       To:   `'AgVote\\SSE\\' => __DIR__ . '/SSE/',`
    2. Line 119 — update the function docblock:
       Change: "Generate an HMAC-signed token for WebSocket authentication."
       To:     "Generate an HMAC-signed token for SSE authentication."
    3. Verify no other "WebSocket" strings remain in bootstrap.php.

    In app/Core/Application.php:
    1. Line 14 — update the use statement:
       From: `use AgVote\Event\Listener\WebSocketListener;`
       To:   `use AgVote\Event\Listener\SseListener;`
    2. Line 198 (approximately) — update the subscribe() call:
       From: `WebSocketListener::subscribe(self::$dispatcher);`
       To:   `SseListener::subscribe(self::$dispatcher);`
    3. Scan for any other "WebSocket" occurrences in Application.php and update them.
  </action>
  <verify>
    <automated>
      grep -n "AgVote\\\\SSE" /home/user/gestion_votes_php/app/bootstrap.php &&
      grep -c "WebSocket" /home/user/gestion_votes_php/app/bootstrap.php | grep -q "^0$" &&
      grep -n "SseListener" /home/user/gestion_votes_php/app/Core/Application.php &&
      grep -c "WebSocket" /home/user/gestion_votes_php/app/Core/Application.php | grep -q "^0$" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "AgVote\\\\SSE\\\\" app/bootstrap.php` matches the autoloader line
    - `grep -c "WebSocket" app/bootstrap.php` returns 0
    - `grep "SseListener" app/Core/Application.php` matches (use statement + call)
    - `grep -c "WebSocket" app/Core/Application.php` returns 0
  </acceptance_criteria>
  <done>bootstrap.php maps AgVote\SSE\ to app/SSE/; Application.php imports and calls SseListener; zero "WebSocket" strings in both files</done>
</task>

</tasks>

<verification>
After all three tasks:
```bash
# New files exist
test -f app/SSE/EventBroadcaster.php && echo "OK: EventBroadcaster in SSE/"
test -f app/Event/Listener/SseListener.php && echo "OK: SseListener exists"

# Old files/dirs gone
test ! -d app/WebSocket/ && echo "OK: WebSocket/ dir deleted"
test ! -f app/Event/Listener/WebSocketListener.php && echo "OK: WebSocketListener deleted"

# Zero WebSocket refs in the 4 touched files
grep -c "WebSocket" app/SSE/EventBroadcaster.php app/Event/Listener/SseListener.php app/bootstrap.php app/Core/Application.php
# All should return 0
```
</verification>

<success_criteria>
- app/SSE/ directory exists, app/WebSocket/ does not
- app/Event/Listener/SseListener.php exists, WebSocketListener.php does not
- app/bootstrap.php autoloads AgVote\SSE\ from app/SSE/
- app/Core/Application.php wires SseListener::subscribe()
- Zero "WebSocket" strings in all four modified files
</success_criteria>

<output>
After completion, create `.planning/phases/58-websocket-to-sse-rename/58-01-SUMMARY.md`
</output>
