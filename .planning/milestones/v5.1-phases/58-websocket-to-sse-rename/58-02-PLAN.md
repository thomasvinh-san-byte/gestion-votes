---
phase: 58-websocket-to-sse-rename
plan: 02
type: execute
wave: 2
depends_on:
  - 58-01
files_modified:
  - app/Controller/AttendancesController.php
  - app/Controller/BallotsController.php
  - app/Controller/MeetingWorkflowController.php
  - app/Controller/MotionsController.php
  - app/Controller/OperatorController.php
  - app/Controller/ResolutionDocumentController.php
  - app/Services/AttendancesService.php
  - app/Services/BallotsService.php
  - app/Repository/AttendanceRepository.php
  - tests/Unit/BallotsServiceTest.php
autonomous: true
requirements:
  - SSE-02
  - SSE-03

must_haves:
  truths:
    - "All 8 controller/service files use AgVote\\SSE\\EventBroadcaster, not AgVote\\WebSocket\\EventBroadcaster"
    - "All [WebSocket] error_log prefixes in controllers and services are renamed to [SSE]"
    - "All inline comments referencing WebSocket broadcast are updated to say SSE"
    - "The PHPUnit suite passes with no test failures related to the rename"
    - "grep -ri 'websocket' app/ returns zero results"
  artifacts:
    - path: "app/Controller/AttendancesController.php"
      provides: "Controller using AgVote\\SSE\\EventBroadcaster"
      contains: "AgVote\\SSE\\EventBroadcaster"
    - path: "app/Services/BallotsService.php"
      provides: "Service using AgVote\\SSE\\EventBroadcaster"
      contains: "AgVote\\SSE\\EventBroadcaster"
    - path: "tests/Unit/BallotsServiceTest.php"
      provides: "Test with SSE terminology, no WebSocket strings"
  key_links:
    - from: "app/Controller/*.php + app/Services/*.php"
      to: "app/SSE/EventBroadcaster.php"
      via: "use AgVote\\SSE\\EventBroadcaster"
      pattern: "use AgVote\\\\SSE\\\\EventBroadcaster"
---

<objective>
Update all remaining references to AgVote\WebSocket\EventBroadcaster across 6 controllers, 2 services, 1 repository comment, and 1 test file. Replace all [WebSocket] error_log prefixes and inline comments with [SSE] / SSE terminology.

Purpose: Complete the rename so zero "WebSocket" strings exist anywhere in app/ (except vendor/). Depends on Plan 01 having established the new app/SSE/EventBroadcaster.php.
Output: 10 files updated; zero "websocket" strings in app/ (case-insensitive).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/58-websocket-to-sse-rename/58-CONTEXT.md
@.planning/phases/58-websocket-to-sse-rename/58-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 output — the new class the executor must reference -->
From app/SSE/EventBroadcaster.php:
```php
namespace AgVote\SSE;

class EventBroadcaster {
    // All static methods unchanged: toMeeting(), toTenant(), motionOpened(),
    // motionClosed(), motionUpdated(), voteCast(), voteUpdated(),
    // attendanceUpdated(), quorumUpdated(), meetingStatusChanged(),
    // speechQueueUpdated()
}
```
The import line to use in every updated file:
```php
use AgVote\SSE\EventBroadcaster;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update use statements in 6 controllers</name>
  <files>
    app/Controller/AttendancesController.php,
    app/Controller/BallotsController.php,
    app/Controller/MeetingWorkflowController.php,
    app/Controller/MotionsController.php,
    app/Controller/OperatorController.php,
    app/Controller/ResolutionDocumentController.php
  </files>
  <read_first>
    Each file listed above — at minimum read the `use` statements block (first 20 lines) and grep for "WebSocket" before editing.
  </read_first>
  <action>
    For EACH of the 6 controller files, perform these replacements:

    1. Replace the use statement:
       From: `use AgVote\WebSocket\EventBroadcaster;`
       To:   `use AgVote\SSE\EventBroadcaster;`

    2. Replace all error_log prefix strings:
       From: `'[WebSocket]`  (with the opening quote)
       To:   `'[SSE]`

    3. Replace inline comments that say "WebSocket broadcast":
       - "Non-critical: WebSocket broadcast failure doesn't affect the response"
         becomes: "Non-critical: SSE broadcast failure doesn't affect the response"
       - Any other comment containing "WebSocket" becomes "SSE"

    Specific known occurrences (verify against actual file content):
    - AttendancesController.php line 9: use statement; line 148: error_log '[WebSocket]'
    - BallotsController.php line 11: use statement; line 186: error_log '[WebSocket]'
    - MeetingWorkflowController.php line 10: use statement; lines 180, 292: error_log '[WebSocket]'
    - MotionsController.php line 11: use statement; lines 489, 565: comment "WebSocket broadcast"
    - OperatorController.php line 9: use statement (verify for comments)
    - ResolutionDocumentController.php line 8: use statement (verify for comments)

    Do NOT change any business logic, method calls on EventBroadcaster, or non-WebSocket content.
  </action>
  <verify>
    <automated>
      grep -c "AgVote\\\\WebSocket" \
        /home/user/gestion_votes_php/app/Controller/AttendancesController.php \
        /home/user/gestion_votes_php/app/Controller/BallotsController.php \
        /home/user/gestion_votes_php/app/Controller/MeetingWorkflowController.php \
        /home/user/gestion_votes_php/app/Controller/MotionsController.php \
        /home/user/gestion_votes_php/app/Controller/OperatorController.php \
        /home/user/gestion_votes_php/app/Controller/ResolutionDocumentController.php
      # All 6 files must return 0
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "AgVote\\\\WebSocket" app/Controller/` returns no files
    - `grep -rl "AgVote\\\\SSE\\\\EventBroadcaster" app/Controller/` returns all 6 files
    - `grep -rl "\[WebSocket\]" app/Controller/` returns no files
  </acceptance_criteria>
  <done>All 6 controllers use AgVote\SSE\EventBroadcaster; no [WebSocket] strings remain in any controller</done>
</task>

<task type="auto">
  <name>Task 2: Update use statements in 2 services and 1 repository comment</name>
  <files>
    app/Services/AttendancesService.php,
    app/Services/BallotsService.php,
    app/Repository/AttendanceRepository.php
  </files>
  <read_first>
    app/Services/AttendancesService.php — read lines 1-20 and grep for "WebSocket"
    app/Services/BallotsService.php — read lines 1-20 and grep for "WebSocket"
    app/Repository/AttendanceRepository.php — read lines 110-125 (the comment)
  </read_first>
  <action>
    In app/Services/AttendancesService.php:
    1. Line 11 — replace use statement:
       From: `use AgVote\WebSocket\EventBroadcaster;`
       To:   `use AgVote\SSE\EventBroadcaster;`
    2. Line 151 — update method comment:
       From: "Broadcast attendance stats via WebSocket."
       To:   "Broadcast attendance stats via SSE."
    3. Line 158 — update error_log prefix:
       From: `error_log('[WebSocket] Broadcast failed after bulk attendance update:`
       To:   `error_log('[SSE] Broadcast failed after bulk attendance update:`

    In app/Services/BallotsService.php:
    1. Line 12 — replace use statement:
       From: `use AgVote\WebSocket\EventBroadcaster;`
       To:   `use AgVote\SSE\EventBroadcaster;`
    2. Line 204 — update inline comment:
       From: "// Broadcast WebSocket event with updated tally (outside transaction)"
       To:   "// Broadcast SSE event with updated tally (outside transaction)"
    3. Line 209 — update error_log prefix:
       From: `error_log('[WebSocket] Broadcast failed after vote cast:`
       To:   `error_log('[SSE] Broadcast failed after vote cast:`

    In app/Repository/AttendanceRepository.php:
    1. Line 116 — update docblock comment:
       From: "Statistics par mode de presence (pour WebSocket broadcast)."
       To:   "Statistics par mode de presence (pour SSE broadcast)."

    Do NOT change any business logic or non-WebSocket content.
  </action>
  <verify>
    <automated>
      grep -c "WebSocket" \
        /home/user/gestion_votes_php/app/Services/AttendancesService.php \
        /home/user/gestion_votes_php/app/Services/BallotsService.php \
        /home/user/gestion_votes_php/app/Repository/AttendanceRepository.php
      # All 3 files must return 0
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WebSocket" app/Services/AttendancesService.php` returns 0
    - `grep -c "WebSocket" app/Services/BallotsService.php` returns 0
    - `grep -c "WebSocket" app/Repository/AttendanceRepository.php` returns 0
    - `grep "use AgVote\\\\SSE\\\\EventBroadcaster" app/Services/AttendancesService.php` matches
    - `grep "use AgVote\\\\SSE\\\\EventBroadcaster" app/Services/BallotsService.php` matches
  </acceptance_criteria>
  <done>Both services import AgVote\SSE\EventBroadcaster; repository comment updated; zero "WebSocket" strings in all three files</done>
</task>

<task type="auto">
  <name>Task 3: Update test file and run PHPUnit to verify no breakage</name>
  <files>tests/Unit/BallotsServiceTest.php</files>
  <read_first>
    tests/Unit/BallotsServiceTest.php — read lines 700-720 (the WebSocket comment block)
  </read_first>
  <action>
    In tests/Unit/BallotsServiceTest.php:
    1. Line 704 — update test comment:
       From: "// castBallot() -- WebSocket broadcast failure does not break vote"
       To:   "// castBallot() -- SSE broadcast failure does not break vote"
    2. Line 711 — update the exception message string:
       From: `->willThrowException(new RuntimeException('WebSocket down'))`
       To:   `->willThrowException(new RuntimeException('SSE down'))`

    After editing the test file, run the PHPUnit suite to confirm zero regressions:
    ```
    cd /home/user/gestion_votes_php && php vendor/bin/phpunit --no-coverage 2>&1 | tail -20
    ```

    If any tests fail, investigate whether the failure is caused by the rename (e.g., a test asserting on the 'WebSocket down' string) and fix accordingly.

    Finally, run the full grep to confirm the rename is complete across all of app/:
    ```
    grep -ri "websocket" /home/user/gestion_votes_php/app/ | grep -v "vendor/"
    ```
    This must return zero results.
  </action>
  <verify>
    <automated>
      grep -c "WebSocket" /home/user/gestion_votes_php/tests/Unit/BallotsServiceTest.php | grep -q "^0$" &&
      grep -ri "websocket" /home/user/gestion_votes_php/app/ | grep -v vendor/ | wc -l | grep -q "^0$" &&
      cd /home/user/gestion_votes_php && php vendor/bin/phpunit --no-coverage 2>&1 | grep -E "^(OK|Tests:)" | head -1
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WebSocket" tests/Unit/BallotsServiceTest.php` returns 0
    - `grep -ri "websocket" app/ | grep -v vendor/` returns zero lines
    - `php vendor/bin/phpunit --no-coverage` exits 0 (test suite passes)
    - PHPUnit output contains "OK" with no failures or errors
  </acceptance_criteria>
  <done>Test file updated; PHPUnit passes; grep -ri "websocket" app/ returns zero results confirming the rename is complete</done>
</task>

</tasks>

<verification>
Final end-to-end verification after both plans complete:
```bash
# Requirement SSE-01: namespace gone
grep -r "AgVote\\\\WebSocket" /home/user/gestion_votes_php/app/ | wc -l
# Must be 0

# Requirement SSE-02: class renamed
grep -r "WebSocketListener" /home/user/gestion_votes_php/app/ | wc -l
# Must be 0

# Requirement SSE-03: zero stale terminology
grep -ri "websocket" /home/user/gestion_votes_php/app/ | wc -l
# Must be 0

# Application boots (autoloader resolves AgVote\SSE\)
php -r "require 'app/bootstrap.php'; use AgVote\SSE\EventBroadcaster; echo 'OK';" 2>&1
# Must output "OK"

# Tests pass
php vendor/bin/phpunit --no-coverage 2>&1 | tail -5
```
</verification>

<success_criteria>
- `grep -ri "websocket" app/` returns zero results (case-insensitive, all terminology eliminated)
- `grep -r "AgVote\\SSE" app/` returns matches in all 10 previously-updated files
- PHPUnit test suite passes with no failures related to the rename
- app/SSE/EventBroadcaster.php is autoloaded correctly (PHP can instantiate via class_exists)
</success_criteria>

<output>
After completion, create `.planning/phases/58-websocket-to-sse-rename/58-02-SUMMARY.md`
</output>
