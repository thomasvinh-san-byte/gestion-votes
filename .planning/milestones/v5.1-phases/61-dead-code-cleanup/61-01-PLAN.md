---
phase: 61-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SETUP.md
  - docs/directive-projet.md
  - phpunit.xml
  - app/Command/EmailProcessQueueCommand.php
  - app/Command/MonitoringCheckCommand.php
  - app/Command/RateLimitCleanupCommand.php
  - app/Command/RedisHealthCommand.php
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]
must_haves:
  truths:
    - "Zero controller stubs remain in production controller code"
    - "No copropriete/syndic vocabulary exists in documentation or seed files"
    - "phpunit.xml source coverage paths reference only existing directories"
    - "All four app/Command files have a docblock comment documenting intentional retention"
    - "Full PHPUnit test suite passes with zero regressions"
  artifacts:
    - path: "SETUP.md"
      provides: "Demo seed documentation without syndic vocabulary"
      contains: "Renouvellement du bureau"
    - path: "docs/directive-projet.md"
      provides: "Project directive without copropriete/syndic references"
      contains: "conseil d'administration"
    - path: "phpunit.xml"
      provides: "Correct coverage source paths"
      contains: "app/SSE"
    - path: "app/Command/EmailProcessQueueCommand.php"
      provides: "CLI tool with retention documentation"
      contains: "CLI tool"
    - path: "app/Command/MonitoringCheckCommand.php"
      provides: "CLI tool with retention documentation"
      contains: "CLI tool"
    - path: "app/Command/RateLimitCleanupCommand.php"
      provides: "CLI tool with retention documentation"
      contains: "CLI tool"
    - path: "app/Command/RedisHealthCommand.php"
      provides: "CLI tool with retention documentation"
      contains: "CLI tool"
  key_links:
    - from: "phpunit.xml"
      to: "app/SSE/EventBroadcaster.php"
      via: "source directory include"
      pattern: "directory.*app/SSE"
---

<objective>
Remove all dead code artifacts from the AG-VOTE codebase: verify zero controller stubs remain (CLEAN-01), purge copropriete/syndic vocabulary from documentation files (CLEAN-02), fix the stale phpunit.xml coverage path left over from the Phase 58 WebSocket-to-SSE rename (CLEAN-03), and document intentional retention of CLI command files (CLEAN-03).

Purpose: Close the v5.1 Operational Hardening milestone with a clean codebase — no stubs, no vocabulary drift, no stale configuration references, and all retained files documented.
Output: Seven files updated (SETUP.md, docs/directive-projet.md, phpunit.xml, 4 Command files), full test suite green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-dead-code-cleanup/61-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge copropriete/syndic vocabulary from documentation and fix phpunit.xml stale path</name>
  <files>SETUP.md, docs/directive-projet.md, phpunit.xml</files>
  <read_first>
    - SETUP.md (full file, focus on line 158)
    - docs/directive-projet.md (full file, focus on lines 58 and 153)
    - phpunit.xml (full file, focus on line 29)
    - app/SSE/EventBroadcaster.php (confirm directory exists)
  </read_first>
  <action>
Make exactly these edits:

**SETUP.md line 158** — Replace copropriete/syndic motion title:
- Before: `  - Changement de syndic — **en attente**`
- After: `  - Renouvellement du bureau — **en attente**`

**docs/directive-projet.md line 58** — Replace syndic use case in majority table:
- Before: `| **Majorité absolue (art. 25)** | >50% des voix de tous les membres | Élection syndic, travaux importants |`
- After: `| **Majorité absolue (art. 25)** | >50% des voix de tous les membres | Élection du président, travaux importants |`

**docs/directive-projet.md line 153** — Replace conseil syndical enum value (already confirmed absent from DB schema — this is stale documentation drift only):
- Before: `├── id, titre, type (enum: AG ordinaire, AG extra, conseil syndical)`
- After: `├── id, titre, type (enum: AG ordinaire, AG extraordinaire, conseil d'administration)`

**phpunit.xml line 29** — Fix stale coverage source directory (app/WebSocket was renamed to app/SSE in Phase 58):
- Before: `<directory suffix=".php">app/WebSocket</directory>`
- After: `<directory suffix=".php">app/SSE</directory>`

IMPORTANT: Do NOT touch any other lines. Do NOT search-replace "lot" — those are legitimate French "batch" references, not vocabulary violations.
  </action>
  <verify>
    <automated>grep -rni "syndic\|copropri" SETUP.md docs/directive-projet.md && echo "FAIL: vocabulary remains" || echo "PASS: no copropriete/syndic vocabulary"; grep -n "WebSocket\|websocket" phpunit.xml && echo "FAIL: stale WebSocket reference" || echo "PASS: phpunit.xml clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rni "syndic\|copropri" SETUP.md docs/directive-projet.md` returns zero matches
    - `grep -n "WebSocket" phpunit.xml` returns zero matches
    - `grep -n "app/SSE" phpunit.xml` returns exactly one match on the source include line
    - `grep "Renouvellement du bureau" SETUP.md` returns one match at line 158
    - `grep "Élection du président" docs/directive-projet.md` returns one match at line 58
    - `grep "conseil d'administration" docs/directive-projet.md` returns one match at line 153
  </acceptance_criteria>
  <done>All three documentation files updated. Zero copropriete/syndic vocabulary remains. phpunit.xml points to app/SSE instead of app/WebSocket.</done>
</task>

<task type="auto">
  <name>Task 2: Verify CLEAN-01 (zero controller stubs) and run full test suite regression check</name>
  <files>None (verification-only task)</files>
  <read_first>
    - app/Controller/ (directory listing to confirm scope)
  </read_first>
  <action>
Run the CLEAN-01 verification grep to confirm zero controller stubs remain in production code. This requirement was already satisfied by earlier phases — this task documents the proof.

**Step 1 — Verify CLEAN-01 (controller stubs):**
Run: `grep -rn "not.implemented\|status.*stub\|TODO\s\|FIXME\s\|return.*\[.*status.*=>.*not\|placeholder" app/Controller/ --include="*.php"`
Expected: zero output (no stubs found).

If any matches are found, investigate each one:
- If it is a genuine stub (returns placeholder/not-implemented response), either implement it or delete the method
- If it is a false positive (e.g., the word "placeholder" in a legitimate context), document why it is not a stub

**Step 2 — Run full PHPUnit suite to confirm zero regressions from Task 1 edits:**
Run: `vendor/bin/phpunit --no-coverage`
Expected: All tests pass (baseline: 2267 tests, 0 failures).

The phpunit.xml edit (app/WebSocket -> app/SSE) changes coverage source paths. While --no-coverage does not exercise source paths directly, this confirms no XML parsing breakage occurred.

**Step 3 — Verify coverage source path works:**
Run: `vendor/bin/phpunit --testsuite Unit --coverage-text 2>&1 | head -5`
Expected: Coverage report generates without errors referencing app/WebSocket.
  </action>
  <verify>
    <automated>grep -rn "not.implemented\|stub\|TODO\s\|FIXME\s\|placeholder" app/Controller/ --include="*.php" | grep -v "// placeholder" | wc -l | xargs -I{} test {} -eq 0 && echo "PASS: CLEAN-01 verified" || echo "FAIL: stubs found"; vendor/bin/phpunit --no-coverage 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "not.implemented\|stub\|TODO\|FIXME" app/Controller/ --include="*.php"` returns zero matches (or only documented false positives)
    - `vendor/bin/phpunit --no-coverage` reports 0 failures, 0 errors
    - No PHP warnings or errors related to phpunit.xml parsing
  </acceptance_criteria>
  <done>CLEAN-01 verified as already satisfied (zero stubs in 41 controller files). Full test suite passes with zero regressions after CLEAN-02 and CLEAN-03 edits.</done>
</task>

<task type="auto">
  <name>Task 3: Document intentional retention of app/Command CLI tools</name>
  <files>app/Command/EmailProcessQueueCommand.php, app/Command/MonitoringCheckCommand.php, app/Command/RateLimitCleanupCommand.php, app/Command/RedisHealthCommand.php</files>
  <read_first>
    - app/Command/EmailProcessQueueCommand.php (first 10 lines)
    - app/Command/MonitoringCheckCommand.php (first 10 lines)
    - app/Command/RateLimitCleanupCommand.php (first 10 lines)
    - app/Command/RedisHealthCommand.php (first 10 lines)
  </read_first>
  <action>
Add a retention documentation comment to each of the 4 files in `app/Command/`. These files have no unit tests and are not called from the main application, but they are legitimate CLI tools that are intentionally retained.

For each file, insert the following comment immediately after the `namespace AgVote\Command;` line (line 5), with a blank line before and after:

```php
// CLI tool — intentionally retained, no unit test required
```

The four files to edit:
1. `app/Command/EmailProcessQueueCommand.php`
2. `app/Command/MonitoringCheckCommand.php`
3. `app/Command/RateLimitCleanupCommand.php`
4. `app/Command/RedisHealthCommand.php`

Do NOT change any other lines in these files. The comment documents the dead-code-audit finding that these are retained by design.
  </action>
  <verify>
    <automated>grep -l "CLI tool" app/Command/*.php | wc -l | xargs -I{} test {} -eq 4 && echo "PASS: all 4 Command files documented" || echo "FAIL: missing retention comments"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "CLI tool" app/Command/*.php | wc -l` returns exactly 4
    - Each of the 4 Command files contains the comment "CLI tool — intentionally retained, no unit test required"
    - No other changes made to these files
  </acceptance_criteria>
  <done>All 4 app/Command files have retention documentation comments. CLEAN-03 dead file audit is fully addressed — stale phpunit.xml path fixed (Task 1) and CLI tools documented as intentionally retained.</done>
</task>

</tasks>

<verification>
Phase-level verification commands:

1. **CLEAN-01 (controller stubs):** `grep -rn "not.implemented\|stub\|TODO\|FIXME" app/Controller/ --include="*.php"` — expect empty output
2. **CLEAN-02 (vocabulary):** `grep -rni "syndic\|copropri" SETUP.md docs/directive-projet.md` — expect empty output
3. **CLEAN-03 (dead references):** `grep -n "WebSocket" phpunit.xml` — expect empty output; `grep -n "app/SSE" phpunit.xml` — expect one match
4. **CLEAN-03 (CLI tool docs):** `grep -l "CLI tool" app/Command/*.php | wc -l` — expect 4
5. **Regression:** `vendor/bin/phpunit --no-coverage` — expect 0 failures
</verification>

<success_criteria>
- Zero copropriete/syndic vocabulary in SETUP.md and docs/directive-projet.md
- phpunit.xml references app/SSE (not app/WebSocket)
- All 4 app/Command files have retention documentation comments
- Zero controller stubs confirmed via grep audit
- Full PHPUnit suite passes (2267+ tests, 0 failures)
- v5.1 milestone requirements CLEAN-01, CLEAN-02, CLEAN-03 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/61-dead-code-cleanup/61-01-SUMMARY.md`
</output>
