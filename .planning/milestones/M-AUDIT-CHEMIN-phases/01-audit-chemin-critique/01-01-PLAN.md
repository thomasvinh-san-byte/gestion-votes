---
phase: 01-audit-chemin-critique
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/CRITICAL-PATH-AUDIT.md
autonomous: true
requirements:
  - AUDIT-CHEMIN-01
  - AUDIT-CHEMIN-02
  - AUDIT-CHEMIN-03
  - AUDIT-CHEMIN-04
  - AUDIT-CHEMIN-05
  - AUDIT-CHEMIN-06
  - AUDIT-CHEMIN-07
  - AUDIT-CHEMIN-08
  - AUDIT-CHEMIN-09
  - AUDIT-CHEMIN-10
  - AUDIT-CHEMIN-11
  - AUDIT-CHEMIN-12
must_haves:
  truths:
    - "12 verdicts statiques (✓/⚠/✗/❓) écrits, un par étape du chemin critique"
    - "Chaque étape liste les Controllers + Services + routes concernés (traçabilité code)"
    - "Chaque étape liste les tests existants couvrant l'étape (PHPUnit + Playwright si présents)"
    - "Chaque étape fournit une procédure live dev-machine reproductible (commandes shell concrètes)"
    - "Chaque étape classe l'impact (bloquant dogfood / bloquant 1.0 / nice-to-have / esthétique)"
    - "Synthèse finale avec tableau récapitulatif + recommandations Stage 2 (audit stack) et Stage 3 (décision Voie A/B/C)"
    - "Aucun fichier de code production n'est modifié (audit only, no fix)"
  artifacts:
    - path: ".planning/CRITICAL-PATH-AUDIT.md"
      provides: "Document d'audit consolidé 12 étapes + synthèse"
      contains: "## Étape 01"
    - path: ".planning/CRITICAL-PATH-AUDIT.md"
      provides: "Synthèse finale Stage 1 → Stage 2/3"
      contains: "## Synthèse"
  key_links:
    - from: ".planning/CRITICAL-PATH-AUDIT.md"
      to: "app/Controller/, app/Services/"
      via: "références fichiers cités par étape"
      pattern: "app/(Controller|Services)/"
    - from: ".planning/CRITICAL-PATH-AUDIT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "couverture des 12 IDs AUDIT-CHEMIN-01..12"
      pattern: "AUDIT-CHEMIN-[0-9]{2}"
---

<objective>
Audit statique sandbox du chemin critique fonctionnel d'AgVote (12 étapes : setup admin → archive). Pour chaque étape, livrer un verdict, les références code (Controllers + Services + routes), les tests existants, une procédure live dev-machine et un classement d'impact.

Purpose : Stage 1 du pivot stratégique 2026-05-05 — prouver E2E où en est le code aujourd'hui avant toute décision de fix / refacto / rebuild (Stage 3). Aucun fix n'est livré ici, juste constat.
Output : `.planning/CRITICAL-PATH-AUDIT.md` (1 fichier unique, ~15-25 pages) consolidant les 12 verdicts + synthèse + recommandations Stage 2/3.

**Boundary stricte** : aucun fichier de `app/`, `public/`, `database/`, `tests/` n'est modifié. Lecture seule du code, écriture uniquement dans `.planning/`.
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/gestion-votes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/intel/EXPLORE-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md
@CLAUDE.md
@app/routes.php
@database/schema-master.sql

<interfaces>
<!-- Inventaire haut-niveau Controllers/Services par étape (extrait `ls app/Controller/`, `ls app/Services/`). -->
<!-- L'executor utilise ces hints comme point de départ ; vérification réelle = lecture du fichier. -->

Controllers principaux (app/Controller/):
- SetupController* (à confirmer présence) / AuthController.php / AccountController.php → étape 01
- ImportController.php / MembersController.php / MemberGroupsController.php → étape 02
- MeetingsController.php / AgendaController.php / MotionsController.php → étape 03
- MeetingWorkflowController.php / OperatorController.php / DashboardController.php → étape 04
- AttendancesController.php → étape 05
- BallotsController.php / VoteEngine → étapes 06-08
- ProxiesController* (via ProxiesService) → étape 08
- MeetingWorkflowController → étape 09 (clôture)
- MeetingReportsController.php / ExportController.php → étape 10 (PV PDF)
- AuditController.php → étape 11

Services principaux (app/Services/):
- VoteEngine.php, QuorumEngine.php → comptages, pondération, quorum (étapes 05-08)
- BallotsService.php → cast vote, idempotence (étapes 06-08)
- MeetingTransitionService.php / MeetingLifecycleService.php / MeetingWorkflowService.php → transitions status (étapes 04, 09, 11)
- MeetingReportService.php / ReportGenerator.php / ProcurationPdfService.php → PV PDF (étape 10)
- CsvImporter.php / XlsxImporter.php / ImportService.php → import membres (étape 02)
- ProxiesService.php → procurations (étape 08)
- OfficialResultsService.php → résultats officiels (étapes 06-07)
- AttendancesService.php → émargement (étape 05)

Schema master : database/schema-master.sql (tables : meetings, motions, ballots, members, attendances, proxies, audit_events avec hash chain prev_hash/this_hash).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 01: Bootstrap CRITICAL-PATH-AUDIT.md (header + structure + étape 01 setup admin)</name>
  <files>.planning/CRITICAL-PATH-AUDIT.md</files>
  <read_first>
    - app/Controller/AccountController.php
    - app/Controller/AuthController.php
    - app/Controller/PasswordResetController.php
    - app/Controller/SetupRedirectException.php
    - app/Services/AdminService.php
    - app/Core/Providers/EnvProvider.php (chemin probable, sinon grep "EnvProvider")
    - app/Core/Providers/SecurityProvider.php (grep si chemin différent)
    - public/setup.html (si présent) ou grep "setup" dans public/*.html
    - app/routes.php (chercher routes /setup, /login, /account)
    - database/schema-master.sql (table users, tenants)
    - tests/Unit/AccountControllerTest.php ou tests/Unit/AuthControllerTest.php (s'ils existent — `ls tests/Unit/ | grep -i -E 'account|auth|setup'`)
  </read_first>
  <action>
Créer le fichier `.planning/CRITICAL-PATH-AUDIT.md` avec en-tête + section étape 01 (AUDIT-CHEMIN-01).

**Header obligatoire** (en français, ASCII pour compat CLAUDE.md "pas d'accents dans le doc Stage" — non, garder accents français pour lisibilité, c'est un doc planning interne) :
```markdown
# Audit Chemin Critique — M-AUDIT-CHEMIN

**Date** : 2026-05-05
**Scope** : audit statique sandbox (lecture code + tests existants). Validation live dev-machine = follow-up.
**Stage** : 1 du pivot stratégique 2026-05-05 (avant Stage 2 audit stack et Stage 3 décision Voie A/B/C).
**Boundary** : aucun fix ici — uniquement constat. Tickets de fix = livrable Stage 3.

**Légende verdict** :
- ✓ : code lu semble OK + tests existants passent + pas de signal de régression
- ⚠ : code OK mais nuances connues (TODO, code récemment refactoré, edge case identifié)
- ✗ : problème détecté en lecture statique (bug visible, dépendance manquante, contrat cassé)
- ❓ : impossible de juger sans exécution live (dépend de runtime data, intégration externe, état DB)

**Légende impact** :
- 🛑 bloquant dogfood : la 1re asso pilote ne peut pas utiliser l'app
- 🔴 bloquant 1.0 shipped : OK pour dogfood mais bloque release publique
- 🟡 nice-to-have : améliore l'expérience sans bloquer
- ⚪ esthétique : polish sans impact fonctionnel

**Verdict global** : (à conclure dans la synthèse, étape 12)

---
```

**Étape 01 — Setup admin vierge (AUDIT-CHEMIN-01)** :
Section ~1-2 pages avec :
- **Description du flow** : install Docker fresh → première navigation → écran setup admin → création compte admin → config tenant initial.
- **Code concerné** : lister les Controllers + Services + routes lus, avec chemin exact `app/Controller/X.php` et 1-2 lignes de rôle chacun.
- **Tests existants** : `ls tests/Unit/ | grep -iE 'account|auth|setup|admin'` puis citer les tests trouvés (ou écrire "aucun test couvrant cette étape" si vide).
- **Verdict statique** : ✓ / ⚠ / ✗ / ❓ avec justification 3-5 phrases (citer code observé, signaux faibles, anti-patterns trouvés via grep si pertinent).
- **Reproduction live (dev-machine)** : bloc bash concret :
  ```bash
  docker compose down -v
  docker compose up -d
  # attendre healthchecks
  curl -I http://localhost:8080/setup
  # ouvrir http://localhost:8080/setup dans navigateur
  # remplir formulaire admin (email, password)
  # vérifier redirection + cookie session + accès dashboard
  ```
- **Impact** : 1 ligne avec emoji + justification.
- **Recommandation** : 1-2 phrases (rien à faire / vérifier en live / ticket à créer Stage 3 / refactoring suggéré).

Format strict : début de section `## Étape 01 — Setup admin vierge (AUDIT-CHEMIN-01)`. L'executor des tasks suivantes ajoute `## Étape NN ...` à la fin du fichier.

Respecter CLAUDE.md : texte français, jamais "copropriété" ou "syndic" (parler "association / collectivité").
  </action>
  <verify>
    <automated>test -f .planning/CRITICAL-PATH-AUDIT.md && grep -c "^## Étape 01" .planning/CRITICAL-PATH-AUDIT.md | grep -q "^1$" && grep -q "AUDIT-CHEMIN-01" .planning/CRITICAL-PATH-AUDIT.md && grep -qE "^\*\*Verdict statique\*\* : (✓|⚠|✗|❓)" .planning/CRITICAL-PATH-AUDIT.md</automated>
  </verify>
  <done>Fichier créé avec header + étape 01 complète (description, code, tests, verdict, repro, impact, reco). 1 verdict posé.</done>
</task>

<task type="auto">
  <name>Task 02: Étape 02 — Import CSV membres (AUDIT-CHEMIN-02)</name>
  <files>.planning/CRITICAL-PATH-AUDIT.md</files>
  <read_first>
    - app/Controller/ImportController.php
    - app/Controller/MembersController.php
    - app/Controller/MemberGroupsController.php
    - app/Services/CsvImporter.php
    - app/Services/XlsxImporter.php
    - app/Services/ImportService.php
    - app/routes.php (routes /api/v1/import*, /api/v1/members*)
    - database/schema-master.sql (tables members, member_groups)
    - tests/Unit/ pour CsvImporter / Import / Members (`ls tests/Unit/ | grep -iE 'import|member|csv|xlsx'`)
  </read_first>
  <action>
Append section `## Étape 02 — Import CSV membres (AUDIT-CHEMIN-02)` au fichier `.planning/CRITICAL-PATH-AUDIT.md`. Format identique à l'étape 01.

**Flow audité** : fixture 50 membres CSV (attributs : nom, email, poids vote, statut, groupe). Vérifier upload, parsing, dédoublonnage (email unique), validation (poids ≥ 0, email valide), persistance.

**Points à investiguer** :
- Format CSV attendu (colonnes obligatoires/optionnelles, séparateur, encoding UTF-8).
- Comportement sur doublons (rejet, merge, écrasement ?).
- Gestion erreurs partielles (ligne 3/50 invalide → rollback total ou partiel ?).
- Limite taille fichier / nombre lignes.

**Reproduction live** : générer un CSV 50 lignes (snippet bash `printf` ou seq), uploader via curl multipart sur la route, vérifier en DB `SELECT count(*) FROM members WHERE tenant_id = ...`.

Verdict + impact + reco. Respecter CLAUDE.md (français, pas de "copropriété").
  </action>
  <verify>
    <automated>grep -c "^## Étape 02" .planning/CRITICAL-PATH-AUDIT.md | grep -q "^1$" && grep -q "AUDIT-CHEMIN-02" .planning/CRITICAL-PATH-AUDIT.md</automated>
  </verify>
  <done>Section étape 02 ajoutée avec les 7 sous-sections obligatoires + verdict.</done>
</task>

<task type="auto">
  <name>Task 03: Étape 03 — Création séance + ordre du jour (AUDIT-CHEMIN-03)</name>
  <files>.planning/CRITICAL-PATH-AUDIT.md</files>
  <read_first>
    - app/Controller/MeetingsController.php
    - app/Controller/AgendaController.php
    - app/Controller/MotionsController.php
    - app/Services/MeetingValidator.php
    - app/Services/MotionsService.php
    - app/routes.php (routes /api/v1/meetings*, /api/v1/agenda*, /api/v1/motions*)
    - database/schema-master.sql (tables meetings, agenda_items, motions)
    - tests/Unit/ pour Meeting / Motion / Agenda
  </read_first>
  <action>
Append section `## Étape 03 — Création séance + ordre du jour (AUDIT-CHEMIN-03)`.

**Flow** : wizard création séance (titre, date, type), ajout 3+ motions (1 résolution Pour/Contre/Abstention, 1 élection multi-candidats, 1 question ouverte). Vérifier persistance, ordre, validation.

**Points** : types de motion supportés, validation min/max candidats pour élection, ordre de l'agenda éditable, contraintes (pas de motion si séance != draft).

**Reproduction live** : suite de POST curl avec session admin (créer séance, ajouter 3 motions, GET pour vérifier).

Verdict + impact + reco.
  </action>
  <verify>
    <automated>grep -c "^## Étape 03" .planning/CRITICAL-PATH-AUDIT.md | grep -q "^1$" && grep -q "AUDIT-CHEMIN-03" .planning/CRITICAL-PATH-AUDIT.md</automated>
  </verify>
  <done>Section étape 03 ajoutée.</done>
</task>

<task type="auto">
  <name>Task 04: Étape 04 — Ouverture séance live (AUDIT-CHEMIN-04)</name>
  <files>.planning/CRITICAL-PATH-AUDIT.md</files>
  <read_first>
    - app/Controller/MeetingWorkflowController.php
    - app/Controller/OperatorController.php
    - app/Controller/DashboardController.php
    - app/Services/MeetingTransitionService.php
    - app/Services/MeetingLifecycleService.php
    - app/Services/MeetingWorkflowService.php
    - app/Services/OperatorWorkflowService.php
    - app/routes.php (routes /api/v1/meetings/*/transition, /api/v1/meetings/*/freeze, etc.)
    - database/schema-master.sql (colonne meetings.status, énum)
    - tests/Unit/ pour MeetingTransition / MeetingWorkflow / Operator
  </read_first>
  <action>
Append section `## Étape 04 — Ouverture séance live (AUDIT-CHEMIN-04)`.

**Flow** : transition `draft → frozen → live`. Vérifier guards (qui peut, quand), idempotence, accessibilité du cockpit opérateur après passage live, données chargées (membres, motions, présence vide).

**Points** : irréversibilité partielle (frozen → draft permis ? live → frozen ?), audit_events émis, événements SSE diffusés, état initial cockpit.

**Reproduction live** : POST sur /transition avec différents states, vérifier 200/409/422 selon contexte, GET /cockpit.

Verdict + impact + reco.
  </action>
  <verify>
    <automated>grep -c "^## Étape 04" .planning/CRITICAL-PATH-AUDIT.md | grep -q "^1$" && grep -q "AUDIT-CHEMIN-04" .planning/CRITICAL-PATH-AUDIT.md</automated>
  </verify>
  <done>Section étape 04 ajoutée.</done>
</task>

<task type="auto">
  <name>Task 05: Étape 05 — Émargement présence + quorum (AUDIT-CHEMIN-05)</name>
  <files>.planning/CRITICAL-PATH-AUDIT.md</files>
  <read_first>
    - app/Controller/AttendancesController.php
    - app/Services/AttendancesService.php
    - app/Services/QuorumEngine.php
    - app/routes.php (routes /api/v1/attendances*, /api/v1/quorum*)
    - database/schema-master.sql (table attendances, colonnes weight)
    - tests/Unit/QuorumEngineTest.php
    - tests/Unit/AttendancesServiceTest.php (si existe)
  </read_first>
  <action>
Append section `## Étape 05 — Émargement présence + quorum (AUDIT-CHEMIN-05)`.

**Flow** : marquer 30/50 membres présents (manuellement ou via QR/token), QuorumEngine calcule quorum (pondération respectée).

**Points** : algorithme quorum (% membres présents OU % poids cumulé ?), gestion procurations dans calcul présence (porteur compte pour 1+1 ?), seuils configurables tenant, atteinte du quorum déclenche-t-elle un event ?

**Stress test (si possible static)** : grep pour confirmer que QuorumEngine itère sur tous les membres et somme les poids, pas un comptage flat.

**Reproduction live** : seed 50 membres, marquer 30 présents (curl boucle), GET /quorum, vérifier `quorum_met=true` et `weighted_total` cohérent.

Verdict + impact + reco.
  </action>
  <verify>
    <automated>grep -c "^## Étape 05" .planning/CRITICAL-PATH-AUDIT.md | grep -q "^1$" && grep -q "AUDIT-CHEMIN-05" .planning/CRITICAL-PATH-AUDIT.md</automated>
  </verify>
  <done>Section étape 05 ajoutée.</done>
</task>

<task type="auto">
  <name>Task 06: Étape 06 — Vote motion résolution simple (AUDIT-CHEMIN-06)</name>
  <files>.planning/CRITICAL-PATH-AUDIT.md</files>
  <read_first>
    - app/Controller/BallotsController.php
    - app/Controller/MotionsController.php
    - app/Services/BallotsService.php
    - app/Services/VoteEngine.php
    - app/Services/OfficialResultsService.php
    - app/routes.php (routes /api/v1/ballots*, /api/v1/motions/*/open, /close)
    - database/schema-master.sql (table ballots, motion.kind)
    - tests/Unit/VoteEngineTest.php (si existe)
    - tests/Unit/BallotsServiceTest.php (si existe)
  </read_first>
  <action>
Append section `## Étape 06 — Vote motion résolution simple (AUDIT-CHEMIN-06)`.

**Flow** : ouvrir vote d'une motion résolution (Pour/Contre/Abstention), votants envoient leur ballot, fermer le vote, VoteEngine compte avec pondération.

**Points** : idempotence (même votant 2x → 1 ballot ou rejet ?), vote anonyme vs nominatif, transition state motion (pending → open → closed), audit_events par ballot, événement SSE de progression.

**Reproduction live** : ouvrir motion via POST, 30 votants émettent /ballots (boucle curl avec sessions différentes ou tokens), fermer via POST, GET /results.

Verdict + impact + reco. Vérifier que le résultat respecte la pondération (pas juste compter les votants).
  </action>
  <verify>
    <automated>grep -c "^## Étape 06" .planning/CRITICAL-PATH-AUDIT.md | grep -q "^1$" && grep -q "AUDIT-CHEMIN-06" .planning/CRITICAL-PATH-AUDIT.md</automated>
  </verify>
  <done>Section étape 06 ajoutée.</done>
</task>

<task type="auto">
  <name>Task 07: Étape 07 — Vote motion élection multi-candidats (AUDIT-CHEMIN-07)</name>
  <files>.planning/CRITICAL-PATH-AUDIT.md</files>
  <read_first>
    - app/Services/VoteEngine.php (méthodes spécifiques élection — grep "election|candidate|stv|majoritaire")
    - app/Services/OfficialResultsService.php
    - app/Controller/MotionsController.php (kind=election)
    - app/Controller/BallotsController.php
    - database/schema-master.sql (motion.kind enum, ballots.choices structure JSON ?)
    - tests/Unit/ (grep "election" dans tests)
  </read_first>
  <action>
Append section `## Étape 07 — Vote motion élection multi-candidats (AUDIT-CHEMIN-07)`.

**Flow** : motion type "élection" avec N candidats, M sièges, votants choisissent K candidats (K ≤ M ou autre règle). Calcul résultats (scrutin majoritaire à 1 tour ? STV ? approbation ?).

**Points cruciaux** :
- Quel mode de scrutin est implémenté ? (chercher dans VoteEngine + OfficialResultsService)
- Validation côté serveur (votant ne peut pas choisir > M candidats)
- Égalité (départage : tirage au sort, ancienneté, ?)
- Pondération : 1 électeur pondéré 5 vote pour 3 candidats — chaque candidat reçoit 5 ou 5/3 ?

**Reproduction live** : créer motion election, 3-5 candidats, ouvrir vote, votants émettent choix, fermer, vérifier classement.

Verdict + impact + reco. Étape probable ❓ ou ⚠ — l'élection est souvent moins testée que la résolution.
  </action>
  <verify>
    <automated>grep -c "^## Étape 07" .planning/CRITICAL-PATH-AUDIT.md | grep -q "^1$" && grep -q "AUDIT-CHEMIN-07" .planning/CRITICAL-PATH-AUDIT.md</automated>
  </verify>
  <done>Section étape 07 ajoutée.</done>
</task>

<task type="auto">
  <name>Task 08: Étape 08 — Vote avec procuration active (AUDIT-CHEMIN-08)</name>
  <files>.planning/CRITICAL-PATH-AUDIT.md</files>
  <read_first>
    - app/Services/ProxiesService.php
    - app/Services/ProcurationPdfService.php
    - app/Services/VoteEngine.php (méthodes proxy — grep "proxy|procuration")
    - app/Services/QuorumEngine.php (impact proxy sur quorum)
    - app/Controller/ (chercher ProxiesController ou similaire)
    - app/routes.php (routes /api/v1/proxies*)
    - database/schema-master.sql (table proxies)
    - tests/Unit/ (grep "proxy|procuration")
  </read_first>
  <action>
Append section `## Étape 08 — Vote avec procuration active (AUDIT-CHEMIN-08)`.

**Flow** : membre A donne procuration à membre B. B est présent, A absent. B vote sur motion → comptage doit appliquer poids(A)+poids(B), pas dédoubler.

**Points cruciaux** :
- Limites légales (1 procuration max par porteur ? 3 ? configurable ?).
- Révocation procuration (A revient en cours de séance, B peut-il encore voter pour A ?).
- Procuration transitive interdite (A→B, B→C → C vote pour 3 ?).
- Audit trail procuration (qui a donné, à qui, quand, signé comment ?).
- Si A est présent ET a donné procuration à B → la procuration tombe automatiquement ? B est notifié ?

**Reproduction live** : créer 2 membres avec poids 1, A donne procuration à B (POST /proxies), marquer B présent et A absent, B vote → résultat doit montrer 2 voix pour le choix de B.

Verdict + impact + reco. Étape sensible — vraisemblablement ⚠ ou ❓.
  </action>
  <verify>
    <automated>grep -c "^## Étape 08" .planning/CRITICAL-PATH-AUDIT.md | grep -q "^1$" && grep -q "AUDIT-CHEMIN-08" .planning/CRITICAL-PATH-AUDIT.md</automated>
  </verify>
  <done>Section étape 08 ajoutée.</done>
</task>

<task type="auto">
  <name>Task 09: Étape 09 — Clôture séance (AUDIT-CHEMIN-09)</name>
  <files>.planning/CRITICAL-PATH-AUDIT.md</files>
  <read_first>
    - app/Controller/MeetingWorkflowController.php
    - app/Services/MeetingTransitionService.php
    - app/Services/MeetingLifecycleService.php
    - app/routes.php (routes /api/v1/meetings/*/close)
    - database/schema-master.sql (transitions valides depuis live)
    - tests/Unit/MeetingTransitionServiceTest.php (si existe)
  </read_first>
  <action>
Append section `## Étape 09 — Clôture séance (AUDIT-CHEMIN-09)`.

**Flow** : transition `live → closed`. Lock motions (plus de votes), interdiction d'émargement, audit_event émis, état "closed" affiché côté UI.

**Points** :
- Que se passe-t-il pour une motion encore "open" au moment du close ? (Auto-close ? Erreur ? Forcing ?)
- Idempotence (close 2x = 200 ou 409 ?).
- Réouverture interdite (closed → live ? closed → frozen ?).
- Side effects : email/notifs envoyés ? Stats calculées ?

**Reproduction live** : POST /close après vote terminé, vérifier 200, retenter POST → vérifier 409, tenter POST /ballots → vérifier 403/422.

Verdict + impact + reco.
  </action>
  <verify>
    <automated>grep -c "^## Étape 09" .planning/CRITICAL-PATH-AUDIT.md | grep -q "^1$" && grep -q "AUDIT-CHEMIN-09" .planning/CRITICAL-PATH-AUDIT.md</automated>
  </verify>
  <done>Section étape 09 ajoutée.</done>
</task>

<task type="auto">
  <name>Task 10: Étape 10 — Génération PV PDF (AUDIT-CHEMIN-10)</name>
  <files>.planning/CRITICAL-PATH-AUDIT.md</files>
  <read_first>
    - app/Controller/MeetingReportsController.php
    - app/Controller/ExportController.php
    - app/Services/MeetingReportService.php
    - app/Services/MeetingReportsService.php
    - app/Services/ReportGenerator.php
    - app/Services/ProcurationPdfService.php
    - app/Services/ExportService.php
    - app/routes.php (routes /api/v1/reports*, /api/v1/exports*, /api/v1/meetings/*/pv)
    - composer.json (vérifier dompdf/dompdf version)
    - tests/Unit/ pour MeetingReport / ReportGenerator
  </read_first>
  <action>
Append section `## Étape 10 — Génération PV PDF (AUDIT-CHEMIN-10)`.

**Flow** : générer PV PDF (≥5 pages avec contenu varié — résolution + élection + procuration). Vérifier header, footer, accents UTF-8 (é/è/ç/à), pagination, signature placeholder, contenu cohérent avec la séance.

**Points cruciaux** :
- Charset DOMPDF (souvent piège : sans `<meta charset="utf-8">` les accents cassent).
- Police embarquée (DejaVuSans typiquement, sinon caractères perdus).
- Performance (génération PV 5 pages doit être < 5 sec).
- Template du PV : où est-il ? (chercher `app/Templates/` ou `public/templates/`).
- Signature placeholder : zone réservée ? Hash du contenu déjà calculé ?
- Numérotation des pages.

**Reproduction live** : depuis séance closed, GET /pv (ou POST /reports/generate), sauver PDF, ouvrir avec viewer, inspecter visuellement (pagination + accents + structure).

**Static check** : grep "DejaVu\|charset\|utf-8" dans services PDF, vérifier qu'il y a setup explicite.

Verdict + impact + reco. Étape critique pour Core Value (traçabilité ≥ papier) — ⚠ ou ✓ probable.
  </action>
  <verify>
    <automated>grep -c "^## Étape 10" .planning/CRITICAL-PATH-AUDIT.md | grep -q "^1$" && grep -q "AUDIT-CHEMIN-10" .planning/CRITICAL-PATH-AUDIT.md</automated>
  </verify>
  <done>Section étape 10 ajoutée.</done>
</task>

<task type="auto">
  <name>Task 11: Étape 11 — Archive + audit hash chain (AUDIT-CHEMIN-11)</name>
  <files>.planning/CRITICAL-PATH-AUDIT.md</files>
  <read_first>
    - app/Controller/AuditController.php
    - app/Services/MeetingTransitionService.php (transitions closed → validated → archived)
    - app/Services/MeetingLifecycleService.php
    - app/Core/ (chercher classe gérant audit_log et hash chain — `grep -rn "this_hash\|prev_hash" app/`)
    - app/api.php (fonction globale audit_log)
    - app/routes.php (routes /api/v1/audit*)
    - database/schema-master.sql (table audit_events, colonnes prev_hash, this_hash, payload, created_at)
    - tests/Unit/ pour Audit / hash chain (`ls tests/Unit/ | grep -iE 'audit|hash'`)
  </read_first>
  <action>
Append section `## Étape 11 — Archive + audit hash chain (AUDIT-CHEMIN-11)`.

**Flow** : `closed → validated → archived`. Vérifier hash chain `audit_events` cohérent (chaque ligne a `prev_hash` = `this_hash` de la ligne précédente du même tenant). Tentative de modif post-archive bloquée.

**Points cruciaux** :
- Algo hash : sha256 sur quoi exactement ? (`prev_hash || canonical_payload || timestamp` ?)
- Premier event d'un tenant : `prev_hash = NULL` ou `'0x00...'` ?
- Détection rupture chaîne : y a-t-il un endpoint/job de vérification ? (`/api/v1/audit/verify` ?)
- Validation script : SQL ou PHP ?
- Modif post-archive : vérifier qu'un UPDATE/DELETE sur ballots/meetings échoue (trigger DB ou guard service ?).

**Reproduction live** :
1. Transition validated puis archived.
2. SQL : `SELECT id, prev_hash, this_hash FROM audit_events WHERE tenant_id = ... ORDER BY created_at` — vérifier chaîne intacte.
3. Tenter UPDATE ballots → vérifier rejet.
4. Si endpoint verify existe : GET /api/v1/audit/verify → 200 + chain_ok=true.

**Static check** :
- `grep -rn "this_hash\|prev_hash" app/ database/` pour confirmer où la chaîne est calculée.
- Chercher trigger SQL dans `database/migrations/*.sql`.

Verdict + impact + reco. Critique pour Core Value (traçabilité légale) — ⚠ ou ✓ probable, ✗ si chaîne fragile.
  </action>
  <verify>
    <automated>grep -c "^## Étape 11" .planning/CRITICAL-PATH-AUDIT.md | grep -q "^1$" && grep -q "AUDIT-CHEMIN-11" .planning/CRITICAL-PATH-AUDIT.md</automated>
  </verify>
  <done>Section étape 11 ajoutée.</done>
</task>

<task type="auto">
  <name>Task 12: Synthèse finale (AUDIT-CHEMIN-12) + recommandations Stage 2/3</name>
  <files>.planning/CRITICAL-PATH-AUDIT.md</files>
  <read_first>
    - .planning/CRITICAL-PATH-AUDIT.md (re-lire les 11 étapes pour synthétiser)
    - .planning/PROJECT.md (rappel Core Value pivot)
    - .planning/ROADMAP.md (M-AUDIT-STACK + M-DECISION pour caler reco)
  </read_first>
  <action>
Append section `## Synthèse (AUDIT-CHEMIN-12)` au fichier `.planning/CRITICAL-PATH-AUDIT.md`. Cette section consolide les 11 verdicts précédents.

**Contenu obligatoire** :

### Tableau récapitulatif
Tableau Markdown 4 colonnes :
| # | Étape | Verdict | Impact |
|---|-------|---------|--------|
| 01 | Setup admin vierge | ✓ ou ⚠ ou ✗ ou ❓ | 🛑/🔴/🟡/⚪ |
| ... | ... | ... | ... |
| 11 | Archive + audit hash chain | ... | ... |

### Compteurs
**Total ✓** : N | **⚠** : M | **✗** : X | **❓** : Y (avec N+M+X+Y = 11)

### Bloquants identifiés
Liste à puces des étapes 🛑 et 🔴 avec 1 ligne de résumé chacune. Si aucun, écrire "Aucun bloquant détecté en lecture statique".

### Non-bloquants
Liste à puces des étapes 🟡 et ⚪.

### Inconnus (live required)
Liste des étapes ❓ avec ce qu'il faut tester en live pour trancher.

### Verdict global Stage 1
Conclusion 1 paragraphe : le chemin critique est-il fonctionnel en sandbox lecture ? Quels sont les 2-3 risques majeurs identifiés ?

### Recommandation pour Stage 2 (audit stack)
Quelles dépendances ou composants méritent priorité dans M-AUDIT-STACK (ex : DOMPDF si étape 10 fragile, custom Router si étape 04 a des bugs de routing, hash chain si étape 11 douteuse) ?

### Recommandation pour Stage 3 (décision Voie A/B/C)
Sur la base des verdicts :
- **Voie A (refacto sur place)** est-elle envisageable ? Quelles étapes seraient les plus coûteuses à refacto ?
- **Voie B (rebuild partiel infra)** : quels modules à reconstruire ?
- **Voie C (rebuild from scratch)** : quels signaux pousseraient à cette voie ?
Donner une recommandation préférée avec 2-3 arguments.

### Boundary respectée
Confirmer en fin de doc : "Cet audit n'a modifié aucun fichier de production. Tickets de fix = livrable Stage 3 (M-DECISION)."

Format strict, français, jamais "copropriété/syndic".
  </action>
  <verify>
    <automated>grep -c "^## Synthèse" .planning/CRITICAL-PATH-AUDIT.md | grep -q "^1$" && grep -q "AUDIT-CHEMIN-12" .planning/CRITICAL-PATH-AUDIT.md && grep -q "Stage 2" .planning/CRITICAL-PATH-AUDIT.md && grep -q "Stage 3" .planning/CRITICAL-PATH-AUDIT.md && grep -qE "Voie [AB]" .planning/CRITICAL-PATH-AUDIT.md</automated>
  </verify>
  <done>Synthèse présente avec tableau 11 lignes, compteurs, bloquants, recommandations Stage 2 et Stage 3 explicites.</done>
</task>

<task type="auto">
  <name>Task 13: Coverage check + boundary verification</name>
  <files>.planning/CRITICAL-PATH-AUDIT.md</files>
  <read_first>
    - .planning/CRITICAL-PATH-AUDIT.md
  </read_first>
  <action>
Vérifier que le document final couvre intégralement les 12 requirements et respecte la boundary "audit only".

**Checks à exécuter** (commandes ci-dessous, corriger le doc si une assertion échoue) :

1. Couverture des 12 IDs : chaque `AUDIT-CHEMIN-NN` (01..12) doit apparaître au moins 1 fois.
2. 11 sections d'étape + 1 synthèse = 12 sections H2 commençant par `## Étape` ou `## Synthèse`.
3. Aucun fichier de production touché (la boundary du plan).

Si un check échoue, ajouter/corriger directement dans le doc. Ne pas créer de fichier supplémentaire.

Aucun commit ici — le commit final est géré par execute-phase.
  </action>
  <verify>
    <automated>for n in 01 02 03 04 05 06 07 08 09 10 11 12; do grep -q "AUDIT-CHEMIN-$n" .planning/CRITICAL-PATH-AUDIT.md || { echo "MISSING AUDIT-CHEMIN-$n"; exit 1; }; done && [ "$(grep -cE '^## (Étape|Synthèse)' .planning/CRITICAL-PATH-AUDIT.md)" = "12" ] && [ -z "$(git status --porcelain app/ public/ database/ tests/ composer.json composer.lock 2>/dev/null)" ]</automated>
  </verify>
  <done>Les 12 IDs présents, 12 sections H2 (11 étapes + synthèse), aucun fichier de prod modifié.</done>
</task>

</tasks>

<verification>
Vérifications globales du plan :

1. `test -f .planning/CRITICAL-PATH-AUDIT.md`
2. `for n in 01 02 03 04 05 06 07 08 09 10 11 12; do grep -q "AUDIT-CHEMIN-$n" .planning/CRITICAL-PATH-AUDIT.md; done`
3. `grep -cE '^## (Étape|Synthèse)' .planning/CRITICAL-PATH-AUDIT.md` retourne `12`
4. `grep -cE '^\*\*Verdict statique\*\* :' .planning/CRITICAL-PATH-AUDIT.md` retourne au moins `11`
5. `grep -q "Stage 2" .planning/CRITICAL-PATH-AUDIT.md && grep -q "Stage 3" .planning/CRITICAL-PATH-AUDIT.md`
6. Boundary : `git status --porcelain app/ public/ database/ tests/` est vide
7. CLAUDE.md compliance : `! grep -iE 'copropriété|copropriete|syndic' .planning/CRITICAL-PATH-AUDIT.md`
</verification>

<success_criteria>
- `.planning/CRITICAL-PATH-AUDIT.md` existe (~15-25 pages, document unique)
- 11 sections d'étape complètes avec : description du flow, code concerné, tests existants, verdict ✓/⚠/✗/❓ justifié, procédure live dev-machine, impact, recommandation
- 1 section synthèse avec tableau récapitulatif, compteurs, bloquants, recommandations Stage 2 et Stage 3
- 12 IDs `AUDIT-CHEMIN-01..12` tous référencés
- Aucun fichier de production (`app/`, `public/`, `database/`, `tests/`, `composer.*`) modifié
- Texte français, sans "copropriété" ni "syndic" (CLAUDE.md)
</success_criteria>

<output>
After completion, create `.planning/phases/01-audit-chemin-critique/01-01-SUMMARY.md` selon le template SUMMARY standard, en incluant :
- Verdict global de l'audit (✓/⚠/✗/❓ majoritaire)
- Compteurs synthèse (N ✓, M ⚠, X ✗, Y ❓)
- Top 3 bloquants identifiés (ou "aucun")
- Recommandation Stage 3 (Voie préférée et pourquoi)
- Lien vers `.planning/CRITICAL-PATH-AUDIT.md`
</output>
