---
phase: 02-form-layout-modernization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/members.htmx.html
  - public/users.htmx.html
  - public/meetings.htmx.html
  - public/admin.htmx.html
  - public/validate.htmx.html
autonomous: true
requirements: [FORM-01, FORM-02, FORM-03]

must_haves:
  truths:
    - "Le formulaire de creation membre (name, email, active, button) affiche en grille horizontale exploitant la largeur"
    - "Le modal utilisateur (name, email, password, role) affiche en form-grid-2 sur ecrans larges"
    - "Le modal d'edition seance (title, date, description) affiche en form-grid-2"
    - "Le formulaire admin (create user inline) exploite la largeur avec une grille"
    - "Tous les inputs/selects/textareas sur ces 5 pages utilisent les classes form-input/form-select/form-textarea"
  artifacts:
    - path: "public/members.htmx.html"
      provides: "Grid-based member creation form"
      contains: "form-grid"
    - path: "public/users.htmx.html"
      provides: "Grid layout for user modal form"
      contains: "form-grid-2"
    - path: "public/meetings.htmx.html"
      provides: "Grid layout for meeting edit modal"
      contains: "form-grid-2"
    - path: "public/admin.htmx.html"
      provides: "Grid layout for admin create form"
      contains: "form-grid"
    - path: "public/validate.htmx.html"
      provides: "Consistent field classes on validate form"
      contains: "form-input"
  key_links:
    - from: "public/members.htmx.html"
      to: "public/assets/css/design-system.css"
      via: "form-grid-2 or form-grid classes"
      pattern: "form-grid"
    - from: "public/users.htmx.html"
      to: "public/assets/css/design-system.css"
      via: "form-grid-2 class on modal form"
      pattern: "form-grid-2"
---

<objective>
Apply multi-column grid layouts and normalize field classes on 5 medium-form pages: members, users, meetings, admin, and validate.

Purpose: These pages have meaningful form sections (create forms, edit modals) that benefit from horizontal layout. Members and users are the most-used data entry pages.
Output: 5 HTML files with form-grid layouts and consistent field class usage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-form-layout-modernization/02-CONTEXT.md
@public/assets/css/design-system.css (lines 1803-2020 — form-grid, form-group, field class definitions)
</context>

<interfaces>
<!-- CSS classes available from design-system.css -->

From public/assets/css/design-system.css:
```css
.form-grid-2 {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-field) 12px;
}
.form-grid-2 .form-group-full { grid-column: 1 / -1; }

.form-grid-3 {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-field) 12px;
}

.form-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  gap: var(--gap-md, 16px);
}
.form-grid .form-group-full { grid-column: 1 / -1; }
```

From public/assets/css/members.css:
```css
/* .create-form already has its own grid: grid-template-columns: 2fr 1.5fr 1fr auto auto;
   This already provides a multi-column horizontal layout.
   If it works well, leave it. Only ensure field class consistency. */
.create-form {
  display: grid;
  grid-template-columns: 2fr 1.5fr 1fr auto auto;
  column-gap: var(--space-3);
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Grid layouts for members, users, and meetings pages</name>
  <files>public/members.htmx.html, public/users.htmx.html, public/meetings.htmx.html</files>
  <action>
**members.htmx.html:**

1. **Create member form** (lines ~152-171): The `.create-form` already has a custom 5-column grid from members.css (`2fr 1.5fr 1fr auto auto`). This is already a multi-column horizontal layout — do NOT replace it. Just verify field classes are correct.

2. **Group create form** (lines ~266-270): Has name input + color picker. These 2 fields should be in a `form-grid-2` wrapper if not already horizontal.

3. **Normalize field classes**: Change any `<select class="form-input">` to `<select class="form-select">`. The sort select (line ~215) and pagination select (line ~247) currently use `form-input` — change to `form-select`.

**users.htmx.html:**

1. **User modal form** (lines ~168-191, 4 form-groups: name, email, password, role): Wrap these 4 form-groups in a `form-grid-2` container. Name and email go side-by-side. Password and role go side-by-side. This exploits horizontal space in the modal.

2. **Normalize field classes**: Change `<select class="form-input" id="modalUserRole">` to `<select class="form-select" id="modalUserRole">`.

3. **Search bar**: The search input (line ~109) is a toolbar element — leave inline. Ensure it uses `form-input`.

**meetings.htmx.html:**

1. **Edit meeting modal** (lines ~194-202, 3 form-groups: title, date, description): Wrap title+date in `form-grid-2`. If there is a description textarea, mark it `form-group-full`.

2. **Search/sort bar** (lines ~112-116): These are toolbar filters — leave inline. Change `<select class="form-input">` to `<select class="form-select">` for the sort select.

3. **Normalize field classes**: Ensure all selects use `form-select`, all textareas use `form-textarea`.

IMPORTANT: Do not break existing IDs, data attributes, event handlers, or HTMX attributes. Only modify CSS class attributes and add wrapper divs.
  </action>
  <verify>
    <automated>php -r "foreach(['public/members.htmx.html','public/users.htmx.html','public/meetings.htmx.html'] as \$f){echo \$f.': '; \$c=file_get_contents(\$f); echo substr_count(\$c,'form-grid').' grids, '; echo substr_count(\$c,'form-select').' form-selects'; echo PHP_EOL;}"</automated>
  </verify>
  <done>Users modal has form-grid-2 on the 4-field user form. Meetings modal has form-grid-2 on title+date. Members create form keeps its custom grid. All selects use form-select class on all 3 pages.</done>
</task>

<task type="auto">
  <name>Task 2: Grid layouts for admin and validate pages</name>
  <files>public/admin.htmx.html, public/validate.htmx.html</files>
  <action>
**admin.htmx.html:**

1. **Inline create user form** (lines ~123-132): Has name, email, password, role fields in a `create-form` container. Check if `.create-form` from pages.css provides horizontal layout. If fields are stacking vertically, wrap the form-groups in a `form-grid-2` or add `form-grid` to the container.

2. **Search/filter bar** (lines ~108-110): Search input + role filter select are toolbar items. Ensure select uses `form-select` class.

3. **Normalize field classes**: Change any `<select class="form-input">` to `<select class="form-select">`.

**validate.htmx.html:**

1. **President name field** (lines ~138-142): Single input in a form-group — this is a standalone field, no grid needed. Ensure it uses `form-input`.

2. **Confirm text field** (lines ~223-225): Single confirmation input — standalone, no grid needed. Ensure it uses `form-input`.

3. **Width constraint check**: Both forms are contextual (inside cards/sections). If either form section is full-width single-column, add `max-width: 60%` via inline style or ensure the parent card constrains width. Per FORM-03, no single-column form should exceed 60% viewport width. If the card already constrains, this is satisfied.

4. **Normalize field classes**: Verify all inputs use `form-input`.

IMPORTANT: Do not break existing IDs, data attributes, event handlers, or HTMX attributes.
  </action>
  <verify>
    <automated>php -r "foreach(['public/admin.htmx.html','public/validate.htmx.html'] as \$f){echo \$f.': '; \$c=file_get_contents(\$f); echo substr_count(\$c,'form-grid').' grids, '; echo substr_count(\$c,'form-select').' form-selects, '; echo substr_count(\$c,'form-input').' form-inputs'; echo PHP_EOL;}"</automated>
  </verify>
  <done>Admin create form uses horizontal grid layout. Validate forms have consistent field classes. All selects use form-select. No single-column form exceeds 60% viewport width.</done>
</task>

</tasks>

<verification>
1. Open members page — create form stays horizontal, selects use form-select
2. Open users page — user modal shows 2-column form layout (name+email, password+role)
3. Open meetings page — edit modal shows title+date side-by-side
4. Open admin page — create user form is horizontal
5. Open validate page — fields use correct form-input class
6. Grep confirms zero `<select class="form-input"` on these 5 pages
</verification>

<success_criteria>
- All 5 pages have appropriate grid layouts on multi-field forms
- All select elements use form-select class
- All textarea elements use form-textarea class
- No existing IDs, HTMX attributes, or JS hooks are broken
</success_criteria>

<output>
After completion, create `.planning/phases/02-form-layout-modernization/02-02-SUMMARY.md`
</output>
