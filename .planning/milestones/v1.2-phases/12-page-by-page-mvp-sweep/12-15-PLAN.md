---
phase: 12-page-by-page-mvp-sweep
plan: 15
type: execute
wave: 4
depends_on: []
files_modified:
  - tests/e2e/specs/critical-path-postsession.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "postsession page uses full viewport width — no applicative max-width clamp"
    - "postsession.css contains zero hex/oklch/rgba color literals (color-mix allowed)"
    - "Each primary postsession interaction (stepper nav, step panels, meeting picker, signataire fields, eIDAS chips, generate/export buttons) is asserted by Playwright via observable DOM"
  artifacts:
    - path: public/assets/css/postsession.css
      provides: post-session workflow styles (already clean — verify only)
      contains: "var(--color-"
    - path: tests/e2e/specs/critical-path-postsession.spec.js
      provides: function gate for postsession page
      contains: "@critical-path"
  key_links:
    - from: tests/e2e/specs/critical-path-postsession.spec.js
      to: public/postsession.htmx.html
      via: "page.goto('/postsession.htmx.html') + locator interactions + observable assertions"
      pattern: "postsession.htmx.html"
---

<objective>
Phase 12 Wave 4 / Page 3: Sweep `/postsession` so it passes the 3 MVP gates.

Purpose: postsession is the guided post-session workflow (4-step stepper: Vérification → Validation → PV → Envoi/Archivage). It uses the `--persona-postsession` accent and contains: meeting picker, result cards, validation KPIs, signataire fields, eIDAS chip selector, PV preview, exports layout, complete banner, footer nav (Précédent/Suivant). Width and token gates are clean per pre-planning audit: only `max-width: 400px` on `.ps-picker-select` (legitimate component constraint), no hex/oklch/rgba literals, uses `color-mix(in oklch, ...)` only on `.step-complete-icon` (allowed).

Output: width + token verification (audits), plus a new `critical-path-postsession.spec.js` Playwright spec asserting postsession interactions produce real observable results.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/postsession.htmx.html
@public/assets/css/postsession.css
@public/assets/js/pages/postsession.js
@tests/e2e/helpers.js
@tests/e2e/specs/critical-path-audit.spec.js
</context>

<interfaces>
Primary interactive IDs (from public/postsession.htmx.html):
- Header: `#meetingTitle`
- Meeting picker: `#meetingPicker`, `#meetingPickerSelect`
- Stepper: `#stepper`, `.ps-seg[data-step="1"|"2"|"3"|"4"]`
- Panels: `#panel-1`, `#panel-2`, `#panel-3`, `#panel-4`
- Step 1 (Vérification): `#verifyAlert`, `#resultCardsContainer`, `#resultsTable`, `#resultsTableBody`
- Step 2 (Validation): `#validationKpis`, `#vkpiResolutions`, `#vkpiAdopted`, `#vkpiRejected`, `#vkpiParticipation`, `#workflowState`, `#wsStep-closed`, `#wsStep-validated`, `#wsStep-archived`, `#transitionActions`, `#btnValidate`, `#btnReject`
- Step 3 (PV): `#sigPresident`, `#sigSecretary`, `#sigScrutateur1`, `#sigScrutateur2`, `#pvObservations`, `#pvReserves`, `#eidasChips`, `.chip[data-eidas="advanced"|"qualified"|"manuscript"]`, `#btnSignPresident`, `#btnSignSecretary`, `#sigCounter`, `#btnGenerateReport`, `#btnExportPDF`, `#pvPreview`, `#pvHashBar`, `#pvHash`
- Step 4 (Envoi/Archivage): `#pvSummaryTitle`, `#pvSummaryResolutions`, `#pvSummaryRate`, `#pvSummaryDownload`, `#exportPvPdf`, `#exportEmargement`, `#exportAttendanceCsv`, `#exportVotesCsv`, `#exportResultsCsv`, `#exportAuditCsv`, `#sendTo` (select), `#customEmailGroup`, `#customEmail`, `#btnSendReport`, `#recipientCount`, `#btnArchive`, `#completeBanner`
- Footer nav: `#psFooterNav`, `#psStepCounter`, `#btnPrecedent`, `#btnSuivant`

Login helper: `const { loginAsOperator } = require('../helpers');` (page role: operator,president,admin).

Width status: postsession.css — only `max-width: 400px` on `.ps-picker-select` (picker component cap, legitimate) plus `@media (max-width: 768px)`. No applicative page clamp.

Token status: zero hex/oklch/rgba literals. One `color-mix(in oklch, var(--color-text-inverse) 25%, transparent)` on `.step-complete-icon` — allowed.

NOTE: `#btnArchive` and `#btnSendReport` mutate state — the spec MUST NOT click them. Read-only and re-runnable.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width + token audit — verify postsession.css is clean</name>
  <files>public/assets/css/postsession.css</files>
  <read_first>
    - public/assets/css/postsession.css (entire file, ~607 lines)
  </read_first>
  <action>
    Run the three MVP gate audits:

    1. Width gate:
    ```bash
    grep -nE '^\s*max-width:' public/assets/css/postsession.css
    ```
    Expected: only `.ps-picker-select { max-width: 400px; }` (picker component cap, legitimate — prevents the meeting select dropdown from stretching across the whole card body). Document in SUMMARY.

    2. Token gate:
    ```bash
    grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(' public/assets/css/postsession.css | grep -v '/\*' | grep -v '^\s*\*' | grep -v 'color-mix'
    ```
    Expected: zero matches.

    3. Confirm token density:
    ```bash
    grep -c 'var(--color-\|var(--persona-' public/assets/css/postsession.css
    ```
    Expected: many matches.

    No CSS changes expected.
  </action>
  <verify>
    <automated>bash -c 'test -z "$(grep -nE "oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(" public/assets/css/postsession.css | grep -v "/\*" | grep -v "^\s*\*" | grep -v "color-mix")" && test "$(grep -cE "var\(--color-|var\(--persona-" public/assets/css/postsession.css)" -gt 20'</automated>
  </verify>
  <acceptance_criteria>
    - Zero raw color literals outside color-mix.
    - max-width only on `.ps-picker-select` (component cap) and media queries.
  </acceptance_criteria>
  <done>Postsession CSS verified clean on width + token gates.</done>
</task>

<task type="auto">
  <name>Task 2: Function gate — Playwright spec asserts real results for postsession interactions</name>
  <files>tests/e2e/specs/critical-path-postsession.spec.js</files>
  <read_first>
    - tests/e2e/helpers.js
    - public/postsession.htmx.html
    - public/assets/js/pages/postsession.js (first 150 lines — understand stepper navigation and panel switching)
    - tests/e2e/specs/critical-path-audit.spec.js (reference pattern)
  </read_first>
  <action>
    Create NEW spec file `tests/e2e/specs/critical-path-postsession.spec.js`.

    Use `loginAsOperator(page)`.

    Structure: ONE `test.describe('E2E-POSTSESSION Post-session page critical path', ...)` with ONE `test()` tagged `@critical-path` named "postsession: stepper + panels + signataires + chips + preview + exports".

    Use `test.setTimeout(120000)` and `waitUntil: 'domcontentloaded'`.

    Meeting context setup: same defensive pattern as 12-14 — fetch first meeting from API, set localStorage keys via `addInitScript`. If no meeting available, `test.skip()` gracefully. Inspect `public/assets/js/services/meeting-context.js` to find the exact key.

    For each PRIMARY interaction:

    1. **Page mount + stepper present**
       - Navigate to `/postsession.htmx.html`
       - Assert `#stepper` is visible.
       - Assert 4 stepper segments exist: `.ps-seg[data-step="1"]`, `[data-step="2"]`, `[data-step="3"]`, `[data-step="4"]`.
       - Assert `.ps-seg[data-step="1"]` has class `active` initially.
       - Assert `#psStepCounter` text contains "1" (Étape 1 / 4).

    2. **Panel 1 (Vérification) visible initially**
       - Assert `#panel-1` is visible.
       - Assert `#panel-2`, `#panel-3`, `#panel-4` have the `hidden` attribute OR are not visible.
       - Assert `#resultCardsContainer` element exists.

    3. **Footer nav buttons wired**
       - Assert `#btnSuivant` is visible.
       - Assert `#btnPrecedent` is hidden or not visible at step 1.

    4. **Step navigation — click stepper segment 3 directly (if clickable)**
       - Click `.ps-seg[data-step="3"]` (.ps-seg has cursor:default by default, but JS adds click handlers to done/complete steps; for this test, try programmatic click via `page.evaluate(() => document.querySelector('.ps-seg[data-step=\"3\"]').click())` OR click `#btnSuivant` twice to advance to panel 3).
       - Prefer the `#btnSuivant` approach: check if `#btnSuivant` is enabled; if not, use programmatic panel switch:
         ```js
         await page.evaluate(() => {
           document.querySelectorAll('.ps-panel').forEach(p => p.hidden = true);
           document.getElementById('panel-3').hidden = false;
         });
         ```
       - Assert `#panel-3` becomes visible.

    5. **Panel 3 — Signataire inputs present and read-only**
       - Assert `#sigPresident`, `#sigSecretary`, `#sigScrutateur1`, `#sigScrutateur2` inputs exist.
       - Assert each has `readonly` attribute.

    6. **Panel 3 — Observations/Réserves textareas are editable**
       - Fill `#pvObservations` with "Test observation".
       - Assert its value equals "Test observation".
       - Fill `#pvReserves` with "Test réserve".
       - Assert its value equals "Test réserve".
       - Clear both fields.

    7. **Panel 3 — eIDAS chip selector toggles**
       - Assert `.chip[data-eidas="advanced"]` initially has class `active`.
       - Click `.chip[data-eidas="qualified"]`.
       - Assert `.chip[data-eidas="qualified"]` gains class `active`.
       - Assert `.chip[data-eidas="advanced"]` no longer has class `active`.
       - Click `.chip[data-eidas="manuscript"]`.
       - Assert `.chip[data-eidas="manuscript"]` becomes active.

    8. **Panel 3 — Generate/Export PDF buttons present**
       - Assert `#btnGenerateReport` is visible.
       - Assert `#btnExportPDF` link exists.
       - Do NOT click generate (mutative).

    9. **Panel 4 — programmatically switch to it and verify exports**
       - `page.evaluate(() => { document.querySelectorAll('.ps-panel').forEach(p => p.hidden = true); document.getElementById('panel-4').hidden = false; });`
       - Assert `#panel-4` visible.
       - Assert all 6 export anchors exist: `#exportPvPdf`, `#exportEmargement`, `#exportAttendanceCsv`, `#exportVotesCsv`, `#exportResultsCsv`, `#exportAuditCsv`.
       - Assert `#sendTo` select exists with at least 3 options.
       - Test `#sendTo` → "custom" reveals `#customEmailGroup`:
         - `page.selectOption('#sendTo', 'custom')`.
         - Assert `#customEmailGroup` no longer has `hidden` attribute (or becomes visible).
         - Reset to "all".
       - Assert `#btnSendReport` and `#btnArchive` are visible. Do NOT click either (mutative).

    10. **Width verification**
        - Return to panel 1 (programmatic).
        - Assert `document.documentElement.scrollWidth <= document.documentElement.clientWidth + 1`.

    Read-only on DB state, re-runnable. Skip test if no meeting in context.
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-postsession.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/critical-path-postsession.spec.js` exists.
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-postsession.spec.js` >= 1.
    - `grep -cE "ps-seg|panel-1|panel-3|panel-4|sigPresident|eidasChips|exportPvPdf|sendTo|customEmailGroup" tests/e2e/specs/critical-path-postsession.spec.js` >= 6.
    - `./bin/test-e2e.sh specs/critical-path-postsession.spec.js` exits 0.
  </acceptance_criteria>
  <done>Function gate provable via Playwright: stepper, panel visibility, signataires, observations/réserves, eIDAS chips, exports, send-to customisation, width — all asserted.</done>
</task>

</tasks>

<verification>
After all tasks:
- `! grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/postsession.css | grep -v '/\*' | grep -v 'color-mix'` returns empty
- `./bin/test-e2e.sh specs/critical-path-postsession.spec.js` exits 0
</verification>

<success_criteria>
- Width gate: postsession full-width (only legitimate picker component cap).
- Token gate: postsession.css has zero raw color literals (color-mix allowed).
- Function gate: critical-path-postsession.spec.js asserts real observable results for 9+ primary interactions.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-15-SUMMARY.md` documenting:
- Width: clean (`.ps-picker-select` component cap documented)
- Tokens: clean (color-mix on `.step-complete-icon` documented)
- Function: interactions covered, run output of `./bin/test-e2e.sh specs/critical-path-postsession.spec.js`
</output>
