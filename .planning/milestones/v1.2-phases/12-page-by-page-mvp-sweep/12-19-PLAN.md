---
phase: 12-page-by-page-mvp-sweep
plan: 19
type: execute
wave: 5
depends_on: []
files_modified:
  - public/assets/css/email-templates.css
  - tests/e2e/specs/critical-path-email-templates.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "email-templates page uses full viewport width — .templates-grid applicative clamp removed (was max-width: 1200px)"
    - "email-templates.css contains zero hex/oklch/rgba color literals — every color is a design-system token"
    - "Each primary email-templates interaction (filter by type, new-template button opens editor, create-defaults button wired, edit/delete template buttons) is asserted by Playwright via observable DOM change"
  artifacts:
    - path: public/assets/css/email-templates.css
      provides: email templates page styles (remove 1200px clamp on .templates-grid)
      contains: "var(--color-"
    - path: tests/e2e/specs/critical-path-email-templates.spec.js
      provides: function gate for email-templates page
      contains: "@critical-path"
  key_links:
    - from: tests/e2e/specs/critical-path-email-templates.spec.js
      to: public/email-templates.htmx.html
      via: "page.goto('/email-templates.htmx.html') + editor modal open/close + observable assertions"
      pattern: "email-templates.htmx.html"
---

<objective>
Phase 12 Wave 5 / Page 3 of 5: Sweep `/email-templates` so it passes the 3 MVP gates.

Purpose: email-templates is an applicative admin page (page-role="admin") for managing SMTP message templates — grid of cards, editor modal with form + preview panels, variables helper. Pre-planning audit findings:
- Width gate: one applicative clamp to FIX — `.templates-grid { max-width: 1200px }` (line 57). The editor modal cap `.template-editor-content { max-width: 1100px }` (line 193) is a legitimate modal component cap.
- Token gate: clean (zero raw-color matches).
- Function gate: no spec exists.

Output: removal of the applicative 1200px clamp from `.templates-grid`, width + token verification, and a new `critical-path-email-templates.spec.js` asserting the primary editor interactions produce real observable results.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/email-templates.htmx.html
@public/assets/css/email-templates.css
@public/assets/js/pages/email-templates.js
@tests/e2e/helpers.js
@tests/e2e/specs/critical-path-admin.spec.js
</context>

<interfaces>
Primary interactive IDs (from public/email-templates.htmx.html):
- Toolbar: `#filterType` (select: invitation/reminder/confirmation/custom/all), `#btnCreateDefaults`, `#btnNewTemplate`
- Grid: `#templatesGrid` (rendered by JS), `#emptyState`, `#btnEmptyCreate`
- Editor modal: `#templateEditor` (class `template-editor` + `.active` when open), `#editorTitle`, `#btnCloseEditor`
- Editor form: `#templateId` (hidden), `#templateName`, `#templateType`, `#templateSubject`, `#templateBody` (textarea), `#templateIsDefault` (checkbox), `#variablesList`
- Editor preview: `#previewFrame` (iframe), `#btnRefreshPreview`
- Editor footer: `#editorStatus`, `#btnCancelEdit`, `#btnSaveTemplate`

Login helper: page-role is "admin" → use `loginAsAdmin(page)` from `tests/e2e/helpers.js`.

Width status: `.templates-grid { max-width: 1200px }` (line 57) — APPLICATIVE CLAMP to REMOVE. Modal cap `.template-editor-content { max-width: 1100px }` is legitimate.

Token status: zero hex/oklch/rgba literals.

⚠️ MUTATION SAFETY: `#btnCreateDefaults` creates database rows (default templates). The spec should VERIFY the button exists and is wired but SHOULD NOT click it unless defaults are already present (idempotent). Prefer asserting the button click opens a confirmation or triggers a fetch, rather than actually creating rows. `#btnSaveTemplate` writes to DB — do NOT click it. Test the editor OPEN + field interaction + CANCEL flow only.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width fix + token audit — remove .templates-grid 1200px clamp</name>
  <files>public/assets/css/email-templates.css</files>
  <read_first>
    - public/assets/css/email-templates.css (entire file, ~337 lines)
  </read_first>
  <action>
    1. Edit `public/assets/css/email-templates.css` line 57: REMOVE the `max-width: 1200px;` declaration from the `.templates-grid { ... }` rule. The grid already has `grid-template-columns: repeat(auto-fill, minmax(320px, 1fr))` which handles card sizing intrinsically — removing the clamp lets the grid use the full available width on wide monitors.

    Replace:
    ```css
    .templates-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
      gap: var(--space-4);
      max-width: 1200px;
    }
    ```
    With:
    ```css
    .templates-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
      gap: var(--space-4);
      /* v1.2 MVP-01: full-width applicative grid — no artificial clamp */
    }
    ```

    2. Keep `.template-editor-content { max-width: 1100px }` (line 193) — this is a legitimate modal component cap.

    3. Run audits:
    ```bash
    grep -nE '^\s*max-width:' public/assets/css/email-templates.css
    ```
    Expected: `.template-editor-content { max-width: 1100px }` + media queries ONLY. `.templates-grid` should no longer appear in this list.

    ```bash
    grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(' public/assets/css/email-templates.css | grep -v '/\*' | grep -v '^\s*\*' | grep -v 'color-mix'
    ```
    Expected: zero matches.

    ```bash
    grep -c 'var(--color-' public/assets/css/email-templates.css
    ```
    Expected: ≥ 15 matches.
  </action>
  <verify>
    <automated>bash -c 'test -z "$(grep -nE "oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(" public/assets/css/email-templates.css | grep -v "/\*" | grep -v "^\s*\*" | grep -v "color-mix")" && test "$(grep -c "var(--color-" public/assets/css/email-templates.css)" -gt 15 && ! grep -nE "templates-grid" public/assets/css/email-templates.css | xargs -I{} sh -c "echo {}" | head -20 | grep -q "1200px"'</automated>
  </verify>
  <acceptance_criteria>
    - `.templates-grid` rule no longer contains `max-width: 1200px`.
    - Zero raw color literals.
    - `.template-editor-content { max-width: 1100px }` remains unchanged (legitimate modal cap).
    - `grep -c "var(--color-" public/assets/css/email-templates.css` ≥ 15.
  </acceptance_criteria>
  <done>email-templates.css width gate passes (applicative clamp removed, modal cap kept), token gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: Function gate — Playwright spec asserts real results for email-templates editor</name>
  <files>tests/e2e/specs/critical-path-email-templates.spec.js</files>
  <read_first>
    - tests/e2e/helpers.js
    - public/email-templates.htmx.html
    - public/assets/js/pages/email-templates.js (first 200 lines — understand editor open/close, filter wiring, template render)
    - tests/e2e/specs/critical-path-admin.spec.js (reference)
  </read_first>
  <action>
    Create NEW spec file `tests/e2e/specs/critical-path-email-templates.spec.js`.

    Use `loginAsAdmin(page)` (page-role="admin").

    Structure: ONE `test.describe('E2E-EMAIL-TEMPLATES Email templates critical path', ...)` containing ONE `test()` tagged `@critical-path` named `email-templates: filter + new template opens editor + fields + cancel (non-destructive)`.

    Use `test.setTimeout(90000)` and navigate with `{ waitUntil: 'domcontentloaded' }`.

    ⚠️ SAFETY: NEVER click `#btnSaveTemplate` (would persist a template). NEVER click `#btnCreateDefaults` (idempotent but mutates). Test ONLY read-only opens + cancel.

    For each PRIMARY interaction:

    1. **Page mount + toolbar visible**
       - Navigate to `/email-templates.htmx.html`.
       - Wait for `#filterType` visible (timeout 10s).
       - Assert `#btnNewTemplate`, `#btnCreateDefaults` are visible.
       - Assert `#templatesGrid` OR `#emptyState` is visible (at least one).

    2. **Filter dropdown — changing value triggers grid re-render**
       - Read current grid child count: `const before = await page.locator('#templatesGrid > *').count();`
       - `page.selectOption('#filterType', 'invitation');`
       - Wait 500ms.
       - Assert `#filterType` value is `'invitation'`.
       - Read `#templatesGrid` content and verify the select fired — the grid should either:
         - still be visible (render handler ran), OR
         - empty state appeared.
       - Select `''` (all) to restore.

    3. **btnCreateDefaults — button wired but NOT clicked**
       - Assert `#btnCreateDefaults` is enabled and visible. Do NOT click.

    4. **btnNewTemplate — click opens editor modal**
       - Click `#btnNewTemplate`.
       - Wait for `#templateEditor` to become active:
         ```js
         await page.waitForFunction(() => {
           const el = document.getElementById('templateEditor');
           return el && el.classList.contains('active');
         }, { timeout: 5000 });
         ```
       - Assert `#editorTitle` is visible with text "Nouveau template".
       - Assert `#templateName`, `#templateSubject`, `#templateBody` inputs are visible.
       - Assert `#templateId` (hidden) has empty value: `expect(await page.locator('#templateId').inputValue()).toBe('');`

    5. **Editor form — fill fields and verify values**
       - Fill `#templateName` with `E2E test template (do not save)`.
       - Fill `#templateSubject` with `E2E test subject`.
       - Fill `#templateBody` with `<p>E2E test body</p>`.
       - Assert all three values readable back via `inputValue()`.

    6. **templateType select changes**
       - `page.selectOption('#templateType', 'reminder');`
       - Assert `#templateType` value is `'reminder'`.

    7. **Preview iframe exists**
       - Assert `#previewFrame` is visible in the DOM.
       - Do NOT attempt to introspect iframe content (sandboxed, race-prone).

    8. **btnRefreshPreview — click handler runs without crashing**
       - Click `#btnRefreshPreview`.
       - Wait 300ms.
       - Assert page still on `/email-templates.htmx.html` (did not navigate).

    9. **Cancel flow — btnCancelEdit closes editor**
       - Click `#btnCancelEdit`.
       - Wait for `#templateEditor` to lose `.active` class:
         ```js
         await page.waitForFunction(() => {
           const el = document.getElementById('templateEditor');
           return el && !el.classList.contains('active');
         }, { timeout: 5000 });
         ```
       - 🚨 SAFETY: `#btnSaveTemplate` is NEVER clicked — the test ends with a cancel.

    10. **btnCloseEditor — alternative close via × button**
        - Re-open: click `#btnNewTemplate`.
        - Wait for `.active` class.
        - Click `#btnCloseEditor`.
        - Assert `.active` class is removed.

    11. **Width verification**
        - Set viewport 1920×1080: `await page.setViewportSize({ width: 1920, height: 1080 });`
        - Assert no horizontal overflow: scrollWidth ≤ clientWidth + 1.
        - Assert `.templates-grid` uses more than 1200px when content allows:
          ```js
          const gridWidth = await page.locator('#templatesGrid').evaluate(el => el.getBoundingClientRect().width);
          // The grid should be able to exceed 1200px now (applicative clamp removed)
          // If empty state is showing, this assertion may not apply — check conditionally
          ```
          If `#templatesGrid` is visible (not empty state), assert `gridWidth > 1200` (proves clamp removal took effect). If empty state, skip this sub-assertion.

    Non-mutative, re-runnable.
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-email-templates.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/critical-path-email-templates.spec.js` exists and is NEW.
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-email-templates.spec.js` ≥ 1.
    - `grep -cE "btnSaveTemplate.*click\|click.*btnSaveTemplate" tests/e2e/specs/critical-path-email-templates.spec.js` equals 0 (NEVER save).
    - `grep -cE "btnCreateDefaults.*click\|click.*btnCreateDefaults" tests/e2e/specs/critical-path-email-templates.spec.js` equals 0 (NEVER create defaults).
    - `grep -cE "btnNewTemplate|templateEditor|btnCancelEdit|filterType|templateName" tests/e2e/specs/critical-path-email-templates.spec.js` ≥ 5.
    - `./bin/test-e2e.sh specs/critical-path-email-templates.spec.js` exits 0.
  </acceptance_criteria>
  <done>Function gate provable: filter changes, new-template button opens editor, form fields fillable, type select works, refresh preview wired, both cancel paths close the editor — no DB writes.</done>
</task>

</tasks>

<verification>
After all tasks:
- `! grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/email-templates.css | grep -v '/\*' | grep -v 'color-mix'` returns empty
- `grep 'max-width: 1200px' public/assets/css/email-templates.css` returns empty (clamp removed)
- `./bin/test-e2e.sh specs/critical-path-email-templates.spec.js` exits 0
- Spec NEVER clicks `#btnSaveTemplate` or `#btnCreateDefaults`
</verification>

<success_criteria>
- Width gate: `.templates-grid` no longer clamped at 1200px (full applicative width).
- Token gate: email-templates.css has zero raw color literals.
- Function gate: critical-path-email-templates.spec.js asserts real observable results for 8+ primary interactions, non-destructive.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-19-SUMMARY.md` documenting:
- Width: 1200px clamp removed from `.templates-grid`, modal cap kept.
- Tokens: clean.
- Function: interactions covered, run output, explicit note that save and create-defaults are never clicked.
- Note progress toward Wave 5 completion (3/5 done).
</output>
