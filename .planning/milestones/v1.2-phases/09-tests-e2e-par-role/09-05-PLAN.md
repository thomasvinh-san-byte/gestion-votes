---
phase: 09-tests-e2e-par-role
plan: 05
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - tests/e2e/specs/critical-path-votant.spec.js
autonomous: true
requirements: ["E2E-04"]
must_haves:
  truths:
    - "A votant can authenticate (cookie injection) and reach /vote.htmx.html"
    - "The vote page renders the meeting selector and identity banner shells"
    - "The votant can interact with the meeting selector to load a session"
    - "The vote page exposes confirm button and progression dots wiring"
    - "The spec is tagged @critical-path and re-runnable without DB writes"
  artifacts:
    - path: "tests/e2e/specs/critical-path-votant.spec.js"
      provides: "E2E-04 votant critical path spec"
      contains: "@critical-path"
      min_lines: 50
  key_links:
    - from: "tests/e2e/specs/critical-path-votant.spec.js"
      to: "tests/e2e/helpers.js"
      via: "loginAsVoter import"
      pattern: "loginAsVoter"
    - from: "tests/e2e/specs/critical-path-votant.spec.js"
      to: "/vote.htmx.html"
      via: "page.goto"
      pattern: "vote\\.htmx\\.html"
---

<objective>
Implement E2E-04: a Playwright spec exercising the votant critical path (login → /vote → meeting selector → identity banner → vote UI ready state).

Note on scope: CONTEXT.md mentions a "vote token flow without session". In the current codebase (verified by reading tests/e2e/specs/vote.spec.js), the vote page is session-protected and uses loginAsVoter. We follow what actually works — Phase 10 UAT will validate the manual token flow if it exists.

Purpose: Prove a votant can reach the voting interface and interact with its primary controls.
Output: tests/e2e/specs/critical-path-votant.spec.js with one tagged test.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-tests-e2e-par-role/09-CONTEXT.md
@tests/e2e/helpers.js
@tests/e2e/specs/vote.spec.js
@public/vote.htmx.html

<interfaces>
helpers.js exports:
  const { loginAsVoter, CREDENTIALS, E2E_MOTION_1 } = require('../helpers');

vote.htmx.html known IDs (verified by grep):
- #voteApp                    — outer container with data-vote-state attribute
- #meetingSelect              — meeting selector (also matched by ag-searchable-select)
- #memberSelect               — member selector
- #identityBanner             — identity banner (initially hidden)
- #voteWaitingState           — initial waiting state panel
- #voteResolutionCounter      — counter that updates after meeting load
- #btnConfirm                 — confirm button
- #btnConfirmInline           — inline confirm
- #btnZoom                    — zoom toggle

Existing vote.spec.js pattern (proven baseline for this page):
  await loginAsVoter(page);
  await page.goto('/vote.htmx.html', { waitUntil: 'domcontentloaded' });
  await waitForHtmxSettled(page);
  const meetingSelect = page.locator('#meetingSelect, ag-searchable-select');
  await expect(meetingSelect.first()).toBeVisible({ timeout: 10000 });

waitForHtmxSettled helper (Phase 5):
  const { waitForHtmxSettled } = require('../helpers/waitForHtmxSettled');
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create critical-path-votant.spec.js</name>
  <files>tests/e2e/specs/critical-path-votant.spec.js</files>
  <read_first>
    - tests/e2e/specs/vote.spec.js (existing baseline pattern — copy structure)
    - tests/e2e/helpers.js (loginAsVoter export)
    - tests/e2e/helpers/waitForHtmxSettled.js (skim — understand the wait helper)
    - public/vote.htmx.html lines 40-160 (selectors for vote app, banner, controls)
    - .planning/phases/09-tests-e2e-par-role/09-CONTEXT.md (E2E-04 critical path)
  </read_first>
  <action>
    Create tests/e2e/specs/critical-path-votant.spec.js with the structure below.

    The file content (use this verbatim, including the @critical-path tag in the test title):

      // @ts-check
      const { test, expect } = require('@playwright/test');
      const { loginAsVoter } = require('../helpers');
      const { waitForHtmxSettled } = require('../helpers/waitForHtmxSettled');

      // E2E-04: Votant critical path
      // login (cookie) -> /vote.htmx.html -> meeting selector visible
      //               -> vote app renders waiting state -> confirm button wiring present
      //
      // Scope note: CONTEXT.md mentions a token-based flow. The current
      // vote.htmx.html is session-based (verified via vote.spec.js). We follow
      // what works -- token-flow gaps will surface in Phase 10 UAT.
      //
      // Re-runnable: no DB writes, only navigation and visibility assertions.

      test.describe('E2E-04 Votant critical path', () => {

        test('votant: vote page -> meeting selector -> vote app ready @critical-path', async ({ page }) => {
          test.setTimeout(120000);

          // Step 1: Login as voter (cookie injection)
          await loginAsVoter(page);

          const whoami = await page.request.get('/api/v1/whoami.php');
          expect(whoami.ok()).toBeTruthy();

          // Step 2: Navigate to vote page
          await page.goto('/vote.htmx.html', { waitUntil: 'domcontentloaded' });
          await waitForHtmxSettled(page);

          // Step 3: Vote app shell rendered
          await expect(page.locator('#voteApp')).toBeVisible({ timeout: 15000 });
          await expect(page.locator('#voteApp')).toHaveAttribute('data-vote-state', /waiting|loading|ready/, { timeout: 10000 });

          // Step 4: Meeting selector visible (entry point of votant journey)
          const meetingSelect = page.locator('#meetingSelect, ag-searchable-select');
          await expect(meetingSelect.first()).toBeVisible({ timeout: 10000 });

          // Step 5: Member selector visible (next step after meeting pick)
          await expect(page.locator('#memberSelect')).toBeVisible({ timeout: 5000 });

          // Step 6: Waiting state panel visible (proves vote app booted)
          await expect(page.locator('#voteWaitingState')).toBeVisible({ timeout: 10000 });

          // Step 7: Confirmation control wired (required for any vote submission)
          // The button may be hidden until a vote is staged; check it exists in DOM.
          const btnConfirm = page.locator('#btnConfirm');
          await expect(btnConfirm).toHaveCount(1);

          // Step 8: Zoom toggle proves vote-app JS is alive
          const btnZoom = page.locator('#btnZoom');
          if (await btnZoom.isVisible().catch(() => false)) {
            await btnZoom.click();
            await expect(btnZoom).toHaveAttribute('aria-pressed', /true|false/, { timeout: 5000 });
          }
        });

      });

    Notes:
    - Single test() per file
    - @critical-path tag in test title
    - All selectors verified to exist in vote.htmx.html
    - Uses waitForHtmxSettled helper (Phase 5 pattern, not networkidle)
    - No DB writes — pure read+visibility flow
    - Re-runnable

    Why no real ballot submission: a real cast requires (a) an OPEN meeting in a specific motion state, (b) idempotency key + CSRF on /api/v1/ballots_cast.php, (c) cleanup that breaks re-runnability. Phase 10 UAT will validate end-to-end vote submission manually.

    DO NOT add more tests. DO NOT mock. DO NOT call /api/v1/ballots_cast.php.
  </action>
  <verify>
    <automated>node --check tests/e2e/specs/critical-path-votant.spec.js &amp;&amp; grep -q "@critical-path" tests/e2e/specs/critical-path-votant.spec.js &amp;&amp; grep -q "loginAsVoter" tests/e2e/specs/critical-path-votant.spec.js &amp;&amp; grep -q "vote.htmx.html" tests/e2e/specs/critical-path-votant.spec.js &amp;&amp; ! grep -q "networkidle" tests/e2e/specs/critical-path-votant.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File tests/e2e/specs/critical-path-votant.spec.js exists
    - `node --check` passes
    - Contains "@critical-path"
    - Contains "loginAsVoter" import and call
    - Navigates to /vote.htmx.html
    - References #voteApp, #meetingSelect, #voteWaitingState, #btnConfirm
    - Does NOT contain "networkidle"
    - Has exactly one `test(` block
    - Uses waitForHtmxSettled helper
  </acceptance_criteria>
  <done>
    Spec exists, parses, covers the votant journey from login to vote-app-ready state with verified selectors and a re-runnable design.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run the spec in container and capture pass/fail</name>
  <files></files>
  <read_first>
    - bin/test-e2e.sh
  </read_first>
  <action>
    Run only the new spec (CLAUDE.md rule: never run the full suite):

    timeout 180 ./bin/test-e2e.sh tests/e2e/specs/critical-path-votant.spec.js --reporter=line

    Expected: 1 passed. Same triage rules as 09-02/09-03/09-04 (max 2 retries, then stop and document).
  </action>
  <verify>
    <automated>test -f tests/e2e/specs/critical-path-votant.spec.js &amp;&amp; node --check tests/e2e/specs/critical-path-votant.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - Spec executed in container at least once
    - Outcome documented in SUMMARY
    - Max 2 runs consumed
  </acceptance_criteria>
  <done>
    Spec ran in container, result captured for SUMMARY.
  </done>
</task>

</tasks>

<verification>
- File parses with `node --check`
- @critical-path tag present
- All selectors verified to exist in vote.htmx.html
- Spec executed in container at least once
</verification>

<success_criteria>
E2E-04 satisfied: tests/e2e/specs/critical-path-votant.spec.js exercises the votant critical path from login to vote-app-ready state, and is re-runnable.
</success_criteria>

<output>
After completion, create `.planning/phases/09-tests-e2e-par-role/09-05-SUMMARY.md` documenting the file, selectors used, run result, and any backend issues observed.
</output>
