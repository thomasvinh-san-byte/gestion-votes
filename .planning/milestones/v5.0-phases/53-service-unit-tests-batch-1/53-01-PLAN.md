---
phase: 53-service-unit-tests-batch-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Unit/VoteEngineTest.php
  - tests/Unit/ImportServiceTest.php
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "QuorumEngineTest already passes with comprehensive coverage (verify only, no changes needed)"
    - "VoteEngine::computeMotionResult() is tested via mocked repos covering adopted, rejected, no_quorum, no_votes, and no_policy statuses"
    - "ImportService static methods are tested covering CSV reading, column mapping, attendance mode parsing, boolean parsing, voting power parsing, and file validation edge cases"
  artifacts:
    - path: "tests/Unit/VoteEngineTest.php"
      provides: "Augmented VoteEngine tests with computeMotionResult coverage"
      min_lines: 900
    - path: "tests/Unit/ImportServiceTest.php"
      provides: "Complete ImportService unit test file"
      min_lines: 200
  key_links:
    - from: "tests/Unit/VoteEngineTest.php"
      to: "app/Services/VoteEngine.php"
      via: "mocked repos for computeMotionResult"
      pattern: "computeMotionResult"
    - from: "tests/Unit/ImportServiceTest.php"
      to: "app/Services/ImportService.php"
      via: "direct static method calls"
      pattern: "ImportService::"
---

<objective>
Augment VoteEngineTest with computeMotionResult instance-method tests (currently only the static computeDecision is tested) and create ImportServiceTest from scratch for all static utility methods. Also verify existing QuorumEngineTest already satisfies TEST-01.

Purpose: Close the two biggest test gaps in the voting/import layer -- VoteEngine's main entry point (computeMotionResult) is untested, and ImportService has zero tests.
Output: Augmented VoteEngineTest.php, new ImportServiceTest.php, verified QuorumEngineTest passes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-service-unit-tests-batch-1/53-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify QuorumEngineTest coverage + Augment VoteEngineTest with computeMotionResult tests</name>
  <files>tests/Unit/VoteEngineTest.php</files>
  <read_first>
    - app/Services/VoteEngine.php (the service under test -- study computeMotionResult method lines 155-285)
    - app/Services/QuorumEngine.php (reference for mock patterns)
    - tests/Unit/VoteEngineTest.php (existing tests to augment)
    - tests/Unit/QuorumEngineTest.php (reference for buildEngine mock pattern)
    - tests/Unit/BallotsServiceTest.php (reference for setUp/mock patterns)
  </read_first>
  <action>
First, run `vendor/bin/phpunit --filter QuorumEngineTest` to confirm TEST-01 is already satisfied (all existing tests pass). No changes to QuorumEngineTest.php.

Then augment tests/Unit/VoteEngineTest.php by adding a new section at the end (before the helper methods) testing VoteEngine::computeMotionResult() with mocked repos. Follow the buildEngine pattern from QuorumEngineTest:

Add a private `buildVoteEngine(array $overrides = [])` helper that creates mocks for MotionRepository, BallotRepository, MemberRepository, PolicyRepository, AttendanceRepository and returns a new VoteEngine instance.

Add these test methods:

**computeMotionResult -- input validation:**
- `testComputeMotionResultEmptyIdThrows()` -- empty string throws InvalidArgumentException
- `testComputeMotionResultMotionNotFoundThrows()` -- motionRepo returns null, throws RuntimeException

**computeMotionResult -- adopted scenario:**
- `testComputeMotionResultAdopted()` -- mock motion with vote_policy_id, ballotRepo tally returns 60 for / 30 against / 10 abstain, memberRepo returns 20 eligible/200 weight, policyRepo returns expressed base/0.5 threshold. Assert decision.status === 'adopted', majority.met === true, tallies structure correct.

**computeMotionResult -- rejected scenario:**
- `testComputeMotionResultRejected()` -- 30 for / 60 against. Assert decision.status === 'rejected'.

**computeMotionResult -- no quorum blocks adoption:**
- `testComputeMotionResultNoQuorum()` -- set quorum_policy_id, memberRepo returns 20 eligible, expressedMembers only 5 (quorum at 0.5 needs 10). Assert decision.status === 'no_quorum'.

**computeMotionResult -- no votes:**
- `testComputeMotionResultNoVotes()` -- ballotRepo tally returns all zeros. Assert decision.status === 'no_votes'.

**computeMotionResult -- no policy:**
- `testComputeMotionResultNoPolicy()` -- motion has no vote_policy_id and no meeting_vote_policy_id. Assert decision.status === 'no_policy'.

**computeMotionResult -- policy fallback:**
- `testComputeMotionResultFallsBackToMeetingVotePolicy()` -- motion has no vote_policy_id but has meeting_vote_policy_id. Assert the meeting-level policy is used.

**computeMotionResult -- present base resolves presentWeight:**
- `testComputeMotionResultPresentBaseUsesAttendanceWeight()` -- votePolicy base='present', verify attendanceRepo.sumPresentWeight is called and used as denominator.

**computeMotionResult -- result structure:**
- `testComputeMotionResultStructure()` -- verify all top-level keys present: motion, tallies, eligible, expressed, quorum, majority, decision.

For mock setup, use this pattern for motionRepo.findWithVoteContext return:
```php
[
    'motion_id' => 'mot-1', 'motion_title' => 'Test',
    'meeting_id' => 'm-1', 'tenant_id' => 't-1',
    'vote_policy_id' => 'vp-1', 'quorum_policy_id' => null,
    'meeting_vote_policy_id' => null, 'meeting_quorum_policy_id' => null,
    'secret' => false,
]
```

For ballotRepo.tally return:
```php
[
    'count_for' => 10, 'weight_for' => 60.0,
    'count_against' => 5, 'weight_against' => 30.0,
    'count_abstain' => 2, 'weight_abstain' => 10.0,
    'count_nsp' => 1, 'weight_nsp' => 0.0,
]
```
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter QuorumEngineTest && vendor/bin/phpunit --filter VoteEngineTest</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit --filter QuorumEngineTest` exits 0 (all 35+ existing tests pass, confirming TEST-01)
    - `vendor/bin/phpunit --filter VoteEngineTest` exits 0
    - Minimum 9 new test methods added for computeMotionResult
    - Total VoteEngineTest methods >= 40 (existing ~31 + new ~9)
    - Key scenarios covered: adopted, rejected, no_quorum, no_votes, no_policy, policy_fallback, present_base, empty_id_throws, motion_not_found_throws
  </acceptance_criteria>
  <done>VoteEngine::computeMotionResult fully tested via mocked repos. QuorumEngineTest confirmed passing. TEST-01 and TEST-02 requirements met.</done>
</task>

<task type="auto">
  <name>Task 2: Create ImportServiceTest with comprehensive coverage of all static methods</name>
  <files>tests/Unit/ImportServiceTest.php</files>
  <read_first>
    - app/Services/ImportService.php (the service under test -- all static methods)
    - tests/Unit/BallotsServiceTest.php (reference for test patterns and namespace)
    - tests/Unit/VoteEngineTest.php (reference for test structure)
  </read_first>
  <action>
Create tests/Unit/ImportServiceTest.php. Use namespace `Tests\Unit` (matching VoteEngineTest pattern, not `AgVote\Tests\Unit` from BallotsServiceTest -- check phpunit.xml autoload to confirm which works).

ImportService is entirely static methods, no constructor dependencies needed. Create temporary files for CSV reading tests.

**validateUploadedFile tests:**
- `testValidateUploadedFileSuccess()` -- valid file array with correct extension/size, returns ['ok' => true]
- `testValidateUploadedFileMissingTmpName()` -- returns error
- `testValidateUploadedFileUploadError()` -- error !== UPLOAD_ERR_OK, returns error
- `testValidateUploadedFileWrongExtension()` -- csv file with xlsx extension, returns error with message containing "Extension attendue"
- `testValidateUploadedFileTooLarge()` -- size > 5MB, returns error with "trop volumineux"

**readCsvFile tests:**
- `testReadCsvFileCommaSeparated()` -- create temp CSV with commas, verify headers normalized to lowercase, rows returned
- `testReadCsvFileSemicolonSeparated()` -- create temp CSV with semicolons, verify separator detected as ';'
- `testReadCsvFileSkipsEmptyRows()` -- CSV with blank lines, verify empty rows filtered out
- `testReadCsvFileInvalidPath()` -- non-existent path returns error "Impossible d'ouvrir"
- `testReadCsvFileEmptyFile()` -- empty file returns error about invalid headers

**mapColumns tests:**
- `testMapColumnsFindsMatchingAliases()` -- headers=['nom','email','ponderation'], map matches 'name' -> 'nom', 'email' -> 'email', 'voting_power' -> 'ponderation'
- `testMapColumnsSkipsMissingColumns()` -- header missing an alias, result has fewer keys
- `testMapColumnsReturnsEmptyForNoMatches()` -- completely unrelated headers

**getMembersColumnMap / getAttendancesColumnMap / getMotionsColumnMap / getProxiesColumnMap:**
- `testColumnMapsReturnNonEmptyArrays()` -- verify each returns non-empty array with expected keys

**parseAttendanceMode tests:**
- `testParseAttendanceModePresent()` -- 'present', 'présent', 'p', '1', 'oui', 'yes' all return 'present'
- `testParseAttendanceModeRemote()` -- 'remote', 'distant', 'visio' return 'remote'
- `testParseAttendanceModeExcused()` -- 'excused', 'excusé' return 'excused'
- `testParseAttendanceModeAbsent()` -- 'absent', 'a', '0', '', 'non' return 'absent'
- `testParseAttendanceModeProxy()` -- 'proxy', 'procuration' return 'proxy'
- `testParseAttendanceModeUnknown()` -- 'xyz' returns null
- `testParseAttendanceModeCaseInsensitive()` -- 'PRESENT', 'Remote' work (via mb_strtolower)

**parseBoolean tests:**
- `testParseBooleanTrueValues()` -- '1', 'true', 'oui', 'yes', 'actif', 'o', 'y' all return true
- `testParseBooleanFalseValues()` -- '0', 'false', 'non', 'no', '' return false

**parseVotingPower tests:**
- `testParseVotingPowerInteger()` -- '5' returns 5.0
- `testParseVotingPowerFloat()` -- '3.5' returns 3.5
- `testParseVotingPowerFrenchDecimal()` -- '3,5' returns 3.5 (comma -> dot)
- `testParseVotingPowerZeroDefaultsToOne()` -- '0' returns 1.0
- `testParseVotingPowerNegativeDefaultsToOne()` -- '-5' returns 1.0
- `testParseVotingPowerEmptyDefaultsToOne()` -- '' returns 1.0

Use `setUp`/`tearDown` with a temp directory for CSV file tests. Clean up temp files in tearDown.
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter ImportServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit --filter ImportServiceTest` exits 0
    - Minimum 25 test methods
    - All static methods covered: validateUploadedFile, readCsvFile, mapColumns, parseAttendanceMode, parseBoolean, parseVotingPower, column map getters
    - Key scenarios: valid/invalid file upload, CSV comma/semicolon detection, empty rows skipped, French decimal parsing, attendance mode aliases in French and English, boolean aliases
  </acceptance_criteria>
  <done>ImportServiceTest created with comprehensive coverage of all static utility methods. TEST-03 requirement met.</done>
</task>

</tasks>

<verification>
Run all three test suites to confirm full coverage:
```bash
vendor/bin/phpunit --filter QuorumEngineTest && vendor/bin/phpunit --filter VoteEngineTest && vendor/bin/phpunit --filter ImportServiceTest
```
All three must exit 0 with zero failures.
</verification>

<success_criteria>
- QuorumEngineTest passes (confirms TEST-01 already satisfied)
- VoteEngineTest passes with 40+ total methods including computeMotionResult coverage (TEST-02)
- ImportServiceTest passes with 25+ methods covering all static methods (TEST-03)
- Zero test failures across all three suites
</success_criteria>

<output>
After completion, create `.planning/phases/53-service-unit-tests-batch-1/53-01-SUMMARY.md`
</output>
