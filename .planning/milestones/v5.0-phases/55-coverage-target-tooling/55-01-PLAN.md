---
phase: 55-coverage-target-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - phpunit.xml
autonomous: true
requirements:
  - COV-01
  - COV-02
must_haves:
  truths:
    - "Running vendor/bin/phpunit --coverage-html generates a readable HTML report without errors"
    - "phpunit.xml source includes cover both app/Services/ and app/Controller/"
    - "Baseline coverage percentages for Services and Controllers are known"
  artifacts:
    - path: "phpunit.xml"
      provides: "Coverage source configuration including app/Controller/"
      contains: "app/Controller"
    - path: "coverage-report/index.html"
      provides: "HTML coverage report"
  key_links:
    - from: "phpunit.xml"
      to: "pcov extension"
      via: "PHPUnit detects pcov as coverage driver"
      pattern: "coverage"
---

<objective>
Install pcov coverage driver locally, fix phpunit.xml to include app/Controller/ in source coverage, and measure baseline coverage for Services and Controllers.

Purpose: We need to know our starting point before writing any gap-filling tests. The 90% target may already be met after phases 53-54 added 233 new tests. Also, phpunit.xml currently omits app/Controller/ from coverage source — that must be fixed before any measurement is meaningful.

Output: pcov installed, phpunit.xml updated, baseline coverage numbers documented.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@phpunit.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pcov and update phpunit.xml source includes</name>
  <files>phpunit.xml</files>
  <action>
1. Install pcov coverage driver locally:
   - Run: sudo apt-get install -y php8.3-pcov
   - Verify: php -m | grep pcov shows "pcov"

2. Update phpunit.xml to add app/Controller/ to the source includes. The current source block is:
   ```xml
   <source>
     <include>
       <directory suffix=".php">app/Core</directory>
       <directory suffix=".php">app/Services</directory>
       <directory suffix=".php">app/Repository</directory>
       <directory suffix=".php">app/WebSocket</directory>
       <directory suffix=".php">app/Templates</directory>
     </include>
   ```
   Add this line after the Templates entry:
   ```xml
       <directory suffix=".php">app/Controller</directory>
   ```

3. Also add a Dockerfile comment/block for CI coverage (pcov in Docker). Add pcov installation to the Dockerfile AFTER the redis extension block and BEFORE the apk del cleanup — but ONLY as a build-arg-gated block so production images stay lean. Actually, do NOT modify Dockerfile yet — that belongs to Phase 57 CI pipeline. Just note in summary that Dockerfile pcov installation is needed for CI.

4. Do NOT add coverage enforcement thresholds yet — that comes in Plan 02 after we know the baseline.
  </action>
  <verify>
    <automated>php -m | grep pcov && vendor/bin/phpunit --testsuite Unit --coverage-text 2>&1 | head -5</automated>
  </verify>
  <done>pcov extension loads, phpunit.xml includes app/Controller/ in source, coverage-text output begins without "No code coverage driver available" error</done>
</task>

<task type="auto">
  <name>Task 2: Measure baseline coverage and document gaps</name>
  <files>coverage-report/index.html</files>
  <action>
1. Run full test suite with coverage:
   ```bash
   vendor/bin/phpunit --testsuite Unit --coverage-text --coverage-html coverage-report/ 2>&1
   ```

2. Parse the text coverage output to extract:
   - Overall line coverage percentage
   - app/Services/ directory coverage percentage
   - app/Controller/ directory coverage percentage
   - Per-file breakdown: identify any file below 90% in Services/ or Controller/

3. Document the baseline in the SUMMARY. List:
   - Services at 90%+: (list files)
   - Services below 90%: (list files with their %)
   - Controllers at 90%+: (list files)
   - Controllers below 90%: (list files with their %)

4. This information determines whether Plan 02 needs gap-filling tests or if 90% is already met.

5. If Services and Controllers are BOTH already at 90%+, note that Plan 02 only needs to add the threshold enforcement to phpunit.xml.
  </action>
  <verify>
    <automated>test -f coverage-report/index.html && vendor/bin/phpunit --testsuite Unit --coverage-text 2>&1 | grep -E "Services|Controller" | head -10</automated>
  </verify>
  <done>Baseline coverage measured, HTML report generated at coverage-report/, per-directory percentages documented, gap files identified</done>
</task>

</tasks>

<verification>
- php -m | grep pcov returns "pcov"
- vendor/bin/phpunit --coverage-text runs without "No code coverage driver" error
- phpunit.xml source includes app/Controller/
- coverage-report/index.html exists and is browsable
- Baseline percentages for Services/ and Controller/ are known
</verification>

<success_criteria>
Coverage driver operational, baseline measured, gaps identified for Plan 02 to close.
</success_criteria>

<output>
After completion, create `.planning/phases/55-coverage-target-tooling/55-01-SUMMARY.md`

CRITICAL: The summary MUST include the baseline coverage percentages and the list of files below 90% — Plan 02 depends on this data.
</output>
