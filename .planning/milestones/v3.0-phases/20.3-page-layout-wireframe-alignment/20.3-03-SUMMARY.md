---
phase: 20.3-page-layout-wireframe-alignment
plan: "03"
subsystem: ui-layout
tags: [dashboard, hub, css, density, tables]
dependency_graph:
  requires: []
  provides: [dashboard-zero-scroll, hub-density, table-compaction]
  affects: [public/assets/css/pages.css, public/assets/css/hub.css, public/dashboard.htmx.html]
tech_stack:
  added: []
  patterns: [dashboard-density-overrides, hub-layout-compaction, global-table-density]
key_files:
  created: []
  modified:
    - public/assets/css/pages.css
    - public/assets/css/hub.css
    - public/dashboard.htmx.html
decisions:
  - ".kpi-grid.dashboard-grid .kpi-card scoped override (not global) to avoid breaking other kpi-card usages"
  - "Option B scoped selectors for table density — .audit-table and .table cover all data tables without HTML changes"
  - "Hub layout verified correct (220px stepper, 24px gap) — only padding/margin compaction applied"
metrics:
  duration: "8min"
  completed: "2026-03-17"
  tasks_completed: 2
  files_modified: 3
---

# Phase 20.3 Plan 03: Dashboard Compaction + Hub Density + Table Rows Summary

**One-liner:** Dashboard zero-scroll at 1080px via kpi-card 12px padding, shortcut-card 14px padding, hub compacted to 14px margins, and 8px vertical padding applied to all data tables.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Compact dashboard to zero-scroll at 1080px | 87f1fe9 | pages.css, dashboard.htmx.html |
| 2 | Verify hub layout + compact density, apply table row 8px padding | 1c23ab8 | hub.css, pages.css |

## What Was Built

### Task 1 — Dashboard zero-scroll
- Added `.kpi-grid.dashboard-grid .kpi-card { padding: 12px 10px }` — scoped to dashboard context, doesn't affect other kpi-card usages (e.g. hub)
- Changed `.shortcut-card` padding from `20px 18px` to `14px`, gap from `16px` to `12px`
- Added `dashboard-grid` class to the grid-3 shortcuts `<div>` in dashboard.htmx.html so the existing `margin-bottom: 14px` rule on `.dashboard-grid` applies

Note: `.dashboard-grid`, `.dashboard-panel`, `.urgent-card` compaction was already done in prior plan work.

### Task 2 — Hub density + table compaction
- `.hub-identity` padding: `1rem 1.5rem` → `10px 14px`, margin-bottom: `1.25rem` → `14px`
- `.hub-action` padding: `1.25rem 1.5rem` → `14px`, gap: `1rem` → `0.75rem`, margin-bottom: `1.25rem` → `14px`
- `.hub-checklist` padding/margin-bottom: `1.25rem` → `14px`
- Hub 2-column layout verified: `.hub-layout-body` flex+gap:24px, `.hub-stepper-col` 220px — structure correct, no changes needed
- Table density rule added in pages.css: `.audit-table td/th`, `.table td/th`, `.data-table td/th` → `padding: 8px 12px`
  - `.audit-table` covers audit.htmx.html
  - `.table` covers archives.js generated tables
  - `.data-table` covers any future table using the utility class

## Deviations from Plan

None — plan executed exactly as written. Table approach was Option B (scoped selectors) as members and users pages use card-based layouts rather than HTML tables.

## Self-Check: PASSED

Files verified:
- public/assets/css/pages.css — exists, contains dashboard-grid, kpi-card, shortcut-card, data table rules
- public/assets/css/hub.css — exists, contains hub-identity, hub-action compaction
- public/dashboard.htmx.html — grid-3 div has dashboard-grid class

Commits verified:
- 87f1fe9 — feat(20.3-03): compact dashboard to zero-scroll at 1080px
- 1c23ab8 — feat(20.3-03): compact hub density and apply 8px table row padding globally
