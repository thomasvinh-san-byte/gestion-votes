---
phase: 20.3-page-layout-wireframe-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/settings.htmx.html
  - public/assets/css/settings.css
  - public/assets/js/pages/settings.js
  - public/wizard.htmx.html
  - public/assets/css/wizard.css
autonomous: true
requirements:
  - LAYOUT-SETTINGS
  - LAYOUT-WIZARD

must_haves:
  truths:
    - "Settings page uses vertical sidebar nav (200px) on the left with content area on the right, matching wireframe v3.19.2"
    - "Settings tab switching still works after HTML restructure — clicking each nav item shows the correct panel"
    - "Wizard step-nav is pinned at the bottom of the card via flex margin-top:auto — each step fits without scroll at 1080px"
    - "No JS console errors on settings or wizard pages after restructure"
  artifacts:
    - path: "public/assets/css/settings.css"
      provides: "Sidebar nav layout CSS"
      contains: "settings-layout"
    - path: "public/settings.htmx.html"
      provides: "Restructured settings with sidebar nav"
      contains: "settings-sidenav"
    - path: "public/assets/css/wizard.css"
      provides: "Flex column card with pinned footer"
      contains: "margin-top: auto"
  key_links:
    - from: "public/settings.htmx.html"
      to: "public/assets/js/pages/settings.js"
      via: "Tab switching selectors preserved or updated"
      pattern: "settings-tab|settings-sidenav|data-stab"
---

<objective>
Restructure settings page from horizontal tabs to wireframe sidebar nav, and fix wizard step-nav to be pinned at bottom.

Purpose: Settings and wizard are the two remaining pages requiring HTML+CSS structural changes (beyond operator, which is Plan 01). Settings needs a 200px vertical sidebar nav per wireframe. Wizard needs each step to fit in 1080px viewport with the step-nav pinned at the bottom.

Output: Restructured settings.htmx.html with sidebar nav, updated settings.css and settings.js selectors, and wizard.css with flex-column card layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.3-page-layout-wireframe-alignment/20.3-CONTEXT.md
@.planning/phases/20.3-page-layout-wireframe-alignment/20.3-RESEARCH.md
@ag_vote_wireframe.html
@public/settings.htmx.html
@public/assets/css/settings.css
@public/assets/js/pages/settings.js
@public/wizard.htmx.html
@public/assets/css/wizard.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure settings page to sidebar nav layout</name>
  <files>public/settings.htmx.html, public/assets/css/settings.css, public/assets/js/pages/settings.js</files>
  <action>
FIRST: Read settings.js to catalog all DOM selectors used for tab switching. Identify the exact class names and data attributes used (likely `.settings-tab`, `.settings-panel`, `data-stab`). Record which selectors must be preserved or updated.

THEN: Read ag_vote_wireframe.html to confirm the settings layout structure (search for "Settings" or "Parametres" sections — expect: flex row, 200px sidebar, 14px gap, content area flex:1).

THEN: Restructure settings.htmx.html:

1. Remove or replace the `.container` wrapper that constrains max-width (settings needs full `.app-main` width for the sidebar layout)
2. Replace the horizontal `.settings-tabs` row with a `.settings-layout` flex container:
   - Left: `<nav class="settings-sidenav">` (200px) containing tab buttons as `.settings-sidenav-item` buttons
   - Right: `<div class="settings-content">` (flex:1) containing existing panel content unchanged
3. Each sidenav button should have the same `data-stab` attribute as the current tabs for JS compatibility
4. The first sidenav item gets class `active` by default

THEN: Add to settings.css the sidebar layout classes (from RESEARCH.md code examples):
- `.settings-layout`: `display: flex; gap: 14px; min-height: 0; align-items: flex-start`
- `.settings-sidenav`: `width: 200px; flex-shrink: 0`
- `.settings-sidenav-item`: vertical nav button styling (10px 12px padding, 8px radius, full width, left-aligned text, muted color, 600 weight)
- `.settings-sidenav-item:hover`: subtle background
- `.settings-sidenav-item.active`: primary-subtle background, primary color, 700 weight
- `.settings-content`: `flex: 1; min-width: 0`
- Remove or override old horizontal `.settings-tabs` styles that conflict

THEN: Update settings.js tab-switching logic:
- If settings.js uses `.settings-tab` class for event listeners, update to also target `.settings-sidenav-item` (or replace the selector entirely if the old horizontal tabs HTML is removed)
- The active class toggle must work with the new `.settings-sidenav-item` elements
- Test: each data-stab value still shows the corresponding panel
  </action>
  <verify>
    <automated>grep -c "settings-layout\|settings-sidenav" public/assets/css/settings.css && grep -c "settings-sidenav" public/settings.htmx.html && npx eslint public/assets/js/pages/settings.js 2>/dev/null || echo "ESLint not configured for this file"</automated>
  </verify>
  <done>Settings page shows vertical sidebar nav (200px) on left with content panels on right. Tab switching works for all panels. No JS errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix wizard step-nav to pin at card bottom via flex</name>
  <files>public/wizard.htmx.html, public/assets/css/wizard.css</files>
  <action>
Read wizard.htmx.html to understand current card structure. Each wizard step is likely a `.card.wiz-step` containing step content and a `.step-nav` at the bottom.

THEN: Read ag_vote_wireframe.html to confirm the wizard layout structure (search for "Wizard" or "Creation" sections — expect: progress bar + single card + footer nav pattern).

Apply these changes:

1. In wizard.css, make `.wiz-step` (the card for each step) use flex column layout:
   ```css
   .wiz-step {
     display: flex;
     flex-direction: column;
     min-height: 0;
   }
   ```

2. If step content is not in a wrapper div, add a `.wiz-step-body` wrapper in wizard.htmx.html around the step content (everything except `.step-nav`):
   ```css
   .wiz-step-body {
     flex: 1;
     overflow-y: auto;
     min-height: 0;
   }
   ```

3. Update `.step-nav` in wizard.css to pin at bottom:
   ```css
   .step-nav {
     margin-top: auto;  /* push to bottom in flex column */
     flex-shrink: 0;
     /* keep existing border-top + padding */
   }
   ```

4. Ensure the wizard's `.app-main` area uses flex-column so the card can stretch to fill height. If `.app-main` already has `flex: 1; overflow-y: auto`, the card needs `flex: 1` or `min-height: calc(100vh - header - footer - progress - padding)` to fill the space. Prefer the flex approach: add to wizard.css a scoped rule for wizard context.

5. Also compact wizard card padding if currently >16px — target 14-16px per wireframe.

Do NOT modify any wizard JavaScript — this is CSS + HTML structure only.
  </action>
  <verify>
    <automated>grep -c "margin-top.*auto\|flex-direction.*column" public/assets/css/wizard.css && grep "wiz-step" public/wizard.htmx.html | head -3</automated>
  </verify>
  <done>Each wizard step fits in 1080px viewport. Step-nav is pinned at bottom of card via flex margin-top:auto. Content area scrolls if needed. No JS changes required.</done>
</task>

</tasks>

<verification>
- Load settings page — sidebar nav visible on left (200px), content on right
- Click each settings tab — correct panel shows, active state highlights
- No JS console errors on settings page
- Load wizard — each step fits without page-level scroll at 1080px
- Step-nav buttons visible at bottom of card on all 4 steps
- No JS console errors on wizard page
</verification>

<success_criteria>
- Settings uses vertical sidebar nav layout per wireframe v3.19.2
- Settings tab switching works for all panels
- Wizard steps fit in 1080px viewport with step-nav pinned at bottom
- No broken JS functionality on either page
</success_criteria>

<output>
After completion, create `.planning/phases/20.3-page-layout-wireframe-alignment/20.3-02-SUMMARY.md`
</output>
