---
phase: 20.3-page-layout-wireframe-alignment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/pages.css
  - public/assets/css/hub.css
  - public/dashboard.htmx.html
  - public/hub.htmx.html
autonomous: true
requirements:
  - LAYOUT-DASHBOARD
  - LAYOUT-HUB
  - LAYOUT-TABLES

must_haves:
  truths:
    - "Dashboard shows ALL content without scroll at 1920x1080: urgent banner + KPI grid-4 + grid-2 panels + grid-3 shortcuts"
    - "Hub 2-column layout verified: stepper (220px) left, action card + checklist right, gap 24px — compacted to fit 1080px"
    - "Data tables across members, meetings, audit, archives, users pages use 8px vertical padding on td elements"
  artifacts:
    - path: "public/assets/css/pages.css"
      provides: "Dashboard density overrides and table compaction"
      contains: "dashboard-grid"
    - path: "public/assets/css/hub.css"
      provides: "Hub layout compaction"
      contains: "hub-layout"
  key_links:
    - from: "public/assets/css/pages.css"
      to: "public/dashboard.htmx.html"
      via: "Dashboard grid and panel classes"
      pattern: "dashboard-grid|dashboard-panel|kpi-card|shortcut-card"
---

<objective>
Compact dashboard to zero-scroll at 1080px, verify+compact hub layout, and apply 8px table row density across all data tables.

Purpose: Dashboard is the landing page and must be a zero-scroll action launcher at 1080px. Hub already has the correct 2-column structure but may need density reduction. All data tables (members, meetings, audit, archives, users) need 8px vertical padding for more visible rows.

Output: Updated pages.css with dashboard density overrides and table compaction, verified hub.css, minor HTML adjustments if needed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.3-page-layout-wireframe-alignment/20.3-CONTEXT.md
@.planning/phases/20.3-page-layout-wireframe-alignment/20.3-RESEARCH.md
@ag_vote_wireframe.html
@public/dashboard.htmx.html
@public/hub.htmx.html
@public/assets/css/pages.css
@public/assets/css/hub.css
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compact dashboard to zero-scroll at 1080px</name>
  <files>public/assets/css/pages.css, public/dashboard.htmx.html</files>
  <action>
FIRST: Read dashboard.htmx.html to understand current structure: urgent banner, KPI grid, grid-2 panels (sessions + tasks), grid-3 shortcuts.

THEN: Read pages.css to find current padding/margin values for `.dashboard-grid`, `.dashboard-panel`, `.kpi-card`, `.shortcut-card`, `.urgent-card`.

THEN: Read design-system.css for base `.kpi-card`, `.card`, `.card-body` padding values.

THEN: Read ag_vote_wireframe.html dashboard section to confirm target values.

Apply these CSS overrides in pages.css (scoped to dashboard context):

1. `.kpi-grid.dashboard-grid` — margin-bottom: 14px (was ~24px/1.5rem)
2. `.grid-2.dashboard-grid` — margin-bottom: 14px (was ~20px/1.25rem)
3. `.app-main .kpi-card` or `.dashboard-grid .kpi-card` — padding: 12px 10px (was ~20px from var(--space-5))
4. `.dashboard-panel` — padding: 14px (was ~20px)
5. `.dashboard-panel .flex-between` — margin-bottom: 8px (was ~12px)
6. `.shortcut-card` — padding: 14px (was ~20px 18px), gap: 12px (was ~16px)
7. `.urgent-card` — already 14px, verify and leave if correct

Also check `.page-header` on dashboard — if it has large padding/margin, compact it. Target: the page title row should use minimal vertical space.

Height budget at 1080px:
- Header: ~56px
- App-main padding: ~32px (16px top + 16px bottom)
- Page header: ~40px
- Urgent card: ~72px + 14px margin
- KPI row: ~70px + 14px margin
- Grid-2 panels: ~180px + 14px margin
- Grid-3 shortcuts: ~70px
- Footer: ~32px
Total must be <= 1080px

If dashboard.htmx.html has unnecessary wrapper divs adding height, simplify them.

Do NOT modify design-system.css. All overrides in pages.css scoped to dashboard classes.
  </action>
  <verify>
    <automated>grep -c "dashboard-grid\|dashboard-panel\|shortcut-card\|kpi-card" public/assets/css/pages.css</automated>
  </verify>
  <done>Dashboard fits entirely in 1920x1080 without scrollbar. All sections visible: urgent banner + KPIs + panels + shortcuts. Density matches wireframe v3.19.2.</done>
</task>

<task type="auto">
  <name>Task 2: Verify hub layout + compact density, apply table row 8px padding globally</name>
  <files>public/assets/css/hub.css, public/hub.htmx.html, public/assets/css/pages.css</files>
  <action>
PART A — Hub verification and compaction:

Read hub.htmx.html and hub.css. The hub already has `.hub-layout-body` (flex, gap:24px), `.hub-stepper-col` (width:220px), `.hub-main-col` (flex:1). Per RESEARCH.md pitfall 2, do NOT duplicate these classes.

Focus on COMPACTION only:
1. `.hub-identity` — reduce padding from ~1rem 1.5rem to 10px 14px if it saves meaningful height
2. `.hub-action .card-body` — reduce padding if >16px
3. `.hub-details-body` — verify it doesn't cause unnecessary scroll
4. Check overall hub height at 1080px — header (56px) + hub-identity + status-bar + layout-body must fit

Only make changes if hub currently scrolls at 1080px. If it already fits, document "verified, no changes needed" and move on.

PART B — Table row density (8px vertical padding):

Add a global table density rule in pages.css that applies 8px vertical padding to data table rows. Two approaches (pick the cleanest):

Option A — Utility class (preferred if tables use a common class):
```css
.data-table td {
  padding: 8px 12px;
}
.data-table th {
  padding: 8px 12px;
}
```
Then add `data-table` class to tables in: members.htmx.html, meetings.htmx.html, audit.htmx.html, archives.htmx.html, users.htmx.html.

Option B — Scoped selectors (if tables already have specific classes):
```css
.members-table td,
.meetings-table td,
.audit-table td,
.archives-table td,
.users-table td {
  padding: 8px 12px;
}
```

Read the actual table HTML to determine which approach is cleaner. Check if tables already have a common class like `.table` or page-specific table classes.

Do NOT reduce horizontal padding below 12px — this prevents horizontal overflow (per RESEARCH.md pitfall 4).
Do NOT modify design-system.css.
  </action>
  <verify>
    <automated>grep -c "8px 12px\|8px" public/assets/css/pages.css && grep -c "hub-identity\|hub-action" public/assets/css/hub.css</automated>
  </verify>
  <done>Hub fits at 1080px (verified or compacted). All data tables use 8px vertical padding on td elements, showing ~40% more rows per screen.</done>
</task>

</tasks>

<verification>
- Load dashboard at 1920x1080 — no scrollbar, all sections visible
- Load hub at 1920x1080 — stepper left, content right, no unnecessary scroll
- Load members page — table rows visibly more compact (8px padding)
- Load audit page — table rows compact
- Load meetings page — table rows compact
- No visual breakage on any modified page
</verification>

<success_criteria>
- Dashboard zero-scroll at 1080px with all sections visible
- Hub layout verified correct (220px stepper, 24px gap) and compacted if needed
- Data tables across 5+ pages use 8px vertical padding
- No design-system.css modifications
</success_criteria>

<output>
After completion, create `.planning/phases/20.3-page-layout-wireframe-alignment/20.3-03-SUMMARY.md`
</output>
