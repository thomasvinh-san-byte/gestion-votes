---
phase: 20.4-design-system-enforcement
plan: 07
type: execute
wave: 4
depends_on: ["20.4-01"]
files_modified:
  - public/hub.htmx.html
  - public/assets/css/hub.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Hub page fits on 1920x1080 without vertical scroll"
    - "Two-column layout matches wireframe: stepper + main content"
    - "Identity banner with persona accent border is present"
    - "Checklist and action card match wireframe density (14px margins)"
  artifacts:
    - path: "public/hub.htmx.html"
      provides: "Hub HTML aligned with PageHub wireframe"
    - path: "public/assets/css/hub.css"
      provides: "Hub CSS aligned with wireframe"
  key_links:
    - from: "public/hub.htmx.html"
      to: "public/assets/css/hub.css"
      via: "hub-specific classes"
      pattern: "\\.hub-"
---

<objective>
Reconstruct the hub page to match the PageHub wireframe section. Verify two-column layout (stepper + main), identity banner, status bar, action card, and checklist density.

Purpose: Hub is the session detail sheet — central navigation point for session management.
Output: Hub HTML/CSS aligned with wireframe v3.19.2 at 1920x1080.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.4-design-system-enforcement/20.4-RESEARCH.md

@ag_vote_wireframe.html
@public/hub.htmx.html
@public/assets/css/hub.css
@public/assets/css/pages.css
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align hub page with PageHub wireframe</name>
  <files>public/hub.htmx.html, public/assets/css/hub.css</files>
  <action>
1. Open ag_vote_wireframe.html and find `PageHub` component. Note: identity banner at top, status bar, two-column layout (stepper sidebar + main content area), action card, checklist.

2. Open hub.htmx.html and compare:

**Identity banner:**
- Wireframe shows session identity with border-top accent per persona
- Per decisions: `hub-identity` has `border-top` persona accent
- Verify styling matches wireframe

**Status bar:**
- Wireframe shows session status (draft/scheduled/live/closed/validated)
- Verify alignment and spacing

**Two-column layout:**
- Wireframe: stepper on left, main content on right
- Verify grid/flex setup creates this layout
- Check column widths match wireframe proportions

**Action card:**
- Primary action card (launch session, etc.)
- Verify card styling matches wireframe

**Checklist:**
- Checklist items (attendance, motions, vote, etc.)
- Check spacing between items (14px per RESEARCH)
- Verify checklist status indicators

3. RESEARCH rates hub as MEDIUM severity. Fix spacing discrepancies, ensure density matches wireframe.

4. Fix CSS in hub.css only. Use hub-scoped selectors. Do NOT modify design-system.css.

5. Ensure page fits at 1920x1080 without scroll.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -c "hub-identity\|hub-status\|hub-checklist" public/hub.htmx.html | xargs -I{} echo "hub structure elements: {}" && echo "PASS"</automated>
  </verify>
  <done>Hub page matches PageHub wireframe: identity banner, status bar, two-column layout, action card, checklist with 14px density. Fits at 1920x1080.</done>
</task>

</tasks>

<verification>
Open hub page at 1920x1080:
1. Identity banner at top with persona accent
2. Two-column layout: stepper + main
3. Checklist items with uniform spacing
4. No vertical scrollbar
</verification>

<success_criteria>
- Hub matches PageHub wireframe at 1920x1080
- Two-column layout proportions correct
- Density matches wireframe (14px margins)
- Scoped CSS only
</success_criteria>

<output>
After completion, create `.planning/phases/20.4-design-system-enforcement/20.4-07-SUMMARY.md`
</output>
