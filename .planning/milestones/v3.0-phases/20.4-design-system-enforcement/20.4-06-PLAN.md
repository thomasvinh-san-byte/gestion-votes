---
phase: 20.4-design-system-enforcement
plan: 06
type: execute
wave: 3
depends_on: ["20.4-01"]
files_modified:
  - public/vote.htmx.html
  - public/assets/css/vote.css
  - public/public.htmx.html
  - public/assets/css/public.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Vote page displays tablet frame (780px max) with 2x2 vote buttons matching wireframe"
    - "Public/projection page fills full screen with gradient and bar chart"
    - "Both pages fit their target displays without scroll"
  artifacts:
    - path: "public/vote.htmx.html"
      provides: "Vote HTML aligned with PageVotant wireframe"
    - path: "public/assets/css/vote.css"
      provides: "Vote CSS aligned with wireframe"
    - path: "public/public.htmx.html"
      provides: "Public HTML aligned with PageEcran wireframe"
    - path: "public/assets/css/public.css"
      provides: "Public CSS aligned with wireframe"
  key_links:
    - from: "public/vote.htmx.html"
      to: "public/assets/css/vote.css"
      via: "vote-specific classes"
      pattern: "\\.vote-"
    - from: "public/public.htmx.html"
      to: "public/assets/css/public.css"
      via: "projection-specific classes"
      pattern: "\\.projection-"
---

<objective>
Reconstruct the vote and public (projection) pages to match their wireframe sections. Both are standalone display pages (vote = tablet for voters, public = projector) with isolated CSS.

Purpose: Vote and public pages are live session displays — they must look polished for end users and audiences.
Output: Vote and public pages aligned with wireframe v3.19.2.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.4-design-system-enforcement/20.4-RESEARCH.md

@ag_vote_wireframe.html
@public/vote.htmx.html
@public/assets/css/vote.css
@public/public.htmx.html
@public/assets/css/public.css
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align vote page with PageVotant wireframe</name>
  <files>public/vote.htmx.html, public/assets/css/vote.css</files>
  <action>
1. Open ag_vote_wireframe.html and find `PageVotant` (or `PageVote`) component. Note: tablet frame (780px max-width), 2x2 vote button grid, confirmation inline display, header minimal.

2. Open vote.htmx.html and compare:

**Tablet frame:**
- Wireframe shows max-width ~780px centered container
- Verify `.vote-app` or equivalent has this constraint
- Check padding and centering

**2x2 vote buttons:**
- Wireframe shows 4 vote options (Pour, Contre, Abstention, NPPV) in a 2x2 grid
- Verify grid layout, gap (10px per wireframe), button sizing
- Per Phase 20.1 decisions: inline colored confirmation button

**Header:**
- Wireframe shows minimal header with motion title
- Verify no excessive spacing

3. RESEARCH rates vote page as LOW severity — it's well-isolated. Focus on verifying alignment rather than major reconstruction. Only fix actual discrepancies.

4. Fix CSS in vote.css only. Do NOT modify design-system.css.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -c "vote-app\|vote-grid\|vote-btn" public/vote.htmx.html | xargs -I{} echo "vote structure elements: {}" && echo "PASS"</automated>
  </verify>
  <done>Vote page matches PageVotant wireframe: tablet frame, 2x2 buttons, inline confirmation, minimal header.</done>
</task>

<task type="auto">
  <name>Task 2: Align public/projection page with PageEcran wireframe</name>
  <files>public/public.htmx.html, public/assets/css/public.css</files>
  <action>
1. Open ag_vote_wireframe.html and find `PageEcran` component. Note: full-screen gradient background, projection title (Fraunces font), bar chart for results, quorum bar.

2. Open public.htmx.html and compare:

**Gradient background:**
- Wireframe shows full-screen gradient
- Verify background covers the viewport

**Title (Fraunces):**
- Per Phase 20.1 decisions: `h1.projection-title` with Fraunces `clamp(32px,3.6vw,56px)`
- Verify this is in place

**Bar chart / results:**
- Wireframe shows horizontal bar chart for vote results
- Verify chart container sizing and spacing

**Quorum bar:**
- Per Phase 20.1 decisions: quorum bar moved to header with `projection-quorum` modifier (max-width 600px, 0.4vh padding)
- Verify this is in place

3. RESEARCH rates public page as LOW severity — well-implemented. Focus on verification, fix only actual discrepancies.

4. Fix CSS in public.css only. Do NOT modify design-system.css.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -c "projection-title\|projection-quorum" public/public.htmx.html | xargs -I{} echo "projection elements: {}" && echo "PASS"</automated>
  </verify>
  <done>Public/projection page matches PageEcran wireframe: gradient, Fraunces title, bar chart, quorum bar in header. Fills full screen.</done>
</task>

</tasks>

<verification>
1. Vote page: tablet frame centered, 2x2 buttons, inline confirmation
2. Public page: full-screen gradient, large title, bar chart, quorum bar
3. Neither page has unnecessary scroll
</verification>

<success_criteria>
- Vote page matches PageVotant wireframe
- Public page matches PageEcran wireframe
- Both pages display correctly on their target devices
</success_criteria>

<output>
After completion, create `.planning/phases/20.4-design-system-enforcement/20.4-06-SUMMARY.md`
</output>
