---
phase: 20.4-design-system-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/pages.css
  - public/assets/css/app.css
  - public/assets/css/wizard.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "No duplicate CSS property definitions within the same file (pages.css)"
    - "Grid gaps match wireframe value (10px) globally"
    - "page-header margin-bottom defined in exactly one place"
    - "wizard.css does not re-define classes already in design-system.css unless scoped"
  artifacts:
    - path: "public/assets/css/pages.css"
      provides: "Cleaned shared page styles with no duplicate blocks"
    - path: "public/assets/css/app.css"
      provides: "Single canonical page-header definition"
    - path: "public/assets/css/wizard.css"
      provides: "Wizard styles using global definitions where possible"
  key_links:
    - from: "public/assets/css/pages.css"
      to: "public/assets/css/design-system.css"
      via: "cascade order (design-system first, pages second)"
      pattern: "no conflicting overrides of same property at same specificity"
---

<objective>
Step 0: CSS Contradiction Cleanup — Remove duplicate/conflicting CSS rules accumulated across phases 20.1-20.3 before any per-page reconstruction begins.

Purpose: Establish a clean, predictable CSS cascade so that per-page fixes in subsequent plans don't get silently overridden by contradictory global rules.
Output: Clean pages.css, app.css, wizard.css with zero duplicate definitions and wireframe-aligned gap values.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.4-design-system-enforcement/20.4-RESEARCH.md

@public/assets/css/pages.css
@public/assets/css/app.css
@public/assets/css/wizard.css
@public/assets/css/design-system.css
@ag_vote_wireframe.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate definitions in pages.css and consolidate page-header</name>
  <files>public/assets/css/pages.css, public/assets/css/app.css</files>
  <action>
Fix the 4 contradictory CSS rules identified in RESEARCH.md:

1. **`.section-header` margin-bottom** — pages.css has TWO definitions (line ~916: `margin-bottom: 1.25rem` and line ~1277: `margin-bottom: 14px`). DELETE the first definition (line ~916 block) entirely. Keep the second (14px) which matches wireframe density.

2. **`.tab-toolbar` margin-bottom** — pages.css has TWO definitions (line ~181: `margin-bottom: 1.5rem` and line ~1284: `margin-bottom: 14px`). DELETE the first definition (line ~181 value). Keep the second (14px).

3. **`.page-header` margin-bottom** — Defined in app.css (line ~70: `var(--space-6)` = 24px) AND pages.css (line ~1267: `14px`). REMOVE the pages.css override and update app.css to use `margin-bottom: 14px` (wireframe density). This way page-header is defined in ONE canonical place (app.css).

4. **`.meeting-card` hover** — pages.css adds `transform: translateY(-2px)` on `.meeting-card:hover`, but `.meeting-card` already extends `.card-clickable` which has the same hover in design-system.css. REMOVE the duplicate `transform: translateY(-2px)` from pages.css `.meeting-card:hover` to avoid double-application. Keep any other hover styles (box-shadow changes are OK).

For each removal, add a brief comment like `/* consolidated — see app.css */` or `/* inherited from .card-clickable */` so future readers understand why the rule is absent.

DO NOT modify design-system.css. DO NOT change any selectors — only remove duplicate property declarations.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -n "section-header" public/assets/css/pages.css | grep "margin-bottom" | wc -l | xargs test 1 -eq && echo "section-header: 1 definition OK" && grep -n "tab-toolbar" public/assets/css/pages.css | grep "margin-bottom" | wc -l | xargs test 1 -eq && echo "tab-toolbar: 1 definition OK" && grep -n "page-header" public/assets/css/pages.css | grep -c "margin-bottom" | xargs test 0 -eq && echo "page-header: not in pages.css OK" && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>Each of the 4 contradictory rules has exactly ONE canonical definition. No duplicate margin-bottom for section-header, tab-toolbar, or page-header across files.</done>
</task>

<task type="auto">
  <name>Task 2: Align grid gaps to wireframe 10px and clean wizard.css re-definitions</name>
  <files>public/assets/css/pages.css, public/assets/css/wizard.css</files>
  <action>
**Grid gap alignment:**

1. In pages.css, find `.grid-2` gap definition (currently `var(--space-5, 1.25rem)` = 20px). Change to `gap: 10px` to match wireframe.
2. In pages.css, find `.grid-3` and `.grid-4` gap definitions. Change all to `gap: 10px` if they differ.
3. In wizard.css, find `.grid-2` and `.grid-4` local re-definitions. These currently set `gap: 12px`. Since the global is now 10px (matching wireframe), REMOVE the wizard.css grid gap overrides entirely. If wizard.css has `.grid-2` or `.grid-4` blocks that ONLY set gap, remove the entire block. If they set other properties too, remove only the gap line.

**Wizard field re-definitions cleanup:**

4. In wizard.css, find `.field-label` definition. Compare against design-system.css `.field-label`. If the wizard version only duplicates what's in design-system.css (font-size, font-weight, color, text-transform), REMOVE the wizard duplicate. If wizard adds truly unique properties (wizard-specific spacing), keep only those unique properties and add comment `/* extends design-system .field-label */`.

5. In wizard.css, find `.field-input` definition. The research notes it uses `background: var(--color-surface)` while wireframe uses `var(--surface-raised)`. Since design-system.css is canonical, REMOVE the wizard.css `.field-input` block if it only duplicates design-system values. If the `background` differs, update wizard.css to use `var(--color-surface-raised)` (the implementation token for wireframe's `--surface-raised`), but ONLY if design-system.css doesn't already set this. Check design-system.css first.

6. In wizard.css, find `.flex-between` if it exists. If it duplicates design-system.css, remove it.

DO NOT modify design-system.css.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && python3 -c "
import re
# Check grid gaps in pages.css
with open('public/assets/css/pages.css') as f: pages = f.read()
# Find .grid-2 gap
m = re.search(r'\.grid-2\s*\{[^}]*gap:\s*([^;]+);', pages)
if m and '10px' in m.group(1): print('grid-2 gap: 10px OK')
else: print('FAIL: grid-2 gap not 10px'); exit(1)
# Check wizard.css has no gap override for .grid-2
with open('public/assets/css/wizard.css') as f: wiz = f.read()
if re.search(r'\.grid-2\s*\{[^}]*gap:', wiz): print('FAIL: wizard.css still has grid-2 gap'); exit(1)
else: print('wizard.css grid-2 gap removed OK')
print('ALL CHECKS PASSED')
"</automated>
  </verify>
  <done>All grid gaps are 10px (wireframe value). wizard.css no longer re-defines classes that are already canonical in design-system.css or pages.css.</done>
</task>

</tasks>

<verification>
After both tasks:
1. Open any page in browser at 1920x1080 — no visual regressions from cascade cleanup
2. Grid spacing should be tighter (10px vs previous 12-20px)
3. Page headers should have consistent 14px bottom margin
</verification>

<success_criteria>
- Zero duplicate property definitions for the same selector within pages.css
- page-header margin-bottom defined in exactly one file (app.css)
- All .grid-N gap values are 10px
- wizard.css does not re-define global utility classes
</success_criteria>

<output>
After completion, create `.planning/phases/20.4-design-system-enforcement/20.4-01-SUMMARY.md`
</output>
