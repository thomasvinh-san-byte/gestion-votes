---
phase: 20.4-design-system-enforcement
plan: 08
type: execute
wave: 4
depends_on: ["20.4-01"]
files_modified:
  - public/postsession.htmx.html
  - public/assets/css/postsession.css
  - public/settings.htmx.html
  - public/assets/css/settings.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Post-session stepper and step panels match wireframe PagePostSession"
    - "Settings sidebar nav (200px) + content panels match wireframe PageParametres"
    - "Both pages fit on 1920x1080 without vertical scroll"
  artifacts:
    - path: "public/postsession.htmx.html"
      provides: "Post-session HTML aligned with wireframe"
    - path: "public/assets/css/postsession.css"
      provides: "Post-session CSS aligned with wireframe"
    - path: "public/settings.htmx.html"
      provides: "Settings HTML aligned with wireframe"
    - path: "public/assets/css/settings.css"
      provides: "Settings CSS aligned with wireframe"
  key_links:
    - from: "public/postsession.htmx.html"
      to: "public/assets/css/postsession.css"
      via: "postsession-specific classes"
      pattern: "\\.ps-|\\.postsession-"
    - from: "public/settings.htmx.html"
      to: "public/assets/css/settings.css"
      via: "settings-specific classes"
      pattern: "\\.settings-"
---

<objective>
Reconstruct post-session and settings pages to match their wireframe sections. Both are LOW severity per RESEARCH — primarily verification and minor fixes.

Purpose: Post-session completes the session lifecycle; settings configures the system. Both need consistent design language.
Output: Post-session and settings aligned with wireframe v3.19.2 at 1920x1080.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.4-design-system-enforcement/20.4-RESEARCH.md

@ag_vote_wireframe.html
@public/postsession.htmx.html
@public/assets/css/postsession.css
@public/settings.htmx.html
@public/assets/css/settings.css
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align post-session with PagePostSession wireframe</name>
  <files>public/postsession.htmx.html, public/assets/css/postsession.css</files>
  <action>
1. Open ag_vote_wireframe.html and find `PagePostSession` component. Note: segmented stepper (4 steps), step panels, exports layout.

2. Open postsession.htmx.html and compare:

**Segmented stepper:**
- Wireframe shows 4 step segments (verify, consolidate, generate PV, archive)
- Verify stepper bar styling matches wireframe
- Check active/completed step indicators

**Step panels:**
- Each step has a content panel
- Verify card padding (14px), spacing between elements
- Check that panel content fills available space

**Exports layout:**
- Step 3 (PV generation) has download/export actions
- Verify button placement and styling

3. RESEARCH rates this as LOW severity — clean CSS with good token usage. Focus on verifying rather than rebuilding. Fix only actual discrepancies.

4. Fix CSS in postsession.css only. Do NOT modify design-system.css.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && test -f public/postsession.htmx.html && test -f public/assets/css/postsession.css && echo "PASS"</automated>
  </verify>
  <done>Post-session matches PagePostSession wireframe: segmented stepper, step panels, exports layout. Fits at 1920x1080.</done>
</task>

<task type="auto">
  <name>Task 2: Align settings with PageParametres wireframe</name>
  <files>public/settings.htmx.html, public/assets/css/settings.css</files>
  <action>
1. Open ag_vote_wireframe.html and find `PageParametres` component. Note: sidebar nav (200px) + content panels.

2. Open settings.htmx.html and compare:

**Sidebar navigation:**
- Per Phase 20.3 decisions: sidebar HTML/JS already implemented, task is CSS verification + responsive breakpoints
- Verify sidebar width (~200px) matches wireframe
- Check nav items styling (active state, hover)

**Content panels:**
- Each settings section has a content panel
- Verify padding, spacing between form fields
- Check form field alignment with wireframe

**Responsive breakpoints:**
- Per Phase 20.3: responsive breakpoint was added
- Verify it collapses sidebar on narrow screens

3. RESEARCH rates this as LOW severity — clean token usage, responsive present. Verify and fix minor issues only.

4. Fix CSS in settings.css only. Do NOT modify design-system.css.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && test -f public/settings.htmx.html && test -f public/assets/css/settings.css && echo "PASS"</automated>
  </verify>
  <done>Settings matches PageParametres wireframe: 200px sidebar nav, content panels, responsive breakpoints. Fits at 1920x1080.</done>
</task>

</tasks>

<verification>
1. Post-session: segmented stepper with 4 steps, panels fill space
2. Settings: sidebar nav visible, content panels properly spaced
3. Neither page scrolls at 1920x1080
</verification>

<success_criteria>
- Post-session matches PagePostSession wireframe
- Settings matches PageParametres wireframe
- Both fit at 1920x1080 without scroll
- Scoped CSS only
</success_criteria>

<output>
After completion, create `.planning/phases/20.4-design-system-enforcement/20.4-08-SUMMARY.md`
</output>
