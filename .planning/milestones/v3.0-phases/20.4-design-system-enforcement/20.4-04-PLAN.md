---
phase: 20.4-design-system-enforcement
plan: 04
type: execute
wave: 2
depends_on: ["20.4-01"]
files_modified:
  - public/wizard.htmx.html
  - public/assets/css/wizard.css
  - public/assets/js/pages/wizard.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "Wizard steps fit on 1920x1080 without vertical scroll"
    - "Import button says 'Importer (.csv)' only — .xlsx removed from label and accept"
    - "Wizard field styles inherit from design-system.css (no local re-definitions after Plan 01)"
    - "Step navigation is pinned at bottom (margin-top:auto in flex column)"
  artifacts:
    - path: "public/wizard.htmx.html"
      provides: "Wizard HTML with fixed import button and wireframe-aligned layout"
    - path: "public/assets/css/wizard.css"
      provides: "Wizard CSS aligned with wireframe (post Plan 01 cleanup)"
    - path: "public/assets/js/pages/wizard.js"
      provides: "No .xlsx reference in file handling"
  key_links:
    - from: "public/wizard.htmx.html"
      to: "public/assets/js/pages/wizard.js"
      via: "import button click handler"
      pattern: "btnImportCSV|csvFileInput"
    - from: "public/wizard.htmx.html"
      to: "public/assets/css/wizard.css"
      via: "wizard-specific classes"
      pattern: "\\.wiz-"
---

<objective>
Reconstruct the wizard page to match the PageWizard wireframe section. Fix the import button bug (.xlsx not supported but advertised). Align step layout, field styles, and navigation with wireframe density.

Purpose: Wizard is session creation flow — third in critical path. Import button bug is user-facing and must be fixed.
Output: Wizard aligned with wireframe, import bug fixed, fits at 1920x1080.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.4-design-system-enforcement/20.4-RESEARCH.md

@ag_vote_wireframe.html
@public/wizard.htmx.html
@public/assets/css/wizard.css
@public/assets/js/pages/wizard.js
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix wizard import button bug and align with PageWizard wireframe</name>
  <files>public/wizard.htmx.html, public/assets/css/wizard.css, public/assets/js/pages/wizard.js</files>
  <action>
**Bug fix — import button (.xlsx not supported):**

1. In wizard.htmx.html, find the import button (id="btnImportCSV" around line 198-207):
   - Change button label from "Importer (.csv, .xlsx)" to "Importer (.csv)"
   - Change the file input `accept` attribute from ".csv,.xlsx" to ".csv"

2. In wizard.js, find `handleCsvFile()` (around line 322). No code change needed since it already only handles CSV. But verify there's no `.xlsx` reference in the function comments or error messages — if there is, update them to say CSV only.

**Wireframe alignment:**

3. Open ag_vote_wireframe.html and find `PageWizard` component. Note: stepper progress bar at top, card-based step content, pinned step-nav at bottom.

4. Compare wizard.htmx.html structure:

**Stepper progress:**
- Verify stepper bar matches wireframe design (numbered steps, active state highlight)
- Check spacing/padding

**Step content area:**
- Wireframe shows each step in a card with flex-column layout
- Check card padding matches wireframe (14px)
- Step content should fill available height

**Step navigation (bottom):**
- Per Phase 20.3 decision: `.step-nav` uses `margin-top:auto` in flex column — verify this is still in place
- Navigation buttons (Precedent/Suivant) should be at the bottom of the step card

**Field styles:**
- After Plan 01 cleaned wizard.css re-definitions, verify fields now inherit from design-system.css properly
- If any visual issues remain (wrong background, wrong border), fix with scoped `.wiz-step .field-input` selectors in wizard.css

5. Fix all CSS discrepancies in wizard.css using scoped selectors. Do NOT modify design-system.css.

6. Ensure each wizard step fits at 1920x1080 without scroll.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -q 'accept=".csv"' public/wizard.htmx.html && echo "accept attribute: .csv only OK" && grep -q "Importer (.csv)" public/wizard.htmx.html && echo "button label: .csv only OK" && ! grep -q "\.xlsx" public/wizard.htmx.html && echo "no .xlsx reference in HTML OK" && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>Import button shows "Importer (.csv)" and only accepts .csv files. Wizard layout matches PageWizard wireframe: stepper, card steps, pinned nav. Fits at 1920x1080.</done>
</task>

</tasks>

<verification>
1. Open wizard page — import button says "Importer (.csv)" not "Importer (.csv, .xlsx)"
2. Click import button — file picker only shows .csv files
3. Each wizard step fits at 1920x1080 without scroll
4. Step navigation buttons pinned at bottom of card
5. Field styles consistent with design-system (no visual breaks)
</verification>

<success_criteria>
- Import button bug fixed (no .xlsx reference)
- Wizard matches PageWizard wireframe layout
- Step navigation uses margin-top:auto pattern
- No vertical scroll at 1920x1080
</success_criteria>

<output>
After completion, create `.planning/phases/20.4-design-system-enforcement/20.4-04-SUMMARY.md`
</output>
