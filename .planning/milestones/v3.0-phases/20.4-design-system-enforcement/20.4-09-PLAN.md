---
phase: 20.4-design-system-enforcement
plan: 09
type: execute
wave: 5
depends_on: ["20.4-01"]
files_modified:
  - public/admin.htmx.html
  - public/assets/css/admin.css
  - public/members.htmx.html
  - public/assets/css/members.css
  - public/analytics.htmx.html
  - public/assets/css/analytics.css
  - public/archives.htmx.html
  - public/assets/css/archives.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Admin page tabs and user table match wireframe PageUtilisateurs"
    - "Members page card-based layout matches wireframe PageMembres"
    - "Analytics chart grid and KPI cards match wireframe PageStats"
    - "Archives data table matches wireframe PageArchives"
  artifacts:
    - path: "public/admin.htmx.html"
      provides: "Admin HTML aligned with wireframe"
    - path: "public/members.htmx.html"
      provides: "Members HTML aligned with wireframe"
    - path: "public/analytics.htmx.html"
      provides: "Analytics HTML aligned with wireframe"
    - path: "public/archives.htmx.html"
      provides: "Archives HTML aligned with wireframe"
  key_links: []
---

<objective>
Batch reconstruction of secondary pages group 1: admin, members, analytics, archives. These are lower-severity pages that need wireframe verification and minor fixes.

Purpose: Secondary pages must maintain the same design language established on critical path pages.
Output: 4 secondary pages aligned with wireframe v3.19.2.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.4-design-system-enforcement/20.4-RESEARCH.md

@ag_vote_wireframe.html
@public/admin.htmx.html
@public/assets/css/admin.css
@public/members.htmx.html
@public/assets/css/members.css
@public/analytics.htmx.html
@public/assets/css/analytics.css
@public/archives.htmx.html
@public/assets/css/archives.css
@public/assets/css/pages.css
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align admin and members pages with wireframe</name>
  <files>public/admin.htmx.html, public/assets/css/admin.css, public/members.htmx.html, public/assets/css/members.css</files>
  <action>
**Admin page (PageUtilisateurs):**
1. Open ag_vote_wireframe.html, find `PageUtilisateurs`. Note: admin tabs, user table.
2. Compare admin.htmx.html. RESEARCH rates LOW severity — well-structured.
3. Verify: tab styling, table density (8px 12px per wireframe), spacing.
4. Fix only actual discrepancies in admin.css.

**Members page (PageMembres):**
1. Find `PageMembres` in wireframe. Note: card-based member list (not table, per decisions).
2. Compare members.htmx.html. RESEARCH rates MEDIUM — 1,142 lines CSS, may have inherited style conflicts from app.css `.member-card`.
3. Check: card layout grid, card padding (14px), gap (10px).
4. If `.member-card` in members.css conflicts with app.css, use scoped selectors to resolve.
5. Fix in members.css only.

For both: ensure they fit at 1920x1080 without unnecessary scroll (some scroll acceptable for long member lists/user tables, but the chrome/header shouldn't waste space).
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && test -f public/admin.htmx.html && test -f public/members.htmx.html && echo "PASS"</automated>
  </verify>
  <done>Admin tabs + user table match wireframe. Members card-based layout aligned with wireframe density. No style conflicts.</done>
</task>

<task type="auto">
  <name>Task 2: Align analytics and archives pages with wireframe</name>
  <files>public/analytics.htmx.html, public/assets/css/analytics.css, public/archives.htmx.html, public/assets/css/archives.css</files>
  <action>
**Analytics page (PageStats):**
1. Find `PageStats` in wireframe. Note: chart grid, KPI cards, tab content.
2. Compare analytics.htmx.html. RESEARCH rates LOW severity.
3. Verify: chart grid gap (10px), KPI card padding, tab styling.
4. Print styles with !important are acceptable (standard pattern).
5. Fix only actual discrepancies in analytics.css.

**Archives page (PageArchives):**
1. Find `PageArchives` in wireframe. Note: data table with pagination.
2. Compare archives.htmx.html. RESEARCH rates MEDIUM — uses shared table styles from pages.css.
3. Verify: table density (8px 12px cells), pagination styling, header sticky if present.
4. After Plan 01 fixed global table density, verify archives benefits correctly.
5. Fix in archives.css or with scoped selectors. Do NOT modify pages.css.

For both: reasonable scroll acceptable for data-heavy pages, but header area shouldn't waste space.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && test -f public/analytics.htmx.html && test -f public/archives.htmx.html && echo "PASS"</automated>
  </verify>
  <done>Analytics chart grid and KPI cards match wireframe. Archives table density correct with proper pagination. Both aligned with design language.</done>
</task>

</tasks>

<verification>
1. Admin: tabs work, user table is dense and readable
2. Members: card grid with proper spacing
3. Analytics: charts and KPIs properly laid out
4. Archives: table with pagination, correct density
</verification>

<success_criteria>
- All 4 secondary pages match their wireframe sections
- Consistent design language with critical path pages
- No cross-page style leaks
</success_criteria>

<output>
After completion, create `.planning/phases/20.4-design-system-enforcement/20.4-09-SUMMARY.md`
</output>
