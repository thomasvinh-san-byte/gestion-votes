---
phase: 20.1-refonte-ui-alignement-wireframe-et-reduction-charge-mentale
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/public.htmx.html
  - public/assets/css/public.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Projection page has a gradient background instead of a flat solid color"
    - "Meeting title is large (clamp 32px-56px) using Fraunces display font"
    - "Quorum bar is integrated in the header area, not buried in main content"
    - "Result bars scale responsively from small screens to 4m projectors via clamp()"
    - "Theme toggle works — gradient switches between dark and light palettes"
    - "FOUC is eliminated on the projection page"
  artifacts:
    - path: "public/public.htmx.html"
      provides: "Restructured projection page with title in Fraunces, quorum bar in header, FOUC inline style"
    - path: "public/assets/css/public.css"
      provides: "Gradient background, clamp() scaling, Fraunces typography, quorum bar header integration"
  key_links:
    - from: "public/assets/css/public.css"
      to: "public/public.htmx.html"
      via: "CSS classes for gradient, projection-title, quorum-row"
      pattern: "linear-gradient.*0B0F1A|projection-title|font-display"
---

<objective>
Refonte de la page projection salle : fond gradient, titre Fraunces grand, barre quorum dans le header, barres de resultats clamp() responsive, mode clair/sombre.

Purpose: L'ecran de projection est vu par toute la salle. Il doit avoir un caractere "Acte Officiel" — gradient profond, typographie imposante, lisibilite depuis le fond de salle (4m). Le wireframe v3.19.2 PageEcran est la reference.
Output: Page projection avec composition wireframe, fond gradient, titre lisible, quorum integre.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.1-refonte-ui-alignement-wireframe-et-reduction-charge-mentale/20.1-CONTEXT.md
@.planning/phases/20.1-refonte-ui-alignement-wireframe-et-reduction-charge-mentale/20.1-RESEARCH.md
@public/public.htmx.html
@public/assets/css/public.css
@ag_vote_wireframe.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: FOUC fix + gradient background + header restructuring</name>
  <files>public/public.htmx.html, public/assets/css/public.css</files>
  <action>
1. **FOUC fix** — In `public.htmx.html`, add an inline `<style id="critical-tokens">` block right after `<meta charset="UTF-8">` and BEFORE the `<script src="/assets/js/theme-init.js">` line:
   ```html
   <style id="critical-tokens">
     :root { --color-bg: #EDECE6; --color-surface: #FAFAF7; --color-text: #151510; }
     [data-theme="dark"] { --color-bg: #0B0D10; --color-surface: #141820; --color-text: #ECF0FA; }
     html, body { background: var(--color-bg); }
   </style>
   ```
   Note: public.htmx.html also has an inline script that forces dark theme. The `<style>` must come BEFORE both scripts so the dark token values resolve immediately.

2. **Gradient background** — In `public.css`, update `body.projection-body`:
   ```css
   body.projection-body {
     min-height: 100vh;
     display: flex;
     flex-direction: column;
     overflow: hidden;
     background: linear-gradient(170deg, #0B0F1A, #111827);
   }
   [data-theme="light"] body.projection-body {
     background: linear-gradient(170deg, #F8FAFC, #EEF2FF);
   }
   ```
   This ensures the gradient switches when the theme toggle is used. The dark gradient is the default (forced by the inline script).

3. **Header restructure** — In `public.htmx.html`, restructure `.projection-header`:
   - The meeting title (`#meeting_title`) should use a new class `projection-title` and move to a prominent position.
   - Move the quorum visual bar (`#quorumVisual`) from `<main>` into the header area — place it directly after the title row. This makes quorum immediately visible.
   - Keep status badge, motion counter, clock, and controls in the header.

   New header structure:
   ```html
   <header class="projection-header" role="banner">
     <div class="projection-header-top">
       <div class="header-left">
         <span class="status-badge off" id="badge">hors seance</span>
         <span class="motion-counter" id="motionCounter" hidden>
           <span id="motionCounterText">-- / --</span>
         </span>
       </div>
       <h1 class="projection-title" id="meeting_title">Aucune seance</h1>
       <div class="projection-header-right">
         <div class="clock" id="clock">--:--</div>
         <div class="projection-controls">
           <!-- keep existing controls unchanged -->
         </div>
       </div>
     </div>
     <!-- Quorum bar moved here from main -->
     <div class="quorum-visual projection-quorum" id="quorumVisual" hidden>
       <!-- keep existing quorum HTML unchanged -->
     </div>
   </header>
   ```

4. **Header CSS** — In `public.css`:
   - `.projection-header` background becomes transparent (let gradient show through) with a subtle `border-bottom: 1px solid rgba(255,255,255,0.08)`.
   - `.projection-header-top` is `display: flex; align-items: center; justify-content: space-between; padding: var(--space-4) var(--space-8);`
   - `.projection-title`: `font-family: var(--font-display); font-size: clamp(32px, 3.6vw, 56px); font-weight: 800; letter-spacing: -0.02em; line-height: 1.1; color: var(--color-text); text-align: center; flex: 1;`
   - `.projection-quorum` (modifier): `padding: 0.4vh var(--space-8); max-width: 600px; margin: 0 auto;`

5. **Motion title Fraunces** — Add to `public.css`:
   ```css
   .motion-title,
   .projection-title {
     font-family: var(--font-display);
   }
   ```

6. **Clamp() scaling for result bars** — Update the Phase 7.3 clamp section in `public.css`:
   - `.bar-wrapper` height: `clamp(24px, 3vw, 48px)` (was fixed 32px)
   - `.bar-item` grid: `grid-template-columns: clamp(4rem, 8vw, 8rem) 1fr clamp(3.5rem, 6vw, 6rem)` for responsive label/value columns
   - `.bar-percentage`: already has clamp, verify `clamp(1.25rem, 3vw, 2.5rem)` for larger projectors
   - `.bar-label`: `font-size: clamp(0.875rem, 2vw, 1.5rem)` (increase max for projector)

7. **Decision section scaling** — `.decision-value`: `font-size: clamp(1.25rem, 3vw, 2.5rem)` for projector readability.

IMPORTANT: Do NOT rename any existing element IDs (`meeting_title`, `badge`, `motionCounter`, `clock`, `quorumVisual`, `quorumVisualFill`, `quorumSeuil`, etc.) — they are all referenced by public.js. Do NOT modify public.js — CSS/HTML changes only.
  </action>
  <verify>
    <automated>grep -c "critical-tokens" public/public.htmx.html && grep -c "linear-gradient.*0B0F1A" public/assets/css/public.css && grep -c "font-display" public/assets/css/public.css && grep -c "projection-title" public/public.htmx.html && grep -c "projection-quorum" public/public.htmx.html</automated>
  </verify>
  <done>Projection page has: FOUC-preventing inline style, gradient background with light/dark variants, Fraunces title at clamp(32px,3.6vw,56px), quorum bar moved to header, result bars with clamp() scaling for projector readability. All existing IDs preserved, public.js untouched.</done>
</task>

</tasks>

<verification>
- Open `public.htmx.html` in browser at 1920x1080: gradient background visible, meeting title in Fraunces large font
- Toggle theme (sun/moon button): gradient switches between dark and light
- Quorum bar visible in header area below title
- Result bars readable at large viewport (simulating projector)
- DevTools Network throttle 3G: no FOUC flash
- `vendor/bin/phpunit --testsuite Unit` passes
</verification>

<success_criteria>
- Gradient background on projection page (dark: #0B0F1A->#111827, light: #F8FAFC->#EEF2FF)
- Meeting title in Fraunces at clamp(32px, 3.6vw, 56px)
- Quorum bar in header area
- Theme toggle switches gradient
- FOUC eliminated
- No JS console errors
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-refonte-ui-alignement-wireframe-et-reduction-charge-mentale/20.1-02-SUMMARY.md`
</output>
