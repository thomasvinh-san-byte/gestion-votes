---
phase: quick
plan: 260331-7s9
type: execute
wave: 1
depends_on: []
files_modified:
  - data/sample-members.csv
  - public/assets/demo_membres_ag.csv
  - public/wizard.htmx.html
  - public/assets/js/pages/wizard.js
  - public/members.htmx.html
  - public/assets/js/pages/members.js
  - public/index.html
  - public/help.htmx.html
  - public/assets/js/pages/operator-attendance.js
  - public/assets/js/core/utils.js
autonomous: true
requirements: [QUICK-remove-voting-weight-ui]

must_haves:
  truths:
    - "No mention of ponderation/voting weight/poids de vote visible in any UI page"
    - "Sample CSV files have no ponderation column"
    - "Wizard does not show voting power toggle"
    - "Members page does not show voting power KPI, sort, or edit fields"
    - "Backend PHP, database schema, and unit tests are untouched"
  artifacts:
    - path: "data/sample-members.csv"
      provides: "Clean sample CSV without ponderation column"
    - path: "public/assets/demo_membres_ag.csv"
      provides: "Clean demo CSV without ponderation column"
    - path: "public/wizard.htmx.html"
      provides: "Wizard without voting power toggle"
    - path: "public/members.htmx.html"
      provides: "Members page without voting weight references"
  key_links:
    - from: "public/assets/js/core/utils.js"
      to: "CSV import flow"
      via: "CSV parsing still accepts voting_power column silently"
      pattern: "voting_power|poids|ponderation"
---

<objective>
Remove all user-facing references to voting weight/ponderation from the UI, sample CSVs, and help text.

Purpose: The user no longer uses weighted votes. The backend already defaults voting_power to 1.0, so this is purely a UI cleanup — hide the feature without breaking the engine.
Output: All 10 files cleaned of voting weight UI references. Backend untouched.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Key constraint: Backend PHP (VoteEngine, QuorumEngine, MemberRepository, BallotsController, AttendanceRepository), database schema (voting_power column), and unit tests MUST NOT be modified. They all default to 1.0 and remain correct. CSV import in utils.js should still silently accept a ponderation/voting_power column if present — just don't advertise it in UI or help text.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean CSV files and HTML templates (wizard, members, index, help)</name>
  <files>
    data/sample-members.csv,
    public/assets/demo_membres_ag.csv,
    public/wizard.htmx.html,
    public/members.htmx.html,
    public/index.html,
    public/help.htmx.html
  </files>
  <action>
**CSV files:**
- `data/sample-members.csv` — Remove the `ponderation` column entirely (header and all row values). Keep all other columns intact.
- `public/assets/demo_membres_ag.csv` — Same: remove `ponderation` column entirely.

**public/wizard.htmx.html:**
- Around lines 274-280, find the "Activer la ponderation des voix" toggle checkbox and its surrounding container/label. Remove the entire toggle block. Do NOT remove other wizard toggles nearby.

**public/members.htmx.html:**
- Line ~44: subtitle "Gestion des participants et ponderation des voix" — change to "Gestion des participants" (remove the ponderation part).
- Line ~118: "Verifier les ponderations" KPI label — remove or replace with a different meaningful KPI label (e.g., remove the entire KPI card if it only shows voting weight info; or rename to something relevant like "Participants inscrits" if the underlying data is member count).
- Line ~174: popover about "Nombre de voix" — remove the popover or simplify its text to remove voting weight explanation.
- Line ~301: CSV help text that mentions the ponderation column — remove the mention of ponderation from the help text. Keep the rest of the CSV format help.

**public/index.html:**
- Line ~305: "Ponderation des voix" feature card on landing page — remove the entire feature card. If it's in a grid, ensure the remaining cards still display properly (no gap).

**public/help.htmx.html:**
- Line ~420: CSV help section mentioning `voting_power` or `ponderation` column — remove those column references from the CSV format documentation.
- Line ~443: FAQ item about "poids vote ponderation" — remove the entire FAQ item (question + answer).
  </action>
  <verify>
    grep -rn "ponderation\|pondération\|Pondération\|Ponderation\|voting.power\|poids.de.vote\|voting_power" data/sample-members.csv public/assets/demo_membres_ag.csv public/wizard.htmx.html public/members.htmx.html public/index.html public/help.htmx.html || echo "CLEAN — no voting weight references found"
  </verify>
  <done>All 6 HTML/CSV files contain zero user-facing references to voting weight, ponderation, or poids de vote. CSV files have the ponderation column fully removed.</done>
</task>

<task type="auto">
  <name>Task 2: Clean JavaScript files (wizard.js, members.js, operator-attendance.js, utils.js)</name>
  <files>
    public/assets/js/pages/wizard.js,
    public/assets/js/pages/members.js,
    public/assets/js/pages/operator-attendance.js,
    public/assets/js/core/utils.js
  </files>
  <action>
**public/assets/js/pages/wizard.js:**
- Lines ~69-79: Remove the `toggleVotingPower()` function entirely.
- Lines ~991-997: Remove the toggle event handler that calls toggleVotingPower.
- Remove any draft saving/loading of `votingPowerEnabled` state.
- Keep all other wizard.js functionality intact. Search for any other references to "votingPower", "voting_power", "ponderation" and remove UI-facing ones.

**public/assets/js/pages/members.js:**
- Remove the `formatVotingPower()` function.
- Remove voting power KPI card rendering/update logic.
- Remove "sort by power" option from any sort dropdowns.
- Remove voting power display in member detail view.
- Remove voting power edit input in member edit form.
- Keep the rest of members.js intact — member list, CRUD, other KPIs, etc.

**public/assets/js/pages/operator-attendance.js:**
- Lines ~167, 176: CSV import placeholder text mentions `voting_power` — simplify the placeholder to remove voting_power mention. Keep the rest of the CSV import logic working.

**public/assets/js/core/utils.js:**
- Lines ~458, 478, 489, 497, 589: CSV parsing that maps `voting_power`/`poids`/`weight`/`ponderation` columns. KEEP the parsing logic (so CSV imports with these columns still work silently) but remove any preview table column headers or user-facing labels that expose voting weight. The mapping from CSV column to `voting_power` field should remain functional — just hidden from preview UI.
- If there is a preview table that shows voting_power as a visible column, remove that column from the preview rendering. The data still gets parsed and sent to the server.
  </action>
  <verify>
    # Verify no UI-facing voting weight text remains (but backend mapping in utils.js is OK)
    grep -n "formatVotingPower\|toggleVotingPower\|votingPowerEnabled\|sort.*power\|Pondération\|pondération" public/assets/js/pages/wizard.js public/assets/js/pages/members.js public/assets/js/pages/operator-attendance.js || echo "CLEAN — no voting weight UI functions remain"
  </verify>
  <done>
    - wizard.js: toggleVotingPower function and handler removed, no votingPowerEnabled state
    - members.js: formatVotingPower removed, no voting power KPI/sort/display/edit
    - operator-attendance.js: CSV placeholder simplified, no voting_power mention
    - utils.js: CSV parsing still silently maps ponderation/voting_power columns, but preview table does not expose them
    - All other functionality in these files remains intact
  </done>
</task>

</tasks>

<verification>
1. Run: `grep -rn "ponderation\|pondération\|Pondération\|voting.power\|poids.de.vote\|formatVotingPower\|toggleVotingPower\|votingPowerEnabled" data/sample-members.csv public/assets/demo_membres_ag.csv public/wizard.htmx.html public/members.htmx.html public/index.html public/help.htmx.html public/assets/js/pages/wizard.js public/assets/js/pages/members.js public/assets/js/pages/operator-attendance.js` — should return NOTHING.
2. Run: `grep -n "voting_power\|poids\|weight\|ponderation" public/assets/js/core/utils.js` — should show ONLY the silent CSV column mapping (parser logic), not UI labels.
3. Run: `php artisan test --testsuite=Unit` — all existing unit tests pass (backend untouched).
4. Verify `data/sample-members.csv` and `public/assets/demo_membres_ag.csv` are valid CSV (correct column count per row, no orphaned commas).
</verification>

<success_criteria>
- Zero user-visible references to voting weight/ponderation in the UI
- Sample CSVs have no ponderation column
- CSV import still silently accepts ponderation/voting_power if present
- All backend PHP code, database schema, and unit tests remain untouched
- All PHPUnit tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260331-7s9-remove-voting-weight-ponderation-from-ui/260331-7s9-SUMMARY.md`
</output>
