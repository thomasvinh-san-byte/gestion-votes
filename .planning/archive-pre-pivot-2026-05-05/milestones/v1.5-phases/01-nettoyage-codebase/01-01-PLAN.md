---
phase: 01-nettoyage-codebase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/js/components/ag-searchable-select.js
  - public/assets/js/components/index.js
  - public/assets/js/core/page-components.js
  - public/assets/js/core/shared.js
  - public/assets/js/core/shell.js
  - public/assets/js/core/event-stream.js
  - public/assets/js/pages/analytics-dashboard.js
  - public/assets/js/pages/audit.js
  - public/assets/js/pages/email-templates-editor.js
  - public/assets/js/pages/hub.js
  - public/assets/js/pages/operator-realtime.js
  - public/assets/js/pages/operator-tabs.js
  - public/assets/js/pages/public.js
  - public/assets/js/pages/settings.js
  - public/assets/js/pages/vote.js
  - public/assets/js/services/meeting-context.js
  - public/assets/css/postsession.css
  - app/Core/Security/PermissionChecker.php
  - tests/Unit/PermissionCheckerTest.php
  - tests/Integration/AdminCriticalPathTest.php
  - app/Services/VoteTokenService.php
  - tests/Unit/VoteTokenServiceTest.php
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "grep -rn 'console.log\\|console.warn\\|console.error' public/assets/js/ returns only the 4 critical handlers in core/utils.js"
    - "grep -rn 'PermissionChecker' app/ returns zero results"
    - "grep -rn 'TODO\\|FIXME' public/assets/js/ public/assets/css/ returns zero results"
    - "VoteTokenService has no deprecated methods (validate, consume)"
  artifacts:
    - path: "app/Core/Security/PermissionChecker.php"
      provides: "DELETED"
    - path: "tests/Unit/PermissionCheckerTest.php"
      provides: "DELETED"
    - path: "app/Services/VoteTokenService.php"
      provides: "validate() and consume() methods removed"
  key_links:
    - from: "tests/Integration/AdminCriticalPathTest.php"
      to: "app/Core/Security/PermissionChecker.php"
      via: "import/use statement"
      pattern: "PermissionChecker"
---

<objective>
Remove all production noise from JS/CSS (console statements, TODOs) and delete dead PHP code (PermissionChecker class, VoteTokenService deprecated methods).

Purpose: Satisfy CLEAN-01 (zero console.log), CLEAN-02 (zero deprecated code), CLEAN-03 (zero TODO/FIXME) so the codebase is clean before service refactoring begins.
Output: Clean JS/CSS files with no debug noise, deleted PermissionChecker, trimmed VoteTokenService.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-nettoyage-codebase/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove console statements from JS and TODO from CSS</name>
  <files>
    public/assets/js/components/ag-searchable-select.js
    public/assets/js/components/index.js
    public/assets/js/core/page-components.js
    public/assets/js/core/shared.js
    public/assets/js/core/shell.js
    public/assets/js/core/event-stream.js
    public/assets/js/pages/analytics-dashboard.js
    public/assets/js/pages/audit.js
    public/assets/js/pages/email-templates-editor.js
    public/assets/js/pages/hub.js
    public/assets/js/pages/operator-realtime.js
    public/assets/js/pages/operator-tabs.js
    public/assets/js/pages/public.js
    public/assets/js/pages/settings.js
    public/assets/js/pages/vote.js
    public/assets/js/services/meeting-context.js
    public/assets/css/postsession.css
  </files>
  <read_first>
    public/assets/js/core/utils.js (to confirm the 4 critical handlers at lines ~663, 666, 710, 713 that must be KEPT)
    public/assets/css/postsession.css (to find the TODO at line 31)
  </read_first>
  <action>
Remove all console.log, console.warn, console.error statements from the JS files listed in the RESEARCH inventory "To REMOVE" section (approximately 30 statements across 16 files). For each file:

1. Read the file first
2. Remove the console statement line entirely. If the console statement is the only line in a catch block, replace it with a comment `// silent` or leave the catch block empty with a comment.
3. For `.catch(console.error)` patterns (vote.js:1252), replace with `.catch(() => {})` or `.catch(() => { /* silent */ })`

PRESERVE the 4 critical error handlers in core/utils.js (lines ~663, 666, 710, 713) -- do NOT touch utils.js.

For the 2 JSDoc comment examples (ag-searchable-select.js:21, page-components.js:29): rewrite the JSDoc example to not use console.log. Replace with a descriptive comment like `// handle value change` or use an event handler example instead.

For event-stream.js: remove both console.warn statements (lines ~43 and ~144). The polling fallback handles these cases silently.

For postsession.css line 31: remove the `/* TODO: HTML partial needs .form-grid on form containers */` comment entirely.

CRITICAL: Do NOT modify core/utils.js. The 4 console.error calls there are critical infrastructure error handlers.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -rn 'console\.\(log\|warn\|error\)' public/assets/js/ | grep -v 'core/utils.js' | wc -l</automated>
    Expected: 0

    <automated>cd /home/user/gestion_votes_php && grep -rn 'TODO\|FIXME' public/assets/js/ public/assets/css/ | wc -l</automated>
    Expected: 0
  </verify>
  <acceptance_criteria>
    - `grep -rn 'console\.\(log\|warn\|error\)' public/assets/js/ | grep -v 'core/utils.js'` returns 0 lines
    - `grep -rn 'console\.\(log\|warn\|error\)' public/assets/js/core/utils.js` returns exactly 4 lines (the critical handlers)
    - `grep -rn 'TODO\|FIXME' public/assets/js/ public/assets/css/` returns 0 lines
    - No JS syntax errors introduced (files are syntactically valid)
  </acceptance_criteria>
  <done>Zero console statements outside utils.js critical handlers. Zero TODO/FIXME in JS/CSS.</done>
</task>

<task type="auto">
  <name>Task 2: Delete PermissionChecker and VoteTokenService deprecated methods</name>
  <files>
    app/Core/Security/PermissionChecker.php
    tests/Unit/PermissionCheckerTest.php
    tests/Integration/AdminCriticalPathTest.php
    app/Services/VoteTokenService.php
    tests/Unit/VoteTokenServiceTest.php
  </files>
  <read_first>
    app/Core/Security/PermissionChecker.php (confirm deprecated, identify full content)
    tests/Unit/PermissionCheckerTest.php (confirm it only tests PermissionChecker)
    tests/Integration/AdminCriticalPathTest.php (check if it imports/uses PermissionChecker -- need to update or delete)
    app/Services/VoteTokenService.php (find validate() at ~line 95 and consume() at ~line 132, confirm @deprecated)
    tests/Unit/VoteTokenServiceTest.php (find test methods that call validate() and consume() -- lines 242, 268, 291, 298, 309, 440, 446, 461, 473, 517)
  </read_first>
  <action>
1. DELETE `app/Core/Security/PermissionChecker.php` entirely (227 LOC deprecated class, zero production callers).

2. DELETE `tests/Unit/PermissionCheckerTest.php` entirely (tests for deleted class).

3. UPDATE `tests/Integration/AdminCriticalPathTest.php`: Remove any `use AgVote\Core\Security\PermissionChecker` import and any test methods that instantiate or reference PermissionChecker. If the entire file only tests PermissionChecker, delete it. If it tests other things too, only remove the PermissionChecker-related parts.

4. UPDATE `app/Services/VoteTokenService.php`: Delete the `validate()` method (starts at ~line 95, ~31 LOC, marked @deprecated) and the `consume()` method (starts at ~line 132, ~19 LOC, marked @deprecated). Keep all other methods intact. Run `php -l app/Services/VoteTokenService.php` to verify syntax.

5. UPDATE `tests/Unit/VoteTokenServiceTest.php`: Delete all test methods that call `->validate()` or `->consume()` on VoteTokenService. Based on research these are at lines ~242, 268, 291, 298, 309, 440, 446, 461, 473, 517. Keep all other test methods intact. Run `php -l tests/Unit/VoteTokenServiceTest.php` to verify syntax.

6. Run `timeout 60 php vendor/bin/phpunit tests/Unit/VoteTokenServiceTest.php --no-coverage` to confirm remaining tests pass.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -rn 'PermissionChecker' app/ tests/ | wc -l</automated>
    Expected: 0

    <automated>cd /home/user/gestion_votes_php && grep -n '@deprecated' app/Services/VoteTokenService.php | wc -l</automated>
    Expected: 0

    <automated>cd /home/user/gestion_votes_php && timeout 60 php vendor/bin/phpunit tests/Unit/VoteTokenServiceTest.php --no-coverage 2>&1 | tail -3</automated>
    Expected: OK (no failures)
  </verify>
  <acceptance_criteria>
    - `grep -rn 'PermissionChecker' app/ tests/` returns 0 lines
    - `app/Core/Security/PermissionChecker.php` does not exist
    - `tests/Unit/PermissionCheckerTest.php` does not exist
    - `grep -n '@deprecated' app/Services/VoteTokenService.php` returns 0 lines
    - `timeout 60 php vendor/bin/phpunit tests/Unit/VoteTokenServiceTest.php --no-coverage` passes green
  </acceptance_criteria>
  <done>PermissionChecker fully deleted. VoteTokenService has no deprecated methods. All remaining VoteTokenService tests pass.</done>
</task>

</tasks>

<verification>
Phase-level checks for CLEAN-01, CLEAN-02, CLEAN-03:
1. `grep -rn 'console\.\(log\|warn\|error\)' public/assets/js/` returns ONLY the 4 lines in core/utils.js
2. `grep -rn 'PermissionChecker' app/` returns zero
3. `grep -rn 'TODO\|FIXME' public/assets/js/ public/assets/css/` returns zero
4. `grep -rn '@deprecated' app/Services/VoteTokenService.php` returns zero
</verification>

<success_criteria>
- CLEAN-01: Only 4 critical console.error in utils.js remain; all others removed
- CLEAN-02: PermissionChecker deleted; VoteTokenService validate()/consume() deleted; all tests green
- CLEAN-03: Zero TODO/FIXME in JS/CSS
</success_criteria>

<output>
After completion, create `.planning/phases/01-nettoyage-codebase/01-01-SUMMARY.md`
</output>
