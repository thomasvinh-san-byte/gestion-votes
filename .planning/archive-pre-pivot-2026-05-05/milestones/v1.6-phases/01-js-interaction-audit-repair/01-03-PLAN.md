---
phase: 01-js-interaction-audit-repair
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/dashboard.htmx.html
  - public/assets/js/pages/dashboard.js
  - public/admin.htmx.html
  - public/assets/js/pages/admin.js
  - public/users.htmx.html
  - public/assets/js/pages/users.js
  - public/trust.htmx.html
  - public/assets/js/pages/trust.js
  - public/hub.htmx.html
  - public/assets/js/pages/hub.js
  - public/archives.htmx.html
  - public/assets/js/pages/archives.js
  - public/audit.htmx.html
  - public/assets/js/pages/audit.js
  - public/analytics.htmx.html
  - public/assets/js/pages/analytics-dashboard.js
  - public/docs.htmx.html
  - public/assets/js/pages/docs-viewer.js
  - public/email-templates.htmx.html
  - public/assets/js/pages/email-templates-editor.js
  - public/help.htmx.html
  - public/assets/js/pages/help-faq.js
  - public/public.htmx.html
  - public/assets/js/pages/public.js
  - public/report.htmx.html
  - public/assets/js/pages/report.js
autonomous: true
requirements: [JSFIX-01, JSFIX-02, JSFIX-03]

must_haves:
  truths:
    - "All 14 pages load without JS errors — every selector resolves to existing DOM elements"
    - "Every button/action on all 14 pages responds to click and produces the expected effect"
    - "All forms on all 14 pages submit via HTMX without full page reload"
  artifacts:
    - path: "public/dashboard.htmx.html"
      provides: "Dashboard page with valid DOM targets"
    - path: "public/admin.htmx.html"
      provides: "Admin page with valid DOM targets"
    - path: "public/trust.htmx.html"
      provides: "Trust delegation page with valid DOM targets"
    - path: "public/analytics.htmx.html"
      provides: "Analytics page with valid DOM targets"
  key_links:
    - from: "public/assets/js/pages/dashboard.js"
      to: "public/dashboard.htmx.html"
      via: "querySelector for dashboard widgets"
      pattern: "querySelector|getElementById"
    - from: "public/assets/js/pages/trust.js"
      to: "public/trust.htmx.html"
      via: "querySelector for delegation forms"
      pattern: "querySelector|hx-post"
    - from: "public/assets/js/pages/analytics-dashboard.js"
      to: "public/analytics.htmx.html"
      via: "querySelector for chart containers"
      pattern: "querySelector|getElementById"
---

<objective>
Audit and fix JS/HTMX interactions on the remaining 14 pages: dashboard, admin, users, trust, hub, archives, audit, analytics, docs, email-templates, help, public, report. These are supporting/admin pages with moderate to low JS complexity (98-984 LOC each).

Purpose: Complete the 21-page audit. While individually simpler than operator/vote/wizard, these pages collectively cover admin, analytics, documentation, and trust delegation — all must work correctly.
Output: All JS selectors resolve, all HTMX forms submit correctly, all buttons respond on all 14 pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-js-interaction-audit-repair/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix admin, users, trust, hub, archives, audit, and dashboard pages</name>
  <files>
    public/dashboard.htmx.html, public/assets/js/pages/dashboard.js,
    public/admin.htmx.html, public/assets/js/pages/admin.js,
    public/users.htmx.html, public/assets/js/pages/users.js,
    public/trust.htmx.html, public/assets/js/pages/trust.js,
    public/hub.htmx.html, public/assets/js/pages/hub.js,
    public/archives.htmx.html, public/assets/js/pages/archives.js,
    public/audit.htmx.html, public/assets/js/pages/audit.js
  </files>
  <action>
    For each of the 7 pages, perform the standard audit:

    **Per page:**

    1. **Read HTML and JS files** completely.

    2. **Build DOM inventory:** Extract all element IDs, selector-targeted classes, and HTMX attributes (hx-target, hx-post, hx-get, hx-patch, hx-delete, hx-swap).

    3. **Cross-reference JS selectors:** Find every querySelector/querySelectorAll/getElementById/getElementsByClassName call. Verify each resolves to an element in the HTML.

    4. **Check event handlers:** Verify addEventListener targets exist, HTMX event hooks reference real elements.

    5. **Check HTMX forms:** Verify endpoints, hx-target, hx-swap for all HTMX-submitting elements.

    6. **Fix all issues found.** Per D-03: minimal fix for simple issues, root cause fix for recurring patterns.

    **Page-specific checks:**

    - **dashboard (217 LOC JS):** Verify widget containers exist for data population, meeting quick-action buttons work
    - **admin (598 LOC JS):** Verify tenant management CRUD modals, user management actions, system config forms
    - **users (469 LOC JS):** Verify user create/edit/delete modals, role assignment dropdowns, password reset buttons
    - **trust (673 LOC JS):** Verify delegation create/revoke forms, trust chain display, auditor/assessor actions
    - **hub (656 LOC JS):** Verify meeting selection, hub navigation, quick-access buttons
    - **archives (587 LOC JS):** Verify archive list filtering, meeting detail expansion, export buttons
    - **audit (704 LOC JS):** Verify audit log filtering, date range selectors, export functionality
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && for page in dashboard admin users trust hub archives audit; do js_file="public/assets/js/pages/${page}.js"; html="public/${page}.htmx.html"; if [ -f "$js_file" ]; then broken=$(grep -oP "getElementById\(['\"]([^'\"]+)['\"]\)" "$js_file" | grep -oP "(?<=')[^']+(?=')" | while read id; do grep -q "id=\"$id\"" "$html" || echo "$id"; done); if [ -n "$broken" ]; then echo "BROKEN in $page: $broken"; fi; fi; done</automated>
  </verify>
  <done>
    All JS selectors in dashboard.js, admin.js, users.js, trust.js, hub.js, archives.js, and audit.js resolve to real DOM elements. All HTMX forms submit correctly. All action buttons target valid elements. Zero dead selectors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix analytics, docs, email-templates, help, public, and report pages</name>
  <files>
    public/analytics.htmx.html, public/assets/js/pages/analytics-dashboard.js,
    public/docs.htmx.html, public/assets/js/pages/docs-viewer.js,
    public/email-templates.htmx.html, public/assets/js/pages/email-templates-editor.js,
    public/help.htmx.html, public/assets/js/pages/help-faq.js,
    public/public.htmx.html, public/assets/js/pages/public.js,
    public/report.htmx.html, public/assets/js/pages/report.js
  </files>
  <action>
    For each of the 6 pages, perform the standard audit. Note: some JS files have different names than their HTML counterparts — use the correct mapping:
    - analytics.htmx.html -> analytics-dashboard.js
    - docs.htmx.html -> docs-viewer.js
    - email-templates.htmx.html -> email-templates-editor.js
    - help.htmx.html -> help-faq.js
    - public.htmx.html -> public.js
    - report.htmx.html -> report.js

    Also check if help.htmx.html (625 LOC, no separate JS file listed earlier — verify it loads help-faq.js or has inline JS).

    **Per page:**

    1. **Read HTML and JS files** completely.

    2. **Build DOM inventory:** Extract all element IDs, selector-targeted classes, and HTMX attributes.

    3. **Cross-reference JS selectors:** Verify every querySelector/getElementById call resolves to a real element.

    4. **Check event handlers:** Verify addEventListener targets and HTMX hooks.

    5. **Check HTMX forms:** Verify endpoints, hx-target, hx-swap.

    6. **Fix all issues found.**

    **Page-specific checks:**

    - **analytics (705 LOC JS):** Verify Chart.js canvas containers exist, data loading selectors work, date range filters
    - **docs (290 LOC JS):** Verify document viewer navigation, markdown rendering container
    - **email-templates (317 LOC JS):** Verify template editor load/save, preview functionality, variable insertion
    - **help (136 LOC JS):** Verify FAQ accordion expand/collapse, search functionality if present
    - **public (555 LOC JS):** Verify public voting interface, ballot submission, result display
    - **report (140 LOC JS):** Verify report generation triggers, print/export buttons, PDF download
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && for pair in "analytics:analytics-dashboard" "docs:docs-viewer" "email-templates:email-templates-editor" "help:help-faq" "public:public" "report:report"; do page="${pair%%:*}"; js="${pair##*:}"; js_file="public/assets/js/pages/${js}.js"; html="public/${page}.htmx.html"; if [ -f "$js_file" ]; then broken=$(grep -oP "getElementById\(['\"]([^'\"]+)['\"]\)" "$js_file" | grep -oP "(?<=')[^']+(?=')" | while read id; do grep -q "id=\"$id\"" "$html" || echo "$id"; done); if [ -n "$broken" ]; then echo "BROKEN in $page: $broken"; fi; fi; done</automated>
  </verify>
  <done>
    All JS selectors in analytics-dashboard.js, docs-viewer.js, email-templates-editor.js, help-faq.js, public.js, and report.js resolve to real DOM elements. Chart.js containers exist in analytics HTML. Template editor targets valid elements. Zero dead selectors across all 6 pages.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All 14 pages have been audited — every JS file's selectors cross-referenced against its HTML
2. Grep confirms zero broken getElementById/querySelector references across all files
3. All HTMX hx-target values resolve to existing elements
4. Combined with Plans 01 and 02, all 21 pages are now fully audited
</verification>

<success_criteria>
- Zero broken selectors across all 14 pages
- All HTMX form submissions have valid targets and endpoints
- All action buttons and modals target real DOM elements
- Combined with Plans 01 and 02: all 21 HTMX pages audited and fixed (JSFIX-01, JSFIX-02, JSFIX-03 fully satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/01-js-interaction-audit-repair/01-03-SUMMARY.md`
</output>
