---
phase: 01-js-interaction-audit-repair
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/wizard.htmx.html
  - public/assets/js/pages/wizard.js
  - public/postsession.htmx.html
  - public/assets/js/pages/postsession.js
  - public/meetings.htmx.html
  - public/assets/js/pages/meetings.js
  - public/members.htmx.html
  - public/assets/js/pages/members.js
  - public/validate.htmx.html
  - public/assets/js/pages/validate.js
  - public/settings.htmx.html
  - public/assets/js/pages/settings.js
autonomous: true
requirements: [JSFIX-01, JSFIX-02, JSFIX-03]

must_haves:
  truths:
    - "wizard.htmx.html loads without JS errors — all 4 wizard steps navigate correctly"
    - "postsession.htmx.html loads without JS errors — all buttons and forms work"
    - "meetings.htmx.html loads without JS errors — meeting CRUD actions work"
    - "members.htmx.html loads without JS errors — member management actions work"
    - "validate.htmx.html loads without JS errors — validation actions work"
    - "settings.htmx.html loads without JS errors — settings forms submit via HTMX"
  artifacts:
    - path: "public/wizard.htmx.html"
      provides: "Wizard page with valid DOM targets for wizard.js"
    - path: "public/assets/js/pages/wizard.js"
      provides: "4-step wizard navigation and form handling"
    - path: "public/meetings.htmx.html"
      provides: "Meeting list/CRUD page with valid DOM targets"
    - path: "public/members.htmx.html"
      provides: "Member management page with valid DOM targets"
  key_links:
    - from: "public/assets/js/pages/wizard.js"
      to: "public/wizard.htmx.html"
      via: "querySelector for step navigation and form inputs"
      pattern: "querySelector|getElementById"
    - from: "public/assets/js/pages/meetings.js"
      to: "public/meetings.htmx.html"
      via: "querySelector for meeting cards and CRUD modals"
      pattern: "querySelector|hx-post|hx-delete"
    - from: "public/assets/js/pages/members.js"
      to: "public/members.htmx.html"
      via: "querySelector for member table and import forms"
      pattern: "querySelector|hx-post|hx-patch"
---

<objective>
Audit and fix JS/HTMX interactions on 6 workflow pages: wizard (1690 LOC), postsession (1185 LOC), meetings (1138 LOC), members (1422 LOC), validate (559 LOC), and settings (1211 LOC). These are core workflow pages where users create meetings, manage members, and validate results.

Purpose: These pages handle the primary user workflow (create meeting -> manage members -> run meeting -> validate). Broken interactions here directly impact core functionality.
Output: All JS selectors resolve, all HTMX forms submit correctly, all buttons respond to clicks on these 6 pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-js-interaction-audit-repair/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix wizard, postsession, and validate pages</name>
  <files>
    public/wizard.htmx.html,
    public/assets/js/pages/wizard.js,
    public/postsession.htmx.html,
    public/assets/js/pages/postsession.js,
    public/validate.htmx.html,
    public/assets/js/pages/validate.js
  </files>
  <action>
    For each of the 3 pages (wizard, postsession, validate), perform the full audit:

    **Per page:**

    1. **Read HTML and JS files** completely.

    2. **Build DOM inventory:** Extract all element IDs, selector-targeted classes, and HTMX attributes (hx-target, hx-post, hx-get, hx-patch, hx-delete, hx-swap).

    3. **Cross-reference JS selectors:** Find every querySelector/querySelectorAll/getElementById/getElementsByClassName call in the JS. Verify each selector resolves to an element in the HTML.

    4. **Check event handlers:** Find every addEventListener call. Verify the target element exists and the event name is valid. Check HTMX event hooks (htmx:afterSwap, htmx:beforeRequest, etc.).

    5. **Check HTMX forms:** For every element with hx-post/hx-patch/hx-delete:
       - Verify endpoint path matches API pattern (/api/v1/...)
       - Verify hx-target resolves to existing element
       - Verify hx-swap value is valid

    6. **Fix all issues found.** Per D-03: minimal fix for simple issues, root cause fix for recurring patterns. If HTML structure from v4.2 redesign is the root cause, fix the HTML.

    **Wizard-specific checks (1172 LOC JS):**
    - Verify step navigation (step 1-4 transitions) — selectors for step containers must exist
    - Verify form validation on each step before advancing
    - Check that `form-grid-2` class (from design-system.css) targets exist in wizard HTML

    **Postsession-specific checks (657 LOC JS):**
    - Verify report generation and export buttons work
    - Check modal triggers for PV review

    **Validate-specific checks (302 LOC JS):**
    - Verify validation approval/rejection buttons
    - Check status update HTMX submissions
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && for page in wizard postsession validate; do js_files=$(ls public/assets/js/pages/${page}.js 2>/dev/null); html="public/${page}.htmx.html"; if [ -n "$js_files" ]; then broken=$(grep -oP "getElementById\(['\"]([^'\"]+)['\"]\)" $js_files | grep -oP "(?<=')[^']+(?=')" | while read id; do grep -q "id=\"$id\"" "$html" || echo "$id"; done); if [ -n "$broken" ]; then echo "BROKEN in $page: $broken"; fi; fi; done</automated>
  </verify>
  <done>
    All JS selectors in wizard.js, postsession.js, and validate.js resolve to real DOM elements in their HTML files. Wizard step navigation targets exist. All HTMX forms submit without page reload. Zero dead selectors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix meetings, members, and settings pages</name>
  <files>
    public/meetings.htmx.html,
    public/assets/js/pages/meetings.js,
    public/members.htmx.html,
    public/assets/js/pages/members.js,
    public/settings.htmx.html,
    public/assets/js/pages/settings.js
  </files>
  <action>
    For each of the 3 pages (meetings, members, settings), perform the full audit:

    **Per page:**

    1. **Read HTML and JS files** completely.

    2. **Build DOM inventory:** Extract all element IDs, selector-targeted classes, and HTMX attributes.

    3. **Cross-reference JS selectors:** Find every querySelector/querySelectorAll/getElementById call. Verify each resolves to an element in the HTML.

    4. **Check event handlers:** Verify addEventListener targets exist, event names are valid, HTMX hooks target real elements.

    5. **Check HTMX forms:** Verify endpoints, hx-target elements, hx-swap values for all HTMX-submitting elements.

    6. **Fix all issues found.** Use judgment on fix depth per D-03.

    **Meetings-specific checks (859 LOC JS):**
    - Verify meeting create/edit/delete modals open and submit
    - Check meeting status change buttons (open, close, archive)
    - Verify meeting list filtering/sorting

    **Members-specific checks (1060 LOC JS):**
    - Verify member add/edit/remove modals
    - Check CSV/XLSX import form and file upload handling
    - Verify member search/filter functionality
    - Check proxy/delegation assignment buttons

    **Settings-specific checks (815 LOC JS):**
    - Verify settings form sections save independently
    - Check tenant configuration toggles
    - Verify password change and email settings forms
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && for page in meetings members settings; do js_files=$(ls public/assets/js/pages/${page}.js 2>/dev/null); html="public/${page}.htmx.html"; if [ -n "$js_files" ]; then broken=$(grep -oP "getElementById\(['\"]([^'\"]+)['\"]\)" $js_files | grep -oP "(?<=')[^']+(?=')" | while read id; do grep -q "id=\"$id\"" "$html" || echo "$id"; done); if [ -n "$broken" ]; then echo "BROKEN in $page: $broken"; fi; fi; done</automated>
  </verify>
  <done>
    All JS selectors in meetings.js, members.js, and settings.js resolve to real DOM elements. Meeting CRUD modals target valid elements. Member import forms submit via HTMX. Settings forms save without page reload. Zero dead selectors.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Grep all 6 JS files for querySelector/getElementById — every extracted selector must match an element in the corresponding HTML
2. Grep all HTMX attributes in the 6 HTML files — every hx-target must resolve
3. No orphaned event listeners on non-existent elements
</verification>

<success_criteria>
- Zero broken selectors across wizard.js, postsession.js, validate.js, meetings.js, members.js, settings.js
- All HTMX form submissions have valid targets and endpoints on all 6 pages
- Wizard step navigation works (step container selectors valid)
- Member import and meeting CRUD modals target real DOM elements
</success_criteria>

<output>
After completion, create `.planning/phases/01-js-interaction-audit-repair/01-02-SUMMARY.md`
</output>
