---
phase: 03-wizard-single-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/wizard.css
  - public/wizard.htmx.html
autonomous: true
requirements:
  - WIZ-01

must_haves:
  truths:
    - "Step 0 (Informations generales) fits in viewport on 1080p without scrolling when Regles de vote is collapsed"
    - "Step 1 (Participants) fits in viewport on 1080p without scrolling when member list is empty or has few entries"
    - "Step 2 (Resolutions) fits in viewport on 1080p without scrolling when resolution list is empty"
    - "Step 3 (Revision) fits in viewport on 1080p without scrolling with typical recap content"
    - "Existing stepper navigation and step-nav buttons remain functional"
    - "Form fields use horizontal layout via form-grid-2 where applicable"
  artifacts:
    - path: "public/assets/css/wizard.css"
      provides: "Compacted wizard CSS with reduced spacing for viewport fit"
      contains: "wiz-step-body"
    - path: "public/wizard.htmx.html"
      provides: "Wizard HTML with form-grid-2 on remaining form groups"
      contains: "form-grid-2"
  key_links:
    - from: "public/wizard.htmx.html"
      to: "public/assets/css/wizard.css"
      via: "CSS classes"
      pattern: "wiz-step-body|wiz-section|step-nav"
---

<objective>
Compact the 4-step meeting creation wizard so each step fits within a single 1080p viewport without vertical scrolling. Apply form-grid-2 horizontal layouts to remaining form groups, reduce vertical spacing (padding, margins, gaps) in wizard CSS, and compact the stepper and navigation areas.

Purpose: WIZ-01 — the wizard must be usable without scrolling on a standard 1080p monitor.
Output: Modified wizard.css and wizard.htmx.html with compact, viewport-fitting layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-wizard-single-page/03-CONTEXT.md
@public/wizard.htmx.html
@public/assets/css/wizard.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compact wizard CSS spacing for 1080p viewport fit</name>
  <files>public/assets/css/wizard.css</files>
  <action>
Read the full wizard.css file and apply these spacing reductions targeting 1080p viewport fit. The app-header + stepper + step-body + step-nav + footer must all fit within 1080px height.

**Stepper (.wiz-progress-wrap):**
- Reduce margin-bottom from `var(--space-4)` to `var(--space-2)` (or 8px)
- Reduce padding from `4px` to `2px`
- Reduce `.wiz-step-item` padding from `8px 16px` to `6px 12px`

**Step body (.wiz-step-body):**
- Reduce padding from `1rem 1.5rem` to `0.75rem 1.5rem` (tighter vertical, keep horizontal)

**Sections (.wiz-section):**
- Reduce margin-bottom from `1rem` to `0.5rem`
- Reduce padding-top from `0.75rem` to `0.5rem`

**Section titles (.wiz-section-title):**
- Reduce margin-bottom from `0.5rem` to `0.25rem`
- Reduce font-size from `0.8125rem` to `0.75rem`

**Required note (.wiz-required-note):**
- Reduce margin-bottom from `0.75rem` to `0.375rem`

**Field labels (.wiz-step-body .field-label):**
- Reduce margin-bottom from `8px` to `4px`
- Reduce font-size from `0.875rem` to `0.8125rem`

**Step navigation (.step-nav):**
- Reduce padding from `12px 1.5rem` to `8px 1.5rem`

**Advanced toggle (.wiz-advanced-toggle):**
- Reduce margin-top from `0.5rem` to `0.25rem`
- Reduce `.wiz-advanced-summary` padding from `10px 14px` to `6px 14px`
- Reduce `.wiz-advanced-body` padding from `0 14px 14px` to `0 14px 10px`

**Content area (.wiz-content):**
- Reduce padding from `var(--space-4)` to `var(--space-2)` (reduce vertical breathing room around step card)

**Template grid (.wiz-template-grid):**
- Reduce margin-bottom from `1rem` to `0.5rem`
- Reduce gap from `10px` to `8px`
- Reduce `.wiz-template-card` padding from `12px` to `8px`

**Resolution add (.reso-add-trigger):**
- Reduce margin from `12px 0` to `6px 0`

**Alerts (.wiz-step .alert):**
- Reduce margin-bottom from `1rem` to `0.5rem`

**Members (Step 1):**
- Reduce `.members-action-row` margin-bottom from `1rem` to `0.5rem`
- Reduce `.upload-zone` padding from `12px 16px` to `8px 12px`
- Reduce `.upload-zone` margin-bottom from `0.75rem` to `0.5rem`
- Reduce `.wiz-member-add-row` padding from `12px 14px` to `8px 12px`
- Reduce `.wiz-member-add-row` margin-bottom from `1rem` to `0.5rem`

**Error banner (.wiz-error-banner):**
- Reduce padding from `10px 14px` to `6px 14px`

**Responsive 1024px breakpoint:**
- Update `.wiz-step-body` padding to `0.75rem 1rem` (was `1.25rem`)
- Update `.step-nav` padding to `8px 1rem` (was `1rem 1.25rem`)

Do NOT change font sizes of input fields, select options, or button text — only labels, section titles, and spacing.
Do NOT change the mobile breakpoint rules (<768px) — mobile layout is separate.
Keep all existing CSS custom property references (design tokens).
  </action>
  <verify>
    <automated>php -r "echo 'CSS syntax not validated by PHP, checking file exists and is non-empty: '; echo filesize('public/assets/css/wizard.css') > 500 ? 'OK' : 'FAIL';" && echo "" && grep -c "0\.5rem\|0\.25rem\|0\.375rem" public/assets/css/wizard.css | xargs -I{} test {} -ge 8 && echo "Spacing reductions applied"</automated>
  </verify>
  <done>
    - .wiz-step-body padding reduced to 0.75rem vertical
    - .wiz-section margins reduced to 0.5rem
    - .step-nav padding reduced to 8px vertical
    - .wiz-progress-wrap margin and padding reduced
    - .wiz-content padding reduced
    - All spacing reductions applied consistently
    - No mobile breakpoint changes
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply form-grid-2 to remaining wizard form groups and verify viewport fit</name>
  <files>public/wizard.htmx.html</files>
  <action>
Read the full wizard.htmx.html. The following form groups already use `form-grid-2`: the Type+DateTime row (line 123), the Lieu section (line 153), the Quorum+Majority row (line 174), and the resolution add panel Title+Majority row (line 374).

Check each step for remaining stacked form groups that could benefit from horizontal layout. Based on the HTML structure:

**Step 1 (Participants):** The member-add row (`.wiz-member-add-row`) is already horizontal flex. No changes needed — this step uses a list pattern, not form fields.

**Step 2 (Resolutions):** The add-resolution panel already has `form-grid-2` on Title+Majority. The Description textarea and Vote secret chips should remain full-width. No additional form-grid-2 needed.

**Step 3 (Revision):** This is a read-only recap with alerts. No form grids needed.

**Step 0 (General info):** Already has form-grid-2 in all sections. Confirm the Titre field is intentionally full-width (it is — single important field). No changes needed.

Since all form groups already use form-grid-2 where appropriate, the HTML task focuses on:

1. Add `class="wiz-step-subtitle"` to the page subtitle (line 50) if not already present — confirm it is (it is: `class="page-sub wiz-step-subtitle"`).

2. Verify the `wiz-date-time-row` inside the first `form-grid-2` does not break grid layout — it is a nested grid which is fine.

3. No HTML structure changes needed — the Phase 2 work already applied form-grid-2 to all wizard form groups.

**Viewport verification:** After CSS changes from Task 1, verify each step fits by measuring approximate content height:
- Header area: ~80px (breadcrumb + title + subtitle)
- Stepper: ~40px (reduced)
- Content padding: ~16px (reduced wiz-content)
- Step body: variable per step
- Step nav: ~40px (reduced)
- Footer: ~40px
- Total chrome: ~216px, leaving ~864px for step content on 1080p

Step 0 with collapsed advanced section: sections + fields ~ 400px. Fits.
Step 1 with empty list: action row + dropzone + add form + total row ~ 300px. Fits.
Step 2 with no resolutions: template grid + add button ~ 250px. Fits.
Step 3: recap + alerts ~ 350px. Fits.

If any HTML adjustments are needed after reviewing, make them. Otherwise confirm the HTML is already optimized.
  </action>
  <verify>
    <automated>grep -c "form-grid-2" public/wizard.htmx.html | xargs -I{} test {} -ge 4 && echo "form-grid-2 present (4+ instances)" && php -r "echo file_exists('public/wizard.htmx.html') ? 'HTML OK' : 'MISSING';"</automated>
  </verify>
  <done>
    - All wizard form groups use form-grid-2 where fields can be side-by-side
    - Full-width fields (titre, description, file upload) remain full-width
    - Stepper, step-nav, and step toggling remain unchanged and functional
    - Each step content area fits within ~864px available height on 1080p
  </done>
</task>

</tasks>

<verification>
1. Open wizard page in browser at 1080p resolution
2. Step through all 4 steps — each should fit without vertical scrolling
3. Step 0: with "Regles de vote" collapsed, no scroll needed
4. Step 1: with empty member list, no scroll needed
5. Step 2: with template cards visible, no scroll needed
6. Step 3: recap with alerts visible, no scroll needed
7. Stepper navigation clicks work correctly
8. Prev/Next buttons work correctly
9. Form fields are readable and inputs are usable at reduced spacing
</verification>

<success_criteria>
- All 4 wizard steps fit within a 1080p viewport without vertical scrolling (WIZ-01)
- form-grid-2 applied to all eligible form groups
- Spacing reduced consistently without breaking readability
- No regression in step navigation or form submission
</success_criteria>

<output>
After completion, create `.planning/phases/03-wizard-single-page/03-01-SUMMARY.md`
</output>
