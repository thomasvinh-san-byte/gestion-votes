---
phase: 12-page-by-page-mvp-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/settings.css
  - tests/e2e/specs/critical-path-settings.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "settings page main panel uses full viewport width on >=768px (no artificial container limit)"
    - "settings.css contains zero hex/oklch/rgba color literals — every color is a design-system token"
    - "Each primary settings interaction (tab switch, save, SMTP test) is asserted by Playwright via observable result (DOM update or 2xx API)"
  artifacts:
    - path: public/assets/css/settings.css
      provides: settings page styles, full-width container
      contains: "max-width: 100%"
    - path: tests/e2e/specs/critical-path-settings.spec.js
      provides: function gate for settings page
      contains: "test.describe"
  key_links:
    - from: tests/e2e/specs/critical-path-settings.spec.js
      to: public/settings.htmx.html
      via: "page.goto('/settings.htmx.html') + locator click + assert observable"
      pattern: "settings.htmx.html"
---

<objective>
Phase 12 Wave 1 / Page 1: Sweep `/settings` so it passes the 3 MVP gates (width, design tokens, function).

Purpose: settings is the most-cited "theatre" page in v1.2-PAGES-AUDIT.md. Phase 11 already wired the broken backend bindings; Phase 12 now proves the visible UI is full-width, on-brand, and provably functional.

Output: settings.css full-width + token-pure, plus a `critical-path-settings.spec.js` Playwright spec that asserts the primary interactions produce real, observable results.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-PAGES-AUDIT.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/settings.htmx.html
@public/assets/css/settings.css
@tests/e2e/specs/critical-path-admin.spec.js
@tests/e2e/helpers.js
</context>

<interfaces>
Existing helper (extract):
```js
// tests/e2e/helpers.js
const { loginAsAdmin } = require('../helpers');
// usage: await loginAsAdmin(page);
```

Existing CSRF pattern (from critical-path-admin.spec.js):
```js
const csrfResp = await page.request.get('/api/v1/auth_csrf.php');
const csrfBody = await csrfResp.json();
const csrfToken = csrfBody?.token || csrfBody?.data?.token || csrfBody?.csrf_token || '';
// header: { 'X-Csrf-Token': csrfToken }
```

Settings page primary interactive IDs (from public/settings.htmx.html):
- Tabs: `[data-stab="regles"]`, `[data-stab="communication"]`, `[data-stab="securite"]`, `[data-stab="accessibilite"]`
- Tab panels: `#stab-regles`, `#stab-communication`, `#stab-securite`, `#stab-accessibilite`
- Wired fields: `#settVoteMode`, `#settQuorumThreshold`, `#settMajority`, `#settSmtpHost`, `#settSmtpPort`, `#settSessionTimeout`
- Buttons: `#btnTestSmtp`, `#btnSaveTemplate`, `#btnAddQuorumPolicy`

Current settings.css width violation (line 14):
```css
.settings-shell { max-width: var(--content-narrow, 720px); }
```
This artificially caps the panel at 720px on a wide screen — the worst offender. Must become full-width.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width gate — remove artificial 720px cap on settings panel</name>
  <files>public/assets/css/settings.css</files>
  <read_first>
    - public/assets/css/settings.css (entire file, 381 lines)
    - public/settings.htmx.html (look for the root container class — `.settings-shell` or `.settings-page`)
  </read_first>
  <action>
    Locate the rule at line ~14: `max-width: var(--content-narrow, 720px);` on `.settings-shell` (or whichever selector wraps the main settings panel).

    Replace the cap with a responsible full-width layout:
    ```css
    .settings-shell {
      max-width: 100%;
      width: 100%;
      padding-inline: var(--space-6);
      margin-inline: auto;
    }
    ```

    Do NOT touch the `@media (max-width: 768px)` rule — that's a media query breakpoint, not a container cap. Leave any sub-component max-widths (e.g., individual form-input width) untouched if they're component-internal.

    After editing, run `grep -nE 'max-width:\s*(var\(--content-narrow|[0-9]+px)' public/assets/css/settings.css` and confirm the only matches are inside `@media` queries.
  </action>
  <verify>
    <automated>! grep -nE '^\s*max-width:\s*(var\(--content-narrow|[0-9]+px)' public/assets/css/settings.css | grep -v '@media'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'max-width: var\(--content-narrow' public/assets/css/settings.css` returns zero hits.
    - `.settings-shell` rule includes `max-width: 100%`.
    - `grep -c '@media (max-width: 768px)' public/assets/css/settings.css` still returns 1 (responsive breakpoint preserved).
  </acceptance_criteria>
  <done>Settings panel root has no artificial container cap; only media-query breakpoints remain.</done>
</task>

<task type="auto">
  <name>Task 2: Token gate — zero color literals in settings.css</name>
  <files>public/assets/css/settings.css</files>
  <read_first>
    - public/assets/css/settings.css (entire file)
    - public/assets/css/design-system.css (only the `:root` token block at the top — to know which `var(--*)` names exist)
  </read_first>
  <action>
    Run the violation grep:
    ```bash
    grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\(' public/assets/css/settings.css
    ```

    For each match:
    - If it's a hex/rgb color → replace with the matching `var(--color-*)` from design-system.css
    - If it's a translucent overlay (e.g., `rgba(0,0,0,0.1)`) → replace with `color-mix(in oklch, var(--color-on-surface) 10%, transparent)`
    - If the literal is inside a `/* comment */` → leave it (comments are not selectors)

    Common token map (use these names exactly — see design-system.css to confirm):
    - Primary text: `var(--color-on-surface)`
    - Surface background: `var(--color-surface)` / `var(--color-surface-raised)`
    - Border: `var(--color-border)`
    - Brand: `var(--color-primary)` / `var(--color-primary-hover)`
    - Danger: `var(--color-danger)`

    After editing, the violation grep MUST return zero results (excluding comments, which the grep regex above does not exclude — so you must also confirm visually that any remaining matches are inside `/* ... */`).
  </action>
  <verify>
    <automated>test -z "$(grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\(' public/assets/css/settings.css | grep -v '^\s*\*' | grep -v '/\*')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\(' public/assets/css/settings.css` returns zero non-comment lines.
    - All replaced color values use `var(--*)` or `color-mix(in oklch, var(--*), ...)`.
    - File still parses (no broken declarations) — confirmed by absence of `]` errors when reading.
  </acceptance_criteria>
  <done>settings.css contains zero raw color literals outside comments.</done>
</task>

<task type="auto">
  <name>Task 3: Function gate — Playwright spec asserts real results for settings interactions</name>
  <files>tests/e2e/specs/critical-path-settings.spec.js</files>
  <read_first>
    - tests/e2e/specs/critical-path-admin.spec.js (the existing admin spec which already covers settings tab switching — Lines 27-37)
    - tests/e2e/helpers.js (for `loginAsAdmin`)
    - public/settings.htmx.html (lines 80-340 — to know which IDs are present)
  </read_first>
  <action>
    Create a NEW spec file `tests/e2e/specs/critical-path-settings.spec.js` (do not extend critical-path-admin — keep page-scoped specs separate per Phase 12 methodology).

    The spec must use `loginAsAdmin(page)` and contain ONE `test()` tagged `@critical-path` named "settings: tabs + persist setting + smtp test".

    For each PRIMARY interaction below, trigger AND assert an observable result:

    1. **Tab switch — Regles → Communication**
       - Click `[data-stab="communication"]`
       - Assert `#stab-communication` becomes visible AND `#stab-regles` becomes hidden (`toBeHidden` or `toHaveAttribute('hidden', '')`)

    2. **Tab switch — Communication → Securite**
       - Click `[data-stab="securite"]`
       - Assert `#stab-securite` is visible AND `#settSessionTimeout` is visible

    3. **Persist a setting (real DB write proof)**
       - Switch back to Regles tab: `[data-stab="regles"]`
       - Read current value of `#settQuorumThreshold`, then fill with a different valid value (e.g., flip between "50" and "60")
       - Click the form's primary save button (look for `#btnSaveSettings` OR `button[type="submit"]` inside `#stab-regles`)
       - Wait for either an HTMX-settled response OR a `.toast.success` element
       - Reload the page (`page.reload()`)
       - Assert `#settQuorumThreshold` reads back the value just saved
       - Restore the original value to keep the test re-runnable

    4. **SMTP test button — assert API call**
       - Switch to Communication tab
       - Set up `page.waitForResponse(r => r.url().includes('/api/v1/settings_test_smtp') || r.url().includes('test_smtp'))`
       - Click `#btnTestSmtp`
       - Assert response `.status() < 500` (the test endpoint may legitimately return 4xx if SMTP is unconfigured in test env — that's still a wired endpoint, which is what we're proving)

    Use `test.setTimeout(120000)` and `waitUntil: 'domcontentloaded'`.

    If `#btnSaveSettings` does not exist (audit may show a different ID or per-section save), substitute with the actual button found in the HTML — DO NOT invent.
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-settings.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/critical-path-settings.spec.js` exists.
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-settings.spec.js` returns at least 1.
    - `grep -c "page.reload" tests/e2e/specs/critical-path-settings.spec.js` returns at least 1 (proves persistence).
    - `./bin/test-e2e.sh specs/critical-path-settings.spec.js` exits 0.
  </acceptance_criteria>
  <done>The function gate is provable: a Playwright run asserts tab switching, setting persistence across reload, and SMTP test endpoint connectivity.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
- `! grep -nE '^\s*max-width:\s*var\(--content-narrow' public/assets/css/settings.css` (width gate)
- `! grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\(' public/assets/css/settings.css | grep -v '/\*'` (token gate)
- `./bin/test-e2e.sh specs/critical-path-settings.spec.js` exits 0 (function gate)
- `php -l public/settings.htmx.html` not applicable (HTML, not PHP) — but visual inspection in browser at /settings should show the panel filling the viewport on a 1440px screen.
</verification>

<success_criteria>
- Width gate: settings panel uses full viewport width.
- Token gate: settings.css has zero raw color literals.
- Function gate: critical-path-settings.spec.js asserts real observable results for tabs, persistence, and SMTP wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-01-SUMMARY.md` documenting:
- Width: which selector was capped, what the fix was
- Tokens: how many literals replaced (count of grep matches before vs after)
- Function: list of interactions covered, the run output of `./bin/test-e2e.sh specs/critical-path-settings.spec.js`
</output>
