---
phase: 12-page-by-page-mvp-sweep
plan: 11
type: execute
wave: 3
depends_on: []
files_modified:
  - public/assets/css/users.css
  - tests/e2e/specs/critical-path-users.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "users page uses full viewport width with no artificial container limit"
    - "users.css contains zero hex/oklch/rgba color literals — every color is a design-system token"
    - "Each primary users interaction (role filter, search, add user modal, role distribution cards, refresh, pagination) is asserted by Playwright via observable DOM change or API call"
  artifacts:
    - path: public/assets/css/users.css
      provides: users page styles, full-width verified
      contains: "width: 100%"
    - path: tests/e2e/specs/critical-path-users.spec.js
      provides: function gate for users page
      contains: "test.describe"
  key_links:
    - from: tests/e2e/specs/critical-path-users.spec.js
      to: public/users.htmx.html
      via: "page.goto('/users.htmx.html') + locator click + assert observable"
      pattern: "users.htmx.html"
---

<objective>
Phase 12 Wave 3 / Page 3: Sweep `/users` so it passes the 3 MVP gates (width, design tokens, function).

Purpose: users is the user management page with role distribution cards, search, role filter pills, user list with CRUD (add/edit/delete/toggle/password), and pagination. v1.2-PAGES-AUDIT.md marked it as "FONCTIONNE" (213 lines HTML, 715 lines JS). No existing E2E coverage specific to this page. This plan proves the visible UI is full-width, on-brand, and provably functional.

Output: users.css verified clean (width + tokens), plus a `critical-path-users.spec.js` Playwright spec asserting primary interactions produce real observable results.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-PAGES-AUDIT.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/users.htmx.html
@public/assets/css/users.css
@public/assets/js/pages/users.js
@tests/e2e/helpers.js
</context>

<interfaces>
Existing helper (extract):
```js
// tests/e2e/helpers.js
const { loginAsAdmin } = require('../helpers');
// usage: await loginAsAdmin(page);
```

Existing CSRF pattern (from critical-path-admin.spec.js):
```js
const csrfResp = await page.request.get('/api/v1/auth_csrf.php');
const csrfBody = await csrfResp.json();
const csrfToken = csrfBody?.token || csrfBody?.data?.token || csrfBody?.csrf_token || '';
```

No existing E2E coverage for `/users` page.

Users page primary interactive IDs (from public/users.htmx.html):
- Role distribution cards: `.role-dist-card[data-role="admin"]`, `[data-role="operator"]`, `[data-role="auditor"]`, `[data-role="viewer"]`
- Role counts: `#roleCountAdmin`, `#roleCountOperator`, `#roleCountAuditor`, `#roleCountViewer`
- Search: `#searchUser` (input type="search")
- Role filter: `#roleFilter` with `.filter-tab` children (Tous, Admin, Operateur, Auditeur, Observateur)
- Users count: `#usersCount`
- Users list: `#usersTableBody`
- Refresh: `#btnRefresh`
- Add user: `#btnAddUser`
- User modal: `#userModal` (ag-modal component)
- Modal fields: `#modalUserId`, `#modalUserName`, `#modalUserEmail`, `#modalUserPassword`, `#modalUserRole`
- Modal buttons: `#btnSaveUser`, `#btnCancelUser`
- Password strength: `#passwordStrength`, `#passwordStrengthFill`, `#passwordStrengthText`
- Pagination: `#usersPagination` (ag-pagination component)
- Column headers: `.users-list-header`

Current users.css width status: `.users-page { width: 100%; }` at line 18 — already full-width. No artificial max-width on page container. Sub-component max-widths on `.users-search-wrap` (320px) are legitimate input constraints.

Current users.css token status: zero hex/oklch/rgba literals found. All values use `var(--*)`. Clean.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width gate + Token gate — verify users page is full-width and token-pure</name>
  <files>public/assets/css/users.css</files>
  <read_first>
    - public/assets/css/users.css (entire file, 575 lines)
  </read_first>
  <action>
    Run the width audit grep:
    ```bash
    grep -nE 'max-width:\s*[0-9]+px' public/assets/css/users.css | grep -v '@media'
    ```

    Expected: only sub-component hits (`.users-search-wrap` 320px at line 146). This is an input field constraint, NOT a page-container cap. No fix needed.

    Verify `.users-page { width: 100%; }` is present (line 18).

    Run the token violation grep:
    ```bash
    grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(' public/assets/css/users.css | grep -v '/\*' | grep -v '^\s*\*'
    ```

    Expected: zero matches. users.css was rebuilt in v4.4 with full design-system tokens.

    If any unexpected literal is found, replace with the matching `var(--color-*)` from design-system.css.
  </action>
  <verify>
    <automated>bash -c '! grep -nE "^\s*max-width:\s*[0-9]+px" public/assets/css/users.css | grep -v "@media" | grep -v "search-wrap\|popover\|tooltip\|modal" && test -z "$(grep -nE "oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(" public/assets/css/users.css | grep -v "/\*" | grep -v "^\s*\*" | grep -v "color-mix")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'max-width:\s*[0-9]+px' public/assets/css/users.css | grep -v '@media'` returns only sub-component hits (search-wrap), zero page-container hits.
    - `.users-page { width: 100%; }` confirmed present.
    - `grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/users.css` returns zero non-comment lines.
  </acceptance_criteria>
  <done>Users page verified full-width and token-pure; no changes needed.</done>
</task>

<task type="auto">
  <name>Task 2: Function gate — Playwright spec asserts real results for users interactions</name>
  <files>tests/e2e/specs/critical-path-users.spec.js</files>
  <read_first>
    - tests/e2e/helpers.js (for loginAsAdmin)
    - public/users.htmx.html (full file — to know which IDs are present)
    - public/assets/js/pages/users.js (first 60 lines — to understand API call pattern and state)
  </read_first>
  <action>
    Create a NEW spec file `tests/e2e/specs/critical-path-users.spec.js`.

    The spec must use `loginAsAdmin(page)` and contain ONE `test()` tagged `@critical-path` named "users: role filter + search + add modal + role counts + data load".

    Use `test.setTimeout(120000)` and `waitUntil: 'domcontentloaded'`.

    For each PRIMARY interaction below, trigger AND assert an observable result:

    1. **API data load — users list populates**
       - Navigate to `/users.htmx.html`
       - Wait for `#usersTableBody` to have `aria-busy="false"` (JS sets this after load)
       - Assert `#usersCount` text contains a number (not just the placeholder dash)
       - Assert at least one role count (`#roleCountAdmin` or `#roleCountOperator`) shows a number > 0

    2. **Role filter click — observable DOM change**
       - Click the "Admin" filter tab (`#roleFilter .filter-tab[data-role="admin"]`)
       - Assert the clicked tab gains `.active` class
       - Assert the "Tous" tab loses `.active` class
       - Assert `#usersCount` text changes (might show fewer users)
       - Click "Tous" to restore

    3. **Search input — filters the list**
       - Fill `#searchUser` with `zzz-no-match-${Date.now()}`
       - Wait 400ms (debounce)
       - Assert `#usersTableBody` shows empty state or zero `.user-row` elements
       - Clear the search input
       - Wait 400ms
       - Assert user rows reappear

    4. **Add user modal — opens with correct fields**
       - Click `#btnAddUser`
       - Wait for `#userModal` to be visible (ag-modal opens)
       - Assert `#modalUserName` is visible and empty
       - Assert `#modalUserEmail` is visible and empty
       - Assert `#modalUserRole` is visible and has a value (default option)
       - Close via `#btnCancelUser`
       - Assert the modal closes (hidden)

    5. **Refresh button — reloads data**
       - Click `#btnRefresh`
       - Wait for `#usersTableBody` `aria-busy` to transition (busy then not busy) OR just wait for the list to be visible after 2s
       - Assert user rows are still present

    If test data has only one user (the admin), adapt: role filter on "Admin" should keep that user, filter on "Operator" should show empty, search for admin name should keep one row.
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-users.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/critical-path-users.spec.js` exists.
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-users.spec.js` returns at least 1.
    - `grep -c "searchUser\|roleFilter\|btnAddUser\|userModal\|usersTableBody" tests/e2e/specs/critical-path-users.spec.js` returns at least 4 (proves multiple interactions covered).
    - `./bin/test-e2e.sh specs/critical-path-users.spec.js` exits 0.
  </acceptance_criteria>
  <done>The function gate is provable: a Playwright run asserts role filter, search, add user modal, role counts, data loading, and refresh — all via observable DOM changes.</done>
</task>

</tasks>

<verification>
After all tasks:
- `! grep -nE '^\s*max-width:\s*[0-9]+px' public/assets/css/users.css | grep -v '@media' | grep -v 'search-wrap'` returns empty (width gate)
- `! grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/users.css | grep -v '/\*' | grep -v 'color-mix'` returns empty (token gate)
- `./bin/test-e2e.sh specs/critical-path-users.spec.js` exits 0 (function gate)
</verification>

<success_criteria>
- Width gate: users page verified full-width (no cap to remove).
- Token gate: users.css has zero raw color literals.
- Function gate: critical-path-users.spec.js asserts real observable results for role filter, search, add user modal, role counts, data loading, and refresh.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-11-SUMMARY.md` documenting:
- Width: verification result (already clean or what was fixed)
- Tokens: verification result (already clean or how many literals replaced)
- Function: list of interactions covered, the run output of `./bin/test-e2e.sh specs/critical-path-users.spec.js`
</output>
