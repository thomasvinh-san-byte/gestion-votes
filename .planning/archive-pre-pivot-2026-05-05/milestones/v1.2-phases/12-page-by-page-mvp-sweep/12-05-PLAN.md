---
phase: 12-page-by-page-mvp-sweep
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - public/assets/css/meetings.css
  - tests/e2e/specs/critical-path-meetings.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "meetings page uses full viewport width with no artificial container limit"
    - "meetings.css contains zero hex/oklch/rgba color literals — every color is a design-system token"
    - "Each primary meetings interaction (filter pills, search, sort, view toggle, edit modal, delete modal) is asserted by Playwright via observable DOM change or API call"
  artifacts:
    - path: public/assets/css/meetings.css
      provides: meetings page styles, full-width verified
      contains: "width: 100%"
    - path: tests/e2e/specs/critical-path-meetings.spec.js
      provides: function gate for meetings page
      contains: "test.describe"
  key_links:
    - from: tests/e2e/specs/critical-path-meetings.spec.js
      to: public/meetings.htmx.html
      via: "page.goto('/meetings.htmx.html') + locator click + assert observable"
      pattern: "meetings.htmx.html"
---

<objective>
Phase 12 Wave 2 / Page 1: Sweep `/meetings` so it passes the 3 MVP gates (width, design tokens, function).

Purpose: meetings is the session list page (filter pills, search, calendar, edit/delete modals). Phase 11 removed the orphan `#btnStartTour`. This plan now proves the remaining UI elements are full-width, on-brand, and provably functional.

Output: meetings.css verified clean (width + tokens), plus a `critical-path-meetings.spec.js` Playwright spec asserting primary interactions produce real observable results.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-PAGES-AUDIT.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/meetings.htmx.html
@public/assets/css/meetings.css
@tests/e2e/specs/page-interactions.spec.js
@tests/e2e/helpers.js
</context>

<interfaces>
Existing helper (extract):
```js
// tests/e2e/helpers.js
const { loginAsOperator } = require('../helpers');
// usage: await loginAsOperator(page);
```

Existing CSRF pattern (from critical-path-admin.spec.js):
```js
const csrfResp = await page.request.get('/api/v1/auth_csrf.php');
const csrfBody = await csrfResp.json();
const csrfToken = csrfBody?.token || csrfBody?.data?.token || csrfBody?.csrf_token || '';
```

Meetings page primary interactive IDs (from public/meetings.htmx.html):
- Filter pills: `#filterPills` with `.filter-pill` children (Toutes, A venir, En cours, Terminees)
- Search: `#meetingsSearch` (input type="search")
- Sort: `#meetingsSort` (select)
- View toggle: `.view-toggle-btn` (list/calendar)
- Sessions list: `#meetingsList`
- Calendar: `#calendarContainer`, `#calendarPrev`, `#calendarNext`, `#calendarToday`
- Pagination: `#meetingsPagination`
- Edit modal: `#editMeetingModal`, `#editMeetingSaveBtn`
- Delete modal: `#deleteMeetingModal`, `#deleteMeetingConfirmBtn`
- Result count: `#meetingsCount`
- New meeting CTA: `a.btn.btn-primary[href="/wizard"]`

Current meetings.css width status: `.meetings-main .page-content { width: 100%; }` at line 16 — already full-width. No artificial max-width on page container. Only `@media (max-width: ...)` breakpoints exist.

Current meetings.css token status: zero hex/oklch/rgba literals found. All `color-mix()` calls use `var(--*)` bases. Clean.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width gate — verify meetings page is full-width (no cap to remove)</name>
  <files>public/assets/css/meetings.css</files>
  <read_first>
    - public/assets/css/meetings.css (entire file, 618 lines)
  </read_first>
  <action>
    Run the width audit grep:
    ```bash
    grep -nE 'max-width:\s*[0-9]+px' public/assets/css/meetings.css | grep -v '@media'
    ```

    Expected: zero matches outside `@media` queries. The `.meetings-main .page-content` already has `width: 100%` at line 16 with no max-width constraint.

    If any unexpected cap is found on a page-level container (`.meetings-main`, `.page-content`, or similar), remove it and replace with `max-width: 100%`.

    Do NOT touch `@media (max-width: ...)` breakpoints — those are responsive queries, not container caps.

    The only non-media max-width values should be on sub-components like `.calendar-grid min-width: 420px` (mobile scroll) or `.calendar-day-popover min-width: 200px` — those are component-internal and legitimate.
  </action>
  <verify>
    <automated>! grep -nE '^\s*max-width:\s*[0-9]+px' public/assets/css/meetings.css | grep -v '@media' | grep -v 'popover\|tooltip\|modal\|overlay'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'max-width:\s*[0-9]+px' public/assets/css/meetings.css | grep -v '@media'` returns zero page-container hits.
    - `.meetings-main .page-content { width: 100%; }` confirmed present.
  </acceptance_criteria>
  <done>Meetings page container verified full-width; no artificial cap found or removed.</done>
</task>

<task type="auto">
  <name>Task 2: Token gate — verify zero color literals in meetings.css</name>
  <files>public/assets/css/meetings.css</files>
  <read_first>
    - public/assets/css/meetings.css (entire file)
  </read_first>
  <action>
    Run the violation grep:
    ```bash
    grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(' public/assets/css/meetings.css | grep -v '/\*' | grep -v '^\s*\*'
    ```

    Expected: zero matches. meetings.css was rebuilt in Phase 6 (design tokens) and already uses `var(--*)` exclusively.

    All `color-mix(in oklch, ...)` usages use `var(--*)` base tokens (e.g., `color-mix(in oklch, currentColor 15%, transparent)` at line 129) — these are compliant per CONTEXT.md rules.

    If any unexpected literal is found, replace with the matching `var(--color-*)` from design-system.css.
  </action>
  <verify>
    <automated>test -z "$(grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(' public/assets/css/meetings.css | grep -v '/\*' | grep -v '^\s*\*' | grep -v 'color-mix')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/meetings.css` returns zero non-comment, non-color-mix lines.
    - All color values use `var(--*)` or `color-mix(in oklch, var(--*), ...)`.
  </acceptance_criteria>
  <done>meetings.css verified: zero raw color literals outside comments.</done>
</task>

<task type="auto">
  <name>Task 3: Function gate — Playwright spec asserts real results for meetings interactions</name>
  <files>tests/e2e/specs/critical-path-meetings.spec.js</files>
  <read_first>
    - tests/e2e/specs/page-interactions.spec.js (existing meetings test at lines 39-49 — basic navigation only)
    - tests/e2e/helpers.js (for `loginAsOperator`)
    - public/meetings.htmx.html (full file — to know which IDs are present)
  </read_first>
  <action>
    Create a NEW spec file `tests/e2e/specs/critical-path-meetings.spec.js` (do not extend page-interactions — keep page-scoped specs separate per Phase 12 methodology).

    The spec must use `loginAsOperator(page)` and contain ONE `test()` tagged `@critical-path` named "meetings: filter + search + sort + view toggle + API data load".

    Use `test.setTimeout(120000)` and `waitUntil: 'domcontentloaded'`.

    For each PRIMARY interaction below, trigger AND assert an observable result:

    1. **API data load — meetings list populates**
       - Navigate to `/meetings.htmx.html`
       - Wait for `#meetingsList` to be visible
       - Assert `#meetingsCount` text does NOT equal "0 seances" OR assert `#meetingsList` contains at least one child element (`.session-card` or similar). If test env has no meetings, assert the empty state message is visible instead.

    2. **Filter pill click — observable DOM change**
       - Click the second filter pill (e.g., `#filterPills .filter-pill:nth-child(2)`)
       - Assert the clicked pill gains `.active` class
       - Assert `#meetingsCount` text changes (compare before vs after click)

    3. **Search input — filters the list**
       - Fill `#meetingsSearch` with a unique string like `zzz-no-match-${Date.now()}`
       - Wait 500ms (debounce)
       - Assert `#meetingsCount` shows "0" OR `#meetingsList` shows empty state
       - Clear the search input
       - Assert `#meetingsCount` reverts to non-zero OR list repopulates

    4. **Sort select — observable change**
       - Get current first meeting title from `#meetingsList .session-card:first-child` (store text)
       - Change `#meetingsSort` to a different value (e.g., `selectOption({ index: 1 })`)
       - Assert the sort select has the new value (proves the select is wired)

    5. **View toggle — calendar view**
       - Click the calendar view toggle button (`.view-toggle-btn:last-child` or the one without `.active`)
       - Assert `#calendarContainer` becomes visible (has `.active` class or is not hidden)
       - Assert `#meetingsList` is hidden or `#calendarContainer.active` is present

    6. **New meeting CTA — navigation**
       - Click `a.btn.btn-primary[href="/wizard"]`
       - Assert URL changes to `/wizard`

    If test data has no meetings (empty state), adapt assertions: filter pill click and search should still work on zero items (count stays "0"), view toggle should still flip between list and calendar views.
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-meetings.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/critical-path-meetings.spec.js` exists.
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-meetings.spec.js` returns at least 1.
    - `grep -c "meetingsSearch\|filterPills\|meetingsSort\|calendarContainer" tests/e2e/specs/critical-path-meetings.spec.js` returns at least 3 (proves multiple interactions covered).
    - `./bin/test-e2e.sh specs/critical-path-meetings.spec.js` exits 0.
  </acceptance_criteria>
  <done>The function gate is provable: a Playwright run asserts filter pills, search, sort, view toggle, data loading, and navigation — all via observable DOM changes.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
- `! grep -nE '^\s*max-width:\s*[0-9]+px' public/assets/css/meetings.css | grep -v '@media'` returns empty (width gate)
- `! grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/meetings.css | grep -v '/\*' | grep -v 'color-mix'` returns empty (token gate)
- `./bin/test-e2e.sh specs/critical-path-meetings.spec.js` exits 0 (function gate)
</verification>

<success_criteria>
- Width gate: meetings page verified full-width (no cap to remove).
- Token gate: meetings.css has zero raw color literals.
- Function gate: critical-path-meetings.spec.js asserts real observable results for filter pills, search, sort, view toggle, data load, and CTA navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-05-SUMMARY.md` documenting:
- Width: verification result (already clean or what was fixed)
- Tokens: verification result (already clean or how many literals replaced)
- Function: list of interactions covered, the run output of `./bin/test-e2e.sh specs/critical-path-meetings.spec.js`
</output>
