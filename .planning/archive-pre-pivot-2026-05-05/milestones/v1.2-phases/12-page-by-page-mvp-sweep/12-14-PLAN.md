---
phase: 12-page-by-page-mvp-sweep
plan: 14
type: execute
wave: 4
depends_on: []
files_modified:
  - tests/e2e/specs/critical-path-report.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "report page uses full viewport width — report.css has no applicative max-width clamp"
    - "report.css contains zero hex/oklch/rgba color literals — every color is a design-system token (color-mix allowed)"
    - "Each primary report interaction (email field, export link hrefs, PV preview iframe load, timeline status) is asserted by Playwright via observable DOM"
  artifacts:
    - path: public/assets/css/report.css
      provides: report/PV page styles (already clean — verify only)
      contains: "var(--color-"
    - path: tests/e2e/specs/critical-path-report.spec.js
      provides: function gate for report page
      contains: "@critical-path"
  key_links:
    - from: tests/e2e/specs/critical-path-report.spec.js
      to: public/report.htmx.html
      via: "page.goto('/report.htmx.html') + locator interactions + observable assertions"
      pattern: "report.htmx.html"
---

<objective>
Phase 12 Wave 4 / Page 2: Sweep `/report` (Procès-verbal) so it passes the 3 MVP gates.

Purpose: report is the PV preview and export page (email form, PV iframe preview, status timeline generated/validated/sent/archived, Excel/CSV exports, download CTA). Width and token gates are already clean per pre-planning audit: report.css has only `@media (max-width: 768px)` and `@media print { body { max-width: 880px } }` (print rule — legitimate), zero hex/oklch/rgba literals, uses `color-mix(in oklch, ...)` as the only oklch reference. Note: a legacy `tests/e2e/specs/report.spec.js` exists — the new spec must be named `critical-path-report.spec.js` to avoid conflict.

Output: width + token verification (audits), plus a new `critical-path-report.spec.js` Playwright spec asserting report page interactions produce real observable results.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/report.htmx.html
@public/assets/css/report.css
@public/assets/js/pages/report.js
@tests/e2e/helpers.js
@tests/e2e/specs/critical-path-audit.spec.js
</context>

<interfaces>
Primary interactive IDs (from public/report.htmx.html):
- Header: `#meetingTitle`
- Export PDF link (header): `#btnExportPDF`
- Meeting context alert: `#meetingContext`, `#meetingName`, `#notValidatedWarning`
- Email form: `#email` (input), `#btnSendEmail` (button), `#emailMsg`
- PV preview: `#pvEmptyState`, `#pvFrameLoading`, `#pvFrame` (iframe), `#btnOpenNewTab`
- Status timeline: `.pv-timeline-step[data-step="generated"|"validated"|"sent"|"archived"]`
- Excel exports: `#exportFullXlsx`, `#exportFullXlsxWithVotes`
- Download CTA: `#pvDownloadCta`
- Excel individuals: `#exportAttendanceXlsx`, `#exportVotesXlsx`, `#exportResultsXlsx`
- CSV exports: `#exportPV`, `#exportAttendance`, `#exportVotes`, `#exportMotions`, `#exportMembers`, `#exportAudit`
- Nav: `#reportToArchives`

Login helper: `const { loginAsOperator } = require('../helpers');` (page role: operator,president,auditor).

Meeting context: the report page normally needs a meeting_id in context (either via query string or meeting-context service). For the critical-path spec, discover an existing meeting via the API and set it into the meeting context before navigation.

Width status: report.css — only media queries + print rule `@media print { body { max-width: 880px } }` (legitimate for print, not screen). No screen-viewport applicative clamp.

Token status: zero hex/oklch/rgba literals. Only oklch reference is `color-mix(in oklch, var(--color-surface-raised) 15%, transparent)` on `.pv-file-meta` — this is the allowed color-mix pattern per CONTEXT.md MVP-02 rules.

NOTE: `tests/e2e/specs/report.spec.js` already exists (legacy). Name the new spec `critical-path-report.spec.js` to avoid conflict.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width + token audit — verify report.css is clean</name>
  <files>public/assets/css/report.css</files>
  <read_first>
    - public/assets/css/report.css (entire file, ~275 lines)
  </read_first>
  <action>
    Run the three MVP gate audits:

    1. Width gate (applicative clamp check):
    ```bash
    grep -nE '^\s*max-width:' public/assets/css/report.css
    ```
    Expected: only `@media print { body { max-width: 880px; margin: 0 auto; } }` inside the print block — legitimate (print output for legal archives). Document in SUMMARY.

    2. Token gate:
    ```bash
    grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(' public/assets/css/report.css | grep -v '/\*' | grep -v '^\s*\*' | grep -v 'color-mix'
    ```
    Expected: zero matches. The `color-mix(in oklch, ...)` on `.pv-file-meta` is filtered out by the `color-mix` grep-v — it's the allowed pattern.

    3. Confirm token density:
    ```bash
    grep -c 'var(--color-' public/assets/css/report.css
    ```
    Expected: many matches.

    No CSS changes expected. If a violation is found, fix it.
  </action>
  <verify>
    <automated>bash -c 'test -z "$(grep -nE "oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(" public/assets/css/report.css | grep -v "/\*" | grep -v "^\s*\*" | grep -v "color-mix")" && test "$(grep -c "var(--color-" public/assets/css/report.css)" -gt 10'</automated>
  </verify>
  <acceptance_criteria>
    - Zero raw color literals outside `color-mix()` expressions.
    - `max-width` only inside `@media print` or `@media (max-width: ...)` queries.
    - `grep -c 'var(--color-' public/assets/css/report.css` > 10.
  </acceptance_criteria>
  <done>Report CSS verified clean on width + token gates.</done>
</task>

<task type="auto">
  <name>Task 2: Function gate — Playwright spec asserts real results for report page interactions</name>
  <files>tests/e2e/specs/critical-path-report.spec.js</files>
  <read_first>
    - tests/e2e/helpers.js (loginAsOperator)
    - public/report.htmx.html (interactive IDs)
    - public/assets/js/pages/report.js (first 120 lines — understand meeting context and export URL wiring)
    - tests/e2e/specs/critical-path-audit.spec.js (reference pattern)
  </read_first>
  <action>
    Create NEW spec file `tests/e2e/specs/critical-path-report.spec.js` (avoid conflict with existing legacy `report.spec.js`).

    Use `loginAsOperator(page)`.

    Structure: ONE `test.describe('E2E-REPORT Report/PV page critical path', ...)` with ONE `test()` tagged `@critical-path` named "report: meeting context + email form + export links + preview + timeline".

    Use `test.setTimeout(120000)` and `waitUntil: 'domcontentloaded'`.

    Setup: before navigation, set a meeting into context. Fetch the first available meeting from `/api/v1/meetings_list.php?limit=1` (or the current meetings list endpoint — check `meeting-context.js` for the actual mechanism). If no meeting exists in the test DB, the test should skip gracefully (`test.skip(!meetingId, 'no meeting in test DB')`).

    Set the meeting in localStorage under the key used by `meeting-context.js` (inspect `public/assets/js/services/meeting-context.js` for the exact key — likely `currentMeeting` or `agvote.meeting.current`):
    ```js
    await page.addInitScript((mid) => {
      try { localStorage.setItem('currentMeetingId', mid); } catch (e) {}
      try { localStorage.setItem('agvote.meeting.current', JSON.stringify({ id: mid })); } catch (e) {}
    }, meetingId);
    ```
    Use both candidate keys (be defensive — discover actual key from meeting-context.js during implementation).

    For each PRIMARY interaction, trigger AND assert:

    1. **Page mount + static elements present**
       - Navigate to `/report.htmx.html`
       - Assert `#meetingTitle` is visible.
       - Assert `#btnExportPDF` (header link) is visible and has an `href` attribute.
       - Assert the PV timeline exists: `.pv-timeline-step[data-step="generated"]` is visible.

    2. **Email form is interactive**
       - Assert `#email` input is visible.
       - Fill `#email` with `test-nobody@example.invalid`.
       - Assert `#email` has value matching the fill.
       - Assert `#btnSendEmail` is visible and enabled.
       - Do NOT click send (mutative — would attempt SMTP).

    3. **PV preview area renders empty-state or iframe**
       - Assert either `#pvEmptyState` OR `#pvFrame` is in the DOM (report.js either shows empty-state or loads iframe depending on meeting status).
       - Assert `#btnOpenNewTab` link exists.

    4. **Export links have non-empty hrefs after meeting context load**
       - Wait up to 5s for `#exportFullXlsx` to have an `href` attribute NOT equal to `#` (report.js rewrites hrefs once meeting context resolves).
       - If the href stays `#` (no meeting context), assert the link is still present and document as "meeting context not resolved" — do not fail the test (keep it re-runnable on empty DB). Use `page.waitForFunction` with a short timeout and wrap in try/catch if needed.
       - Assert the following IDs exist in the DOM (the href may or may not be rewritten depending on meeting state):
         `#exportFullXlsx`, `#exportFullXlsxWithVotes`, `#exportAttendanceXlsx`, `#exportVotesXlsx`, `#exportResultsXlsx`, `#exportPV`, `#exportAttendance`, `#exportVotes`, `#exportMotions`, `#exportMembers`, `#exportAudit`.

    5. **Download CTA is wired**
       - Assert `#pvDownloadCta` is visible.
       - Assert it has an `href` attribute (may be `#` pre-load, that is still a valid attribute).

    6. **Timeline: status dots are rendered**
       - Assert exactly 4 `.pv-timeline-step` elements exist (generated/validated/sent/archived).
       - Assert `.pv-timeline-step[data-step="generated"]` has class `done` OR at minimum has a `.pv-step-dot` child.

    7. **Nav link present**
       - Assert `#reportToArchives` is visible with `href="/archives"`.

    8. **Width verification — no horizontal scroll at desktop viewport**
       - Assert `document.documentElement.scrollWidth <= document.documentElement.clientWidth + 1`.

    Read-only, non-mutative, re-runnable.
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-report.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/critical-path-report.spec.js` exists.
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-report.spec.js` >= 1.
    - `grep -cE "exportFullXlsx|btnSendEmail|pvEmptyState|pvFrame|pv-timeline-step|pvDownloadCta" tests/e2e/specs/critical-path-report.spec.js` >= 5.
    - `./bin/test-e2e.sh specs/critical-path-report.spec.js` exits 0.
  </acceptance_criteria>
  <done>Function gate provable via Playwright — asserts meeting context, email form, PV preview, export IDs, timeline dots, download CTA, and width.</done>
</task>

</tasks>

<verification>
After all tasks:
- `! grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/report.css | grep -v '/\*' | grep -v 'color-mix'` returns empty
- `./bin/test-e2e.sh specs/critical-path-report.spec.js` exits 0
</verification>

<success_criteria>
- Width gate: report page full-width (only print-media `max-width: 880px` for legal output).
- Token gate: report.css has zero raw color literals (color-mix allowed).
- Function gate: critical-path-report.spec.js asserts real observable results for 7+ primary interactions.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-14-SUMMARY.md` documenting:
- Width: clean (only `@media print` clamp for legal archives)
- Tokens: clean (color-mix pattern documented)
- Function: interactions covered, run output of `./bin/test-e2e.sh specs/critical-path-report.spec.js`
</output>
