---
phase: 12-page-by-page-mvp-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/hub.css
  - tests/e2e/specs/critical-path-hub.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "hub page main grid uses full viewport width with no artificial container cap"
    - "hub.css contains zero hex/oklch/rgba color literals outside comments"
    - "Each primary hub interaction (main CTA, operator CTA, attachments list, checklist progress) is asserted by Playwright with observable result"
  artifacts:
    - path: public/assets/css/hub.css
      provides: hub fiche-séance styles
      contains: "var(--color-"
    - path: tests/e2e/specs/critical-path-hub.spec.js
      provides: function gate for hub page
      contains: "hubMainBtn"
  key_links:
    - from: tests/e2e/specs/critical-path-hub.spec.js
      to: public/hub.htmx.html
      via: "page.goto('/hub.htmx.html?meeting_id=...') + locator click + assert observable"
      pattern: "hub.htmx.html"
---

<objective>
Phase 12 Wave 1 / Page 3: Sweep `/hub` so it passes the 3 MVP gates.

Purpose: hub is the meeting fiche-séance — the page where presidents and operators land to see status, checklist progress, and attachments. Phase 11 wired the previously-broken `meeting_attachments_public` endpoint; Phase 12 now proves the full hub UI is full-width, on-brand, and that primary CTAs produce real results.

Output: hub.css width-clean + token-pure, plus new `critical-path-hub.spec.js` covering main CTAs, checklist refresh, and attachments listing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-PAGES-AUDIT.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/hub.htmx.html
@public/assets/css/hub.css
@tests/e2e/specs/critical-path-operator.spec.js
@tests/e2e/helpers.js
</context>

<interfaces>
Hub page primary interactive IDs (from public/hub.htmx.html):
- `#hubTitle`, `#hubTypeTag`, `#hubStatusTag`, `#hubDate`, `#hubPlace`, `#hubParticipants` (state)
- `#hubMainBtn` (primary CTA — anchor)
- `#hubOperatorBtn` (operator console CTA — anchor href="/operator")
- `#hubChecklist` + `#hubChecklistProgress` (checklist progress chip)
- `#hubQuorumSection` + `#hubQuorumPct` + `#hubQuorumBar` (quorum live state, hidden until session opens)
- `#hubAttachmentsSection` + `#hubAttachmentsCount` (attachments list, hidden until loaded)

Hub CSS architecture (public/assets/css/hub.css):
- Hero card uses padding `1.5rem 1.75rem`, no container cap
- Two-column body: 280px sidebar + fluid right (per file header)
- Only `max-width` matches are inside `@media (max-width: 768px)` (responsive breakpoint)

Existing operator spec uses meeting create via API + hybrid pattern. Hub needs the same pattern: create meeting, then visit `/hub.htmx.html?meeting_id=...`.

Existing helper: `loginAsAdmin(page)` (works for hub viewing — admin has read access to all meetings).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width gate — verify hub layout is fluid, remove any container caps</name>
  <files>public/assets/css/hub.css</files>
  <read_first>
    - public/assets/css/hub.css (full file, 543 lines)
    - public/hub.htmx.html (root container class — likely `.hub-page` or `.hub-shell` or just `.app-shell`)
  </read_first>
  <action>
    1. Inspect every `max-width` in hub.css. Run:
       ```bash
       grep -nE '^\s*max-width:' public/assets/css/hub.css
       ```
    2. From the existing audit, all current matches should be inside `@media (max-width: 768px)` queries — those are responsive breakpoints, not container caps. PRESERVE them.
    3. If any container-level rule is found (e.g., `.hub-shell { max-width: NNNpx }` or `.hub-content { max-width: NNNpx }`), REMOVE the cap and replace with:
       ```css
       .hub-shell { max-width: 100%; padding-inline: var(--space-6); }
       ```
    4. The two-column body (`280px + 1fr`) is already fluid — preserve it.
    5. The hero card padding (`1.5rem 1.75rem`) is fine — internal padding ≠ container cap.

    If grep returns ONLY `@media` matches, the file is already width-compliant — add a single comment at the top of the file confirming Phase 12 audit pass:
    ```css
    /* Phase 12 width gate: PASS — fluid grid, no container cap, only @media breakpoints. */
    ```
  </action>
  <verify>
    <automated>! grep -nE '^\s*max-width:\s*[0-9]+px' public/assets/css/hub.css | grep -v '@media'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^\s*max-width:' public/assets/css/hub.css | grep -v '@media'` returns zero lines.
    - The file header (or top) contains the Phase 12 width gate audit comment.
    - Two-column body grid (`280px + 1fr` or similar) preserved.
  </acceptance_criteria>
  <done>hub.css has zero container-level width caps; only responsive breakpoints remain.</done>
</task>

<task type="auto">
  <name>Task 2: Token gate — zero color literals in hub.css</name>
  <files>public/assets/css/hub.css</files>
  <read_first>
    - public/assets/css/hub.css (full file)
    - public/assets/css/design-system.css (the `:root` token block)
  </read_first>
  <action>
    Run the violation grep:
    ```bash
    grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\(' public/assets/css/hub.css
    ```

    The hub.css file header already states "All colors use var(--color-*) design tokens. Dark mode via tokens." — so violations should be RARE. For each non-comment match:
    - Hero gradient `linear-gradient(90deg, var(--color-primary), var(--color-primary-hover))` (line ~33) is already token-based — LEAVE it.
    - Status badges → `var(--color-info)`, `var(--color-success)`, `var(--color-warning)`, `var(--color-danger)`
    - Translucent overlays → `color-mix(in oklch, var(--color-on-surface) 10%, transparent)`

    After editing, the violation grep must return only comment lines (file header, section comments).
  </action>
  <verify>
    <automated>test -z "$(grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\(' public/assets/css/hub.css | grep -v '/\*' | grep -v '^\s*\*')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\(' public/assets/css/hub.css | grep -v '/\*' | grep -v '^\s*\*'` returns zero lines.
    - File still parses.
  </acceptance_criteria>
  <done>hub.css contains zero raw color literals outside comments.</done>
</task>

<task type="auto">
  <name>Task 3: Function gate — Playwright spec for hub primary interactions</name>
  <files>tests/e2e/specs/critical-path-hub.spec.js</files>
  <read_first>
    - tests/e2e/specs/critical-path-operator.spec.js (full file — for the meeting-create-via-API + cookie-CSRF pattern to copy)
    - public/hub.htmx.html (lines 80-220 for primary IDs)
    - tests/e2e/helpers.js
  </read_first>
  <action>
    Create NEW spec `tests/e2e/specs/critical-path-hub.spec.js`. Use the SAME hybrid API+UI pattern as critical-path-operator.spec.js:

    1. Login via `loginAsOperator(page)` (operator can create meetings; admin would also work but operator matches the actual hub user role).
    2. Get CSRF token via `GET /api/v1/auth_csrf`.
    3. Create a meeting via `POST /api/v1/meetings` with a unique `runId = e2e-hub-${Date.now()}` title.
    4. Capture `meetingId`.
    5. Navigate: `await page.goto('/hub.htmx.html?meeting_id=' + meetingId, { waitUntil: 'domcontentloaded' })`

    For each PRIMARY interaction, trigger AND assert observable result:

    1. **Hub state loaded**
       - Assert `#hubTitle` has text content NOT equal to "Chargement…" (or NOT empty) within 15s timeout. This proves the meeting fetch wired through.
       - Assert `#hubStatusTag` is visible and has text.

    2. **Main CTA wiring**
       - Assert `await page.locator('#hubMainBtn').getAttribute('href')` is non-empty AND not `'#'`.
       - Click `#hubMainBtn` and assert navigation OR new tab open: use `page.waitForNavigation` with a short timeout, OR check that the URL contains the expected target after click.
       - If the CTA opens in `_blank`, use `Promise.all([page.waitForEvent('popup'), page.locator('#hubMainBtn').click()])` and assert the popup URL is non-blank.

    3. **Operator CTA wiring**
       - Assert `await page.locator('#hubOperatorBtn').getAttribute('href')` includes `/operator`.

    4. **Checklist progress chip**
       - Assert `#hubChecklistProgress` is visible and has text matching `/^\d+\/\d+$/` (e.g., "0/3" or "2/3"). This proves the checklist API call returned and rendered.

    5. **Attachments section endpoint hit (proves Phase 11 wiring)**
       - Set up `const attachResp = page.waitForResponse(r => r.url().includes('meeting_attachments') && r.status() < 500, { timeout: 10000 })`.
       - Reload the page (`page.reload({ waitUntil: 'domcontentloaded' })`) to re-trigger the fetch.
       - `await attachResp` — assert the endpoint responded (any non-5xx, since the meeting may have zero attachments — that's a valid 200 with empty array).

    Use `test.setTimeout(120000)`, tag `@critical-path`, and ensure no destructive operations.
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-hub.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/critical-path-hub.spec.js` exists.
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-hub.spec.js` returns at least 1.
    - `grep -c 'meeting_attachments' tests/e2e/specs/critical-path-hub.spec.js` returns at least 1 (proves Phase 11 attachments endpoint is exercised).
    - `grep -c 'hubMainBtn' tests/e2e/specs/critical-path-hub.spec.js` returns at least 1.
    - `./bin/test-e2e.sh specs/critical-path-hub.spec.js` exits 0.
  </acceptance_criteria>
  <done>hub function gate provable by Playwright: state load, main CTA, operator CTA, checklist progress, attachments endpoint exercised.</done>
</task>

</tasks>

<verification>
- `! grep -nE '^\s*max-width:' public/assets/css/hub.css | grep -v '@media'` (width gate)
- `! grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\(' public/assets/css/hub.css | grep -v '/\*'` (token gate)
- `./bin/test-e2e.sh specs/critical-path-hub.spec.js` exits 0 (function gate)
</verification>

<success_criteria>
- Width gate: hub.css fluid; container caps absent.
- Token gate: zero raw color literals.
- Function gate: critical-path-hub.spec.js exercises state, both CTAs, checklist, and attachments endpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-03-SUMMARY.md` with:
- Width: gate verdict (PASS — already fluid OR FIX — list rules removed)
- Tokens: count of literals replaced
- Function: list of interactions covered + test run output
</output>
