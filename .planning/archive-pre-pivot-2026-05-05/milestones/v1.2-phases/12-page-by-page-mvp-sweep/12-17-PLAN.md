---
phase: 12-page-by-page-mvp-sweep
plan: 17
type: execute
wave: 5
depends_on: []
files_modified:
  - tests/e2e/specs/critical-path-trust.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "trust page uses full viewport width — no applicative max-width clamp on .app-main or page wrapper"
    - "trust.css contains zero hex/oklch/rgba color literals — every color is a design-system token"
    - "Each primary trust interaction (meeting select, refresh, severity pills, audit chips, view toggle, audit log filter, recheck) is asserted by Playwright via observable DOM/state change"
  artifacts:
    - path: public/assets/css/trust.css
      provides: control & audit page styles (already clean — verify only)
      contains: "var(--color-"
    - path: tests/e2e/specs/critical-path-trust.spec.js
      provides: function gate for trust page
      contains: "@critical-path"
  key_links:
    - from: tests/e2e/specs/critical-path-trust.spec.js
      to: public/trust.htmx.html
      via: "page.goto('/trust.htmx.html') + locator interactions + observable assertions"
      pattern: "trust.htmx.html"
---

<objective>
Phase 12 Wave 5 / Page 1 of 5: Sweep `/trust` (Contrôle & Audit) so it passes the 3 MVP gates.

Purpose: trust is the audit/integrity page for auditors and assessors. Integrity KPIs, anomaly list, coherence checks, motions coherence table, audit log with table/timeline views, severity pills, category chips, and an event detail modal. Pre-planning audit confirms: (a) width gate is already clean — `trust.css` has only two legit component caps (`.audit-table .audit-hash-col { max-width: 120px }` for hash column truncation and `.audit-modal { max-width: 560px }` for modal dialog), (b) token gate is clean — grep for `oklch(|#[0-9a-f]{3,8}|rgba?(` returns zero raw-color matches. Only missing gate is function: no `critical-path-trust.spec.js` exists (the legacy `trust.spec.js` is a 15-line API check).

Output: width + token verification (audits), plus a new `critical-path-trust.spec.js` Playwright spec asserting the primary trust page interactions produce real observable results.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/trust.htmx.html
@public/assets/css/trust.css
@public/assets/js/pages/trust.js
@tests/e2e/helpers.js
@tests/e2e/specs/critical-path-audit.spec.js
</context>

<interfaces>
Primary interactive IDs (from public/trust.htmx.html):
- Header: `#meetingTitle`, `#btnExportTrust`, `#btnRefresh`, drawer buttons `[data-drawer="anomalies"|"infos"|"context"]`
- Context card: `#meetingSelect` (select), `#statusBox`
- Integrity summary: `#kpiIntegrity`, `#kpiEvents`, `#kpiAnomalies`, `#kpiLastSession`, `#kpiMotions`, `#kpiPresent`, `#kpiBallots`, `#integrityHash`, `#integrityHashValue`, `#btnCopyHash`
- Anomalies card: `#severityFilters` with `.severity-pill[data-severity="all|danger|warning|info"]`, `#badgeAnomalies`, `#countDanger`, `#countWarning`, `#countInfo`, `#anomaliesList`
- Coherence checks: `#checksList`, `#btnRecheck`
- Motions table: `#motionsTbody`
- Audit log: `#auditCategoryChips` with `.audit-chip[data-category="all|votes|presences|security|system"]`, `#auditLogFilter` (search input), `#auditViewToggle` with `.audit-view-btn[data-view="table|timeline"]`, `#auditTableView`, `#auditTimelineView`, `#auditTable`, `#auditTableBody`, `#btnExportAudit`, `#auditSelectAll`
- Event modal: `#auditEventModal` (has `hidden` attribute initially), `#auditModalClose`

Login helper: the page supports roles `auditor,assessor`. Use `loginAsOperator(page)` if available in helpers.js (operator generally has audit read access) — fall back to admin. Read `tests/e2e/helpers.js` to pick the correct helper (check exports: `loginAsAdmin`, `loginAsOperator`, `loginAsAuditor`).

Width status: trust.css — only legitimate caps (`.audit-table .audit-hash-col` 120px, `.audit-modal` 560px). No applicative clamp on `.app-main` or page wrapper.

Token status: zero hex/oklch/rgba literals. All colors via `var(--*)`.

Critical note: trust.htmx.html has an inline `<style id="critical-tokens">` in the `<head>` that DOES use raw `oklch(...)` values. These are INTENTIONAL — critical-tokens are bootstrapped before the full stylesheet loads to prevent FOUC. The token gate only audits `public/assets/css/trust.css`, NOT the inline critical-tokens. Do NOT flag these.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width + token audit — verify trust.css is clean</name>
  <files>public/assets/css/trust.css</files>
  <read_first>
    - public/assets/css/trust.css (entire file, ~627 lines)
  </read_first>
  <action>
    Run the three MVP gate audits on `public/assets/css/trust.css` ONLY (not the inline critical-tokens in trust.htmx.html which are intentionally raw oklch for FOUC prevention).

    1. Width gate — expected matches only on legitimate component caps:
    ```bash
    grep -nE '^\s*max-width:' public/assets/css/trust.css
    ```
    Expected:
    - `.audit-table .audit-hash-col { max-width: 120px }` — hash column truncation, legitimate
    - `.audit-modal { max-width: 560px }` — modal dialog cap, legitimate
    - `@media (max-width: 768px)` and `@media (max-width: 480px)` — media queries, not applicative clamps

    Document each occurrence and its justification in SUMMARY. NO applicative clamp on `.app-main`, `.container`, or page wrapper.

    2. Token gate:
    ```bash
    grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(' public/assets/css/trust.css | grep -v '/\*' | grep -v '^\s*\*' | grep -v 'color-mix'
    ```
    Expected: zero matches.

    3. Confirm token density:
    ```bash
    grep -c 'var(--color-' public/assets/css/trust.css
    ```
    Expected: ≥ 40 matches (trust.css uses color tokens extensively).
  </action>
  <verify>
    <automated>bash -c 'test -z "$(grep -nE "oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(" public/assets/css/trust.css | grep -v "/\*" | grep -v "^\s*\*" | grep -v "color-mix")" && test "$(grep -c "var(--color-" public/assets/css/trust.css)" -gt 40'</automated>
  </verify>
  <acceptance_criteria>
    - Zero raw color literals in trust.css.
    - Only two max-width declarations outside media queries (hash column 120px, modal 560px).
    - `grep -c "var(--color-" public/assets/css/trust.css` > 40.
  </acceptance_criteria>
  <done>Trust CSS verified clean on width + token gates; all raw-color/oklch mentions in trust.htmx.html's inline critical-tokens are documented as intentional FOUC-prevention and NOT a violation.</done>
</task>

<task type="auto">
  <name>Task 2: Function gate — Playwright spec asserts real results for trust page interactions</name>
  <files>tests/e2e/specs/critical-path-trust.spec.js</files>
  <read_first>
    - tests/e2e/helpers.js
    - public/trust.htmx.html
    - public/assets/js/pages/trust.js (first 200 lines — understand meeting select, anomalies fetch, audit log filter wiring)
    - tests/e2e/specs/critical-path-audit.spec.js (reference structure + meeting-context pattern)
  </read_first>
  <action>
    Create NEW spec file `tests/e2e/specs/critical-path-trust.spec.js`. Do NOT touch legacy `tests/e2e/specs/trust.spec.js` (15-line API check — keep as-is).

    Pick login helper: read `tests/e2e/helpers.js` and prefer (in order): `loginAsOperator`, `loginAsAuditor`, `loginAsAdmin` — whichever exists and grants access to `/trust.htmx.html` (data-page-role="auditor,assessor").

    Structure: ONE `test.describe('E2E-TRUST Trust page critical path', ...)` containing ONE `test()` tagged `@critical-path` named `trust: meeting select + severity pills + audit chips + view toggle + filter + recheck + refresh`.

    Use `test.setTimeout(120000)` and navigate with `{ waitUntil: 'domcontentloaded' }`.

    Meeting context setup (defensive, same pattern as `critical-path-audit.spec.js`):
    ```js
    // Fetch first meeting via API; if none, skip the meeting-dependent steps gracefully
    const meetings = await page.evaluate(async () => {
      try {
        const r = await fetch('/api/v1/meetings.php?limit=1');
        if (!r.ok) return [];
        const j = await r.json();
        return (j.items || j.meetings || j.data || []);
      } catch (_) { return []; }
    });
    const meetingId = meetings[0]?.id || meetings[0]?.meeting_id || null;
    ```

    For each PRIMARY interaction, assert an observable result:

    1. **Page mount + header visible**
       - Navigate to `/trust.htmx.html`.
       - Wait for `#meetingSelect` to be visible (timeout 10s).
       - Assert `#btnRefresh`, `#btnExportTrust`, `#severityFilters`, `#auditCategoryChips` are all visible.

    2. **Meeting select — changes trigger anomalies + audit fetch**
       - Read all `<option>` values in `#meetingSelect`:
         ```js
         const options = await page.$$eval('#meetingSelect option', els => els.map(e => e.value).filter(Boolean));
         ```
       - If `options.length >= 1`:
         - `page.selectOption('#meetingSelect', options[0]);`
         - Wait 1000ms for AJAX to settle.
         - Assert `#statusBox` text is NOT just `—` (proves the status update handler ran): `const statusText = (await page.locator('#statusBox').textContent()).trim(); expect(statusText.length).toBeGreaterThan(0);`
         - Assert at least one of `#kpiEvents`, `#kpiMotions`, `#kpiPresent`, `#kpiBallots` has non-placeholder text (i.e., value is not the entity-encoded em-dash `—`):
           ```js
           const kpis = await Promise.all(['#kpiEvents','#kpiMotions','#kpiPresent','#kpiBallots'].map(s => page.locator(s).textContent()));
           expect(kpis.some(t => t && t.trim() !== '—' && t.trim() !== '')).toBeTruthy();
           ```
       - If no options: log `'trust: no meetings available in selectbox — skipping meeting-dependent assertions'` and continue with static UI assertions only.

    3. **Severity pills — click toggles .active class**
       - Click `.severity-pill[data-severity="danger"]`.
       - Assert `.severity-pill[data-severity="danger"]` now has class `active`.
       - Assert `.severity-pill[data-severity="all"]` no longer has class `active` (exclusive selection).
       - Click `.severity-pill[data-severity="all"]` to restore.

    4. **Audit chips — click toggles .active class**
       - Click `.audit-chip[data-category="votes"]`.
       - Assert `.audit-chip[data-category="votes"]` now has class `active`.
       - Assert `.audit-chip[data-category="all"]` no longer has class `active`.
       - Click `.audit-chip[data-category="all"]` to restore.

    5. **View toggle — table ↔ timeline**
       - Click `.audit-view-btn[data-view="timeline"]`.
       - Assert `#auditTableView` is hidden (has `hidden` attribute OR not visible).
       - Assert `#auditTimelineView` is visible (no `hidden` attribute).
       - Click `.audit-view-btn[data-view="table"]` to restore.
       - Assert `#auditTableView` visible and `#auditTimelineView` hidden.

    6. **Audit log filter — typing filters rows (or at least triggers input event)**
       - Fill `#auditLogFilter` with `xyz-unlikely-filter-query`.
       - Wait 300ms (debounce).
       - Assert the input value is `xyz-unlikely-filter-query`.
       - Clear the input via `.fill('')`.

    7. **Recheck button — click triggers reload**
       - Click `#btnRecheck`.
       - Wait 500ms.
       - Assert `#checksList` is still visible (spec is non-mutative; the button re-runs coherence checks which don't modify state).

    8. **Refresh button — click does not crash**
       - Click `#btnRefresh`.
       - Wait 500ms.
       - Assert page did not navigate: `expect(page.url()).toContain('/trust.htmx.html');`

    9. **Width verification**
       - Assert no horizontal overflow: `const hasOverflow = await page.evaluate(() => document.documentElement.scrollWidth > document.documentElement.clientWidth + 1); expect(hasOverflow).toBe(false);`

    Read-only, non-mutative, re-runnable. The trust page is pure read/audit — no write operations tested.
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-trust.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/critical-path-trust.spec.js` exists and is NEW (does not overwrite `trust.spec.js`).
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-trust.spec.js` ≥ 1.
    - `grep -cE "meetingSelect|severity-pill|audit-chip|audit-view-btn|auditLogFilter|btnRecheck|btnRefresh" tests/e2e/specs/critical-path-trust.spec.js` ≥ 6.
    - `./bin/test-e2e.sh specs/critical-path-trust.spec.js` exits 0.
  </acceptance_criteria>
  <done>Function gate provable via Playwright: meeting select updates KPIs, severity pills / audit chips toggle active state, view toggle switches table↔timeline, log filter accepts input, recheck/refresh buttons run without crashing. All observable via DOM.</done>
</task>

</tasks>

<verification>
After all tasks:
- `! grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/trust.css | grep -v '/\*' | grep -v 'color-mix'` returns empty
- `./bin/test-e2e.sh specs/critical-path-trust.spec.js` exits 0
- Legacy `tests/e2e/specs/trust.spec.js` is unchanged
</verification>

<success_criteria>
- Width gate: trust page full-width (only legitimate component caps: hash column 120px + modal 560px).
- Token gate: trust.css has zero raw color literals.
- Function gate: critical-path-trust.spec.js asserts real observable results for 7+ primary interactions.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-17-SUMMARY.md` documenting:
- Width: clean (legitimate caps documented: hash 120px, modal 560px)
- Tokens: clean (inline critical-tokens in trust.htmx.html are intentional FOUC-prevention and NOT a violation)
- Function: interactions covered, run output of `./bin/test-e2e.sh specs/critical-path-trust.spec.js`
- Note progress toward Wave 5 completion (1/5 done).
</output>
