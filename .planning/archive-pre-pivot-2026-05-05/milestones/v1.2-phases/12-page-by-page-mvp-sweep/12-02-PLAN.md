---
phase: 12-page-by-page-mvp-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/operator.css
  - tests/e2e/specs/critical-path-operator.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "operator console main grid uses full viewport width (no artificial container cap on the page shell)"
    - "operator.css contains zero hex/oklch/rgba color literals outside comments"
    - "Each primary operator interaction (mode switch, refresh, vote launch CTA presence + click) is asserted by Playwright with observable result"
  artifacts:
    - path: public/assets/css/operator.css
      provides: operator console styles, full-fluid grid
      contains: "grid-template-columns: 280px 1fr"
    - path: tests/e2e/specs/critical-path-operator.spec.js
      provides: extended function gate covering all primary operator buttons
      contains: "btnModeExec"
  key_links:
    - from: tests/e2e/specs/critical-path-operator.spec.js
      to: public/operator.htmx.html
      via: "real UI clicks via page.locator + assert observable state"
      pattern: "operator.htmx.html"
---

<objective>
Phase 12 Wave 1 / Page 2: Sweep `/operator` so it passes the 3 MVP gates.

Purpose: Operator console is the most-used page for live meetings. Phase 11 already wired the broken endpoints (procuration_pdf, motions_override_decision, invitations_send_reminder); Phase 12 now proves the visible UI is full-width, on-brand, and that every primary button produces a real result.

Output: operator.css width-clean + token-pure, plus extended `critical-path-operator.spec.js` covering mode switching, refresh, and vote-flow CTAs with real assertions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-PAGES-AUDIT.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/operator.htmx.html
@public/assets/css/operator.css
@tests/e2e/specs/critical-path-operator.spec.js
@tests/e2e/helpers.js
</context>

<interfaces>
Existing operator spec already covers (lines 99-124 of critical-path-operator.spec.js):
- Login + meeting create via API
- Open `/operator.htmx.html?meeting_id=...`
- Assert presence of `#btnBarRefresh`, `#btnModeSetup`, `#btnModeExec`
- Assert `#btnModeSetup` `aria-pressed="true"`

Operator page primary interactive IDs (from public/operator.htmx.html):
- `#btnBarRefresh` (action bar refresh)
- `#btnModeSetup` / `#btnModeExec` (mode switch)
- `#btnOpenPublicScreen` (line ~1004 — opens public projector)
- `#btnCloseVote` (line ~1037 — only when a vote is running)
- `#btnCloseSession` (line ~1149 — closes the meeting)

Operator CSS architecture (from public/assets/css/operator.css lines 12-32):
```css
[data-page-role="operator"] .app-shell {
  display: grid;
  grid-template-rows: auto auto auto auto auto 1fr;
  grid-template-columns: 1fr;
  height: 100vh;
  overflow: hidden;
}
.op-body {
  display: grid;
  grid-template-columns: 280px 1fr;
}
```

Existing internal max-widths (lines 79, 1137, 1935): `.op-meeting-select { max-width: 360px }`, modal forms 480px, and a sub-card 400px. These are component-internal element widths (a select should NOT span the whole bar), NOT a page container cap. They are LEGITIMATE and must be preserved.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width gate — verify operator shell is fluid, document component max-widths as intentional</name>
  <files>public/assets/css/operator.css</files>
  <read_first>
    - public/assets/css/operator.css (full file, 2021 lines — focus on lines 1-100 for the shell + lines 70-90 for op-meeting-select + lines 1130-1145 for modal max-widths + lines 1930-1945 for the sub-card)
  </read_first>
  <action>
    The operator console already uses a fluid `1fr` main column with no artificial container cap on the shell — width gate passes architecturally.

    Verify and document:
    1. Confirm `[data-page-role="operator"] .app-shell` has `grid-template-columns: 1fr` (line ~17) — no `max-width` on the shell itself.
    2. Run `grep -nE '^\s*max-width:' public/assets/css/operator.css` and inspect each match. Each match must be one of:
       - Inside a `@media` query (responsive breakpoint) — OK
       - On a component-internal element (`select`, modal, dropdown, tooltip) — OK, but add an inline comment `/* component-internal cap, not container */` if not already documented
       - On a page-level container (`.op-shell`, `.op-body`, `.op-main`) — VIOLATION, must be removed

    3. If any of `.op-meeting-select`, modal `.op-modal-content`, or `.op-sub-card-wrapper` lack a clarifying comment, add one above the `max-width:` line:
       ```css
       /* component-internal width cap (form control / modal), not a page container */
       max-width: 360px;
       ```

    4. If a true container cap is found (e.g., a hypothetical `.op-shell { max-width: 1400px }`), REMOVE it and replace with `max-width: 100%`.
  </action>
  <verify>
    <automated>! grep -nE '^\s*max-width:\s*[0-9]+px' public/assets/css/operator.css | grep -vE '(@media|select|modal|dropdown|tooltip|sub-card|component-internal)' | grep -v '//'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'grid-template-columns: 1fr' public/assets/css/operator.css` returns at least 1 (the shell stays fluid).
    - Every `max-width: NNNpx` outside `@media` is either on a form control / modal / dropdown OR has a `component-internal` comment within 2 lines above.
    - No new `max-width` rules introduced on `.op-shell`, `.op-body`, `.op-main`, or `.app-shell`.
  </acceptance_criteria>
  <done>operator.css shell is provably fluid; component-internal caps are documented and preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Token gate — zero color literals in operator.css</name>
  <files>public/assets/css/operator.css</files>
  <read_first>
    - public/assets/css/operator.css (full file)
    - public/assets/css/design-system.css (the `:root` token block)
  </read_first>
  <action>
    Run the violation grep:
    ```bash
    grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\(' public/assets/css/operator.css
    ```

    Replace each non-comment match with the appropriate token. Common operator-page replacements:
    - SSE indicator green/red — these are SEMANTIC status colors. Use `var(--color-success)` and `var(--color-danger)`. The CSS file header says "SSE indicator stays vivid green/red" — vivid means using the SUCCESS/DANGER tokens, NOT raw hex.
    - Mode switch active background → `var(--color-primary)` / `var(--color-primary-hover)`
    - Persona accent (operator) → `var(--persona-operateur, var(--color-primary))` (already exists at line ~46)
    - Translucent overlays (modal scrims) → `color-mix(in oklch, var(--color-on-surface) 50%, transparent)`

    If a literal exists inside a `/* comment */` (e.g., the file header), leave it.

    After editing, the violation grep must return only comment lines.
  </action>
  <verify>
    <automated>test -z "$(grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\(' public/assets/css/operator.css | grep -v '/\*' | grep -v '^\s*\*')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\(' public/assets/css/operator.css | grep -v '/\*' | grep -v '^\s*\*'` returns zero lines.
    - SSE green/red references use `var(--color-success)` / `var(--color-danger)` (verified by `grep -c 'var(--color-success)' public/assets/css/operator.css >= 1`).
    - File still parses (no broken declarations).
  </acceptance_criteria>
  <done>operator.css contains zero raw color literals outside comments.</done>
</task>

<task type="auto">
  <name>Task 3: Function gate — extend operator spec to cover all primary CTAs</name>
  <files>tests/e2e/specs/critical-path-operator.spec.js</files>
  <read_first>
    - tests/e2e/specs/critical-path-operator.spec.js (full file, 128 lines)
    - public/operator.htmx.html (lines 60-100 for action bar + mode switch, lines 1000-1050 for vote section, lines 1140-1160 for close session)
    - tests/e2e/helpers.js
  </read_first>
  <action>
    EXTEND the existing `critical-path-operator.spec.js` (do not create a new file). Add steps AFTER the existing Step 8 (line 124).

    For each interaction, trigger AND assert observable result:

    9. **Refresh button click — assert API hit**
       - Set up `const refreshResponse = page.waitForResponse(r => r.url().includes('/api/v1/meetings') && r.request().method() === 'GET', { timeout: 10000 })`
       - Click `#btnBarRefresh`
       - `await refreshResponse` — assert `.status() < 500`

    10. **Mode switch — Setup → Exec assertion of state change**
        - Click `#btnModeExec`
        - If the button is `disabled` (because the meeting is in draft and exec is gated), the click is a no-op — assert `await page.locator('#btnModeExec').getAttribute('aria-pressed')` stays `'false'` AND `#btnModeExec` has `disabled` attribute.
        - If the button is enabled, assert `aria-pressed` flips to `'true'` after click and `#btnModeSetup` flips to `'false'`.
        - This handles BOTH valid states — the assertion is "the mode switch correctly reflects business rules", not "exec mode always works".

    11. **Public screen CTA — assert href is wired**
        - Assert `await page.locator('#btnOpenPublicScreen').getAttribute('href')` matches `/^https?:\/\/.*\/public/` OR `^/public/`. If the href is `#` or empty, that's a wiring failure.
        - Do NOT click — opens a new tab and we don't need to test the public page in this spec.

    12. **Close session button presence + dispatch (only if meeting is open)**
        - Locate `#btnCloseSession`. If it's hidden / not in DOM, skip with `test.skip()` reason. Otherwise:
        - Stub the confirm dialog: `page.once('dialog', d => d.dismiss())` (we DISMISS — we don't actually want to close the meeting in a re-runnable test)
        - Click `#btnCloseSession`
        - Assert dialog was triggered (dismiss handler was called) — track via a flag variable

    Use `expect.soft` where appropriate so one failure doesn't mask the others.

    Re-runnability MUST be preserved — no destructive API calls, no actual mode commits without revert.
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-operator.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'btnBarRefresh' tests/e2e/specs/critical-path-operator.spec.js` returns at least 2 (existing presence assertion + new click).
    - `grep -c 'btnOpenPublicScreen' tests/e2e/specs/critical-path-operator.spec.js` returns at least 1.
    - `grep -c 'btnCloseSession' tests/e2e/specs/critical-path-operator.spec.js` returns at least 1.
    - `./bin/test-e2e.sh specs/critical-path-operator.spec.js` exits 0.
  </acceptance_criteria>
  <done>critical-path-operator.spec.js asserts real observable results for refresh, mode switch, public screen wiring, and close session dispatch.</done>
</task>

</tasks>

<verification>
- `! grep -nE '^\s*max-width:\s*[0-9]+px' public/assets/css/operator.css | grep -vE '(@media|select|modal|dropdown|tooltip|sub-card|component-internal)'` (width gate)
- `! grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\(' public/assets/css/operator.css | grep -v '/\*'` (token gate)
- `./bin/test-e2e.sh specs/critical-path-operator.spec.js` exits 0 (function gate)
</verification>

<success_criteria>
- Width gate: operator shell is provably fluid; component-internal caps documented.
- Token gate: operator.css has zero raw color literals.
- Function gate: extended spec asserts refresh API hit, mode switch state, public-screen wiring, close-session dispatch.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-02-SUMMARY.md` with:
- Width: list of `max-width` rules and their classification (media / component / container)
- Tokens: count of literals replaced
- Function: list of new interactions covered + test run output
</output>
