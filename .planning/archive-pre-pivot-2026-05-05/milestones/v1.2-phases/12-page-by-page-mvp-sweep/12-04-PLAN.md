---
phase: 12-page-by-page-mvp-sweep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/pages.css
  - tests/e2e/specs/critical-path-dashboard.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "dashboard main content uses full viewport width (no max-width: 1200px cap on .dashboard-content)"
    - "dashboard scope of pages.css contains zero hex/oklch/rgba color literals outside comments"
    - "Each primary dashboard interaction (KPI tiles render, urgent action card link, quick-access nav links) is asserted by Playwright with observable result"
  artifacts:
    - path: public/assets/css/pages.css
      provides: dashboard layout rules in the dashboard section (lines ~1000-1230)
      contains: "max-width: 100%"
    - path: tests/e2e/specs/critical-path-dashboard.spec.js
      provides: function gate for dashboard page
      contains: "kpiSeances"
  key_links:
    - from: tests/e2e/specs/critical-path-dashboard.spec.js
      to: public/dashboard.htmx.html
      via: "page.goto('/dashboard.htmx.html') + KPI value assertion + nav link click"
      pattern: "dashboard.htmx.html"
---

<objective>
Phase 12 Wave 1 / Page 4: Sweep `/dashboard` so it passes the 3 MVP gates.

Purpose: Dashboard is the landing page for every authenticated user. v1.2-PAGES-AUDIT.md flagged it as "FONCTIONNE — risk: empty in dev DB". Phase 11 wired `getDashboardStats()` server-side. Phase 12 now proves the dashboard is full-width, on-brand, and that KPI tiles + navigation actually wire to real data and routes.

Output: pages.css dashboard scope width-clean + token-pure (no dedicated dashboard.css exists — dashboard rules live in `pages.css` and the page only loads `app.css` which imports `pages.css`), plus new `critical-path-dashboard.spec.js`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-PAGES-AUDIT.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/dashboard.htmx.html
@public/assets/css/pages.css
@tests/e2e/specs/critical-path-admin.spec.js
@tests/e2e/helpers.js
</context>

<interfaces>
**No dedicated dashboard.css exists.** dashboard.htmx.html line 20 only loads `app.css`, and `app.css` line 18 imports `pages.css` which contains the `.dashboard-*` rules (lines ~938-1230).

Width violation found in audit (public/assets/css/pages.css line 1006):
```css
.dashboard-content {
  max-width: 1200px;   /* ← VIOLATION: artificial cap on a 1440px+ screen */
  margin: 0 auto;
  padding: var(--space-4) 0;
}
```

Dashboard scope = roughly lines 938-1230 of pages.css (the `.dashboard-*` block).

Dashboard primary interactive IDs (from public/dashboard.htmx.html):
- `#actionUrgente` (urgent action card, anchor href="/hub", initially hidden)
- `#urgentTitle`, `#urgentSub` (urgent state)
- `#kpiSeances`, `#kpiEnCours`, `#kpiConvoc`, `#kpiPV` (4 KPI value spans)
- `#prochaines` (next-sessions list)
- Quick-access aside (`.dashboard-aside`) with anchor links (likely `<a href="/meetings">`, `<a href="/members">`, etc.)

Existing test pattern: critical-path-admin.spec.js uses `loginAsAdmin` + `page.goto` + `expect(locator).toBeVisible`. Dashboard test follows the same shape.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width gate — remove dashboard 1200px cap in pages.css</name>
  <files>public/assets/css/pages.css</files>
  <read_first>
    - public/assets/css/pages.css (lines 1000-1230 — the dashboard scope)
    - public/dashboard.htmx.html (root container — likely `.app-shell` + `.dashboard-content`)
  </read_first>
  <action>
    1. Locate the `.dashboard-content` rule at approximately line 1006:
       ```css
       .dashboard-content {
         max-width: 1200px;
         margin: 0 auto;
         padding: var(--space-4) 0;
       }
       ```
    2. Replace with full-width:
       ```css
       .dashboard-content {
         max-width: 100%;
         width: 100%;
         margin: 0 auto;
         padding: var(--space-4) var(--space-6);
       }
       ```
       (The `padding-inline` ensures content doesn't touch viewport edges on ultra-wide screens.)

    3. Inspect any other `.dashboard-*` rules in the dashboard scope (lines 938-1230) for `max-width: NNNpx`:
       ```bash
       awk 'NR>=938 && NR<=1230' public/assets/css/pages.css | grep -nE 'max-width:'
       ```
       - Inside `@media` (line 1217 `@media (max-width: 1024px)` and 1225 `@media (max-width: 768px)`) → preserve, those are responsive breakpoints.
       - On `.dashboard-aside`, `.dashboard-sessions`, `.dashboard-kpis` containers → remove if they cap below `100%`.

    4. The KPI grid (`grid-template-columns: repeat(4, 1fr)` at line 1015) is fluid and correct — preserve.
  </action>
  <verify>
    <automated>! awk 'NR>=938 && NR<=1230' public/assets/css/pages.css | grep -nE '^\s*max-width:\s*[0-9]+px' | grep -v '@media'</automated>
  </verify>
  <acceptance_criteria>
    - `awk 'NR>=938 && NR<=1230' public/assets/css/pages.css | grep -c 'max-width: 1200px'` returns 0.
    - `grep -c '\.dashboard-content' public/assets/css/pages.css` still returns at least 1 (rule preserved, just modified).
    - The two `@media (max-width: ...)` breakpoints (lines 1217, 1225) preserved.
  </acceptance_criteria>
  <done>Dashboard content fills the viewport on wide screens; no artificial 1200px cap.</done>
</task>

<task type="auto">
  <name>Task 2: Token gate — zero color literals in pages.css dashboard scope</name>
  <files>public/assets/css/pages.css</files>
  <read_first>
    - public/assets/css/pages.css (lines 938-1230, dashboard scope)
    - public/assets/css/design-system.css (the `:root` token block)
  </read_first>
  <action>
    Run the violation grep restricted to the dashboard scope:
    ```bash
    awk 'NR>=938 && NR<=1230' public/assets/css/pages.css | grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\('
    ```

    For each non-comment match, replace with the appropriate token:
    - KPI card backgrounds → `var(--color-surface)` / `var(--color-surface-raised)`
    - KPI value text → `var(--color-on-surface)`
    - Urgent action accent → `var(--color-warning)` or `var(--color-primary)`
    - Borders → `var(--color-border)` / `var(--color-border-alpha)`
    - Hover overlays → `color-mix(in oklch, var(--color-on-surface) 5%, transparent)`

    NOTE: This task only touches the dashboard scope (lines 938-1230). Do NOT modify other parts of pages.css — those are owned by other Phase 12 plans.

    After editing, the scoped grep must return only comment lines.
  </action>
  <verify>
    <automated>test -z "$(awk 'NR>=938 && NR<=1230' public/assets/css/pages.css | grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\(' | grep -v '/\*' | grep -v '^\s*\*')"</automated>
  </verify>
  <acceptance_criteria>
    - Scoped grep (lines 938-1230) returns zero non-comment color literal lines.
    - File still parses end-to-end (no broken declarations) — confirm by running the existing test e2e suite later in Task 3 which loads pages.css.
    - No edits outside lines 938-1230 of pages.css.
  </acceptance_criteria>
  <done>Dashboard scope of pages.css contains zero raw color literals.</done>
</task>

<task type="auto">
  <name>Task 3: Function gate — Playwright spec for dashboard interactions</name>
  <files>tests/e2e/specs/critical-path-dashboard.spec.js</files>
  <read_first>
    - tests/e2e/specs/critical-path-admin.spec.js (template — similar shape: login + goto + assert)
    - public/dashboard.htmx.html (lines 65-200 for KPIs, urgent card, quick-access)
    - tests/e2e/helpers.js
  </read_first>
  <action>
    Create NEW spec `tests/e2e/specs/critical-path-dashboard.spec.js`. Use `loginAsAdmin(page)` (admin sees the full dashboard).

    For each PRIMARY interaction, trigger AND assert observable result:

    1. **Dashboard loads**
       - `await page.goto('/dashboard.htmx.html', { waitUntil: 'domcontentloaded' })`
       - Assert `#kpiSeances` is visible within 15s.

    2. **KPI tiles render with real values (proves DEBT-01 wiring)**
       - For each KPI ID `kpiSeances`, `kpiEnCours`, `kpiConvoc`, `kpiPV`:
         - Assert the locator is visible.
         - Read `await page.locator('#' + id).textContent()` and assert it is NOT the placeholder `"-"` (the dash) AND NOT empty within 10s. Use `expect(locator).not.toHaveText('-', { timeout: 10000 })`.
         - This proves `getDashboardStats()` (Phase 11 DEBT-01) actually returned numbers.
       - If the test DB has zero meetings, the value should still be `"0"` (numeric zero), NOT `"-"` (placeholder). The assertion catches the "wiring not done" case.

    3. **Urgent action card behavior**
       - Locate `#actionUrgente`. The card is initially `hidden`. Either:
         - It becomes visible after the dashboard loads (a session is in progress) → assert `await page.locator('#actionUrgente').getAttribute('href')` includes `/hub`.
         - OR it stays hidden (no active session in test DB) → use `expect(page.locator('#actionUrgente')).toHaveAttribute('hidden', /.*/)`.
       - Use `expect.soft` so we accept BOTH valid states.

    4. **Next-sessions list renders**
       - Assert `#prochaines` is visible.
       - Either the loading class drops (data arrived) or the empty state appears: `await expect(page.locator('#prochaines')).not.toHaveClass(/loading/, { timeout: 15000 })`.

    5. **Quick-access nav links wired**
       - Find all `<a>` elements inside `.dashboard-aside`:
         ```js
         const navLinks = await page.locator('.dashboard-aside a[href]').all();
         expect(navLinks.length).toBeGreaterThan(0);
         for (const link of navLinks) {
           const href = await link.getAttribute('href');
           expect(href).toBeTruthy();
           expect(href).not.toBe('#');
         }
         ```

    6. **Click one quick-access link and assert navigation**
       - Pick the first quick-access link, capture its href, click it, and assert `page.url()` contains the href segment after navigation.
       - Use `await Promise.all([page.waitForLoadState('domcontentloaded'), link.click()])`.

    Tag `@critical-path`. Use `test.setTimeout(120000)`. Re-runnable: only navigation, no DB writes.
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-dashboard.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/critical-path-dashboard.spec.js` exists.
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-dashboard.spec.js` returns at least 1.
    - `grep -cE 'kpi(Seances|EnCours|Convoc|PV)' tests/e2e/specs/critical-path-dashboard.spec.js` returns at least 4 (all 4 KPIs covered).
    - `grep -c 'dashboard-aside' tests/e2e/specs/critical-path-dashboard.spec.js` returns at least 1.
    - `./bin/test-e2e.sh specs/critical-path-dashboard.spec.js` exits 0.
  </acceptance_criteria>
  <done>Dashboard function gate provable: KPIs render real values (not placeholder), urgent card has both valid states handled, quick-access nav is wired, click navigation works.</done>
</task>

</tasks>

<verification>
- `! awk 'NR>=938 && NR<=1230' public/assets/css/pages.css | grep -nE '^\s*max-width:\s*[0-9]+px' | grep -v '@media'` (width gate)
- `! awk 'NR>=938 && NR<=1230' public/assets/css/pages.css | grep -nE 'oklch\(|#[0-9a-f]{6}|#[0-9a-f]{3}\b|rgba?\(' | grep -v '/\*'` (token gate)
- `./bin/test-e2e.sh specs/critical-path-dashboard.spec.js` exits 0 (function gate)
</verification>

<success_criteria>
- Width gate: dashboard content full-width, 1200px cap removed.
- Token gate: dashboard scope of pages.css token-pure.
- Function gate: Playwright proves KPI tiles render real values, navigation links wired and clickable.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-04-SUMMARY.md` with:
- Width: rule removed, replacement, screenshot of dashboard at 1440px viewport (optional)
- Tokens: count of literals replaced in dashboard scope
- Function: list of KPIs + nav links covered + test run output
</output>
