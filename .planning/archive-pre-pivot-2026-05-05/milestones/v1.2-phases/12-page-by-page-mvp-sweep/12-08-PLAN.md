---
phase: 12-page-by-page-mvp-sweep
plan: 08
type: execute
wave: 2
depends_on: []
files_modified:
  - public/assets/css/wizard.css
  - tests/e2e/specs/critical-path-wizard.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "wizard page content area uses full viewport width without artificial 960px/900px cap"
    - "wizard.css contains zero hex/oklch/rgba color literals — every color is a design-system token"
    - "Each primary wizard interaction (step navigation, field validation, member add, resolution add, final create) is asserted by Playwright via observable DOM change or API call"
  artifacts:
    - path: public/assets/css/wizard.css
      provides: wizard page styles, width caps removed
      contains: "max-width: 100%"
    - path: tests/e2e/specs/critical-path-wizard.spec.js
      provides: function gate for wizard page
      contains: "test.describe"
  key_links:
    - from: tests/e2e/specs/critical-path-wizard.spec.js
      to: public/wizard.htmx.html
      via: "page.goto('/wizard.htmx.html') + locator click + assert observable"
      pattern: "wizard.htmx.html"
---

<objective>
Phase 12 Wave 2 / Page 4: Sweep `/wizard` so it passes the 3 MVP gates (width, design tokens, function).

Purpose: wizard is the 4-step session creation flow (general info, members, resolutions, review). It has a 960px content track and 900px stepper — both artificial caps per the width gate rules. The wizard is the audit's "most solid" page (18 buttons, 38 handlers well-wired), so the function gate should confirm this with real Playwright assertions.

Output: wizard.css width caps removed, token compliance verified, plus a `critical-path-wizard.spec.js` Playwright spec asserting the 4-step flow produces real observable results.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-PAGES-AUDIT.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/wizard.htmx.html
@public/assets/css/wizard.css
@tests/e2e/helpers.js
</context>

<interfaces>
Existing helper (extract):
```js
// tests/e2e/helpers.js
const { loginAsOperator } = require('../helpers');
// usage: await loginAsOperator(page);
```

Existing CSRF pattern:
```js
const csrfResp = await page.request.get('/api/v1/auth_csrf.php');
const csrfBody = await csrfResp.json();
const csrfToken = csrfBody?.token || csrfBody?.data?.token || csrfBody?.csrf_token || '';
```

Wizard page primary interactive IDs (from public/wizard.htmx.html):
**Step 0 — General info:**
- Fields: `#wizTitle`, `#wizType`, `#wizDate`, `#wizTime`, `#wizPlace`, `#wizAddr`
- Advanced: `#wizQuorum`, `#wizDefaultMaj`
- Error banners: `#errBannerStep0`, `#errWizTitle`, `#errWizDate`, `#errWizTime`
- Nav: `#btnNext0`

**Step 1 — Members:**
- Add form: `#wizMemberName`, `#wizMemberEmail`, `#btnAddMemberInline`
- CSV import: `#btnImportCSV`, `#csvFileInput`, `#csvDropZone`
- Members list: `#wizMembersList`, `#wizMemberCount`, `#wizVoixTotal`
- Nav: `#btnPrev1`, `#btnNext1`

**Step 2 — Resolutions:**
- Add form: `#btnShowResoPanel`, `#resoTitle`, `#resoMaj`, `#resoDesc`, `#btnAddReso`
- Secret vote chips: `#chipNonSecret`, `#chipSecret`
- Resolution list: `#wizResoList`
- Nav: `#btnPrev2`, `#btnNext2`

**Step 3 — Review & Create:**
- Recap: `#wizRecap`
- Attachments: `#wizAttachmentSection`, `#wizAttachmentList`
- Nav: `#btnPrev3`, `#btnCreate` (final submit)

**Stepper:** `#wizStepper` with `.wiz-step-item` children (4 steps)

Current wizard.css width issues:
- Line 34: `.wiz-content, .wizard-page { max-width: 960px; }` — caps content at 960px
- Line 74: `.wiz-progress-wrap { max-width: 900px; }` — caps stepper at 900px

Current wizard.css token status: zero hex/oklch/rgba literals. All colors use var(--*) tokens. Clean.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width gate — remove artificial 960px/900px caps on wizard containers</name>
  <files>public/assets/css/wizard.css</files>
  <read_first>
    - public/assets/css/wizard.css lines 28-40 (.wiz-content, .wizard-page)
    - public/assets/css/wizard.css lines 70-88 (.wiz-progress-wrap)
  </read_first>
  <action>
    Fix the following max-width caps:

    1. **Line ~34**: `.wiz-content, .wizard-page { max-width: 960px; }` — Change to `max-width: 100%;`. The wizard is an applicative page and should use full viewport width.

    2. **Line ~74**: `.wiz-progress-wrap { max-width: 900px; }` — Change to `max-width: 100%;`. The stepper should stretch with the content.

    Both selectors already have `margin: 0 auto` for centering, and `padding: var(--space-4)` for breathing room. The full-width layout will use these existing padding values.

    Do NOT touch `@media (max-width: ...)` breakpoint queries — those are responsive, not caps.

    After editing, run:
    ```bash
    grep -nE 'max-width:\s*[0-9]+px' public/assets/css/wizard.css | grep -v '@media'
    ```
    Expected: zero matches.
  </action>
  <verify>
    <automated>test "$(grep -nE '^\s*max-width:\s*[0-9]+px' public/assets/css/wizard.css | grep -v '@media' | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'max-width: 960px' public/assets/css/wizard.css` returns 0.
    - `grep -c 'max-width: 900px' public/assets/css/wizard.css` returns 0.
    - `grep -c 'max-width: 100%' public/assets/css/wizard.css` returns at least 2 (replacements made).
    - `@media (max-width: 768px)` and `@media (max-width: 1024px)` breakpoints still present (responsive preserved).
  </acceptance_criteria>
  <done>Wizard page containers (.wiz-content, .wizard-page, .wiz-progress-wrap) no longer have artificial width caps.</done>
</task>

<task type="auto">
  <name>Task 2: Token gate — verify zero color literals in wizard.css</name>
  <files>public/assets/css/wizard.css</files>
  <read_first>
    - public/assets/css/wizard.css (entire file, 1047 lines)
  </read_first>
  <action>
    Run the violation grep:
    ```bash
    grep -nE '#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(|oklch\(' public/assets/css/wizard.css | grep -v '/\*' | grep -v '^\s*\*'
    ```

    Expected: zero matches. wizard.css was rebuilt with design tokens in Phase 6. No color-mix() calls either (wizard uses only direct var(--*) references).

    If any unexpected literal is found, replace with the matching `var(--color-*)` from design-system.css.
  </action>
  <verify>
    <automated>test -z "$(grep -nE '#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(|oklch\(' public/assets/css/wizard.css | grep -v '/\*' | grep -v '^\s*\*')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(|oklch\(' public/assets/css/wizard.css` returns zero non-comment lines.
    - All color values use `var(--*)`.
  </acceptance_criteria>
  <done>wizard.css verified: zero raw color literals.</done>
</task>

<task type="auto">
  <name>Task 3: Function gate — Playwright spec asserts real results for wizard 4-step flow</name>
  <files>tests/e2e/specs/critical-path-wizard.spec.js</files>
  <read_first>
    - tests/e2e/helpers.js (for `loginAsOperator`)
    - public/wizard.htmx.html (full file — to know which IDs are present)
  </read_first>
  <action>
    Create a NEW spec file `tests/e2e/specs/critical-path-wizard.spec.js`.

    The spec must use `loginAsOperator(page)` and contain ONE `test()` tagged `@critical-path` named "wizard: 4-step flow — fill + navigate + create session".

    Use `test.setTimeout(120000)` and `waitUntil: 'domcontentloaded'`.

    For each PRIMARY interaction below, trigger AND assert an observable result:

    1. **Step 0 — General info: fill required fields + advance**
       - Navigate to `/wizard.htmx.html`
       - Wait for `#step0.active` (or `#step0` visible)
       - Assert stepper first item has `.active` class
       - Fill `#wizTitle` with `E2E-Session-${Date.now()}`
       - Set `#wizDate` to tomorrow's date (compute dynamically)
       - Set `#wizTime` to `"18:00"`
       - Click `#btnNext0`
       - Assert `#step1` becomes visible (proves step navigation works)
       - Assert `#step0` is no longer visible

    2. **Step 0 — Validation fires on empty required field**
       - (Tested BEFORE filling: navigate fresh, click `#btnNext0` without filling title)
       - Assert error banner `#errBannerStep0` becomes visible OR `#errWizTitle` becomes visible
       - This proves validation is wired.
       - Then fill the required fields and proceed.
       - NOTE: If validation prevents advancing, fill the fields then retry `#btnNext0`.

    3. **Step 1 — Add a member inline**
       - Assert `#step1` is visible (already from step 1)
       - Fill `#wizMemberName` with `Membre E2E`
       - Fill `#wizMemberEmail` with `membre-e2e@test.local`
       - Click `#btnAddMemberInline`
       - Assert `#wizMemberCount` text changes from "0" to "1" (proves add worked)
       - Assert `#wizMembersList` contains at least one `.member-row` element

    4. **Step 1 — Navigate to step 2**
       - Click `#btnNext1`
       - Assert `#step2` becomes visible

    5. **Step 2 — Add a resolution**
       - Click `#btnShowResoPanel` to open the add form
       - Fill `#resoTitle` with `Resolution E2E Test`
       - Select a majority type in `#resoMaj` (e.g., `selectOption({ index: 1 })`)
       - Click `#btnAddReso`
       - Assert `#wizResoList` contains at least one `.reso-row`

    6. **Step 2 — Secret vote chip toggle**
       - Click `#chipSecret`
       - Assert `#chipSecret` gains `.active` class
       - Assert `#chipNonSecret` loses `.active` class

    7. **Step 2 — Navigate to step 3 (review)**
       - Click `#btnNext2`
       - Assert `#step3` becomes visible
       - Assert `#wizRecap` contains the session title entered in step 0 (proves data flows through)

    8. **Step 3 — Create session (real API call)**
       - Set up `page.waitForResponse(r => r.url().includes('meeting') || r.url().includes('session') || r.url().includes('wizard'))`
       - Click `#btnCreate`
       - Assert response status is 2xx (proves real backend call happened)
       - Assert page navigates away from `/wizard` OR a success feedback element appears

    IMPORTANT for re-runnability: Each run creates a new session with a unique timestamp title. No cleanup needed — test sessions accumulate but don't conflict.
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-wizard.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/critical-path-wizard.spec.js` exists.
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-wizard.spec.js` returns at least 1.
    - `grep -c "wizTitle\|btnNext0\|btnAddMemberInline\|btnAddReso\|btnCreate\|wizRecap" tests/e2e/specs/critical-path-wizard.spec.js` returns at least 5 (proves all 4 steps covered).
    - `./bin/test-e2e.sh specs/critical-path-wizard.spec.js` exits 0.
  </acceptance_criteria>
  <done>The function gate is provable: a Playwright run asserts the complete 4-step wizard flow — field validation, member addition, resolution creation, review data flow, and session creation via real API call.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
- `grep -c 'max-width: 960px\|max-width: 900px' public/assets/css/wizard.css` returns 0 (width gate)
- `! grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(|oklch\(' public/assets/css/wizard.css | grep -v '/\*'` returns empty (token gate)
- `./bin/test-e2e.sh specs/critical-path-wizard.spec.js` exits 0 (function gate)
</verification>

<success_criteria>
- Width gate: wizard.css page containers (.wiz-content, .wizard-page, .wiz-progress-wrap) use full width.
- Token gate: wizard.css has zero raw color literals.
- Function gate: critical-path-wizard.spec.js asserts the complete 4-step flow with real API calls and DOM mutations.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-08-SUMMARY.md` documenting:
- Width: which selectors were capped, what the fixes were (2 caps removed)
- Tokens: verification result (clean)
- Function: list of interactions covered, the run output of `./bin/test-e2e.sh specs/critical-path-wizard.spec.js`
</output>
