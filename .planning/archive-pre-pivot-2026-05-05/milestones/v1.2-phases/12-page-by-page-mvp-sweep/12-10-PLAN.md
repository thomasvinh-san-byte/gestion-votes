---
phase: 12-page-by-page-mvp-sweep
plan: 10
type: execute
wave: 3
depends_on: []
files_modified:
  - public/assets/css/archives.css
  - tests/e2e/specs/critical-path-archives.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "archives page uses full viewport width with no artificial container limit"
    - "archives.css contains zero hex/oklch/rgba color literals — every color is a design-system token"
    - "Each primary archives interaction (type filter, status filter, year filter, search, view toggle, refresh, exports modal, pagination) is asserted by Playwright via observable DOM change or API call"
  artifacts:
    - path: public/assets/css/archives.css
      provides: archives page styles, full-width verified
      contains: "archives-main"
    - path: tests/e2e/specs/critical-path-archives.spec.js
      provides: function gate for archives page
      contains: "test.describe"
  key_links:
    - from: tests/e2e/specs/critical-path-archives.spec.js
      to: public/archives.htmx.html
      via: "page.goto('/archives.htmx.html') + locator click + assert observable"
      pattern: "archives.htmx.html"
---

<objective>
Phase 12 Wave 3 / Page 2: Sweep `/archives` so it passes the 3 MVP gates (width, design tokens, function).

Purpose: archives is the validated sessions listing page with KPI row, type/status filter chips, year filter, search, card/list view toggle, exports modal, and pagination. v1.2-PAGES-AUDIT.md marked it as "FONCTIONNE". Existing tests are minimal: `archives.spec.js` only checks no horizontal overflow, `page-interactions.spec.js` only checks the refresh button is clickable. This plan proves full-width, on-brand, and provably functional.

Output: archives.css verified clean (width + tokens), plus a `critical-path-archives.spec.js` Playwright spec asserting primary interactions produce real observable results.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-PAGES-AUDIT.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/archives.htmx.html
@public/assets/css/archives.css
@public/assets/js/pages/archives.js
@tests/e2e/specs/page-interactions.spec.js
@tests/e2e/specs/archives.spec.js
@tests/e2e/helpers.js
</context>

<interfaces>
Existing helper (extract):
```js
// tests/e2e/helpers.js
const { loginAsOperator } = require('../helpers');
// usage: await loginAsOperator(page);
```

Existing test coverage:
- `archives.spec.js`: only checks no horizontal overflow (1 test)
- `page-interactions.spec.js` lines 146-160: only checks `#btnRefresh` is clickable and page stays visible after click. Does NOT assert API data load, filter behavior, or export. Insufficient for function gate.

Archives page primary interactive IDs (from public/archives.htmx.html):
- Type filter: `#archiveTypeFilter` with `.filter-tab` children (Toutes, AG Ord., AG Extra., Conseil)
- Status filter: `#archiveStatusFilter` with `.filter-tab` children (Tous statuts, Validee, Archivee, PV envoye)
- Year filter: `#yearFilter` (select)
- Search: `#searchInput` (input type="search")
- View toggle: `.view-toggle-btn[data-view="cards"]`, `.view-toggle-btn[data-view="list"]`
- Refresh: `#btnRefresh`
- Exports modal trigger: `#btnExportsModal`
- Archives list: `#archivesList`
- Pagination: `#archivesPager` (ag-pagination component)
- KPI indicators: `#kpiTotal`, `#kpiWithPV`, `#kpiThisYear`, `#kpiAvgParticipation`, `#kpiDateRange`
- Exports modal: `#exportsModal`, `#exportsBackdrop`
- Export buttons: `#btnExportPV`, `#btnExportAttendance`, `#btnExportVotes`, `#btnExportMotions`, `#btnExportMembers`, `#btnExportAudit`, `#btnExportZip`
- Close exports: `#btnCloseExports`, `#btnCloseExports2`

Current archives.css width status: `.archives-main` uses default `.app-main` padding from design-system.css. No artificial max-width on page container. Sub-component max-widths exist only on `.year-select` (160px) and `.archive-search-input` (240px) — legitimate input constraints.

Current archives.css token status: zero hex/oklch/rgba literals found. All values use `var(--*)` or `color-mix(in oklch, currentColor ...)`. Clean.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width gate + Token gate — verify archives page is full-width and token-pure</name>
  <files>public/assets/css/archives.css</files>
  <read_first>
    - public/assets/css/archives.css (entire file, 510 lines)
  </read_first>
  <action>
    Run the width audit grep:
    ```bash
    grep -nE 'max-width:\s*[0-9]+px' public/assets/css/archives.css | grep -v '@media'
    ```

    Expected: only sub-component hits (`.year-select` 160px, `.archive-search-input` 240px). These are input field constraints, NOT page-container caps. No fix needed.

    Verify no `.archives-main` or parent container has an artificial max-width cap.

    Run the token violation grep:
    ```bash
    grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(' public/assets/css/archives.css | grep -v '/\*' | grep -v '^\s*\*'
    ```

    Expected: zero matches. archives.css was rebuilt in v4.4 with full design-system tokens.

    If any unexpected literal is found, replace with the matching `var(--color-*)` from design-system.css.
  </action>
  <verify>
    <automated>bash -c '! grep -nE "^\s*max-width:\s*[0-9]+px" public/assets/css/archives.css | grep -v "@media" | grep -v "year-select\|search-input\|popover\|tooltip\|modal" && test -z "$(grep -nE "oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(" public/assets/css/archives.css | grep -v "/\*" | grep -v "^\s*\*" | grep -v "color-mix")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'max-width:\s*[0-9]+px' public/assets/css/archives.css | grep -v '@media'` returns only sub-component hits (year-select, search-input), zero page-container hits.
    - No `.archives-main` artificial cap exists.
    - `grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/archives.css` returns zero non-comment lines.
  </acceptance_criteria>
  <done>Archives page verified full-width and token-pure; no changes needed.</done>
</task>

<task type="auto">
  <name>Task 2: Function gate — Playwright spec asserts real results for archives interactions</name>
  <files>tests/e2e/specs/critical-path-archives.spec.js</files>
  <read_first>
    - tests/e2e/specs/page-interactions.spec.js (lines 146-160 — existing archives test, too shallow)
    - tests/e2e/specs/archives.spec.js (existing — only overflow check)
    - tests/e2e/helpers.js (for loginAsOperator)
    - public/archives.htmx.html (full file — to know which IDs are present)
    - public/assets/js/pages/archives.js (first 60 lines — to understand state and API call pattern)
  </read_first>
  <action>
    Create a NEW spec file `tests/e2e/specs/critical-path-archives.spec.js` (do not extend page-interactions or archives.spec.js — keep page-scoped specs separate per Phase 12 methodology).

    The spec must use `loginAsOperator(page)` and contain ONE `test()` tagged `@critical-path` named "archives: type filter + status filter + search + view toggle + exports modal + data load".

    Use `test.setTimeout(120000)` and `waitUntil: 'domcontentloaded'`.

    For each PRIMARY interaction below, trigger AND assert an observable result:

    1. **API data load — archives list populates**
       - Navigate to `/archives.htmx.html`
       - Wait for `#archivesList` to be visible (loading spinner gone)
       - Assert `#kpiTotal` text does NOT equal the mdash placeholder (proves API loaded)
       - OR if test env has no archives, assert the empty state is visible

    2. **Type filter click — observable DOM change**
       - Click the "AG Ord." filter tab (`#archiveTypeFilter .filter-tab[data-type="ag_ordinaire"]`)
       - Assert the clicked tab gains `.active` class
       - Assert the "Toutes" tab loses `.active` class
       - Click "Toutes" to restore

    3. **Status filter click — observable DOM change**
       - Click the "Validee" status tab (`#archiveStatusFilter .filter-tab[data-status="validated"]`)
       - Assert the clicked tab gains `.active` class
       - Click the "Tous statuts" tab to restore

    4. **Search input — filters the list**
       - Fill `#searchInput` with `zzz-no-match-${Date.now()}`
       - Wait 500ms (debounce)
       - Assert `#archivesList` shows empty state or zero cards
       - Clear the search input
       - Wait 500ms
       - Assert archives repopulate or empty state clears

    5. **View toggle — switch to list view**
       - Click `.view-toggle-btn[data-view="list"]`
       - Assert it gains `.active` class
       - Assert `.view-toggle-btn[data-view="cards"]` loses `.active` class
       - Click `.view-toggle-btn[data-view="cards"]` to restore

    6. **Exports modal — opens and shows buttons**
       - Click `#btnExportsModal`
       - Assert `#exportsModal` becomes visible (hidden attribute removed)
       - Assert `#btnExportPV` is visible inside the modal
       - Assert `#btnExportZip` is visible inside the modal
       - Close via `#btnCloseExports`
       - Assert `#exportsModal` is hidden again

    7. **Refresh button — page reloads data**
       - Click `#btnRefresh`
       - Wait for `#archivesList` to be visible (proves reload completed)

    If test data has no archives, adapt: filter clicks work on empty state, search on zero items, exports modal still opens.
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-archives.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/critical-path-archives.spec.js` exists.
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-archives.spec.js` returns at least 1.
    - `grep -c "archiveTypeFilter\|archiveStatusFilter\|searchInput\|exportsModal\|btnRefresh" tests/e2e/specs/critical-path-archives.spec.js` returns at least 4 (proves multiple interactions covered).
    - `./bin/test-e2e.sh specs/critical-path-archives.spec.js` exits 0.
  </acceptance_criteria>
  <done>The function gate is provable: a Playwright run asserts type filter, status filter, search, view toggle, exports modal, refresh, and data loading — all via observable DOM changes.</done>
</task>

</tasks>

<verification>
After all tasks:
- `! grep -nE '^\s*max-width:\s*[0-9]+px' public/assets/css/archives.css | grep -v '@media' | grep -v 'year-select\|search-input'` returns empty (width gate)
- `! grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/archives.css | grep -v '/\*' | grep -v 'color-mix'` returns empty (token gate)
- `./bin/test-e2e.sh specs/critical-path-archives.spec.js` exits 0 (function gate)
</verification>

<success_criteria>
- Width gate: archives page verified full-width (no cap to remove).
- Token gate: archives.css has zero raw color literals.
- Function gate: critical-path-archives.spec.js asserts real observable results for type filter, status filter, search, view toggle, exports modal, refresh, and data loading.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-10-SUMMARY.md` documenting:
- Width: verification result (already clean or what was fixed)
- Tokens: verification result (already clean or how many literals replaced)
- Function: list of interactions covered, the run output of `./bin/test-e2e.sh specs/critical-path-archives.spec.js`
</output>
