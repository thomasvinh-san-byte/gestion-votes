---
phase: 12-page-by-page-mvp-sweep
plan: 12
type: execute
wave: 3
depends_on: []
files_modified:
  - public/assets/css/admin.css
  - tests/e2e/specs/critical-path-admin-page.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "admin page uses full viewport width — the artificial 720px container cap is removed"
    - "admin.css contains zero hex/oklch/rgba color literals — every color is a design-system token"
    - "Each primary admin interaction (KPI load, search, role filter, create user form, pagination) is asserted by Playwright via observable DOM change or API call"
  artifacts:
    - path: public/assets/css/admin.css
      provides: admin page styles, full-width after fix
      contains: "width: 100%"
    - path: tests/e2e/specs/critical-path-admin-page.spec.js
      provides: function gate for admin page
      contains: "test.describe"
  key_links:
    - from: tests/e2e/specs/critical-path-admin-page.spec.js
      to: public/admin.htmx.html
      via: "page.goto('/admin.htmx.html') + locator click + assert observable"
      pattern: "admin.htmx.html"
---

<objective>
Phase 12 Wave 3 / Page 4: Sweep `/admin` so it passes the 3 MVP gates (width, design tokens, function).

Purpose: admin is the administration dashboard page with 4 KPI cards, user management section (search, role filter, create user inline form, user rows, pagination). v1.2-PAGES-AUDIT.md marked it as "FONCTIONNE" (191 lines HTML, 598 lines JS). CRITICAL: admin.css has a `max-width: var(--content-narrow, 720px)` on `.admin-content` (line 11) which is a WIDTH VIOLATION that must be fixed — this page is an app page, not content, and must use full viewport width. Existing E2E coverage via `critical-path-admin.spec.js` tests the admin ROLE flow (login -> settings -> users -> audit -> logout) but does NOT specifically test admin.htmx.html page interactions.

Output: admin.css fixed to full-width + verified token-pure, plus a `critical-path-admin-page.spec.js` Playwright spec asserting admin PAGE interactions produce real observable results.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-PAGES-AUDIT.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/admin.htmx.html
@public/assets/css/admin.css
@public/assets/js/pages/admin.js
@tests/e2e/helpers.js
</context>

<interfaces>
Existing helper (extract):
```js
// tests/e2e/helpers.js
const { loginAsAdmin } = require('../helpers');
// usage: await loginAsAdmin(page);
```

Existing CSRF pattern (from critical-path-admin.spec.js):
```js
const csrfResp = await page.request.get('/api/v1/auth_csrf.php');
const csrfBody = await csrfResp.json();
const csrfToken = csrfBody?.token || csrfBody?.data?.token || csrfBody?.csrf_token || '';
```

NOTE: `critical-path-admin.spec.js` already exists but tests the admin ROLE flow, not the admin PAGE. Name the new spec `critical-path-admin-page.spec.js` to avoid conflict.

Admin page primary interactive IDs (from public/admin.htmx.html):
- KPI cards: `#adminKpiMembers`, `#adminKpiSessions`, `#adminKpiVotes`, `#adminKpiActive`
- Search: `#searchUser` (input type="search")
- Role filter: `#filterRole` (select)
- Create user button: `#btnCreateUser`
- Create form fields: `#newName`, `#newEmail`, `#newPassword`, `#newRole`
- Password strength: `#passwordStrength`, `#passwordStrengthFill`, `#passwordStrengthText`
- Users list: `#usersListContainer`
- Users count: `#usersCount`
- Pagination: `#usersPaginationInfo`, `#usersPrevPage`, `#usersNextPage`, `#usersPaginationPages`
- Refresh: `#btnRefresh`

Current admin.css width status: `.admin-content { max-width: var(--content-narrow, 720px); margin-inline: auto; }` at line 11 — THIS IS A WIDTH VIOLATION. The admin page is an applicative page (KPI + user management), not a content page. It must use full width.

Current admin.css token status: zero hex/oklch/rgba literals found. All values use `var(--*)`. Clean.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width gate — fix admin.css 720px container cap + verify token-pure</name>
  <files>public/assets/css/admin.css</files>
  <read_first>
    - public/assets/css/admin.css (entire file, 344 lines)
  </read_first>
  <action>
    Fix the WIDTH VIOLATION on `.admin-content` (line 11):

    Replace:
    ```css
    .admin-content {
      max-width: var(--content-narrow, 720px);
      margin-inline: auto;
    ```

    With:
    ```css
    .admin-content {
      width: 100%;
    ```

    Keep the rest of `.admin-content` properties (`padding`, `display`, `flex-direction`, `gap`).

    Rationale: admin is an applicative page (KPI dashboard + user management), not a content/docs page. Per CONTEXT.md MVP-01 locked decision: "pages applicatives en pleine largeur".

    Then run the token violation grep:
    ```bash
    grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(' public/assets/css/admin.css | grep -v '/\*' | grep -v '^\s*\*'
    ```

    Expected: zero matches. admin.css was rebuilt in v4.3 with full design-system tokens.

    Run `php -l` on any PHP files if touched (none expected here).
  </action>
  <verify>
    <automated>bash -c '! grep -nE "max-width:\s*var\(--content-narrow" public/assets/css/admin.css && grep -q "width: 100%" public/assets/css/admin.css && test -z "$(grep -nE "oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(" public/assets/css/admin.css | grep -v "/\*" | grep -v "^\s*\*" | grep -v "color-mix")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'max-width.*content-narrow' public/assets/css/admin.css` returns zero matches (720px cap removed).
    - `grep 'width: 100%' public/assets/css/admin.css` returns a match on `.admin-content`.
    - `grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/admin.css` returns zero non-comment lines.
  </acceptance_criteria>
  <done>Admin page fixed to full-width (720px cap removed) and verified token-pure.</done>
</task>

<task type="auto">
  <name>Task 2: Function gate — Playwright spec asserts real results for admin page interactions</name>
  <files>tests/e2e/specs/critical-path-admin-page.spec.js</files>
  <read_first>
    - tests/e2e/helpers.js (for loginAsAdmin)
    - public/admin.htmx.html (full file — to know which IDs are present)
    - public/assets/js/pages/admin.js (first 70 lines — to understand KPI load and API pattern)
  </read_first>
  <action>
    Create a NEW spec file `tests/e2e/specs/critical-path-admin-page.spec.js` (separate from `critical-path-admin.spec.js` which tests the admin ROLE flow, not the admin PAGE).

    The spec must use `loginAsAdmin(page)` and contain ONE `test()` tagged `@critical-path` named "admin page: KPI load + search + role filter + create form + user list".

    Use `test.setTimeout(120000)` and `waitUntil: 'domcontentloaded'`.

    For each PRIMARY interaction below, trigger AND assert an observable result:

    1. **API data load — KPIs populate**
       - Navigate to `/admin.htmx.html`
       - Wait for `#usersListContainer` to be visible
       - Assert `#adminKpiMembers` text is NOT the placeholder "-" (proves KPI API loaded)
       - Assert `#adminKpiSessions` text is NOT the placeholder "-"

    2. **Users list loads**
       - Assert `#usersCount` text contains a number (not just "- utilisateurs")
       - Assert `#usersListContainer` contains at least one `.user-row` element

    3. **Search input — filters the user list**
       - Fill `#searchUser` with `zzz-no-match-${Date.now()}`
       - Wait 400ms (debounce)
       - Assert `#usersListContainer` shows empty state or zero `.user-row` elements
       - Clear the search input
       - Wait 400ms
       - Assert user rows reappear

    4. **Role filter select — observable change**
       - Change `#filterRole` to `admin`
       - Wait 300ms
       - Assert `#filterRole` has value "admin" (proves select is wired)
       - Assert user rows are filtered (count may change)
       - Change `#filterRole` back to "" (all roles)

    5. **Create user form fields are interactive**
       - Fill `#newName` with "Test User"
       - Fill `#newEmail` with "test@example.com"
       - Fill `#newPassword` with "TestPass123!"
       - Assert `#passwordStrength` becomes visible (hidden attribute removed) — proves password strength handler is wired
       - Assert `#passwordStrengthFill` has a class (weak/fair/good/strong)
       - Clear all fields after test (do NOT submit — avoid creating test users in DB)

    6. **Refresh button — reloads data**
       - Click `#btnRefresh`
       - Wait for `#usersListContainer` to contain `.user-row` elements again (proves reload)

    7. **Width verification — no horizontal scroll at desktop viewport**
       - Assert `document.documentElement.scrollWidth <= document.documentElement.clientWidth` (proves full-width layout works without overflow)
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-admin-page.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/critical-path-admin-page.spec.js` exists.
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-admin-page.spec.js` returns at least 1.
    - `grep -c "searchUser\|filterRole\|adminKpiMembers\|newPassword\|passwordStrength\|usersListContainer" tests/e2e/specs/critical-path-admin-page.spec.js` returns at least 4 (proves multiple interactions covered).
    - `./bin/test-e2e.sh specs/critical-path-admin-page.spec.js` exits 0.
  </acceptance_criteria>
  <done>The function gate is provable: a Playwright run asserts KPI load, user list, search, role filter, create form, password strength, refresh, and width — all via observable DOM changes.</done>
</task>

</tasks>

<verification>
After all tasks:
- `! grep 'max-width.*content-narrow' public/assets/css/admin.css` returns empty (width cap removed)
- `grep 'width: 100%' public/assets/css/admin.css` returns a match (full-width confirmed)
- `! grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/admin.css | grep -v '/\*' | grep -v 'color-mix'` returns empty (token gate)
- `./bin/test-e2e.sh specs/critical-path-admin-page.spec.js` exits 0 (function gate)
</verification>

<success_criteria>
- Width gate: admin page fixed to full-width (720px cap removed, replaced with `width: 100%`).
- Token gate: admin.css has zero raw color literals.
- Function gate: critical-path-admin-page.spec.js asserts real observable results for KPI load, user list, search, role filter, create form, password strength, refresh, and width verification.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-12-SUMMARY.md` documenting:
- Width: what was fixed (720px cap -> width: 100%)
- Tokens: verification result (already clean)
- Function: list of interactions covered, the run output of `./bin/test-e2e.sh specs/critical-path-admin-page.spec.js`
</output>
