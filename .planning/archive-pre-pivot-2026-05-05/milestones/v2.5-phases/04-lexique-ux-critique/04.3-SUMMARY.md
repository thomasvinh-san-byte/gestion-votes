---
phase: 04-lexique-ux-critique
plan: 3
subsystem: tests
tags: [playwright, e2e, modal, focus-trap, a11y]

# Dependency graph
requires:
  - plan: 04.2 (`<ag-modal>` instances in 6 HTML files — spec needs concrete IDs to test)
provides:
  - "tests/e2e/specs/modal-focus-trap.spec.js — 6 tests covering MODAL-02 contract"
  - "Permanent regression guard against focus trap being broken by future modal changes"
  - "Multi-file integration smoke (audit + archives modals tested)"
affects: [phase 04 verifier, /gsd:ship gate Playwright manual run]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Multi-modal coverage: tested both auditDetailModal (most central) and exportsModal (archives) for cross-page focus trap consistency"
    - "Stream-timeout recovery pattern: executor committed spec file before timeout; orchestrator finished SUMMARY + state updates manually post-spot-check"

key-files:
  created:
    - tests/e2e/specs/modal-focus-trap.spec.js (257 lignes, 6 tests, tagged @a11y-v2.3)
  modified: []

key-decisions:
  - "Spec cibles : auditDetailModal (audit.htmx.html, modale principale auditeur) + exportsModal (archives.htmx.html, second échantillon cross-page) — couverture suffisante pour valider le pattern <ag-modal>"
  - "6 tests au lieu de 4 minimum : 4 tests sur auditDetailModal (Tab cycle, Shift+Tab cycle, Escape close, focus restore Escape) + 1 test focus restore via X button + 1 test cross-modal cohérence (exportsModal Escape + restore)"
  - "Stream-timeout post-commit handled inline by orchestrator (commit du spec landed avant timeout — verified via git log e4f5cf6 + node --check OK)"

# Verification
verification:
  acceptance:
    - "Fichier `tests/e2e/specs/modal-focus-trap.spec.js` existe (257 lignes) ✓"
    - "`node --check tests/e2e/specs/modal-focus-trap.spec.js` exit 0 ✓"
    - "`grep -c 'test(' tests/e2e/specs/modal-focus-trap.spec.js` retourne 6 ✓"
    - "`grep -F '@a11y-v2.3' tests/e2e/specs/modal-focus-trap.spec.js` matches ✓"
    - "Tests couvrent : Tab cycle, Shift+Tab cycle, Escape close, focus restore Escape, focus restore X button, cross-modal smoke ✓"
    - "0 occurrence de `copropriété` ou `syndic` (anti-régression CLAUDE.md) ✓"

  playwright_runs:
    actually_executed: 0
    reason: "Sandbox sans Playwright (`node_modules/@playwright/test` absent — pattern identique Phase 1 + Phase 2). Spec syntactiquement valide via `node --check`."
    manual_followup_command: "timeout 60 npx playwright test tests/e2e/specs/modal-focus-trap.spec.js --reporter=line"

# Deviations
deviations:
  - rule: "Stream idle timeout (infrastructure)"
    type: "executor agent timeout post-commit"
    rationale: "L'executor a réussi à écrire le spec et le committer (`e4f5cf6` test(04-3): add modal-focus-trap.spec.js — focus trap a11y guard for <ag-modal>) avant le timeout API stream idle (44 tool uses, 268s). Les wrap-up tasks (SUMMARY.md + STATE.md + ROADMAP.md + REQUIREMENTS.md) n'ont pas été complétées par l'executor — l'orchestrator (Claude main thread) les fait manuellement post-spot-check (working tree clean, commit présent, syntax valid)."
    impact: "Aucun. Le livrable principal (le spec) est en place et committé. Les artefacts métadonnées sont produits inline par l'orchestrator avec une référence explicite à cette déviation."

# Tests
tests:
  added:
    - tests/e2e/specs/modal-focus-trap.spec.js (6 tests, tag @a11y-v2.3)
  modified: []
  removed: []

# Followups
followups:
  - id: "04.3-FOLLOWUP-1"
    type: "manual verification"
    description: "Run Playwright spec on dev machine: `timeout 60 npx playwright test tests/e2e/specs/modal-focus-trap.spec.js --reporter=line`. Si une assertion échoue, c'est probablement parce que `<ag-modal>` ne piège pas correctement le focus — investigation côté `public/assets/js/components/ag-modal.js`. Suit le pattern Phase 1 (cockpit-keyboard-shortcuts.spec.js manual followup)."
    owner: "developer"
    blocking_for: "Phase 04 final sign-off / `/gsd:ship`"

# Commits
commits:
  - hash: e4f5cf6
    type: test
    description: "add modal-focus-trap.spec.js — focus trap a11y guard for <ag-modal>"
  - hash: TBD
    type: docs
    description: "complete plan 04.3 (this SUMMARY.md + STATE/ROADMAP/REQUIREMENTS updates) — orchestrator-completed post-stream-timeout"
