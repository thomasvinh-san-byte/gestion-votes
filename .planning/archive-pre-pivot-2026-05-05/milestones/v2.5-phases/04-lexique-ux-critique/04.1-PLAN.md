---
phase: 04-lexique-ux-critique
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - public/operator.htmx.html
  - public/help.htmx.html
  - public/members.htmx.html
  - app/Services/ErrorDictionary.php
autonomous: true
requirements:
  - LEX-01
  - LEX-02
user_setup: []

must_haves:
  truths:
    - "Les 4 fichiers ciblés sont relus cas-par-cas (89 occurrences) — aucun search-replace aveugle"
    - "Distinction sémantique appliquée : membre (inscrit générique) ≠ votant (éligible scrutin) ≠ participant (présent à la séance)"
    - "Le copy de finalisation utilise 'confirmer/valider/verrouiller-archiver' ; 'Approuver' est banni"
    - "Le SUMMARY liste explicitement chaque transformation effectuée par fichier"
  artifacts:
    - path: "public/operator.htmx.html"
      provides: "Copy lexique opérateur (38 occurrences relues)"
      contains: "votant"
    - path: "public/help.htmx.html"
      provides: "Copy aide utilisateur (22 occurrences relues)"
      contains: "votant"
    - path: "public/members.htmx.html"
      provides: "Copy gestion membres (18 occurrences relues)"
      contains: "membre"
    - path: "app/Services/ErrorDictionary.php"
      provides: "Messages d'erreur français (11 occurrences relues)"
      contains: "votant"
  key_links:
    - from: "public/operator.htmx.html"
      to: "ErrorDictionary.php"
      via: "cohérence lexicale entre copy UI et messages d'erreur"
      pattern: "votant|participant|membre"
---

<objective>
Migrer 4 fichiers ciblés vers la convention lexicale unifiée AgVote (LEX-01) :
- `membre` = inscrit générique (ex: "membre du conseil")
- `votant` = éligible au scrutin courant (ex: "membre votant" → "votant")
- `participant` = présent à la séance (réservé à présence/quorum)

Et appliquer LEX-02 transverse :
- "confirmer/valider/verrouiller-archiver" pour la finalisation
- "Approuver" est banni (ambigu juridiquement) → remplacer par "Valider" ou "Confirmer" selon contexte

Purpose: cristalliser la convention sémantique dans le copy utilisateur. Discipline cas-par-cas, pas de search-replace aveugle.
Output: 4 fichiers patchés + SUMMARY listant chaque transformation effectuée.
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@CLAUDE.md

<lexicon_rules>
Pour ChAQUE occurrence de `membre` / `participant` / `votant` / `Approuver` dans les 4 fichiers, classer le contexte AVANT de modifier :

**Règle de classification** :
- "membre du conseil", "ajouter un membre", "supprimer un membre", "liste des membres" → reste `membre` (inscrit générique).
- "membre votant", "membres votants", "le membre vote" (dans contexte scrutin) → `votant` / `votants` (éligible au scrutin courant).
- "présents", "absents", "participation", "quorum", "feuille d'émargement" → `participant` / `participants` (présent à la séance) si le mot d'origine référait à la présence physique.
- "Approuver" / "approbation" en contexte finalisation (vote, PV, ordre du jour) → `Valider` ou `Confirmer` ou `Verrouiller-archiver` selon nuance (LEX-02).
  - "Valider la résolution" : décision d'assemblée
  - "Confirmer le vote" : action utilisateur sur son propre vote
  - "Verrouiller-archiver le PV" : finalisation immuable post-séance

**Règle d'or** : si tu hésites, garde le mot original et note l'ambiguïté dans le SUMMARY pour relecture humaine. JAMAIS de transformation par défaut.
</lexicon_rules>

<read_first>
- `grep -nE 'membre|participant|votant|Approuver|approuv' public/operator.htmx.html` (~38 hits attendus)
- `grep -nE 'membre|participant|votant|Approuver|approuv' public/help.htmx.html` (~22 hits)
- `grep -nE 'membre|participant|votant|Approuver|approuv' public/members.htmx.html` (~18 hits)
- `grep -nE 'membre|participant|votant|Approuver|approuv' app/Services/ErrorDictionary.php` (~11 hits)
- Lire chaque hit avec ±2 lignes de contexte (`grep -nB1 -A1 ...`) AVANT décision.
- Vérifier qu'aucun autre fichier PHP critique ne contient de copy lexique grossièrement obsolète : `grep -rn 'membre votant' app/ public/ --include='*.php' --include='*.html'`. Si hits hors des 4 fichiers ciblés, **noter dans SUMMARY** mais ne pas migrer (scope figé par décision utilisateur).
</read_first>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration lexicale public/operator.htmx.html (38 occurrences)</name>
  <files>public/operator.htmx.html</files>
  <read_first>
    - `grep -nE 'membre|participant|votant|Approuver|approuv' public/operator.htmx.html`
    - Pour chaque hit, lire ±2 lignes de contexte.
  </read_first>
  <action>
1. Pour chacune des ~38 occurrences, classer selon `<lexicon_rules>` :
   - membre du conseil / ajouter un membre / liste des membres → garder
   - membre votant / membres votants (en contexte scrutin) → `votant` / `votants`
   - présents / présence / émargement → `participant` / `participants` si pertinent
   - Approuver / approbation (finalisation) → `Valider` / `Confirmer` / `Verrouiller-archiver`

2. **Garder une trace** : avant chaque `Edit`, noter dans un fichier scratch `.planning/phases/04-lexique-ux-critique/scratch-04.1-operator.txt` la ligne d'origine et la ligne cible. Ce scratch alimente le SUMMARY.

3. Appliquer les `Edit` un par un (PAS de MultiEdit search-replace aveugle).

4. Vérifier le rendu HTML cohérent : pas de balise cassée par accident d'édition.

**Anti-patterns à éviter** :
- Modifier "membre du conseil" en "votant du conseil" (faux : un membre du conseil n'est pas par défaut un votant à la séance courante).
- Modifier "Approuver les CGU" → JAMAIS, "Approuver" est banni UNIQUEMENT en contexte finalisation de vote/PV/OJ.
- Toucher à des `data-*` ou `id` qui contiennent `member` / `voter` (codes techniques, pas du copy).
  </action>
  <verify>
    <automated>grep -cE '\bmembre votant\b|\bApprouver\b' public/operator.htmx.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\bmembre votant\b' public/operator.htmx.html` retourne 0 (sauf cas explicitement justifié dans le scratch).
    - `grep -cE '\bApprouver\b' public/operator.htmx.html` retourne 0 EN CONTEXTE finalisation (vérifier les hits restants ne sont pas en finalisation vote/PV/OJ).
    - Fichier scratch `.planning/phases/04-lexique-ux-critique/scratch-04.1-operator.txt` existe avec une ligne par transformation.
    - HTML reste valide : `grep -c '</' public/operator.htmx.html` count cohérent (pas de tag cassé).
  </acceptance_criteria>
  <done>~38 occurrences relues, transformations appliquées avec discipline sémantique, scratch listant chaque change.</done>
</task>

<task type="auto">
  <name>Task 2: Migration lexicale public/help.htmx.html + public/members.htmx.html (22 + 18 = 40 occurrences)</name>
  <files>public/help.htmx.html, public/members.htmx.html</files>
  <read_first>
    - `grep -nE 'membre|participant|votant|Approuver|approuv' public/help.htmx.html`
    - `grep -nE 'membre|participant|votant|Approuver|approuv' public/members.htmx.html`
    - Contexte ±2 lignes pour chaque hit.
  </read_first>
  <action>
Même méthode que Task 1, sur `public/help.htmx.html` puis `public/members.htmx.html`.

**Spécificité `members.htmx.html`** : ce fichier traite la **gestion des membres** (CRUD inscrits). La majorité des "membre" doivent rester (inscrit générique). Les exceptions :
- Si un onglet/tableau filtre les "membres votants pour la séance X" → la colonne ou le filtre devient "Votants".
- Tout copy parlant de présence à une séance → "participant".

**Spécificité `help.htmx.html`** : page d'aide, le copy doit ÉDUQUER l'utilisateur sur la convention. Profiter de ce fichier pour ajouter (si absent) un mini-encadré expliquant la distinction membre/votant/participant. Si présence d'un glossaire, l'enrichir.

Tracer dans `scratch-04.1-help.txt` et `scratch-04.1-members.txt`.
  </action>
  <verify>
    <automated>grep -cE '\bmembre votant\b' public/help.htmx.html public/members.htmx.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\bmembre votant\b' public/help.htmx.html` retourne 0 (sauf cas pédagogique explicite : "On dit 'votant' et non 'membre votant'").
    - `grep -cE '\bApprouver\b' public/help.htmx.html public/members.htmx.html` retourne 0 EN CONTEXTE finalisation.
    - Scratch files existent avec une ligne par transformation.
    - Si `help.htmx.html` n'avait pas de glossaire, un encadré "Vocabulaire" est ajouté (membre/votant/participant définis).
  </acceptance_criteria>
  <done>~40 occurrences relues sur les 2 fichiers, transformations appliquées avec discipline sémantique.</done>
</task>

<task type="auto">
  <name>Task 3: Migration lexicale app/Services/ErrorDictionary.php (11 occurrences) + scan transverse</name>
  <files>app/Services/ErrorDictionary.php</files>
  <read_first>
    - `grep -nE 'membre|participant|votant|Approuver|approuv' app/Services/ErrorDictionary.php`
    - Lire le fichier en entier (il est compact, ≤500 lignes typiquement).
  </read_first>
  <action>
1. Pour chaque message d'erreur contenant `membre` / `participant` / `votant` / `Approuver` :
   - Si le message concerne un scrutin/vote → privilégier `votant`.
   - Si le message concerne la présence/quorum → `participant`.
   - Si le message concerne la gestion d'inscription → `membre`.
   - Si le message contient "Approuver" / "approbation" en finalisation → `Valider` / `Confirmer`.

2. Appliquer Edit un par un, tracer dans `scratch-04.1-errordict.txt`.

3. **Vérification syntaxe** : `php -l app/Services/ErrorDictionary.php` (CLAUDE.md).

4. **Scan transverse** (audit, ne modifie rien) :
```bash
grep -rnE '\bmembre votant\b|\bApprouver\b' app/ public/ --include='*.php' --include='*.html' | grep -v -E 'operator\.htmx\.html|help\.htmx\.html|members\.htmx\.html|ErrorDictionary\.php' > .planning/phases/04-lexique-ux-critique/scratch-04.1-out-of-scope.txt || true
```
Si le scratch out-of-scope contient des hits, **les lister dans SUMMARY** comme dette technique reportée v2.4 (scope figé par décision utilisateur).
  </action>
  <verify>
    <automated>php -l app/Services/ErrorDictionary.php</automated>
  </verify>
  <acceptance_criteria>
    - `php -l app/Services/ErrorDictionary.php` exit 0.
    - `grep -cE '\bmembre votant\b' app/Services/ErrorDictionary.php` retourne 0.
    - `grep -cE 'Approuver|approbation' app/Services/ErrorDictionary.php` retourne 0 en contexte finalisation.
    - Scratch `scratch-04.1-out-of-scope.txt` existe (vide ou listant les hits hors scope).
    - Le SUMMARY liste les transformations + la dette out-of-scope reportée v2.4.
  </acceptance_criteria>
  <done>11 occurrences relues, ErrorDictionary cohérent avec convention lexicale, audit out-of-scope documenté.</done>
</task>

</tasks>

<verification>
- `grep -cE '\bmembre votant\b' public/operator.htmx.html public/help.htmx.html public/members.htmx.html app/Services/ErrorDictionary.php` retourne 0 (ou hits ≤ exceptions documentées dans scratch).
- Aucun "Approuver" en contexte finalisation dans les 4 fichiers.
- `php -l app/Services/ErrorDictionary.php` exit 0.
- 4 scratch files existent (`scratch-04.1-{operator,help,members,errordict,out-of-scope}.txt`).
</verification>

<success_criteria>
Convention lexicale AgVote cristallisée dans les 4 fichiers ciblés. Chaque transformation tracée et justifiable. Dette out-of-scope documentée pour v2.4. Aucune ambiguïté sémantique introduite.
</success_criteria>

<output>
Créer `.planning/phases/04-lexique-ux-critique/04.1-SUMMARY.md` après exécution. Contenu attendu :
- Total occurrences relues par fichier (38 + 22 + 18 + 11 = 89).
- Pour chaque fichier, liste des transformations effectuées (`avant → après` + ligne approximative).
- Liste des occurrences gardées telles quelles avec justification (ex: "membre du conseil" reste).
- Liste des hits out-of-scope (autres fichiers) reportés v2.4.
- Confirmation `php -l ErrorDictionary.php` exit 0.
</output>
</content>
