---
phase: 02-pages-editoriales
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/js/components/ag-integrity-modal.js
  - public/assets/css/components/ag-integrity-modal.css
autonomous: true
requirements:
  - EDITORIAL-05
user_setup: []

must_haves:
  truths:
    - "Un custom element <ag-integrity-modal> existe et peut être ouvert via attribut/méthode"
    - "Le modal affiche d'abord le préambule pédagogique français VERBATIM avant la chaîne audit_events"
    - "Toutes les valeurs user-injected (date, hashes) sont HTML-escapées (XSS-safe)"
    - "Le modal a un focus trap et se ferme via Escape ou bouton fermeture"
  artifacts:
    - path: "public/assets/js/components/ag-integrity-modal.js"
      provides: "Custom element <ag-integrity-modal> avec préambule + rendu chaîne audit_events"
      contains: "customElements.define"
      min_lines: 80
    - path: "public/assets/css/components/ag-integrity-modal.css"
      provides: "Styles modal (overlay, dialog, chaîne audit_events monospace)"
      contains: ".ag-integrity-modal"
      min_lines: 30
  key_links:
    - from: "public/assets/js/components/ag-integrity-modal.js"
      to: "préambule pédagogique EDITORIAL-05"
      via: "string literal verbatim"
      pattern: "Voici la preuve que ce PV n'a pas été modifié"
    - from: "public/assets/js/components/ag-integrity-modal.js"
      to: "data-events JSON attribute (audit_events)"
      via: "JSON.parse + render"
      pattern: "JSON\\.parse"
---

<objective>
Créer le custom element `<ag-integrity-modal>` (EDITORIAL-05). Le modal s'ouvre depuis le lien "Vérifier l'intégrité" en bas du PV/audit, et affiche **D'ABORD** un préambule pédagogique français VERBATIM, **PUIS** la chaîne `audit_events` (sceaux cryptographiques) en monospace.

Purpose: Rendre la preuve d'intégrité rassurante (pas du jargon). 99% des utilisateurs ne lisent pas un hash chain — la pédagogie en français est la valeur livrée.
Output: 1 fichier JS (custom element) + 1 fichier CSS, prêts à être inclus dans le plan 02.3.
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@CLAUDE.md

<read_first>
- public/assets/js/components/ (lister le dossier — vérifier patterns existants ; ag-health-bar.js du Phase 1 est la référence stylistique)
- public/assets/js/components/ag-health-bar.js (lecture rapide pour calquer le pattern customElements + Shadow DOM ou non — adopter la même convention)
</read_first>

<preamble_verbatim>
**TEXTE OBLIGATOIRE — copier sans modifier (apostrophes typographiques incluses) :**

```
Voici la preuve que ce PV n'a pas été modifié depuis le {date}. Chaque ligne ci-dessous est un sceau cryptographique reliant la précédente — modifier une seule virgule briserait la chaîne.
```

`{date}` est remplacé par la valeur de l'attribut `data-date` (HTML-escapée).
</preamble_verbatim>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Custom element ag-integrity-modal.js avec préambule + chaîne audit_events</name>
  <files>public/assets/js/components/ag-integrity-modal.js</files>
  <read_first>
    - public/assets/js/components/ag-health-bar.js (pattern customElements de référence — adopter mêmes conventions)
  </read_first>
  <behavior>
    - Test 1 : Lorsqu'on instancie `<ag-integrity-modal data-date="2026-04-30" data-events='[{"hash":"abc","prev":"def","at":"2026-04-30T10:00:00Z"}]'></ag-integrity-modal>` et qu'on appelle `.open()`, le DOM rendu contient le préambule VERBATIM avec la date substituée.
    - Test 2 : Une valeur `data-date` contenant `<script>alert(1)</script>` est échappée — le DOM ne contient pas de balise `<script>` injectée (XSS).
    - Test 3 : `.close()` retire le modal du DOM (ou ajoute `hidden`).
    - Test 4 : Touche Escape ferme le modal.
  </behavior>
  <action>
Créer `public/assets/js/components/ag-integrity-modal.js` (vanilla JS, pas de framework). Pattern :

```javascript
class AgIntegrityModal extends HTMLElement {
  static get observedAttributes() { return ['data-date', 'data-events']; }

  connectedCallback() {
    // B-2 fix : auto-hidden par défaut. Le modal n'apparaît qu'après .open()
    // explicite (clic sur "Vérifier l'intégrité"). Sans ce verrou, un user
    // qui charge la page voit le modal flasher avant le hidden CSS.
    if (!this.hasAttribute('hidden') && !this.hasAttribute('data-keep-open')) {
      this.setAttribute('hidden', '');
    }
    if (!this.hasAttribute('data-rendered')) {
      this._render();
      this.setAttribute('data-rendered', '');
    }
    this._bindKeyboard();
  }

  disconnectedCallback() {
    document.removeEventListener('keydown', this._onKeyDown);
  }

  open() { this.removeAttribute('hidden'); this._trapFocus(); }
  close() { this.setAttribute('hidden', ''); this._releaseFocus(); }

  _escape(str) {
    // HTML-escape pour XSS prevention
    return String(str ?? '')
      .replace(/&/g, '&amp;')
      .replace(/</g, '&lt;')
      .replace(/>/g, '&gt;')
      .replace(/"/g, '&quot;')
      .replace(/'/g, '&#39;');
  }

  _render() {
    const date = this._escape(this.getAttribute('data-date') || '');
    let events = [];
    try {
      events = JSON.parse(this.getAttribute('data-events') || '[]');
    } catch (e) { events = []; }

    // PRÉAMBULE VERBATIM — ne pas modifier (EDITORIAL-05)
    const preambleHtml = `
      <p class="ag-integrity-preamble">
        Voici la preuve que ce PV n'a pas été modifié depuis le ${date}. Chaque ligne ci-dessous est un sceau cryptographique reliant la précédente — modifier une seule virgule briserait la chaîne.
      </p>
    `;

    const eventsHtml = events.map(ev => `
      <li class="ag-integrity-event">
        <code class="ag-integrity-hash">${this._escape(ev.hash)}</code>
        <span class="ag-integrity-meta">
          ← ${this._escape(ev.prev || '∅')} · ${this._escape(ev.at || '')}
        </span>
      </li>
    `).join('');

    this.innerHTML = `
      <div class="ag-integrity-modal__overlay" data-close></div>
      <div class="ag-integrity-modal__dialog" role="dialog" aria-modal="true" aria-labelledby="ag-integrity-title">
        <header>
          <h2 id="ag-integrity-title">Intégrité du procès-verbal</h2>
          <button type="button" class="ag-integrity-close" data-close aria-label="Fermer">×</button>
        </header>
        ${preambleHtml}
        <ol class="ag-integrity-chain">${eventsHtml}</ol>
      </div>
    `;

    this.querySelectorAll('[data-close]').forEach(el =>
      el.addEventListener('click', () => this.close())
    );
  }

  _bindKeyboard() {
    this._onKeyDown = (e) => { if (e.key === 'Escape') this.close(); };
    document.addEventListener('keydown', this._onKeyDown);
  }

  _trapFocus() {
    const dialog = this.querySelector('.ag-integrity-modal__dialog');
    if (dialog) dialog.focus();
  }
  _releaseFocus() { /* optionnel : restaurer activeElement précédent */ }
}

customElements.define('ag-integrity-modal', AgIntegrityModal);
```

Règles strictes :
1. **Préambule VERBATIM** — copier la phrase EDITORIAL-05 sans modifier un seul caractère (apostrophes typographiques `'` incluses si présentes dans REQUIREMENTS.md).
2. **Tous les `data-*` user-injected passent par `_escape()` avant injection dans `innerHTML`** (XSS).
3. **Pas de `eval` ni `Function` ni `setTimeout(string)`**.
4. Tester `node --check public/assets/js/components/ag-integrity-modal.js` avant fin.
5. Forbidden words check : `grep -iE "(copropriété|syndic)" public/assets/js/components/ag-integrity-modal.js` retourne 0.
  </action>
  <verify>
    <automated>node --check public/assets/js/components/ag-integrity-modal.js && grep -F "Voici la preuve que ce PV n'a pas été modifié depuis le" public/assets/js/components/ag-integrity-modal.js && grep -F "sceau cryptographique" public/assets/js/components/ag-integrity-modal.js && grep -F "customElements.define('ag-integrity-modal'" public/assets/js/components/ag-integrity-modal.js && grep -F "this.setAttribute('hidden', '')" public/assets/js/components/ag-integrity-modal.js && [ "$(grep -ciE '(copropriété|syndic)' public/assets/js/components/ag-integrity-modal.js)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `public/assets/js/components/ag-integrity-modal.js` exists
    - `node --check` exits 0
    - **Préambule VERBATIM** : `grep -F "Voici la preuve que ce PV n'a pas été modifié depuis le" public/assets/js/components/ag-integrity-modal.js` matches
    - **Métaphore sceau** : `grep -F "sceau cryptographique reliant la précédente" public/assets/js/components/ag-integrity-modal.js` matches
    - **Closure préambule** : `grep -F "modifier une seule virgule briserait la chaîne" public/assets/js/components/ag-integrity-modal.js` matches
    - `grep -F "customElements.define('ag-integrity-modal'" public/assets/js/components/ag-integrity-modal.js` matches
    - **B-2 auto-hidden** : `grep -F "this.setAttribute('hidden', '')" public/assets/js/components/ag-integrity-modal.js` matches au moins 1× (dans connectedCallback)
    - **XSS escape** : `grep -F "_escape(" public/assets/js/components/ag-integrity-modal.js` matches au moins 4× (date + ev.hash + ev.prev + ev.at)
    - **Forbidden words** : `grep -ciE "(copropriété|syndic)" public/assets/js/components/ag-integrity-modal.js` returns 0
  </acceptance_criteria>
  <done>Fichier créé, syntaxe JS valide, préambule VERBATIM présent, customElement enregistré, **auto-hidden dans connectedCallback (B-2)**, escape XSS appliqué sur tous les data-* user-injected, 0 mot interdit.</done>
</task>

<task type="auto">
  <name>Task 2: Styles modal — overlay, dialog, chaîne monospace</name>
  <files>public/assets/css/components/ag-integrity-modal.css</files>
  <read_first>
    - public/assets/css/design-system.css (grep tokens `--font-mono`, `--shadow-`, `--border-default`, `--space-*`)
  </read_first>
  <action>
Créer `public/assets/css/components/ag-integrity-modal.css` :

```css
/*
 * ag-integrity-modal.css — EDITORIAL-05
 * Modal pédagogique d'intégrité du PV (préambule + chaîne audit_events)
 */

ag-integrity-modal[hidden] { display: none; }

ag-integrity-modal {
  position: fixed;
  inset: 0;
  z-index: 9000;
  display: grid;
  place-items: center;
  font-family: var(--font-sans);
}

.ag-integrity-modal__overlay {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.5);
}

.ag-integrity-modal__dialog {
  position: relative;
  max-width: 640px;
  width: calc(100% - var(--space-8));
  max-height: 80vh;
  overflow-y: auto;
  /* F-6 fix : utiliser les vrais tokens du design system (vérifiés dans design-system.css) */
  background: var(--color-surface-raised);  /* le seul blanc pur — modal content */
  border: var(--border-default);             /* token TECH-01 ajouté */
  border-radius: var(--radius-md);           /* 6px */
  box-shadow: var(--shadow-lg);              /* token existant */
  padding: var(--space-6);
}

.ag-integrity-modal__dialog header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--space-4);
}

.ag-integrity-close {
  background: transparent;
  border: 0;
  font-size: 1.5rem;
  cursor: pointer;
  padding: var(--space-1) var(--space-2);
}

.ag-integrity-preamble {
  font-family: var(--font-display);
  line-height: 1.6;
  margin-bottom: var(--space-5);
  color: var(--color-text-secondary);  /* F-6 fix : token réel du design system */
}

.ag-integrity-chain {
  list-style: decimal;
  padding-left: var(--space-6);
  margin: 0;
}

.ag-integrity-event {
  margin-bottom: var(--space-3);
}

.ag-integrity-hash {
  font-family: var(--font-mono);
  font-size: 0.85rem;
  word-break: break-all;
}

.ag-integrity-meta {
  display: block;
  font-size: 0.8rem;
  color: var(--color-text-muted);  /* F-6 fix : token réel */
  margin-top: var(--space-1);
}
```

Règles : 0 padding/margin hardcodé numérique (tout via tokens). Pas de `text-align: center`.
  </action>
  <verify>
    <automated>test -f public/assets/css/components/ag-integrity-modal.css && grep -F "var(--font-mono)" public/assets/css/components/ag-integrity-modal.css && grep -F "var(--font-display)" public/assets/css/components/ag-integrity-modal.css && [ "$(grep -cE '(padding|margin):\s+[0-9]+' public/assets/css/components/ag-integrity-modal.css)" = "0" ]</automated>
  </verify>
  <done>CSS créé, tokens utilisés exclusivement, 0 padding/margin hardcodé.</done>
</task>

</tasks>

<verification>
- `node --check public/assets/js/components/ag-integrity-modal.js`
- `grep -F "Voici la preuve que ce PV n'a pas été modifié depuis le" public/assets/js/components/ag-integrity-modal.js` retourne 1+ match
- `grep -F "sceau cryptographique reliant la précédente" public/assets/js/components/ag-integrity-modal.js` retourne 1+ match
- Le code utilise `innerHTML` uniquement avec données HTML-escapées
- 0 mot interdit (`copropriété`, `syndic`)
</verification>

<success_criteria>
Custom element fonctionnel, préambule pédagogique VERBATIM, sécurisé XSS, prêt à être inclus dans les pages éditoriales (plan 02.3).
</success_criteria>

<output>
Créer `.planning/phases/02-pages-editoriales/02-02-SUMMARY.md` après exécution.
</output>
</content>
</invoke>