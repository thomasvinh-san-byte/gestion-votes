---
phase: 03-layouts-secondaires
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - public/dashboard.htmx.html
  - public/assets/css/dashboard.css
  - public/assets/js/components/ag-empty-state.js
autonomous: true
requirements:
  - DASHBOARD-01
  - DASHBOARD-02
  - DASHBOARD-04
  - DASHBOARD-06
user_setup: []

must_haves:
  truths:
    - "Le dashboard affiche au maximum 3 KPI cards (Convoc. en attente supprimé)"
    - "Le wrapper KPI utilise display: grid avec grid-template-columns: repeat(3, 1fr)"
    - "La hero card supporte 3 états visuels distincts via classes .hero-card--ambient, .hero-card--urgent, .hero-card--live"
    - "L'animation pulse sur .hero-card--live respecte prefers-reduced-motion"
    - "Un empty state markup existe quand 0 séance prévue avec CTA vers /seances/nouvelle"
    - "Le texte 'Aucune séance prévue. Créez-en une pour commencer.' apparaît dans le markup"
  artifacts:
    - path: "public/dashboard.htmx.html"
      provides: "Markup KPI réduit à 3 cards, hero card 3 états, empty state"
      contains: "hero-card--"
    - path: "public/assets/css/dashboard.css"
      provides: "Grid 3 colonnes pour KPI, styles hero-card states, empty state, prefers-reduced-motion guard"
      contains: "grid-template-columns: repeat(3, 1fr)"
  key_links:
    - from: "public/assets/css/dashboard.css"
      to: "design-system.css tokens"
      via: "var(--space-*), var(--color-*), var(--radius-*)"
      pattern: "var\\(--(space|color|radius|font)-"
    - from: "public/dashboard.htmx.html"
      to: "public/assets/css/dashboard.css"
      via: "classes .hero-card--ambient/urgent/live, .ag-empty-state, .kpi-card-wrapper"
      pattern: "hero-card--(ambient|urgent|live)"
---

<objective>
Réduire le dashboard à 3 KPI cards (suppression de `Convoc. en attente` / `kpi-card--3`), poser une grille CSS Grid 3 colonnes sur le wrapper KPI, ajouter les 3 états de hero card (ambient/urgent/live) et un empty state markup quand aucune séance n'est prévue.

Purpose: Aligner le dashboard sur les requirements DASHBOARD-01/02/04/06 — moins de bruit décisionnel, hiérarchie claire pour la séance imminente, layout grid moderne, feedback explicite quand vide.
Output: `public/dashboard.htmx.html` modifié (KPI réduit, hero 3 états, empty state) + `public/assets/css/dashboard.css` modifié (grille 3 colonnes, classes états hero, styles empty, motion guard).
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@CLAUDE.md

<read_first>
- public/dashboard.htmx.html (entier — repérer `kpi-card--3`, le wrapper KPI parent, la hero card actuelle, et où insérer l'empty state)
- public/assets/css/dashboard.css (entier — repérer le sélecteur du wrapper KPI, les styles hero existants)
- public/assets/css/design-system.css (uniquement grep `--space-`, `--color-`, `--radius-`, `--font-` pour récupérer les noms exacts de tokens disponibles)
</read_first>

<product_decision_locked>
**DASHBOARD-01 (figé) :** Le KPI supprimé est `Convoc. en attente` (la 3e card, classe `kpi-card--3`). Garder : `AG à venir`, `En cours`, `PV à envoyer`. Justification produit : la donnée "convocations en attente" est consultable via `/membres` ou un futur lien `/analytics` ; elle n'a pas de charge décisionnelle quotidienne contrairement aux 3 conservées qui pilotent l'action immédiate.
</product_decision_locked>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Supprimer le KPI Convoc. en attente et passer le wrapper en grid 3 colonnes</name>
  <files>public/dashboard.htmx.html, public/assets/css/dashboard.css</files>
  <read_first>
    - public/dashboard.htmx.html (localiser la section KPI, repérer le wrapper parent et l'élément `<div class="kpi-card-wrapper">` ou équivalent contenant `kpi-card--3`)
    - public/assets/css/dashboard.css (localiser le sélecteur qui style le wrapper KPI actuellement)
  </read_first>
  <acceptance_criteria>
    - `grep -c "kpi-card--3" public/dashboard.htmx.html` retourne 0
    - `grep -F "grid-template-columns: repeat(3, 1fr)" public/assets/css/dashboard.css` matche au moins 1 ligne
    - Le wrapper KPI utilise `display: grid` (pas flex / pas flex-basis hack)
    - Les 3 KPI restants (AG à venir, En cours, PV à envoyer) sont préservés intacts
    - Strings françaises inchangées sur les KPI restants (per CLAUDE.md)
  </acceptance_criteria>
  <action>
1. Dans `public/dashboard.htmx.html`, supprimer le bloc complet contenant `kpi-card--3` (le `<div class="kpi-card-wrapper">` ou conteneur unitaire qui enveloppe ce KPI, ainsi que tout son contenu : titre "Convoc. en attente", valeur, lien). Si la classe wrapper ne correspond pas exactement à `kpi-card-wrapper`, utiliser le wrapper réel parent du `kpi-card--3` repéré.
2. Vérifier que les 3 KPI restants (AG à venir, En cours, PV à envoyer) sont conservés tels quels — aucune modification de leur markup ni de leur texte français.
3. Dans `public/assets/css/dashboard.css`, localiser le sélecteur qui style le wrapper KPI parent. Remplacer (ou ajouter si absent) :
   ```css
   .kpi-grid, /* ou le sélecteur réel du wrapper KPI parent identifié dans le HTML */
   .dashboard-kpis {
     display: grid;
     grid-template-columns: repeat(3, 1fr);
     gap: var(--space-4); /* adapter au token réel disponible (--gap-* ou --space-*) */
   }
   ```
   Si l'ancien style utilisait `flex-basis: 25%` ou similaire pour 4 cards, supprimer ces hacks. Adapter les noms de tokens à ceux qui existent dans `design-system.css`.
4. Ajouter un fallback responsive sous 768px (collapse en 1 colonne ou 2 selon ce qui est cohérent avec les autres breakpoints du fichier) :
   ```css
   @media (max-width: 767.98px) {
     .kpi-grid { grid-template-columns: 1fr; }
   }
   ```
5. **Aucune valeur numérique** pour padding/margin/gap : tout via `var(--space-*)` ou `var(--gap-*)`.
  </action>
  <verify>
    <automated>[ "$(grep -c 'kpi-card--3' public/dashboard.htmx.html)" = "0" ] && grep -F "grid-template-columns: repeat(3, 1fr)" public/assets/css/dashboard.css >/dev/null && grep -F "AG à venir" public/dashboard.htmx.html >/dev/null && grep -F "PV à envoyer" public/dashboard.htmx.html >/dev/null</automated>
  </verify>
  <done>`kpi-card--3` absent du HTML ; wrapper KPI en grid 3 colonnes via tokens ; 3 KPI restants intacts avec texte français préservé.</done>
</task>

<task type="auto">
  <name>Task 2: Hero card 3 états (ambient/urgent/live) avec markup conditionnel et CSS</name>
  <files>public/dashboard.htmx.html, public/assets/css/dashboard.css</files>
  <read_first>
    - public/dashboard.htmx.html (repérer la hero card actuelle — souvent `<section class="hero-card">` ou similaire, identifier où injecter la logique de variante)
    - public/assets/css/dashboard.css (repérer les styles hero existants)
  </read_first>
  <acceptance_criteria>
    - Le HTML expose les 3 classes modificateurs : `hero-card--ambient`, `hero-card--urgent`, `hero-card--live` (au moins une présente, avec convention claire pour les autres — commentaire HTMX ou template)
    - `grep -F "hero-card--live" public/dashboard.htmx.html public/assets/css/dashboard.css` matche au moins 1 ligne
    - `grep -F "hero-card--ambient" public/assets/css/dashboard.css` matche
    - `grep -F "hero-card--urgent" public/assets/css/dashboard.css` matche
    - L'animation `pulse` sur `.hero-card--live` est encadrée par `@media (prefers-reduced-motion: no-preference)` OU un override explicite `@media (prefers-reduced-motion: reduce) { animation: none }`
    - Strings d'action françaises : "Préparer la séance" (ambient), "Démarrer maintenant" (urgent), "Reprendre la séance" ou équivalent (live) — aligné avec REQUIREMENTS DASHBOARD-02
  </acceptance_criteria>
  <action>
1. Dans `public/dashboard.htmx.html`, repérer la hero card actuelle. Adapter son markup pour exposer une classe d'état dynamique. Convention : la classe `hero-card--{state}` est ajoutée côté serveur selon `meeting.imminence`. Si le rendu côté serveur n'est pas dans ce fichier, ajouter au moins un commentaire HTMX et le markup d'un état par défaut + commentaire structurel décrivant les 3 :

   ```html
   <!-- Hero card: 3 états selon imminence (DASHBOARD-02)
        - hero-card--ambient : séance prévue >5min et <60min ; CTA "Préparer la séance"
        - hero-card--urgent  : séance prévue <5min ; CTA "Démarrer maintenant"
        - hero-card--live    : séance en cours ; CTA "Reprendre la séance" + pulse
        La classe est appliquée côté serveur selon l'état réel. -->
   <section class="hero-card hero-card--ambient">
     <h2 class="hero-card__title">Prochaine séance</h2>
     <p class="hero-card__meta">…</p>
     <a href="/seances/{id}/preparer" class="btn btn-primary">Préparer la séance</a>
   </section>
   ```

   Si la logique conditionnelle est déjà dans un template PHP/HTMX inclus, utiliser un `if/elseif/else` qui injecte la bonne classe et le bon CTA. Préserver toute logique HTMX existante.

2. Dans `public/assets/css/dashboard.css`, ajouter les styles des 3 variantes :

   ```css
   /* Hero card — état ambient (séance >5min, <60min) */
   .hero-card--ambient {
     background: var(--color-surface, #fff);
     border: 1px solid var(--border-subtle);
   }

   /* Hero card — état urgent (séance <5min) */
   .hero-card--urgent {
     background: var(--color-warning-bg, #fff7ed);
     border: 1px solid var(--color-warning, #f59e0b);
   }

   /* Hero card — état live (séance en cours)
      B1 fix : --color-danger (rouge huissier oklch 0.50 0.165 25), PAS --color-success.
      REQUIREMENTS DASHBOARD-02 : "live (séance en cours, accent danger pulse, action 'Reprendre')".
      Une séance live signale criticité (action irréversible en flux) — pas un succès. */
   .hero-card--live {
     background: var(--color-danger-subtle);   /* token réel — pas de fallback inline */
     border: 1px solid var(--color-danger-border);
   }

   @keyframes hero-card-pulse {
     0%, 100% { box-shadow: 0 0 0 0 color-mix(in oklch, var(--color-danger) 60%, transparent); }
     50%      { box-shadow: 0 0 0 6px color-mix(in oklch, var(--color-danger) 0%, transparent); }
   }

   /* prefers-reduced-motion : pulse activée par défaut, désactivée si l'utilisateur
      a demandé reduce — pattern inverse de "no-preference activates". COCKPIT-03 même contrat. */
   .hero-card--live {
     animation: hero-card-pulse 2s ease-in-out infinite;
   }
   @media (prefers-reduced-motion: reduce) {
     .hero-card--live {
       animation: none;
     }
   }
   ```

   Tokens vérifiés présents dans `design-system.css` : `--color-warning`, `--color-warning-subtle`, `--color-warning-border`, `--color-danger`, `--color-danger-subtle`, `--color-danger-border`. **Ne pas inventer** ni utiliser de fallback inline (les tokens existent — pas besoin).

3. **Aucun padding/margin numérique** : tout via tokens.
  </action>
  <verify>
    <automated>grep -F "hero-card--live" public/assets/css/dashboard.css >/dev/null && grep -F "hero-card--ambient" public/assets/css/dashboard.css >/dev/null && grep -F "hero-card--urgent" public/assets/css/dashboard.css >/dev/null && grep -F "prefers-reduced-motion" public/assets/css/dashboard.css >/dev/null && grep -F "hero-card--" public/dashboard.htmx.html >/dev/null && grep -F "var(--color-danger" public/assets/css/dashboard.css >/dev/null && [ "$(grep -c 'var(--color-success' public/assets/css/dashboard.css | head -c 99)" = "0" ] || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "hero-card--ambient" public/assets/css/dashboard.css` matches
    - `grep -F "hero-card--urgent" public/assets/css/dashboard.css` matches
    - `grep -F "hero-card--live" public/assets/css/dashboard.css` matches
    - **B1 lock** : `grep -F "var(--color-danger" public/assets/css/dashboard.css` matches au moins 2× (background + border ou pulse)
    - **B1 anti-regression** : aucune occurrence de `var(--color-success` ou `var(--color-success-bg` dans le bloc `.hero-card--live` ni `@keyframes hero-card-pulse` (vérifier visuellement le diff)
    - `grep -F "prefers-reduced-motion: reduce" public/assets/css/dashboard.css` matches (pulse désactivée si user demande reduce)
    - `grep -F "@keyframes hero-card-pulse" public/assets/css/dashboard.css` matches
    - **F3 lock CTA exacts** : `grep -F "Préparer la séance" public/dashboard.htmx.html` matches AND `grep -F "Démarrer maintenant" public/dashboard.htmx.html` matches AND `grep -F "Reprendre" public/dashboard.htmx.html` matches (les 3 CTA spec figés DASHBOARD-02 ROADMAP §2)
  </acceptance_criteria>
  <done>3 classes modificateurs définies dans le CSS, hero card du HTML porte les variantes correctes, pulse encadrée par prefers-reduced-motion, **B1 fix appliqué** : `live` utilise `--color-danger*` (sémantique correcte de criticité), **F3 CTA strings tous grep-vérifiés** dans verify.</done>
</task>

<task type="auto">
  <name>Task 3: Empty state markup quand aucune séance prévue</name>
  <files>public/dashboard.htmx.html, public/assets/css/dashboard.css</files>
  <read_first>
    - public/dashboard.htmx.html (repérer la zone séances/agenda principale et la condition existante "0 séance" si présente — sinon ajouter une branche)
  </read_first>
  <acceptance_criteria>
    - `grep -F "Aucune séance prévue" public/dashboard.htmx.html` matche
    - `grep -F "ag-empty-state" public/dashboard.htmx.html` matche
    - `grep -F "/seances/nouvelle" public/dashboard.htmx.html` matche dans le contexte empty state
    - Aucune illustration / SVG décoratif ajouté (pure typographie + bouton, per DASHBOARD-04)
    - CSS `.ag-empty-state` défini dans `dashboard.css` avec centrage et tokens uniquement
  </acceptance_criteria>
  <action>

**B2 fix : utiliser le custom element existant `<ag-empty-state>` (v1.9 pattern), PAS une nouvelle classe CSS.**

Le composant `public/assets/js/components/ag-empty-state.js` existe et est déjà chargé sur dashboard.htmx.html ligne 220. Il rend un SVG par défaut via `EMPTY_SVG[icon] || EMPTY_SVG.generic`. DASHBOARD-04 interdit l'illustration décorative — il faut donc **étendre le composant pour supporter `icon="none"`** OU ne pas passer d'attribut `icon` reconnu.

Solution figée :
- **Étape 1 (composant)** : étendre `public/assets/js/components/ag-empty-state.js` pour supporter `icon="none"` (skip le bloc icon entièrement) tout en restant rétrocompatible avec les usages existants (`icon="meetings"`, `members`, `votes`, `archives`, `generic`).
- **Étape 2 (dashboard)** : utiliser `<ag-empty-state icon="none" ...>` sur le dashboard.

1. **Étendre `ag-empty-state.js`** — patch chirurgical autour de la ligne 59-61 (`var svg = EMPTY_SVG[icon] || EMPTY_SVG.generic; ... h += '<div class="empty-state-icon">'...`) :

   ```javascript
   // Avant (lignes 59-61) :
   //   var svg = EMPTY_SVG[icon] || EMPTY_SVG.generic;
   //   h += '<div class="empty-state-icon">' + svg + '</div>';
   // Après — skip le bloc icon entièrement si icon === 'none' :
   if (icon !== 'none') {
     var svg = EMPTY_SVG[icon] || EMPTY_SVG.generic;
     h += '<div class="empty-state-icon">' + svg + '</div>';
   }
   ```

   `node --check public/assets/js/components/ag-empty-state.js` doit exit 0.

2. **Dans `public/dashboard.htmx.html`**, repérer la section agenda séances. Ajouter le markup empty state :

   ```html
   <!-- Empty state DASHBOARD-04 : affiché par le rendu serveur quand 0 séance prévue ET 0 séance tenue dans les 30 derniers jours.
        icon="none" requis (DASHBOARD-04 — pas d'illustration décorative). -->
   <ag-empty-state
     icon="none"
     title="Aucune séance prévue."
     description="Créez-en une pour commencer."
     action-label="Créer une séance"
     action-href="/seances/nouvelle">
   </ag-empty-state>
   ```

3. **F2 fix — branche conditionnelle côté serveur**. Lire `app/Controller/DashboardController.php` (ou le contrôleur qui hydrate le dashboard) pour identifier la zone agenda. Vérifier qu'une branche `if (count($upcomingMeetings) === 0 && count($recentMeetings) === 0)` existe ou en ajouter une. Acceptance : grep dans le contrôleur d'une condition couvrant les 2 critères (séance prévue ET séance récente <30j). Si la branche n'existe pas, ajouter une assignation au template du flag booléen `$showEmptyState` puis l'utiliser dans le template HTML.

   **Si le contrôleur ne peut pas être trouvé / le pattern de rendu est entièrement HTMX côté client** : documenter dans le SUMMARY que l'empty state s'active uniquement quand `<ag-empty-state>` est rendu par le client (HTMX swap remplace la zone par l'empty state quand l'API retourne 0 résultat). Acceptance dégradée : `grep -F "Aucune séance prévue" public/dashboard.htmx.html` matches.

4. **F1 fix — KPI déposé "intégré ailleurs"**. REQUIREMENTS DASHBOARD-01 exige : *"Le KPI déposé est intégré ailleurs (lien vers /analytics) — aucune information perdue"*. Solution la plus économique : ajouter un **lien discret en dessous des 3 KPI restants** dans dashboard.htmx.html :

   ```html
   <p class="kpi-row__footer-link">
     <a href="/hub" class="link-muted">Voir aussi : convocations en attente</a>
   </p>
   ```

   Et dans dashboard.css :
   ```css
   .kpi-row__footer-link {
     margin-top: var(--space-2);
     text-align: right;
     font-size: var(--text-sm);
   }
   .kpi-row__footer-link a {
     color: var(--color-text-muted);
   }
   ```

   Cela préserve l'accès à la donnée Convocations sans la mettre au même niveau visuel qu'AG à venir / En cours / PV à envoyer.

5. **Aucun padding/margin numérique** — tokens uniquement.
  </action>
  <verify>
    <automated>node --check public/assets/js/components/ag-empty-state.js && grep -F "icon !== 'none'" public/assets/js/components/ag-empty-state.js && grep -F "Aucune séance prévue" public/dashboard.htmx.html && grep -F 'icon="none"' public/dashboard.htmx.html && grep -F "/seances/nouvelle" public/dashboard.htmx.html && grep -F "convocations en attente" public/dashboard.htmx.html</automated>
  </verify>
  <acceptance_criteria>
    - **B2 component extension** : `grep -F "icon !== 'none'" public/assets/js/components/ag-empty-state.js` matches (la garde a été ajoutée)
    - **B2 component syntax valid** : `node --check public/assets/js/components/ag-empty-state.js` exits 0
    - **B2 dashboard usage** : `grep -F '<ag-empty-state' public/dashboard.htmx.html` matches AND `grep -F 'icon="none"' public/dashboard.htmx.html` matches
    - **EDITORIAL-04 text exact** : `grep -F 'title="Aucune séance prévue."' public/dashboard.htmx.html` matches AND `grep -F 'description="Créez-en une pour commencer."' public/dashboard.htmx.html` matches
    - **CTA empty state** : `grep -F 'action-href="/seances/nouvelle"' public/dashboard.htmx.html` matches AND `grep -F 'action-label="Créer une séance"' public/dashboard.htmx.html` matches
    - **F1 KPI déposé "intégré ailleurs"** : `grep -F "convocations en attente" public/dashboard.htmx.html` matches dans le contexte d'un lien (autour de `<a href="/hub"`)
    - **Pas de classe `.ag-empty-state` réinventée** : `grep -F ".ag-empty-state" public/assets/css/dashboard.css` returns 0 (le composant gère son propre style en light DOM)
    - **F2 condition serveur** : `grep -F "showEmptyState" app/Controller/DashboardController.php OR similar` matches OU SUMMARY documente que la branche est gérée côté HTMX swap
  </acceptance_criteria>
  <done>**B2 fix** : composant `<ag-empty-state>` étendu avec `icon="none"` (rétrocompat préservée), utilisé sur dashboard sans réinventer de classe CSS. **F1 fix** : lien `convocations en attente → /hub` ajouté en footer KPI. **F2 fix** : branche conditionnelle vérifiée côté contrôleur ou documentée HTMX-side dans SUMMARY. Empty state texte français exact, pas d'illustration décorative.</done>
</task>

</tasks>

<verification>
- `[ "$(grep -c 'kpi-card--3' public/dashboard.htmx.html)" = "0" ]`
- `grep -F "grid-template-columns: repeat(3, 1fr)" public/assets/css/dashboard.css`
- `grep -F "hero-card--live" public/assets/css/dashboard.css` ; `grep -F "hero-card--ambient" public/assets/css/dashboard.css` ; `grep -F "hero-card--urgent" public/assets/css/dashboard.css`
- `grep -F "prefers-reduced-motion" public/assets/css/dashboard.css`
- `grep -F "Aucune séance prévue" public/dashboard.htmx.html`
- `grep -F "ag-empty-state" public/dashboard.htmx.html public/assets/css/dashboard.css`
- Aucun string "copropriété" ou "syndic" introduit (CLAUDE.md)
</verification>

<success_criteria>
Le dashboard affiche 3 KPI au lieu de 4 (Convoc. en attente supprimé, justifié dans le SUMMARY) ; le wrapper KPI utilise CSS Grid 3 colonnes via tokens ; la hero card supporte 3 états via classes modificateurs avec animation pulse respectant prefers-reduced-motion ; un empty state markup minimal (typographie + CTA) est en place quand aucune séance n'est prévue.
</success_criteria>

<output>
Créer `.planning/phases/03-layouts-secondaires/03-01-SUMMARY.md` après exécution. Documenter dans le SUMMARY :
- Justification produit du KPI supprimé (Convoc. en attente : non décisionnel quotidien, info consultable via /membres ou /analytics futur)
- Tokens utilisés (liste exacte)
- Approche choisie pour la conditionnalité des états hero card (template/serveur ou commentaire structurel)
</output>
