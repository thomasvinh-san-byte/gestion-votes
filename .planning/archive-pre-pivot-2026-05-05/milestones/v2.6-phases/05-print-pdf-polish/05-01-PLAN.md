---
phase: 05-print-pdf-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - composer.lock
  - tests/Unit/MeetingReportsLongPdfTest.php
  - tests/Unit/Fixtures/LongPvFixtureBuilder.php
  - app/Services/MeetingReportsService.php
autonomous: true
requirements:
  - PDF-V26-01
  - PDF-V26-02
  - PDF-V26-03
tags: [pdf, dompdf, paged-media, smoke-test, i18n-fr]
must_haves:
  truths:
    - "Sur un PV ≥10 pages, le header `[Titre séance] — JJ/MM/YYYY` est présent sur chaque page (verifie automatiquement par parsing du PDF binaire)"
    - "L'em-dash UTF-8 et les caracteres francais accentues (e a c o etc.) apparaissent correctement dans le texte extrait du PDF (pas de `?` ou mojibake)"
    - "Le footer `Page X sur Y` est present sur chaque page avec un X croissant de 1 a Y"
    - "Les PVs courts (<=3 pages) deja valides en v2.4 ne regressent pas (PHPUnit existant reste vert)"
  artifacts:
    - path: "tests/Unit/Fixtures/LongPvFixtureBuilder.php"
      provides: "Builder qui synthetise un meeting + N motions + M attendances suffisants pour generer >=10 pages de PDF (entree dans buildPdfHtml/buildPdfBytes)"
      exports: ["build()"]
      min_lines: 40
    - path: "tests/Unit/MeetingReportsLongPdfTest.php"
      provides: "Smoke tests parsant le PDF binaire genere par MeetingReportsService::buildPdfBytes"
      contains: "testHeaderRepeatedOnEveryPage, testFrenchAccentsAndEmDashRendered, testFooterPageXSurYOnEveryPage"
      min_lines: 80
    - path: "composer.json"
      provides: "smalot/pdfparser ^2.x dans require-dev"
      contains: "smalot/pdfparser"
  key_links:
    - from: "tests/Unit/MeetingReportsLongPdfTest.php"
      to: "app/Services/MeetingReportsService::buildPdfBytes"
      via: "service direct call avec mocks repository"
      pattern: "->buildPdfBytes\\("
    - from: "tests/Unit/MeetingReportsLongPdfTest.php"
      to: "Smalot\\PdfParser\\Parser"
      via: "use + parseContent($pdfBytes)"
      pattern: "Smalot\\\\PdfParser"
    - from: "LongPvFixtureBuilder"
      to: "MeetingReportsServiceTest"
      via: "reuse build* helpers ou pattern equivalent (motion/attendance factories)"
      pattern: "buildMeeting|buildMotion|buildAttendance"
---

<objective>
Verifier automatiquement que la generation PDF dompdf (deja recablee en v2.4 TECH-V24-01 avec `@page @top-center` + `@bottom-center` + `counter(page|pages)`) tient ses promesses sur PV >=10 pages : header repete sur chaque page, em-dash + accents francais correctement encodes, footer `Page X sur Y` paginé. Cloture la dette PDF-V26-01/02/03 — la validation visuelle dev-machine (sandbox-deferred en v2.4) est remplacee par un smoke test reproductible en CI.

Purpose: Lever les 3 derniers tickets pending de la dette PDF v2.4. La syntaxe @page est deja presente dans le code (cf. v2.4 SUMMARY 04.1) ; ce plan ne la touche PAS — il ajoute une couche de verification post-rendu sur le binaire dompdf.

Output: 1 dependance dev (smalot/pdfparser), 1 fixture builder (LongPvFixtureBuilder), 1 nouveau test class avec 3+ tests smoke qui parse le PDF binaire produit par MeetingReportsService::buildPdfBytes et assert les 3 contrats SC1/SC2/SC3.
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/gestion-votes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@/home/user/gestion-votes/CLAUDE.md

# v2.4 Phase 04.1 — predecessor that installed @page rules (read for design rationale only)
@.planning/milestones/v2.5-phases/04-print-tech-debt/04.1-SUMMARY.md
@.planning/milestones/v2.5-phases/04-print-tech-debt/04.1-VALIDATION.md

# Sources to MODIFY / extend
@app/Services/MeetingReportsService.php
@tests/Unit/MeetingReportsServiceTest.php
@tests/Unit/ProcurationPdfServiceTest.php
@composer.json

<interfaces>
<!-- Contracts the executor needs. Extracted from app/Services/MeetingReportsService.php -->
<!-- These signatures are STABLE since v1.5 P5 + v2.4 P4. Do NOT modify them. -->

From app/Services/MeetingReportsService.php:
```php
final class MeetingReportsService {
    public function __construct(
        ?MeetingRepository $meetingRepo = null,
        ?MotionRepository $motionRepo = null,
        ?AttendanceRepository $attendanceRepo = null,
        ?BallotRepository $ballotRepo = null,
        ?PolicyRepository $policyRepo = null,
        ?ProxyRepository $proxyRepo = null,
        ?InvitationRepository $invitationRepo = null,
        ?MeetingReportRepository $meetingReportRepo = null,
        ?SettingsRepository $settingsRepo = null,
    ) { /* DI nullable for testing — pattern v1.5 */ }

    /** @return array{pdf: string, hash: string, filename: string, html: string, meeting: array} */
    public function buildPdfBytes(
        string $meetingId,
        string $tenantId,
        bool $isPreview,
        bool $isInline,
        array $meeting,
    ): array;

    public function buildPdfHtml(
        array $meeting,
        array $attendances,
        array $motions,
        array $proxies,
        string $orgName,
        bool $isPreview,
    ): string;
}
```

From tests/Unit/MeetingReportsServiceTest.php (existing v2.4 patterns to REUSE):
```php
// Helper signatures already proven in v2.4:
private function buildMeeting(array $overrides = []): array;     // returns full meeting row
private function buildMotion(array $overrides = []): array;      // returns motion row
private function buildAttendance(array $overrides = []): array;  // returns attendance row

// Existing PDF binary test (DO NOT REGRESS):
public function testBuildPdfBytesReturnsPdfMagicHeader(): void;
// Existing HTML @page test (DO NOT REGRESS):
public function testBuildPdfHtmlContainsAtPageWithHeaderAndFooter(): void;
```

Smalot\PdfParser API (after composer require --dev smalot/pdfparser):
```php
use Smalot\PdfParser\Parser;
use Smalot\PdfParser\Document;
use Smalot\PdfParser\Page;

$parser = new Parser();
$document = $parser->parseContent($pdfBytes);   // returns Document
$pages = $document->getPages();                  // returns Page[]
$pageText = $pages[0]->getText();                // returns string (UTF-8)
$fullText = $document->getText();                // entire document text concatenated
```
</interfaces>

# v2.6 milestone closure note (from REQUIREMENTS.md)
# Strict closure: no opportunistic additions. The @page CSS rules are already present.
# This plan ONLY adds a verification harness — it must NOT touch the production CSS / @page strings.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter smalot/pdfparser en dev + creer LongPvFixtureBuilder</name>
  <files>composer.json, composer.lock, tests/Unit/Fixtures/LongPvFixtureBuilder.php</files>
  <action>
Etape 1 — composer dev dep :
Lancer `composer require --dev smalot/pdfparser:^2` dans le repo. Si l'installeur exige `--ignore-platform-req=...` (PHP 8.4 strict + dep range), lever en ajoutant dans la section `config.platform` existante (`"php": "8.4.0"` deja present) et passer `--with-all-dependencies` si conflit. Si le package refuse d'installer cleanly en 2.x, fallback vers `^0.18` ou `^0.19` (dernieres versions sans contrainte stricte) — documenter la version retenue dans le commit message. Verifier avec `composer show smalot/pdfparser` que l'install reussit, et que `composer.lock` est mis a jour.

Etape 2 — fixture builder :
Creer `tests/Unit/Fixtures/LongPvFixtureBuilder.php` (namespace `Tests\Unit\Fixtures` — vérifier que cela matche autoload-dev `AgVote\\Tests\\` ; si l'autoload pointe sur `Tests\\Unit\\` directement, adapter le namespace). Ce builder doit exposer 4 methodes statiques :

```php
final class LongPvFixtureBuilder
{
    public const MEETING_ID = '12345678-0000-0000-0000-0000000000aa';
    public const TENANT_ID = 'tenant-long-pv';

    /** Meeting suffisamment "riche" pour produire >=10 pages PDF. */
    public static function buildMeeting(): array;

    /** N=25 motions chacune avec description longue (~600 chars de prose francaise + accents + em-dash) — assez pour declencher >=10 pages. */
    public static function buildMotions(int $count = 25): array;

    /** M=60 attendances avec full_name varie (Pierre, Heloise, Theo, Francois-Xavier, etc.) pour gonfler la table de presence. */
    public static function buildAttendances(int $count = 60): array;

    /** Aucune procuration (le scope SC1-3 ne les requiert pas) — retourner []. */
    public static function buildProxies(): array;
}
```

Le but du builder : produire un dataset realiste qui, une fois passe a `MeetingReportsService::buildPdfHtml(...)` puis a dompdf, produit >=10 pages PDF. Heuristique : 1 motion avec description ~600 chars + tableau resultats = ~1/3 page A4 ; 25 motions => ~9 pages + table de presence (~1.5 page) = ~10-11 pages au total. La description doit contenir un panel d'accents francais (`é à è ê ô ç ù ï`), au moins un em-dash UTF-8 (`—`, U+2014, octets `\xE2\x80\x94`), et une apostrophe typographique (`’`) pour exercer l'encoding.

Exemple de description type a injecter dans toutes les motions (varier le numero, garder le panel d'accents) :
```
"Cette résolution propose l'adoption — à l'unanimité du conseil — du règlement intérieur révisé pour l'exercice 2026. Les modifications portent notamment sur les modalités de convocation, les règles de quorum aménagé pour les séances exceptionnelles, ainsi que la procédure de procuration électronique. Le président rappelle qu'aucune objection formelle n'a été déposée pendant la phase consultative. L'approbation requiert la majorité absolue des présents et représentés."
```

Le builder DOIT reutiliser le shape de `MeetingReportsServiceTest::buildMeeting/buildMotion/buildAttendance` (lire ce fichier pour copier les cles exactes — `id`, `title`, `description`, `secret`, `decision`, `official_for`/`against`/`abstain`/`total`, `decision_reason`, etc.). Sinon `MeetingReportsService::buildPdfHtml` echouera ou produira un HTML degenere.
  </action>
  <verify>
    <automated>composer show smalot/pdfparser 2>&amp;1 | grep -E "^name" &amp;&amp; php -l tests/Unit/Fixtures/LongPvFixtureBuilder.php</automated>
  </verify>
  <done>composer.lock contient smalot/pdfparser. `php -l` valide la syntaxe du builder. La classe est instanciable et `LongPvFixtureBuilder::buildMotions(25)` retourne un array de 25 entrees avec les memes cles que `buildMotion` du test existant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Smoke test PHPUnit qui parse le PDF binaire et assert SC1+SC2+SC3</name>
  <files>tests/Unit/MeetingReportsLongPdfTest.php</files>
  <behavior>
- Test 1 (SC1, PDF-V26-01) `testHeaderRepeatedOnEveryPageOfLongPv` : rend un PDF >=10 pages via le fixture builder, parse avec Smalot, asserte que `count($document->getPages()) >= 10` ET que sur **chaque** page individuelle, `$page->getText()` contient le segment header `'AG Cloture v2.6 — 15/06/2026'` (titre + em-dash + date du fixture). Pour les en-tetes invisibles a `getText()` car emis par dompdf en CSS @top-center et stockes hors flux principal, fallback : asserter que `substr_count($document->getText(), 'AG Cloture v2.6 — 15/06/2026') >= count($pages)` (occurrences globales >= nombre de pages, ce qui prouve la repetition). Si Smalot ne capte pas du tout les running headers @top-center, asserter sur la presence ET que le PDF binaire brut contient le titre code via les operateurs PDF text-show (Tj/TJ) — utiliser `preg_match_all('/AG Cloture v2\.6/', $pdfBytes)` comme garantie au moins egale au nombre de pages. Documenter dans un commentaire du test pourquoi le fallback existe.

- Test 2 (SC2, PDF-V26-02) `testEmDashAndFrenchAccentsRenderedCorrectly` : rend un PDF court (3-4 pages suffisent — un seul motion avec la description riche en accents). Parse via Smalot, extrait `$document->getText()`, asserte la presence de chaque element du panel : `'—'` (em-dash U+2014), `'é'`, `'à'`, `'è'`, `'ê'`, `'ô'`, `'ç'`, `'ù'`. Asserte aussi l'absence de `'?'` aux positions ou un accent etait attendu (chercher la sous-chaine `'r?solution'` ou `'?lection'` et asserter `assertStringNotContainsString` — pattern de mojibake classique). Si le test echoue parce que Smalot ne sait pas decoder les glyph IDs DejaVu, fallback : asserter que les bytes UTF-8 `\xE2\x80\x94` (em-dash) et `\xC3\xA9` (e accent aigu) apparaissent dans **le HTML emis** (`buildPdfHtml`) — ce qui prouve l'encodage source correct ; le rendu binaire DejaVu Sans est garanti par le test existant `testBuildPdfBytesReturnsPdfMagicHeader` (dompdf ne crashe pas).

- Test 3 (SC3, PDF-V26-03) `testFooterPageXSurYOnEveryPage` : rend le PDF >=10 pages, parse, asserte que pour chaque numero de page X dans [1..N], la chaine `'Page ' . X . ' sur ' . N` apparait dans `$document->getText()` — soit via comptage `substr_count($fullText, 'Page ' . $X . ' sur ' . $N) >= 1`. Si le pattern n'apparait pas a cause du running footer hors-flux, fallback identique a Test 1 (regex sur le binaire ou occurrences globales >= N).

- Test 4 (regression SC4) `testShortPvStillRendersInPriorPageBudget` : assemble le fixture mais avec `buildMotions(2)` (2 motions seulement) et asserte que `count($pages) <= 3`. Verifie qu'on ne casse pas le cas court (deja valide v2.4).
  </behavior>
  <action>
Creer `tests/Unit/MeetingReportsLongPdfTest.php`. Namespace `Tests\Unit` (matcher l'existant). Etendre `PHPUnit\Framework\TestCase`. Suivre **exactement** le pattern de mocks de `MeetingReportsServiceTest::setUp()` — toutes les 9 dependances sont mockees, aucune base de donnees n'est touchee.

Squelette obligatoire :

```php
<?php
declare(strict_types=1);

namespace Tests\Unit;

use AgVote\Repository\AttendanceRepository;
// ... (toutes les 9 repos comme dans MeetingReportsServiceTest)
use AgVote\Service\MeetingReportsService;
use PHPUnit\Framework\MockObject\MockObject;
use PHPUnit\Framework\TestCase;
use Smalot\PdfParser\Parser;
use Tests\Unit\Fixtures\LongPvFixtureBuilder;

class MeetingReportsLongPdfTest extends TestCase
{
    // 9 mocks (copier setUp depuis MeetingReportsServiceTest)
    // Helper buildService() identique
    // Helper renderPdfBytes(int $motionCount = 25): string — utilise LongPvFixtureBuilder

    public function testHeaderRepeatedOnEveryPageOfLongPv(): void { /* ... */ }
    public function testEmDashAndFrenchAccentsRenderedCorrectly(): void { /* ... */ }
    public function testFooterPageXSurYOnEveryPage(): void { /* ... */ }
    public function testShortPvStillRendersInPriorPageBudget(): void { /* ... */ }
}
```

Dans `renderPdfBytes(int $motionCount)` :
1. Configurer les mocks pour retourner les fixtures de `LongPvFixtureBuilder`.
2. `meetingRepo->method('findByIdForTenant')` -> `LongPvFixtureBuilder::buildMeeting()`.
3. `motionRepo->method('listForReport')` -> `LongPvFixtureBuilder::buildMotions($motionCount)`.
4. `attendanceRepo->method('listForReport')` -> `LongPvFixtureBuilder::buildAttendances(60)`.
5. `proxyRepo->method('listForReport')` -> `[]`.
6. `settingsRepo->method('get')` -> `'Association v2.6'`.
7. `meetingReportRepo->method('upsertFull')` -> `willReturnSelf()`.
8. Appeler `$service->buildPdfBytes(MEETING_ID, TENANT_ID, false, false, $meeting)` et retourner `$result['pdf']`.

Note importante sur le parsing :
- Le PDF binaire dompdf utilise des polices embedded (DejaVu Sans). Smalot 2.x sait les decoder via le ToUnicode CMap. Tester que `parseContent($bytes)->getText()` retourne du texte non vide et lisible avec le titre de la fixture dedans.
- Si Smalot 0.18-0.19 (fallback de Task 1), le decoding peut etre moins fiable — dans ce cas, les fallbacks regex-sur-binaire sont acceptables (le but est de prouver que dompdf ECRIT bien le contenu, pas de re-tester le rendu type).

Constraintes CLAUDE.md :
- Test cible (`vendor/bin/phpunit tests/Unit/MeetingReportsLongPdfTest.php --no-coverage`).
- Timeout 60s : `timeout 60 php vendor/bin/phpunit tests/Unit/MeetingReportsLongPdfTest.php --no-coverage`.
- Si echec 2x consecutifs sur la meme assertion, STOP et demander — le smoke test doit etre robuste, pas flaky.
- Max 3 executions par tache — bien preparer le test avant la 1ere execution.
- Tout texte visible francais (pas un probleme ici, c'est du test ; mais les fixtures francaises restent francaises).

Aucune modification de production code dans cette tache. Si un test echoue, **diagnostiquer dans le test ou le fixture, pas dans MeetingReportsService**.
  </action>
  <verify>
    <automated>timeout 60 php vendor/bin/phpunit tests/Unit/MeetingReportsLongPdfTest.php --no-coverage</automated>
  </verify>
  <done>4 tests verts, 0 erreurs. Le test `testShortPvStillRendersInPriorPageBudget` confirme la non-regression. Les 3 SC PDF-V26-01/02/03 sont assertes mecaniquement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verifier non-regression sur la suite PDF existante + mise a jour SUMMARY trail</name>
  <files>tests/Unit/MeetingReportsServiceTest.php (read-only verification), tests/Unit/ProcurationPdfServiceTest.php (read-only verification), app/Services/MeetingReportsService.php (lecture pour confirmer absence de regression)</files>
  <behavior>
- Test (existing) `MeetingReportsServiceTest::testBuildPdfBytesReturnsPdfMagicHeader` doit rester vert — garantit que dompdf accepte toujours la syntaxe @page.
- Test (existing) `MeetingReportsServiceTest::testBuildPdfHtmlContainsAtPageWithHeaderAndFooter` doit rester vert — garantit que les @page rules sont toujours emises.
- Test (existing) `ProcurationPdfServiceTest::testRenderHtmlContainsTopCenterHeader` + `testRenderHtmlContainsFooterPageCounter` + `testRenderHtmlPreservesPageBreakInsideRules` — tous restent verts.
  </behavior>
  <action>
Lancer cibles separees pour valider non-regression :

```bash
timeout 60 php vendor/bin/phpunit tests/Unit/MeetingReportsServiceTest.php --no-coverage
timeout 60 php vendor/bin/phpunit tests/Unit/ProcurationPdfServiceTest.php --no-coverage
timeout 60 php vendor/bin/phpunit tests/Unit/MeetingReportsLongPdfTest.php --no-coverage
```

Tous les 3 doivent passer sans erreur. Les counts attendus :
- `MeetingReportsServiceTest` : 6 tests minimum (le compte v2.4 SUMMARY etait 6/6) — verifier qu'aucune assertion existante n'est cassee accidentellement.
- `ProcurationPdfServiceTest` : 12 tests minimum (compte v2.4 SUMMARY).
- `MeetingReportsLongPdfTest` : 4 tests verts (nouveau).

Si une regression apparait dans les tests existants : c'est un probleme dans Task 1 ou Task 2 (fixture leak, mock partage, etc.). NE PAS modifier `MeetingReportsService.php` — la production code reste intacte. Diagnostiquer dans la fixture ou le test.

Si tous les tests passent, ajouter une note de coordination dans la prochaine SUMMARY (creee par execute-plan) qui mentionne :
- v2.4 P4 04.1 a installe les @page rules (sandbox-deferred verification visuelle)
- v2.6 P5 ferme la boucle avec smoke test parsant le PDF binaire
- PDF-V26-01/02/03 -> Done

Si la version smalot/pdfparser retenue ne permet pas le decoding ToUnicode (e.g., fallback 0.18) et que les tests Task 2 utilisent les regex-sur-binaire fallbacks, documenter cette deviation explicitement dans le commit message du Task 3 et dans la SUMMARY de phase.
  </action>
  <verify>
    <automated>timeout 60 php vendor/bin/phpunit tests/Unit/MeetingReportsServiceTest.php tests/Unit/ProcurationPdfServiceTest.php tests/Unit/MeetingReportsLongPdfTest.php --no-coverage</automated>
  </verify>
  <done>3 fichiers de test verts. Decompte total >= 22 tests. Aucune erreur, aucun warning PHPUnit. La phase 5 v2.6 peut etre marquee Done dans REQUIREMENTS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture -> dompdf | Donnees synthetiques injectees dans buildPdfHtml ; aucun input utilisateur reel |
| dompdf binary -> Smalot parser | PDF binaire genere localement, parse en memoire ; pas de fichier sur disque |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | LongPvFixtureBuilder injection prose francaise | accept | Texte hard-codé dans le builder, jamais lu d'une source externe — pas de surface d'attaque |
| T-05-02 | Information Disclosure | dompdf options (heritage F16 v2.1) | mitigate | Reutiliser les options secured deja appliquees par buildPdfBytes (`isRemoteEnabled=false`, `isPhpEnabled=false` implicite via service config) — ne pas reconfigurer dompdf dans les tests |
| T-05-03 | Denial of Service | smalot parsing PDF >=10 pages | accept | Test unitaire en sandbox CI ; PDF synthetique connu ; pas de borne adversariale. Timeout 60s applique au PHPUnit (CLAUDE.md) protege la suite |
| T-05-04 | Repudiation | aucune | n/a | Tests unitaires deterministes, pas d'audit trail concerne |

Note : Phase 5 ne touche aucun controller, aucune route, aucune logique d'auth. Le surface d'attaque est nul cote production. Les threats listees sont uniquement des risques operationnels du test harness lui-meme.
</threat_model>

<verification>
1. `composer show smalot/pdfparser` retourne une version installee.
2. `php -l tests/Unit/Fixtures/LongPvFixtureBuilder.php` clean.
3. `php -l tests/Unit/MeetingReportsLongPdfTest.php` clean.
4. `timeout 60 php vendor/bin/phpunit tests/Unit/MeetingReportsLongPdfTest.php --no-coverage` -> 4 tests verts.
5. `timeout 60 php vendor/bin/phpunit tests/Unit/MeetingReportsServiceTest.php --no-coverage` -> tous tests existants verts (>=6).
6. `timeout 60 php vendor/bin/phpunit tests/Unit/ProcurationPdfServiceTest.php --no-coverage` -> tous tests existants verts (>=12).
7. Aucune modification de `app/Services/MeetingReportsService.php` ni de `app/Services/ProcurationPdfService.php` — ces fichiers restent identiques au commit v2.4 SUMMARY. Verifier avec `git diff app/Services/`.
</verification>

<success_criteria>
- [x] PDF-V26-01 — header repete sur chaque page d'un PV >=10 pages, asserte par smoke test
- [x] PDF-V26-02 — em-dash + accents francais correctement encodes, asserte sur le PDF binaire (ou HTML emis en fallback)
- [x] PDF-V26-03 — footer `Page X sur Y` paginé sur chaque page, asserte par smoke test
- [x] SC4 — pas de regression PV courts (test dedie + suite v2.4 verte)
- [x] composer.lock mis a jour avec smalot/pdfparser
- [x] 0 modification de la production code dompdf (les CSS @page rules v2.4 restent intactes)
- [x] Conformite CLAUDE.md : tests cibles, timeout 60, syntax check PHP avant commit
</success_criteria>

<output>
After completion, create `.planning/phases/05-print-pdf-polish/05-01-SUMMARY.md` documenting :
- Version smalot/pdfparser retenue (et raison si fallback)
- Comptage exact pages PDF genere par la fixture (verifie execution-side)
- Strategie de decoding utilisee (Smalot getText vs regex-sur-binaire fallback)
- Updates a faire dans REQUIREMENTS.md : PDF-V26-01/02/03 -> [x] Phase 5
- Updates a faire dans ROADMAP.md : Phase 5 status -> Shipped, total v2.6 5/5 phases done
</output>
