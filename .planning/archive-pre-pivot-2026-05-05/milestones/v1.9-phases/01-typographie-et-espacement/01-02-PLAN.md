---
phase: 01-typographie-et-espacement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
  - public/admin.htmx.html
  - public/archives.htmx.html
  - public/audit.htmx.html
  - public/dashboard.htmx.html
  - public/email-templates.htmx.html
  - public/help.htmx.html
  - public/hub.htmx.html
  - public/meetings.htmx.html
  - public/members.htmx.html
  - public/postsession.htmx.html
  - public/settings.htmx.html
  - public/users.htmx.html
  - public/wizard.htmx.html
autonomous: true
requirements:
  - TYPO-03

must_haves:
  truths:
    - "Le header fait 64px sur toutes les pages"
    - "Le header contient uniquement breadcrumb + titre (plus de sous-titre ni barre decorative)"
    - "Le wizard conserve son sous-titre dynamique (pas supprime)"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Header height token and .app-header rule using token"
      contains: "--header-height: 64px"
    - path: "public/dashboard.htmx.html"
      provides: "Representative page with clean header"
  key_links:
    - from: "public/assets/css/design-system.css :root"
      to: ".app-header height"
      via: "var(--header-height)"
      pattern: "height:\\s*var\\(--header-height\\)"
    - from: "public/assets/css/design-system.css"
      to: "public/*.htmx.html headers"
      via: "CSS cascade — no page-sub elements to render"
      pattern: "page-sub"
---

<objective>
Update header height to 64px and clean up HTML templates by removing subtitle text and decorative bars from page headers.

Purpose: The header becomes a clean 64px bar with breadcrumb + title only. Removing visual clutter (subtitles, decorative bars) makes the header consistent and leaves room for breadcrumb navigation.
Output: Updated --header-height token, fixed .app-header rule, cleaned 12+ HTML templates.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-typographie-et-espacement/01-CONTEXT.md
@.planning/phases/01-typographie-et-espacement/01-RESEARCH.md
@.planning/phases/01-typographie-et-espacement/01-UI-SPEC.md

<interfaces>
<!-- Current state of header-related code in design-system.css -->

Line 486: --header-height: 56px;
Line 909-919: .app-header { height: 56px; ... }  (hardcoded, does NOT use var(--header-height))
Line 3342: --header-height: 56px;  (mobile media query @media max-width: 768px)

<!-- HTML files with page-sub (to remove) -->
admin.htmx.html:44        <p class="page-sub">...</p>
archives.htmx.html:45     <p class="page-sub">...</p>
audit.htmx.html:57        <p class="page-sub">...</p>
dashboard.htmx.html:42    <p class="page-sub">...</p>
email-templates.htmx.html:47  <p class="page-sub">...</p>
help.htmx.html:44         <p class="page-sub">...</p>
meetings.htmx.html:45     <p class="page-sub">...</p>
members.htmx.html:44      <p class="page-sub">...</p>
postsession.htmx.html:58  <p class="page-sub" id="meetingTitle">...</p>
settings.htmx.html:44     <p class="page-sub">...</p>
users.htmx.html:46        <p class="page-sub">...</p>
wizard.htmx.html:50       <p class="page-sub wiz-step-subtitle" id="wizStepSubtitle">...</p>  (KEEP — dynamic JS)

<!-- HTML files with decorative <span class="bar"> in page-title (to remove) -->
admin.htmx.html:40, archives.htmx.html:41, audit.htmx.html:53, dashboard.htmx.html:38,
email-templates.htmx.html:43, help.htmx.html:40, hub.htmx.html:44, meetings.htmx.html:41,
members.htmx.html:40, postsession.htmx.html:54, settings.htmx.html:40, users.htmx.html:42,
wizard.htmx.html:46

NOTE: public.htmx.html has <div class="bar"> for vote progress bars — these are NOT decorative and MUST NOT be removed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update header height token and fix .app-header hardcoded value</name>
  <files>public/assets/css/design-system.css</files>
  <read_first>
    - public/assets/css/design-system.css (line 486 for --header-height token, lines 908-920 for .app-header rule, lines 3339-3343 for mobile media query)
  </read_first>
  <action>
In `public/assets/css/design-system.css`, make these exact changes:

1. **Line 486** — Change `--header-height` from `56px` to `64px`:
   ```css
   --header-height: 64px;
   ```

2. **Line 910** — Fix `.app-header` to use the token instead of hardcoded value:
   ```css
   height: var(--header-height);
   ```
   Change from `height: 56px;` to `height: var(--header-height);`

3. **Line 3342** — Update mobile media query from `56px` to `64px`:
   ```css
   --header-height: 64px;
   ```
  </action>
  <verify>
    <automated>grep -n "header-height" public/assets/css/design-system.css && echo "---" && grep -n "height:" public/assets/css/design-system.css | grep -i "app-header" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Line 486 contains `--header-height: 64px;`
    - Line 910 contains `height: var(--header-height);` (not `height: 56px`)
    - Line 3342 contains `--header-height: 64px;`
    - No remaining `56px` references for header height in the file (grep `header.*56px` returns 0 matches)
  </acceptance_criteria>
  <done>Header height token is 64px, .app-header uses var(--header-height), mobile override is 64px.</done>
</task>

<task type="auto">
  <name>Task 2: Remove page-sub and decorative bar from HTML templates</name>
  <files>
    public/admin.htmx.html
    public/archives.htmx.html
    public/audit.htmx.html
    public/dashboard.htmx.html
    public/email-templates.htmx.html
    public/help.htmx.html
    public/hub.htmx.html
    public/meetings.htmx.html
    public/members.htmx.html
    public/postsession.htmx.html
    public/settings.htmx.html
    public/users.htmx.html
    public/wizard.htmx.html
  </files>
  <read_first>
    - public/dashboard.htmx.html (lines 35-45 for representative header structure)
    - public/wizard.htmx.html (lines 44-52 for wizard-specific page-sub that must stay)
    - public/postsession.htmx.html (lines 52-60 for postsession page-sub with id="meetingTitle")
    - public/hub.htmx.html (lines 40-50 for hub header)
  </read_first>
  <action>
For each of the following 13 HTML files, make two removals in the header section:

**A. Remove `<span class="bar"></span>`** from inside `<h1 class="page-title">`:
Delete the entire `<span class="bar"></span>` line. Files: admin, archives, audit, dashboard, email-templates, help, hub, meetings, members, postsession, settings, users, wizard.

**B. Remove `<p class="page-sub">...</p>`** lines from under the page-title:
Delete the entire `<p class="page-sub">` line. Files: admin, archives, audit, dashboard, email-templates, help, meetings, members, postsession, settings, users.

**EXCEPTIONS — DO NOT remove:**
- `wizard.htmx.html`: KEEP `<p class="page-sub wiz-step-subtitle" id="wizStepSubtitle">` — this is dynamically updated by JavaScript. Only remove the `<span class="bar">` in the wizard.
- `public.htmx.html`: Do NOT touch this file at all — its `.bar` elements are vote progress bars, not decorative.
- `hub.htmx.html`: Verify if it has a `page-sub` — if not, only remove `<span class="bar">`.

**Postsession special case:** The `<p class="page-sub" id="meetingTitle">` has an id used by JS. Remove it — the meeting title is displayed elsewhere in the postsession flow. If JS references `meetingTitle`, leave a hidden element: `<span id="meetingTitle" hidden></span>` on the next line after the h1 closing tag.

After removals, each page header should look like:
```html
<h1 class="page-title">
  <svg ...>...</svg>
  Page Title Text
</h1>
```
  </action>
  <verify>
    <automated>echo "page-sub count:" && grep -r "page-sub" public/*.htmx.html | grep -v "wiz-step-subtitle" | wc -l && echo "decorative bar count:" && grep -r '<span class="bar"></span>' public/*.htmx.html | wc -l && echo "wizard subtitle preserved:" && grep "wiz-step-subtitle" public/wizard.htmx.html | wc -l && echo "public.html bars preserved:" && grep 'class="bar"' public/public.htmx.html | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "page-sub" public/*.htmx.html | grep -v "wiz-step-subtitle" | wc -l` returns 0
    - `grep -r '<span class="bar"></span>' public/*.htmx.html | wc -l` returns 0
    - `grep "wiz-step-subtitle" public/wizard.htmx.html` returns 1 match (wizard subtitle preserved)
    - `grep 'class="bar"' public/public.htmx.html | wc -l` returns 3 (vote progress bars preserved)
    - No HTML syntax errors in modified files
  </acceptance_criteria>
  <done>All page-sub elements removed (except wizard dynamic subtitle). All decorative bar spans removed from page-title headers. Public.htmx.html vote bars untouched. Headers now show breadcrumb + title only.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "header-height: 64px" public/assets/css/design-system.css | wc -l` returns 2 (desktop + mobile)
2. `grep "height: var(--header-height)" public/assets/css/design-system.css` returns at least 1 match
3. `grep -r "page-sub" public/*.htmx.html | grep -v wiz-step-subtitle | wc -l` returns 0
4. `grep -r '<span class="bar"></span>' public/*.htmx.html | wc -l` returns 0
5. `npx playwright test specs/critical-path-dashboard.spec.js --project=chromium` — pages still load
</verification>

<success_criteria>
- --header-height is 64px on desktop and mobile
- .app-header uses var(--header-height) not hardcoded value
- No page-sub elements remain in HTML (except wizard wiz-step-subtitle)
- No decorative <span class="bar"> remains in page headers
- public.htmx.html vote progress bars untouched
- All existing E2E tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-typographie-et-espacement/01-02-SUMMARY.md`
</output>
