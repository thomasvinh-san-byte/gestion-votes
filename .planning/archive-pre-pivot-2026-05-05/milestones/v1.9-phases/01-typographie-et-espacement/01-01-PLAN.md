---
phase: 01-typographie-et-espacement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
autonomous: true
requirements:
  - TYPO-01
  - TYPO-02
  - TYPO-04

must_haves:
  truths:
    - "Le texte courant sur toutes les pages s'affiche a 16px minimum"
    - "Les labels de formulaire s'affichent en casse normale avec couleur lisible"
    - "L'espacement entre champs de formulaire est de 20px"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Updated typography and spacing tokens"
      contains: "--text-base: 1rem"
  key_links:
    - from: "public/assets/css/design-system.css :root"
      to: "body font-size"
      via: "var(--text-base) cascade"
      pattern: "--text-base:\\s*1rem"
    - from: "public/assets/css/design-system.css :root"
      to: ".form-label font-size"
      via: "--type-label-size -> var(--text-base)"
      pattern: "--type-label-size:\\s*var\\(--text-base\\)"
    - from: "public/assets/css/design-system.css :root"
      to: ".form-group + .form-group margin-top"
      via: "--space-field -> var(--form-gap)"
      pattern: "--space-field:\\s*var\\(--form-gap\\)"
---

<objective>
Update CSS design tokens in design-system.css for typography base size (14px -> 16px), form label styling (remove uppercase/muted), and form spacing (16px -> 20px).

Purpose: Establish the foundational CSS tokens that propagate automatically to all pages via CSS cascade. This is the token layer that all visual changes depend on.
Output: Updated design-system.css with new token values, label rules, and spacing aliases.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-typographie-et-espacement/01-CONTEXT.md
@.planning/phases/01-typographie-et-espacement/01-RESEARCH.md
@.planning/phases/01-typographie-et-espacement/01-UI-SPEC.md

<interfaces>
<!-- Current token values in design-system.css that must be changed -->

Line 199: --text-base: 0.875rem;   /* 14px */
Line 295: --space-field: var(--space-4);   /* 16px */
Line 486: --header-height: 56px;
Line 600: --type-label-size: var(--text-sm);
Line 89:  font-size: var(--text-base);     /* 14px */
Line 90:  line-height: var(--leading-base); /* 1.571 */
Lines 1813-1819: .form-label { font-size: var(--text-sm); font-weight: var(--font-semibold); text-transform: uppercase; letter-spacing: .5px; color: var(--color-text-muted); }
Line 277-295: Semantic gap aliases block (where --form-gap and --section-gap will be added)
Line 3430: --text-base: 0.9375rem; /* 15px mobile override */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update typography tokens and body line-height</name>
  <files>public/assets/css/design-system.css</files>
  <read_first>
    - public/assets/css/design-system.css (lines 85-95 for body rule, lines 196-200 for --text-base, line 600 for --type-label-size, lines 3425-3435 for mobile override)
  </read_first>
  <action>
In `public/assets/css/design-system.css`, make these exact changes:

1. **Line 199** — Change `--text-base` from `0.875rem` to `1rem`:
   ```css
   --text-base: 1rem;       /* 16px — primary UI chrome size */
   ```

2. **Line 89-90** — Update body font-size comment and line-height:
   ```css
   font-size: var(--text-base);     /* 16px */
   line-height: var(--leading-md);  /* 1.5 — 16px * 1.5 = 24px */
   ```

3. **Line 600** — Change `--type-label-size` from `var(--text-sm)` to `var(--text-base)`:
   ```css
   --type-label-size:   var(--text-base);
   ```

4. **Line 3430** — Update mobile override from `0.9375rem` to `1rem`:
   ```css
   --text-base: 1rem;      /* 16px — consistent with desktop */
   ```

5. Add a comment after `--text-base` noting `--text-md` is now identical (intentional, defer fusion):
   After the `--text-md` line (around line 200), add nothing — the existing comment suffices. But do NOT change `--text-md`.
  </action>
  <verify>
    <automated>grep -n "text-base: 1rem" public/assets/css/design-system.css | head -5 && grep -n "leading-md" public/assets/css/design-system.css | grep "body" | head -3 && grep -n "type-label-size.*text-base" public/assets/css/design-system.css</automated>
  </verify>
  <acceptance_criteria>
    - Line 199 contains `--text-base: 1rem;`
    - Line ~89 body rule contains `line-height: var(--leading-md);`
    - Line 600 contains `--type-label-size:   var(--text-base);`
    - Line ~3430 mobile override contains `--text-base: 1rem;`
    - `--text-md` line is UNCHANGED (still `1rem`)
  </acceptance_criteria>
  <done>--text-base is 1rem (16px) globally, body line-height uses --leading-md (1.5), --type-label-size points to --text-base, mobile override updated</done>
</task>

<task type="auto">
  <name>Task 2: Fix form-label styling and add spacing aliases</name>
  <files>public/assets/css/design-system.css</files>
  <read_first>
    - public/assets/css/design-system.css (lines 1813-1819 for .form-label, lines 277-299 for semantic gap aliases, line 295 for --space-field)
  </read_first>
  <action>
In `public/assets/css/design-system.css`, make these exact changes:

1. **Lines 1813-1819** — Rewrite `.form-label` rule:
   ```css
   .form-label {
     font-size: var(--type-label-size);
     font-weight: var(--font-semibold);
     color: var(--color-text-dark);
   }
   ```
   Remove `text-transform: uppercase`, `letter-spacing: .5px`, and change `color` from `var(--color-text-muted)` to `var(--color-text-dark)`. Change `font-size` from `var(--text-sm)` to `var(--type-label-size)`.

2. **After line 295** (after `--space-field` in the "Semantic layout spacing aliases" block) — Add two new aliases:
   ```css
   --form-gap:    var(--space-5);   /* 20px — between form fields */
   --section-gap: var(--space-6);   /* 24px — between page sections */
   ```

3. **Line 295** — Update `--space-field` to use the new alias:
   ```css
   --space-field:   var(--form-gap);   /* 20px — between form fields */
   ```

These changes ensure:
- Form labels render in sentence case (no uppercase), at 16px, with readable dark color
- Form field gaps increase from 16px to 20px via the --form-gap alias
- A new --section-gap alias (24px) is available for section spacing
  </action>
  <verify>
    <automated>grep -A4 "^\.form-label {" public/assets/css/design-system.css && echo "---" && grep -n "form-gap\|section-gap\|space-field" public/assets/css/design-system.css</automated>
  </verify>
  <acceptance_criteria>
    - `.form-label` block does NOT contain `text-transform`
    - `.form-label` block does NOT contain `letter-spacing`
    - `.form-label` block contains `color: var(--color-text-dark)`
    - `.form-label` block contains `font-size: var(--type-label-size)`
    - `:root` block contains `--form-gap:    var(--space-5);`
    - `:root` block contains `--section-gap: var(--space-6);`
    - `--space-field` line contains `var(--form-gap)` (not `var(--space-4)`)
  </acceptance_criteria>
  <done>Form labels display in normal case with dark color at 16px. Form spacing token --space-field points to --form-gap (20px). New --section-gap alias (24px) available.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "text-base: 1rem" public/assets/css/design-system.css` returns 2 matches (desktop + mobile)
2. `grep "text-transform" public/assets/css/design-system.css | grep -c "form-label"` returns 0
3. `grep "form-gap.*space-5" public/assets/css/design-system.css` returns 1 match
4. `php -r "echo 'syntax ok';"` — no PHP changes
5. `npx playwright test specs/critical-path-dashboard.spec.js --project=chromium` — pages still load
</verification>

<success_criteria>
- --text-base is 1rem (16px) on both desktop and mobile
- body line-height is var(--leading-md) (1.5)
- --type-label-size points to var(--text-base)
- .form-label has no text-transform, no letter-spacing, color is --color-text-dark
- --form-gap and --section-gap aliases exist in :root
- --space-field points to var(--form-gap) (20px)
- All existing E2E tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-typographie-et-espacement/01-01-SUMMARY.md`
</output>
