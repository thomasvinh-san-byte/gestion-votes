---
phase: 05-validation-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [NAV-04]

must_haves:
  truths:
    - "La page d'accueil affiche une carte centree avec logo AG-VOTE et formulaire de connexion"
    - "Les tests unitaires PHP passent sans regression"
    - "La syntaxe PHP de tous les fichiers modifies est valide"
    - "Les pages cles sont visuellement coherentes apres les phases 1-4"
  artifacts:
    - path: "public/index.html"
      provides: "Login page with centered card layout"
      contains: "accueil-card"
    - path: "public/assets/css/landing.css"
      provides: "Centered card styling"
      contains: "accueil-card"
  key_links:
    - from: "public/index.html"
      to: "public/assets/css/landing.css"
      via: "CSS class accueil-card"
      pattern: "accueil-card"
---

<objective>
Verify NAV-04 conformance (login page centered card with AG-VOTE logo + login form), run PHP syntax checks and unit tests to confirm zero regressions from phases 1-4, and visually verify key pages.

Purpose: Validation gate — no new features, only confirmation that the milestone is complete and regression-free.
Output: Verified codebase ready for milestone tag.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@public/index.html
@public/assets/css/landing.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify NAV-04 + PHP syntax + unit tests</name>
  <files></files>
  <read_first>
    - public/index.html (login page structure)
    - public/assets/css/landing.css (card centering styles)
  </read_first>
  <action>
1. **Verify NAV-04 login page structure** by reading public/index.html and confirming:
   - A container div with class `accueil-card` exists (centered card)
   - Inside it, a div with class `accueil-logo` contains an SVG (logo)
   - An h1 with class `accueil-title` contains text "AG-VOTE"
   - A form with class `login-form` and id `loginForm` exists with email + password fields
   - Read public/assets/css/landing.css and confirm `accueil-card` has centering styles (margin auto or flexbox centering)

2. **Run PHP syntax check** on all PHP files under app/ and public/:
   ```
   find app/ public/ -name '*.php' -exec php -l {} \; 2>&1 | grep -v "No syntax errors" | head -20
   ```
   All files must report no syntax errors.

3. **Run PHP unit tests** (targeted, not full suite):
   ```
   timeout 120 php vendor/bin/phpunit tests/Unit/ --no-coverage 2>&1 | tail -30
   ```
   All tests must pass. If failures, report them — do NOT fix (this is verification only).

4. Report results: for each check, state PASS or FAIL with details.
  </action>
  <verify>
    <automated>php -l public/index.php && grep -q "accueil-card" public/index.html && grep -q "accueil-logo" public/index.html && grep -q "AG-VOTE" public/index.html && grep -q "loginForm" public/index.html && echo "NAV-04 VERIFIED"</automated>
  </verify>
  <acceptance_criteria>
    - grep "accueil-card" public/index.html returns matches
    - grep "accueil-logo" public/index.html returns matches
    - grep "AG-VOTE" public/index.html returns matches
    - grep "loginForm" public/index.html returns matches
    - grep "accueil-card" public/assets/css/landing.css returns matches
    - PHP syntax check reports zero errors
    - PHPUnit reports zero failures
  </acceptance_criteria>
  <done>NAV-04 verified (centered card + logo + form), PHP syntax clean, unit tests green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual coherence verification</name>
  <files></files>
  <read_first>
    - public/index.html
  </read_first>
  <action>
Present the verification checklist to the user. All automated checks from Task 1 are complete. This task requires human visual inspection of the running application to confirm visual coherence across key pages after phases 1-4 modifications.
  </action>
  <what-built>Phases 1-4 applied typography tokens (16px base), static 200px sidebar, empty states, loading indicators, jargon cleanup, tooltips, and export descriptions across all pages. This checkpoint verifies visual coherence.</what-built>
  <how-to-verify>
    1. Open the login page (/) — verify centered card with AG-VOTE logo and login form
    2. Log in as admin — verify dashboard loads with 200px sidebar, 16px text, 64px header
    3. Navigate to Meetings page — verify sidebar stays open, empty state message if no meetings
    4. Navigate to a meeting detail page — verify tooltips on technical terms (hover over admin labels)
    5. Check an export button — verify it has a description line below it
    6. Log in as voter — verify sidebar shows only Voter + Mon compte links
    7. Check vote confirmation — verify persistent confirmation with timestamp after voting
  </how-to-verify>
  <verify>
    <automated>echo "Human visual verification — no automated check"</automated>
  </verify>
  <acceptance_criteria>
    - Login page displays centered card with AG-VOTE logo
    - All pages use 16px base text, 64px header, 200px sidebar
    - No visual regressions observed on key flows
  </acceptance_criteria>
  <done>Human confirms all key pages are visually coherent with no regressions from phases 1-4</done>
  <resume-signal>Type "approved" if all pages look correct, or describe any visual issues found</resume-signal>
</task>

</tasks>

<verification>
- NAV-04: public/index.html contains accueil-card with accueil-logo (SVG) + accueil-title (AG-VOTE) + login-form
- PHP syntax: zero errors across app/ and public/ PHP files
- Unit tests: all pass without regression
- Visual: human confirms coherence across login, dashboard, meetings, vote pages
</verification>

<success_criteria>
1. NAV-04 requirement verified — login page has centered card with logo and form
2. All PHP files pass syntax check
3. All unit tests pass
4. Human approves visual coherence of key pages
</success_criteria>

<output>
After completion, create `.planning/phases/05-validation-gate/05-01-SUMMARY.md`
</output>
