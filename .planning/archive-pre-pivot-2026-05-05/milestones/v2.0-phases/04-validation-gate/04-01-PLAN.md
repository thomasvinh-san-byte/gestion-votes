---
phase: 04-validation-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-validation-gate/04-AUDIT.md
autonomous: true
requirements: [CHECK-01, CHECK-02, CHECK-03, CHECK-04, CHECK-05, FOCUS-01, FOCUS-02, FOCUS-03, ANIM-01, ANIM-02, ANIM-03]

must_haves:
  truths:
    - "Tous les fichiers JS modifies par v2.0 passent node --check"
    - "Aucun fichier PHP n'a ete modifie par v2.0 (pure frontend)"
    - "Aucune fonction JS appelee mais non definie dans operator-{exec,realtime,tabs}.js"
    - "Aucun ID DOM reference par JS mais absent de operator.htmx.html"
    - "Phase 1 patterns (refreshExecChecklist, opChecklistCollapsed) intacts"
    - "Phase 2 patterns (.op-focus-mode, refreshFocusQuorum, opFocusMode) intacts"
    - "Phase 3 patterns (animateVoteCounter, _activeVoteAnimReady, PREFERS_REDUCED_MOTION) intacts"
    - "Manual verification checklist consolide dans 04-AUDIT.md"
  artifacts:
    - path: ".planning/phases/04-validation-gate/04-AUDIT.md"
      provides: "Consolidated manual verification checklist + automated regression results"
      contains: "Phase 1 manual"
  key_links:
    - from: ".planning/phases/04-validation-gate/04-AUDIT.md"
      to: ".planning/phases/{01-checklist-operateur,02-mode-focus,03-animations-vote}/VERIFICATION.md"
      via: "Cross-reference per-phase verifications"
      pattern: "VERIFICATION.md"
---

<objective>
Verify the full v2.0 milestone is regression-free via static analysis. Consolidate manual verification items deferred from Phases 1-3 into a single audit document.

Purpose: This is the validation gate — no new code. Confirm the implementation as merged on main is internally consistent (no orphan references, no syntax errors, no PHP boundary violations) and document what humans/CI must still verify before release.
Output: `04-AUDIT.md` consolidating regression results + manual verification checklist.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-validation-gate/04-CONTEXT.md
@.planning/phases/04-validation-gate/04-VALIDATION.md
@.planning/phases/01-checklist-operateur/VERIFICATION.md
@.planning/phases/02-mode-focus/VERIFICATION.md
@.planning/phases/03-animations-vote/VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static regression audit (JS syntax + orphan references)</name>
  <files>.planning/phases/04-validation-gate/04-AUDIT.md (created in Task 3)</files>
  <read_first>
    - public/assets/js/pages/operator-exec.js
    - public/assets/js/pages/operator-realtime.js
    - public/assets/js/pages/operator-tabs.js
  </read_first>
  <action>
Run these checks and capture results for inclusion in 04-AUDIT.md (Task 3 will write the document):

1. JS syntax check: `node --check` on each of the 3 modified JS files. All must PASS.
2. Orphan function audit: for each `O.fn.X` reference in operator-tabs.js or operator-realtime.js, confirm the function is defined and registered in operator-exec.js. Specifically grep:
   - `O.fn.refreshExecChecklist` (Phase 1) — must be defined
   - `O.fn.updateChecklistSseRow` (Phase 1) — must be defined
   - `O.fn.refreshFocusQuorum` (Phase 2) — must be defined
3. Orphan DOM ID audit: for each `getElementById('opXYZ')` in JS, confirm the ID exists in `public/operator.htmx.html`. Critical IDs:
   - `opChecklistPanel`, `opChecklistRowSse`, `opChecklistRowQuorum`, `opChecklistRowVotes`, `opChecklistRowOnline`, `opChecklistSseValue`, `opChecklistQuorumValue`, `opChecklistVotesValue`, `opChecklistOnlineValue` (Phase 1)
   - `opChecklistSseBanner`, `opChecklistToggle` (Phase 1)
   - `opBtnFocusMode`, `opFocusQuorum`, `opFocusQuorumValue`, `opFocusQuorumStatus` (Phase 2)
   - `execVoteFor`, `execVoteAgainst`, `execVoteAbstain` (Phase 3 vote counter targets)
4. Phase invariants intact:
   - Phase 1: `function refreshExecChecklist` count == 1, `opChecklistCollapsed` count >= 2
   - Phase 2: `op-focus-mode` count >= 3 across files, `opFocusMode` count >= 3
   - Phase 3: `animateVoteCounter` count >= 2, `PREFERS_REDUCED_MOTION` count >= 2

Capture findings into a buffer (mental notes / a temp variable) for Task 3 to write into 04-AUDIT.md.
  </action>
  <verify>
    <automated>node --check public/assets/js/pages/operator-exec.js && node --check public/assets/js/pages/operator-realtime.js && node --check public/assets/js/pages/operator-tabs.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` passes on all 3 JS files
    - All listed `O.fn.X` references resolve to definitions
    - All listed DOM IDs exist in operator.htmx.html
    - Phase 1/2/3 invariant counts meet thresholds
  </acceptance_criteria>
  <done>Static regression audit complete; results captured for AUDIT.md.</done>
</task>

<task type="auto">
  <name>Task 2: PHP boundary check</name>
  <files>(read-only)</files>
  <read_first>(none — git operation)</read_first>
  <action>
Confirm v2.0 made zero PHP changes. Run:

```bash
git diff --name-only $(git log --grep='v2.0\|^docs(0[1-3]\|^feat(0[1-3]' --reverse --format=%H | head -1)~1..HEAD -- '*.php' | wc -l
```

Expected: 0. If non-zero, list the files and run `php -l` on each, capture results for AUDIT.md.

Alternative simple check: `git log --name-only --format= main..HEAD | grep '\.php$' | sort -u` should be empty.

If `php` binary not available in this dev container, document as "BLOCKED — `php` binary missing locally; PHP syntax check defers to CI". The result is still useful: zero PHP files changed = zero risk of PHP regression.
  </action>
  <verify>
    <automated>test "$(git log --since='2026-04-21' --name-only --pretty=format: -- '*.php' | grep -v '^$' | sort -u | wc -l)" -eq 0 && echo "PASS — zero PHP files changed in v2.0" || echo "FAIL — PHP files were modified, list above"</automated>
  </verify>
  <acceptance_criteria>
    - Zero `.php` files in v2.0 commit range, OR
    - All modified `.php` files pass `php -l`, OR
    - Documented as PHP-binary-blocked-locally with deferral to CI
  </acceptance_criteria>
  <done>PHP boundary check complete; result captured for AUDIT.md.</done>
</task>

<task type="auto">
  <name>Task 3: Write 04-AUDIT.md consolidating regression results + manual verification checklist</name>
  <files>.planning/phases/04-validation-gate/04-AUDIT.md</files>
  <read_first>
    - .planning/phases/01-checklist-operateur/VERIFICATION.md
    - .planning/phases/02-mode-focus/VERIFICATION.md
    - .planning/phases/03-animations-vote/VERIFICATION.md
    - .planning/phases/04-validation-gate/04-VALIDATION.md (manual verifications table)
  </read_first>
  <action>
Write `.planning/phases/04-validation-gate/04-AUDIT.md` with these sections:

1. **Header**: phase frontmatter, milestone, audit date
2. **Executive Summary**: PASS/PARTIAL verdict, brief justification
3. **Automated Regression Results** (from Task 1):
   - JS syntax: 3/3 PASS
   - Orphan function audit: results
   - Orphan DOM ID audit: results
   - Phase invariant counts: results
4. **PHP Boundary Check** (from Task 2): zero PHP changes confirmed
5. **Per-Phase Verification Recap**: link to each phase VERIFICATION.md, summarize verdicts
6. **Manual Verification Checklist (deferred)**: consolidated table from each phase's VALIDATION.md "Manual-Only Verifications"
7. **CI Verification Pending**: Playwright E2E suite (blocked locally by libatk)
8. **Sign-Off**: ready for milestone close once manual checklist + CI E2E pass
  </action>
  <verify>
    <automated>test -f .planning/phases/04-validation-gate/04-AUDIT.md && grep -q 'Manual Verification Checklist' .planning/phases/04-validation-gate/04-AUDIT.md && grep -q 'PHP Boundary' .planning/phases/04-validation-gate/04-AUDIT.md && grep -q 'Per-Phase Verification' .planning/phases/04-validation-gate/04-AUDIT.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-validation-gate/04-AUDIT.md` exists
    - Contains sections: Executive Summary, Automated Regression Results, PHP Boundary Check, Per-Phase Verification Recap, Manual Verification Checklist, CI Verification Pending, Sign-Off
    - Cross-references each of the 3 phase VERIFICATION.md files
    - Lists 8 manual verification items consolidated from VALIDATION.md
  </acceptance_criteria>
  <done>04-AUDIT.md written; ready for milestone-close gate.</done>
</task>

</tasks>

<verification>
1. `node --check` PASS on all 3 JS files
2. Zero `.php` files modified in v2.0
3. `04-AUDIT.md` exists with required sections
4. All cross-references resolve
5. Phase 1, 2, 3 invariants confirmed intact
</verification>

<success_criteria>
- All v2.0 requirements (CHECK-01..05, FOCUS-01..03, ANIM-01..03) have a verification path documented (automated + manual + CI)
- Zero regressions detected via static analysis
- Manual verification checklist ready for QA hand-off
- Milestone is ready for `/gsd:complete-milestone` once manual checklist passes
</success_criteria>

<output>
Create `.planning/phases/04-validation-gate/04-01-SUMMARY.md` after completion.
</output>
