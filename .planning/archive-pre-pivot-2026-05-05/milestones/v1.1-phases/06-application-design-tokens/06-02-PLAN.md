---
phase: 06-application-design-tokens
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - public/login.html
  - public/assets/css/login.css
autonomous: true
requirements: [DESIGN-02]
must_haves:
  truths:
    - "Login page shows a two-panel layout on desktop: branding left, form right"
    - "Below 768px viewport, the branding panel is hidden and the form panel takes full width"
    - "The existing 420px login card, brand mark, tagline, and form internals are preserved"
    - "The gradient orb animation stays visible inside the branding panel only (not escaping full-page)"
  artifacts:
    - path: "public/login.html"
      provides: "Restructured login HTML with brand + form panels"
      contains: "login-panel-brand"
    - path: "public/login.html"
      provides: "Form panel wrapper"
      contains: "login-panel-form"
    - path: "public/assets/css/login.css"
      provides: "Grid 2-column layout with 768px collapse"
      contains: "grid-template-columns: 1fr 1fr"
  key_links:
    - from: "public/login.html"
      to: "public/assets/css/login.css"
      via: "login-panel-brand / login-panel-form class hooks"
      pattern: "login-panel-(brand|form)"
---

<objective>
Restructure the login page into a professional 2-panel layout: branding (logo + AgVote + tagline + animated orb) on the left, form card on the right. Below 768px, the branding panel collapses and the form takes full width.

Purpose: Implements DESIGN-02. Addresses memory feedback_horizontal_design.md (use width aggressively) and project_login_redesign.md (2-panel split). This is the first visible page the user sees — the quality bar for the rest of the milestone.
Output: Updated HTML wrapper structure + updated login.css `.login-page` layout block (grid with responsive collapse), orb repositioned from fixed-viewport to absolute-inside-branding-panel.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-application-design-tokens/06-RESEARCH.md
@.planning/phases/06-application-design-tokens/06-UI-SPEC.md

@public/login.html
@public/assets/css/login.css

<interfaces>
<!-- Existing login.html structure (preserve internals): -->
<!-- <div class="login-orb" aria-hidden="true"></div> -->
<!-- <main class="login-page"> -->
<!--   <div class="login-card">...</div>     -->
<!--     <div class="login-brand">...</div>  <- logo + h1 + tagline -->
<!--     <form class="login-form">...</form> -->
<!--   <div class="login-trust">...</div> -->
<!--   <div id="demoPanel">...</div> -->
<!--   <div class="login-footer">...</div> -->
<!-- </main> -->

<!-- Target structure: -->
<!-- <main class="login-page">                      <- becomes display:grid 1fr 1fr -->
<!--   <div class="login-panel-brand">              <- NEW, left panel -->
<!--     <div class="login-orb" aria-hidden="true"></div>  <- MOVED inside panel -->
<!--     <div class="login-brand">...</div>         <- MOVED here (logo + h1 + tagline) -->
<!--   </div> -->
<!--   <div class="login-panel-form">               <- NEW, right panel -->
<!--     <div class="login-card">                   <- contains only the form now -->
<!--       <form class="login-form">...</form> -->
<!--     </div> -->
<!--     <div class="login-trust">...</div>          <- MOVED inside right panel -->
<!--     <div id="demoPanel">...</div>               <- MOVED inside right panel -->
<!--     <div class="login-footer">...</div>         <- MOVED inside right panel -->
<!--   </div> -->
<!-- </main> -->

<!-- Tokens required (already in design-system.css): -->
<!-- --color-surface-raised, --color-primary-subtle, --color-primary-glow, --space-8, --space-12 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure login.html into brand + form panels</name>
  <files>public/login.html</files>
  <read_first>
    - public/login.html (full file — map current DOM)
    - .planning/phases/06-application-design-tokens/06-RESEARCH.md (section: Login 2-Panel Structure — Required HTML restructuring block AND Pitfall 6: trust/footer placement)
    - .planning/phases/06-application-design-tokens/06-UI-SPEC.md (section: Login 2-Panel Layout)
  </read_first>
  <action>
    Modify the body of login.html so that inside `<main class="login-page">`:

    1. Create `<div class="login-panel-brand">` as the FIRST child of `.login-page`.
       - Move the existing `<div class="login-orb" aria-hidden="true"></div>` INSIDE this panel (currently it sits as a sibling before `.login-page` — relocate it inside `.login-panel-brand`).
       - Move the existing `<div class="login-brand">` (which contains `.login-brand-mark` logo, h1 with AgVote name, `.login-tagline`) from inside `.login-card` INTO `.login-panel-brand`.

    2. Create `<div class="login-panel-form">` as the SECOND child of `.login-page`.
       - Move the existing `<div class="login-card">` inside `.login-panel-form`. The card now contains ONLY the form (`<form class="login-form">...`) and any error/message placeholders that live inside `.login-card`.
       - Move `<div class="login-trust">...`, `<div id="demoPanel">...`, `<div class="login-footer">...` from being siblings of `.login-card` to being INSIDE `.login-panel-form` AFTER the `.login-card`.

    3. Preserve every id attribute, every form input name, every data-* attribute, every script tag, every aria attribute.

    4. Do NOT rename `.login-card`, `.login-brand`, `.login-form`, `.login-brand-mark`, `.login-tagline`, `.field-group`, `.field-input`, `.field-label`, `.field-eye`, `.login-trust`, `.login-footer`, `#demoPanel`, `.login-orb`. Only the new wrappers `.login-panel-brand` and `.login-panel-form` are added.

    5. Preserve the existing `<script>` references at the bottom of the file unchanged.

    Tagline text: keep as-is if it matches UI-SPEC ("Gestion des votes pour votre association"). If it mentions "copropriété" or "syndic", replace with "Gestion des votes pour votre association".
  </action>
  <verify>
    <automated>grep -c 'class="login-panel-brand"' public/login.html && grep -c 'class="login-panel-form"' public/login.html && ! grep -iE 'copropriete|copropriété|syndic' public/login.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class="login-panel-brand"' public/login.html` returns exactly `1`
    - `grep -c 'class="login-panel-form"' public/login.html` returns exactly `1`
    - `grep -c 'class="login-card"' public/login.html` returns exactly `1` (preserved, not removed)
    - `grep -c 'class="login-brand"' public/login.html` returns exactly `1` (preserved)
    - `grep -c 'class="login-orb"' public/login.html` returns exactly `1` (preserved, repositioned)
    - `grep -c 'id="demoPanel"' public/login.html` returns exactly `1` (preserved)
    - `grep -c 'class="login-trust"' public/login.html` returns exactly `1`
    - `grep -c 'class="login-footer"' public/login.html` returns exactly `1`
    - `grep -ciE 'copropriete|copropriété|syndic' public/login.html` returns `0`
    - `login-panel-brand` appears in the file BEFORE `login-panel-form` (left panel first)
  </acceptance_criteria>
  <done>login.html has two sibling panels inside `.login-page`, branding content on the left panel and form + trust + demo + footer on the right panel.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite .login-page layout + new panel styles in login.css</name>
  <files>public/assets/css/login.css</files>
  <read_first>
    - public/assets/css/login.css (full file — locate `.login-page`, `.login-orb`, existing media queries)
    - .planning/phases/06-application-design-tokens/06-RESEARCH.md (section: Login 2-Panel Structure — CSS changes block AND Pitfall 3: orb position fixed)
    - .planning/phases/06-application-design-tokens/06-UI-SPEC.md (section: Login 2-Panel Layout)
  </read_first>
  <action>
    Update login.css with the following CSS changes. Use EXISTING tokens — no raw hex, no raw oklch() literals, no new variables.

    1. Replace the existing `.login-page` rule body with a grid layout:
       ```
       .login-page {
         display: grid;
         grid-template-columns: 1fr 1fr;
         min-height: 100vh;
       }
       ```
       Remove any `flex`, `align-items`, `justify-content`, `padding` properties that were on `.login-page`. Preserve any `position: relative` if needed for orb.

    2. Add `.login-panel-brand` rule:
       ```
       .login-panel-brand {
         position: relative;
         overflow: hidden;
         display: flex;
         align-items: center;
         justify-content: center;
         padding: var(--space-12);
         background: radial-gradient(circle at 30% 40%, var(--color-primary-subtle), var(--color-primary-glow) 60%, var(--color-bg) 100%);
       }
       ```
       (If `--color-primary-glow` does not exist, fall back to `var(--color-primary-subtle)` — verify with `grep color-primary-glow public/assets/css/design-system.css` before deciding.)

    3. Add `.login-panel-form` rule:
       ```
       .login-panel-form {
         background: var(--color-surface-raised);
         display: flex;
         flex-direction: column;
         align-items: center;
         justify-content: center;
         padding: var(--space-8);
         gap: var(--space-6);
       }
       ```

    4. Change `.login-orb` from `position: fixed` (viewport-scoped) to `position: absolute` (panel-scoped). Keep its existing `orb-drift` animation, existing size, existing gradient. Add `.login-panel-brand .login-orb { position: absolute; }` override if the original `.login-orb { position: fixed; }` must stay for another context; otherwise just modify the rule in place.

    5. Add the responsive breakpoint:
       ```
       @media (max-width: 768px) {
         .login-page { grid-template-columns: 1fr; }
         .login-panel-brand { display: none; }
         .login-panel-form { padding: var(--space-6); }
       }
       ```

    6. Preserve `.login-card` at `max-width: 420px` — do NOT alter its width. Preserve all `.login-brand-mark`, `.login-tagline`, `.login-form`, `.field-*`, `.login-trust`, `.login-footer` rules unchanged.

    DO NOT add `!important`. DO NOT introduce raw hex or `oklch(...)` literals — only `var(--*)` references. All new rules can sit inside `@layer components { ... }` if the file already uses layers, or at top-level otherwise.
  </action>
  <verify>
    <automated>grep -c 'grid-template-columns: 1fr 1fr' public/assets/css/login.css && grep -c '.login-panel-brand' public/assets/css/login.css && grep -c '.login-panel-form' public/assets/css/login.css && grep -c '@media (max-width: 768px)' public/assets/css/login.css && ! grep -nE '#[0-9a-fA-F]{3,8}(\W|$)|oklch\(' public/assets/css/login.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'grid-template-columns: 1fr 1fr' public/assets/css/login.css` is at least `1`
    - `grep -c '\.login-panel-brand' public/assets/css/login.css` is at least `2` (definition + media query override)
    - `grep -c '\.login-panel-form' public/assets/css/login.css` is at least `1`
    - `grep -c '@media (max-width: 768px)' public/assets/css/login.css` is at least `1`
    - `grep -cE 'display: none' public/assets/css/login.css` shows at least 1 hit inside the 768 media query context
    - `grep -cE 'var\(--color-surface-raised\)' public/assets/css/login.css` is at least `1`
    - `grep -cE '#[0-9a-fA-F]{6}|oklch\(' public/assets/css/login.css` is `0` (no raw colour literals introduced)
    - `grep -c '!important' public/assets/css/login.css` unchanged from before (no new !important rules)
    - `.login-orb` rule uses `position: absolute` (or overridden via `.login-panel-brand .login-orb { position: absolute; }`)
    - `.login-card { ... max-width: 420px ... }` preserved
  </acceptance_criteria>
  <done>login.css drives a 1fr 1fr grid on desktop with branding left / form right, collapses to single column below 768px, uses only design tokens, and keeps the 420px card intact.</done>
</task>

</tasks>

<verification>
- Open http://localhost/login.html in a desktop viewport: brand panel visible on left with orb gradient, form card on right
- Resize below 768px: brand panel disappears, form takes full width
- Inspect .login-card: still 420px max-width
- `grep -r 'copropriete\|syndic' public/login.html` returns 0
- `grep -nE '#[0-9a-fA-F]{6}|oklch\(' public/assets/css/login.css` returns 0
</verification>

<success_criteria>
- DESIGN-02 satisfied: login displays as 2 panels side-by-side on desktop, single column below 768px
- No regression in login form functionality (IDs preserved, form POST unchanged)
- Login page uses only design-system.css tokens — no hardcoded colours
- First impression of the application is modern and professional
</success_criteria>

<output>
After completion, create `.planning/phases/06-application-design-tokens/06-02-SUMMARY.md` following the summary template.
</output>
