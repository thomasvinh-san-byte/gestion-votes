---
phase: 06-application-design-tokens
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - public/operator.htmx.html
  - public/members.htmx.html
  - public/meetings.htmx.html
  - public/assets/js/core/shared.js
autonomous: true
requirements: [DESIGN-03, DESIGN-04]
must_haves:
  truths:
    - "Operator agenda list shows skeleton placeholder rows while content loads"
    - "Members list shows skeleton placeholders while cards load"
    - "Meetings list shows skeleton placeholders while sessions load"
    - "MEETING_STATUS_MAP includes a canonical entry for pv_sent status"
    - "No .htmx-request triggers render without a visible loading indicator"
  artifacts:
    - path: "public/operator.htmx.html"
      provides: "Agenda list with htmx-indicator skeleton rows"
      contains: "htmx-indicator"
    - path: "public/members.htmx.html"
      provides: "Members list with skeleton placeholders"
      contains: "skeleton-row"
    - path: "public/meetings.htmx.html"
      provides: "Meetings list with skeleton placeholders"
      contains: "skeleton-row"
    - path: "public/assets/js/core/shared.js"
      provides: "MEETING_STATUS_MAP with pv_sent entry"
      contains: "pv_sent"
  key_links:
    - from: "public/operator.htmx.html"
      to: "public/assets/css/design-system.css"
      via: ".htmx-indicator + .skeleton-row"
      pattern: "htmx-indicator|skeleton-row"
    - from: "public/assets/js/core/shared.js"
      to: "public/assets/css/design-system.css"
      via: "MEETING_STATUS_MAP badge class names"
      pattern: "badge-(success|danger|warning|info|neutral)"
---

<objective>
Wire the existing `.htmx-indicator` + `.skeleton-row` CSS infrastructure (already defined in design-system.css but unused) into the priority list containers on operator, members, and meetings pages. Close the `pv_sent` gap in MEETING_STATUS_MAP so all meeting statuses map to a canonical badge variant.

Purpose: Implements DESIGN-03 (loading states) and closes the remaining DESIGN-04 gap (pv_sent missing from status map). Loading states reduce perceived latency; the pv_sent fix prevents defensive silent-unstyled bugs.
Output: HTML additions on 3 priority pages (indicator placeholders inside async list containers) + JavaScript addition in shared.js for the pv_sent badge entry.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-application-design-tokens/06-RESEARCH.md
@.planning/phases/06-application-design-tokens/06-UI-SPEC.md

@public/operator.htmx.html
@public/members.htmx.html
@public/meetings.htmx.html
@public/assets/js/core/shared.js

<interfaces>
<!-- Existing CSS (design-system.css) — NO new CSS needed: -->
.htmx-indicator { display: none; }
.htmx-request .htmx-indicator { display: inline-flex; }
.skeleton { background: linear-gradient(90deg, --color-bg-subtle, --color-border, --color-bg-subtle); animation: skeleton-shimmer 1.5s infinite; }
.skeleton-row { /* placeholder row */ }

<!-- HTML wiring pattern (from research.md): -->
<div id="agendaList" hx-indicator="#agendaListIndicator">
  <div id="agendaListIndicator" class="htmx-indicator">
    <div class="skeleton-row"><div class="skeleton-cell skeleton"></div></div>
    <div class="skeleton-row"><div class="skeleton-cell skeleton"></div></div>
    <div class="skeleton-row"><div class="skeleton-cell skeleton"></div></div>
  </div>
  <!-- real content injected here -->
</div>

<!-- MEETING_STATUS_MAP in shared.js ~line 102 (canonical, BEM variants): -->
const MEETING_STATUS_MAP = {
  draft:     { badge: 'badge-neutral',           text: 'Brouillon' },
  live:      { badge: 'badge-danger badge-dot',  text: 'En cours' },
  paused:    { badge: 'badge-warning',           text: 'Suspendu' },
  closed:    { badge: 'badge-success',           text: 'Clôturée' },
  validated: { badge: 'badge-success',           text: 'Validée' },
  archived:  { badge: 'badge-neutral',           text: 'Archivée' },
  scheduled: { badge: 'badge-info',              text: 'Planifiée' },
  frozen:    { badge: 'badge-info',              text: 'Gelée' },
  // MISSING: pv_sent → add { badge: 'badge-info', text: 'PV envoyé' }
};

<!-- Canonical French copy for pv_sent per UI-SPEC: "PV envoyé" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire skeleton loading indicators in operator, members, meetings HTML</name>
  <files>public/operator.htmx.html, public/members.htmx.html, public/meetings.htmx.html</files>
  <read_first>
    - public/operator.htmx.html (locate `#agendaList` — research references line ~649 using `aria-busy="true"`)
    - public/members.htmx.html (locate the main member card grid container — likely `#membersList` or similar)
    - public/meetings.htmx.html (locate the main sessions list container — likely `#meetingsList` or similar)
    - .planning/phases/06-application-design-tokens/06-RESEARCH.md (section: HTMX Loading States)
    - .planning/phases/06-application-design-tokens/06-UI-SPEC.md (section: Loading States)
  </read_first>
  <action>
    For each of the three HTML files, locate the primary async list container and inject an `.htmx-indicator` block with 3 skeleton rows as a direct child, placed BEFORE any existing content placeholder.

    1. operator.htmx.html:
       - Find the element with `id="agendaList"` (research points to ~line 649 with `aria-busy="true"`).
       - As its FIRST child, add:
         ```
         <div class="htmx-indicator" aria-hidden="true">
           <div class="skeleton-row"><div class="skeleton skeleton-cell"></div></div>
           <div class="skeleton-row"><div class="skeleton skeleton-cell"></div></div>
           <div class="skeleton-row"><div class="skeleton skeleton-cell"></div></div>
         </div>
         ```
       - Keep `aria-busy="true"` if present.
       - DO NOT remove any existing child elements, DO NOT rename IDs.

    2. members.htmx.html:
       - Identify the primary members list container (grep for `id="membersList"`, `id="memberList"`, `id="membersGrid"`, or similar). If ambiguous, pick the element that currently shows the member cards grid.
       - Inject the same `.htmx-indicator` skeleton block as the first child.

    3. meetings.htmx.html:
       - Identify the primary sessions list container (grep for `id="meetingsList"`, `id="sessionsList"`, `id="meetingsGrid"`, or the container that currently holds meeting-card elements).
       - Inject the same `.htmx-indicator` skeleton block as the first child.

    For each file:
    - Preserve every existing id attribute, class attribute, data-* attribute, and inline script.
    - DO NOT add new CSS — the classes `.htmx-indicator`, `.skeleton-row`, `.skeleton`, `.skeleton-cell` are already defined in design-system.css.
    - DO NOT add any French user-visible text in the skeleton (aria-hidden is sufficient).
    - The indicator uses only CSS display toggling; `.htmx-request` is added by HTMX/fetch automatically when a request is in flight.
  </action>
  <verify>
    <automated>grep -c 'htmx-indicator' public/operator.htmx.html && grep -c 'htmx-indicator' public/members.htmx.html && grep -c 'htmx-indicator' public/meetings.htmx.html && grep -c 'skeleton-row' public/operator.htmx.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'htmx-indicator' public/operator.htmx.html` is at least `1`
    - `grep -c 'htmx-indicator' public/members.htmx.html` is at least `1`
    - `grep -c 'htmx-indicator' public/meetings.htmx.html` is at least `1`
    - `grep -c 'skeleton-row' public/operator.htmx.html` is at least `3`
    - `grep -c 'skeleton-row' public/members.htmx.html` is at least `3`
    - `grep -c 'skeleton-row' public/meetings.htmx.html` is at least `3`
    - `grep -c 'id="agendaList"' public/operator.htmx.html` unchanged (ID preserved)
    - No new `<style>` blocks added: `grep -c '<style' public/operator.htmx.html` unchanged
    - No new `<script>` blocks added beyond what already existed
  </acceptance_criteria>
  <done>The 3 priority pages show skeleton shimmer placeholders while their lists are loading, wired via the existing design-system.css htmx-indicator infrastructure.</done>
</task>

<task type="auto">
  <name>Task 2: Add pv_sent entry to MEETING_STATUS_MAP in shared.js</name>
  <files>public/assets/js/core/shared.js</files>
  <read_first>
    - public/assets/js/core/shared.js (locate `MEETING_STATUS_MAP` around line 102)
    - .planning/phases/06-application-design-tokens/06-UI-SPEC.md (status badge table — pv_sent row)
    - .planning/phases/06-application-design-tokens/06-RESEARCH.md (Open Questions item 2: pv_sent recommendation)
  </read_first>
  <action>
    Locate the `MEETING_STATUS_MAP` const in shared.js. Add a new entry for `pv_sent`:

    ```javascript
    pv_sent: { badge: 'badge-info', text: 'PV envoyé' },
    ```

    Placement: add it after the `frozen` entry (or alphabetically if the existing order is alphabetical — match the existing style). Preserve every existing entry unchanged. Preserve the trailing comma on the previous last entry if needed.

    The French text MUST be exactly `PV envoyé` (with the é accent). The badge class MUST be exactly `badge-info` (single hyphen, canonical BEM).

    DO NOT remove any existing status entry. DO NOT change the map variable name or its export. DO NOT touch any other part of shared.js. Run `node --check` or equivalent is not required — but make sure the JS file parses as valid JavaScript (no trailing syntax error).
  </action>
  <verify>
    <automated>grep -c "pv_sent.*badge-info.*PV envoyé" public/assets/js/core/shared.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pv_sent" public/assets/js/core/shared.js` is at least `1`
    - `grep -c "badge-info" public/assets/js/core/shared.js` has increased by at least 1 from baseline
    - `grep -c "PV envoyé" public/assets/js/core/shared.js` is at least `1`
    - `grep -c "draft:" public/assets/js/core/shared.js` unchanged (existing entries preserved)
    - `grep -c "MEETING_STATUS_MAP" public/assets/js/core/shared.js` unchanged (single declaration)
    - JS file loads in browser without console syntax error (manual load check on any page using shared.js)
  </acceptance_criteria>
  <done>MEETING_STATUS_MAP maps the pv_sent status to a canonical badge-info variant with French label "PV envoyé".</done>
</task>

</tasks>

<verification>
- Open operator page, trigger agenda list load: skeleton rows visible during fetch, real content replaces them on resolve
- Open members page: same skeleton-during-load behaviour
- Open meetings page: same
- Inspect shared.js MEETING_STATUS_MAP in devtools: pv_sent key present, value = { badge: 'badge-info', text: 'PV envoyé' }
- No JS console errors on any of the modified pages
</verification>

<success_criteria>
- DESIGN-03 satisfied: visual loading feedback on async list containers via existing CSS infrastructure
- DESIGN-04 gap closed: pv_sent status has a canonical badge mapping
- Zero new CSS or new keyframes created (reuse-only)
- No regressions in JS parsing or HTMX flows
</success_criteria>

<output>
After completion, create `.planning/phases/06-application-design-tokens/06-04-SUMMARY.md` following the summary template.
</output>
