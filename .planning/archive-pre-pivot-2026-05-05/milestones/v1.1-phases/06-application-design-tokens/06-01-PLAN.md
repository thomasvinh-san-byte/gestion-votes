---
phase: 06-application-design-tokens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/app.css
  - public/hub.htmx.html
  - app/Controller/QuorumController.php
autonomous: true
requirements: [DESIGN-01, DESIGN-04]
must_haves:
  truths:
    - "@layer pages is declared in app.css so per-page overrides cascade correctly"
    - "hub page status tags render with visible semantic badge styling"
    - "Operator quorum badge renders with visible success/danger styling"
  artifacts:
    - path: "public/assets/css/app.css"
      provides: "Layer order declaration including pages"
      contains: "@layer base, components, v4, pages"
    - path: "public/hub.htmx.html"
      provides: "hub page markup with canonical badge classes"
      contains: "badge badge-neutral"
    - path: "app/Controller/QuorumController.php"
      provides: "Quorum controller emitting canonical badge class names"
      contains: "badge-success"
  key_links:
    - from: "public/hub.htmx.html"
      to: "public/assets/css/design-system.css"
      via: "badge-neutral / badge-info selectors"
      pattern: "badge (badge-neutral|badge-info|badge-success|badge-warning|badge-danger)"
    - from: "app/Controller/QuorumController.php"
      to: "public/assets/css/design-system.css"
      via: "badge-success / badge-danger selectors"
      pattern: "badge-(success|danger|neutral)"
---

<objective>
Wave 0 foundation: declare the `@layer pages` cascade in app.css so later plans can scope page-level overrides correctly, and fix the two known badge defects that cause silent unstyled rendering on hub and operator pages.

Purpose: Unblocks Plan 03 (token sweep needs a legitimate override layer) and closes two visible production bugs where badges render as plain text (hub status tags and operator quorum badge).
Output: Correct layer order in app.css, canonical BEM badge classes in hub.htmx.html, canonical badge emission in QuorumController.php.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-application-design-tokens/06-RESEARCH.md
@.planning/phases/06-application-design-tokens/06-UI-SPEC.md

@public/assets/css/app.css
@public/hub.htmx.html
@app/Controller/QuorumController.php

<interfaces>
<!-- Canonical badge classes defined in design-system.css lines 1713-1762 -->
<!-- Executor MUST use these exact class names, no variations -->

.badge          (base class — flex, padding, radius)
.badge-success  (background: --color-success-subtle; color: --color-success-text)
.badge-danger   (background: --color-danger-subtle;  color: --color-danger-text)
.badge-warning  (background: --color-warning-subtle; color: --color-warning-text)
.badge-info     (background: --color-info-subtle;    color: --color-info-text)
.badge-neutral  (background: --color-bg-subtle;      color: --color-text-secondary)

Usage pattern: <span class="badge badge-{variant}">Label</span>
INVALID (silently unstyled): badge--neutral, badge--info (double dash)
INVALID (silently unstyled): <span class="badge success">  (space-separated modifier)

<!-- Layer order from design-system.css -->
@layer base, components, v4;  /* current */
@layer base, components, v4, pages;  /* target: declared in app.css BEFORE @import */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare @layer pages in app.css</name>
  <files>public/assets/css/app.css</files>
  <read_first>
    - public/assets/css/app.css (full file — check for existing @layer or @import lines)
    - .planning/phases/06-application-design-tokens/06-RESEARCH.md (section: Pitfall 4 + @layer pages declaration example)
  </read_first>
  <action>
    Add the line `@layer base, components, v4, pages;` as the FIRST non-comment line in app.css, BEFORE any `@import url("...design-system.css")` statement.

    This establishes the layer order so per-page CSS files can write `@layer pages { ... }` blocks with correct cascade precedence over `@layer components` defined in design-system.css.

    DO NOT modify design-system.css. DO NOT use `!important`. DO NOT wrap existing rules — only add the declaration line.

    If app.css already has an `@layer` declaration line, REPLACE it with the full list including `pages`.
  </action>
  <verify>
    <automated>grep -E '^@layer base, components, v4, pages;' public/assets/css/app.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@layer base, components, v4, pages;' public/assets/css/app.css` returns `1`
    - `grep -n 'design-system.css' public/assets/css/app.css` shows @import line AFTER the @layer declaration (lower line number for @layer)
    - No `!important` added anywhere in app.css (diff review)
  </acceptance_criteria>
  <done>app.css declares `@layer base, components, v4, pages;` before the design-system import, enabling pages override scope.</done>
</task>

<task type="auto">
  <name>Task 2: Fix hub.htmx.html badge double-dash defects</name>
  <files>public/hub.htmx.html</files>
  <read_first>
    - public/hub.htmx.html (full file — locate all `badge--` occurrences)
    - .planning/phases/06-application-design-tokens/06-RESEARCH.md (section: Badge Normalisation — Defect 1)
  </read_first>
  <action>
    Replace every occurrence of the double-dash BEM variants with single-hyphen canonical variants:

    - `badge--neutral` → `badge-neutral`
    - `badge--info`    → `badge-info`
    - `badge--success` → `badge-success`
    - `badge--warning` → `badge-warning`
    - `badge--danger`  → `badge-danger`

    Ensure each span retains the base `badge` class: the final attribute value must look like `class="badge badge-neutral"`, NOT `class="badge-neutral"` alone.

    Expected occurrences (from RESEARCH.md): 5 total in hub.htmx.html (lines ~92, 93, 129, 204, 213 — line numbers approximate).

    DO NOT change any `id=` attributes. DO NOT modify JS. DO NOT remove surrounding HTML.
  </action>
  <verify>
    <automated>test $(grep -c 'badge--' public/hub.htmx.html) -eq 0 && grep -c 'badge badge-neutral\|badge badge-info' public/hub.htmx.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'badge--' public/hub.htmx.html` returns `0`
    - `grep -cE 'class="badge badge-(neutral|info|success|warning|danger)"' public/hub.htmx.html` returns at least `5`
    - `grep -c 'id="hubStatusTag"' public/hub.htmx.html` unchanged from before (ID contract preserved)
    - `grep -c 'id="hubTypeTag"' public/hub.htmx.html` unchanged from before (ID contract preserved)
  </acceptance_criteria>
  <done>hub.htmx.html uses canonical BEM badge classes, type and status tags render with semantic colours.</done>
</task>

<task type="auto">
  <name>Task 3: Fix QuorumController.php badge emission</name>
  <files>app/Controller/QuorumController.php</files>
  <read_first>
    - app/Controller/QuorumController.php (full file — locate $badgeClass assignments, lines ~56-80 per research)
    - .planning/phases/06-application-design-tokens/06-RESEARCH.md (section: Badge Normalisation — Defect 2 + QuorumController badge fix code example)
  </read_first>
  <action>
    Locate every assignment of `$badgeClass` in QuorumController.php. Replace the single-word modifiers (`'success'`, `'danger'`, `'muted'`, `'warning'`, etc.) with the full BEM modifier class name:

    - `$badgeClass = 'success';` → `$badgeClass = 'badge-success';`
    - `$badgeClass = 'danger';`  → `$badgeClass = 'badge-danger';`
    - `$badgeClass = 'muted';`   → `$badgeClass = 'badge-neutral';`
    - `$badgeClass = 'warning';` → `$badgeClass = 'badge-warning';`
    - `$badgeClass = 'info';`    → `$badgeClass = 'badge-info';`

    The span emission line looks like: `echo '<span class="badge ' . $badgeClass . '">';`
    After the fix, output becomes: `<span class="badge badge-success">` (canonical — `.badge` base + `.badge-success` modifier).

    DO NOT change the echoed span structure. DO NOT remove the `badge` base class from the HTML. Only update the value assigned to $badgeClass.

    Verify syntax after edit: `php -l app/Controller/QuorumController.php`.
  </action>
  <verify>
    <automated>php -l app/Controller/QuorumController.php && ! grep -nE "\$badgeClass\s*=\s*'(success|danger|muted|warning|info)'" app/Controller/QuorumController.php</automated>
  </verify>
  <acceptance_criteria>
    - `php -l app/Controller/QuorumController.php` reports "No syntax errors"
    - `grep -cE "\\\$badgeClass\s*=\s*'badge-" app/Controller/QuorumController.php` is greater than 0
    - `grep -cE "\\\$badgeClass\s*=\s*'(success|danger|muted|warning|info)'" app/Controller/QuorumController.php` is `0` (no bare modifiers remain)
    - Existing echo pattern `class="badge ' . $badgeClass . '"` preserved (grep for literal string)
  </acceptance_criteria>
  <done>QuorumController emits canonical `badge badge-{variant}` class strings, operator quorum badges render with semantic colours.</done>
</task>

</tasks>

<verification>
- app.css declares `@layer base, components, v4, pages;` before the design-system import
- `grep -r 'badge--' public/*.html` returns zero results
- `grep -nE "badgeClass\s*=\s*'(success|danger|muted|warning|info)'" app/Controller/QuorumController.php` returns zero results
- `php -l app/Controller/QuorumController.php` passes
</verification>

<success_criteria>
- @layer pages cascade foundation ready for Plan 03 token sweep
- Hub page status/type tags render with visible pill backgrounds
- Operator page quorum badge renders with success/danger colours
- No regressions in ID contract on hub.htmx.html
</success_criteria>

<output>
After completion, create `.planning/phases/06-application-design-tokens/06-01-SUMMARY.md` following the summary template.
</output>
