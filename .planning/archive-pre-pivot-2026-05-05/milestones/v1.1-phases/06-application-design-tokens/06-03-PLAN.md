---
phase: 06-application-design-tokens
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - public/assets/css/operator.css
  - public/assets/css/settings.css
  - public/assets/css/report.css
  - public/assets/css/vote.css
  - public/assets/css/audit.css
autonomous: true
requirements: [DESIGN-01]
must_haves:
  truths:
    - "No raw oklch() literals or hex colour values remain in the 5 priority per-page CSS files"
    - "Compact spacing patterns (2px 8px, 4px gaps, 999px radii) reference design-system.css tokens"
    - "Multi-column layouts (1fr 1fr, 280px sidebar widths) are NOT collapsed or token-replaced"
  artifacts:
    - path: "public/assets/css/operator.css"
      provides: "Operator stylesheet using only token-based colours"
    - path: "public/assets/css/settings.css"
      provides: "Settings stylesheet using only token-based colours"
    - path: "public/assets/css/report.css"
      provides: "Report stylesheet using only token-based colours"
    - path: "public/assets/css/vote.css"
      provides: "Vote stylesheet using only token-based colours"
    - path: "public/assets/css/audit.css"
      provides: "Audit stylesheet using only token-based colours"
  key_links:
    - from: "public/assets/css/operator.css"
      to: "public/assets/css/design-system.css"
      via: "var(--color-*) token references"
      pattern: "var\\(--color-"
---

<objective>
Token enforcement sweep on the 5 per-page CSS files identified by RESEARCH.md as containing raw colour literals: operator.css, settings.css, report.css, vote.css, audit.css. Replace each violation with the equivalent design-system.css token. Replace `border-radius: 999px` / `99px` patterns with `var(--radius-full)`. Apply judicious spacing token substitutions (only to padding/gap/margin properties — never to structural layout widths).

Purpose: Implements DESIGN-01. Without this sweep, dark-mode and brand-colour changes leak past the design system. Scope is intentionally narrow (5 files) — these are the only ones flagged HIGH confidence by research.
Output: 5 modified per-page CSS files with zero raw `oklch(...)` literals, zero raw hex colours, and `--radius-full` substituted for full-radius patterns.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-application-design-tokens/06-RESEARCH.md
@.planning/phases/06-application-design-tokens/06-UI-SPEC.md

@public/assets/css/operator.css
@public/assets/css/settings.css
@public/assets/css/report.css
@public/assets/css/vote.css
@public/assets/css/audit.css

<interfaces>
<!-- Confirmed colour violations from RESEARCH.md (HIGH confidence): -->

operator.css line 1063: oklch(1 0 0 / 0.20)        → var(--color-surface-raised) or color-mix
settings.css line 331:  oklch(0 0 0 / 0.20) (in box-shadow) → var(--shadow-sm) or shadow token
report.css   line 180:  oklch(1 0 0 / 0.75)        → var(--color-text-on-dark) (use opacity helper)
report.css   line 182:  oklch(1 0 0 / 0.15)        → var(--color-bg-subtle) or color-mix
vote.css     line 16:   rgb(var(--shadow-color) / 0.08)  → var(--shadow-sm)
vote.css     line 1498: rgb(var(--shadow-color) / 0.08)  → var(--shadow-sm)
audit.css    line 125:  oklch(1 0 0 / 0.25)        → var(--color-surface) with opacity

<!-- Spacing token replacements (apply ONLY to padding/gap/margin/border-radius properties): -->
border-radius: 999px / 99px       → var(--radius-full)
padding: 2px 8px                  → var(--space-1) var(--space-2)
padding: 4px 12px                 → var(--space-1) var(--space-3)
gap: 4px                          → var(--space-1)
gap: 8px                          → var(--space-2)

<!-- DO NOT replace these (architectural / not spacing tokens): -->
width: 280px (sidebar)            KEEP literal
max-width: 420px (card)           KEEP literal
min-width: 960px (track)          KEEP literal
height: 16px / 24px / 8px (icons) KEEP literal
grid-template-columns: 280px 1fr  KEEP literal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep colour violations in operator.css and audit.css</name>
  <files>public/assets/css/operator.css, public/assets/css/audit.css</files>
  <read_first>
    - public/assets/css/operator.css (line 1063 area)
    - public/assets/css/audit.css (line 125 area)
    - public/assets/css/design-system.css (grep `--color-surface-raised`, `--color-surface`, `--shadow-sm` to confirm token names exist)
    - .planning/phases/06-application-design-tokens/06-RESEARCH.md (section: Token Enforcement Sweep — Colour violations table)
  </read_first>
  <action>
    For each of operator.css and audit.css:

    1. Locate every raw `oklch(...)` literal and every raw hex colour (`#abc`, `#aabbcc`, `#aabbccdd`).

    2. Replace each with the appropriate token from design-system.css:
       - `oklch(1 0 0 / 0.20)` (white-translucent overlay) → `var(--color-surface-raised)` (or use `color-mix(in oklch, var(--color-surface-raised) 80%, transparent)` if translucency is essential to the visual)
       - `oklch(1 0 0 / 0.25)` (audit overlay)            → `var(--color-surface)` similarly
       - Any other oklch literal: pick the closest semantic token (`--color-bg-subtle`, `--color-border`, `--color-text-secondary`, etc.) — verify the semantic by reading the surrounding rule context

    3. ALSO replace any `border-radius: 999px` or `border-radius: 99px` with `border-radius: var(--radius-full);` ONLY if `--radius-full` is defined in design-system.css (verify with grep first).

    4. DO NOT modify any `width:`, `min-width:`, `max-width:`, `height:`, `grid-template-columns:` declarations.

    5. DO NOT add `!important`. DO NOT introduce new tokens. DO NOT touch dark-mode media queries unless they contain colour literals (in which case fix them too).

    6. After editing, run the verification grep to confirm zero raw colour literals remain.
  </action>
  <verify>
    <automated>! grep -nE 'oklch\(|#[0-9a-fA-F]{6}([^0-9a-fA-F]|$)|#[0-9a-fA-F]{3}([^0-9a-fA-F]|$)' public/assets/css/operator.css public/assets/css/audit.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'oklch\(' public/assets/css/operator.css` returns `0`
    - `grep -cE 'oklch\(' public/assets/css/audit.css` returns `0`
    - `grep -cE '#[0-9a-fA-F]{6}([^0-9a-fA-F]|$)' public/assets/css/operator.css` returns `0`
    - `grep -cE '#[0-9a-fA-F]{6}([^0-9a-fA-F]|$)' public/assets/css/audit.css` returns `0`
    - `grep -c 'var(--color-' public/assets/css/operator.css` is GREATER than the previous count (token usage increased)
    - `grep -c '!important' public/assets/css/operator.css` unchanged from before
    - `grep -c 'grid-template-columns: 280px' public/assets/css/operator.css` unchanged (structural layouts preserved)
  </acceptance_criteria>
  <done>operator.css and audit.css contain zero raw colour literals; all colours flow through design tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep colour violations in settings.css, report.css, vote.css</name>
  <files>public/assets/css/settings.css, public/assets/css/report.css, public/assets/css/vote.css</files>
  <read_first>
    - public/assets/css/settings.css (line 331 area)
    - public/assets/css/report.css (lines 180-182 area)
    - public/assets/css/vote.css (lines 16 and 1498 areas)
    - public/assets/css/design-system.css (grep `--shadow-sm`, `--color-text-on-dark`, `--color-bg-subtle` to confirm tokens exist)
    - .planning/phases/06-application-design-tokens/06-RESEARCH.md (section: Token Enforcement Sweep — Colour violations table)
  </read_first>
  <action>
    For each of settings.css, report.css, vote.css:

    1. Locate every raw `oklch(...)` literal, every `rgb(var(--shadow-color) / 0.NN)` pattern, every raw hex colour.

    2. Apply replacements per RESEARCH.md table:
       - settings.css `oklch(0 0 0 / 0.20)` inside a box-shadow → use existing `var(--shadow-sm)` (or `var(--shadow-md)` if the original shadow had larger blur). Verify shadow token presence in design-system.css before substitution.
       - report.css `oklch(1 0 0 / 0.75)` (white-on-dark text) → `var(--color-text-on-dark)` if defined, else `var(--color-surface-raised)` with `opacity: 0.75` on the parent
       - report.css `oklch(1 0 0 / 0.15)` → `var(--color-bg-subtle)`
       - vote.css `rgb(var(--shadow-color) / 0.08)` (both occurrences) → `var(--shadow-sm)` (the `--shadow-sm` token already encapsulates this pattern)
       - Any other literal: pick the closest semantic token

    3. Apply `border-radius: var(--radius-full);` substitution for any `border-radius: 999px;` / `99px;` occurrences in these 3 files.

    4. DO NOT modify any layout width / height / grid-template values.

    5. DO NOT add `!important`. DO NOT touch JS or HTML. DO NOT modify other CSS files.
  </action>
  <verify>
    <automated>! grep -nE 'oklch\(|#[0-9a-fA-F]{6}([^0-9a-fA-F]|$)|rgb\(var\(--shadow-color\)' public/assets/css/settings.css public/assets/css/report.css public/assets/css/vote.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'oklch\(' public/assets/css/settings.css` returns `0`
    - `grep -cE 'oklch\(' public/assets/css/report.css` returns `0`
    - `grep -cE 'oklch\(' public/assets/css/vote.css` returns `0`
    - `grep -c 'rgb(var(--shadow-color)' public/assets/css/vote.css` returns `0`
    - `grep -cE '#[0-9a-fA-F]{6}([^0-9a-fA-F]|$)' public/assets/css/settings.css` returns `0`
    - `grep -cE '#[0-9a-fA-F]{6}([^0-9a-fA-F]|$)' public/assets/css/report.css` returns `0`
    - `grep -cE '#[0-9a-fA-F]{6}([^0-9a-fA-F]|$)' public/assets/css/vote.css` returns `0`
    - `grep -c 'var(--shadow-' public/assets/css/settings.css` is GREATER than the previous count
    - `grep -c '!important' public/assets/css/vote.css` unchanged from before
  </acceptance_criteria>
  <done>settings.css, report.css, vote.css contain zero raw colour literals; shadow tokens applied where appropriate.</done>
</task>

</tasks>

<verification>
- `grep -nE 'oklch\(|#[0-9a-fA-F]{6}([^0-9a-fA-F]|$)' public/assets/css/{operator,settings,report,vote,audit}.css` returns 0 results
- `grep -n 'rgb(var(--shadow-color)' public/assets/css/vote.css` returns 0 results
- `grep -c '!important' public/assets/css/{operator,settings,report,vote,audit}.css` unchanged from baseline
- Visual smoke test: load operator and report pages, verify no broken colours
</verification>

<success_criteria>
- DESIGN-01 satisfied for the 5 priority per-page CSS files
- All colour values flow through design-system.css tokens
- No structural layouts collapsed or broken
- Token cascade works correctly via the @layer pages declaration from Plan 01
</success_criteria>

<output>
After completion, create `.planning/phases/06-application-design-tokens/06-03-SUMMARY.md` following the summary template.
</output>
