---
phase: 07-playwright-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/specs/navigation.spec.js
  - tests/e2e/specs/vote.spec.js
  - tests/e2e/specs/ux-interactions.spec.js
  - tests/e2e/specs/workflow-meeting.spec.js
  - tests/e2e/specs/audit-regression.spec.js
  - tests/e2e/specs/public-display.spec.js
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "Les 18 specs Playwright existants passent sans erreur sur le project chromium"
    - "Aucune spec n'utilise waitForLoadState('networkidle') — remplacé par domcontentloaded + waitForHtmxSettled ou assertions element-based"
    - "Les specs qui chargent vote.htmx.html / postsession.htmx.html (pages SSE) ne hangent plus"
  artifacts:
    - path: "tests/e2e/specs/navigation.spec.js"
      provides: "Navigation spec without networkidle hangs"
      contains: "domcontentloaded"
    - path: "tests/e2e/specs/vote.spec.js"
      provides: "Vote spec using waitForHtmxSettled instead of networkidle"
      contains: "waitForHtmxSettled"
    - path: "tests/e2e/specs/workflow-meeting.spec.js"
      provides: "Meeting workflow spec without networkidle"
      contains: "domcontentloaded"
  key_links:
    - from: "tests/e2e/specs/vote.spec.js"
      to: "tests/e2e/helpers/waitForHtmxSettled.js"
      via: "require('../helpers/waitForHtmxSettled')"
      pattern: "waitForHtmxSettled"
---

<objective>
Baseline verte: faire passer les 18 specs Playwright existants sans erreur sur le project chromium en remplaçant tous les appels à `waitForLoadState('networkidle')` — qui hang indéfiniment sur les pages SSE — par des stratégies d'attente fiables (domcontentloaded + assertions element-based, ou waitForHtmxSettled pour les pages HTMX).

Purpose: TEST-01 exige une baseline verte zero flaky. Sans cela, TEST-02/03/04 bâtissent sur des sables mouvants.
Output: 6 specs corrigées, suite complète verte sur chromium.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-playwright-coverage/07-CONTEXT.md

<interfaces>
<!-- waitForHtmxSettled helper signature, created in Phase 5 -->

From tests/e2e/helpers/waitForHtmxSettled.js:
```javascript
// Usage: await waitForHtmxSettled(page, timeout = 5000)
// Resolves immediately if window.htmx is not loaded (most pages).
// Only vote.htmx.html and postsession.htmx.html actually use HTMX.
async function waitForHtmxSettled(page, timeout = 5000)
module.exports = { waitForHtmxSettled };
```

Import pattern (already used in vote.spec.js):
```javascript
const { waitForHtmxSettled } = require('../helpers/waitForHtmxSettled');
```

From tests/e2e/helpers.js (login helpers):
```javascript
loginAsOperator(page), loginAsAdmin(page), loginAsVoter(page), loginAsPresident(page)
E2E_MEETING_ID = 'eeeeeeee-e2e0-e2e0-e2e0-eeeeeeee0001'
```
</interfaces>

<known_networkidle_locations>
grep -rn "networkidle" tests/e2e/specs/ produces 22 matches across 6 files:
- tests/e2e/specs/navigation.spec.js (lines 17, 25, 33)
- tests/e2e/specs/vote.spec.js (line 23)
- tests/e2e/specs/ux-interactions.spec.js (lines 111, 119, 137, 153, 215, 252, 269, 283, 313)
- tests/e2e/specs/workflow-meeting.spec.js (lines 74, 82, 97, 123, 209, 228, 248)
- tests/e2e/specs/audit-regression.spec.js (line 53)
- tests/e2e/specs/public-display.spec.js (lines 15, 23)
</known_networkidle_locations>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace networkidle in non-HTMX specs (navigation, ux-interactions, workflow-meeting, audit-regression, public-display)</name>
  <read_first>
    - tests/e2e/specs/navigation.spec.js (full file)
    - tests/e2e/specs/ux-interactions.spec.js (full file)
    - tests/e2e/specs/workflow-meeting.spec.js (full file)
    - tests/e2e/specs/audit-regression.spec.js (full file)
    - tests/e2e/specs/public-display.spec.js (full file)
    - tests/e2e/helpers/waitForHtmxSettled.js
  </read_first>
  <files>
    tests/e2e/specs/navigation.spec.js,
    tests/e2e/specs/ux-interactions.spec.js,
    tests/e2e/specs/workflow-meeting.spec.js,
    tests/e2e/specs/audit-regression.spec.js,
    tests/e2e/specs/public-display.spec.js
  </files>
  <action>
    For EACH `await page.waitForLoadState('networkidle');` occurrence in these 5 files:

    1. Determine context: what is the test waiting for? Read surrounding 5-10 lines.
    2. Replace with ONE of these strategies (choose by context):
       - **Strategy A (navigation just happened):** `await page.waitForLoadState('domcontentloaded');`
       - **Strategy B (waiting for specific element to appear):** Delete the networkidle line and add `await expect(page.locator('SELECTOR')).toBeVisible({ timeout: 10000 });` where SELECTOR is the element the next assertion already depends on.
       - **Strategy C (public-display.spec.js hits pages with SSE):** Use `await page.goto(URL, { waitUntil: 'domcontentloaded' });` and then `await page.waitForSelector('main, #content, [data-ready]', { timeout: 10000 });`.

    3. NEVER introduce new `waitForLoadState('networkidle')` calls.
    4. For public-display.spec.js — the public display page subscribes to SSE and keeps the network busy forever. `networkidle` can NEVER resolve here. Strategy C is mandatory.
    5. For workflow-meeting.spec.js line 74/82/97/123/209/228/248 — these are after clicks that trigger HTMX-less JSON API calls. Use Strategy B: wait for a visible state change (toast, badge, URL change, element appearance) instead of networkidle.

    Do NOT touch vote.spec.js in this task (it's HTMX and handled in Task 2).

    Commit message: `test(07-01): replace networkidle with element-based waits in non-HTMX specs`
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -rn "waitForLoadState('networkidle')" tests/e2e/specs/navigation.spec.js tests/e2e/specs/ux-interactions.spec.js tests/e2e/specs/workflow-meeting.spec.js tests/e2e/specs/audit-regression.spec.js tests/e2e/specs/public-display.spec.js ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "networkidle" tests/e2e/specs/navigation.spec.js` returns 0
    - `grep -c "networkidle" tests/e2e/specs/ux-interactions.spec.js` returns 0
    - `grep -c "networkidle" tests/e2e/specs/workflow-meeting.spec.js` returns 0
    - `grep -c "networkidle" tests/e2e/specs/audit-regression.spec.js` returns 0
    - `grep -c "networkidle" tests/e2e/specs/public-display.spec.js` returns 0
    - Every file still starts with `// @ts-check` (if it did before) and `const { test, expect } = require('@playwright/test');`
  </acceptance_criteria>
  <done>5 spec files free of networkidle waits, each wait replaced with a targeted alternative.</done>
</task>

<task type="auto">
  <name>Task 2: Fix vote.spec.js networkidle and run full chromium suite to validate baseline</name>
  <read_first>
    - tests/e2e/specs/vote.spec.js (full file)
    - tests/e2e/helpers/waitForHtmxSettled.js
    - tests/e2e/playwright.config.js
  </read_first>
  <files>
    tests/e2e/specs/vote.spec.js
  </files>
  <action>
    1. In tests/e2e/specs/vote.spec.js, replace `await page.waitForLoadState('networkidle');` at line 23 with:
       ```javascript
       await waitForHtmxSettled(page);
       ```
       The `waitForHtmxSettled` import should already exist (added in Phase 5 gap closure). If not, add at top: `const { waitForHtmxSettled } = require('../helpers/waitForHtmxSettled');`

    2. Ensure vote.spec.js has no other networkidle references.

    3. Run the full Playwright suite on chromium only (sequential workers to get reliable output):
       ```bash
       cd /home/user/gestion_votes_php/tests/e2e && timeout 300 npx playwright test --project=chromium --workers=2 --reporter=line 2>&1 | tail -80
       ```

    4. If any spec fails, diagnose the failure:
       - Flaky auth? Re-run auth.setup.js: `npx playwright test --project=chromium --workers=2`
       - Missing selector? Fix the selector in place using data-testid / id already in public/*.htmx.html.
       - Timeout on an element? Add explicit wait with `expect(locator).toBeVisible({ timeout: 10000 })`.
       - STOP after 3 test executions per CLAUDE.md rule. If still failing, document the failure in the SUMMARY and mark that particular spec skipped with `test.skip(...)` referencing a follow-up issue.

    5. Final verification run must show: "X passed" with X == total test count, 0 failed.

    Commit: `test(07-01): fix vote.spec networkidle and validate chromium baseline green`
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "networkidle" tests/e2e/specs/vote.spec.js | grep -q '^0$' && grep -q "waitForHtmxSettled" tests/e2e/specs/vote.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "networkidle" tests/e2e/specs/vote.spec.js` returns 0
    - `grep -q "waitForHtmxSettled" tests/e2e/specs/vote.spec.js` succeeds
    - Last playwright run output contains `passed` and contains `0 failed` or does NOT contain `failed`
    - If any spec is skipped, a comment `// SKIPPED: {reason}` exists in that spec file
  </acceptance_criteria>
  <done>Vote spec uses waitForHtmxSettled. Full chromium suite is green (or documented skips only).</done>
</task>

</tasks>

<verification>
Run the full chromium suite one last time to confirm green:
```bash
cd /home/user/gestion_votes_php/tests/e2e && timeout 300 npx playwright test --project=chromium --reporter=line 2>&1 | tail -20
```
Expect: `N passed` where N is the total test count, 0 failed.

Grep confirms zero networkidle usage across all specs:
```bash
grep -rn "waitForLoadState('networkidle')" /home/user/gestion_votes_php/tests/e2e/specs/ ; test $? -eq 1
```
</verification>

<success_criteria>
- [ ] Zero `waitForLoadState('networkidle')` in tests/e2e/specs/
- [ ] Full chromium suite passes (0 failed)
- [ ] vote.spec.js uses waitForHtmxSettled
- [ ] No new flakiness introduced (run twice to confirm)
</success_criteria>

<output>
After completion, create `.planning/phases/07-playwright-coverage/07-01-SUMMARY.md` following the template.
</output>
