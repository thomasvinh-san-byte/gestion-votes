---
phase: 06-controller-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Unit/MeetingsControllerTest.php
  - tests/Unit/MeetingWorkflowControllerTest.php
  - tests/Unit/OperatorControllerTest.php
  - tests/Unit/AdminControllerTest.php
autonomous: true
requirements: [CTRL-05]

must_haves:
  truths:
    - "All 4 controller test files have structural tests rewritten to also assert service class structure"
    - "Existing controller test suites still pass green (no regressions)"
    - "Git log shows test rewrite commits BEFORE any service extraction commits"
  artifacts:
    - path: "tests/Unit/MeetingsControllerTest.php"
      provides: "Updated structural tests referencing MeetingLifecycleService"
      contains: "MeetingLifecycleService"
    - path: "tests/Unit/MeetingWorkflowControllerTest.php"
      provides: "Updated structural tests referencing MeetingTransitionService"
      contains: "MeetingTransitionService"
    - path: "tests/Unit/OperatorControllerTest.php"
      provides: "Updated structural tests referencing OperatorWorkflowService"
      contains: "OperatorWorkflowService"
    - path: "tests/Unit/AdminControllerTest.php"
      provides: "Updated structural tests referencing AdminService"
      contains: "AdminService"
  key_links:
    - from: "tests/Unit/*ControllerTest.php"
      to: "app/Services/*Service.php"
      via: "Reflection structural assertions on service classes"
      pattern: "ReflectionClass.*Service"
---

<objective>
Pre-split reflection audit and test rewrite for all 4 target controllers.

Purpose: CTRL-05 requires that tests asserting controller structure via ReflectionClass are rewritten to ALSO assert service class structure BEFORE any extraction happens. This ensures the git log shows test rewrite commits preceding split commits -- a hard requirement from the success criteria.

Output: Updated test files with service-oriented structural assertions added alongside (not replacing) existing controller structural tests. Controller tests still pass (services don't exist yet, so service tests are marked with @group pending-service and skipped until Plan 02/03 creates the services).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-controller-refactoring/06-RESEARCH.md
</context>

<interfaces>
<!-- Service classes that will be created in Plans 02/03. Tests written here will target these. -->

MeetingLifecycleService (to be created in Plan 02):
- Namespace: AgVote\Service
- final class, constructor(?RepositoryFactory $repos = null)
- Public methods: createFromWizard, updateMeeting, deleteDraft, validateMeeting, getStatus, getStatusForMeeting, getSummary, getStats, handleVoteSettings

MeetingTransitionService (to be created in Plan 02):
- Namespace: AgVote\Service
- final class, constructor(?RepositoryFactory $repos = null)
- Public methods: transition, launch, readyCheck, resetDemo

OperatorWorkflowService (to be created in Plan 03):
- Namespace: AgVote\Service
- final class, constructor(?RepositoryFactory $repos = null)
- Public methods: getWorkflowState, openVote, getAnomalies

AdminService (to be created in Plan 03):
- Namespace: AgVote\Service
- final class, constructor(?RepositoryFactory $repos = null)
- Public methods: handleUserAction, handleMeetingRole, getSystemStatus, getAuditLog
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite structural tests in MeetingsControllerTest and MeetingWorkflowControllerTest</name>
  <files>tests/Unit/MeetingsControllerTest.php, tests/Unit/MeetingWorkflowControllerTest.php</files>
  <read_first>
    - tests/Unit/MeetingsControllerTest.php (lines 54-118: structural test section)
    - tests/Unit/MeetingWorkflowControllerTest.php (lines 50-90: structural test section, lines 2043-2055: completeness check)
    - app/Services/MeetingWorkflowService.php (lines 1-47: DI pattern reference)
    - tests/Unit/MeetingWorkflowServiceTest.php (lines 1-46: test pattern reference)
  </read_first>
  <action>
In MeetingsControllerTest.php, after the existing structural test section (after line 118), add a new section:

```
// =========================================================================
// SERVICE STRUCTURE TESTS (pre-split — validates extraction targets)
// =========================================================================
```

Add these tests (all using `@group pending-service` annotation so they can be excluded until services exist):

1. `testMeetingLifecycleServiceIsFinal()` — `ReflectionClass(MeetingLifecycleService::class)->isFinal()` assertTrue
2. `testMeetingLifecycleServiceHasExpectedMethods()` — Assert hasMethod for: createFromWizard, updateMeeting, deleteDraft, validateMeeting, getStatus, getStatusForMeeting, getSummary, getStats, handleVoteSettings
3. `testMeetingLifecycleServiceUsesNullableDI()` — Assert constructor has 1 parameter, it's nullable, type is RepositoryFactory

Add the import at the top: `use AgVote\Service\MeetingLifecycleService;`

KEEP all existing controller structural tests intact (testControllerIsFinal, testControllerHasAllExpectedMethods, testControllerMethodsArePublic). The controller will STILL have these methods as thin delegators after extraction.

In MeetingWorkflowControllerTest.php, after the structural test section (after line 90), add a new section with `@group pending-service`:

1. `testMeetingTransitionServiceIsFinal()` — ReflectionClass on MeetingTransitionService
2. `testMeetingTransitionServiceHasExpectedMethods()` — Assert hasMethod for: transition, launch, readyCheck, resetDemo
3. `testMeetingTransitionServiceUsesNullableDI()` — Constructor nullable RepositoryFactory check

Also at line ~2043-2055, update the completeness check test: keep the controller completeness check but add a second assertion block for the service (under `@group pending-service`).

Add the import: `use AgVote\Service\MeetingTransitionService;`

IMPORTANT: Use `@group pending-service` PHPDoc annotation on every new service structural test. This way `php vendor/bin/phpunit --exclude-group=pending-service` still passes green.
  </action>
  <verify>
    <automated>timeout 60 php vendor/bin/phpunit tests/Unit/MeetingsControllerTest.php tests/Unit/MeetingWorkflowControllerTest.php --no-coverage --exclude-group=pending-service</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'MeetingLifecycleService' tests/Unit/MeetingsControllerTest.php returns >= 3
    - grep -c 'MeetingTransitionService' tests/Unit/MeetingWorkflowControllerTest.php returns >= 3
    - grep -c '@group pending-service' tests/Unit/MeetingsControllerTest.php returns >= 3
    - grep -c '@group pending-service' tests/Unit/MeetingWorkflowControllerTest.php returns >= 3
    - All existing controller tests still pass (exit code 0 with --exclude-group=pending-service)
  </acceptance_criteria>
  <done>MeetingsControllerTest and MeetingWorkflowControllerTest have service structural tests added with @group pending-service, existing tests still green</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite structural tests in OperatorControllerTest and AdminControllerTest</name>
  <files>tests/Unit/OperatorControllerTest.php, tests/Unit/AdminControllerTest.php</files>
  <read_first>
    - tests/Unit/OperatorControllerTest.php (lines 40-63: structural test section)
    - tests/Unit/AdminControllerTest.php (lines 40-64: structural test section)
    - app/Services/ImportService.php (lines 1-28: DI pattern reference)
  </read_first>
  <action>
In OperatorControllerTest.php, after the structural test section (after line 63), add:

```
// =========================================================================
// SERVICE STRUCTURE TESTS (pre-split — validates extraction targets)
// =========================================================================
```

With `@group pending-service`:
1. `testOperatorWorkflowServiceIsFinal()` — ReflectionClass on OperatorWorkflowService, isFinal assertTrue
2. `testOperatorWorkflowServiceHasExpectedMethods()` — Assert hasMethod for: getWorkflowState, openVote, getAnomalies
3. `testOperatorWorkflowServiceUsesNullableDI()` — Constructor has 1 nullable RepositoryFactory param

Add import: `use AgVote\Service\OperatorWorkflowService;`

In AdminControllerTest.php, after the structural test section (after line 64), add:

```
// =========================================================================
// SERVICE STRUCTURE TESTS (pre-split — validates extraction targets)
// =========================================================================
```

With `@group pending-service`:
1. `testAdminServiceIsFinal()` — ReflectionClass on AdminService, isFinal assertTrue
2. `testAdminServiceHasExpectedMethods()` — Assert hasMethod for: handleUserAction, handleMeetingRole, getSystemStatus, getAuditLog
3. `testAdminServiceUsesNullableDI()` — Constructor has 1 nullable RepositoryFactory param

Add import: `use AgVote\Service\AdminService;`

KEEP all existing controller structural tests intact. Controllers remain as thin HTTP adapters.
  </action>
  <verify>
    <automated>timeout 60 php vendor/bin/phpunit tests/Unit/OperatorControllerTest.php tests/Unit/AdminControllerTest.php --no-coverage --exclude-group=pending-service</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'OperatorWorkflowService' tests/Unit/OperatorControllerTest.php returns >= 3
    - grep -c 'AdminService' tests/Unit/AdminControllerTest.php returns >= 3
    - grep -c '@group pending-service' tests/Unit/OperatorControllerTest.php returns >= 3
    - grep -c '@group pending-service' tests/Unit/AdminControllerTest.php returns >= 3
    - All existing controller tests still pass (exit code 0 with --exclude-group=pending-service)
  </acceptance_criteria>
  <done>OperatorControllerTest and AdminControllerTest have service structural tests added with @group pending-service, existing tests still green</done>
</task>

</tasks>

<verification>
1. `timeout 60 php vendor/bin/phpunit tests/Unit/MeetingsControllerTest.php tests/Unit/MeetingWorkflowControllerTest.php tests/Unit/OperatorControllerTest.php tests/Unit/AdminControllerTest.php --no-coverage --exclude-group=pending-service` — all green, zero failures
2. `grep -l 'pending-service' tests/Unit/MeetingsControllerTest.php tests/Unit/MeetingWorkflowControllerTest.php tests/Unit/OperatorControllerTest.php tests/Unit/AdminControllerTest.php` — all 4 files listed
3. `grep -c 'ReflectionClass' tests/Unit/MeetingsControllerTest.php` — count is HIGHER than before (new service tests added)
</verification>

<success_criteria>
- All 4 test files contain @group pending-service annotated tests targeting the 4 new service classes
- Existing controller tests pass without regressions (--exclude-group=pending-service)
- Service structural tests define the contract: final class, nullable DI, expected public methods
- Git log shows these test rewrite commits before any service creation commits
</success_criteria>

<output>
After completion, create `.planning/phases/06-controller-refactoring/06-01-SUMMARY.md`
</output>
