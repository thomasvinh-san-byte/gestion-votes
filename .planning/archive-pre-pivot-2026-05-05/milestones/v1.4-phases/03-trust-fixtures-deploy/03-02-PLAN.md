---
phase: 03-trust-fixtures-deploy
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - tests/e2e/specs/critical-path-trust.spec.js
  - tests/e2e/specs/accessibility.spec.js
  - tests/e2e/specs/contrast-audit.spec.js
autonomous: true
requirements: [TRUST-03]

must_haves:
  truths:
    - "trust.htmx.html critical-path spec uses loginAsAuditor, not loginAsAdmin or loginAsOperator"
    - "accessibility.spec.js trust entry uses loginAsAuditor, not loginAsAdmin"
    - "contrast-audit.spec.js trust entry uses loginAsAuditor, not loginAsAdmin"
    - "grep loginAsAdmin in trust-related specs returns 0 occurrences"
  artifacts:
    - path: "tests/e2e/specs/critical-path-trust.spec.js"
      provides: "Trust page E2E tests with auditor auth"
      contains: "loginAsAuditor"
    - path: "tests/e2e/specs/accessibility.spec.js"
      provides: "Trust page accessibility entry with auditor auth"
      contains: "loginAsAuditor"
    - path: "tests/e2e/specs/contrast-audit.spec.js"
      provides: "Trust page contrast entry with auditor auth"
      contains: "loginAsAuditor"
  key_links:
    - from: "tests/e2e/specs/critical-path-trust.spec.js"
      to: "tests/e2e/helpers.js"
      via: "require('../helpers') importing loginAsAuditor"
      pattern: "loginAsAuditor.*require"
    - from: "tests/e2e/specs/accessibility.spec.js"
      to: "tests/e2e/helpers.js"
      via: "require('../helpers') importing loginAsAuditor"
      pattern: "loginAsAuditor"
---

<objective>
Migrate all trust-related E2E specs from loginAsAdmin/loginAsOperator to loginAsAuditor, eliminating the admin fallback pattern.

Purpose: Validate trust.htmx.html with actual auditor role (the role it is designed for), ensuring RBAC coverage and eliminating the `loginAsAdmin` fallback noted in v1.3 deferred items.
Output: All trust specs use loginAsAuditor; grep for loginAsAdmin in trust specs returns 0.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-trust-fixtures-deploy/03-RESEARCH.md
@.planning/phases/03-trust-fixtures-deploy/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — loginAsAuditor is now available -->

From tests/e2e/helpers.js (after Plan 01):
```javascript
// Exports now include:
module.exports = {
  OPERATOR_KEY, ADMIN_KEY, VOTER_KEY, CREDENTIALS,
  E2E_MEETING_ID, E2E_MOTION_1, E2E_MOTION_2, E2E_MEMBER_1,
  authStatePath, loginWithEmail,
  loginAsOperator, loginAsAdmin, loginAsVoter, loginAsPresident,
  loginAsAuditor, loginAsAssessor,  // NEW from Plan 01
};
```

From tests/e2e/specs/critical-path-trust.spec.js (CURRENT — needs migration):
```javascript
// Line 3: const { loginAsOperator } = require('../helpers');
// Line 30: await loginAsOperator(page);
```

From tests/e2e/specs/accessibility.spec.js (CURRENT — needs migration):
```javascript
// Line 83: { path: '/trust.htmx.html', loginFn: loginAsAdmin, ... }
```

From tests/e2e/specs/contrast-audit.spec.js (CURRENT — needs migration):
```javascript
// Line 45: { path: '/trust.htmx.html', loginFn: loginAsAdmin, ... }
```

Trust page role gate: `data-page-role="auditor,assessor"`
Trust API routes: `trust_anomalies` and `trust_checks` require `['auditor', 'admin', 'operator']`
Auditor is a system role with access to trust APIs. Operator also has access but is not the target role.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate critical-path-trust.spec.js to loginAsAuditor</name>
  <files>tests/e2e/specs/critical-path-trust.spec.js</files>
  <read_first>
    - tests/e2e/specs/critical-path-trust.spec.js
    - tests/e2e/helpers.js
  </read_first>
  <action>
1. **Update the require import on line 3:**
   - Change: `const { loginAsOperator } = require('../helpers');`
   - To: `const { loginAsAuditor } = require('../helpers');`

2. **Update the login call on line 30:**
   - Change: `await loginAsOperator(page);`
   - To: `await loginAsAuditor(page);`

3. **Update the JSDoc comment on line 19:**
   - Change: `Hybrid auth strategy: cookie injection via loginAsOperator (no rate limit hit).`
   - To: `Hybrid auth strategy: cookie injection via loginAsAuditor (no rate limit hit).`

4. **No other changes needed.** The trust page has `data-page-role="auditor,assessor"` and the auditor system role has access to all trust API endpoints (`trust_anomalies`, `trust_checks` require `['auditor', 'admin', 'operator']`). All existing assertions will work with auditor auth.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c 'loginAsAuditor' tests/e2e/specs/critical-path-trust.spec.js && grep -c 'loginAsOperator\|loginAsAdmin' tests/e2e/specs/critical-path-trust.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'loginAsAuditor' tests/e2e/specs/critical-path-trust.spec.js` returns >= 1
    - `grep -c 'loginAsOperator' tests/e2e/specs/critical-path-trust.spec.js` returns 0
    - `grep -c 'loginAsAdmin' tests/e2e/specs/critical-path-trust.spec.js` returns 0
    - `grep "require.*helpers" tests/e2e/specs/critical-path-trust.spec.js` shows `loginAsAuditor` in the destructured import
  </acceptance_criteria>
  <done>critical-path-trust.spec.js uses loginAsAuditor for authentication, zero references to loginAsOperator or loginAsAdmin</done>
</task>

<task type="auto">
  <name>Task 2: Migrate accessibility and contrast-audit trust entries to loginAsAuditor</name>
  <files>tests/e2e/specs/accessibility.spec.js, tests/e2e/specs/contrast-audit.spec.js</files>
  <read_first>
    - tests/e2e/specs/accessibility.spec.js (lines 1-10 for imports, line 83 for trust entry)
    - tests/e2e/specs/contrast-audit.spec.js (lines 1-10 for imports, line 45 for trust entry)
  </read_first>
  <action>
1. **Update `tests/e2e/specs/accessibility.spec.js`:**
   - Find the require/import line that destructures helpers. Add `loginAsAuditor` to the destructured import if not already present.
   - Line 83: Change `{ path: '/trust.htmx.html', loginFn: loginAsAdmin, requiredLocator: 'main, [data-page]' }` 
     to `{ path: '/trust.htmx.html', loginFn: loginAsAuditor, requiredLocator: 'main, [data-page]' }`
   - Remove the trailing comment `// RESEARCH Pitfall 5: admin fallback` — it no longer applies.
   - Do NOT change the `/users.htmx.html` entry on line 84 — that page genuinely requires admin role.

2. **Update `tests/e2e/specs/contrast-audit.spec.js`:**
   - Find the require/import line that destructures helpers. Add `loginAsAuditor` to the destructured import if not already present.
   - Line 45: Change `{ path: '/trust.htmx.html', loginFn: loginAsAdmin, requiredLocator: 'main, [data-page]' }`
     to `{ path: '/trust.htmx.html', loginFn: loginAsAuditor, requiredLocator: 'main, [data-page]' }`
   - Do NOT change the `/users.htmx.html` entry on line 46 — that page genuinely requires admin role.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && echo "=== accessibility.spec.js ===" && grep 'trust.*loginAs' tests/e2e/specs/accessibility.spec.js && echo "=== contrast-audit.spec.js ===" && grep 'trust.*loginAs' tests/e2e/specs/contrast-audit.spec.js && echo "=== Final check: loginAsAdmin in trust entries ===" && grep -n 'trust.*loginAsAdmin' tests/e2e/specs/accessibility.spec.js tests/e2e/specs/contrast-audit.spec.js; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'trust.*loginAsAuditor' tests/e2e/specs/accessibility.spec.js` returns 1 match
    - `grep 'trust.*loginAsAuditor' tests/e2e/specs/contrast-audit.spec.js` returns 1 match
    - `grep 'trust.*loginAsAdmin' tests/e2e/specs/accessibility.spec.js` returns 0 matches
    - `grep 'trust.*loginAsAdmin' tests/e2e/specs/contrast-audit.spec.js` returns 0 matches
    - `grep 'loginAsAuditor' tests/e2e/specs/accessibility.spec.js` shows it is imported
    - `grep 'loginAsAuditor' tests/e2e/specs/contrast-audit.spec.js` shows it is imported
  </acceptance_criteria>
  <done>accessibility.spec.js and contrast-audit.spec.js both use loginAsAuditor for trust page entries, zero loginAsAdmin fallback references for trust</done>
</task>

</tasks>

<verification>
Phase-level verification after both plans complete:

1. `grep -rc 'loginAsAdmin' tests/e2e/specs/critical-path-trust.spec.js tests/e2e/specs/accessibility.spec.js tests/e2e/specs/contrast-audit.spec.js | grep -v ':0$'` — must return empty (zero loginAsAdmin in any trust-related context)
2. `grep -c 'loginAsAuditor' tests/e2e/specs/critical-path-trust.spec.js` — returns >= 1
3. `grep 'trust.*loginAsAuditor' tests/e2e/specs/accessibility.spec.js` — returns 1 match
4. `grep 'trust.*loginAsAuditor' tests/e2e/specs/contrast-audit.spec.js` — returns 1 match
5. Phase gate: `grep -c loginAsAdmin tests/e2e/specs/critical-path-trust.spec.js tests/e2e/specs/accessibility.spec.js tests/e2e/specs/contrast-audit.spec.js` — all lines show `:0`
</verification>

<success_criteria>
- critical-path-trust.spec.js uses loginAsAuditor (not loginAsOperator or loginAsAdmin)
- accessibility.spec.js trust entry uses loginAsAuditor (not loginAsAdmin)
- contrast-audit.spec.js trust entry uses loginAsAuditor (not loginAsAdmin)
- grep loginAsAdmin across all three files returns 0 for trust-related entries
- No changes to non-trust entries (users.htmx.html keeps loginAsAdmin — it is genuinely admin-only)
</success_criteria>

<output>
After completion, create `.planning/phases/03-trust-fixtures-deploy/03-02-SUMMARY.md`
</output>
