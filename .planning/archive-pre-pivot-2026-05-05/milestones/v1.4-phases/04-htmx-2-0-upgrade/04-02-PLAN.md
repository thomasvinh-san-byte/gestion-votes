---
phase: 04-htmx-2-0-upgrade
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: []
autonomous: true
requirements: [HTMX-05]

must_haves:
  truths:
    - "Playwright full suite passes on chromium with htmx 2.0.6"
    - "Playwright full suite passes on firefox with htmx 2.0.6"
    - "Playwright full suite passes on webkit with htmx 2.0.6"
    - "Playwright full suite passes on mobile-chrome with htmx 2.0.6"
    - "Any pre-existing cross-browser failures documented with rationale"
  artifacts: []
  key_links: []
---

<objective>
Run the full Playwright cross-browser suite (chromium + firefox + webkit + mobile-chrome) to validate zero regressions from the htmx 2.0.6 upgrade.

Purpose: HTMX-05 requires the full Playwright suite to pass across all 4 browser engines against the v1.3 baseline. Pre-existing failures (if any) must be documented with explicit rationale.
Output: Cross-browser test results confirming no htmx-related regressions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-htmx-2-0-upgrade/04-01-SUMMARY.md
@.planning/phases/04-htmx-2-0-upgrade/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run chromium Playwright suite</name>
  <files></files>
  <read_first>
    tests/e2e/playwright.config.js,
    bin/test-e2e.sh
  </read_first>
  <action>
Run the Playwright chromium suite to validate the htmx upgrade causes no regressions.

```bash
PROJECT=chromium ./bin/test-e2e.sh
```

If the command uses Docker (likely `docker exec agvote-playwright ...`), ensure the Docker container has the updated vendor files by checking the bind mount or rebuilding.

**If any test fails:**
1. Check if the failure is related to DELETE operations (the 3 migrated endpoints)
2. Check if the failure is related to htmx event listeners (CSRF injection, error toasts)
3. If htmx-related: debug and fix in the source files from plan 04-01
4. If pre-existing (unrelated to htmx): document in SUMMARY with rationale

**Expected:** All specs pass. The htmx surface area is minimal (2 pages, 4 event listeners, 0 attributes) so regressions are unlikely.
  </action>
  <verify>
    <automated>PROJECT=chromium ./bin/test-e2e.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Chromium Playwright suite exits with code 0
    - All spec files pass (no failures in output)
  </acceptance_criteria>
  <done>Chromium Playwright suite green with htmx 2.0.6</done>
</task>

<task type="auto">
  <name>Task 2: Run cross-browser Playwright suite (firefox + webkit + mobile-chrome)</name>
  <files></files>
  <read_first>
    tests/e2e/playwright.config.js
  </read_first>
  <action>
Run Playwright against the remaining 3 browser projects sequentially:

```bash
PROJECT=firefox ./bin/test-e2e.sh
PROJECT=webkit ./bin/test-e2e.sh
PROJECT=mobile-chrome ./bin/test-e2e.sh
```

**Pre-existing failure handling (Pitfall 4 from research):**
CI only runs chromium; cross-browser is "local-only" per STATE.md tech debt. Some pre-existing failures may exist in webkit/mobile-chrome from the v1.3 baseline.

For any failure:
1. Determine if it is htmx-related (DELETE, event listeners, script loading)
2. If htmx-related: fix in source files
3. If pre-existing (unrelated to htmx): document with explicit rationale per success criteria #5 ("explicit rationale documented for toute regression pre-existante")

v1.3 baseline reference: 25/25 chromium, 25/25 firefox, 23/25 webkit, 21/25 mobile-chrome (from ROADMAP). Use this as the comparison baseline — any failure already present in v1.3 is pre-existing.

Document results for each browser with pass/fail counts.
  </action>
  <verify>
    <automated>PROJECT=firefox ./bin/test-e2e.sh 2>&1 | tail -5; PROJECT=webkit ./bin/test-e2e.sh 2>&1 | tail -5; PROJECT=mobile-chrome ./bin/test-e2e.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Firefox suite passes with same or better results than v1.3 baseline (25/25)
    - WebKit suite passes with same or better results than v1.3 baseline (23/25)
    - Mobile-chrome suite passes with same or better results than v1.3 baseline (21/25)
    - No NEW failures compared to v1.3 baseline (any new failure = htmx regression, must be fixed)
    - Pre-existing failures (if any) documented with rationale
  </acceptance_criteria>
  <done>Cross-browser Playwright suite confirms zero htmx-related regressions across all 4 browsers; results documented in SUMMARY</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Chromium: full suite green
2. Firefox: results match or exceed v1.3 baseline (25/25)
3. WebKit: results match or exceed v1.3 baseline (23/25)
4. Mobile-chrome: results match or exceed v1.3 baseline (21/25)
5. Any pre-existing failures documented with explicit rationale
6. No new htmx-related regressions in any browser
</verification>

<success_criteria>
- Playwright full suite passes chromium + firefox + webkit + mobile-chrome against v1.3 baseline
- Zero new regressions attributable to htmx 2.0.6 upgrade
- Pre-existing cross-browser failures (if any) documented per HTMX-05 requirement
</success_criteria>

<output>
After completion, create `.planning/phases/04-htmx-2-0-upgrade/04-02-SUMMARY.md`
</output>
