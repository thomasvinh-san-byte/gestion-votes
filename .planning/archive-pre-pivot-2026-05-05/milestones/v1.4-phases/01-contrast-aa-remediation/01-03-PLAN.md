---
phase: 01-contrast-aa-remediation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - /home/user/gestion_votes_php/.planning/v1.3-CONTRAST-AUDIT.json
  - /home/user/gestion_votes_php/.planning/v1.3-A11Y-REPORT.md
  - /home/user/gestion_votes_php/public/assets/css/design-system.css
autonomous: false
requirements:
  - CONTRAST-01
  - CONTRAST-04
must_haves:
  truths:
    - "tests/e2e/specs/contrast-audit.spec.js passe avec 0 violation sur les 22 pages × 2 thèmes"
    - ".planning/v1.3-CONTRAST-AUDIT.json est régénéré avec totalViolations = 0 (baseline 316 → 0)"
    - "v1.3-A11Y-REPORT.md déclare 'WCAG 2.1 AA CONFORME' (plus 'partiellement conforme')"
    - "Les specs de régression (accessibility.spec.js, keyboard-nav.spec.js, page-interactions.spec.js) restent verts"
  artifacts:
    - path: ".planning/v1.3-CONTRAST-AUDIT.json"
      provides: "Audit contrast post-remédiation, 0 violation"
      contains: "\"totalViolations\": 0"
    - path: ".planning/v1.3-A11Y-REPORT.md"
      provides: "Rapport a11y mis à jour — CONFORME"
      contains: "CONFORME"
    - path: ".planning/phases/01-contrast-aa-remediation/01-03-VERIFICATION.md"
      provides: "Log des runs successifs et micro-ajustements L* effectués"
  key_links:
    - from: "bin/test-e2e.sh avec CONTRAST_AUDIT=1"
      to: ".planning/v1.3-CONTRAST-AUDIT.json"
      via: "tests/e2e/specs/contrast-audit.spec.js — génération fichier"
      pattern: "CONTRAST_AUDIT=1"
    - from: ".planning/v1.3-CONTRAST-AUDIT.json"
      to: "v1.3-A11Y-REPORT.md §3 + §6"
      via: "mise à jour déclaration de conformité"
      pattern: "CONFORME"
---

<objective>
Vérifier empiriquement que les shifts oklch (Plan 01-01) + retrait des fallbacks hex (Plan 01-02) réduisent les 316 violations de contraste à 0 sur 22 pages × 2 thèmes. Traiter les nœuds résiduels (hors des 4 tokens principaux) par micro-ajustement, puis mettre à jour `v1.3-A11Y-REPORT.md` pour déclarer "WCAG 2.1 AA CONFORME".

Purpose: Boucler Phase 1 — sans preuve empirique par re-run axe, l'hypothèse des 4 tokens couvrant 71% reste théorique. Ce plan prouve le passage à 0 violation et documente formellement la conformité.

Output:
- `v1.3-CONTRAST-AUDIT.json` régénéré avec 0 violation
- `v1.3-A11Y-REPORT.md` mis à jour (CONFORME, timestamp 2026-04-10, référence aux commits 01-01 et 01-02)
- `01-03-VERIFICATION.md` avec le log des runs et des micro-ajustements
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-contrast-aa-remediation/01-CONTEXT.md
@.planning/phases/01-contrast-aa-remediation/01-01-SUMMARY.md
@.planning/phases/01-contrast-aa-remediation/01-02-SUMMARY.md
@.planning/v1.3-A11Y-REPORT.md
@.planning/v1.3-CONTRAST-AUDIT.json

<interfaces>
<!-- Commande de run du spec gated CONTRAST_AUDIT -->
<!-- bin/test-e2e.sh NE propage PAS encore les env vars arbitraires (cf. 16-04-SUMMARY) -->
<!-- Utiliser la commande Docker brute documentée dans v1.3-A11Y-REPORT §7 : -->

PROJECT_NAME=$(basename $(pwd) | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9_-]/_/g')
docker run --rm \
  --network "${PROJECT_NAME}_backend" \
  --volume "$(pwd):/work" \
  --volume "${PROJECT_NAME}_tests-node-modules:/work/tests/e2e/node_modules" \
  --workdir /work/tests/e2e \
  --user "$(id -u):$(id -g)" \
  -e IN_DOCKER=true -e BASE_URL=http://agvote:8080 \
  -e REDIS_HOST=redis -e REDIS_PORT=6379 -e REDIS_PASSWORD="agvote-redis-dev" \
  -e CONTRAST_AUDIT=1 \
  mcr.microsoft.com/playwright:v1.59.1-jammy \
  bash -lc "npx playwright test --project=chromium specs/contrast-audit.spec.js --reporter=line"

<!-- Structure attendue de v1.3-CONTRAST-AUDIT.json (voir baseline) -->
<!-- { "generatedAt": "...", "pages": [...], "totalViolations": N, "uniquePairs": M } -->

Regression specs à ne pas casser :
- bin/test-e2e.sh specs/accessibility.spec.js       (26 passed baseline)
- bin/test-e2e.sh specs/keyboard-nav.spec.js        (6 passed baseline)
- bin/test-e2e.sh specs/page-interactions.spec.js   (baseline v1.2)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Premier run contrast-audit + traitement itératif des résiduels</name>
  <files>
    public/assets/css/design-system.css
    .planning/v1.3-CONTRAST-AUDIT.json
    .planning/phases/01-contrast-aa-remediation/01-03-VERIFICATION.md
  </files>
  <action>
1. Lancer le runner contrast-audit via la commande Docker brute documentée ci-dessus (dans `<interfaces>`). Vérifier que le container agvote est up :
   `docker compose ps agvote redis` — sinon `docker compose up -d`

2. Après exécution, lire `.planning/v1.3-CONTRAST-AUDIT.json` :
   - Si `totalViolations == 0` → passer à Task 2.
   - Sinon, extraire la liste des paires (fg, bg) résiduelles, triées par nombre d'occurrences décroissant.

3. Pour chaque paire résiduelle :
   a. Identifier le token source par grep dans `public/assets/css/design-system.css` (ou dans les critical-tokens inline blocks si spécifique à une page).
   b. Micro-ajuster la valeur oklch dans la plage L* 45-48 (light) ou L* 78-82 (dark) pour atteindre ratio ≥ 4.5.
   c. Si le token est un des 4 déjà modifiés en 01-01 → re-modifier design-system.css ET synchroniser les 22 critical-tokens inline blocks dans le MÊME commit additionnel (règle atomicité inchangée).
   d. Si c'est un nouveau token hors des 4 principaux → même règle atomique, grep hex ancien → 0.

4. Relancer le runner contrast-audit. Maximum 3 itérations (règle CLAUDE.md "Maximum 3 executions de tests par tache"). Si après 3 runs il reste des violations → s'arrêter et demander avis utilisateur.

5. Documenter chaque run dans `.planning/phases/01-contrast-aa-remediation/01-03-VERIFICATION.md` :
   - Run N : commande, durée, totalViolations avant/après, paires résiduelles, micro-ajustement appliqué, SHA commit

**Critical:** Tout nouveau shift de token DOIT respecter la règle atomique du Plan 01-01 (design-system.css + critical-tokens inline blocks dans le même commit). Pitfall #2 reste actif.
  </action>
  <verify>
    <automated>test -f .planning/v1.3-CONTRAST-AUDIT.json &amp;&amp; python3 -c "import json; d=json.load(open('.planning/v1.3-CONTRAST-AUDIT.json')); assert d.get('totalViolations', -1) == 0, f'still {d.get(\"totalViolations\")} violations'"</automated>
  </verify>
  <done>`.planning/v1.3-CONTRAST-AUDIT.json` régénéré avec `totalViolations: 0`. `01-03-VERIFICATION.md` log les itérations. Éventuels commits additionnels respectent l'atomicité Pitfall #2.</done>
</task>

<task type="auto">
  <name>Task 2: Régression — accessibility.spec.js + keyboard-nav.spec.js</name>
  <files>
    .planning/phases/01-contrast-aa-remediation/01-03-VERIFICATION.md
  </files>
  <action>
Vérifier que les specs de régression restent vertes après les shifts de tokens. Le contraste n'est pas censé impacter la structure ARIA / keyboard, mais on vérifie.

1. `bin/test-e2e.sh specs/accessibility.spec.js`
   - Résultat attendu : `26 passed` (22 matrix + 4 legacy unit). Baseline v1.3 Phase 16-02.
2. `bin/test-e2e.sh specs/keyboard-nav.spec.js`
   - Résultat attendu : `6 passed`. Baseline v1.3 Phase 16-04.

Si échec : lire le log, identifier la cause, documenter dans `01-03-VERIFICATION.md`. Ne PAS masquer une régression — si une régression réelle apparaît, remonter à l'utilisateur avant de mettre à jour A11Y-REPORT.

Documenter dans `01-03-VERIFICATION.md` :
- Commande exécutée
- Résultat (passed/failed)
- Timestamp
- Note : "Aucune régression détectée" ou diff détaillé
  </action>
  <verify>
    <automated>bin/test-e2e.sh specs/accessibility.spec.js 2>&amp;1 | tail -5 | grep -E "26 passed|passed \(.*\)"</automated>
  </verify>
  <done>accessibility.spec.js : 26 passed. keyboard-nav.spec.js : 6 passed. Résultats loggés dans 01-03-VERIFICATION.md. Aucune régression structurelle.</done>
</task>

<task type="auto">
  <name>Task 3: Mettre à jour v1.3-A11Y-REPORT.md — déclaration CONFORME</name>
  <files>
    .planning/v1.3-A11Y-REPORT.md
  </files>
  <action>
Mettre à jour `.planning/v1.3-A11Y-REPORT.md` pour refléter la conformité WCAG 2.1 AA complète :

1. **Header** :
   - `**Date de génération:**` → 2026-04-10
   - Ajouter ligne : `**Dernière révision contrast:** 2026-04-10 (phase 01 v1.4 Contrast AA Remediation)`

2. **§3 Audit contraste** :
   - Remplacer le tableau "Résumé" : `Pages avec ≥ 1 violation contraste: 0 / 22`, `Nœuds fautifs totaux: 0`, `Paires distinctes: 0`, `Pire ratio: N/A (all ≥ 4.5)`.
   - Remplacer "Pages et pires ratios" par un tableau post-remédiation : pour chaque page, `0 violations` et ratio min observé.
   - Remplacer "Statut — DÉFÉRÉ en phase de suivi" par "Statut — **RÉSOLU** en phase v1.4 Phase 1 (commits 01-01, 01-02, 01-03). Les 316 violations baseline sont ramenées à 0."
   - Ajouter une référence aux 4 tokens shiftés et au retrait des fallbacks Shadow DOM.

3. **§6 Déclaration de conformité** :
   - Remplacer `partiellement conforme` par `**conforme**`.
   - Dans le tableau "Conformité par dimension", ligne "Contraste des couleurs" :
     statut `**CONFORME**`, justification `"316 violations baseline → 0. Audit ré-exécuté le 2026-04-10 (.planning/v1.3-CONTRAST-AUDIT.json). 4 tokens oklch dual-theme + retrait fallbacks hex Shadow DOM."`
   - Mettre à jour le texte de déclaration : `"L'application **AgVote v1.4** est **conforme** au standard **WCAG 2.1 niveau AA**..."`.
   - **Limitations connues** : retirer le point "Contraste non conforme".
   - Conserver les autres limitations (`ag-modal Shift+Tab` si toujours ouverte, audit screen reader déféré).

4. **§7 Annexe** : vérifier que la commande Docker CONTRAST_AUDIT est à jour avec le résultat actualisé ("1 passed, .planning/v1.3-CONTRAST-AUDIT.json régénéré avec totalViolations: 0").

5. Pied de page : `"*Rapport mis à jour le 2026-04-10 par le plan 01-03 du phase 01-contrast-aa-remediation (milestone v1.4). Référence canonique : CONTRAST-01..04 dans .planning/REQUIREMENTS.md.*"`

Préserver la structure à 7 sections, ne pas supprimer les autres sections (keyboard nav, structure ARIA, waivers actifs, etc.).
  </action>
  <verify>
    <automated>grep -q "CONFORME" .planning/v1.3-A11Y-REPORT.md &amp;&amp; grep -q "2026-04-10" .planning/v1.3-A11Y-REPORT.md &amp;&amp; ! grep -q "partiellement conforme" .planning/v1.3-A11Y-REPORT.md &amp;&amp; ! grep -q "316 violations contraste réparties" .planning/v1.3-A11Y-REPORT.md</automated>
  </verify>
  <done>A11Y-REPORT §3 et §6 mis à jour, "CONFORME" remplace "partiellement conforme", timestamp 2026-04-10, références commits présentes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — revue visuelle des 22 pages light + dark</name>
  <what-built>
Phase 1 complète : 4 tokens contrast-critical shiftés en oklch dual-theme, 22 critical-tokens inline blocks synchronisés, 23 Web Components nettoyés, audit contrast à 0 violation, A11Y-REPORT déclaré CONFORME.
  </what-built>
  <how-to-verify>
1. Démarrer l'app locale : `docker compose up -d agvote redis`
2. Visiter en mode LIGHT les 5 pages canary :
   - http://localhost:8080/wizard.htmx.html (pire ratio baseline 1.83)
   - http://localhost:8080/dashboard.htmx.html
   - http://localhost:8080/settings.htmx.html
   - http://localhost:8080/admin.htmx.html
   - http://localhost:8080/login.html
3. Basculer en DARK mode (toggle ou `data-theme="dark"` manuel dans devtools) et re-vérifier les mêmes 5 pages.
4. Vérifier visuellement :
   - Aucun texte muted illisible
   - Les numéros d'étape wizard sont lisibles (était 1.83, cible ≥ 4.5)
   - Les chips actives dans settings restent visibles
   - Aucune régression d'identité warm-neutral (hue 78 préservée)
   - Aucun Web Component ne montre une couleur "stale" (hex ancien) après cold reload

5. Option : re-run `bin/test-e2e.sh specs/accessibility.spec.js` et confirmer `26 passed`.

**Signal de reprise :** Écrire "approved" pour finaliser Phase 1, ou décrire les régressions visuelles observées pour gap-closure.
  </how-to-verify>
  <resume-signal>Type "approved" ou décris les problèmes visuels détectés.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Commit final — A11Y-REPORT + VERIFICATION + CONTRAST-AUDIT</name>
  <files>
    (commit uniquement)
  </files>
  <action>
Commit final de Phase 1 regroupant la documentation de vérification et le rapport mis à jour.

```bash
git add .planning/v1.3-A11Y-REPORT.md \
        .planning/v1.3-CONTRAST-AUDIT.json \
        .planning/phases/01-contrast-aa-remediation/01-03-VERIFICATION.md
git commit -m "docs(01-03): declare WCAG 2.1 AA CONFORME — 316 contrast violations → 0

- contrast-audit.spec.js ré-exécuté : totalViolations 316 → 0
- accessibility.spec.js regression : 26 passed (no structural regression)
- keyboard-nav.spec.js regression : 6 passed
- v1.3-A11Y-REPORT.md §3 and §6 updated : partially conforme → CONFORME
- Closes CONTRAST-01, CONTRAST-04 (CONTRAST-02 et 03 closed par 01-01 et 01-02)

Phase 1 (v1.4) complete : 4 token shifts oklch + 22 critical-tokens sync
+ 23 Shadow DOM fallbacks stripped. Pitfalls #1, #2, #3 neutralized."
```
  </action>
  <verify>
    <automated>git log -1 --name-only | grep -q "v1.3-A11Y-REPORT.md" &amp;&amp; git log -1 --name-only | grep -q "01-03-VERIFICATION.md"</automated>
  </verify>
  <done>Commit final créé, contient A11Y-REPORT + CONTRAST-AUDIT + VERIFICATION.md. Phase 1 prête à transition.</done>
</task>

</tasks>

<verification>
- `.planning/v1.3-CONTRAST-AUDIT.json` : `totalViolations == 0`
- `.planning/v1.3-A11Y-REPORT.md` : contient "CONFORME", ne contient plus "partiellement conforme", timestamp 2026-04-10
- `bin/test-e2e.sh specs/accessibility.spec.js` : 26 passed
- `bin/test-e2e.sh specs/keyboard-nav.spec.js` : 6 passed
- Checkpoint human-verify sur 5 pages canary light + dark : approved
</verification>

<success_criteria>
- 0 violation contrast sur 22 pages × 2 thèmes (empirique)
- Aucune régression sur specs accessibility/keyboard
- A11Y-REPORT déclare CONFORME
- Checkpoint utilisateur approuvé
- Phase 1 v1.4 prête à `/gsd:transition`
</success_criteria>

<output>
After completion, create `.planning/phases/01-contrast-aa-remediation/01-03-SUMMARY.md` listant :
- Nombre d'itérations contrast-audit nécessaires
- Éventuels tokens additionnels shiftés (hors des 4 principaux)
- Résultats des specs de régression
- Extrait de la nouvelle déclaration A11Y-REPORT §6
- SHA du commit final
- Clôture Phase 1 v1.4 — prête pour `/gsd:transition`
</output>
