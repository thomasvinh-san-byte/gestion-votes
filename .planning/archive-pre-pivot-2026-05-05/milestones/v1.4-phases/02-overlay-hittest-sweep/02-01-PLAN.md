---
phase: 02-overlay-hittest-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
  - public/assets/css/meetings.css
  - public/assets/css/validate.css
  - public/assets/css/members.css
  - public/assets/css/public.css
  - public/assets/css/vote.css
  - public/assets/css/trust.css
  - public/assets/css/settings.css
  - public/assets/css/operator.css
  - public/assets/css/wizard.css
  - public/assets/css/login.css
  - docs/audits/v1.4-overlay-hittest.md
autonomous: true
requirements:
  - OVERLAY-01
  - OVERLAY-02

must_haves:
  truths:
    - "The HTML [hidden] attribute always produces computed display:none regardless of other CSS display rules"
    - "No per-selector [hidden] overrides remain in CSS files — the global rule handles all cases"
    - "An audit document lists every display:flex/grid selector whose element receives [hidden] toggling, with status"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Global :where([hidden]) { display: none !important } rule in @layer base"
      contains: ":where([hidden])"
    - path: "docs/audits/v1.4-overlay-hittest.md"
      provides: "Codebase-wide audit of [hidden] + display conflict sites"
      min_lines: 30
  key_links:
    - from: "public/assets/css/design-system.css"
      to: "all page CSS files"
      via: "@layer base cascade — global rule overrides all per-selector display rules when [hidden] is present"
      pattern: ":where\\(\\[hidden\\]\\)"
---

<objective>
Add the global `:where([hidden]) { display: none !important }` rule to the design-system reset layer, remove all 16 redundant per-selector `[hidden]` overrides across 10 CSS files, and produce a codebase-wide audit document listing every conflict site.

Purpose: Eliminate the recurring pattern where `display: flex/grid` overrides the HTML `[hidden]` attribute, preventing hidden overlays and panels from appearing on screen. One global rule replaces 16 scattered overrides and prevents future occurrences.

Output: Modified design-system.css with global rule, 10 CSS files cleaned of redundant overrides, audit document at docs/audits/v1.4-overlay-hittest.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-overlay-hittest-sweep/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add global :where([hidden]) rule and remove 16 redundant overrides</name>
  <files>
    public/assets/css/design-system.css,
    public/assets/css/meetings.css,
    public/assets/css/validate.css,
    public/assets/css/members.css,
    public/assets/css/public.css,
    public/assets/css/vote.css,
    public/assets/css/trust.css,
    public/assets/css/settings.css,
    public/assets/css/operator.css,
    public/assets/css/wizard.css,
    public/assets/css/login.css
  </files>
  <read_first>
    public/assets/css/design-system.css (lines 59-85 — @layer base reset section, insertion point after box-sizing reset at line 71),
    public/assets/css/meetings.css (line 35),
    public/assets/css/validate.css (lines 79-81),
    public/assets/css/members.css (line 110),
    public/assets/css/public.css (lines 649-651 and 1007),
    public/assets/css/vote.css (lines 151-153, 187-189, 876, 961, 1971-1973),
    public/assets/css/trust.css (lines 515-518),
    public/assets/css/settings.css (lines 94-97),
    public/assets/css/operator.css (lines 1111-1114 and 1141-1144),
    public/assets/css/wizard.css (line 244),
    public/assets/css/login.css (lines 546-548)
  </read_first>
  <action>
**Step 1 — Add global rule to design-system.css:**

Insert the following rule in `@layer base`, immediately after the `*, *::before, *::after { box-sizing: border-box; ... }` reset block (after line 71, before the `html {` block):

```css
/* HTML [hidden] attribute must always hide — prevent display:flex/grid override.
   :where() keeps specificity at (0,0,0); !important ensures it wins over author display. */
:where([hidden]) {
  display: none !important;
}
```

**Step 2 — Remove all 16 per-selector [hidden] overrides:**

Remove these exact rule blocks (including preceding comments if they mention [hidden]):

1. `meetings.css:35` — `.onboarding-banner[hidden] { display: none; }`
2. `validate.css:79-81` — `.validate-modal-backdrop[hidden] { display: none; }`
3. `members.css:110` — `.members-onboarding[hidden] { display: none; }`
4. `public.css:649-651` — `.meeting-picker-overlay[hidden] { display: none; }`
5. `public.css:1007` — `.app-footer[hidden] { display: none; }`
6. `vote.css:151-153` — `.offline-banner[hidden] { display: none; }`
7. `vote.css:187-189` — `.current-speaker-banner[hidden] { display: none; }`
8. `vote.css:876` — `.vote-hint[hidden] { display: none; }`
9. `vote.css:961` — `.app-footer[hidden] { display: none; }`
10. `vote.css:1971-1973` — `.blocked-overlay[hidden] { display: none; }`
11. `trust.css:515-518` — `.audit-modal-overlay[hidden] { display: none; }` (and the comment above it)
12. `settings.css:94-97` — `.settings-panel[hidden] { display: none; }` (and the comment above it)
13. `operator.css:1111-1114` — `.op-transition-card[hidden] { display: none; }` (and the comment above it)
14. `operator.css:1141-1144` — `.op-quorum-overlay[hidden] { display: none; }` (and the comment above it)
15. `wizard.css:244` — `.wiz-error-banner[hidden] { display: none; }`
16. `login.css:546-548` — `.demo-panel[hidden] { display: none; }`

Do NOT touch the `:not([hidden])` selectors in design-system.css (lines 5286-5293) — those are positive selectors for showing elements, not [hidden] overrides.

After removal, clean up any resulting double-blank-lines (max 1 blank line between rules).
  </action>
  <verify>
    <automated>grep -c ':where(\[hidden\])' public/assets/css/design-system.css | grep -q '^1$' && echo "PASS: global rule exists" && grep -rn '\[hidden\].*{' public/assets/css/*.css | grep 'display.*none' | grep -v ':where' | grep -v ':not' && echo "FAIL: redundant overrides remain" || echo "PASS: no redundant overrides"</automated>
  </verify>
  <acceptance_criteria>
    - `grep ':where(\[hidden\])' public/assets/css/design-system.css` returns exactly 1 line containing `:where([hidden]) { display: none !important; }` (or the multi-line equivalent with `display: none !important`)
    - `grep -rn '\[hidden\]' public/assets/css/*.css | grep 'display.*none' | grep -v ':where' | grep -v ':not'` returns 0 lines (no per-selector overrides remain)
    - The global rule is inside `@layer base` (between `@layer base {` and the next `@layer`)
    - Lines 5286-5293 of design-system.css (`:not([hidden])` selectors) are unchanged
  </acceptance_criteria>
  <done>Global :where([hidden]) rule exists in @layer base of design-system.css. All 16 per-selector [hidden] overrides removed from 10 CSS files. Zero redundant overrides remain.</done>
</task>

<task type="auto">
  <name>Task 2: Produce codebase-wide overlay hittest audit document</name>
  <files>docs/audits/v1.4-overlay-hittest.md</files>
  <read_first>
    public/assets/css/design-system.css (verify global rule from Task 1 is present),
    .planning/phases/02-overlay-hittest-sweep/02-RESEARCH.md (audit methodology and known conflict sites table)
  </read_first>
  <action>
**Step 1 — Create directory:**
```bash
mkdir -p docs/audits
```

**Step 2 — Audit methodology:**

Run the following to identify all display:flex/grid selectors that may conflict with [hidden]:

```bash
# Find all display:flex/grid/block selectors in CSS files
grep -rn 'display:\s*\(flex\|grid\|inline-flex\|inline-grid\)' public/assets/css/*.css

# Find all JS files that toggle .hidden
grep -rn '\.hidden\s*=' public/assets/js/*.js public/assets/js/**/*.js
```

Cross-reference: for each CSS selector with display:flex/grid, check if matching elements receive [hidden] toggling in JS or HTML.

**Step 3 — Write audit document** at `docs/audits/v1.4-overlay-hittest.md`:

Structure:
```markdown
# Overlay Hittest Audit — v1.4

**Date:** {today}
**Phase:** 02-overlay-hittest-sweep
**Methodology:** Cross-reference CSS `display: flex|grid` selectors with JS `.hidden` toggling and HTML `[hidden]` attributes

## Global Fix

`:where([hidden]) { display: none !important }` added to `@layer base` in `design-system.css`.
This rule ensures any element with the HTML `[hidden]` attribute computes `display: none` regardless of other CSS rules.

## Removed Per-Selector Overrides

| # | File | Selector | Status |
|---|------|----------|--------|
| 1 | meetings.css | `.onboarding-banner[hidden]` | REMOVED — global rule handles |
| 2 | validate.css | `.validate-modal-backdrop[hidden]` | REMOVED — global rule handles |
... (all 16 entries)

## Conflict Site Inventory

| CSS File | Selector | display Value | Receives [hidden]? | Source (JS/HTML) | Status |
|----------|----------|---------------|-------------------|------------------|--------|
| operator.css | `.op-transition-card` | flex | Yes (operator-exec.js) | OK — global rule |
| settings.css | `.settings-panel` | flex | Yes (settings.js) | OK — global rule |
... (complete inventory from grep cross-reference)

## Non-Applicable Sites

Selectors with `display: flex/grid` that NEVER receive `[hidden]`:
- Listed as N/A with brief reason (e.g., "static layout, no JS toggling")

## Shadow DOM Analysis

Web Components with Shadow DOM have their own style scope. The global `:where([hidden])` rule does NOT penetrate shadow roots. However:
- `[hidden]` on custom element hosts IS handled (host elements live in light DOM)
- Grep shows no shadow-internal `[hidden]` usage — components use `visibility: hidden` or `aria-hidden` internally
- Status: N/A — no shadow DOM conflicts

## Conclusion

All 16 known conflict sites are handled by the global rule. No new conflict sites discovered that require additional fixes.
```

Populate the "Conflict Site Inventory" table by running the grep commands and cross-referencing. Every entry must have a concrete Status value: `OK — global rule`, `N/A — no [hidden] toggling`, or `FIXED — override removed`.
  </action>
  <verify>
    <automated>test -f docs/audits/v1.4-overlay-hittest.md && wc -l docs/audits/v1.4-overlay-hittest.md | awk '{if ($1 >= 30) print "PASS: audit doc exists with " $1 " lines"; else print "FAIL: too short"}'</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/audits/v1.4-overlay-hittest.md` exists and has >= 30 lines
    - `grep -c 'REMOVED' docs/audits/v1.4-overlay-hittest.md` returns >= 16 (all removed overrides listed)
    - `grep -c '|' docs/audits/v1.4-overlay-hittest.md` returns >= 20 (table rows for conflict inventory)
    - Document contains sections: "Global Fix", "Removed Per-Selector Overrides", "Conflict Site Inventory", "Shadow DOM Analysis"
  </acceptance_criteria>
  <done>Audit document at docs/audits/v1.4-overlay-hittest.md lists all 16 removed overrides, complete conflict site inventory with display:flex/grid selectors cross-referenced against JS [hidden] toggling, and shadow DOM analysis.</done>
</task>

</tasks>

<verification>
- `grep ':where(\[hidden\])' public/assets/css/design-system.css` returns the global rule
- `grep -rn '\[hidden\].*{' public/assets/css/*.css | grep 'display.*none' | grep -v ':where' | grep -v ':not'` returns 0 lines
- `docs/audits/v1.4-overlay-hittest.md` exists with >= 30 lines and all 16 overrides documented
</verification>

<success_criteria>
- Global `:where([hidden]) { display: none !important }` rule exists in @layer base of design-system.css
- All 16 per-selector [hidden] { display: none } overrides removed from 10 CSS files
- Audit document produced with complete conflict site inventory
- No changes to `:not([hidden])` selectors in design-system.css
</success_criteria>

<output>
After completion, create `.planning/phases/02-overlay-hittest-sweep/02-01-SUMMARY.md`
</output>
