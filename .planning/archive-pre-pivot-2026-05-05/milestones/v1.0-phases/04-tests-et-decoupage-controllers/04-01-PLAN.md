---
phase: 04-tests-et-decoupage-controllers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Unit/EventBroadcasterTest.php
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "Tests verify that queued events preserve insertion order via dequeue()"
    - "Tests verify that publishToSse fans out events to all registered consumers"
    - "Tests verify that dequeue atomically empties the queue (no double-delivery)"
    - "Tests verify that isServerRunning returns false after heartbeat key expires"
  artifacts:
    - path: "tests/Unit/EventBroadcasterTest.php"
      provides: "SSE race condition and delivery reliability tests"
      contains: "testQueuePreservesInsertionOrder"
  key_links:
    - from: "tests/Unit/EventBroadcasterTest.php"
      to: "app/SSE/EventBroadcaster.php"
      via: "static method calls toMeeting(), dequeue(), isServerRunning()"
      pattern: "EventBroadcaster::(toMeeting|dequeue|isServerRunning)"
---

<objective>
Add characterization tests to EventBroadcasterTest.php covering SSE race conditions and delivery reliability (TEST-03).

Purpose: Close the TEST-03 gap — the existing 6 tests verify structural cleanliness (no file constants/methods) and basic behavior, but none test event ordering, consumer fan-out, atomic dequeue, or heartbeat expiry.

Output: EventBroadcasterTest.php extended with 5+ new test methods covering race conditions and delivery guarantees.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tests-et-decoupage-controllers/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SSE delivery reliability and race condition tests to EventBroadcasterTest</name>
  <files>tests/Unit/EventBroadcasterTest.php</files>
  <read_first>
    - tests/Unit/EventBroadcasterTest.php (current 6 tests, setUp/tearDown pattern)
    - app/SSE/EventBroadcaster.php (full source: toMeeting, queue, queueRedis, publishToSse, dequeue, isServerRunning)
    - app/Core/Providers/RedisProvider.php (connection() static method, configure() for test setUp)
  </read_first>
  <action>
Add the following test methods to `EventBroadcasterTest.php`, preserving all 6 existing tests. Use unique meetingIds per test (e.g., `'test-meeting-' . uniqid()`) to avoid key collisions. Clean up all created Redis keys in each test or in an enhanced `tearDown()`.

**Test 1: `testQueuePreservesInsertionOrder()`**
- Delete `sse:event_queue` key
- Call `EventBroadcaster::toMeeting('meeting-order-test', 'event.first', ['seq' => 1])`
- Call `EventBroadcaster::toMeeting('meeting-order-test', 'event.second', ['seq' => 2])`
- Call `EventBroadcaster::toMeeting('meeting-order-test', 'event.third', ['seq' => 3])`
- Call `EventBroadcaster::dequeue()` — assert returns exactly 3 events
- Assert `$events[0]['type'] === 'event.first'`, `$events[1]['type'] === 'event.second'`, `$events[2]['type'] === 'event.third'`

**Test 2: `testDequeueEmptiesQueueAtomically()`**
- Delete `sse:event_queue` key
- Call `EventBroadcaster::toMeeting(...)` twice with distinct event types
- First `dequeue()` returns 2 events
- Second `dequeue()` returns empty array (queue was emptied by first call)
- Assert both calls explicitly

**Test 3: `testPublishToSseFansOutToRegisteredConsumers()`**
- Generate unique `$meetingId = 'test-meeting-' . uniqid()`
- Set up Redis directly: `$redis->sAdd("sse:consumers:{$meetingId}", 'consumer-a')` and `$redis->sAdd("sse:consumers:{$meetingId}", 'consumer-b')`
- Set `PUSH_ENABLED` env var to `'1'` via `putenv('PUSH_ENABLED=1')`
- Call `EventBroadcaster::toMeeting($meetingId, 'vote.cast', ['motion_id' => 'xyz'])`
- Read Redis keys `sse:queue:{$meetingId}:consumer-a` and `sse:queue:{$meetingId}:consumer-b` directly (use SERIALIZER_NONE, decode JSON manually)
- Assert both queues have 1 event each, with `type === 'vote.cast'`
- Clean up: delete consumer set and queue keys, restore env with `putenv('PUSH_ENABLED=')`

**Test 4: `testPublishToSseSkipsTenantEvents()`**
- `publishToSse` returns early when `meeting_id` is null (tenant events have `tenant_id` not `meeting_id`)
- Generate unique meetingId, register a consumer via `sAdd`
- Call `EventBroadcaster::toTenant('some-tenant', 'meeting.status_changed', [...])`
- Read consumer queue — assert it is empty (tenant events skip publishToSse per the null check on line 159)
- Note: `toTenant` DOES still push to `sse:event_queue` via `queueRedis`, just not to per-consumer queues

**Test 5: `testIsServerRunningReturnsFalseAfterHeartbeatExpiry()`**
- Set `sse:server:active` with EX 1 (1-second TTL)
- Assert `isServerRunning()` returns true immediately
- `usleep(1100000)` (1.1 seconds to ensure expiry)
- Assert `isServerRunning()` returns false

**Test 6: `testConsumerQueueTrimmedToLast100Events()`**
- Generate unique meetingId, register one consumer
- Set `PUSH_ENABLED=1`
- Push 105 events via `toMeeting()` in a loop
- Read consumer queue length via `$redis->lLen("sse:queue:{$meetingId}:{$consumerId}")`
- Assert queue length is 100 (lTrim(-100, -1) keeps only last 100)
- Clean up all keys

Update `tearDown()` to also clean up any `sse:consumers:*` and `sse:queue:*` test keys. Use a try/catch wrapping the cleanup to match existing pattern. Store test-created keys in a `private array $testKeys = []` property, populated by each test, and delete them all in tearDown.
  </action>
  <verify>
    <automated>timeout 60 php vendor/bin/phpunit tests/Unit/EventBroadcasterTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - EventBroadcasterTest.php contains method `testQueuePreservesInsertionOrder`
    - EventBroadcasterTest.php contains method `testDequeueEmptiesQueueAtomically`
    - EventBroadcasterTest.php contains method `testPublishToSseFansOutToRegisteredConsumers`
    - EventBroadcasterTest.php contains method `testPublishToSseSkipsTenantEvents`
    - EventBroadcasterTest.php contains method `testIsServerRunningReturnsFalseAfterHeartbeatExpiry`
    - EventBroadcasterTest.php contains method `testConsumerQueueTrimmedToLast100Events`
    - All 12 tests (6 existing + 6 new) pass with exit code 0
    - No test uses mocking or Reflection on EventBroadcaster — all use real Redis with state setup/teardown
  </acceptance_criteria>
  <done>EventBroadcasterTest has 12+ passing tests covering event ordering, atomic dequeue, consumer fan-out, heartbeat expiry, and queue trim — satisfying TEST-03 race condition and delivery reliability requirements.</done>
</task>

</tasks>

<verification>
timeout 60 php vendor/bin/phpunit tests/Unit/EventBroadcasterTest.php --no-coverage
All tests green, 12+ tests total (6 existing structural + 6 new delivery/race condition tests).
</verification>

<success_criteria>
- TEST-03 closed: EventBroadcaster has tests for event ordering, atomic dequeue (no double-delivery), consumer fan-out, heartbeat expiry, and queue trim limit
- All existing tests continue to pass (no regressions)
- Tests run against real Redis (@group redis), no mocks
</success_criteria>

<output>
After completion, create `.planning/phases/04-tests-et-decoupage-controllers/04-01-SUMMARY.md`
</output>
