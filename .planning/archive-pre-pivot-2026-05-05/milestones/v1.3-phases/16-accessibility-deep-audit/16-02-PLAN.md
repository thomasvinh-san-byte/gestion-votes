---
phase: 16-accessibility-deep-audit
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - public/admin.htmx.html
  - public/analytics.htmx.html
  - public/archives.htmx.html
  - public/docs.htmx.html
  - public/email-templates.htmx.html
  - public/help.htmx.html
  - public/hub.htmx.html
  - public/postsession.htmx.html
  - public/public.htmx.html
  - public/report.htmx.html
  - public/trust.htmx.html
  - public/users.htmx.html
  - public/validate.htmx.html
  - public/vote.htmx.html
  - public/wizard.htmx.html
  - public/dashboard.htmx.html
  - public/meetings.htmx.html
  - public/members.htmx.html
  - public/operator.htmx.html
  - public/settings.htmx.html
  - public/audit.htmx.html
  - public/assets/js/components/ag-modal.js
  - public/assets/js/pages/operator.js
  - public/assets/js/pages/members.js
  - public/assets/js/pages/validate.js
  - .planning/phases/16-accessibility-deep-audit/16-02-BASELINE.md
autonomous: true
requirements: [A11Y-01, A11Y-02]
must_haves:
  truths:
    - "Baseline axe run executed before any fix — full violation list captured verbatim"
    - "Violations grouped by rule-id, sorted by node count descending (batch-fix order derived from data)"
    - "All critical + serious violations either fixed or waived with justified comment"
    - "Re-run after all batches shows 0 critical + 0 serious violations (22 pages)"
    - "Any waiver added carries format: // A11Y-WAIVER: <rule-id> — <reason> — expires YYYY-MM-DD"
  artifacts:
    - path: ".planning/phases/16-accessibility-deep-audit/16-02-BASELINE.md"
      provides: "Raw baseline axe output grouped by rule-id before fixes"
      contains: "rule-id"
    - path: "tests/e2e/specs/accessibility.spec.js"
      provides: "Green axe run across 22 pages after fixes"
  key_links:
    - from: "Each .htmx.html page"
      to: "ARIA pattern fix"
      via: "aria-label / role / label / id uniqueness"
      pattern: "aria-label|role=|for="
---

<objective>
Run the baseline axe audit (now that 16-01 parametrized the spec), capture the full violation inventory grouped by rule-id, then batch-fix all critical/serious violations in rule-id-frequency order per D-07. Produce a committable green run across all 22 pages.

Purpose: deliver A11Y-01 (scan executed) + A11Y-02 (violations fixed). Baseline-first is mandatory per RESEARCH — do NOT assume which violations exist.
Output: 16-02-BASELINE.md + a series of commits, one per rule-id batch + final green run.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-accessibility-deep-audit/16-CONTEXT.md
@.planning/phases/16-accessibility-deep-audit/16-RESEARCH.md
@.planning/phases/16-accessibility-deep-audit/16-01-SUMMARY.md
@tests/e2e/specs/accessibility.spec.js
@tests/e2e/helpers/axeAudit.js

<interfaces>
<!-- Expected violation families (from RESEARCH lines 307-327) — list is a PRIOR, not truth. -->
Likely rules: button-name (critical), label (critical), link-name (serious),
landmark-one-main, region, aria-valid-attr-value, aria-required-children,
aria-required-parent, duplicate-id, duplicate-id-aria, heading-order,
dialog-name. Baseline run confirms actual set.

<!-- Waiver format (D-09, mandatory) -->
// A11Y-WAIVER: <rule-id> — <reason> — expires YYYY-MM-DD

<!-- Pitfall 3 (RESEARCH): sidebar partial is injected on 21 pages via data-include-sidebar — -->
<!-- fix duplicate-id once in shared template, 21 pages benefit. Do not patch per-page. -->

<!-- Pitfall 5: trust.htmx.html expects auditor/assessor — 16-01 used loginAsAdmin fallback. -->
<!-- If still blocked, document in BASELINE.md and adjust. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Baseline axe run + violation inventory</name>
  <files>.planning/phases/16-accessibility-deep-audit/16-02-BASELINE.md</files>
  <read_first>
    - tests/e2e/specs/accessibility.spec.js (confirm PAGES array from 16-01)
    - tests/e2e/helpers/axeAudit.js (confirm it throws with debug output)
    - .planning/phases/16-accessibility-deep-audit/16-RESEARCH.md §"Expected Violation Families"
  </read_first>
  <action>
Execute the parametrized axe suite and CAPTURE every failure verbatim into the baseline document. Do NOT fix anything yet.

Steps:
1. Run: `cd tests/e2e && timeout 180 npx playwright test specs/accessibility.spec.js --project=chromium --reporter=line 2>&1 | tee /tmp/16-02-axe-baseline.txt`
2. Parse `/tmp/16-02-axe-baseline.txt` for all lines matching `[critical]` or `[serious]` (axeAudit.js format: `- [impact] rule-id: help (N nodes)` followed by indented node targets).
3. Create `.planning/phases/16-accessibility-deep-audit/16-02-BASELINE.md` with structure:

```markdown
# Phase 16 — Axe Baseline (before fixes)

**Run date:** {YYYY-MM-DD}
**Runner:** `cd tests/e2e && npx playwright test specs/accessibility.spec.js --project=chromium`
**Pages covered:** 22 (21 HTMX + login)
**Disabled rules:** color-contrast (D-04, structural runner only)

## Summary
- Tests passing: {N}/22
- Tests failing: {N}/22
- Unique rule-ids with critical/serious impact: {N}
- Total offending nodes: {N}

## Violations grouped by rule-id (fix order = descending node count)

### 1. <rule-id> — <impact> — <M nodes across K pages>
<axe "help" text>
Pages affected:
- /foo.htmx.html: N nodes
  - <target selector> | <html snippet>
- /bar.htmx.html: ...

### 2. ...

## Per-page failure summary
| Page | Passing | Failing rules |
|------|---------|---------------|
| /login.html | ✅ | — |
| /dashboard.htmx.html | ❌ | button-name, duplicate-id |

## Open questions raised by baseline
- trust.htmx.html role fallback: worked? (Pitfall 5)
- public.htmx.html dark theme forced: axe runs in dark? Document.
- wizard.htmx.html: step 1 only scoped, yes/no.

## Proposed batch-fix order
1. <rule-id with most nodes> — <hypothesis of fix location>
2. ...
```

4. If ALL 22 tests pass on first run: still produce BASELINE.md with "No critical/serious violations found" and skip Task 2 (mark as N/A). This is valid outcome per A11Y-02.
5. Commit: `git add .planning/phases/16-accessibility-deep-audit/16-02-BASELINE.md && git commit -m "docs(16-02): axe baseline capture — N failing pages, K unique rules"`.

Do not exceed 3 `npx playwright test` invocations total across this task (CLAUDE.md rule: max 3 test runs per task). If the first run completes cleanly, done.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-accessibility-deep-audit/16-02-BASELINE.md && grep -q "Violations grouped by rule-id" .planning/phases/16-accessibility-deep-audit/16-02-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/16-accessibility-deep-audit/16-02-BASELINE.md` exists and is non-empty
    - Contains "## Violations grouped by rule-id" section
    - Contains "## Per-page failure summary" table with all 22 pages listed
    - Contains "## Proposed batch-fix order" section (or "N/A — all green" marker)
    - Committed via git: `git log -1 --name-only` includes 16-02-BASELINE.md
    - `/tmp/16-02-axe-baseline.txt` exists for downstream reference during Task 2
  </acceptance_criteria>
  <done>Baseline captured, committed, batch-fix order derived from actual data (not assumptions).</done>
</task>

<task type="auto">
  <name>Task 2: Batch-fix critical/serious violations by rule-id</name>
  <files>public/*.htmx.html (pages with violations), public/assets/js/components/ag-modal.js (if dialog-name), public/assets/js/pages/*.js (for dynamically-generated icon buttons), tests/e2e/specs/accessibility.spec.js (only if waivers added via extraDisabled in PAGES entries)</files>
  <read_first>
    - .planning/phases/16-accessibility-deep-audit/16-02-BASELINE.md (full file — this is the scope)
    - /tmp/16-02-axe-baseline.txt (raw output with node selectors)
    - For each violation, read the affected file BEFORE modifying it
    - .planning/phases/16-accessibility-deep-audit/16-RESEARCH.md §"Common Pitfalls" (Pitfalls 3, 4, 5)
  </read_first>
  <action>
Execute batch-fix ordered by 16-02-BASELINE.md's "Proposed batch-fix order". Per D-07, fix each rule-id across ALL pages before moving to the next rule-id — do NOT page-by-page.

For each batch (one rule-id):
1. Identify offending nodes from BASELINE.md (all pages).
2. Diagnose the shared root cause (shared partial? dynamic JS generator? per-page hardcoded?).
3. Apply the fix preferentially at the SHARED source (Pitfall 3 — fix once, 21 pages benefit).
4. If a violation is unfixable (third-party widget, dompdf iframe, opaque blob), add a waiver:
   - Inline in `accessibility.spec.js` by adding `extraDisabled: ['<rule-id>']` to the specific PAGES entry with a preceding comment:
     `// A11Y-WAIVER: <rule-id> — <reason> — expires 2026-10-09`
   - Scope waiver to the narrowest PAGES entry possible; never globally in axeAudit.js.
5. After each batch, run `cd tests/e2e && timeout 180 npx playwright test specs/accessibility.spec.js --project=chromium --reporter=line` (COUNT: max 3 total runs for this task).
6. Commit each batch as: `fix(16-02): resolve axe <rule-id> — <brief cause> (<N nodes across M pages>)`.
7. If a batch depends on JS changes (e.g. `members.js` adds aria-label on dynamic buttons), verify the affected page manually with a grep: `grep -n "aria-label" public/assets/js/pages/members.js`.

French-language rule (CLAUDE.md): every user-visible aria-label added MUST be in French. Examples:
- `aria-label="Modifier ce membre"`, `aria-label="Supprimer cette motion"`, `aria-label="Fermer la boîte de dialogue"`.
- NEVER use "copropriété" or "syndic" in any label.

Pitfall-specific guidance:
- **button-name**: add `aria-label` (French) to icon-only buttons. If generated in JS, fix in the generator.
- **dialog-name**: if `ag-modal.js` template uses `aria-label="${title}"` with empty title, add fallback: `aria-label="${title || 'Boîte de dialogue'}"`.
- **duplicate-id**: usually sidebar partial or toast root — fix in shared template once.
- **label**: inputs without `<label for>`, typical in wizard step 1. Add visible label or `aria-label`.
- **landmark-one-main**: ensure each page has exactly one `<main>` element.
- **region**: wrap orphan content in `<section aria-labelledby>` or move into an existing landmark.

Budget: max 3 test runs across the whole task per CLAUDE.md. Use `--grep` to re-run only impacted pages during iterations if tighter feedback is needed (counts as a run).

Final run must show: 0 critical, 0 serious, across all 22 pages. If not achievable without over-waivering, STOP and report in SUMMARY.

If Task 1 produced "all green" baseline: this task is trivially done — just add note to SUMMARY and move on.
  </action>
  <verify>
    <automated>cd tests/e2e && timeout 180 npx playwright test specs/accessibility.spec.js --project=chromium --reporter=line 2>&1 | tee /tmp/16-02-axe-final.txt | grep -E "^\s*[0-9]+ passed" | grep -qv "failed"</automated>
  </verify>
  <acceptance_criteria>
    - Final run: `grep -c "critical\|serious" /tmp/16-02-axe-final.txt` = 0 (or only inside waiver comments, not violation reports)
    - Final run exit code: 0
    - All 22 pages pass: `grep -c "✓.*has no critical/serious" /tmp/16-02-axe-final.txt` ≥ 22 (or equivalent passing count)
    - Every waiver added contains the literal string `A11Y-WAIVER:` and `expires 20` (date) — `grep -rn "A11Y-WAIVER" tests/e2e/specs/` output verifiable
    - All aria-labels added are French (grep verify: `grep -oE 'aria-label="[^"]*"' public/assets/js/pages/*.js public/*.htmx.html | grep -iE "copropri|syndic"` returns nothing)
    - At least one commit per rule-id batch (visible in `git log --oneline`)
    - `php -l` passes for any .php touched (none expected in this task)
  </acceptance_criteria>
  <done>Full parametrized axe suite green: 22/22 pages pass, 0 critical + 0 serious violations (either fixed or waived with justified comments).</done>
</task>

</tasks>

<verification>
- `cd tests/e2e && npx playwright test specs/accessibility.spec.js --project=chromium --reporter=line` → exit 0, 22/22 tests pass
- `test -f .planning/phases/16-accessibility-deep-audit/16-02-BASELINE.md`
- `grep -rn "A11Y-WAIVER" tests/e2e/specs/ | grep -v "expires" | wc -l` = 0 (every waiver has expiry)
- `git log --oneline -20 | grep "fix(16-02)"` shows per-rule commits
</verification>

<success_criteria>
- Baseline document captured BEFORE fixes (A11Y-01 scan executed)
- All critical + serious violations resolved or justified-waived (A11Y-02)
- Parametrized suite green across 22 pages
- Waivers traceable (A11Y-WAIVER format + expiry dates)
</success_criteria>

<output>
After completion, create `.planning/phases/16-accessibility-deep-audit/16-02-SUMMARY.md` with: baseline counts (before), fix batches applied (rule-id + node count + files touched), waivers added (list), final counts (after = 0/0), any open question escalated.
</output>
