---
phase: 14-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/js/pages/members.js
  - tests/e2e/specs/critical-path-members.spec.js
autonomous: true
requirements:
  - POLISH-01

must_haves:
  truths:
    - "window.AgToast.show(type, message) is globally callable from any page script (already exposed by ag-toast.js line 257)"
    - "At least 5 applicative pages call AgToast.show() for save/error/info feedback (settings.js, hub.js, vote.js, wizard.js, users.js already do — members.js is the 5th conversion target to cross the threshold unambiguously)"
    - "members.js no longer uses the inline setNotif() banner pattern for save/error feedback — it delegates to AgToast.show() exclusively"
    - "A Playwright assertion proves that a save action on the members page causes an <ag-toast> element to appear in the DOM within 3 seconds"
  artifacts:
    - path: "public/assets/js/pages/members.js"
      provides: "Member CRUD feedback via AgToast"
      contains: "AgToast.show"
    - path: "tests/e2e/specs/critical-path-members.spec.js"
      provides: "Toast adoption assertion after a save action"
      contains: "ag-toast"
  key_links:
    - from: "public/assets/js/pages/members.js"
      to: "public/assets/js/components/ag-toast.js"
      via: "window.AgToast.show() global"
      pattern: "AgToast\\.show\\("
    - from: "tests/e2e/specs/critical-path-members.spec.js"
      to: "public/assets/js/pages/members.js"
      via: "DOM assertion on <ag-toast> element after form submit"
      pattern: "ag-toast"
---

<objective>
Unify feedback notifications across pages on AgToast (POLISH-01). The AgToast component already exists and is exposed globally as `window.AgToast` via `public/assets/js/components/ag-toast.js` line 257. Five pages already call it (settings.js, hub.js, vote.js, wizard.js, users.js — verified by grep). This plan makes two small, surgical moves:

1. Convert `members.js` from its legacy inline `setNotif()` banner pattern to `AgToast.show()` so the adoption count sits at ≥5 independent pages with no ambiguity (members.js has ~20 call sites identified via grep).
2. Extend `tests/e2e/specs/critical-path-members.spec.js` with one assertion that triggers a save action and verifies an `<ag-toast>` element appears in the DOM.

Purpose: close POLISH-01 with concrete, grep-verifiable evidence — not a vague “5 pages converted” claim.

Output: members.js refactored to AgToast, critical-path-members spec extended with toast assertion.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-visual-polish/14-CONTEXT.md
@.planning/phases/14-visual-polish/14-UI-SPEC.md
@public/assets/js/pages/members.js
@public/assets/js/pages/settings.js
@public/assets/js/components/ag-toast.js
@tests/e2e/specs/critical-path-members.spec.js

<interfaces>
<!-- AgToast global API — already defined, do not modify ag-toast.js -->
From public/assets/js/components/ag-toast.js:
```javascript
// Line 212: static show(type, message, duration)
//   type: 'success' | 'error' | 'warning' | 'info'
//   message: string (HTML-escaped internally)
//   duration: optional ms, defaults success/info=5000, warning/error=8000
// Line 257: window.AgToast = AgToast;  — exposed globally
AgToast.show('success', 'Enregistrement reussi');
AgToast.show('error', 'Une erreur est survenue. Veuillez reessayer.');
```

<!-- Call pattern already in use in settings.js (reference conversion) -->
From public/assets/js/pages/settings.js (lines 125-129):
```javascript
AgToast.show('success', 'Paramètre enregistré');
// ...
AgToast.show('error', 'Erreur de sauvegarde');
```

<!-- members.js legacy pattern to replace -->
From public/assets/js/pages/members.js (grep-verified call sites):
```javascript
// ~20 call sites of setNotif('error' | 'success', message)
setNotif('error', 'Le nom du groupe est requis');    // line 293
setNotif('success', 'Groupe créé');                   // line 305
setNotif('error', body?.detail || body?.error || 'Erreur');  // lines 309, 363, 396, 684, 714, 739, 816, 995
setNotif('error', err.message);                       // lines 312, 367, 399, 687, 717, 742, 839, 998
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert members.js setNotif() calls to AgToast.show()</name>
  <files>public/assets/js/pages/members.js</files>
  <read_first>
    - public/assets/js/pages/members.js
    - public/assets/js/pages/settings.js
    - public/assets/js/components/ag-toast.js
  </read_first>
  <action>
Replace every `setNotif(type, message)` call in `public/assets/js/pages/members.js` with `AgToast.show(type, message)`.

**Exact replacements (verified by grep):**
- Every occurrence of `setNotif('error', ...)` → `AgToast.show('error', ...)`
- Every occurrence of `setNotif('success', ...)` → `AgToast.show('success', ...)`
- Every occurrence of `setNotif('info', ...)` → `AgToast.show('info', ...)` (if any exist)

**Argument order:** AgToast.show takes `(type, message)` — same order as setNotif, so arguments can be passed through unchanged.

**Leave alone:**
- The `setNotif` function definition itself can stay (other code paths may still call it) — but it MUST have zero callers after this refactor. If the function becomes dead code, remove its definition. Verify with `grep -n "setNotif" public/assets/js/pages/members.js` — only the definition may remain, OR the definition is also removed.
- Do NOT change import form — AgToast is already global via window.AgToast, no imports needed.
- Do NOT touch any other `public/assets/js/pages/*.js` file.
- Do NOT modify `ag-toast.js`.

**PHP + lint safety:** This is a pure JS refactor. No PHP files touched. No CSS touched.

**Why:** POLISH-01 requires ≥5 pages adopting AgToast. settings.js, hub.js, vote.js, wizard.js, users.js already use it (5 files confirmed via `grep -l AgToast public/assets/js/pages/`). Converting members.js pushes adoption to 6 pages and eliminates the inline-banner inconsistency flagged in 14-UI-SPEC.md § POLISH-01.
  </action>
  <verify>
    <automated>grep -c "setNotif(" public/assets/js/pages/members.js | grep -qE "^0$|^1$" && grep -c "AgToast.show(" public/assets/js/pages/members.js | awk '{if($1>=15)print"OK";else{print"FAIL: expected >=15 AgToast calls, got "$1; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setNotif(" public/assets/js/pages/members.js` returns 0 or 1 (only the function definition may remain, or zero)
    - `grep -c "AgToast.show(" public/assets/js/pages/members.js` returns ≥15 (replacing the ~20 setNotif call sites)
    - `grep -l "AgToast" public/assets/js/pages/*.js` lists at least 6 files (was 5, now includes members.js)
    - No other files in `public/assets/js/pages/` are modified
    - File passes `node -c public/assets/js/pages/members.js` (or equivalent JS syntax check — pages/members.js is classic JS, not a module)
  </acceptance_criteria>
  <done>members.js uses AgToast for all notification feedback; POLISH-01 adoption count reaches 6 pages.</done>
</task>

<task type="auto">
  <name>Task 2: Extend critical-path-members spec with toast appearance assertion</name>
  <files>tests/e2e/specs/critical-path-members.spec.js</files>
  <read_first>
    - tests/e2e/specs/critical-path-members.spec.js
    - tests/e2e/helpers.js
    - public/assets/js/pages/members.js
  </read_first>
  <action>
Add a new test case (or extend the existing critical-path members test) in `tests/e2e/specs/critical-path-members.spec.js` that:

1. Logs in as admin or operator (whichever is already used in the file — reuse the existing `loginAsAdmin` / `loginAsOperator` helper).
2. Navigates to `/members.htmx.html`.
3. Triggers a save action that members.js is guaranteed to handle — **prefer a validation error path to avoid DB writes** (re-runnable constraint from STATE.md):
   - Click the "Ajouter un groupe" (or equivalent add-group) button to open the form.
   - Submit the form with an empty name field.
   - This triggers `setNotif('error', 'Le nom du groupe est requis')` → now `AgToast.show('error', ...)`.
4. Assert that an `<ag-toast>` element appears in the DOM within 3 seconds of the submit:
   ```javascript
   await expect(page.locator('ag-toast')).toBeVisible({ timeout: 3000 });
   await expect(page.locator('ag-toast')).toHaveAttribute('type', 'error');
   ```
5. Tag the new test with `@critical-path` so it runs under existing critical-path grep filters.

**Defensive selector rules:**
- Look at the current spec file to find the exact selector used for the add-group button — do not guess. If the button id/name is unclear, grep `public/members.htmx.html` for `btnAddGroup`, `ajouter`, or similar.
- If the current spec does not yet navigate to members, add the navigation step.
- Do not remove or weaken any existing assertion in the file.

**Re-runnable:** No DB mutation happens when submitting an empty-name form (members.js throws the validation toast client-side before any API call). This keeps the E2E spec idempotent per the 09-spec family's re-runnable contract.

**Do NOT:**
- Modify `ag-toast.js` or any pages/*.js file.
- Use `page.evaluate` to inject fake toasts — assert on the real DOM only.
- Add reliance on animation timings beyond the 3s timeout.
  </action>
  <verify>
    <automated>grep -n "ag-toast" tests/e2e/specs/critical-path-members.spec.js && node -c tests/e2e/specs/critical-path-members.spec.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ag-toast" tests/e2e/specs/critical-path-members.spec.js` returns ≥1
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-members.spec.js` unchanged or higher (never lower)
    - File passes `node -c tests/e2e/specs/critical-path-members.spec.js` syntax check
    - The new assertion targets a locator on `ag-toast` (the real custom element), not a CSS class substitute
    - No other spec file is modified
  </acceptance_criteria>
  <done>critical-path-members spec contains a grep-verifiable assertion on <ag-toast> appearance after a save action.</done>
</task>

</tasks>

<verification>
- members.js adoption: `grep -c "AgToast.show(" public/assets/js/pages/members.js` ≥ 15
- members.js cleanup: `grep -c "setNotif(" public/assets/js/pages/members.js` ≤ 1
- Adoption threshold: `grep -l AgToast public/assets/js/pages/*.js | wc -l` ≥ 6
- Spec extension: `grep -n "ag-toast" tests/e2e/specs/critical-path-members.spec.js` returns at least one line
- Syntax: `node -c` passes on both modified files
</verification>

<success_criteria>
- POLISH-01 requirement is grep-provable: 6 pages call AgToast.show(), members.js is fully converted, and a Playwright spec asserts a real `<ag-toast>` DOM element appears after a save action.
- AgToast global exposure (`window.AgToast`) remains unchanged — ag-toast.js is not modified.
- No regression to other pages (only members.js and critical-path-members.spec.js touched).
</success_criteria>

<output>
After completion, create `.planning/phases/14-visual-polish/14-01-SUMMARY.md`
</output>
