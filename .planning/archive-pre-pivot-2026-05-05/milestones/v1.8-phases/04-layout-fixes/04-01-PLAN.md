---
phase: 04-layout-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/landing.css
  - public/operator.htmx.html
  - public/meetings.htmx.html
  - public/wizard.htmx.html
  - public/assets/css/design-system.css
autonomous: true
requirements: [UI-10, UI-11, UI-12]

must_haves:
  truths:
    - "La landing page montre les roles sans scroll sur 1080p"
    - "Le type de seance est un select sur operator, meetings et wizard"
    - "design-system.css ne contient plus de .kpi-card mort"
  artifacts:
    - path: "public/assets/css/landing.css"
      provides: "Hero compacte sans 100vh"
      contains: "min-height: auto"
    - path: "public/operator.htmx.html"
      provides: "Select type seance"
      contains: "form-select"
    - path: "public/meetings.htmx.html"
      provides: "Select type seance"
      contains: "form-select"
    - path: "public/wizard.htmx.html"
      provides: "Select type seance"
      contains: "form-select"
    - path: "public/assets/css/design-system.css"
      provides: "KPI dead code supprime"
  key_links:
    - from: "public/operator.htmx.html"
      to: "backend API"
      via: "same name attribute and value format"
      pattern: "name=\"meeting_type\""
---

<objective>
Fix three layout issues: compact the landing hero for 1080p, convert meeting type radio buttons to select dropdowns, and remove dead KPI CSS.

Purpose: Achieve optimal page layouts — content visible without scroll, modern form controls, zero dead code.
Output: 5 files modified (1 CSS, 3 HTML, 1 CSS cleanup).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-layout-fixes/04-CONTEXT.md
@public/assets/css/landing.css
@public/operator.htmx.html
@public/meetings.htmx.html
@public/wizard.htmx.html
@public/assets/css/design-system.css
@public/assets/css/pages.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing hero compaction for 1080p</name>
  <files>public/assets/css/landing.css</files>
  <action>
In landing.css, find the `.hero` rule (around lines 59-70):
1. Change `min-height: 100vh` to `min-height: auto`
2. Reduce vertical padding — target approximately `padding: var(--space-6) var(--space-6) var(--space-8)` (or equivalent values so the hero + roles section fits in ~700px total height on 1080p)
3. Keep the 2-panel layout (text left, login right) unchanged per D-03
4. Do NOT add scroll indicators or animations (out of scope)

The goal: on a 1080p viewport (1920x1080), the roles/features section below the hero should be partially visible without scrolling.
  </action>
  <verify>
    <automated>grep -n "min-height" public/assets/css/landing.css | grep -v "100vh" && echo "PASS: no 100vh"</automated>
  </verify>
  <done>Hero no longer forces 100vh, padding reduced, roles section visible on 1080p without scroll</done>
</task>

<task type="auto">
  <name>Task 2: Radio buttons to select on 3 pages</name>
  <files>public/operator.htmx.html, public/meetings.htmx.html, public/wizard.htmx.html</files>
  <action>
On each of the 3 pages, replace the meeting type radio button group with a select element:

```html
<select name="meeting_type" class="form-select">
  <option value="ag_ordinaire">AG ordinaire</option>
  <option value="ag_extraordinaire">AG extraordinaire</option>
  <option value="conseil">Conseil</option>
  <option value="bureau">Bureau</option>
  <option value="autre">Autre</option>
</select>
```

Specific locations:
- `operator.htmx.html`: settings panel (around lines 430-443) — find the radio inputs with name="meeting_type" or similar
- `meetings.htmx.html`: edit modal (around lines 208-224) — find the radio inputs
- `wizard.htmx.html`: step 0 type selection — find the radio inputs

Rules per decisions D-04 through D-06:
- Keep the same `name` attribute for backend compatibility
- Keep the same value format (ag_ordinaire, ag_extraordinaire, conseil, bureau, autre)
- Use class="form-select" (design-system standard)
- Remove the wrapping radio div and all associated radio input+label elements
- If any JS event listener targets these radios (e.g., change events), update to work with select element (select fires the same "change" event, so usually no change needed)
  </action>
  <verify>
    <automated>grep -c "type=\"radio\".*meeting_type\|name=\"meeting_type\".*type=\"radio\"" public/operator.htmx.html public/meetings.htmx.html public/wizard.htmx.html 2>/dev/null; grep -l "form-select" public/operator.htmx.html public/meetings.htmx.html public/wizard.htmx.html | wc -l | xargs -I{} test {} -eq 3 && echo "PASS: all 3 pages have form-select"</automated>
  </verify>
  <done>All 3 pages use a select.form-select for meeting type with identical name/values, zero radio buttons remain for meeting type</done>
</task>

<task type="auto">
  <name>Task 3: Delete dead KPI code from design-system.css</name>
  <files>public/assets/css/design-system.css</files>
  <action>
In design-system.css, delete the dead `.kpi-card` block and related dead modifiers (around lines 2582-2610 per D-07/D-08):

Delete:
- The `.kpi-card` rule block that is overridden by pages.css
- The `.kpi-value.primary`, `.kpi-value.success`, and similar color modifier rules that are non-functional

Do NOT touch:
- pages.css `.kpi-card` definition (that is the active single source of truth per D-09)
- Any other rules in design-system.css

After deletion, verify no other file imports or references these specific dead rules.
  </action>
  <verify>
    <automated>grep -c "\.kpi-card" public/assets/css/design-system.css | xargs -I{} test {} -eq 0 && echo "PASS: no kpi-card in design-system.css"</automated>
  </verify>
  <done>design-system.css contains zero .kpi-card or .kpi-value rules, pages.css version remains untouched</done>
</task>

</tasks>

<verification>
1. `grep "100vh" public/assets/css/landing.css` returns nothing
2. `grep -r "type=\"radio\"" public/operator.htmx.html public/meetings.htmx.html public/wizard.htmx.html | grep -i "meeting_type\|type_seance\|session_type"` returns nothing
3. `grep "\.kpi-card" public/assets/css/design-system.css` returns nothing
4. `grep "\.kpi-card" public/assets/css/pages.css` still returns the active definition
5. `php -l` passes on any modified PHP files (none expected)
</verification>

<success_criteria>
- Landing hero fits in ~60% of 1080p viewport (no forced full-screen)
- Meeting type is a single select dropdown on all 3 pages
- design-system.css has zero dead KPI rules
- No visual regressions on pages.css KPI cards (untouched)
- Backend compatibility maintained (same form field names and values)
</success_criteria>

<output>
After completion, create `.planning/phases/04-layout-fixes/04-01-SUMMARY.md`
</output>
