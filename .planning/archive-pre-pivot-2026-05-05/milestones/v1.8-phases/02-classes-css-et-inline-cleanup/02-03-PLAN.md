---
phase: 02-classes-css-et-inline-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/js/core/shell.js
  - public/assets/css/design-system.css
autonomous: true
requirements:
  - UI-06
must_haves:
  truths:
    - "Le drawer shell.js utilise des classes CSS au lieu de styles inline pour le layout"
    - "Les sections du drawer (Informations, Check-list, Anomalies) sont stylees via classes"
    - "Les messages d'etat (chargement, erreur, vide) utilisent des classes"
    - "Les anomaly cards dans le drawer utilisent des classes au lieu de inline styles"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Classes drawer-section, drawer-label, drawer-value, drawer-alert, drawer-badge, drawer-placeholder"
      contains: "drawer-section"
    - path: "public/assets/js/core/shell.js"
      provides: "Drawer render functions using CSS classes instead of inline styles"
  key_links:
    - from: "public/assets/js/core/shell.js"
      to: "public/assets/css/design-system.css"
      via: "drawer-* CSS classes"
      pattern: "drawer-(section|label|value|alert|placeholder|checklist)"
---

<objective>
Replace all inline style strings in shell.js drawer rendering functions with design-system CSS classes.

Purpose: shell.js has 40+ inline style attributes in HTML string templates for the drawer panel. These should use CSS classes for maintainability and CSP compatibility.
Output: shell.js drawer functions using class="drawer-*" instead of style="...", new drawer content classes in design-system.css.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-classes-css-et-inline-cleanup/02-CONTEXT.md
@public/assets/js/core/shell.js
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create drawer content CSS classes in design-system.css</name>
  <files>public/assets/css/design-system.css</files>
  <action>
Add these classes near the existing `.drawer` section in design-system.css (after `.drawer-body`):

```css
/* --- Drawer content classes (Phase 02 - UI-06) --- */

/* Placeholder/loading/error/empty states */
.drawer-placeholder {
  padding: 16px;
  text-align: center;
}

/* Section wrapper with bottom spacing */
.drawer-section {
  margin-bottom: 16px;
}

/* Section heading: small uppercase muted label */
.drawer-label {
  font-size: 11px;
  text-transform: uppercase;
  color: var(--color-text-muted, #888);
  letter-spacing: 0.3px;
  margin-bottom: 6px;
  font-weight: 600;
}

/* Primary value (meeting title, etc.) */
.drawer-value {
  font-weight: 600;
  margin-bottom: 4px;
}

/* Inline row for key-value pairs */
.drawer-row {
  display: flex;
  gap: 10px;
  align-items: center;
  margin-bottom: 4px;
}

/* Checklist item row */
.drawer-checklist-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 3px 0;
}

/* Anomaly/alert card */
.drawer-alert {
  padding: 8px 10px;
  border-radius: 6px;
  background: var(--color-bg-warning, #fff3cd);
  border-left: 3px solid var(--color-warning, #e6a800);
  margin-bottom: 6px;
}

.drawer-alert__code {
  font-weight: 600;
  font-size: 12px;
  color: var(--color-danger, #a05252);
}

/* Anomaly card (neutral, for renderAnomalies standalone) */
.drawer-anomaly-card {
  padding: 8px 12px;
  border-radius: 8px;
  background: var(--color-bg, #f5f5f5);
}

.drawer-anomaly-card__code {
  font-weight: 600;
  color: var(--color-danger, #a05252);
}

/* Readiness badge override for larger size */
.drawer-readiness-badge {
  font-size: 14px;
  padding: 6px 12px;
}

/* Info layout - vertical flex with gap */
.drawer-info-layout {
  display: flex;
  flex-direction: column;
  gap: 12px;
  padding: 4px 0;
}

/* Readiness layout - vertical flex */
.drawer-readiness-layout {
  padding: 4px 0;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

/* Anomalies layout */
.drawer-anomalies-layout {
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding: 4px 0;
}

/* Sections wrapper */
.drawer-sections {
  padding: 4px 0;
}

/* Footer note for partial loading */
.drawer-footer-note {
  padding: 8px 12px;
  font-size: 11px;
  color: var(--color-text-muted, #999);
  border-top: 1px solid var(--color-border, #eee);
  margin-top: 8px;
}

/* Badge with bottom margin in drawer context */
.drawer-badge-mb {
  margin-bottom: 8px;
}
```

Also add notification panel classes to replace shell.js notification inline styles:

```css
/* Notification panel content classes */
.notif-panel-header {
  padding: 12px 16px;
  font-size: 13px;
  font-weight: 700;
  border-bottom: 1px solid var(--color-border-subtle, #e8e7e2);
}

.notif-panel-footer {
  display: block;
  padding: 10px 16px;
  text-align: center;
  font-size: 12px;
  font-weight: 600;
  color: var(--color-primary);
  border-top: 1px solid var(--color-border-subtle, #e8e7e2);
  text-decoration: none;
}

.notif-empty {
  padding: 20px;
  text-align: center;
  font-size: 13px;
  color: var(--color-text-muted, #95a3a4);
}

/* Command palette classes */
.cmd-kbd {
  font-size: 11px;
  padding: 2px 6px;
  border-radius: 4px;
  border: 1px solid var(--color-border, #d5dbd2);
  color: var(--color-text-muted, #95a3a4);
}

.cmd-kbd-sm {
  font-size: 10px;
  padding: 1px 5px;
  border-radius: 3px;
  border: 1px solid var(--color-border, #d5dbd2);
  margin-left: 8px;
  color: var(--color-text-muted, #95a3a4);
}
```
  </action>
  <verify>
    <automated>grep -c 'drawer-section\|drawer-label\|drawer-value\|drawer-alert\|drawer-placeholder\|drawer-checklist' public/assets/css/design-system.css</automated>
  </verify>
  <done>All drawer content classes defined in design-system.css. At least 15 new class selectors present.</done>
</task>

<task type="auto">
  <name>Task 2: Replace shell.js inline styles with drawer CSS classes</name>
  <files>public/assets/js/core/shell.js</files>
  <action>
Replace all inline style="" attributes in shell.js HTML string templates with the CSS classes defined in Task 1. Per D-11, keep the HTML structure — only replace style="..." with class="...".

**renderContext function (~line 259-324):**
- `style="padding:16px;"` on placeholder divs -> `class="drawer-placeholder"`
- `style="margin-bottom:16px;"` section wrappers -> `class="drawer-section"`
- `style="font-size:11px;text-transform:uppercase;..."` section labels -> `class="drawer-label"`
- `style="font-weight:600;margin-bottom:4px;"` title -> `class="drawer-value"`
- `style="display:flex;gap:10px;align-items:center;margin-bottom:4px;"` row -> `class="drawer-row"`
- `style="margin-bottom:8px;"` on badge -> add `class="drawer-badge-mb"` (keep badge class)
- `style="display:flex;align-items:center;gap:8px;padding:3px 0;"` checklist items -> `class="drawer-checklist-item"`
- `style="padding:8px 10px;border-radius:6px;background:...;border-left:..."` anomaly cards -> `class="drawer-alert"`
- `style="font-weight:600;font-size:12px;color:..."` anomaly code -> `class="drawer-alert__code"`
- `style="padding:8px 12px;font-size:11px;..."` footer note -> `class="drawer-footer-note"`
- `style="padding:16px;text-align:center;"` empty state -> `class="drawer-placeholder"`
- `style="padding:4px 0;"` sections wrapper -> `class="drawer-sections"`

**renderReadiness function (~line 329-339):**
- `style="padding:16px;"` placeholders -> `class="drawer-placeholder"`
- `style="padding:4px 0;display:flex;flex-direction:column;gap:10px;"` -> `class="drawer-readiness-layout"`
- `style="font-size:14px;padding:6px 12px;"` badge -> add `class="drawer-readiness-badge"` (keep badge class)
- `style="display:flex;align-items:center;gap:8px;"` check items -> `class="drawer-checklist-item"`

**renderInfos function (~line 343-353):**
- `style="padding:16px;"` placeholders -> `class="drawer-placeholder"`
- `style="display:flex;flex-direction:column;gap:12px;padding:4px 0;"` -> `class="drawer-info-layout"`

**renderAnomalies function (~line 357-370):**
- `style="padding:16px;"` placeholders -> `class="drawer-placeholder"`
- `style="padding:16px;text-align:center;"` empty -> `class="drawer-placeholder"`
- `style="display:flex;flex-direction:column;gap:8px;padding:4px 0;"` -> `class="drawer-anomalies-layout"`
- `style="padding:8px 12px;border-radius:8px;background:..."` cards -> `class="drawer-anomaly-card"`
- `style="font-weight:600;color:..."` code -> `class="drawer-anomaly-card__code"`

**Notification panel (~line 626-696):**
- `style="display:none"` on notif-count -> `hidden` attribute
- `style="padding:12px 16px;font-size:13px;font-weight:700;..."` header -> `class="notif-panel-header"`
- `style="display:block;padding:10px 16px;..."` footer link -> already has `class="notif-footer"`, replace inline style by adding `notif-panel-footer` class
- `style="padding:20px;text-align:center;..."` empty state -> `class="notif-empty"`
- `style="background:..."` on notif-dot -> keep this one (dynamic color per notification type, runtime value per D-08)

**Command palette (~line 761, 806, 851):**
- `style="font-size:11px;padding:2px 6px;..."` kbd -> `class="cmd-kbd"`
- `style="padding:20px;text-align:center;..."` empty -> `class="notif-empty"` (reuse)
- `style="font-size:10px;padding:1px 5px;..."` small kbd -> `class="cmd-kbd-sm"`
  </action>
  <verify>
    <automated>REMAINING=$(grep -c 'style=' public/assets/js/core/shell.js); echo "Remaining style= count: $REMAINING"; if [ "$REMAINING" -le 3 ]; then echo "PASS: nearly all inline styles replaced"; else echo "REVIEW: $REMAINING style= remaining"; fi</automated>
  </verify>
  <done>shell.js drawer functions use class="drawer-*" instead of style="...". Remaining style= count is 3 or fewer (only dynamic runtime values like notif-dot background color).</done>
</task>

</tasks>

<verification>
- `grep -c 'style=' public/assets/js/core/shell.js` returns 3 or fewer (only dynamic runtime colors)
- `grep 'drawer-section\|drawer-label\|drawer-value\|drawer-alert' public/assets/js/core/shell.js | wc -l` returns 10+
- `grep 'drawer-section' public/assets/css/design-system.css` returns match
</verification>

<success_criteria>
Shell.js drawer render functions use CSS classes from design-system.css instead of inline styles. At most 3 inline styles remain (dynamic runtime values only).
</success_criteria>

<output>
After completion, create `.planning/phases/02-classes-css-et-inline-cleanup/02-03-SUMMARY.md`
</output>
