---
phase: 02-classes-css-et-inline-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/wizard.htmx.html
autonomous: true
requirements:
  - UI-04
must_haves:
  truths:
    - "Zero occurrence de field-input dans wizard.htmx.html"
    - "Zero occurrence de field-label dans wizard.htmx.html"
    - "Tous les select du wizard ont la classe form-select"
    - "Les champs du wizard sont visuellement styles (padding, border, radius)"
  artifacts:
    - path: "public/wizard.htmx.html"
      provides: "Wizard avec classes design-system standard"
      contains: "form-input"
  key_links:
    - from: "public/wizard.htmx.html"
      to: "public/assets/css/design-system.css"
      via: "form-input, form-label, form-select classes"
      pattern: "form-input|form-label|form-select"
---

<objective>
Migrate all wizard form fields from legacy field-input/field-label classes to design-system standard form-input/form-label/form-select classes.

Purpose: The wizard currently uses field-input which has no styling since login.css is not loaded on the wizard page, leaving fields unstyled. Switching to form-input/form-select from design-system.css fixes this.
Output: wizard.htmx.html with zero legacy class references.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-classes-css-et-inline-cleanup/02-CONTEXT.md
@public/wizard.htmx.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace wizard field classes with design-system classes</name>
  <files>public/wizard.htmx.html</files>
  <action>
In public/wizard.htmx.html, perform the following replacements:

1. Replace all `class="field-input"` on `<input>` and `<textarea>` elements with `class="form-input"` (12 occurrences)
2. Replace all `class="field-input"` on `<select>` elements with `class="form-select"` (4 occurrences: wizType, wizQuorum, wizDefaultMaj, resoMaj)
3. Replace all `class="field-label"` with `class="form-label"` (14 occurrences)
4. Keep `field-hint` and `field-error-msg` unchanged — per D-04/D-05 these have no design-system equivalent
5. Do NOT modify wizard.js — per D-05 only HTML class attributes change

Verify each replacement preserves the id, name, placeholder, aria-* attributes exactly as-is.
  </action>
  <verify>
    <automated>grep -c 'field-input' public/wizard.htmx.html | grep -q '^0$' && grep -c 'field-label' public/wizard.htmx.html | grep -q '^0$' && grep -c 'form-input' public/wizard.htmx.html && grep -c 'form-select' public/wizard.htmx.html && grep -c 'form-label' public/wizard.htmx.html && echo "PASS: all classes migrated"</automated>
  </verify>
  <done>Zero field-input and zero field-label in wizard.htmx.html. form-input count >= 8, form-select count >= 3, form-label count >= 12.</done>
</task>

</tasks>

<verification>
- `grep -c 'field-input' public/wizard.htmx.html` returns 0
- `grep -c 'field-label' public/wizard.htmx.html` returns 0
- `grep 'form-input\|form-select\|form-label' public/wizard.htmx.html | wc -l` returns 30+
</verification>

<success_criteria>
All wizard form fields use design-system standard classes. No field-input or field-label remains in the wizard.
</success_criteria>

<output>
After completion, create `.planning/phases/02-classes-css-et-inline-cleanup/02-01-SUMMARY.md`
</output>
