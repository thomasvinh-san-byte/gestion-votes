---
phase: 03-coherence-cross-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Controller/PageController.php
  - public/partials/sidebar.html
  - public/index.html
  - public/dashboard.htmx.html
  - public/wizard.htmx.html
  - public/validate.htmx.html
  - public/analytics.htmx.html
  - public/public.htmx.html
  - public/report.htmx.html
  - public/docs.htmx.html
  - public/trust.htmx.html
  - public/vote.htmx.html
  - public/postsession.htmx.html
  - public/operator.htmx.html
  - public/hub.htmx.html
  - public/meetings.htmx.html
  - public/users.htmx.html
  - public/help.htmx.html
  - public/members.htmx.html
  - public/admin.htmx.html
  - public/settings.htmx.html
  - public/archives.htmx.html
  - public/audit.htmx.html
  - public/email-templates.htmx.html
autonomous: true
requirements: [UI-07]

must_haves:
  truths:
    - "All pages display the same version string"
    - "Version comes from a single source (PHP constant)"
    - "No hardcoded v3.19, v4.3, v4.4, or v5.0 in HTML files"
  artifacts:
    - path: "app/Controller/PageController.php"
      provides: "Version constant and injection logic"
      contains: "APP_VERSION"
    - path: "public/partials/sidebar.html"
      provides: "Version placeholder instead of hardcoded"
      contains: "%%APP_VERSION%%"
  key_links:
    - from: "app/Controller/PageController.php"
      to: "public/*.htmx.html"
      via: "str_replace %%APP_VERSION%%"
      pattern: "str_replace.*APP_VERSION"
---

<objective>
Unify all version strings across the application to a single PHP-defined source.

Purpose: Eliminate version fragmentation (v3.19/v4.3/v4.4/v5.0 displayed simultaneously) so the app presents a coherent identity.
Output: Single version constant in PageController, all 21 htmx pages + sidebar + landing use it.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-coherence-cross-pages/03-CONTEXT.md
@app/Controller/PageController.php
@public/partials/sidebar.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create version constant and add injection to PageController</name>
  <files>app/Controller/PageController.php, public/partials/sidebar.html</files>
  <action>
1. Add a class constant to PageController: `public const APP_VERSION = 'v2.0';` (using v2.0 per Claude's discretion — clean major version for the unified display).

2. In the `serve()` method, after the CSP nonce replacement, add a second str_replace:
   `$html = str_replace('%%APP_VERSION%%', self::APP_VERSION, $html);`

3. The sidebar partial is included within .htmx.html pages, so its placeholder will be replaced by the same mechanism. In `public/partials/sidebar.html` line 141, replace `v3.19` with `%%APP_VERSION%%`.

4. Verify syntax: `php -l app/Controller/PageController.php`
  </action>
  <verify>
    <automated>php -l app/Controller/PageController.php && grep -q 'APP_VERSION' app/Controller/PageController.php && grep -q '%%APP_VERSION%%' public/partials/sidebar.html</automated>
  </verify>
  <done>PageController has APP_VERSION constant and injects it via str_replace. Sidebar uses placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all hardcoded versions in HTML files with placeholder</name>
  <files>public/dashboard.htmx.html, public/wizard.htmx.html, public/validate.htmx.html, public/analytics.htmx.html, public/public.htmx.html, public/report.htmx.html, public/docs.htmx.html, public/trust.htmx.html, public/vote.htmx.html, public/postsession.htmx.html, public/operator.htmx.html, public/hub.htmx.html, public/meetings.htmx.html, public/users.htmx.html, public/help.htmx.html, public/members.htmx.html, public/admin.htmx.html, public/settings.htmx.html, public/archives.htmx.html, public/audit.htmx.html, public/email-templates.htmx.html, public/index.html</files>
  <action>
1. In all 21 .htmx.html files, find the footer version span pattern `<span class="text-muted">vX.XX</span>` and replace the version text with `%%APP_VERSION%%`. The versions to replace are: v3.19, v4.3, v4.4.

   Specific files and their current versions:
   - v3.19: dashboard, validate, analytics, public, report, docs, trust, vote, postsession, wizard (10 files)
   - v4.3: operator, hub, admin, settings, members, audit (6 files)
   - v4.4: archives, meetings, users, help, email-templates (5 files)

2. For `public/index.html` (line 344): replace `AG-VOTE v5.0` with `AG-VOTE %%APP_VERSION%%`. Since index.html is NOT served through PageController (it's a static file served directly by nginx/Apache), add a small inline JS snippet that replaces the placeholder. Alternative approach: since the landing page is static and rarely changes, simply hardcode `v2.0` there. Use hardcoded approach for simplicity — update line 344 to show `v2.0` instead of `v5.0`. Add a comment: `<!-- Version: update when PageController::APP_VERSION changes -->`.

3. Also remove the version references from GO-LIVE-STATUS comments at the top of files (lines like "rebuild v4.3", "rebuild v4.4") — these are internal dev comments that are now misleading. Remove only the version part from the comment, keep the rest of the comment intact.

4. Verify no hardcoded version remains: `grep -rn "v3\.19\|v4\.3\|v4\.4\|v5\.0" public/*.htmx.html public/partials/sidebar.html public/index.html` should return nothing (except GO-LIVE-STATUS comments if kept).
  </action>
  <verify>
    <automated>grep -rn '"text-muted">v[0-9]' public/*.htmx.html public/partials/sidebar.html | grep -v APP_VERSION; test $? -eq 1</automated>
  </verify>
  <done>All 21 htmx pages use %%APP_VERSION%% placeholder. index.html shows v2.0. No hardcoded version strings remain in footer spans.</done>
</task>

</tasks>

<verification>
- `grep -rn 'APP_VERSION' app/Controller/PageController.php` shows constant and str_replace
- `grep -c '%%APP_VERSION%%' public/*.htmx.html` shows 21 files with placeholder
- `php -l app/Controller/PageController.php` passes syntax check
- No v3.19/v4.3/v4.4/v5.0 in footer spans
</verification>

<success_criteria>
A single version string (v2.0) is displayed across all pages. The version is defined once in PageController::APP_VERSION and injected at serve time.
</success_criteria>

<output>
After completion, create `.planning/phases/03-coherence-cross-pages/03-01-SUMMARY.md`
</output>
