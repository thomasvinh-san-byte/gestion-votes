---
quick_id: 260430-86c
type: execute
autonomous: true
requirements: [TECH-01]
files_modified:
  - .planning/quick/260430-86c-consolidation-73-box-shadow-57-borders-v/260430-86c-INVENTORY.md
  - public/assets/css/design-system.css
  - public/assets/css/**/*.css  # Tâche 3 — fichiers haute confiance uniquement, listés dans l'INVENTORY
must_haves:
  truths:
    - "Un inventaire exhaustif des 73 box-shadow et 57 border distinctes existe avec mapping et niveau de confiance"
    - "Les tokens manquants identifiés (--shadow-xs + shorthand --border-*) sont définis dans design-system.css"
    - "Les occurrences haute-confiance sont remplacées par les tokens correspondants, sans régression visuelle"
    - "Les occurrences basse-confiance restent en l'état avec documentation pour Phase 2/3"
  artifacts:
    - path: ".planning/quick/260430-86c-consolidation-73-box-shadow-57-borders-v/260430-86c-INVENTORY.md"
      provides: "Inventaire CSV des variantes box-shadow + border avec mapping et confiance"
    - path: "public/assets/css/design-system.css"
      provides: "Tokens --shadow-xs et shorthand --border-* ajoutés"
  key_links:
    - from: "260430-86c-INVENTORY.md"
      to: "public/assets/css/design-system.css"
      via: "tokens manquants identifiés en T1 → ajoutés en T2"
      pattern: "--shadow-xs|--border-default|--border-subtle|--border-strong|--border-dashed|--border-focus"
    - from: "public/assets/css/*.css (fichiers haute confiance)"
      to: "design-system.css tokens"
      via: "remplacement var() en T3"
      pattern: "var\\(--shadow-|var\\(--border-"
---

<objective>
TECH-01 — Consolider les 73 box-shadow et 57 border distinctes du dossier `public/assets/css/` vers les tokens du design system (Schoger S-2).

Purpose: Réduire la dette CSS en alignant toutes les valeurs sur les tokens existants/à créer. Ce quick traite uniquement les remplacements haute-confiance (mapping évident, pas d'ambiguïté visuelle) — les cas basse-confiance sont documentés pour traitement en Phase 2/3 quand chaque fichier sera retravaillé.

Output: Un INVENTORY.md, des tokens supplémentaires dans design-system.css, et des fichiers CSS nettoyés (commit par fichier).
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/gestion-votes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/gestion-votes/CLAUDE.md
@/home/user/gestion-votes/.planning/REQUIREMENTS.md
@/home/user/gestion-votes/.planning/v2.3-UX-REVIEW-SCHOGER.md
@/home/user/gestion-votes/public/assets/css/design-system.css

<interfaces>
<!-- Tokens déjà présents dans design-system.css — utiliser tels quels, ne PAS redéfinir -->

Shadow tokens existants (7) :
  --shadow-sm
  --shadow-md
  --shadow-lg
  --shadow-inner
  --shadow-inset-sm
  --shadow-focus
  --shadow-focus-danger

Border color tokens existants (6) :
  --color-border
  --color-border-alpha
  --color-border-subtle
  --color-border-strong
  --color-border-dash
  --color-border-focus

Tokens à AJOUTER (T2) :
  --shadow-xs           : pour shadows 1px subtiles (ombres très légères type "0 1px 2px rgba(0,0,0,0.05)")
  --border-default      : 1px solid var(--color-border)
  --border-subtle       : 1px solid var(--color-border-subtle)
  --border-strong       : 1px solid var(--color-border-strong)
  --border-dashed       : 1px dashed var(--color-border-dash)
  --border-focus        : 2px solid var(--color-border-focus)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : Audit exhaustif (INVENTORY.md)</name>
  <files>.planning/quick/260430-86c-consolidation-73-box-shadow-57-borders-v/260430-86c-INVENTORY.md</files>
  <read_first>
    - public/assets/css/design-system.css (relire les tokens existants — voir <interfaces>)
    - .planning/v2.3-UX-REVIEW-SCHOGER.md (section S-2 — comprendre la baseline 73/57)
  </read_first>
  <action>
    Produire un inventaire complet des box-shadow et border distinctes dans `public/assets/css/`.

    Étape A — Extraction box-shadow :
    ```bash
    grep -rn 'box-shadow:' public/assets/css/ \
      | grep -v '^Binary' \
      | sed -E 's/.*box-shadow:\s*//; s/;.*//' \
      | sort -u > /tmp/shadows-distinct.txt
    grep -rn 'box-shadow:' public/assets/css/ > /tmp/shadows-occurrences.txt
    ```

    Étape B — Extraction border (border, border-top, border-bottom, border-left, border-right) :
    ```bash
    grep -rnE '^\s*border(-top|-bottom|-left|-right)?:\s*[0-9]' public/assets/css/ \
      | grep -v '^Binary' \
      | sed -E 's/.*border[a-z-]*:\s*//; s/;.*//' \
      | sort -u > /tmp/borders-distinct.txt
    grep -rnE '^\s*border(-top|-bottom|-left|-right)?:\s*[0-9]' public/assets/css/ > /tmp/borders-occurrences.txt
    ```

    Étape C — Construire INVENTORY.md avec deux sections (## Box-shadow puis ## Borders), chacune avec un tableau Markdown :

    | Valeur source | Token suggéré | Confiance | # occurrences | Fichiers (file:line, max 5 + "...") |
    |---|---|---|---|---|

    Règles de mapping (haute confiance) :
    - `none` → `none` (pas de remplacement nécessaire, exclure)
    - Valeur strictement identique à un token existant (au pixel/alpha près < 2%) → HAUTE
    - Shadow `0 1px 2px rgba(0,0,0,0.0X)` avec X ≤ 6 → `var(--shadow-xs)` (HAUTE après création T2)
    - `1px solid <couleur du token --color-border*>` → `var(--border-default|subtle|strong)` (HAUTE après création T2)
    - `1px dashed ...` → `var(--border-dashed)` (HAUTE après création T2)
    - `2px solid` sur élément focus → `var(--border-focus)` (HAUTE)

    Règles de mapping (basse confiance) :
    - Shadow multi-layered custom (≥ 2 layers non-token) → BASSE
    - Border avec couleur ad-hoc (hex/rgb non présent dans tokens) → BASSE
    - Toute valeur où l'intention design est ambiguë → BASSE

    Étape D — Ajouter une section `## Résumé` à la fin :
    - Total distinct box-shadow : X (vs baseline 73)
    - Total distinct borders : Y (vs baseline 57)
    - Haute confiance (replaçable) : Hb shadows / Hb borders
    - Basse confiance (à reporter) : Lb shadows / Lb borders
    - Fichiers couverts en T3 : liste de paths

    Étape E — Commit :
    ```bash
    git add .planning/quick/260430-86c-consolidation-73-box-shadow-57-borders-v/260430-86c-INVENTORY.md
    git commit -m "chore(quick-260430-86c): add box-shadow and border inventory for TECH-01"
    ```
  </action>
  <verify>
    <automated>test -f .planning/quick/260430-86c-consolidation-73-box-shadow-57-borders-v/260430-86c-INVENTORY.md && grep -c '^|' .planning/quick/260430-86c-consolidation-73-box-shadow-57-borders-v/260430-86c-INVENTORY.md | awk '$1 >= 20 { exit 0 } { exit 1 }' && grep -qE 'HAUTE|BASSE' .planning/quick/260430-86c-consolidation-73-box-shadow-57-borders-v/260430-86c-INVENTORY.md && grep -q '## Résumé' .planning/quick/260430-86c-consolidation-73-box-shadow-57-borders-v/260430-86c-INVENTORY.md</automated>
  </verify>
  <done>
    INVENTORY.md existe, contient deux tableaux (shadows + borders), au moins 20 lignes de tableau, niveaux de confiance HAUTE/BASSE annotés, section ## Résumé chiffrée. Commité.
  </done>
</task>

<task type="auto">
  <name>Tâche 2 : Ajout des tokens manquants dans design-system.css</name>
  <files>public/assets/css/design-system.css</files>
  <read_first>
    - public/assets/css/design-system.css (localiser le bloc :root des shadow tokens et border tokens)
    - 260430-86c-INVENTORY.md (confirmer que --shadow-xs et --border-* shorthand sont effectivement nécessaires)
  </read_first>
  <action>
    Ajouter dans le bloc `:root { ... }` (ou bloc tokens existant) de `public/assets/css/design-system.css`, à proximité des tokens existants équivalents :

    ```css
    /* Shadow XS — for very subtle 1px shadows (TECH-01 / Schoger S-2) */
    --shadow-xs: 0 1px 2px rgba(0, 0, 0, 0.05);

    /* Border shorthand tokens (TECH-01 / Schoger S-2) */
    --border-default: 1px solid var(--color-border);
    --border-subtle:  1px solid var(--color-border-subtle);
    --border-strong:  1px solid var(--color-border-strong);
    --border-dashed:  1px dashed var(--color-border-dash);
    --border-focus:   2px solid var(--color-border-focus);
    ```

    Placer `--shadow-xs` juste avant `--shadow-sm` ; placer le bloc `--border-*` juste après les `--color-border-*`.

    Ne PAS toucher aux tokens existants. Ne PAS dupliquer.

    Commit :
    ```bash
    git add public/assets/css/design-system.css
    git commit -m "chore(quick-260430-86c): add --shadow-xs and --border-* shorthand tokens"
    ```
  </action>
  <verify>
    <automated>grep -c -- '--shadow-xs:' public/assets/css/design-system.css | awk '$1 == 1 { exit 0 } { exit 1 }' && grep -c -- '--border-default:' public/assets/css/design-system.css | awk '$1 == 1 { exit 0 } { exit 1 }' && grep -c -- '--border-subtle:' public/assets/css/design-system.css | awk '$1 == 1 { exit 0 } { exit 1 }' && grep -c -- '--border-strong:' public/assets/css/design-system.css | awk '$1 == 1 { exit 0 } { exit 1 }' && grep -c -- '--border-dashed:' public/assets/css/design-system.css | awk '$1 == 1 { exit 0 } { exit 1 }' && grep -c -- '--border-focus:' public/assets/css/design-system.css | awk '$1 == 1 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    Six nouveaux tokens (`--shadow-xs`, `--border-default`, `--border-subtle`, `--border-strong`, `--border-dashed`, `--border-focus`) présents exactement une fois chacun dans design-system.css. Commité.
  </done>
</task>

<task type="auto">
  <name>Tâche 3 : Remplacement haute-confiance (commit par fichier)</name>
  <files>public/assets/css/*.css (uniquement les fichiers listés "Fichiers couverts en T3" dans INVENTORY.md)</files>
  <read_first>
    - 260430-86c-INVENTORY.md (sections HAUTE confiance + liste "Fichiers couverts en T3")
    - public/assets/css/design-system.css (confirmer présence des nouveaux tokens)
  </read_first>
  <action>
    Pour CHAQUE fichier CSS listé en HAUTE confiance dans INVENTORY.md (un fichier = un commit) :

    1. Lire le fichier complet.
    2. Pour chaque occurrence HAUTE confiance documentée dans INVENTORY :
       - Remplacer la valeur littérale par `var(--<token>)` correspondant
       - Conserver les commentaires éventuels en bout de ligne
       - Ne PAS toucher aux occurrences BASSE confiance (les laisser intactes)
       - Ne PAS toucher aux valeurs `none`, `inherit`, `0`, `unset`
    3. Sauvegarder.
    4. Vérifier que le fichier est syntaxiquement valide (pas d'accolade orpheline) :
       ```bash
       awk -v file="$f" '{open += gsub(/\{/, "{"); close += gsub(/\}/, "}")} END { if (open != close) { print "BRACE MISMATCH in " file ": " open " { vs " close " }"; exit 1 } }' "$f"
       ```
    5. Commit individuel :
       ```bash
       git add public/assets/css/<fichier>.css
       git commit -m "chore(quick-260430-86c): consolidate shadows/borders to tokens in <fichier>.css"
       ```

    À la fin, mettre à jour INVENTORY.md avec une section `## Replacement Report` :
    | Fichier | Shadows remplacées / total | Borders remplacées / total | Reportées (basse conf) |
    |---|---|---|---|

    Commit final du report :
    ```bash
    git add .planning/quick/260430-86c-consolidation-73-box-shadow-57-borders-v/260430-86c-INVENTORY.md
    git commit -m "chore(quick-260430-86c): document replacement report in INVENTORY"
    ```

    GARDE-FOUS :
    - Si un fichier a 0 occurrence haute-confiance, NE PAS le toucher.
    - Si en cours de remplacement un mapping semble douteux (couleur source ≠ token color à plus de 5%), reclasser en BASSE et passer.
    - Maximum 1 retry par fichier en cas d'échec verify : si la deuxième tentative échoue, reverter le fichier (`git checkout HEAD -- <fichier>`) et le marquer "skipped" dans le Replacement Report.
  </action>
  <verify>
    <automated>bash -c 'for f in $(git diff --name-only HEAD~ HEAD -- "public/assets/css/*.css" | grep -v design-system.css); do awk "{open += gsub(/\\{/, \"{\"); close += gsub(/\\}/, \"}\")} END { if (open != close) exit 1 }" "$f" || { echo "BRACE FAIL: $f"; exit 1; }; done && grep -q "## Replacement Report" .planning/quick/260430-86c-consolidation-73-box-shadow-57-borders-v/260430-86c-INVENTORY.md'</automated>
  </verify>
  <done>
    Tous les fichiers CSS modifiés ont des accolades équilibrées. INVENTORY.md contient la section ## Replacement Report avec un compteur par fichier. Chaque fichier touché a son propre commit `chore(quick-260430-86c): ...`. Aucune occurrence basse-confiance n'a été touchée.
  </done>
</task>

</tasks>

<verification>
- INVENTORY.md liste exhaustivement les variantes distinctes (shadows + borders) avec mapping et confiance.
- design-system.css contient les 6 nouveaux tokens, exactement une fois chacun.
- Les fichiers CSS modifiés en T3 ont des accolades équilibrées (vérification syntaxique légère).
- INVENTORY.md ## Replacement Report quantifie ce qui a été fait vs reporté.
- Chaque fichier modifié a son commit individuel (`git log --oneline | grep 'quick-260430-86c'` montre la séquence).
</verification>

<success_criteria>
- [ ] `260430-86c-INVENTORY.md` existe, structuré, chiffré
- [ ] `--shadow-xs` + 5 `--border-*` shorthand tokens présents dans design-system.css
- [ ] Au moins 1 fichier CSS (hors design-system.css) a vu des occurrences haute-confiance remplacées par `var(--shadow-*)` ou `var(--border-*)`
- [ ] Aucun fichier CSS modifié n'a d'accolade orpheline
- [ ] Aucune occurrence BASSE confiance n'a été touchée (vérifiable en croisant INVENTORY avec git diff)
- [ ] Commits format `chore(quick-260430-86c): ...`, un par étape logique
</success_criteria>

<output>
Après complétion, créer `.planning/quick/260430-86c-consolidation-73-box-shadow-57-borders-v/260430-86c-SUMMARY.md` avec :
- Compteurs finaux (shadows distinctes avant/après dans les fichiers traités, borders idem)
- Liste des fichiers touchés en T3
- Liste des occurrences reportées en Phase 2/3 (par fichier)
- Note de risque résiduel : "Régression visuelle possible — recommander un visual regression test ou une revue manuelle des pages clés (login, dashboard, meeting, vote)"
</output>
