---
phase: 01-coherence-visuelle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v2.7-VISUAL-AUDIT.md
  - .planning/v2.7-VISUAL-AUDIT-screenshots/
autonomous: true
requirements:
  - VISUAL-V27-01
must_haves:
  truths:
    - "Un fichier `.planning/v2.7-VISUAL-AUDIT.md` existe avec un tableau de scoring 0-3 sur ≥30 écrans nommés"
    - "Pour chaque écran le score est justifié par un constat précis (typo, tokens, HTML legacy, transitions, interactions)"
    - "Le backlog de migration final liste, par requirement (V27-02..06), la liste des sites concrets à migrer (chemin:ligne) que 01-02 et 01-03 vont consommer"
    - "Les baselines `grep` (counts par fichier pour fonts/hex/spacing/transitions/modals legacy) sont gelées dans l'audit comme métriques avant/après"
  artifacts:
    - path: ".planning/v2.7-VISUAL-AUDIT.md"
      provides: "Audit cartographique scoring 0-3 + baselines grep + backlog de migration"
      contains: "## Scoring par écran"
    - path: ".planning/v2.7-VISUAL-AUDIT.md"
      provides: "Liste des fichiers concrets à migrer pour 01-02 et 01-03"
      contains: "## Backlog de migration"
  key_links:
    - from: ".planning/v2.7-VISUAL-AUDIT.md"
      to: "01-02-PLAN.md (structural migrations)"
      via: "Backlog de migration → tâches"
      pattern: "Backlog de migration"
    - from: ".planning/v2.7-VISUAL-AUDIT.md"
      to: "01-03-PLAN.md (spacing + audit final)"
      via: "Liste fichiers/sites spacing + baselines grep"
      pattern: "Spacing baseline"
---

<objective>
Produire l'audit cartographique cohérence visuelle de v2.7 — score 0-3 par écran (≥30 écrans),
baselines grep (fonts/hex/spacing/transitions/modals legacy) gelées dans le document,
et backlog concret de migration (chemin:ligne par requirement V27-02..06) que les plans 01-02 et 01-03
vont exécuter sans avoir à re-explorer le codebase.

Purpose : sans cet audit, la migration devient une chasse au trésor. Cet audit est le contrat
d'exécution pour 01-02 (structural) et 01-03 (spacing + clôture).

Output : `.planning/v2.7-VISUAL-AUDIT.md` (tableau scoring + baselines + backlog) +
optionnellement `.planning/v2.7-VISUAL-AUDIT-screenshots/` (références visuelles si capturables).
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/gestion-votes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/gestion-votes/.planning/PROJECT.md
@/home/user/gestion-votes/.planning/ROADMAP.md
@/home/user/gestion-votes/.planning/STATE.md
@/home/user/gestion-votes/.planning/REQUIREMENTS.md
@/home/user/gestion-votes/CLAUDE.md
@/home/user/gestion-votes/.planning/milestones/v2.6-TOKENS-AUDIT-FINAL.md

<interfaces>
<!-- État actuel mesuré (baselines à figer dans le livrable) -->

Inventaire écrans (à scorer) — 28 pages dans `public/` + login.html :
- Operateur : `operator.htmx.html`, `vote.htmx.html`
- Admin/Org : `dashboard.htmx.html`, `meetings.htmx.html`, `members.htmx.html`,
  `procurations.htmx.html` (si présent — sinon documenter absent),
  `users.htmx.html`, `admin.htmx.html`, `admin-error-stats.htmx.html`,
  `email-templates.htmx.html`, `settings.htmx.html`, `wizard.htmx.html`
- Editorial : `audit.htmx.html`, `trust.htmx.html`, `archives.htmx.html`,
  `report.htmx.html`, `postsession.htmx.html`
- Public/Hub : `hub.htmx.html`, `public.htmx.html`, `analytics.htmx.html`,
  `validate.htmx.html`, `help.htmx.html`, `docs.htmx.html`
- Auth : `login.html`, `index.html`
- (note : pas de `setup.html` ni `procurations.htmx.html` ni `admin/*` sous-dir
   à la date de la planification — documenter cela dans l'audit)

CSS files (24) sous `public/assets/css/` : `admin.css analytics.css app.css
archives.css audit.css doc.css editorial.css email-templates.css help.css
hub.css landing.css login.css meetings.css members.css operator.css pages.css
postsession.css public.css report.css settings.css trust.css users.css
validate.css vote.css wizard.css` (+ `design-system.css` exclu de l'audit) +
`components/{ag-health-bar,ag-integrity-modal,ag-shortcuts-overlay,op-action-disclosure}.css`.

JS web components disponibles (réutilisables, ne pas réinventer) :
`public/assets/js/components/` contient `ag-modal.js`, `ag-empty-state.js`,
`ag-card`-pattern (CSS only, pas de JS), `ag-badge`, `ag-spinner`, `ag-toast`,
`ag-popover`, `ag-quorum-bar`, `ag-health-bar`, `ag-integrity-modal`,
`ag-vote-button`, `ag-pagination`, `ag-page-header`, `ag-stepper`, `ag-kpi`,
`ag-mini-bar`, `ag-donut`, `ag-confirm`, `ag-pdf-viewer`, `ag-searchable-select`,
`ag-time-input`, `ag-tz-picker`, `ag-tooltip`, `ag-scroll-top`, `ag-breadcrumb`.

Baselines `grep` mesurées le 2026-05-05 (à confirmer/gelifier dans le livrable) :
- font-family Arial/Helvetica/Verdana/Times brut hors design-system + emails : **0 hits** (déjà conforme)
- hex `#hhh|hhhhhh` hors design-system + emails : **8 hits** (tous dans editorial.css `@media print` — tous justifiables N&B)
- transition: Ns brut hors design-system : **27 hits** (wizard.css 8, operator.css 6, public.css 5, vote.css 3, hub.css 3, help.css 2)
- modale legacy `<dialog>` ou `class="modal"` (hors `<ag-modal>`) : **27 hits** (members 1 dialog, validate 3, operator 3, trust ≥17 audit-modal-row classes — la majorité = sub-classes d'un modal déjà migré, à vérifier site par site)
- padding/margin :Npx hors design-system : **~525 hits** répartis (operator 102, vote 79, wizard 65, doc 57, analytics 35, hub 29, public 18, pages 13, members 13, app 12, help 11, users 9, meetings 8, report 7, postsession 7, landing 7, editorial 7, archives 6, login 5, settings 4, email-templates 4, admin 4, validate 3, audit 3)

Tokens disponibles dans design-system.css (cibles de migration) :
- spacing : `--space-{0,1,2,3,4,5,6,7,8}` + `--space-px` (vérifier l'échelle exacte dans design-system.css)
- motion : `--motion-{fast,base,slow,…}` (vérifier nomenclature exacte)
- ease : `--ease-standard, --ease-emphasized, --ease-default, --ease-in, --ease-out, --ease-spring, --ease-bounce`
- color : `--color-{primary,danger,success,bg,surface,border,text,…}`

Convention scoring (à figer dans le livrable) :
- 0 = "v1 brut" : aucun token utilisé OU font-family système OU HTML legacy `<dialog>` non-migré sur l'écran
- 1 = "partiel" : tokens partiellement appliqués mais ≥3 sites de spacing/transition/hex hardcoded
- 2 = "presque" : ≤3 sites hardcoded restants, typo conforme, modales migrées
- 3 = "pleinement migré" : 0 hex, 0 spacing brut, 0 transition brut, modales `<ag-modal>`, typo Inter/Newsreader/Mono selon contexte
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 : Geler les baselines grep et inventorier l'arbre des écrans</name>
  <files>
    .planning/v2.7-VISUAL-AUDIT.md
  </files>
  <action>
    Créer `.planning/v2.7-VISUAL-AUDIT.md` avec en-tête, contexte (lien vers REQUIREMENTS V27-01..06,
    référence au scoring v2.6-TOKENS-AUDIT-FINAL.md), et la section `## Baselines grep (figées 2026-05-XX)`.

    Pour chaque baseline, exécuter les commandes ci-dessous, capturer le count, et coller le résultat
    avec le détail par fichier (count > 0). L'audit DOIT contenir les commandes exactes utilisées
    pour permettre la re-mesure post-migration en 01-02 / 01-03.

    Commandes (lancer depuis racine repo) :
    1. fonts brut :
       `grep -rnE "font-family:.*(Arial|Helvetica|Verdana|Times)" public/assets/css/ --include="*.css" | grep -v design-system.css | grep -v email`
    2. hex hardcoded :
       `for f in public/assets/css/*.css; do bn=$(basename "$f"); [[ "$bn" == "design-system.css" ]] && continue; [[ "$bn" == email* ]] && continue; c=$(grep -cE "#[0-9a-fA-F]{3,6}\b" "$f"); [[ $c -gt 0 ]] && echo "$bn: $c"; done`
       (puis détail des hits dans editorial.css + tout autre fichier identifié)
    3. spacing brut (attention regex stricte longue forme) :
       `for f in public/assets/css/*.css; do bn=$(basename "$f"); [[ "$bn" == "design-system.css" ]] && continue; c=$(grep -cE "(padding|margin)(-(top|right|bottom|left|inline|block|inline-start|inline-end|block-start|block-end))?:\s*[0-9]+" "$f"); [[ $c -gt 0 ]] && echo "$bn: $c"; done`
    4. transitions brut :
       `for f in public/assets/css/*.css; do bn=$(basename "$f"); [[ "$bn" == "design-system.css" ]] && continue; c=$(grep -cE "transition:[^;]*[0-9]+(\.[0-9]+)?s" "$f"); [[ $c -gt 0 ]] && echo "$bn: $c"; done`
    5. modales legacy :
       `grep -rnE "<dialog\b|class=\"[^\"]*\bmodal\b" public/*.html | grep -v "ag-modal"` (puis distinguer dialog HTML brut vs sous-classes CSS de modal déjà migré)

    Exclusions explicites à documenter :
    - `design-system.css` : définition des tokens — exclu de toutes les baselines
    - `email-*.css` : hex en dur conservé (compat clients email — décision v2.2)
    - `@media print` blocs : `#000`/`#fff` acceptables N&B (décision v2.3 EDITORIAL-07)

    Inventaire écrans : lister exhaustivement les fichiers `public/*.html` et `public/*.htmx.html`
    via `ls public/*.html public/*.htmx.html`. Documenter dans `## Écrans recensés` chaque fichier
    avec son fichier CSS associé probable (déduit du nom). Si un fichier mentionné dans la roadmap
    (`procurations.htmx.html`, `setup.html`, `admin/*.htmx.html`) est absent à la date de l'audit,
    le NOTER explicitement avec mention "absent du codebase 2026-05-XX — n'apparaîtra pas dans le scoring".
  </action>
  <verify>
    <automated>test -f .planning/v2.7-VISUAL-AUDIT.md &amp;&amp; grep -q "## Baselines grep" .planning/v2.7-VISUAL-AUDIT.md &amp;&amp; grep -q "## Écrans recensés" .planning/v2.7-VISUAL-AUDIT.md</automated>
  </verify>
  <done>
    Le fichier existe, contient les 5 baselines avec counts par fichier et commandes exactes,
    l'inventaire écrans liste tous les `*.html` et `*.htmx.html` de `public/` et signale les absents.
  </done>
</task>

<task type="auto">
  <name>Task 2 : Scorer chaque écran 0-3 et bâtir le tableau scoring</name>
  <files>
    .planning/v2.7-VISUAL-AUDIT.md
  </files>
  <action>
    Ajouter à `.planning/v2.7-VISUAL-AUDIT.md` la section `## Scoring par écran` avec un tableau
    de la forme :

    | Écran | CSS associé | Score 0-3 | Justification (constat précis) | Requirements impactés |
    |---|---|---|---|---|
    | dashboard.htmx.html | dashboard ⊂ pages.css | 2 | 13 spacing brut + 0 hex + 0 transition + modales déjà OK | V27-04 |
    | operator.htmx.html | operator.css | 1 | 102 spacing + 6 transitions + 3 modales legacy à vérifier | V27-04, V27-05, V27-06 |
    | ... | ... | ... | ... | ... |

    Méthode pour chaque écran (à appliquer mécaniquement) :
    1. Identifier le CSS principal (mapping nom HTML → nom CSS, fallback `pages.css` ou `app.css`).
    2. Lire le fichier HTML : compter `<dialog>` non migrés, `class="...modal..."` non `<ag-modal>`,
       inline `style="..."` avec hex/spacing/transition.
    3. Lire le CSS associé : reporter les counts spacing/hex/transition mesurés à la Task 1.
    4. Appliquer la convention scoring (0/1/2/3 — figée dans `## Convention scoring` à créer en
       en-tête de cette section, copier la convention donnée dans `<interfaces>`).
    5. Justification = phrase d'1 ligne qui cite les counts précis et les requirements impactés.

    Cibler ≥30 lignes dans le tableau — actuellement 24 CSS + 24 HTML pages = ~28 écrans recensables.
    Si <30, ajouter les composants partiels (`partials/sidebar.html`, `partials/operator-live-tabs.html`)
    et chaque CSS components/* comme entrée distincte.

    À la fin de la section, ajouter `### Synthèse par requirement` :
    - V27-02 (typo) : N écrans en score <3 sur ce critère (probablement 0 vu baseline)
    - V27-03 (hex) : N écrans concernés (probablement 1, editorial — print justifiable)
    - V27-04 (spacing) : N écrans concernés (24 fichiers CSS)
    - V27-05 (HTML legacy) : N écrans avec modales legacy non migrées (≤27 sites bruts)
    - V27-06 (transitions) : N fichiers CSS concernés (6 fichiers)
  </action>
  <verify>
    <automated>grep -q "## Scoring par écran" .planning/v2.7-VISUAL-AUDIT.md &amp;&amp; awk '/^## Scoring par écran/,/^## /' .planning/v2.7-VISUAL-AUDIT.md | grep -cE "^\| [a-zA-Z][a-zA-Z0-9._/-]+" | awk '{ if ($1 &gt;= 30) print "OK"; else { print "FAIL: " $1 " lines &lt; 30"; exit 1 } }'</automated>
  </verify>
  <done>
    Tableau de scoring contient ≥30 lignes, chaque ligne a un score 0-3 et une justification
    avec counts numériques. Synthèse par requirement présente.
  </done>
</task>

<task type="auto">
  <name>Task 3 : Bâtir le backlog de migration concret consommable par 01-02 et 01-03</name>
  <files>
    .planning/v2.7-VISUAL-AUDIT.md
  </files>
  <action>
    Ajouter à `.planning/v2.7-VISUAL-AUDIT.md` la section `## Backlog de migration` structurée
    par requirement, prête à être consommée tâche-par-tâche par les plans suivants.

    Format pour chaque sous-section :

    ### V27-02 Typographie
    - Statut baseline : 0 hits hors design-system → CONFORME, rien à migrer.
    - Action 01-02 : ré-exécuter le grep en clôture pour verrouiller le statut.

    ### V27-03 Hex hardcoded
    - Statut baseline : 8 hits, tous dans editorial.css `@media print` (lignes 137, 145, 146, 194, 224, 232, 233, 243).
    - Action 01-02 : pour chacun, soit (a) garder comme exception documentée dans
      `.planning/v2.7-TOKENS-FINAL.md` (justification N&B print), soit (b) migrer vers
      `var(--color-{neutral-0,neutral-100})` si tokens print existent. Décision : garder + documenter.
    - Cible : ≤5 hits documentés (success criterion).

    ### V27-04 Spacing brut
    - Statut baseline : ~525 hits sur 24 fichiers CSS.
    - Liste exhaustive : reproduire la table count-par-fichier triée descendante (operator.css 102,
      vote.css 79, wizard.css 65, doc.css 57, analytics.css 35, hub.css 29, public.css 18, pages.css 13,
      members.css 13, app.css 12, help.css 11, users.css 9, meetings.css 8, report.css 7, postsession.css 7,
      landing.css 7, editorial.css 7, archives.css 6, login.css 5, settings.css 4, email-templates.css 4,
      admin.css 4, validate.css 3, audit.css 3 — réajuster avec mesures Task 1).
    - Action 01-03 : passe mécanique fichier-par-fichier, remplacement `Npx → var(--space-N)` selon mapping
      (à figer ici : 4→`--space-1`, 8→`--space-2`, 12→`--space-3`, 16→`--space-4`, 20→`--space-5`,
      24→`--space-6`, 32→`--space-7`, 40→`--space-8` — confirmer en lisant `:root` de design-system.css
      et écrire la table définitive ici).
    - Exclusions : tokens partiels (`1px`, `2px` pour bordures), valeurs fractionnaires (`0.5rem`),
      `0` (no-token-needed), `auto`, `inherit`. Notation `padding: 0 16px` → `padding: 0 var(--space-4)`.
    - Cible : ≤10 hits documentés (success criterion).

    ### V27-05 HTML legacy → composants v2.x
    - Statut baseline : 27 hits modal/dialog. Distinguer site par site via lecture des fichiers ciblés :
      * `members.htmx.html:332` `<dialog id="memberDetailDialog">` → MIGRER vers `<ag-modal>`.
      * `operator.htmx.html:1677,1723,1726` `<div class="op-quorum-modal|launch-modal-summary|launch-modal-warning">` :
        si `op-quorum-modal` est ouverte par JS comme une vraie modale → migrer ; si c'est un block inline
        décoratif → garder mais renommer (pas de blocage). Vérifier le JS associé.
      * `validate.htmx.html:207,211` `validate-modal-{warning,checkbox}` : sous-classes d'un modal probable —
        vérifier le wrapper. Si déjà `<ag-modal>` → no-op.
      * `trust.htmx.html` : ~17 occurrences `audit-modal-{row,label,value}` — sous-classes d'un modal
        déjà migré (cf. audit.htmx.html ligne 242 `<ag-modal id="auditDetailModal">`). Vérifier le
        wrapper trust.htmx.html ; probablement no-op (sub-classes CSS only).
    - Pour chaque site : statuer MIGRATE | KEEP-RENAME | NO-OP avec justification.
    - Cards ad-hoc : grep `box-shadow + border-radius` inline dans HTML (`grep -nE "style=\"[^\"]*box-shadow"
      public/*.html`) et lister sites à migrer vers pattern `.card` design-system. (Les CSS-only cards
      par classe restent OK — ce critère cible les cards inline.)
    - Cible : 0 dialog brut, 0 inline modal legacy après migration.

    ### V27-06 Transitions brut
    - Statut baseline : 27 hits. Liste site par site avec ligne précise :
      `grep -nE "transition:[^;]*[0-9]+(\.[0-9]+)?s" public/assets/css/{wizard,operator,public,vote,hub,help}.css`
      Coller le résultat dans le backlog.
    - Mapping : `0.15s`/`0.2s` → `var(--motion-fast)` si défini sinon `150ms` token équivalent ;
      `0.3s` → `var(--motion-base)` ; `0.5s`+ → `var(--motion-slow)`. Easing `ease`/`ease-in-out`
      → `var(--ease-standard)` ; `ease-out` → `var(--ease-out)` ; `linear` → `var(--ease-linear)`.
      (Confirmer la nomenclature exacte des `--motion-*` tokens en lisant design-system.css avant
      de figer le mapping.)
    - Cible : 0 hits hors design-system.

    Conclure le document par `## Critères de sortie phase 1` qui ré-énumère les success criteria
    de la roadmap (les 6 points VISUAL-V27-XX) et précise pour chacun la commande grep de validation
    finale (à ré-exécuter par 01-03 dans `v2.7-TOKENS-FINAL.md`).
  </action>
  <verify>
    <automated>grep -q "## Backlog de migration" .planning/v2.7-VISUAL-AUDIT.md &amp;&amp; grep -q "### V27-02" .planning/v2.7-VISUAL-AUDIT.md &amp;&amp; grep -q "### V27-03" .planning/v2.7-VISUAL-AUDIT.md &amp;&amp; grep -q "### V27-04" .planning/v2.7-VISUAL-AUDIT.md &amp;&amp; grep -q "### V27-05" .planning/v2.7-VISUAL-AUDIT.md &amp;&amp; grep -q "### V27-06" .planning/v2.7-VISUAL-AUDIT.md &amp;&amp; grep -q "## Critères de sortie" .planning/v2.7-VISUAL-AUDIT.md</automated>
  </verify>
  <done>
    Backlog de migration contient une sous-section pour chacun des 5 requirements V27-02..06 avec
    sites concrets (chemin:ligne) et action prescrite. Section "Critères de sortie phase 1" en
    clôture liste les commandes grep de validation finale.
  </done>
</task>

</tasks>

<verification>
- `.planning/v2.7-VISUAL-AUDIT.md` existe et passe le verify de chaque task.
- Le document est self-contained : un nouvel exécuteur Claude qui le lit doit pouvoir
  travailler sur 01-02 et 01-03 sans relire le codebase pour identifier où migrer.
- Les baselines grep figées sont les chiffres avant ; 01-03 les ré-exécutera comme métriques après.
</verification>

<success_criteria>
- VISUAL-V27-01 satisfait : audit cartographique livré avec scoring 0-3 sur ≥30 écrans + screenshots
  reference (ou note explicite si capture non possible) + liste de sites à migrer.
- Baselines grep figées dans le document — réutilisables comme avant/après.
- Aucun écran avec score 0 sans plan d'action documenté dans le backlog.
- Le document est consommable par 01-02 (structural) et 01-03 (spacing + clôture) sans re-exploration.
</success_criteria>

<output>
After completion, create `.planning/phases/01-coherence-visuelle/01-01-SUMMARY.md` listing :
- Compte final d'écrans scorés.
- Distribution des scores (combien à 0, 1, 2, 3).
- Chiffres baselines figés (5 mesures).
- Volumétrie estimée pour 01-02 (sites structural) et 01-03 (sites spacing).
- Surprises ou exclusions découvertes (ex : `procurations.htmx.html` absent — décider si à créer
  ou ignorer hors scope).
</output>
