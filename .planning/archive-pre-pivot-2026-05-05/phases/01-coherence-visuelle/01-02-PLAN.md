---
phase: 01-coherence-visuelle
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - public/assets/css/wizard.css
  - public/assets/css/operator.css
  - public/assets/css/public.css
  - public/assets/css/vote.css
  - public/assets/css/hub.css
  - public/assets/css/help.css
  - public/assets/css/editorial.css
  - public/members.htmx.html
  - public/operator.htmx.html
  - public/validate.htmx.html
  - public/trust.htmx.html
autonomous: true
requirements:
  - VISUAL-V27-02
  - VISUAL-V27-03
  - VISUAL-V27-05
  - VISUAL-V27-06
must_haves:
  truths:
    - "Le grep `font-family:.*(Arial|Helvetica|Verdana|Times)` hors design-system + emails retourne 0 hits"
    - "Le grep `transition:[^;]*[0-9]+(\\.[0-9]+)?s` hors design-system retourne 0 hits"
    - "Aucun `<dialog>` brut dans `public/*.html` qui ne soit pas `<ag-modal>` (ou justifié comme native dialog non-modal-pattern)"
    - "Les 8 hex hardcoded dans editorial.css sont soit migrés vers tokens print, soit listés comme exceptions documentées"
    - "Aucune régression visuelle observable : les pages chargent, les modales s'ouvrent et se ferment"
  artifacts:
    - path: "public/assets/css/wizard.css"
      provides: "Transitions migrées vers var(--motion-*) + var(--ease-*)"
    - path: "public/assets/css/operator.css"
      provides: "Transitions migrées vers tokens"
    - path: "public/members.htmx.html"
      provides: "Dialog memberDetailDialog migré vers <ag-modal>"
  key_links:
    - from: "transitions migrées"
      to: "design-system.css :root --motion-* / --ease-*"
      via: "var(--motion-base) var(--ease-standard)"
      pattern: "var\\(--motion-"
    - from: "members.htmx.html dialog migration"
      to: "ag-modal.js custom element"
      via: "<ag-modal id=memberDetailDialog>"
      pattern: "<ag-modal"
---

<objective>
Migrations structurelles cohérence visuelle — appliquer le backlog V27-02/03/05/06 produit
par 01-01. Trois cibles distinctes mais petites en volume :
- V27-02 typo : verify (déjà 0 hits — re-grep en clôture).
- V27-03 hex : statuer sur les 8 hits editorial.css print (migrer vers tokens print
  s'ils existent, sinon documenter exception).
- V27-05 HTML legacy : migrer le `<dialog>` brut de members.htmx.html vers `<ag-modal>`,
  statuer site par site sur les 26 autres hits modal/dialog (la majorité = sub-classes
  CSS d'un modal déjà migré → no-op).
- V27-06 transitions : migrer les 27 sites `transition: Ns` vers `var(--motion-*) var(--ease-*)`
  dans 6 fichiers CSS ciblés.

Purpose : ces migrations sont structurellement distinctes du gros morceau spacing (01-03).
Les regrouper ici les rend pertinentes (touchent peu de sites, mais critiques pour V27-02/05/06).

Output : 6 CSS files + 4 HTML files modifiés. Aucune régression visuelle. Backlog 01-01 consommé
pour V27-02/03/05/06.
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/gestion-votes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user/gestion-votes/.planning/PROJECT.md
@/home/user/gestion-votes/.planning/ROADMAP.md
@/home/user/gestion-votes/.planning/REQUIREMENTS.md
@/home/user/gestion-votes/CLAUDE.md
@/home/user/gestion-votes/.planning/v2.7-VISUAL-AUDIT.md
@/home/user/gestion-votes/.planning/phases/01-coherence-visuelle/01-01-SUMMARY.md
@/home/user/gestion-votes/public/assets/css/design-system.css

<interfaces>
<!-- Contrats clés extraits de design-system.css et des composants v2.x existants -->

Tokens motion / ease (lignes 630-641 de design-system.css) :
```css
--ease-standard:       cubic-bezier(0.2, 0, 0, 1);
--ease-emphasized:     cubic-bezier(0.05, 0.7, 0.1, 1.0);
--ease-emphasized-out: cubic-bezier(0.3, 0, 0.8, 0.15);
--ease-linear:         linear;
--ease-spring:         cubic-bezier(0.34, 1.56, 0.64, 1);
--ease-overshoot:      cubic-bezier(0.34, 1.3, 0.64, 1);
--ease-default: var(--ease-standard);
--ease-in:      cubic-bezier(0.4, 0, 1, 1);
--ease-out:     cubic-bezier(0, 0, 0.2, 1);
--ease-bounce:  var(--ease-spring);
```
À RELIRE avant migration : `grep -nE "^\s*--motion-" public/assets/css/design-system.css`
pour la liste exacte des tokens `--motion-*` (durations).

Mapping recommandé (à confirmer en lisant les valeurs réelles `--motion-*`) :
- `0.1s`, `0.15s` → `var(--motion-fast)` (≈100-150ms)
- `0.2s`, `0.25s` → `var(--motion-base)` (≈200-250ms)
- `0.3s` → `var(--motion-base)` ou `var(--motion-slow)` selon valeur exacte
- `0.5s`, `0.6s` → `var(--motion-slow)`
- ease/ease-in-out non spécifié → `var(--ease-standard)`
- ease-out → `var(--ease-out)`
- linear → `var(--ease-linear)`

Si un token `--motion-*` n'existe pas pour une valeur observée, AJOUTER le token dans
design-system.css plutôt que d'inventer une valeur ad-hoc — CLAUDE.md interdit la dette
nouvelle. Ce fichier est listé dans `files_modified` SEULEMENT si ajout de token nécessaire
(à priori : non, design-system.css devrait déjà couvrir l'échelle).

Sites transitions identifiés (27 hits, 6 fichiers) :
```
public/assets/css/wizard.css   : 8 hits
public/assets/css/operator.css : 6 hits
public/assets/css/public.css   : 5 hits
public/assets/css/vote.css     : 3 hits
public/assets/css/hub.css      : 3 hits
public/assets/css/help.css     : 2 hits
```
Lignes exactes à récupérer via :
`grep -nE "transition:[^;]*[0-9]+(\\.[0-9]+)?s" public/assets/css/{wizard,operator,public,vote,hub,help}.css`

Sites modales legacy (27 hits) — distinguer migrate vs no-op :
- `public/members.htmx.html:332` `<dialog id="memberDetailDialog" class="member-detail-dialog">` :
  MIGRATE → `<ag-modal id="memberDetailDialog" title="Détail membre" size="md">`. Vérifier
  les call-sites JS (`document.getElementById('memberDetailDialog').showModal()` →
  `.show()` sur `<ag-modal>` qui expose une API similaire). Lire `ag-modal.js` avant migration
  pour confirmer l'API publique du custom element.
- `public/operator.htmx.html:1677,1723,1726` : VÉRIFIER chaque ligne — `op-quorum-modal`,
  `launch-modal-summary`, `launch-modal-warning`. Si ce sont des div décoratifs internes à un
  modal déjà migré → NO-OP (renommer optionnel hors scope). Si modale standalone → MIGRATE.
- `public/validate.htmx.html:207,211` `validate-modal-{warning,checkbox}` : sub-classes —
  vérifier le wrapper dans le même fichier ; probablement NO-OP.
- `public/trust.htmx.html` ~17 occurrences `audit-modal-{row,label,value}` : sub-classes du
  audit detail modal qui est probablement déjà migré (pattern symétrique à audit.htmx.html
  ligne 242 qui utilise `<ag-modal>`). VÉRIFIER le wrapper de trust.htmx.html. Si déjà
  `<ag-modal>` → NO-OP (les sub-classes restent valides comme styling interne). Si c'est
  encore un wrapper legacy → MIGRATE.

Hex hardcoded — 8 hits dans editorial.css :
- ligne 137  `color: #555`        — dans @media print, gris foncé pour annotations
- ligne 145  `background: #fff !important` — print
- ligne 146  `color: #000 !important`      — print
- ligne 194  `border-top: 1px solid #000`  — print
- ligne 224  `color: #555`                 — print
- ligne 232  `color: #555`                 — print
- ligne 233  `border-bottom: 1px solid #000` — print
- ligne 243  `color: #000`                 — print

Décision pré-cadrée : ces hits sont dans `@media print` et expriment du noir / gris d'imprimante.
Si design-system.css définit `--color-print-fg`, `--color-print-bg`, `--color-print-muted` →
migrer. Sinon, GARDER ces 8 hits comme exceptions documentées dans v2.7-TOKENS-FINAL.md (cible
roadmap = ≤5 hits documentés ; on serait à 8 mais tous justifiés print). Si on est >5,
01-03 (clôture audit final) doit soit (a) ajouter les tokens print à design-system.css et
migrer ces 8 sites en boucle de finition, soit (b) renégocier la cible avec écrit dans
TOKENS-FINAL.md. Dans le doute : migrer si tokens print existent, sinon documenter.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 : Migrer les 27 transitions hardcoded vers tokens motion + ease (V27-06)</name>
  <files>
    public/assets/css/wizard.css,
    public/assets/css/operator.css,
    public/assets/css/public.css,
    public/assets/css/vote.css,
    public/assets/css/hub.css,
    public/assets/css/help.css
  </files>
  <action>
    Étape A — confirmer les tokens `--motion-*` exacts :
    `grep -nE "^\s*--motion-" public/assets/css/design-system.css` (lire valeurs).
    Figer le mapping durée/token dans le diff (ex commit msg).

    Étape B — pour chaque fichier (wizard.css, operator.css, public.css, vote.css, hub.css, help.css)
    dans cet ordre :
    1. `grep -nE "transition:[^;]*[0-9]+(\\.[0-9]+)?s" <fichier>` pour lister les sites.
    2. Pour chaque site, remplacer la durée brute par le token motion correspondant et l'easing
       brut par le token ease correspondant. Garder la propriété animée et la structure de la
       déclaration intacte. Exemples :
       - `transition: all 0.2s ease;` → `transition: all var(--motion-base) var(--ease-standard);`
       - `transition: opacity 0.15s ease-out;` → `transition: opacity var(--motion-fast) var(--ease-out);`
       - `transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);` → si le bezier est ≈ ease-standard
         (vérifier proches), → `transition: transform var(--motion-base) var(--ease-standard);`
         sinon laisser le bezier custom et seulement migrer la durée.
    3. Si une durée n'a pas de token équivalent dans `--motion-*`, NE PAS inventer une valeur :
       soit ajouter le token dans design-system.css (avec justification commit), soit garder le hit
       et le documenter dans v2.7-VISUAL-AUDIT.md `## Exceptions transitions` avec rationale.

    Étape C — re-grep de validation :
    `grep -rnE "transition:[^;]*[0-9]+(\\.[0-9]+)?s" public/assets/css/ --include="*.css" | grep -v design-system.css | wc -l`
    DOIT retourner 0 (ou égal au nombre d'exceptions documentées avec justification écrite).

    Aucune régression visuelle attendue — les valeurs tokenisées sont identiques aux valeurs originales.
  </action>
  <verify>
    <automated>test "$(grep -rnE 'transition:[^;]*[0-9]+(\\.[0-9]+)?s' public/assets/css/ --include='*.css' | grep -v design-system.css | wc -l)" -le 3</automated>
  </verify>
  <done>
    grep transitions brutes hors design-system ≤3 hits (tolérance pour exceptions documentées).
    Idéal = 0. Tous les fichiers passent `php -l` non applicable (CSS) — pas de validation syntaxe
    PHP nécessaire ici.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 : Migrer le dialog members.htmx.html vers ag-modal et statuer sur les 26 autres sites (V27-05)</name>
  <files>
    public/members.htmx.html,
    public/operator.htmx.html,
    public/validate.htmx.html,
    public/trust.htmx.html
  </files>
  <action>
    Étape A — lire `public/assets/js/components/ag-modal.js` (≤200 lignes attendu) pour confirmer
    l'API publique : nom de la méthode `.show()/.open()/.close()`, attributs supportés
    (`title`, `size`, `id`), événements émis. Noter dans le commit msg l'API utilisée.

    Étape B — `public/members.htmx.html:332` :
    1. Lire les ~50 lignes autour de ligne 332 pour voir le contenu du `<dialog>`.
    2. Chercher les call-sites JS qui ouvrent ce dialog :
       `grep -rn "memberDetailDialog\|member-detail-dialog" public/ --include="*.html" --include="*.js"`
    3. Remplacer le `<dialog>` par `<ag-modal id="memberDetailDialog" title="Détail du membre" size="md">`
       en gardant le contenu interne tel quel.
    4. Adapter les call-sites JS si l'API diffère (`.showModal()` → `.show()` ou équivalent
       documenté dans ag-modal.js).
    5. Garder `member-detail-dialog` comme classe CSS si elle porte du styling interne ;
       supprimer si elle ne servait que de hook au `<dialog>` natif.

    Étape C — pour chacun des 3 autres fichiers, exécuter le grep ciblé pour relister précisément
    les hits à la date d'exécution :
    `grep -nE "<dialog\b|class=\"[^\"]*\\bmodal\\b" public/{operator,validate,trust}.htmx.html | grep -v "ag-modal"`

    Pour CHAQUE hit restant, statuer dans le commit msg :
    - **MIGRATE** : si c'est un wrapper `<dialog>` ou `<div class="modal">` standalone qui agit
      comme une vraie modale (overlay + focus trap nécessaire). Migrer vers `<ag-modal>`.
    - **NO-OP** : si c'est une sub-class CSS interne à un modal déjà migré (ex : `audit-modal-row`
      à l'intérieur d'un `<ag-modal>` parent). Justifier dans le commit msg.
    - **KEEP-RENAME** : si c'est un block décoratif inline qui n'est pas une modale (ex : warning
      panel `launch-modal-warning` rendu inline dans la page). Renommage hors scope ici, mais
      documenter pour évacuer la confusion sémantique.

    Étape D — re-grep de validation (cible : 0 hits MIGRATE en attente) :
    `grep -rnE "<dialog\b" public/*.html | grep -v "<!-- "`
    DOIT retourner 0 hits hors commentaires HTML.

    Vérification fonctionnelle manuelle non bloquante mais recommandée :
    `php -l public/members.htmx.html` (no-op, fichier HTML — sauter), puis charger la page
    members.htmx.html et tester ouverture/fermeture du modal.
  </action>
  <verify>
    <automated>test "$(grep -rE '&lt;dialog\b' public/*.html 2&gt;/dev/null | grep -v '&lt;!--' | wc -l)" -eq 0</automated>
  </verify>
  <done>
    Aucun `<dialog>` brut dans `public/*.html` (hors commentaires). Les sites NO-OP et KEEP-RENAME
    sont documentés dans le commit msg. members.htmx.html charge sans erreur JS dans la console.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 : Statuer sur les 8 hex print de editorial.css + verify final V27-02/03/06 (clôture structurelle)</name>
  <files>
    public/assets/css/editorial.css,
    .planning/v2.7-VISUAL-AUDIT.md
  </files>
  <action>
    Étape A — vérifier si design-system.css définit des tokens print :
    `grep -nE "^\s*--color-print|--print-" public/assets/css/design-system.css`

    Étape B — selon résultat :
    - **Si tokens print existent** (`--color-print-fg`, `--color-print-muted`, etc.) :
      migrer les 8 hits editorial.css :
      * `#000` → `var(--color-print-fg)` (lignes 146, 194, 233, 243)
      * `#fff` → `var(--color-print-bg)` (ligne 145)
      * `#555` → `var(--color-print-muted)` (lignes 137, 224, 232)
      Re-grep editorial.css doit retourner 0 hex.
    - **Si tokens print n'existent pas** : ne pas en créer ici (hors scope ; design-system.css change
      = ouvre un risque de régression cross-screen). DOCUMENTER les 8 hits dans v2.7-VISUAL-AUDIT.md
      dans une nouvelle section `## Exceptions hex acceptées` avec le rationale "couleurs N&B
      d'imprimante dans @media print, pas de tokens dédiés au moment de v2.7". Cible roadmap
      ≤5 hits ; renégocier explicitement à 8 hits documentés dans cette section.

    Étape C — re-grep V27-02 typo (validation conformité) :
    `grep -rnE "font-family:.*(Arial|Helvetica|Verdana|Times)" public/assets/css/ --include="*.css" | grep -v design-system.css | grep -v email`
    DOIT retourner 0 hits. Coller la sortie (ou son absence) dans v2.7-VISUAL-AUDIT.md
    dans `## Verify clôture` sous-section V27-02.

    Étape D — re-grep V27-06 transitions (validation conformité après Task 1) :
    `grep -rnE "transition:[^;]*[0-9]+(\\.[0-9]+)?s" public/assets/css/ --include="*.css" | grep -v design-system.css`
    DOIT retourner 0 hits (ou égal aux exceptions documentées). Coller la sortie dans
    v2.7-VISUAL-AUDIT.md `## Verify clôture` sous-section V27-06.

    Étape E — re-grep V27-05 modales (validation après Task 2) :
    `grep -rnE "<dialog\b" public/*.html | grep -v "<!--"`
    DOIT retourner 0 hits. Coller dans v2.7-VISUAL-AUDIT.md `## Verify clôture` sous-section V27-05.

    Étape F — mettre à jour la section `## Critères de sortie phase 1` du v2.7-VISUAL-AUDIT.md :
    cocher `[x]` les 4 lignes V27-02/03/05/06. Laisser `[ ]` la ligne V27-04 (à cocher par 01-03).
  </action>
  <verify>
    <automated>grep -q "## Verify clôture" .planning/v2.7-VISUAL-AUDIT.md &amp;&amp; test "$(grep -rE 'font-family:.*(Arial|Helvetica|Verdana|Times)' public/assets/css/ --include='*.css' 2&gt;/dev/null | grep -v design-system.css | grep -v email | wc -l)" -eq 0</automated>
  </verify>
  <done>
    Section `## Verify clôture` créée dans v2.7-VISUAL-AUDIT.md avec sorties grep V27-02/05/06
    toutes à 0 hits. Hex print soit migrés (0 hits editorial.css) soit documentés en exception
    (≤8 hits acceptés). Critères de sortie phase 1 mis à jour avec 4/5 lignes cochées.
  </done>
</task>

</tasks>

<verification>
- 4 requirements V27-02, V27-03, V27-05, V27-06 satisfaits ou exceptions documentées.
- Aucune régression visible : pages se chargent, modales s'ouvrent/se ferment, transitions
  conservent leur feel (durées identiques tokenisées).
- Le document v2.7-VISUAL-AUDIT.md contient maintenant la section `## Verify clôture` avec
  sorties grep avant/après pour preuve.
</verification>

<success_criteria>
- VISUAL-V27-02 : 0 hits font-family brut hors design-system + emails (verify Task 3).
- VISUAL-V27-03 : ≤8 hits hex documentés en exception (idéalement 0 si tokens print
  existent et ont été utilisés) (verify Task 3).
- VISUAL-V27-05 : 0 `<dialog>` brut dans `public/*.html` ; les sub-classes restent
  comme styling interne aux `<ag-modal>` parents (verify Task 2).
- VISUAL-V27-06 : ≤3 hits transitions brutes hors design-system (idéal 0) (verify Task 1).
- Aucune file PHP/HTML cassée (`php -l` sur fichiers PHP non applicable ici, fichiers HTML
  validés par chargement page).
</success_criteria>

<output>
After completion, create `.planning/phases/01-coherence-visuelle/01-02-SUMMARY.md` listing :
- Décisions site-par-site V27-05 (MIGRATE | NO-OP | KEEP-RENAME) avec count par catégorie.
- Mapping motion/ease utilisé (figé du résultat Task 1 étape A).
- Statut hex print (migré ou documenté en exception).
- Sortie des 5 grep verify clôture.
- Sites éventuellement non-traités avec rationale (ne devrait pas y en avoir).
</output>
