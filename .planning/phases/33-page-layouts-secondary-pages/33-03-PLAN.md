---
phase: 33-page-layouts-secondary-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/email-templates.css
  - public/assets/css/meetings.css
autonomous: true
requirements: [LAY-11, LAY-12]

must_haves:
  truths:
    - "Email templates editor overlay uses 1fr 400px grid columns (not 1fr 1fr)"
    - "Email templates preview panel uses var(--color-surface-raised) background"
    - "Email templates page content constrained to max-width 1200px"
    - "Meetings list items have var(--space-3) (12px) gap between them"
    - "Meetings list session items have no margin-bottom (gap handles spacing)"
    - "Meetings page content constrained to max-width 1200px"
  artifacts:
    - path: "public/assets/css/email-templates.css"
      provides: "Editor overlay 1fr+400px grid, page max-width 1200px"
      contains: "grid-template-columns: 1fr 400px"
    - path: "public/assets/css/meetings.css"
      provides: "Meetings list density alignment, max-width 1200px"
      contains: "max-width: 1200px"
  key_links:
    - from: "public/assets/css/email-templates.css"
      to: "design-system.css tokens"
      via: "var(--color-surface-raised)"
      pattern: "var\\(--color-surface-raised\\)"
    - from: "public/assets/css/meetings.css"
      to: "design-system.css tokens"
      via: "var(--space-3)"
      pattern: "var\\(--space-3\\)"
---

<objective>
Apply Phase 32 layout language to email templates and meetings list pages — editor overlay grid fix for email templates, density alignment and max-width for meetings list.

Purpose: These are the simplest layout changes. Email templates needs a grid column ratio fix in the editor overlay. Meetings list needs density token alignment and max-width constraint to match dashboard.
Output: Updated email-templates.css and meetings.css with correct grid specs, density tokens, and max-width constraints.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-page-layouts-secondary-pages/33-CONTEXT.md
@.planning/phases/33-page-layouts-secondary-pages/33-RESEARCH.md
@public/assets/css/email-templates.css
@public/assets/css/meetings.css
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Email templates — editor overlay grid fix + page max-width (LAY-11)</name>
  <files>public/assets/css/email-templates.css</files>
  <read_first>public/assets/css/email-templates.css, public/assets/css/design-system.css</read_first>
  <action>
Update email-templates.css with the following specific changes:

1. **Fix `.template-editor-body` grid columns:**
   - Change `grid-template-columns: 1fr 1fr` to `grid-template-columns: 1fr 400px`
   - This makes the preview panel a fixed 400px while the form takes remaining space

2. **Update `.template-editor-preview` background:**
   - Change `background: var(--color-bg-subtle)` to `background: var(--color-surface-raised)`
   - Keep `border-left: 1px solid var(--color-border);`

3. **Add page-level max-width constraint** — add new rule:
   ```css
   .email-templates-main .page-content,
   .email-templates-main .templates-grid {
     max-width: 1200px;
     margin-left: auto;
     margin-right: auto;
   }
   ```
   Check whether `.page-content` wrapper exists in the HTML, otherwise target `.templates-grid` directly.

4. **Tokenize `.template-card` border-radius:**
   - Change `border-radius: 8px` to `border-radius: var(--radius-lg);`

5. **Tokenize `.template-editor-content` border-radius:**
   - Already `border-radius: 12px` — change to `border-radius: var(--radius-lg);`

6. **Tokenize `.template-card-header`, `.template-card-body`, `.template-card-footer` padding:**
   - `.template-card-header`: change `padding: 1rem` to `padding: var(--space-4);`
   - `.template-card-body`: change `padding: 1rem` to `padding: var(--space-4);`
   - `.template-card-footer`: change `padding: 0.75rem 1rem` to `padding: var(--space-3) var(--space-4);`

7. **Tokenize `.template-editor-header` and `.template-editor-footer` padding:**
   - `.template-editor-header`: change `padding: 1rem 1.5rem` to `padding: var(--space-4) var(--space-card);`
   - `.template-editor-footer`: change `padding: 1rem 1.5rem` to `padding: var(--space-4) var(--space-card);`
   - `.template-editor-form`: change `padding: 1.5rem` to `padding: var(--space-card);`

Do NOT touch JavaScript. Only CSS changes.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep "grid-template-columns: 1fr 400px" public/assets/css/email-templates.css && grep -c "color-surface-raised" public/assets/css/email-templates.css && grep -c "max-width: 1200px" public/assets/css/email-templates.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "grid-template-columns: 1fr 400px" email-templates.css returns 1 match
    - grep "grid-template-columns: 1fr 1fr" email-templates.css returns 0 matches (replaced)
    - grep "color-surface-raised" email-templates.css returns at least 1 match (preview panel)
    - grep "max-width: 1200px" email-templates.css returns at least 1 match
    - grep "var(--radius-lg)" email-templates.css returns at least 1 match
    - grep "padding: 1rem" email-templates.css returns 0 matches (all tokenized)
    - No hardcoded hex values added
  </acceptance_criteria>
  <done>Email templates editor overlay uses 1fr+400px grid, preview panel uses raised surface, page content constrained to 1200px, all spacing tokenized</done>
</task>

<task type="auto">
  <name>Task 2: Meetings list — density alignment + max-width (LAY-12)</name>
  <files>public/assets/css/meetings.css</files>
  <read_first>public/assets/css/meetings.css, public/assets/css/design-system.css</read_first>
  <action>
Update meetings.css with the following specific changes:

1. **Add max-width constraint to meetings page content:**
   ```css
   .meetings-main .page-content {
     max-width: 1200px;
     margin: 0 auto;
   }
   ```

2. **Fix `.sessions-list` density — switch from per-item margin to container gap:**
   - Change `gap: 0` to `gap: var(--space-3);` (12px between items)

3. **Remove per-item margin from `.session-item`:**
   - Change `margin-bottom: 8px` to `margin-bottom: 0;` (gap on parent handles spacing)

4. **Remove per-item margin from `.skeleton-session-item`:**
   - Change `margin-bottom: 8px` to `margin-bottom: 0;`

5. **Tokenize `.filter-pills` spacing:**
   - Change `gap: 6px` to `gap: var(--space-2);`
   - Change `margin-bottom: 12px` to `margin-bottom: var(--space-3);`

6. **Tokenize `.filter-pill` padding:**
   - Change `padding: 6px 14px` to `padding: var(--space-2) var(--space-4);`

7. **Tokenize `.meetings-toolbar` spacing:**
   - Change `gap: 12px` to `gap: var(--space-3);`
   - Change `margin-bottom: 12px` to `margin-bottom: var(--space-3);`

8. **Tokenize `.session-item` spacing:**
   - Change `gap: 12px` to `gap: var(--space-3);`
   - Change `padding: 12px 16px` to `padding: var(--space-3) var(--space-4);`
   - Change `border-radius: var(--radius-md, 8px)` to `border-radius: var(--radius);`

9. **Tokenize `.meetings-pagination`:**
   - Change `margin-top: 12px` to `margin-top: var(--space-3);`
   - Change `padding-top: 10px` to `padding-top: var(--space-3);`

10. **Tokenize `.meetings-main .section-header`:**
    - Change `margin-bottom: 14px` to `margin-bottom: var(--space-4);`

Do NOT touch JavaScript. Do NOT modify the `.meeting-card-status` or `.session-dot` styles (they are correct). Only CSS spacing/layout changes.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "max-width: 1200px" public/assets/css/meetings.css && grep "gap: var(--space-3)" public/assets/css/meetings.css | head -1 && grep -c "margin-bottom: 8px" public/assets/css/meetings.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "max-width: 1200px" meetings.css returns at least 1 match
    - grep "gap: var(--space-3)" meetings.css returns at least 1 match (sessions-list)
    - grep "margin-bottom: 8px" meetings.css returns 0 matches (replaced with gap or 0)
    - grep "margin-bottom: 0" meetings.css returns at least 1 match (session-item)
    - grep "gap: 0" meetings.css returns 0 matches in .sessions-list context (replaced with var(--space-3))
    - grep "var(--space-3)" meetings.css returns multiple matches
    - No hardcoded hex values added
  </acceptance_criteria>
  <done>Meetings list uses var(--space-3) gap for item density (matching dashboard), per-item margins removed, content constrained to 1200px, all spacing tokenized</done>
</task>

</tasks>

<verification>
- Open email templates page: template cards should be constrained within 1200px centered area
- Click edit on a template: the preview panel on the right should be narrower than the form (400px vs remaining)
- Open meetings page: session list items should have consistent 12px gaps between them
- Meetings content should be constrained to 1200px max-width (same as dashboard)
- Status badges on meetings should remain visually correct (not touched)
</verification>

<success_criteria>
- Email templates editor uses 1fr 400px grid (not 1fr 1fr)
- Email templates preview panel uses raised surface background
- Email templates page constrained to 1200px
- Meetings list gap is var(--space-3) (12px)
- Meetings session-item has no margin-bottom
- Meetings page constrained to 1200px
- Zero hardcoded spacing values remaining
</success_criteria>

<output>
After completion, create `.planning/phases/33-page-layouts-secondary-pages/33-03-SUMMARY.md`
</output>
