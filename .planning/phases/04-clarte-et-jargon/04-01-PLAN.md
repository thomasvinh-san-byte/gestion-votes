---
phase: 04-clarte-et-jargon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/public.htmx.html
  - public/help.htmx.html
  - public/validate.htmx.html
  - public/assets/js/pages/validate.js
autonomous: true
requirements:
  - CLAR-01
  - CLAR-03
must_haves:
  truths:
    - "L'interface votant n'affiche aucun terme technique visible (Quorum, SHA-256, token, hash)"
    - "Le pattern tapez VALIDER est remplace par une checkbox de confirmation"
  artifacts:
    - path: "public/public.htmx.html"
      provides: "Seuil de participation au lieu de Quorum (2 locations)"
      contains: "Seuil de participation"
    - path: "public/help.htmx.html"
      provides: "FAQ voter-visible sans jargon technique"
      contains: "empreinte num"
    - path: "public/validate.htmx.html"
      provides: "Modal confirmation sans input texte VALIDER"
    - path: "public/assets/js/pages/validate.js"
      provides: "Gate checkbox-only sans confirmText"
  key_links:
    - from: "public/validate.htmx.html"
      to: "public/assets/js/pages/validate.js"
      via: "confirmCheckbox element + updateModalConfirmState"
      pattern: "confirmCheckbox"
---

<objective>
Eliminer le jargon technique de toutes les pages votant et simplifier le pattern de confirmation.

Purpose: Les votants non-techniques ne doivent jamais voir eIDAS, SHA-256, quorum, CNIL, hash, ou token. Le pattern "tapez VALIDER" est remplace par une checkbox plus simple et accessible.
Output: public.htmx.html, help.htmx.html, validate.htmx.html, validate.js modifies
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-clarte-et-jargon/04-CONTEXT.md
@.planning/phases/04-clarte-et-jargon/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminate voter-facing jargon (CLAR-01)</name>
  <files>public/public.htmx.html, public/help.htmx.html</files>
  <read_first>
    - public/public.htmx.html (lines 50-65 for quorum-visual-label, lines 150-165 for decision-label)
    - public/help.htmx.html (lines 240-520 for FAQ sections — identify role-guarded vs unguarded sections)
  </read_first>
  <action>
**public.htmx.html — 2 label swaps:**
- Line ~57: Change `<span class="quorum-visual-label">Quorum</span>` to `<span class="quorum-visual-label">Seuil de participation</span>`
- Line ~158: Change `<div class="decision-label">Quorum</div>` to `<div class="decision-label">Seuil de participation</div>`
- DO NOT touch any element IDs (quorumValue, quorum_value, quorumVisualValue) — these are JS data containers

**help.htmx.html — FAQ rewrite in voter-visible sections ONLY:**
The security FAQ tab has `data-required-role="admin,auditor,assessor"` — leave that section's jargon intact (CLAR-02 adds tooltips there instead).

In the `general` section (no role guard, visible to voters):
- Line ~250: Replace "Le journal d'audit a chainage SHA256 est infalsifiable" with "Le journal d'audit est securise et infalsifiable"

In the `vote` section (no role guard, visible to voters):
- Replace "tokens de vote (un par votant)" with "codes de vote a usage unique (un par votant)"
- Replace "Le token est consomme (anti-rejeu)" with "Le code est utilise une seule fois"
- Replace any visible "hash SHA256" with "empreinte numerique"
- Replace "chaine de hachage SHA256 : chaque evenement inclut le hash de l'evenement precedent" with "chaine d'empreintes numeriques : chaque evenement inclut l'empreinte de l'evenement precedent"

IMPORTANT: Only change voter-visible (unguarded) sections. Admin-guarded sections keep their technical terms.
  </action>
  <verify>
    <automated>bash -c 'echo "=== public.htmx.html ===" && grep -n "Seuil de participation" public/public.htmx.html && echo "=== Quorum label count (expect 0) ===" && grep -c ">Quorum<" public/public.htmx.html || true && echo "=== help.htmx.html voter sections ===" && grep -n "empreinte" public/help.htmx.html && echo "=== SHA-256 in general/vote sections ===" && grep -n "SHA.256" public/help.htmx.html | head -10'</automated>
  </verify>
  <acceptance_criteria>
    - grep "Seuil de participation" public/public.htmx.html returns 2 matches
    - grep -c ">Quorum<" public/public.htmx.html returns 0
    - grep "empreinte" public/help.htmx.html returns matches in voter-visible sections
    - No SHA-256/SHA256 mentions remain in unguarded FAQ sections (general and vote tabs)
    - "token" replaced by "code" in vote FAQ section
  </acceptance_criteria>
  <done>public.htmx.html shows "Seuil de participation" in both locations, help.htmx.html voter-visible FAQ uses plain French (empreinte numerique, code de vote) with zero technical jargon</done>
</task>

<task type="auto">
  <name>Task 2: Replace tapez VALIDER with checkbox confirmation (CLAR-03)</name>
  <files>public/validate.htmx.html, public/assets/js/pages/validate.js</files>
  <read_first>
    - public/validate.htmx.html (lines 215-235 for confirmText form-group and surrounding modal structure)
    - public/assets/js/pages/validate.js (lines 140-200 for updateModalConfirmState, confirmText variable, closeValidateModal)
  </read_first>
  <action>
**validate.htmx.html:**
- DELETE the entire form-group containing the confirmText input (lines ~224-226):
  ```
  <div class="form-group mt-4">
    <label class="form-label" for="confirmText">Tapez <strong>VALIDER</strong> pour confirmer</label>
    <input class="form-input" type="text" id="confirmText" placeholder="VALIDER" autocomplete="off" spellcheck="false">
  </div>
  ```
- Keep the checkbox (`confirmIrreversible`) and the button (`btnModalConfirm`) as-is
- The checkbox label should already read something like "Je confirme cette action" — verify and adjust if needed to be clear in French

**validate.js — simplify gate to checkbox-only:**
- Remove the `confirmText` variable declaration (the getElementById line)
- Remove the `confirmText` input event listener
- Remove the `confirmText` keydown event listener (if any)
- In `closeValidateModal` function: remove the `confirmText.value = ''` reset line
- Simplify `updateModalConfirmState` to:
  ```javascript
  function updateModalConfirmState() {
    if (!btnModalConfirm) return;
    btnModalConfirm.disabled = !(confirmCheckbox && confirmCheckbox.checked);
  }
  ```
- Keep `confirmCheckbox.addEventListener('change', updateModalConfirmState)` intact

CRITICAL: This is an atomic change — HTML removal and JS cleanup MUST happen together. Leaving confirmText JS references after removing the HTML input will cause TypeError on modal open.
  </action>
  <verify>
    <automated>bash -c 'echo "=== confirmText in HTML (expect 0) ===" && grep -c "confirmText" public/validate.htmx.html && echo "=== confirmText in JS (expect 0) ===" && grep -c "confirmText" public/assets/js/pages/validate.js && echo "=== tapez VALIDER in HTML (expect 0) ===" && grep -ci "tapez.*VALIDER" public/validate.htmx.html && echo "=== checkbox still present ===" && grep -c "confirmIrreversible\|confirmCheckbox" public/validate.htmx.html && echo "=== JS syntax check ===" && node --check public/assets/js/pages/validate.js 2>&1 || true'</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "confirmText" public/validate.htmx.html returns 0
    - grep -c "confirmText" public/assets/js/pages/validate.js returns 0
    - grep -ci "tapez.*VALIDER" public/validate.htmx.html returns 0
    - confirmIrreversible checkbox still present in HTML
    - validate.js passes node --check syntax validation
  </acceptance_criteria>
  <done>The "tapez VALIDER" input is gone, confirmation uses checkbox only, validate.js has zero confirmText references, no JS errors</done>
</task>

</tasks>

<verification>
- All voter-facing pages (public, help, validate) contain zero technical jargon
- Confirmation modal uses checkbox-only pattern
- No JavaScript errors introduced by the HTML/JS changes
- Admin-guarded FAQ sections untouched (technical terms preserved for CLAR-02)
</verification>

<success_criteria>
- public.htmx.html: "Seuil de participation" x2, zero ">Quorum<"
- help.htmx.html: zero SHA-256/token in voter-visible sections, "empreinte numerique" present
- validate.htmx.html: zero "confirmText", zero "tapez VALIDER", checkbox present
- validate.js: zero "confirmText" references, passes syntax check
</success_criteria>

<output>
After completion, create `.planning/phases/04-clarte-et-jargon/04-01-SUMMARY.md`
</output>
