---
phase: 04-clarte-et-jargon
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/settings.htmx.html
  - public/operator.htmx.html
  - public/postsession.htmx.html
  - public/audit.htmx.html
  - public/archives.htmx.html
  - public/trust.htmx.html
  - public/assets/css/archives.css
autonomous: true
requirements:
  - CLAR-02
  - CLAR-04
must_haves:
  truths:
    - "Les termes techniques cote admin/operateur ont des tooltips explicatifs en francais au survol"
    - "Chaque bouton d'export a une description d'une ligne expliquant le contenu du fichier"
  artifacts:
    - path: "public/settings.htmx.html"
      provides: "ag-tooltip sur quorum et CNIL"
      contains: "ag-tooltip"
    - path: "public/operator.htmx.html"
      provides: "ag-tooltip sur quorum et procuration"
      contains: "ag-tooltip"
    - path: "public/postsession.htmx.html"
      provides: "ag-tooltip sur eIDAS + export description"
      contains: "ag-tooltip"
    - path: "public/audit.htmx.html"
      provides: "ag-tooltip sur SHA-256 + export descriptions"
      contains: "export-desc"
    - path: "public/archives.htmx.html"
      provides: "Export descriptions sur 7 boutons"
      contains: "export-desc"
    - path: "public/trust.htmx.html"
      provides: "Export descriptions (ag-popover already on SHA-256)"
      contains: "export-desc"
    - path: "public/assets/css/archives.css"
      provides: "CSS for export-btn-wrap and export-desc"
      contains: "export-btn-wrap"
  key_links:
    - from: "public/archives.htmx.html"
      to: "public/assets/css/archives.css"
      via: "export-btn-wrap and export-desc classes"
      pattern: "export-btn-wrap"
---

<objective>
Ajouter des tooltips explicatifs sur les termes techniques des pages admin et des descriptions visibles sur tous les boutons d'export.

Purpose: Les operateurs et admins comprennent les termes techniques grace aux tooltips. Les utilisateurs savent ce que chaque export contient avant de cliquer.
Output: 6 pages HTML modifiees + CSS pour les descriptions d'export
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-clarte-et-jargon/04-CONTEXT.md
@.planning/phases/04-clarte-et-jargon/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add admin tooltips on technical terms (CLAR-02)</name>
  <files>public/settings.htmx.html, public/operator.htmx.html, public/postsession.htmx.html, public/audit.htmx.html</files>
  <read_first>
    - public/settings.htmx.html (line ~112 for quorum label, lines 265-275 for CNIL section)
    - public/operator.htmx.html (search for "quorum" and "procuration" labels)
    - public/postsession.htmx.html (lines 290-305 for eIDAS section title)
    - public/audit.htmx.html (search for "SHA-256" or "SHA256" in onboarding tips)
  </read_first>
  <action>
Wrap each technical term in ag-tooltip using the existing component. Pattern:
```html
<ag-tooltip text="Tooltip text here" position="top">Term visible</ag-tooltip>
```

**settings.htmx.html:**
1. Quorum label (near line 112): Wrap "quorum" portion in `<ag-tooltip text="Nombre minimum de participants requis pour que le vote soit valide" position="top">Seuil de quorum (%)</ag-tooltip>` — keep the label's `for` attribute on the `<label>` element, put ag-tooltip INSIDE the label wrapping the text
2. CNIL reference (near line 268-271): Wrap "CNIL" in `<ag-tooltip text="Commission Nationale de l'Informatique et des Libertes — regles de protection des donnees personnelles" position="top">CNIL</ag-tooltip>`

**operator.htmx.html:**
1. Quorum display: Wrap the quorum label text in `<ag-tooltip text="Nombre minimum de participants requis pour valider le scrutin" position="top">Quorum</ag-tooltip>`
2. Procuration references: Wrap "Procuration" label(s) in `<ag-tooltip text="Delegation de vote : un membre absent autorise un autre a voter en son nom" position="top">Procuration</ag-tooltip>` — apply to the first/most prominent occurrence, not every single mention

**postsession.htmx.html:**
1. eIDAS section title (near line 293): Wrap "eIDAS" in `<ag-tooltip text="Reglement europeen sur l'identification electronique — garantit la valeur legale de la signature" position="top">eIDAS</ag-tooltip>`

**audit.htmx.html:**
1. SHA-256 onboarding tip: Wrap "SHA-256" (or "SHA256") mention in `<ag-tooltip text="Algorithme de calcul d'empreinte numerique — toute modification des donnees change l'empreinte" position="top">SHA-256</ag-tooltip>`

**trust.htmx.html:** SKIP — already has ag-popover on SHA-256 hash label (confirmed in research).

IMPORTANT: Keep ag-tooltip INSIDE its parent element (label, heading, span). Never place ag-tooltip BETWEEN a label and its input — this breaks click-to-focus association.
  </action>
  <verify>
    <automated>bash -c 'echo "=== settings tooltips ===" && grep -c "ag-tooltip" public/settings.htmx.html && echo "=== operator tooltips ===" && grep -c "ag-tooltip" public/operator.htmx.html && echo "=== postsession tooltips ===" && grep -c "ag-tooltip" public/postsession.htmx.html && echo "=== audit tooltips ===" && grep -c "ag-tooltip" public/audit.htmx.html'</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ag-tooltip" public/settings.htmx.html returns at least 2 (quorum + CNIL)
    - grep -c "ag-tooltip" public/operator.htmx.html returns at least 2 (quorum + procuration)
    - grep -c "ag-tooltip" public/postsession.htmx.html returns at least 1 (eIDAS)
    - grep -c "ag-tooltip" public/audit.htmx.html returns at least 1 (SHA-256)
    - All tooltip text attributes are in French
  </acceptance_criteria>
  <done>Technical terms on settings, operator, postsession, and audit pages have French tooltips via ag-tooltip component</done>
</task>

<task type="auto">
  <name>Task 2: Add export descriptions on all export buttons (CLAR-04)</name>
  <files>public/archives.htmx.html, public/audit.htmx.html, public/postsession.htmx.html, public/trust.htmx.html, public/assets/css/archives.css</files>
  <read_first>
    - public/assets/css/archives.css (lines 365-390 for .export-grid CSS structure — check current grid children selectors)
    - public/archives.htmx.html (lines 189-250 for export modal with 6 buttons + ZIP)
    - public/audit.htmx.html (lines 55-70 for export buttons)
    - public/postsession.htmx.html (line ~326 for PDF export)
    - public/trust.htmx.html (lines 40-50 for export buttons)
  </read_first>
  <action>
**Step 1: Add CSS for export descriptions (archives.css)**

Add to the EXPORTS MODAL section of archives.css:
```css
.export-btn-wrap {
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
}
.export-btn-wrap .btn {
  width: 100%;
}
.export-btn-wrap--full {
  grid-column: 1 / -1;
}
.export-desc {
  font-size: 0.75rem;
  color: var(--color-text-secondary);
  line-height: 1.3;
  padding: 0 var(--space-1);
}
```

**Step 2: archives.htmx.html — wrap 7 export buttons**

Wrap each button in `<div class="export-btn-wrap">` and add `<small class="export-desc">`:

| Button ID | Description text |
|-----------|-----------------|
| btnExportPV | Proces-verbal complet de la seance (format web imprimable) |
| btnExportAttendance | Liste des membres presents et leurs procurations |
| btnExportVotes | Detail de chaque bulletin : votant, resolution, choix |
| btnExportMotions | Resultats par resolution : decision, majorite, quorum |
| btnExportMembers | Liste des membres et leur poids de vote |
| btnExportAudit | Journal complet des evenements pour verification legale |
| btnExportZip | Tous les fichiers precedents reunis en une seule archive |

For the ZIP button wrapper, add class `export-btn-wrap--full` (it currently spans full width via `grid-column: 1 / -1` on the button — move that span to the wrapper).

PITFALL: The `.export-grid` grid targets direct children. After wrapping, children change from `<button>` to `<div>`. The grid gap and columns still apply via `> *`. But `grid-column: 1 / -1` for ZIP MUST be on `.export-btn-wrap--full`, not the button.

**Step 3: audit.htmx.html — 2 export buttons**

Wrap each export button with description:
| Button | Description |
|--------|------------|
| btnExportSelection | Evenements filtres selon la selection courante (CSV) |
| btnExportAll | Journal d'audit complet toutes seances confondues (CSV) |

Use same `export-btn-wrap` + `export-desc` pattern (archives.css is shared).

**Step 4: postsession.htmx.html — PDF export**

Wrap the PDF export button/link with description:
- Description: "Proces-verbal de la seance en format PDF signable"

**Step 5: trust.htmx.html — 2 export buttons**

Wrap each export button with description:
| Button | Description |
|--------|------------|
| btnExportAuditJson | Donnees d'audit brutes pour verification technique |
| btnExportTrust | Rapport de controle d'integrite complet |
  </action>
  <verify>
    <automated>bash -c 'echo "=== archives export-desc count (expect 7) ===" && grep -c "export-desc" public/archives.htmx.html && echo "=== audit export-desc count (expect 2) ===" && grep -c "export-desc" public/audit.htmx.html && echo "=== postsession export-desc (expect 1) ===" && grep -c "export-desc" public/postsession.htmx.html && echo "=== trust export-desc (expect 2) ===" && grep -c "export-desc" public/trust.htmx.html && echo "=== CSS export-btn-wrap ===" && grep -c "export-btn-wrap" public/assets/css/archives.css && echo "=== CSS export-desc ===" && grep -c "export-desc" public/assets/css/archives.css'</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export-desc" public/archives.htmx.html returns 7
    - grep -c "export-desc" public/audit.htmx.html returns 2
    - grep -c "export-desc" public/postsession.htmx.html returns 1
    - grep -c "export-desc" public/trust.htmx.html returns 2
    - grep -c "export-btn-wrap" public/assets/css/archives.css returns at least 3 (class definitions)
    - grep -c "export-desc" public/assets/css/archives.css returns at least 1 (class definition)
    - All description texts are in French
  </acceptance_criteria>
  <done>All 12 export buttons across 4 pages have visible French descriptions, CSS properly styles the layout</done>
</task>

</tasks>

<verification>
- All admin pages have ag-tooltip on technical terms (quorum, CNIL, eIDAS, SHA-256, procuration)
- All export buttons have visible descriptions in French
- CSS export-btn-wrap and export-desc classes exist in archives.css
- No layout breakage in archives export-grid (ZIP button still spans full width)
</verification>

<success_criteria>
- 6 ag-tooltip instances across 4 admin pages (settings x2, operator x2, postsession x1, audit x1)
- 12 export-desc instances across 4 pages (archives x7, audit x2, postsession x1, trust x2)
- CSS classes export-btn-wrap, export-btn-wrap--full, and export-desc defined in archives.css
- trust.htmx.html SHA-256 ag-popover unchanged (already present)
</success_criteria>

<output>
After completion, create `.planning/phases/04-clarte-et-jargon/04-02-SUMMARY.md`
</output>
