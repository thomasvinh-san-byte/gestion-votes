---
phase: 20.2-deep-ui-wireframe-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
autonomous: true
requirements:
  - UI-ALIGN-FORMS
  - UI-ALIGN-TABLES
  - UI-ALIGN-TAGS

must_haves:
  truths:
    - "Form inputs have 1px border (not 2px), surface-raised background, and min-height 42px"
    - "Form labels are 13px uppercase bold with 0.5px letter-spacing"
    - "Tables have striped even rows, sticky uppercase headers, and cell overflow ellipsis"
    - "Table row hover uses accent-tinted background"
    - "Tags use 5px border-radius (not pill) and semantic variants have colored borders"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Updated form, table, and tag styles matching wireframe v3.19.2"
      contains: "text-transform: uppercase"
  key_links:
    - from: "public/assets/css/design-system.css"
      to: "all pages using .form-input, .table, .tag classes"
      via: "CSS cascade"
      pattern: "form-input|table th|tag-danger"
---

<objective>
Update form field, table, and tag component CSS in design-system.css to match wireframe v3.19.2 specifications.

Purpose: Forms are used on every data-entry page (wizard, settings, operator). Tables appear on dashboard, audit, members. Tags appear throughout. These three component families complete the HIGH/MEDIUM priority design system alignment.
Output: Updated `.form-input`, `.form-label`, `.table`, `.tag` rules in design-system.css
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.2-deep-ui-wireframe-alignment/20.2-CONTEXT.md
@.planning/phases/20.2-deep-ui-wireframe-alignment/20.2-RESEARCH.md

<interfaces>
<!-- Current form CSS: design-system.css lines 1317-1425 -->
<!-- Current table CSS: design-system.css lines 1452-1485 -->
<!-- Current tag CSS: design-system.css lines 3230-3240 -->

Current .form-input (line 1335):
  border: 2px solid var(--color-border)
  background: var(--color-surface)
  /* missing min-height */

Current .form-label (line 1324):
  font-size: var(--text-sm)
  font-weight: var(--font-medium)
  /* not uppercase, no letter-spacing */

Current .form-input:focus (line 1364):
  border-color: var(--color-primary)
  box-shadow: 0 0 0 3px var(--ring-color)
  /* focus ring uses box-shadow — still visible with 1px border */

Current .table th (line 1465):
  font-weight: var(--font-semibold)
  color: var(--color-text-secondary)
  /* not uppercase, not sticky, no letter-spacing */

Current .table td (line 1473):
  padding: var(--space-3) var(--space-4)
  /* no max-width, no overflow guard */

Current .table tbody tr:hover (line 1483):
  background: var(--color-bg-subtle)
  /* not accent-tinted */

Current .tag (line 3230):
  border-radius: var(--radius-full)  /* 999px pill */
  /* no border on semantic variants */

Current .tag-danger (line 3237):
  background: var(--color-danger-subtle); color: var(--color-danger)
  /* no border */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update form field and label styles — border, background, min-height, uppercase labels</name>
  <files>public/assets/css/design-system.css</files>
  <action>
Modify the following CSS rules in design-system.css:

1. `.form-input, .form-select, .form-textarea` (line 1335-1349): Change `border: 2px solid var(--color-border)` to `border: 1px solid var(--color-border)`. Change `background: var(--color-surface)` to `background: var(--color-surface-raised)`. Add `min-height: 42px` (after padding line).

2. `.form-label` (line 1324-1328): Change to wireframe spec:
   ```
   .form-label {
     font-size: 13px;
     font-weight: 700;
     text-transform: uppercase;
     letter-spacing: .5px;
     color: var(--color-text);
   }
   ```

3. `.form-input-sm, .form-select-sm` (line 1351): Add `min-height: 34px` to maintain proportional sizing for small variant.

IMPORTANT: Do NOT change `.form-input:focus` — the focus ring uses `box-shadow: 0 0 0 3px` which provides excellent visibility regardless of border width. Do NOT change any `.is-error`, `.is-invalid`, `.is-valid` rules. Do NOT change `::placeholder` rules. These all stay as-is.

Verify after changes: The focus ring (`box-shadow: 0 0 0 3px var(--ring-color)`) still provides clear visual feedback with 1px border. This is expected — the ring is independent of border width.
  </action>
  <verify>
    <automated>grep -A2 "\.form-input," public/assets/css/design-system.css | grep -q "1px solid" && grep -q "min-height: 42px" public/assets/css/design-system.css && grep -A3 "\.form-label {" public/assets/css/design-system.css | grep -q "text-transform: uppercase" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Form inputs have 1px border, surface-raised background, and 42px min-height. Form labels are 13px, 700 weight, uppercase with 0.5px letter-spacing. Focus ring and validation states are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update table styles — striped rows, sticky headers, cell overflow, accent hover</name>
  <files>public/assets/css/design-system.css</files>
  <action>
Modify the following CSS rules in design-system.css:

1. `.table th` (line 1465): Add `text-transform: uppercase`, `letter-spacing: .7px`, and `position: sticky; top: 0; z-index: 1` to make headers sticky within `.table-wrapper`:
   ```
   .table th {
     padding: var(--space-3) var(--space-4);
     font-weight: var(--font-semibold);
     color: var(--color-text-secondary);
     background: var(--color-bg-subtle);
     border-bottom: 1px solid var(--color-border);
     text-transform: uppercase;
     letter-spacing: .7px;
     position: sticky;
     top: 0;
     z-index: 1;
   }
   ```

2. `.table td` (line 1473): Add cell overflow guard:
   ```
   .table td {
     padding: var(--space-3) var(--space-4);
     border-bottom: 1px solid var(--color-border-subtle);
     vertical-align: middle;
     max-width: 260px;
     overflow: hidden;
     text-overflow: ellipsis;
   }
   ```

3. Add NEW striped row rule AFTER `.table tbody tr:last-child td` (after line 1481):
   ```
   .table tbody tr:nth-child(even) td {
     background: rgba(21,21,16,.022);
   }
   ```

4. `.table tbody tr:hover` (line 1483): Change from `background: var(--color-bg-subtle)` to accent-tinted:
   ```
   .table tbody tr:hover td {
     background: color-mix(in srgb, var(--color-primary) 6%, var(--color-surface));
   }
   ```
   Note: Changed selector to `tr:hover td` (not `tr:hover`) to override the per-td striped background.

IMPORTANT: Do NOT touch `.table-wrapper`, `.table-responsive`, `.table-stack` rules. These stay as-is.
  </action>
  <verify>
    <automated>grep -q "nth-child(even)" public/assets/css/design-system.css && grep -q "position: sticky" public/assets/css/design-system.css && grep -q "text-overflow: ellipsis" public/assets/css/design-system.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Tables have striped even rows, sticky uppercase headers with 0.7px letter-spacing, cell overflow ellipsis at 260px max-width, and accent-tinted hover rows.</done>
</task>

<task type="auto">
  <name>Task 3: Update tag styles — border-radius 5px, colored borders on semantic variants</name>
  <files>public/assets/css/design-system.css</files>
  <action>
Modify the following CSS rules in design-system.css:

1. `.tag` (line 3230): Change `border-radius: var(--radius-full)` to `border-radius: 5px`. Add `border: 1px solid transparent` as base border:
   ```
   .tag {
     display: inline-flex; align-items: center; gap: 4px;
     padding: 3px 10px; border-radius: 5px;
     font-size: 12px; font-weight: 700; white-space: nowrap;
     background: var(--tag-bg, #e8e7e2); color: var(--tag-text, #6b6b6b);
     border: 1px solid transparent;
   }
   ```

2. Add colored borders to semantic variants. Update each variant to include a border using `color-mix` for a subtle tinted border:
   ```
   .tag-accent { background: var(--color-primary-subtle); color: var(--color-primary); border-color: color-mix(in srgb, var(--color-primary) 30%, transparent); }
   .tag-danger { background: var(--color-danger-subtle); color: var(--color-danger); border-color: color-mix(in srgb, var(--color-danger) 30%, transparent); }
   .tag-success { background: var(--color-success-subtle); color: var(--color-success); border-color: color-mix(in srgb, var(--color-success) 30%, transparent); }
   .tag-warn { background: var(--color-warning-subtle); color: var(--color-warning); border-color: color-mix(in srgb, var(--color-warning) 30%, transparent); }
   .tag-purple { background: var(--color-purple-subtle, #f0e8f8); color: var(--color-purple, #7c3aed); border-color: color-mix(in srgb, var(--color-purple, #7c3aed) 30%, transparent); }
   ```

IMPORTANT: Do NOT change `.chip` rules that follow tags. Do NOT change font-size/weight/padding — these already match wireframe.
  </action>
  <verify>
    <automated>grep "\.tag {" public/assets/css/design-system.css | grep -q "border-radius: 5px" && grep "tag-danger" public/assets/css/design-system.css | grep -q "border-color" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Tags use 5px border-radius (slightly rounded rectangle instead of pill). All semantic variants (.tag-accent, .tag-danger, .tag-success, .tag-warn, .tag-purple) have subtle colored borders.</done>
</task>

</tasks>

<verification>
- Open wizard page — form labels should be uppercase 13px bold, inputs should have 1px border and white background
- Open any page with tables (audit, members) — headers should be uppercase and sticky on scroll, even rows lightly striped
- Check tag appearance on hub or dashboard — should be rounded rectangle (5px) not pill
- Verify form focus ring still clearly visible (box-shadow 3px ring is independent of border width)
</verification>

<success_criteria>
- `.form-input` has `border: 1px solid`, `background: var(--color-surface-raised)`, `min-height: 42px`
- `.form-label` is `13px`, `700 weight`, `uppercase`, `0.5px letter-spacing`
- `.table th` is `uppercase`, `sticky`, `0.7px letter-spacing`
- `.table td` has `max-width: 260px`, `text-overflow: ellipsis`
- Striped rows on even `tbody tr`
- Hover row uses accent-tinted `color-mix`
- `.tag` has `border-radius: 5px`, semantic variants have colored borders
</success_criteria>

<output>
After completion, create `.planning/phases/20.2-deep-ui-wireframe-alignment/20.2-02-SUMMARY.md`
</output>
