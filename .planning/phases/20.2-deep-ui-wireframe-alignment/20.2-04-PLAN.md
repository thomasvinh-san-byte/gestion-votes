---
phase: 20.2-deep-ui-wireframe-alignment
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
autonomous: true
requirements:
  - UI-ALIGN-NEW-COMPONENTS
  - UI-ALIGN-SCROLLBAR
  - UI-ALIGN-PRINT

must_haves:
  truths:
    - "Scroll-to-top button CSS exists (.scroll-top) with fixed positioning and fade transition"
    - "Pure CSS tooltip system exists (.tip-wrap, .tip-body) with arrow pseudo-element"
    - "Confirm dialog CSS exists (.confirm-dialog) with centered icon circle and Fraunces title"
    - "Theme toggle switch CSS exists (.theme-toggle) with knob animation"
    - "Keyboard hint badges exist (.op-kbd) with mono font"
    - "Global scrollbar is 5px wide with themed thumb"
    - "Print styles extended with additional hide list and print-only utility"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "New component CSS patterns and global enhancements"
      contains: "scroll-top"
  key_links:
    - from: "public/assets/css/design-system.css"
      to: "future HTML that uses these component classes"
      via: "CSS cascade"
      pattern: "scroll-top|tip-wrap|confirm-dialog|theme-toggle|op-kbd"
---

<objective>
Add missing component CSS patterns (scroll-to-top, tooltip, confirm dialog, theme toggle, keyboard hints) and global enhancements (scrollbar, print styles) to design-system.css.

Purpose: The wireframe defines 5 component patterns not yet in the design system. These are CSS-only definitions — HTML usage will come when features need them. Global scrollbar and print style improvements polish the overall experience.
Output: New CSS classes for `.scroll-top`, `.tip-wrap`, `.tip-body`, `.confirm-dialog`, `.theme-toggle`, `.op-kbd` plus global scrollbar and extended print rules
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.2-deep-ui-wireframe-alignment/20.2-CONTEXT.md
@.planning/phases/20.2-deep-ui-wireframe-alignment/20.2-RESEARCH.md

<interfaces>
<!-- End of design-system.css: line 4236 -->
<!-- Existing print styles: lines 3010-3098 and 4226-4236 -->
<!-- Existing scrollbar: only sidebar-specific at line 667-668 -->
<!-- Fraunces font already loaded: used in projection page -->

Existing print @media (line 3010):
  Hides: .app-sidebar, .app-header, .drawer, .modal-backdrop, .btn,
         .toast-container, .tour-*, .cmd-palette-overlay, .session-timeout-banner,
         .onboarding-banner, .skip-link, .tour-trigger-btn, .meeting-bar,
         .tabs-nav, .bottom-nav
  Cards: box-shadow: none; border: 1px solid #d1d5db
  Missing: .search-overlay, .search-trigger, .theme-toggle, .scroll-top, footer, .mobile-nav

Existing sidebar scrollbar (line 667-668):
  .sidebar-scroll::-webkit-scrollbar { width: 3px; }
  .sidebar-scroll::-webkit-scrollbar-thumb { background: rgba(255,255,255,.25); border-radius: 2px; }
  /* No global scrollbar styling */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new component CSS — scroll-top, tooltip, confirm dialog, theme toggle, keyboard hints</name>
  <files>public/assets/css/design-system.css</files>
  <action>
Add the following NEW CSS sections to design-system.css BEFORE the final print `@media` block (before line 4226). Place them in a new section:

```css
/* =============================================================================
   WIREFRAME v3.19.2 — NEW COMPONENTS
   ============================================================================= */

/* ── Scroll-to-Top Button ── */
.scroll-top {
  position: fixed;
  bottom: 24px;
  right: 24px;
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  box-shadow: var(--shadow-md);
  color: var(--color-text-secondary);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
  visibility: hidden;
  transform: translateY(8px);
  transition: opacity .2s ease, visibility .2s ease, transform .2s ease, background .15s ease, color .15s ease;
  z-index: var(--z-sticky);
}

.scroll-top.visible {
  opacity: 1;
  visibility: visible;
  transform: translateY(0);
}

.scroll-top:hover {
  background: var(--color-primary);
  color: white;
  border-color: var(--color-primary);
}

/* ── Pure CSS Tooltip System ── */
.tip-wrap {
  position: relative;
  display: inline-flex;
}

.tip-body {
  position: absolute;
  bottom: calc(100% + 8px);
  left: 50%;
  transform: translateX(-50%);
  padding: 6px 10px;
  background: var(--color-text);
  color: var(--color-bg);
  font-size: 12px;
  font-weight: 600;
  border-radius: 5px;
  white-space: nowrap;
  pointer-events: none;
  opacity: 0;
  transition: opacity .15s ease;
  z-index: var(--z-tooltip, 9000);
}

.tip-body::after {
  content: '';
  position: absolute;
  top: 100%;
  left: 50%;
  transform: translateX(-50%);
  border: 5px solid transparent;
  border-top-color: var(--color-text);
}

.tip-wrap:hover .tip-body {
  opacity: 1;
}

/* ── Confirm Dialog ── */
.confirm-dialog {
  text-align: center;
  padding: var(--space-6) var(--space-5);
}

.confirm-dialog-icon {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto var(--space-4);
  font-size: 24px;
}

.confirm-dialog-icon--danger {
  background: var(--color-danger-subtle);
  color: var(--color-danger);
}

.confirm-dialog-icon--warn {
  background: var(--color-warning-subtle);
  color: var(--color-warning);
}

.confirm-dialog-icon--success {
  background: var(--color-success-subtle);
  color: var(--color-success);
}

.confirm-dialog-title {
  font-family: var(--font-display, 'Fraunces', serif);
  font-size: var(--text-xl);
  font-weight: 700;
  margin-bottom: var(--space-2);
}

.confirm-dialog-msg {
  font-size: 14px;
  color: var(--color-text-secondary);
  margin-bottom: var(--space-5);
  line-height: 1.5;
}

.confirm-dialog-actions {
  display: flex;
  gap: var(--space-3);
  justify-content: center;
}

/* ── Theme Toggle Switch ── */
.theme-toggle {
  position: relative;
  width: 44px;
  height: 24px;
  border-radius: 12px;
  background: var(--color-border);
  border: none;
  cursor: pointer;
  transition: background .2s ease;
  padding: 0;
}

.theme-toggle::after {
  content: '';
  position: absolute;
  top: 2px;
  left: 2px;
  width: 20px;
  height: 20px;
  border-radius: 50%;
  background: white;
  box-shadow: 0 1px 3px rgba(0,0,0,.2);
  transition: transform .2s ease;
}

.theme-toggle[aria-pressed="true"],
.theme-toggle.active {
  background: var(--color-primary);
}

.theme-toggle[aria-pressed="true"]::after,
.theme-toggle.active::after {
  transform: translateX(20px);
}

/* ── Operator Keyboard Hints ── */
.op-kbd {
  display: inline-block;
  font-family: var(--font-mono, 'SF Mono', 'Consolas', monospace);
  font-size: 10px;
  font-weight: 600;
  padding: 2px 5px;
  border-radius: 3px;
  background: var(--color-bg-subtle);
  border: 1px solid var(--color-border);
  color: var(--color-text-muted);
  line-height: 1;
  vertical-align: middle;
  margin-left: 4px;
}
```

IMPORTANT: These are CSS-only definitions. They do NOT require any HTML changes or JavaScript. They are ready for use when features need them.
  </action>
  <verify>
    <automated>grep -q "\.scroll-top" public/assets/css/design-system.css && grep -q "\.tip-wrap" public/assets/css/design-system.css && grep -q "\.confirm-dialog" public/assets/css/design-system.css && grep -q "\.theme-toggle" public/assets/css/design-system.css && grep -q "\.op-kbd" public/assets/css/design-system.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Five new component CSS patterns exist: scroll-to-top (fixed, circular, fade on .visible), tooltip (pure CSS, position above, arrow), confirm dialog (centered icon 56px, Fraunces title), theme toggle (switch with knob animation), keyboard hints (mono badge). All ready for HTML consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Add global scrollbar styling and extend print styles</name>
  <files>public/assets/css/design-system.css</files>
  <action>
Modify design-system.css in two areas:

**GLOBAL SCROLLBAR:**

Add global scrollbar rules near the top of the file, after the `:root` / `[data-theme="dark"]` token blocks but before the first component section. Find the line after the dark theme closing brace and utility reset rules. Add:

```css
/* ── Global Scrollbar ── */
::-webkit-scrollbar {
  width: 5px;
  height: 5px;
}

::-webkit-scrollbar-track {
  background: transparent;
}

::-webkit-scrollbar-thumb {
  background: var(--color-border);
  border-radius: 3px;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--color-text-light);
}
```

Note: The existing `.sidebar-scroll::-webkit-scrollbar` (line 667) stays as-is — it overrides the global 5px to 3px specifically for the sidebar, which is intentional.

**PRINT STYLES:**

1. In the main `@media print` block (line 3010), add to the hide list (after `.bottom-nav` on line 3045):
   ```
   .search-overlay,
   .search-trigger,
   .scroll-top,
   .theme-toggle,
   .mobile-bottom-nav,
   ```

2. After the existing print card rules (after line 3060), add `.print-only` utility:
   ```
   .print-only {
     display: none;
   }
   ```

3. Inside the `@media print` block, after existing rules, add:
   ```
   .print-only {
     display: block !important;
   }
   ```

IMPORTANT: Do NOT modify the sidebar-specific scrollbar rules (line 667-668). Do NOT remove any existing items from the print hide list. Only ADD to them.
  </action>
  <verify>
    <automated>grep -q "::-webkit-scrollbar {" public/assets/css/design-system.css && grep -q "width: 5px" public/assets/css/design-system.css && grep -q "\.print-only" public/assets/css/design-system.css && grep -q "scroll-top" public/assets/css/design-system.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Global scrollbar is 5px wide with themed thumb (border color, text-light on hover). Print styles extended to hide search overlay, scroll-top, theme toggle. Print-only utility class available for print-exclusive content.</done>
</task>

</tasks>

<verification>
- Scroll any long page — scrollbar should be thin (5px) with subtle themed thumb
- Print preview any page — search, scroll-top, theme toggle should be hidden; cards should have no shadow
- Inspect design-system.css for all 5 new component class patterns
- Verify sidebar scrollbar still uses its 3px override (not affected by global)
</verification>

<success_criteria>
- `.scroll-top` class exists with fixed bottom-right, circular, fade transition, accent hover
- `.tip-wrap` / `.tip-body` exist with positioned-above tooltip and arrow pseudo-element
- `.confirm-dialog` exists with `.confirm-dialog-icon` (56px circle), `.confirm-dialog-title` (Fraunces), centered layout
- `.theme-toggle` exists with `::after` knob and `translateX(20px)` on active
- `.op-kbd` exists with mono font, 10px, background badge
- Global `::-webkit-scrollbar` is 5px with `var(--color-border)` thumb
- Print hide list includes `.search-overlay`, `.scroll-top`, `.theme-toggle`
- `.print-only` utility works (hidden by default, visible in print)
</success_criteria>

<output>
After completion, create `.planning/phases/20.2-deep-ui-wireframe-alignment/20.2-04-SUMMARY.md`
</output>
