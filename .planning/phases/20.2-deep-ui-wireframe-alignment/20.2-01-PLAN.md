---
phase: 20.2-deep-ui-wireframe-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
autonomous: true
requirements:
  - UI-ALIGN-BUTTONS
  - UI-ALIGN-CARDS

must_haves:
  truths:
    - "Primary buttons show gradient background with inner highlight and colored shadow"
    - "Danger and success buttons show gradient + shadow pattern matching primary"
    - "Default .btn has visible border (not transparent)"
    - "All colored buttons lift on hover (translateY -1px) and press on active (scale .98)"
    - "Cards have hover transition on box-shadow, border-color, transform, background"
    - "Card titles are 13px uppercase bold with letter-spacing"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Updated button and card styles matching wireframe v3.19.2"
      contains: "linear-gradient.*color-primary"
  key_links:
    - from: "public/assets/css/design-system.css"
      to: "all pages using .btn and .card classes"
      via: "CSS cascade"
      pattern: "btn-primary|card-title"
---

<objective>
Update button and card component CSS in design-system.css to match wireframe v3.19.2 specifications.

Purpose: Buttons are the most visually impactful component — gradient, shadow, and lift effects bring the polished wireframe feel. Card title uppercase change is a global visual shift affecting 15+ pages.
Output: Updated `.btn`, `.btn-primary`, `.btn-danger`, `.btn-success`, `.card`, `.card-title` rules in design-system.css
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.2-deep-ui-wireframe-alignment/20.2-CONTEXT.md
@.planning/phases/20.2-deep-ui-wireframe-alignment/20.2-RESEARCH.md

<interfaces>
<!-- Current button CSS: design-system.css lines 1063-1227 -->
<!-- Current card CSS: design-system.css lines 1230-1263 -->

Current .btn (line 1063):
  font-weight: var(--font-medium)  /* 500 */
  border: 1px solid transparent
  transition: color, background-color, border-color, box-shadow

Current .btn-primary (line 1100):
  background: var(--color-primary)
  color: var(--color-primary-text)
  border-color: var(--color-primary)

Current .btn-primary:hover (line 1106):
  background: var(--color-primary-hover)
  border-color: var(--color-primary-hover)

Current .card (line 1230):
  background: var(--color-surface)
  border: 1px solid var(--color-border)
  border-radius: var(--radius-lg)
  box-shadow: var(--shadow-sm)
  /* NO transition */

Current .card-title (line 1253):
  font-size: var(--text-lg)  /* 18px */
  font-weight: var(--font-semibold)  /* 600 */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update button styles — gradient, shadow, hover lift, active press</name>
  <files>public/assets/css/design-system.css</files>
  <action>
Modify the following CSS rules in design-system.css:

1. `.btn` (line 1063): Change `font-weight` from `var(--font-medium)` to `var(--font-bold)` (700). Change `border: 1px solid transparent` to `border: 1px solid var(--color-border)`. Add `min-height: 40px`. Add `transform` to the transition list.

2. `.btn-primary` (line 1100): Replace flat background with gradient:
   ```
   background: linear-gradient(180deg, var(--color-primary) 0%, color-mix(in srgb, var(--color-primary), #000 8%) 100%);
   box-shadow: inset 0 1px 0 rgba(255,255,255,.12), 0 2px 4px rgba(22,80,224,.3);
   ```
   Keep border-color as var(--color-primary).

3. `.btn-primary:hover:not(:disabled)` (line 1106): Add `transform: translateY(-1px)` and enhance shadow:
   ```
   box-shadow: inset 0 1px 0 rgba(255,255,255,.15), 0 4px 8px rgba(22,80,224,.35);
   ```

4. Add NEW rule `.btn-primary:active:not(:disabled)` after the hover rule:
   ```
   .btn-primary:active:not(:disabled) {
     transform: scale(.98) translateY(0);
     box-shadow: inset 0 1px 0 rgba(255,255,255,.12), 0 1px 2px rgba(22,80,224,.2);
   }
   ```

5. `.btn-danger` (line 1132): Same gradient pattern but with `--color-danger`:
   ```
   background: linear-gradient(180deg, var(--color-danger) 0%, color-mix(in srgb, var(--color-danger), #000 8%) 100%);
   box-shadow: inset 0 1px 0 rgba(255,255,255,.12), 0 2px 4px rgba(220,38,38,.3);
   ```

6. `.btn-danger:hover:not(:disabled)` (line 1138): Add `transform: translateY(-1px)` and enhance shadow with danger color.

7. Add `.btn-danger:active:not(:disabled)` with same pattern.

8. `.btn-success` (line 1122): Same gradient pattern with `--color-success`:
   ```
   background: linear-gradient(180deg, var(--color-success) 0%, color-mix(in srgb, var(--color-success), #000 8%) 100%);
   box-shadow: inset 0 1px 0 rgba(255,255,255,.12), 0 2px 4px rgba(22,163,74,.3);
   ```

9. `.btn-success:hover:not(:disabled)` and `:active` — same hover lift and active press pattern.

10. DO NOT add gradient/lift to `.btn-ghost`, `.btn-link`, `.btn-secondary` — these stay flat per wireframe. But `.btn-secondary` keeps its existing border (already has `border-color: var(--color-border)`).

IMPORTANT: Preserve existing focus-visible, disabled, aria-busy, size variant, and icon button rules untouched. Only modify the rules listed above.
  </action>
  <verify>
    <automated>grep -c "linear-gradient" public/assets/css/design-system.css | grep -q "[3-9]" && grep -c "translateY(-1px)" public/assets/css/design-system.css | grep -q "[3-9]" && echo "PASS: gradient and hover lift found" || echo "FAIL"</automated>
  </verify>
  <done>Primary, danger, and success buttons show gradient backgrounds with inner highlights, colored shadows, hover lift translateY(-1px), and active press scale(.98). Default .btn has font-weight 700, min-height 40px, and visible border.</done>
</task>

<task type="auto">
  <name>Task 2: Update card styles — transition, hover, clickable hover, card-title uppercase</name>
  <files>public/assets/css/design-system.css</files>
  <action>
Modify the following CSS rules in design-system.css:

1. `.card` (line 1230): Add transition property:
   ```
   transition: box-shadow .18s ease, border-color .18s ease, transform .18s ease, background .18s ease;
   ```

2. Add NEW rule `.card:hover` after `.card` (after line 1235):
   ```
   .card:hover {
     box-shadow: var(--shadow-md);
   }
   ```

3. Add NEW rule `.card-clickable` for cards that are interactive (links, buttons):
   ```
   .card-clickable {
     cursor: pointer;
   }
   .card-clickable:hover {
     transform: translateY(-2px);
     box-shadow: var(--shadow-md);
     border-color: color-mix(in srgb, var(--color-primary) 25%, var(--color-border));
   }
   ```

4. `.card-title` (line 1253): Change to wireframe spec:
   ```
   .card-title {
     font-size: 13px;
     font-weight: 700;
     text-transform: uppercase;
     letter-spacing: .7px;
     color: var(--color-text);
   }
   ```

IMPORTANT: Do not modify `.card-header`, `.card-body`, `.card-footer`, `.card-description` — these stay as-is.
  </action>
  <verify>
    <automated>grep -q "text-transform: uppercase" public/assets/css/design-system.css && grep -q "card-clickable" public/assets/css/design-system.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Cards have smooth hover shadow transition, clickable cards lift on hover with accent border tint, and card titles are 13px uppercase bold with 0.7px letter-spacing.</done>
</task>

</tasks>

<verification>
- Open any page with buttons (e.g., operator console) — primary buttons should show gradient and lift on hover
- Open any page with cards (e.g., dashboard) — cards should have hover shadow, titles should be uppercase 13px
- Check that ghost buttons and link buttons are NOT affected by gradient
- Check that disabled buttons still look disabled (opacity 0.5, no hover effects)
</verification>

<success_criteria>
- `.btn-primary`, `.btn-danger`, `.btn-success` have gradient backgrounds with inner highlight and colored shadow
- All three have hover lift (translateY -1px) and active press (scale .98)
- `.btn` base has font-weight 700, min-height 40px, visible border
- `.card` has transition on box-shadow, border-color, transform, background
- `.card-clickable` hover lifts with accent border
- `.card-title` is 13px, uppercase, 700 weight, 0.7px letter-spacing
</success_criteria>

<output>
After completion, create `.planning/phases/20.2-deep-ui-wireframe-alignment/20.2-01-SUMMARY.md`
</output>
