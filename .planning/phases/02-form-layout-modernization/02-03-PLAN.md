---
phase: 02-form-layout-modernization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/vote.htmx.html
  - public/report.htmx.html
  - public/archives.htmx.html
  - public/trust.htmx.html
  - public/audit.htmx.html
  - public/analytics.htmx.html
  - public/help.htmx.html
autonomous: true
requirements: [FORM-02, FORM-03]

must_haves:
  truths:
    - "Tous les selects sur les 7 pages utilisent la classe form-select (pas form-input)"
    - "Tous les inputs sur les 7 pages utilisent la classe form-input de maniere coherente"
    - "Les barres de filtre multi-champs (archives, audit, trust) sont horizontales et exploitent la largeur"
    - "Aucun formulaire mono-colonne ne depasse 60% de la largeur du viewport"
  artifacts:
    - path: "public/vote.htmx.html"
      provides: "Consistent field classes"
      contains: "form-select"
    - path: "public/archives.htmx.html"
      provides: "Consistent field classes"
      contains: "form-select"
    - path: "public/audit.htmx.html"
      provides: "Consistent field classes"
      contains: "form-select"
    - path: "public/trust.htmx.html"
      provides: "Consistent field classes"
      contains: "form-select"
    - path: "public/analytics.htmx.html"
      provides: "Consistent field classes"
      contains: "form-select"
    - path: "public/report.htmx.html"
      provides: "Consistent field classes"
      contains: "form-input"
    - path: "public/help.htmx.html"
      provides: "Consistent field classes"
      contains: "form-input"
  key_links:
    - from: "all 7 pages"
      to: "public/assets/css/design-system.css"
      via: "form-input, form-select class normalization"
      pattern: "form-select"
---

<objective>
Normalize field classes and ensure horizontal layout on 7 light-form pages: vote, report, archives, trust, audit, analytics, and help. These pages have filter/search bars rather than data entry forms.

Purpose: Ensure FORM-02 (uniform field styling) and FORM-03 (no single-column >60% width) are satisfied across all remaining pages. Most work is class normalization (changing form-input on selects to form-select).
Output: 7 HTML files with consistent field classes and verified width constraints.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-form-layout-modernization/02-CONTEXT.md
@public/assets/css/design-system.css (lines 1826-1841 — form-input, form-select, form-textarea definitions)
</context>

<interfaces>
<!-- CSS classes for field normalization -->

From public/assets/css/design-system.css:
```css
/* These three classes provide uniform styling: same padding, font-size, border, radius */
.form-input,
.form-select,
.form-textarea {
  width: 100%;
  max-width: 100%;
  padding: var(--space-2) var(--space-3);
  font-size: var(--text-base);
  line-height: var(--leading-normal);
  color: var(--color-text);
  background: var(--color-surface-raised);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-base);
  min-height: var(--input-height);
}

/* Custom select chevron — only applied to .form-select, NOT .form-input */
select.form-input, .form-select {
  appearance: none;
  background-image: url("data:image/svg+xml,...chevron...");
  padding-right: 36px;
}
```

Key insight: Using `form-select` instead of `form-input` on `<select>` elements gives the proper custom chevron appearance.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Normalize field classes on archives, audit, trust, and analytics pages</name>
  <files>public/archives.htmx.html, public/audit.htmx.html, public/trust.htmx.html, public/analytics.htmx.html</files>
  <action>
**archives.htmx.html:**
- Line ~131: `<select class="form-input year-select">` — change to `<select class="form-select year-select">`
- Line ~134: search input already uses `form-input` — correct, leave as-is
- Line ~203: `<select class="form-input">` (export meeting select) — change to `<select class="form-select">`

**audit.htmx.html:**
- Line ~138: search input uses `form-input` — correct, leave as-is
- Line ~140: `<select class="form-input audit-sort-select">` — change to `<select class="form-select audit-sort-select">`

**trust.htmx.html:**
- Line ~75: `<select class="form-input">` (meeting select) — change to `<select class="form-select">`
- Line ~250: search input uses `form-input` — correct, leave as-is

**analytics.htmx.html:**
- Line ~45: `<select class="form-input form-input-sm analytics-year-filter">` — change to `<select class="form-select form-select-sm analytics-year-filter">`. Note: also change `form-input-sm` to `form-select-sm` for consistent sizing class. Verify `form-select-sm` is not defined in design-system.css — if not, keep `form-input-sm` as a size modifier since `form-input-sm` is defined (line 1843).

Actually, checking the CSS: `.form-input-sm, .form-select-sm` are defined together (line 1843-1849). So `form-select-sm` IS a valid class. Use `form-select form-select-sm`.

IMPORTANT: Do not modify IDs, data attributes, aria-labels, or any non-class attributes. Only change CSS class values.
  </action>
  <verify>
    <automated>php -r "\$pages=['public/archives.htmx.html','public/audit.htmx.html','public/trust.htmx.html','public/analytics.htmx.html']; \$ok=true; foreach(\$pages as \$f){\$c=file_get_contents(\$f); if(preg_match('/<select[^>]*class=\"[^\"]*form-input(?!-sm)[^\"]*\"/', \$c, \$m)){echo 'FAIL '.\$f.': '.\$m[0].PHP_EOL; \$ok=false;}} if(\$ok) echo 'ALL OK: no select with form-input class'.PHP_EOL;"</automated>
  </verify>
  <done>All select elements on archives, audit, trust, and analytics pages use form-select class. No select uses form-input. Input elements retain form-input correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize field classes on vote, report, and help pages</name>
  <files>public/vote.htmx.html, public/report.htmx.html, public/help.htmx.html</files>
  <action>
**vote.htmx.html:**
- Lines ~78-87: Two form-groups exist. Check if they contain selects or inputs and ensure correct class usage. If any select uses `form-input`, change to `form-select`.

**report.htmx.html:**
- Line ~78: `<div class="form-group flex-1 mb-0">` — check the input/select inside. If it's a select, ensure it uses `form-select`. If it's an input, confirm `form-input`.

**help.htmx.html:**
- Line ~175: `<input type="text" id="faqSearch" class="form-input search-input-field">` — already uses `form-input`, correct. Leave as-is.

For all 3 pages: Scan the full file for any remaining `<select` elements that use `form-input` instead of `form-select`, and any `<textarea` elements that use `form-input` instead of `form-textarea`. Fix all instances found.

**Width constraint (FORM-03)**: These pages have filter bars, not full forms. Filter bars are typically inline within cards/toolbars that already constrain width. No action needed unless a standalone single-column form is found exceeding 60% viewport width.

IMPORTANT: Do not modify IDs, data attributes, aria-labels, or any non-class attributes.
  </action>
  <verify>
    <automated>php -r "\$pages=['public/vote.htmx.html','public/report.htmx.html','public/help.htmx.html']; \$ok=true; foreach(\$pages as \$f){\$c=file_get_contents(\$f); if(preg_match('/<select[^>]*class=\"[^\"]*form-input[^\"]*\"/', \$c, \$m)){echo 'FAIL '.\$f.': '.\$m[0].PHP_EOL; \$ok=false;}} if(\$ok) echo 'ALL OK: no select with form-input class'.PHP_EOL;"</automated>
  </verify>
  <done>All select elements on vote, report, and help pages use form-select. All input elements use form-input. All textarea elements use form-textarea. Field styling is uniform across all 7 light-form pages.</done>
</task>

</tasks>

<verification>
1. Grep across all 16 pages (excluding wizard): zero instances of `<select class="...form-input...">`
2. All pages load without console errors
3. Select elements display custom chevron (visual confirmation that form-select is applied)
</verification>

<success_criteria>
- Every select element across all 7 pages uses form-select (not form-input)
- Every textarea element uses form-textarea (not form-input)
- Input elements correctly retain form-input
- No IDs, HTMX attributes, or JS hooks are broken
</success_criteria>

<output>
After completion, create `.planning/phases/02-form-layout-modernization/02-03-SUMMARY.md`
</output>
