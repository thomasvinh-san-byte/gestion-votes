---
phase: 40-configuration-cluster
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/admin.htmx.html
  - public/assets/css/admin.css
  - public/assets/js/pages/admin.js
  - public/help.htmx.html
  - public/assets/css/help.css
  - public/assets/js/pages/help-faq.js
autonomous: true
requirements: [CORE-06, SEC-03]

must_haves:
  truths:
    - "Admin KPI strip values each have ag-tooltip explaining what the metric measures"
    - "Admin tab labels have 16px section icons and ag-tooltip explaining what each tab manages"
    - "Compact users card replaced with a 4-KPI strip using design-system kpi-grid pattern"
    - "State machine nodes have ag-tooltip explaining possible actions from each state"
    - "Help tabs use filter-tab pill pattern instead of solid primary background"
    - "FAQ section titles have left 4px primary border accent instead of bottom underline"
    - "FAQ answer blocks have left 2px primary border accent for visual continuity"
    - "Help support card at bottom is a prominent CTA card with icon"
    - "Doc links section wrapped in a proper card with card-header"
  artifacts:
    - path: "public/admin.htmx.html"
      provides: "ag-tooltip wraps on KPI labels, tab icons, users KPI strip, state node tooltips"
    - path: "public/assets/css/admin.css"
      provides: "Tab icon styling, users kpi-grid strip, state node tooltip compat"
    - path: "public/assets/js/pages/admin.js"
      provides: "Users KPI count population, role count rendering"
    - path: "public/help.htmx.html"
      provides: "Filter-tab pill classes on category tabs, doc-links card wrapper, support card upgrade"
    - path: "public/assets/css/help.css"
      provides: "Filter-tab pill styling, section title left-border, answer left-border, support card elevation, search input upgrade"
    - path: "public/assets/js/pages/help-faq.js"
      provides: "Tab switching using filter-tab active class"
  key_links:
    - from: "public/admin.htmx.html"
      to: "public/assets/css/admin.css"
      via: "KPI strip uses kpi-grid from design-system, tab icon styling"
      pattern: "kpi-grid|admin-tab.*icon"
    - from: "public/help.htmx.html"
      to: "public/assets/css/help.css"
      via: "filter-tab pill classes, faq-section-title left-border"
      pattern: "filter-tab|faq-section-title.*border-left"
---

<objective>
Redesign Admin page (KPI tooltips, tab icons, users stats strip, state node guidance) and Help/FAQ page (filter-tab pills, section title accents, search upgrade, support card prominence) to match the configuration cluster quality bar.

Purpose: Admin and Help are low-frequency but high-trust pages. Admin must explain every metric and setting tab. Help must let users find answers by category without scrolling through unrelated content.
Output: 6 files modified — admin page has self-explanatory KPIs and tabs, help page has premium category navigation and section styling.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-configuration-cluster/40-RESEARCH.md

@public/admin.htmx.html
@public/assets/css/admin.css
@public/assets/js/pages/admin.js
@public/help.htmx.html
@public/assets/css/help.css
@public/assets/js/pages/help-faq.js
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin page — KPI tooltips, tab icons with tooltips, users KPI strip, state machine node tooltips</name>
  <files>public/admin.htmx.html, public/assets/css/admin.css, public/assets/js/pages/admin.js</files>
  <read_first>public/admin.htmx.html, public/assets/css/admin.css, public/assets/js/pages/admin.js, public/assets/css/design-system.css, .planning/phases/40-configuration-cluster/40-RESEARCH.md</read_first>
  <action>
**HTML changes in admin.htmx.html:**

1. KPI strip tooltips: Wrap each `.dash-kpi-label` text content in `<ag-tooltip>` with a French explanation of what the metric measures:
   - "AG a venir" -> `<ag-tooltip text="Nombre de seances programmees dans le futur" position="top">AG a venir</ag-tooltip>`
   - "En cours" -> `<ag-tooltip text="Seances actuellement en cours de vote" position="top">En cours</ag-tooltip>`
   - "Convocations" -> `<ag-tooltip text="Convocations en attente d'envoi pour les prochaines seances" position="top">Convocations</ag-tooltip>`
   - "PV en attente" -> `<ag-tooltip text="Proces-verbaux non encore valides apres cloture de seance" position="top">PV en attente</ag-tooltip>`

2. Replace compact users card (the card showing "-- utilisateurs" + "Gerer les utilisateurs" link, around lines 199-209) with a 4-KPI strip using design-system `.kpi-grid` + `.kpi-card` pattern:
```html
<div class="kpi-grid" style="margin-bottom: var(--space-4);">
  <div class="kpi-card">
    <ag-tooltip text="Nombre total de comptes utilisateurs enregistres" position="top">
      <span class="kpi-label">Total utilisateurs</span>
    </ag-tooltip>
    <span class="kpi-value" id="adminKpiUsers">--</span>
  </div>
  <div class="kpi-card">
    <ag-tooltip text="Utilisateurs avec le role Administrateur" position="top">
      <span class="kpi-label">Admins</span>
    </ag-tooltip>
    <span class="kpi-value" id="adminKpiAdmins">--</span>
  </div>
  <div class="kpi-card">
    <ag-tooltip text="Utilisateurs avec le role Operateur de seance" position="top">
      <span class="kpi-label">Operateurs</span>
    </ag-tooltip>
    <span class="kpi-value" id="adminKpiOperators">--</span>
  </div>
  <div class="kpi-card">
    <ag-tooltip text="Utilisateurs connectes dans les 7 derniers jours" position="top">
      <span class="kpi-label">Actifs (7j)</span>
    </ag-tooltip>
    <span class="kpi-value" id="adminKpiActive">--</span>
  </div>
</div>
```

3. Admin tab labels: Add a 16px SVG icon BEFORE each tab label text inside `.admin-tab` buttons. Use inline SVG with appropriate icons:
   - "Roles de seance" -> users/team icon
   - "Politiques" -> shield/policy icon
   - "Permissions" -> lock icon
   - "Machine a etats" -> git-branch/workflow icon
   - "Systeme" -> server/cpu icon
Also wrap each tab label in ag-tooltip:
```html
<button class="admin-tab active" data-tab="roles">
  <svg class="icon admin-tab-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><!-- users icon paths --></svg>
  <ag-tooltip text="Definir les roles et permissions pour les participants aux seances" position="bottom">
    Roles de seance
  </ag-tooltip>
</button>
```

4. State machine nodes: If `.state-flow-visual` exists with state node elements, wrap each node text in `<ag-tooltip>` explaining what actions are possible from that state. Example:
   - "Brouillon" -> `<ag-tooltip text="Seance en cours de creation. Actions: programmer, supprimer" position="top">Brouillon</ag-tooltip>`
   - "Programmee" -> `<ag-tooltip text="Seance programmee. Actions: convoquer, annuler, ouvrir" position="top">Programmee</ag-tooltip>`
   - etc. for each state node

**CSS changes in admin.css:**

1. Admin tab icon styling:
```css
.admin-tab {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
}
.admin-tab-icon {
  width: 16px;
  height: 16px;
  flex-shrink: 0;
  color: var(--color-text-muted);
}
.admin-tab.active .admin-tab-icon {
  color: var(--color-primary);
}
```

2. Remove any local `.stat-card` / `.stats-grid` overrides that duplicate design-system.css `.kpi-grid` / `.kpi-card` (per Phase 39 lesson — use DS classes directly). Search admin.css for `.stat-card`, `.stat-card-value`, `.stat-card-label` and mark them as deprecated with a comment or remove them if the new `.kpi-grid` strip replaces them.

3. Ensure `.kpi-grid` / `.kpi-card` / `.kpi-value` / `.kpi-label` from design-system.css are NOT overridden in admin.css. If admin.css has local definitions, remove them to use DS directly.

**JS changes in admin.js:**

1. Populate the new users KPI strip values. Find where users data is loaded (likely in the dashboard or system tab initialization). After loading users, compute:
```javascript
function updateAdminUserKpis(users) {
  var total = users.length;
  var admins = users.filter(function(u) { return u.role === 'admin'; }).length;
  var operators = users.filter(function(u) { return u.role === 'operator'; }).length;
  var sevenDaysAgo = Date.now() - 7 * 24 * 60 * 60 * 1000;
  var active = users.filter(function(u) { return u.last_login && new Date(u.last_login).getTime() > sevenDaysAgo; }).length;
  var el;
  el = document.getElementById('adminKpiUsers'); if (el) el.textContent = total;
  el = document.getElementById('adminKpiAdmins'); if (el) el.textContent = admins;
  el = document.getElementById('adminKpiOperators'); if (el) el.textContent = operators;
  el = document.getElementById('adminKpiActive'); if (el) el.textContent = active;
}
```
Call this after users data is fetched. If no users API is called on admin page load, add a fetch to `/api/v1/users.php` or use the existing data source.

**CRITICAL — Anti-pattern from RESEARCH:** Do NOT add local kpi-grid/kpi-card overrides in admin.css. Use design-system.css definitions directly.

**CRITICAL — Pitfall 2:** ag-tooltip wrapping KPI cards would break the grid. Instead, place ag-tooltip INSIDE the kpi-card, wrapping the kpi-label (not the whole card).
  </action>
  <verify>
    <automated>grep -c "ag-tooltip" public/admin.htmx.html | head -1 && grep -c "kpi-grid" public/admin.htmx.html | head -1 && grep -c "admin-tab-icon" public/assets/css/admin.css | head -1</automated>
  </verify>
  <acceptance_criteria>
  - Each of the 4 existing KPI labels (AG a venir, En cours, Convocations, PV en attente) wrapped in ag-tooltip with French explanation
  - Compact users card replaced with 4-KPI strip using kpi-grid/kpi-card from design-system.css
  - KPI values populated from users API (total, admins, operators, active in 7 days) with JetBrains Mono numbers
  - Each admin tab has a 16px inline SVG icon before the label text
  - Each admin tab label wrapped in ag-tooltip explaining what the tab manages
  - Active tab icon inherits primary color
  - State machine nodes have ag-tooltip explaining possible actions
  - No local kpi-grid/kpi-card overrides in admin.css (uses design-system.css directly)
  </acceptance_criteria>
  <done>Admin page KPIs are self-explanatory with tooltips, tabs have icons and tooltips, users section shows a proper KPI strip, state nodes have guidance tooltips</done>
</task>

<task type="auto">
  <name>Task 2: Help/FAQ page — filter-tab pills, section title left-border accent, search upgrade, doc-links card, support card prominence</name>
  <files>public/help.htmx.html, public/assets/css/help.css, public/assets/js/pages/help-faq.js</files>
  <read_first>public/help.htmx.html, public/assets/css/help.css, public/assets/js/pages/help-faq.js, public/assets/css/design-system.css, .planning/phases/40-configuration-cluster/40-RESEARCH.md</read_first>
  <action>
**CSS changes in help.css:**

1. Category tabs: Replace solid-background `.help-tab.active` with filter-tab pill pattern (Pattern 5 from RESEARCH):
```css
.help-tab {
  padding: 0.375rem 0.875rem;
  border: 1px solid var(--color-border);
  background: none;
  border-radius: var(--radius-full);
  font-size: var(--text-sm);
  font-weight: 500;
  color: var(--color-text-muted);
  cursor: pointer;
  transition: background var(--duration-fast), color var(--duration-fast), border-color var(--duration-fast);
}
.help-tab:hover {
  background: var(--color-bg-subtle);
  color: var(--color-text);
}
.help-tab.active {
  background: var(--color-primary-subtle);
  color: var(--color-primary);
  border-color: var(--color-primary);
  font-weight: 600;
}
```
Remove the existing `.help-tab` and `.help-tab.active` rules (solid primary bg, white text, etc.) and replace with the above.

2. Remove tab bar bottom border:
```css
.help-tabs {
  /* Remove: border-bottom: 1px solid var(--color-border); */
  border-bottom: none;
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  padding-bottom: var(--space-3);
}
```

3. FAQ section title: Replace bottom underline with left-border accent:
```css
.faq-section-title {
  border-bottom: none; /* remove existing underline */
  border-left: 4px solid var(--color-primary);
  padding-left: 0.75rem;
  font-size: var(--text-base);
  font-weight: 700;
  color: var(--color-text);
  margin-bottom: var(--space-3);
  margin-top: var(--space-section);
}
.faq-section-title:first-of-type {
  margin-top: 0;
}
```

4. FAQ answer left-border accent:
```css
.faq-answer {
  border-left: 2px solid var(--color-primary-subtle);
  padding-left: var(--space-4);
  margin-left: var(--space-2);
}
```

5. Search input upgrade (increase visual weight):
```css
.search-input-wrap {
  margin-bottom: var(--space-4);
}
.search-input-wrap .form-input,
.search-input-wrap input[type="text"] {
  height: 48px;
  font-size: var(--text-base);
  padding-left: 2.75rem; /* space for icon */
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-sm);
  transition: box-shadow var(--duration-fast), border-color var(--duration-fast);
}
.search-input-wrap .form-input:focus,
.search-input-wrap input[type="text"]:focus {
  box-shadow: var(--shadow-md);
  border-color: var(--color-primary);
}
.search-input-icon {
  top: 50%;
  transform: translateY(-50%);
  width: 20px;
  height: 20px;
}
```

6. Tour card upgrade:
```css
.tour-icon {
  width: 44px;
  height: 44px;
  color: var(--color-primary);
}
.tour-card:hover {
  box-shadow: var(--shadow-md);
  transform: translateY(-1px);
  transition: box-shadow var(--duration-fast), transform var(--duration-fast);
}
```

7. Doc links section as card:
```css
.doc-links-card {
  background: var(--color-surface);
  border: 1px solid var(--color-border-subtle);
  border-radius: var(--radius-lg);
  overflow: hidden;
  margin-top: var(--space-section);
}
.doc-links-card-header {
  padding: var(--space-3) var(--space-card);
  border-bottom: 1px solid var(--color-border-subtle);
  font-weight: 700;
  font-size: var(--text-base);
}
.doc-links-card-body {
  padding: var(--space-card);
}
```

8. Support card prominence:
```css
.help-support {
  background: var(--color-primary-subtle);
  border: 1px solid var(--color-primary);
  border-radius: var(--radius-lg);
  padding: var(--space-6);
  margin-top: var(--space-section);
  display: flex;
  align-items: center;
  gap: var(--space-4);
  box-shadow: var(--shadow-sm);
}
.help-support-icon {
  width: 48px;
  height: 48px;
  color: var(--color-primary);
  flex-shrink: 0;
}
.help-support-title {
  font-weight: 700;
  font-size: var(--text-lg);
  margin-bottom: 0.25rem;
}
.help-support-desc {
  font-size: var(--text-sm);
  color: var(--color-text-muted);
}
```

**HTML changes in help.htmx.html:**

1. Category tabs: Change class from old help-tab styling to the new filter-tab pill. No class name change needed — CSS handles it. BUT if the HTML has inline styles on any `.help-tab`, remove them.

2. FAQ section titles: If section titles have inline icon SVGs, keep them. If they don't, add a category icon (16px) inline before each section title text. Example section icons:
   - "Premiers pas" -> play-circle icon
   - "Votes et resolutions" -> check-square icon
   - "Gestion des membres" -> users icon
   - "Securite" -> shield icon
   - etc. based on actual section names in the HTML

3. Doc links section: Wrap the existing `.doc-links` div in a card structure:
```html
<div class="doc-links-card">
  <div class="doc-links-card-header">Documentation</div>
  <div class="doc-links-card-body">
    <!-- existing doc-links content moves here -->
  </div>
</div>
```

4. Support card: Add a 48px icon SVG (headphones or life-buoy icon) inside the `.help-support` card. Structure:
```html
<div class="help-support">
  <svg class="help-support-icon" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><!-- headphones/life-buoy icon paths --></svg>
  <div>
    <div class="help-support-title">Besoin d'aide ?</div>
    <p class="help-support-desc">Contactez l'administrateur ou consultez la documentation technique.</p>
    <!-- existing contact links/buttons -->
  </div>
</div>
```

5. Help header: Upgrade the `.help-header` to feel more like a hero zone — add a subtitle below the h1:
```html
<div class="help-header">
  <h1>Centre d'aide</h1>
  <p class="help-header-subtitle">Trouvez rapidement des reponses a vos questions</p>
</div>
```
Add CSS:
```css
.help-header-subtitle {
  font-size: var(--text-base);
  color: var(--color-text-muted);
  margin-top: 0.25rem;
}
```

**JS changes in help-faq.js:**

1. If tab switching JS uses class manipulation for `.help-tab.active`, verify it still works with the new pill styling (it should — no class name change). The JS should:
   - Remove `active` from all `.help-tab` buttons
   - Add `active` to clicked tab
   - Filter visible FAQ sections
This logic should already exist — verify it works unchanged.

2. If the search input filtering JS references any class names that changed, update selectors. But since we're only changing CSS rules (not class names), no JS changes should be needed for search.

**CRITICAL — Pitfall 5:** Do NOT remove the tour-grid section or tour-card HTML. The rejection was about not building NEW tours, not removing existing ones. Only improve visual treatment.
  </action>
  <verify>
    <automated>grep -c "border-radius.*radius-full" public/assets/css/help.css | head -1 && grep -c "border-left.*4px" public/assets/css/help.css | head -1 && grep -c "doc-links-card" public/help.htmx.html | head -1 && grep -c "help-support-icon" public/help.htmx.html | head -1</automated>
  </verify>
  <acceptance_criteria>
  - Help tabs are filter-tab pills (rounded, border, primary-subtle+primary on active) not solid primary background
  - Tab bar has no bottom border line — pills float with gap
  - FAQ section titles have left 4px primary border accent, no bottom underline
  - FAQ answer blocks have left 2px primary-subtle border for visual continuity
  - Search input is 48px height with larger font, shadow on focus, rounded corners
  - Tour card icons are 44px with hover shadow+lift effect
  - Doc links section wrapped in a card with "Documentation" header
  - Support card has 48px icon, prominent CTA layout, shadow
  - Help header has subtitle "Trouvez rapidement des reponses a vos questions"
  - Tour section NOT removed (Pitfall 5 — visual upgrade only)
  - Tab switching JS still works (class names unchanged)
  </acceptance_criteria>
  <done>Help/FAQ page has premium filter-tab category pills, section titles with left-border accents, prominent search, doc-links card wrapper, and CTA support card with icon</done>
</task>

</tasks>

<verification>
- Open admin.htmx.html in browser: KPI strip labels show tooltips on hover, tabs have icons and tooltips
- Admin users section: 4-KPI strip shows Total/Admins/Operateurs/Actifs with JetBrains Mono numbers
- Admin state machine: hovering state nodes shows tooltip with available actions
- Open help.htmx.html in browser: category tabs are pills (not solid blocks)
- Click different help tabs: active state switches to primary-subtle pill, sections filter correctly
- FAQ section titles: left-border accent visible, no bottom underline
- FAQ answers: left 2px primary-subtle border visible when expanded
- Search input: 48px height, shadow on focus
- Scroll to bottom: doc-links in card wrapper, support card with icon prominent
- Dark mode: pill borders visible, left-border accents contrast correctly, tooltip backgrounds correct
</verification>

<success_criteria>
Admin page KPIs and tabs are self-explanatory — every metric has a tooltip, every tab has an icon and explanation. Help/FAQ page has premium category navigation with filter-tab pills, clean section titles with accent borders, and a prominent support CTA. Both pages feel visually consistent with the settings page from plan 01.
</success_criteria>

<output>
After completion, create `.planning/phases/40-configuration-cluster/40-02-SUMMARY.md`
</output>
