---
phase: 41-public-and-utility-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
  - public/assets/css/landing.css
autonomous: true
requirements:
  - SEC-02

must_haves:
  truths:
    - "Landing hero communicates 'officiel et confiance' with trust signals visible below subtitle"
    - "Features section uses card layout with hover lift effect, not flat icon-list rows"
    - "Bottom CTA section with gradient strip and prominent button is visible after features"
    - "Hero title is readable in both light and dark mode"
  artifacts:
    - path: "public/index.html"
      provides: "Landing page with trust strip, CTA section, upgraded login button"
      contains: "hero-trust-strip"
    - path: "public/assets/css/landing.css"
      provides: "Landing CSS with trust strip, feature cards, CTA section, dark mode fix"
      contains: "cta-section"
  key_links:
    - from: "public/index.html"
      to: "public/assets/css/landing.css"
      via: "class references"
      pattern: "hero-trust-strip|cta-section|btn-cta"
---

<objective>
Redesign the landing page to communicate "officiel et confiance" instantly — trust signal strip below hero, feature items upgraded to cards with hover lift, gradient bottom CTA section, and dark mode fix on hero title.

Purpose: The landing page is the first thing external visitors see. It must immediately signal professional governance tool quality.
Output: Redesigned landing page with trust strip, feature cards, CTA section.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-public-and-utility-pages/41-RESEARCH.md
@public/index.html
@public/assets/css/landing.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing page HTML additions — trust strip and CTA section</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (full file — understand current structure)
    - public/assets/css/landing.css (understand existing classes)
    - public/assets/css/login.css (reference gradient CTA pattern lines ~1-50)
  </read_first>
  <action>
    Add three HTML blocks to public/index.html:

    1. **Trust strip** — After the `.hero-bullets` ul (line ~62), before the `.login-card` div, insert:
    ```html
    <div class="hero-trust-strip">
      <div class="trust-item">
        <svg class="trust-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path></svg>
        <span>S&eacute;curis&eacute;</span>
      </div>
      <div class="trust-item">
        <svg class="trust-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 3 2 7v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V7l-10-4z"></path><path d="m9 12 2 2 4-4"></path></svg>
        <span>Conforme</span>
      </div>
      <div class="trust-item">
        <svg class="trust-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="10"></circle><polyline points="12 6 12 12 16 14"></polyline></svg>
        <span>Temps r&eacute;el</span>
      </div>
    </div>
    ```

    2. **CTA section** — After the closing `</section>` of `.features-section` (line ~326), before the `<footer>`, insert:
    ```html
    <section class="cta-section" aria-labelledby="cta-title">
      <div class="cta-body">
        <h2 id="cta-title" class="cta-title">Pr&ecirc;t &agrave; commencer ?</h2>
        <p class="cta-subtitle">Cr&eacute;ez votre premi&egrave;re s&eacute;ance en quelques minutes.</p>
        <a href="#login-card" class="btn-cta">Commencer</a>
      </div>
    </section>
    ```

    3. **Footer version** — In the `.landing-footer`, update the copyright paragraph to include version: add "v4.2" before the dash.

    Do NOT change the login button class — it already has `login-btn` class (line 83: `btn btn-primary btn-lg login-btn`), which means the gradient is already applied if login.css is loaded. Since landing.css is loaded instead, the gradient must be added in landing.css (Task 2).
  </action>
  <verify>
    <automated>grep -c "hero-trust-strip\|cta-section\|cta-title" public/index.html | grep -q "3" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - hero-trust-strip div with 3 trust-item children exists between hero-bullets and login-card
    - cta-section with cta-title, cta-subtitle, btn-cta exists between features-section and footer
    - Footer contains v4.2 version indicator
  </acceptance_criteria>
  <done>Landing HTML has trust strip, CTA section, and version in footer</done>
</task>

<task type="auto">
  <name>Task 2: Landing CSS — trust strip, feature cards, CTA section, dark mode fix, hero gradient</name>
  <files>public/assets/css/landing.css</files>
  <read_first>
    - public/assets/css/landing.css (full file)
    - public/assets/css/login.css (gradient pattern, trust signal pattern)
    - public/assets/css/design-system.css (tokens: --color-text, --font-display, --radius-xl, --shadow-lg, --space-*, --color-primary, --color-primary-hover, --color-primary-text, --color-surface, --color-border, --color-text-muted, --color-success, --color-text-inverse)
  </read_first>
  <action>
    Add/modify the following CSS in landing.css:

    **A. Fix hero title dark mode (Pitfall 6 from RESEARCH):**
    Find `.hero-title` rule. Change `color: var(--color-text-dark, #1a1a1a)` to `color: var(--color-text)`.

    **B. Upgrade hero gradient to dual radial:**
    Find `.hero` background rule. Replace the single radial gradient with:
    ```css
    background:
      radial-gradient(ellipse 100% 60% at 60% -10%, color-mix(in srgb, var(--color-primary) 15%, transparent) 0%, transparent 55%),
      radial-gradient(ellipse 60% 40% at 20% 80%, color-mix(in srgb, var(--color-primary) 6%, transparent) 0%, transparent 50%),
      var(--color-bg);
    ```

    **C. Add trust strip styles (new rules):**
    ```css
    .hero-trust-strip {
      display: flex;
      align-items: center;
      gap: var(--space-6);
      margin-top: var(--space-6);
      flex-wrap: wrap;
    }
    .trust-item {
      display: flex;
      align-items: center;
      gap: var(--space-2);
      font-size: var(--text-sm);
      color: var(--color-text-muted);
      font-weight: var(--font-medium);
    }
    .trust-icon {
      width: 16px;
      height: 16px;
      color: var(--color-success);
      flex-shrink: 0;
    }
    ```

    **D. Upgrade feature items to cards (Pitfall 7 from RESEARCH):**
    Find `.feature-item` rules. Replace with card pattern:
    ```css
    .feature-item {
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
      padding: var(--space-6);
      background: var(--color-surface);
      border: 1px solid var(--color-border);
      border-radius: var(--radius-xl);
      transition: transform var(--duration-normal) var(--ease-out),
                  box-shadow var(--duration-normal) var(--ease-out);
    }
    .feature-item:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-lg);
    }
    ```
    Also update `.features-grid` from `auto-fit minmax(280px, 1fr)` to `repeat(3, 1fr)` for forced 3-column grid. Add responsive fallback at the existing mobile breakpoint: `grid-template-columns: 1fr` for small screens.

    **E. Add CTA section styles (new rules):**
    ```css
    .cta-section {
      background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-hover) 100%);
      padding: var(--space-16) var(--space-6);
      text-align: center;
    }
    .cta-body {
      max-width: 600px;
      margin: 0 auto;
    }
    .cta-title {
      font-family: var(--font-display);
      font-size: clamp(1.75rem, 4vw, 2.5rem);
      font-weight: 700;
      color: var(--color-text-inverse);
      margin-bottom: var(--space-3);
    }
    .cta-subtitle {
      color: rgba(255,255,255,0.85);
      font-size: var(--text-lg);
      margin-bottom: var(--space-8);
    }
    .btn-cta {
      display: inline-flex;
      align-items: center;
      padding: var(--space-4) var(--space-8);
      background: var(--color-surface-raised);
      color: var(--color-primary);
      border-radius: var(--radius-lg);
      font-weight: var(--font-bold);
      font-size: var(--text-md);
      text-decoration: none;
      transition: transform var(--duration-fast), box-shadow var(--duration-fast);
    }
    .btn-cta:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-lg);
    }
    ```

    **F. Add login-btn gradient to landing.css (Pitfall 1 — don't import login.css):**
    The `.login-btn` class is already on the button in HTML. Add the gradient rule in landing.css so it takes effect without loading login.css:
    ```css
    .login-card .login-btn {
      background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-hover) 100%);
      color: var(--color-primary-text);
      border: none;
    }
    .login-card .login-btn:hover {
      box-shadow: var(--shadow-md);
    }
    ```
  </action>
  <verify>
    <automated>grep -c "hero-trust-strip\|cta-section\|cta-title\|btn-cta\|feature-item.*flex-direction.*column\|color: var(--color-text)" public/assets/css/landing.css | xargs -I{} test {} -ge 4 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .hero-title uses var(--color-text) not var(--color-text-dark)
    - .hero has dual radial gradient
    - .hero-trust-strip, .trust-item, .trust-icon rules exist
    - .feature-item is now a card with flex-direction: column, background, border, radius, hover lift
    - .features-grid uses repeat(3, 1fr)
    - .cta-section, .cta-body, .cta-title, .cta-subtitle, .btn-cta rules exist
    - .login-card .login-btn has gradient background
  </acceptance_criteria>
  <done>Landing page CSS produces trust strip, feature cards with hover lift, gradient CTA section, dark-mode-safe hero title, and gradient login button</done>
</task>

</tasks>

<verification>
Open http://localhost/index.html in browser. Verify:
1. Hero title renders in both light and dark mode (toggle theme)
2. Trust strip (Securise, Conforme, Temps reel) visible below hero subtitle
3. Feature items are now cards with hover lift effect
4. CTA section visible as gradient strip before footer
5. Login button has gradient background
6. Footer shows v4.2 version
</verification>

<success_criteria>
Landing page has trust signals, feature cards, gradient CTA section, and correct dark mode rendering — visually distinct from pre-v4.2 version.
</success_criteria>

<output>
After completion, create `.planning/phases/41-public-and-utility-pages/41-01-SUMMARY.md`
</output>
