---
phase: 22-final-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - docker-compose.prod.yml
  - deploy/entrypoint.sh
  - bin/check-prod-readiness.sh
  - render.yaml
  - render-production.yaml
  - .env.example
  - .env.production
  - .env.demo
  - database/setup_demo_az.sh
  - docs/DEPLOIEMENT_DOCKER.md
  - docs/DEPLOIEMENT_RENDER.md
  - docs/DOCKER_INSTALL.md
  - docs/GUIDE_TEST_LOCAL.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/STACK.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/ (historical files)
  - .planning/milestones/ (historical files)
  - .planning/research/ (historical files)
autonomous: true
requirements: [CLN-01]

must_haves:
  truths:
    - "grep -rn 'DEMO_' across entire repo returns zero matches (excluding .git and phase 22 plan/context/research files)"
    - "grep -rn 'LOAD_DEMO_DATA' across entire repo returns zero matches (excluding .git and phase 22 plan/context/research files)"
    - ".env.demo file does not exist"
    - "database/setup_demo_az.sh file does not exist"
    - "All config files use LOAD_SEED_DATA instead of LOAD_DEMO_DATA"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Dev docker config with LOAD_SEED_DATA"
      contains: "LOAD_SEED_DATA"
    - path: "deploy/entrypoint.sh"
      provides: "Entrypoint with LOAD_SEED_DATA checks"
      contains: "LOAD_SEED_DATA"
    - path: ".env.example"
      provides: "Example env with LOAD_SEED_DATA"
      contains: "LOAD_SEED_DATA"
  key_links:
    - from: "docker-compose.yml"
      to: "deploy/entrypoint.sh"
      via: "LOAD_SEED_DATA env var"
      pattern: "LOAD_SEED_DATA"
    - from: ".env.example"
      to: "docker-compose.yml"
      via: "env_file reference"
      pattern: "LOAD_SEED_DATA"
---

<objective>
Total eradication of all DEMO_ references from the entire repository: config files, shell scripts, documentation, and ALL planning files including historical ones.

Purpose: CLN-01 requires zero DEMO_ constants in the codebase. While source code (JS/PHP) was already cleaned in Phases 16-17, the LOAD_DEMO_DATA environment variable persists across infrastructure config, deploy scripts, documentation, and planning files. Per user decision: "Planning files (.planning/) are also cleaned -- no historical exception." This plan renames LOAD_DEMO_DATA to LOAD_SEED_DATA, deletes .env.demo and setup_demo_az.sh, and cleans ALL references including historical planning files.

Output: A repo where `grep -rn 'DEMO_'` returns zero results (excluding .git/).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-final-audit/22-CONTEXT.md
@.planning/phases/22-final-audit/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename LOAD_DEMO_DATA to LOAD_SEED_DATA in all config and scripts, delete .env.demo and setup_demo_az.sh</name>
  <files>
    docker-compose.yml
    docker-compose.prod.yml
    deploy/entrypoint.sh
    bin/check-prod-readiness.sh
    render.yaml
    render-production.yaml
    .env.example
    .env.production
    .env.demo
    database/setup_demo_az.sh
  </files>
  <read_first>
    docker-compose.yml
    docker-compose.prod.yml
    deploy/entrypoint.sh
    bin/check-prod-readiness.sh
    render.yaml
    render-production.yaml
    .env.example
    .env.production
    .env.demo
    database/setup_demo_az.sh
  </read_first>
  <action>
**Delete these files entirely:**
- `.env.demo` -- `.env.example` already exists as the reference config
- `database/setup_demo_az.sh` -- demo seed script, no longer needed

**Rename LOAD_DEMO_DATA to LOAD_SEED_DATA in these files (exact replacements):**

1. **docker-compose.yml** line 45-46:
   - Comment: `# --- Demo data ---` becomes `# --- Seed data ---`
   - Value: `LOAD_DEMO_DATA: "${LOAD_DEMO_DATA:-1}"` becomes `LOAD_SEED_DATA: "${LOAD_SEED_DATA:-1}"`

2. **docker-compose.prod.yml** line 28:
   - `LOAD_DEMO_DATA: "0"` becomes `LOAD_SEED_DATA: "0"`

3. **deploy/entrypoint.sh**:
   - Line 28: `if [ "$LOAD_DEMO_DATA" = "1" ]; then` becomes `if [ "$LOAD_SEED_DATA" = "1" ]; then`
   - Line 29: `echo "[FATAL] LOAD_DEMO_DATA=1 interdit en production."` becomes `echo "[FATAL] LOAD_SEED_DATA=1 interdit en production."`
   - Line 93: `if [ "${LOAD_DEMO_DATA:-1}" = "1" ]; then` becomes `if [ "${LOAD_SEED_DATA:-1}" = "1" ]; then`
   - Also update the echo message at line 94 area: `"Chargement des donnees de demo..."` becomes `"Chargement des donnees de seed..."`

4. **bin/check-prod-readiness.sh**:
   - Line 94: `if [ "$LOAD_DEMO_DATA" = "0" ] || [ -z "$LOAD_DEMO_DATA" ]; then` becomes `if [ "$LOAD_SEED_DATA" = "0" ] || [ -z "$LOAD_SEED_DATA" ]; then`
   - Line 95: `ok "LOAD_DEMO_DATA=0 (pas de données de test)"` becomes `ok "LOAD_SEED_DATA=0 (pas de donnees de test)"`
   - Line 97: `fail "LOAD_DEMO_DATA=$LOAD_DEMO_DATA (doit être 0 en prod !)"` becomes `fail "LOAD_SEED_DATA=$LOAD_SEED_DATA (doit etre 0 en prod !)"`

5. **render.yaml** line 45-46:
   - `- key: LOAD_DEMO_DATA` becomes `- key: LOAD_SEED_DATA`
   - Value stays `"1"`

6. **render-production.yaml** lines 61-63:
   - Comment: `# --- Pas de données de démo ---` becomes `# --- Pas de donnees de seed ---`
   - `- key: LOAD_DEMO_DATA` becomes `- key: LOAD_SEED_DATA`
   - Value stays `"0"`

7. **.env.example** lines 59-64:
   - Comment: `# Données de démo (utilisateurs test, AG fictives)` becomes `# Donnees de seed (utilisateurs test, AG fictives)`
   - Comment: `# DOIT être à 0 en production !` stays
   - `LOAD_DEMO_DATA=1` becomes `LOAD_SEED_DATA=1`
   - `# LOAD_DEMO_DATA=0` becomes `# LOAD_SEED_DATA=0`

8. **.env.production** line 29:
   - `LOAD_DEMO_DATA=0` becomes `LOAD_SEED_DATA=0`

Also rename the render.yaml header comment from "DEMO" to "DEV" where appropriate:
- render.yaml line 2: `AG-VOTE — Render Blueprint DÉMO (one-click deploy)` stays as-is since it's about the demo blueprint purpose, but the env var name changes
  </action>
  <verify>
    <automated>grep -rn 'LOAD_DEMO_DATA' docker-compose.yml docker-compose.prod.yml deploy/entrypoint.sh bin/check-prod-readiness.sh render.yaml render-production.yaml .env.example .env.production 2>/dev/null | wc -l | grep -q '^0$' && echo "PASS: zero LOAD_DEMO_DATA in config" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'LOAD_DEMO_DATA' docker-compose.yml docker-compose.prod.yml deploy/entrypoint.sh bin/check-prod-readiness.sh render.yaml render-production.yaml .env.example .env.production` returns zero lines
    - `grep -c 'LOAD_SEED_DATA' docker-compose.yml` returns at least 1
    - `grep -c 'LOAD_SEED_DATA' deploy/entrypoint.sh` returns at least 2
    - `grep -c 'LOAD_SEED_DATA' .env.example` returns at least 1
    - `test ! -f .env.demo && echo PASS` prints PASS
    - `test ! -f database/setup_demo_az.sh && echo PASS` prints PASS
  </acceptance_criteria>
  <done>All config/script files use LOAD_SEED_DATA, .env.demo and setup_demo_az.sh deleted</done>
</task>

<task type="auto">
  <name>Task 2: Clean DEMO_ references from documentation and ALL planning files (no historical exception)</name>
  <files>
    docs/DEPLOIEMENT_DOCKER.md
    docs/DEPLOIEMENT_RENDER.md
    docs/DOCKER_INSTALL.md
    docs/GUIDE_TEST_LOCAL.md
    .planning/codebase/INTEGRATIONS.md
    .planning/codebase/STACK.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/phases/ (all files with DEMO_ references)
    .planning/milestones/ (all files with DEMO_ references)
    .planning/research/ (all files with DEMO_ references)
  </files>
  <read_first>
    docs/DEPLOIEMENT_DOCKER.md
    docs/DEPLOIEMENT_RENDER.md
    docs/DOCKER_INSTALL.md
    docs/GUIDE_TEST_LOCAL.md
    .planning/codebase/INTEGRATIONS.md
    .planning/codebase/STACK.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
**Documentation files -- rename every occurrence of `LOAD_DEMO_DATA` to `LOAD_SEED_DATA`:**

1. **docs/DEPLOIEMENT_DOCKER.md** (9 occurrences at lines 117, 143, 176, 216, 276, 292, 329, 672):
   - Every `LOAD_DEMO_DATA` becomes `LOAD_SEED_DATA`
   - Update surrounding French text where it says "demo" in context of this env var:
     - Line 216: `Charger les donnees de demo` becomes `Charger les donnees de seed/test`
     - Line 276: `Donnees de demo` stays (describes the check category)
     - Line 292: `seeds de demo si LOAD_DEMO_DATA=1` becomes `seeds si LOAD_SEED_DATA=1`
     - Line 329: `Si LOAD_DEMO_DATA=1` becomes `Si LOAD_SEED_DATA=1`
     - Line 672: `LOAD_DEMO_DATA=1 interdit` becomes `LOAD_SEED_DATA=1 interdit`

2. **docs/DEPLOIEMENT_RENDER.md** (4 occurrences at lines 86, 88, 177, 221):
   - Every `LOAD_DEMO_DATA` becomes `LOAD_SEED_DATA`

3. **docs/DOCKER_INSTALL.md** (3 occurrences at lines 182, 221, 232):
   - Every `LOAD_DEMO_DATA` becomes `LOAD_SEED_DATA`
   - Line 232: `données de démo : LOAD_DEMO_DATA=0` becomes `donnees de seed : LOAD_SEED_DATA=0`

4. **docs/GUIDE_TEST_LOCAL.md** line 79:
   - `LOAD_DEMO_DATA=1` becomes `LOAD_SEED_DATA=1`

**Active planning reference files -- rename LOAD_DEMO_DATA to LOAD_SEED_DATA:**

5. **.planning/codebase/INTEGRATIONS.md** line 139:
   - `LOAD_DEMO_DATA=0` becomes `LOAD_SEED_DATA=0`

6. **.planning/codebase/STACK.md** line 97:
   - `LOAD_DEMO_DATA=0` becomes `LOAD_SEED_DATA=0`

7. **.planning/REQUIREMENTS.md** line 53:
   - Change `DEMO_` to reflect the updated state: mark CLN-01 as complete `[x]`

**Historical planning files -- clean ALL DEMO_ references (per user decision: "no historical exception"):**

8. Run a comprehensive find-and-replace across ALL `.planning/` subdirectories. For each file containing `DEMO_` references:

   a. First, discover all files: `grep -rln 'DEMO_' .planning/ | grep -v '22-CONTEXT.md' | grep -v '22-RESEARCH.md' | grep -v '22-01-PLAN.md' | grep -v '22-02-PLAN.md'`

   b. For each file found, apply context-appropriate replacements:
      - `LOAD_DEMO_DATA` becomes `LOAD_SEED_DATA` everywhere
      - `DEMO_SESSION` becomes `DEMO_SESSION` (keep as-is -- these are historical references to a JS constant name that was removed; renaming would make the historical record inaccurate about what the constant was called. The goal is to eliminate live DEMO_ constants, not rewrite history about what things were named.)
      - Actually, per "no historical exception" and "clean everything": replace ALL `DEMO_` patterns:
        - `DEMO_EVENTS` -- this was a removed JS constant. In historical text describing its removal, change to reference "the removed demo events constant" or simply leave the description but ensure no bare `DEMO_EVENTS` variable reference remains. Use `sed -i 's/DEMO_EVENTS/SEED_EVENTS/g'` as the simplest approach.
        - `DEMO_SESSION` -- same pattern: `sed -i 's/DEMO_SESSION/SEED_SESSION/g'`
        - `DEMO_FILES` -- same: `sed -i 's/DEMO_FILES/SEED_FILES/g'`
        - `MEETING_ID_DEMO_AZ` -- `sed -i 's/MEETING_ID_DEMO_AZ/MEETING_ID_SEED_AZ/g'`
        - `LOAD_DEMO_DATA` -- `sed -i 's/LOAD_DEMO_DATA/LOAD_SEED_DATA/g'`

   c. The affected historical files include (but discover via grep, do not assume this list is exhaustive):
      - `.planning/phases/17-demo-data-removal/17-RESEARCH.md`
      - `.planning/phases/17-demo-data-removal/17-CONTEXT.md`
      - `.planning/milestones/v2.0-phases/11-post-session-records/11-03-SUMMARY.md`
      - `.planning/milestones/v2.0-phases/11-post-session-records/11-03-PLAN.md`
      - `.planning/milestones/v2.0-phases/11-post-session-records/11-VERIFICATION.md`
      - `.planning/milestones/v2.0-phases/14-wizard-hub-dashboard-api/14-VERIFICATION.md`
      - `.planning/milestones/v2.0-phases/08-session-wizard-hub/08-03-SUMMARY.md`
      - `.planning/milestones/v2.0-phases/08-session-wizard-hub/08-VERIFICATION.md`
      - `.planning/research/STACK.md`
      - `.planning/research/PITFALLS.md`
      - `.planning/research/SUMMARY.md`
      - `.planning/research/FEATURES.md`
      - `.planning/research/ARCHITECTURE.md`

   d. After bulk replacement, verify: `grep -rn 'DEMO_' .planning/ | grep -v '.git/' | grep -v '22-CONTEXT.md' | grep -v '22-RESEARCH.md' | grep -v '22-01-PLAN.md' | grep -v '22-02-PLAN.md'` should return zero lines.

After all edits, run a final comprehensive grep to find any remaining DEMO_ references across the entire repo (excluding only .git/ and the phase 22 plan/context/research files themselves which naturally reference the term being cleaned).
  </action>
  <verify>
    <automated>count=$(grep -rn 'DEMO_' .planning/ docs/ 2>/dev/null | grep -v '.git/' | grep -v '22-CONTEXT.md' | grep -v '22-RESEARCH.md' | grep -v '22-01-PLAN.md' | grep -v '22-02-PLAN.md' | wc -l); test "$count" -eq 0 && echo "PASS: zero DEMO_ in docs and planning" || echo "FAIL: $count remaining"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'LOAD_DEMO_DATA' docs/` returns zero lines
    - `grep -c 'LOAD_SEED_DATA' docs/DEPLOIEMENT_DOCKER.md` returns at least 5
    - `grep -c 'LOAD_SEED_DATA' docs/DEPLOIEMENT_RENDER.md` returns at least 2
    - `grep -c 'LOAD_SEED_DATA' docs/DOCKER_INSTALL.md` returns at least 2
    - `grep -c 'LOAD_SEED_DATA' .planning/codebase/INTEGRATIONS.md` returns 1
    - `grep -rn 'DEMO_' . | grep -v '.git/' | grep -v 'node_modules/' | grep -v '22-CONTEXT.md' | grep -v '22-RESEARCH.md' | grep -v '22-01-PLAN.md' | grep -v '22-02-PLAN.md' | wc -l` returns 0 (final comprehensive check -- no exclusions for .planning/phases/, .planning/milestones/, or .planning/research/)
  </acceptance_criteria>
  <done>All documentation and ALL planning files (including historical) use LOAD_SEED_DATA/SEED_SESSION/SEED_EVENTS/SEED_FILES; zero DEMO_ references remain anywhere in the repo</done>
</task>

</tasks>

<verification>
Run a comprehensive repo-wide grep excluding only .git/ and the phase 22 plan/context/research files:
```bash
grep -rn 'DEMO_' . | grep -v '.git/' | grep -v 'node_modules/' | grep -v '22-CONTEXT.md' | grep -v '22-RESEARCH.md' | grep -v '22-01-PLAN.md' | grep -v '22-02-PLAN.md'
```
Expected: zero results. No historical exception -- ALL planning files are cleaned.

Verify deleted files:
```bash
test ! -f .env.demo && test ! -f database/setup_demo_az.sh && echo "PASS: files deleted"
```
</verification>

<success_criteria>
- Zero DEMO_ references in source code, config files, deploy scripts, documentation, AND all planning files (including historical)
- .env.demo and database/setup_demo_az.sh no longer exist
- All infrastructure configs use LOAD_SEED_DATA consistently
- docker-compose up still works (entrypoint.sh reads correct env var name)
</success_criteria>

<output>
After completion, create `.planning/phases/22-final-audit/22-01-SUMMARY.md`
</output>
