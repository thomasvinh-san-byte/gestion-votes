---
phase: 22-final-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/hub.htmx.html
  - public/vote.htmx.html
  - public/report.htmx.html
  - public/assets/js/pages/postsession.js
  - public/assets/js/pages/public.js
  - public/assets/js/pages/pv-print.js
  - public/assets/js/pages/report.js
autonomous: true
requirements: [CLN-02]

must_haves:
  truths:
    - "Every page that makes an API call displays a loading indicator while the request is in flight"
    - "Every page that makes an API call displays a meaningful error message when the request fails"
    - "Every page that makes an API call shows an appropriate empty state when the response contains no data"
  artifacts:
    - path: "public/hub.htmx.html"
      provides: "Hub page with KPI skeleton loading"
      contains: "skeleton"
    - path: "public/vote.htmx.html"
      provides: "Vote page with initial loading skeleton or spinner"
      contains: "ag-spinner"
    - path: "public/report.htmx.html"
      provides: "Report page with iframe loading indicator"
      contains: "ag-spinner"
    - path: "public/assets/js/pages/postsession.js"
      provides: "Post-session with no silent catches on data-loading calls"
    - path: "public/assets/js/pages/public.js"
      provides: "Projection page with reconnection indicator"
  key_links:
    - from: "public/hub.htmx.html"
      to: "public/assets/js/pages/hub.js"
      via: "skeleton replaced by JS on data load"
      pattern: "aria-busy"
    - from: "public/vote.htmx.html"
      to: "public/assets/js/pages/vote.js"
      via: "spinner replaced by JS on data load"
      pattern: "ag-spinner"
    - from: "public/report.htmx.html"
      to: "public/assets/js/pages/report.js"
      via: "spinner hidden when iframe loads"
      pattern: "ag-spinner"
---

<objective>
Audit every API call site across all page JS files for loading, error, and empty states. Document the complete coverage matrix. Fix remaining gaps to satisfy CLN-02.

Purpose: CLN-02 requires every API call site to have loading, error, and empty states. Research (22-RESEARCH.md) identified most pages as complete and 6-8 pages with potential gaps. Since research, several gaps have been fixed by prior phases. This plan performs a fresh audit against actual current code, documents the matrix, and fixes any genuine remaining gaps.

Output: Comprehensive audit checklist in SUMMARY.md + any code fixes needed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-final-audit/22-CONTEXT.md
@.planning/phases/22-final-audit/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit every page JS file for loading/error/empty coverage and document findings</name>
  <files>
    (no files modified -- audit only, output goes to SUMMARY.md)
  </files>
  <read_first>
    public/assets/js/pages/hub.js
    public/assets/js/pages/postsession.js
    public/assets/js/pages/public.js
    public/assets/js/pages/vote.js
    public/assets/js/pages/report.js
    public/assets/js/pages/pv-print.js
    public/assets/js/pages/wizard.js
    public/assets/js/pages/landing.js
    public/assets/js/pages/dashboard.js
    public/assets/js/pages/meetings.js
    public/assets/js/pages/operator-tabs.js
    public/assets/js/pages/members.js
    public/assets/js/pages/users.js
    public/assets/js/pages/admin.js
    public/assets/js/pages/analytics-dashboard.js
    public/assets/js/pages/archives.js
    public/assets/js/pages/audit.js
    public/assets/js/pages/trust.js
    public/assets/js/pages/validate.js
    public/assets/js/pages/settings.js
    public/assets/js/pages/email-templates-editor.js
    public/assets/js/pages/docs-viewer.js
    public/hub.htmx.html
    public/postsession.htmx.html
    public/vote.htmx.html
    public/public.htmx.html
    public/report.htmx.html
  </read_first>
  <action>
For every page JS file in `public/assets/js/pages/`, examine each `fetch()`, `Utils.apiGet()`, `Utils.apiPost()`, `window.api()` call site and check three things:
1. **Loading indicator**: Is there a skeleton in the HTML template, a spinner, `Shared.btnLoading()`, or `aria-busy` attribute set before the call?
2. **Error state**: Is there a `.catch()` or `catch` block that shows a user-visible error (toast, banner, inline message)?
3. **Empty state**: When the API returns valid but empty data (zero items), is there an explicit empty state shown?

**Categorize each page as:**
- COMPLETE: All three states covered for all API calls
- MINOR GAP: One state partially missing but not user-impacting (e.g., form-based page with button loading only)
- GAP: Missing a clearly needed state (e.g., data-loading page with no loading indicator)
- N/A: Page makes no data-loading API calls (e.g., help-faq.js)

**Exceptions per CONTEXT.md decisions (do NOT flag as gaps):**
- Browser API catches (localStorage, sessionStorage, focus, fullscreen) -- robustness catches, not API failures
- Background calls (SSE heartbeat, device_heartbeat, SW cache) -- silent retry is correct UX
- Form-based pages (wizard, settings) that don't load data on init -- button loading on submit is sufficient
- Auth-check pages (landing.js whoami) -- showing login form on failure is intentional

Build a complete checklist table in this format for the SUMMARY:
```
| Page | JS File | Loading | Error | Empty | Status | Notes |
```

Identify any remaining genuine gaps that need code fixes.
  </action>
  <verify>
    <automated>echo "Audit task produces documentation in SUMMARY, not code changes. Verify by reading SUMMARY after plan completion."</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY.md contains a table with ALL page JS files (30+ files) listed
    - Each row has Loading/Error/Empty columns with specific evidence (function names, line refs)
    - Each row has a Status column (COMPLETE/MINOR GAP/GAP/N/A)
    - Gap count is explicitly stated
  </acceptance_criteria>
  <done>Complete coverage matrix documented for all page JS files with gap identification</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining loading/error/empty gaps identified in audit</name>
  <files>
    public/hub.htmx.html
    public/vote.htmx.html
    public/report.htmx.html
    public/assets/js/pages/postsession.js
    public/assets/js/pages/public.js
    public/assets/js/pages/report.js
    public/assets/js/pages/pv-print.js
  </files>
  <read_first>
    public/hub.htmx.html
    public/assets/js/pages/hub.js
    public/assets/js/pages/postsession.js
    public/postsession.htmx.html
    public/assets/js/pages/public.js
    public/public.htmx.html
    public/assets/js/pages/pv-print.js
    public/vote.htmx.html
    public/assets/js/pages/vote.js
    public/report.htmx.html
    public/assets/js/pages/report.js
  </read_first>
  <action>
Based on the audit from Task 1, fix all identified GAP-status pages. The research identified these potential remaining gaps (verify each is still a gap before fixing):

**1. hub.htmx.html -- Add KPI skeleton loading (if gap confirmed)**

The hub checklist area (line 132-135) already has skeleton loading. The KPI grid area (`hub-details-body` div, line 147+) starts `hidden` and is revealed by a toggle. If the KPI grid area is visible by default on page load (check JS initialization), add skeleton placeholders inside the KPI cards. The current KPI cards show "-" as default which may be sufficient as a loading indicator.

If fix needed: Inside each `.hub-kpi-card` element, add `aria-busy="true"` to the parent `hub-details-body` div, and ensure the dash values ("-") are replaced by actual data when loaded (already done by hub.js `renderKpis()`).

**2. postsession.js -- Verify no remaining silent catches on data-loading calls**

Research originally flagged line 139 (`/* silent */`) and line 353 (`/* silent */`). Check current code:
- If line 139 catch block now has `setNotif('error', ...)` -- no fix needed
- If line 353 `.catch()` now has `setNotif('error', ...)` -- no fix needed
- If any data-loading catch still silences errors, add `setNotif('error', 'Impossible de charger...')` pattern

**3. public.js -- Add reconnection indicator for persistent poll failures**

The page polls for meeting state. When polls fail repeatedly (3+ consecutive failures), show a subtle connection-lost banner. The heartbeat already tracks `_heartbeatFails` (line 462-474). Apply same pattern to the main refresh poll:

Add a `_refreshFails` counter. In the `catch` block of the refresh function (around line 446):
- Increment `_refreshFails`
- If `_refreshFails >= 3`: show a reconnection banner element (create a `<div id="connectionLost" class="projection-connection-lost">` with text "Connexion interrompue, reconnexion en cours..." and a subtle yellow/orange style)
- On successful refresh: reset `_refreshFails` to 0 and hide the banner

Also add the banner element to `public.htmx.html` (hidden by default):
```html
<div id="connectionLost" class="projection-connection-lost" hidden>
  Connexion interrompue — reconnexion en cours&hellip;
</div>
```

**4. vote.htmx.html -- Add loading skeleton/spinner for initial page render (Priority 2)**

Research confirmed: "NO skeleton or spinner in `vote.htmx.html`. The page relies on JS to render everything." The vote page fetches meeting/resolution data on load and renders the voting interface.

Add a loading spinner inside the main vote content area in `vote.htmx.html`:
```html
<div id="voteContent">
  <div class="text-center p-6">
    <ag-spinner size="lg" label="Chargement..."></ag-spinner>
  </div>
</div>
```

The `vote.js` init code should replace this spinner content when data loads. Verify that the JS already targets an element like `#voteContent` or similar and replaces its innerHTML -- if so, the spinner will be naturally replaced. If not, add a line in the JS init to hide/remove the spinner once data is rendered.

**5. report.htmx.html -- Add iframe loading indicator (Priority 3)**

Research confirmed: "NO skeleton or spinner in `report.htmx.html`. The iframe (`pvFrame`) loads content but has no loading indicator while it loads."

Add a loading spinner overlaying the iframe area in `report.htmx.html`:
```html
<div id="pvFrameWrapper" style="position: relative;">
  <div id="pvFrameLoading" class="text-center p-6">
    <ag-spinner size="lg" label="Chargement du PV..."></ag-spinner>
  </div>
  <!-- existing iframe element stays here -->
</div>
```

In `report.js`, add an `onload` handler for the iframe that hides the spinner:
```javascript
var pvFrame = document.getElementById('pvFrame');
var pvFrameLoading = document.getElementById('pvFrameLoading');
if (pvFrame && pvFrameLoading) {
  pvFrame.addEventListener('load', function() {
    pvFrameLoading.hidden = true;
  });
}
```

Also show the spinner again whenever a new report is loaded (before setting iframe src).

**6. pv-print.js -- Minor: no changes needed if error banner already exists**

Research said the error banner exists but could be more prominent. Check if current implementation at line 62-68 (red-bordered banner at top of body) is sufficient. If it shows "Attention : donnees manquantes (...)" with red styling, this is adequate -- no change needed.

**Pattern references (use these exact patterns for any fixes):**
- Loading skeleton: `<div class="skeleton skeleton-text skeleton-w-80"></div>` (from dashboard.htmx.html)
- Error banner: `setNotif('error', 'message')` or `Shared.showToast('message', 'error')` (from hub.js)
- Empty state: `Shared.emptyState({ icon: 'icon-name', title: 'Title', description: 'Desc' })` (from meetings.js)
- Retry pattern: `tryLoad(attempt)` with `setTimeout(function(){ tryLoad(attempt + 1); }, 2000)` (from audit.js)
  </action>
  <verify>
    <automated>node -c public/assets/js/pages/postsession.js && node -c public/assets/js/pages/public.js && node -c public/assets/js/pages/report.js 2>/dev/null; echo "JS syntax check done" && grep -c 'ag-spinner\|skeleton\|spinner' public/vote.htmx.html | grep -q '[1-9]' && echo "PASS: vote.htmx.html has loading indicator" || echo "FAIL: vote.htmx.html missing loading indicator" && grep -c 'ag-spinner\|skeleton\|spinner\|Loading' public/report.htmx.html | grep -q '[1-9]' && echo "PASS: report.htmx.html has loading indicator" || echo "FAIL: report.htmx.html missing loading indicator"</automated>
  </verify>
  <acceptance_criteria>
    - `node -c public/assets/js/pages/postsession.js` exits 0
    - `node -c public/assets/js/pages/public.js` exits 0
    - `grep -c '\/\* silent \*\/' public/assets/js/pages/postsession.js` returns 0 (no silent catches on data-loading calls)
    - If public.js was modified: `grep -c 'connectionLost\|_refreshFails' public/assets/js/pages/public.js` returns at least 1
    - If hub.htmx.html was modified: `grep -c 'aria-busy\|skeleton' public/hub.htmx.html` returns at least 3
    - `grep -c 'ag-spinner\|skeleton\|spinner' public/vote.htmx.html` returns at least 1 (vote page has loading indicator)
    - `grep -c 'ag-spinner\|skeleton\|spinner\|Loading' public/report.htmx.html` returns at least 1 (report page has iframe loading indicator)
    - `node -c public/assets/js/pages/report.js` exits 0 (if modified)
    - `node -c public/assets/js/pages/pv-print.js` exits 0 (if modified)
  </acceptance_criteria>
  <done>All identified GAP-status pages fixed with unified loading/error/empty patterns including vote.htmx.html and report.htmx.html; comprehensive audit matrix in SUMMARY confirms complete CLN-02 coverage</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Run syntax validation on all modified JS files:
```bash
for f in public/assets/js/pages/postsession.js public/assets/js/pages/public.js public/assets/js/pages/pv-print.js public/assets/js/pages/report.js; do node -c "$f" && echo "OK: $f" || echo "FAIL: $f"; done
```

2. Verify no silent catches remain on data-loading calls:
```bash
grep -n '\/\* silent \*\/' public/assets/js/pages/postsession.js
```
Expected: zero matches

3. Verify vote and report pages have loading indicators:
```bash
grep -c 'ag-spinner\|skeleton\|spinner' public/vote.htmx.html public/report.htmx.html
```
Expected: at least 1 match per file

4. SUMMARY.md contains complete audit matrix covering all 30+ page JS files
</verification>

<success_criteria>
- Complete audit matrix in SUMMARY.md covering every page JS file
- All GAP-status pages fixed with established patterns (ag-spinner, Shared.emptyState, tryLoad/setNotif error)
- vote.htmx.html has a loading skeleton or spinner for initial page render
- report.htmx.html has a loading indicator for iframe content
- Zero genuine loading/error/empty gaps remain on data-loading pages
- All modified JS files pass `node -c` syntax check
- CLN-02 requirement satisfied: every API call site has loading, error, and empty states
</success_criteria>

<output>
After completion, create `.planning/phases/22-final-audit/22-02-SUMMARY.md`
</output>
