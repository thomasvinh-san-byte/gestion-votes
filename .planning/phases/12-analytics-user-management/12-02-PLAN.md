---
phase: 12-analytics-user-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/users.htmx.html
  - public/assets/css/users.css
  - public/assets/js/pages/users.js
  - public/partials/sidebar.html
  - public/admin.htmx.html
  - public/assets/js/pages/admin.js
  - public/assets/js/core/shell.js
autonomous: true
requirements: [USR-01, USR-02, USR-03]

must_haves:
  truths:
    - "Users page exists at /users.htmx.html as a standalone page with its own CSS and JS"
    - "Role info panel describes 4 roles (Admin, Operateur, Auditeur, Observateur) with color-coded tags"
    - "Users table shows avatar (initials), name, email, role tag, status, last login, and edit button per row"
    - "Add user button opens a modal form for creating new users"
    - "Pagination via ag-pagination component controls users list"
    - "Sidebar Systeme section links to /users.htmx.html as Utilisateurs"
    - "Admin page no longer has users tab; shows compact user count summary with link to users page"
  artifacts:
    - path: "public/users.htmx.html"
      provides: "Standalone users management page"
    - path: "public/assets/css/users.css"
      provides: "Users page styles (role panel, table, avatars, pagination)"
    - path: "public/assets/js/pages/users.js"
      provides: "Users page JS (load, render, CRUD, search, filter, pagination)"
    - path: "public/partials/sidebar.html"
      provides: "Updated sidebar with Utilisateurs link under Systeme"
    - path: "public/admin.htmx.html"
      provides: "Admin page without users tab, with compact summary"
    - path: "public/assets/js/pages/admin.js"
      provides: "Admin JS with loadUsers stubbed out"
    - path: "public/assets/js/core/shell.js"
      provides: "Search index updated with Utilisateurs entry"
  key_links:
    - from: "public/users.htmx.html"
      to: "/api/v1/admin_users.php"
      via: "fetch in users.js loadUsers()"
      pattern: "api.*admin_users"
    - from: "public/partials/sidebar.html"
      to: "public/users.htmx.html"
      via: "nav-item href"
      pattern: "users\\.htmx\\.html"
    - from: "public/admin.htmx.html"
      to: "public/users.htmx.html"
      via: "compact summary link"
      pattern: "users\\.htmx\\.html"
---

<objective>
Extract user management from admin.htmx.html into a dedicated users.htmx.html page following the Phase 11 audit extraction pattern. Create users.css + users.js, update sidebar navigation, and clean up admin page.

Purpose: Complete USR-01 (role info panel), USR-02 (users table), USR-03 (add user + pagination) requirements.
Output: Standalone users page, updated sidebar, cleaned admin page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-analytics-user-management/12-CONTEXT.md

@public/admin.htmx.html
@public/assets/js/pages/admin.js
@public/assets/css/admin.css
@public/audit.htmx.html
@public/partials/sidebar.html
@public/assets/js/core/shell.js

<interfaces>
<!-- Phase 11 audit extraction pattern (reference for this extraction) -->

From public/audit.htmx.html (page structure pattern):
- Standalone page with own CSS (audit.css) + JS (audit.js)
- data-include-sidebar with data-page="audit"
- app-shell > app-sidebar + app-header + app-main + app-footer
- Skip links, ARIA landmarks, component imports

From public/assets/js/core/shell.js (sidebar + search):
- Sidebar loaded from /partials/sidebar.html
- SEARCH_IDX array at line ~681 for global search entries
- Sidebar nav groups: Preparation, Seance en direct, Apres la seance, Controle, Systeme

From public/partials/sidebar.html (Systeme section, lines 96-112):
- Group "Systeme" with data-group="admin"
- Currently: admin.htmx.html (Utilisateurs, icon-key) + admin.htmx.html?tab=settings (Parametres, icon-settings) + help.htmx.html

From admin.js (user functions to extract):
- loadUsers() — fetches /api/v1/admin_users.php
- filterAndRenderUsers() — search + fuzzy filter
- renderUsersTable() — initials avatar, role badges, status, actions
- Password strength indicator logic
- Event handlers: searchUser input, filterRole change, createUser btn, edit/toggle/delete/password delegated clicks

API endpoints:
- GET /api/v1/admin_users.php — list users
- POST /api/v1/admin_users.php — create user
- PUT /api/v1/admin_user_update.php — update user
- DELETE /api/v1/admin_user_delete.php — delete user
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create users.htmx.html, users.css, and users.js</name>
  <files>public/users.htmx.html, public/assets/css/users.css, public/assets/js/pages/users.js</files>
  <action>
**1. users.htmx.html** — Create following the audit.htmx.html pattern:
- Standard HTML boilerplate with `lang="fr"`, `data-page-role="admin"`
- Head: theme-init.js, fonts, app.css, users.css (no Chart.js needed)
- Skip links: `#main-content` and `#main-nav`
- App shell: sidebar (`data-include-sidebar data-page="users"`), header, main, footer
- Header: h1 "Utilisateurs" with icon-users SVG icon, subtitle "Gestion des comptes et des roles", refresh button, add user button (`id="btnAddUser"`, class `btn btn-primary btn-sm`)
- Main content sections (in order):

  a. **Role info panel (USR-01):** Card with class `roles-explainer` containing a `roles-explainer-grid` div with 4 role items. Each item has a color-coded tag + description:
     - `<span class="tag tag-accent">Admin</span>` — "Acces complet au systeme"
     - `<span class="tag tag-success">Operateur</span>` — "Gestion des seances et votes"
     - `<span class="tag tag-purple">Auditeur</span>` — "Consultation audit et controle"
     - `<span class="tag">Observateur</span>` — "Acces en lecture seule"
     (Per CONTEXT: keep current 4 roles, do NOT add Gestionnaire)

  b. **Filter/search bar:** Card with flex row: search input (`id="searchUser"`, placeholder "Nom ou email..."), role filter select (`id="filterRole"` with options: Tous/Admin/Operateur/Auditeur/Observateur), users count span (`id="usersCount"`)

  c. **Users table container:** Card with `id="usersTableBody"` and skeleton loading rows (3 skeleton rows like admin.htmx.html pattern). Set `aria-busy="true"` and `aria-label="Liste des utilisateurs"`.

  d. **Pagination:** `<ag-pagination id="usersPagination" page-size="10"></ag-pagination>` below the table card.

- Footer: same app-footer pattern as other pages
- Scripts: component index.js, utils.js, shared.js, shell.js, meeting-context.js, users.js

**2. users.css** — Create with styles extracted/adapted from admin.css:
- Copy relevant user-related CSS classes from admin.css:
  - `.roles-explainer`, `.roles-explainer-grid`, `.role-explain` (role info panel layout)
  - `.user-row`, `.user-avatar`, `.user-row-body`, `.user-row-main`, `.user-row-name`, `.user-row-email`, `.user-row-meta`, `.user-row-actions` (table rows)
  - `.user-status-badge`, `.user-pw-badge`, `.role-badge` variants (admin, operator, auditor, viewer)
  - `.is-active`, `.is-inactive` row states
  - `.inline-form`, `.form-group-search`, `.form-group-filter` (filter bar)
  - `.password-strength`, `.password-strength-bar`, `.password-strength-fill` variants
- Add new styles for wireframe alignment:
  - `.user-avatar` — circular, 36px, colored background based on role, white text, font-weight 600
  - `.user-row` — flex row with gap, align-items center, border-bottom, padding 12px 16px, hover background var(--color-surface-alt)
  - `.user-row-lastlogin` — text-sm text-muted for last login column
  - `.users-header-actions` — flex gap for header buttons
- Use design tokens throughout (no hardcoded colors). Support dark theme via existing token system.

**3. users.js** — Create as IIFE pattern:
- Extract from admin.js: loadUsers(), filterAndRenderUsers(), renderUsersTable(), password strength logic, and all user CRUD event handlers.
- Use `var` keyword throughout (codebase convention).
- Key functions:
  - `loadUsers()` — fetch GET /api/v1/admin_users.php with role filter param, store in `_allUsers`
  - `filterAndRenderUsers()` — apply search (fuzzy via Utils.fuzzyFilter if available, fallback to includes), update count, call renderUsersTable(), update pagination
  - `renderUsersTable(users)` — render user rows into #usersTableBody. Each row shows: initials avatar, name, email, role tag (color-coded: admin=tag-accent, operator=tag-success, auditor=tag-purple, viewer=default), status badge (Actif/Inactif), last login date, edit button. Use escapeHtml() for all user data.
  - `createUser()` — open ag-confirm or ag-modal dialog with form fields (name, email, password, role select). On confirm, POST to /api/v1/admin_users.php, reload list, show toast. Per CONTEXT: modal dialog for user creation (not inline form).
  - `editUser(id)` — open modal with prefilled form, PUT to /api/v1/admin_user_update.php on save
  - `deleteUser(id, name)` — ag-confirm danger dialog, DELETE to /api/v1/admin_user_delete.php
  - `toggleUser(id, isActive)` — PUT to toggle active status
  - `resetPassword(id, name)` — ag-confirm with new password input
  - Pagination: listen to ag-pagination `page-change` event, slice _users array, re-render. Page size = 10.
- Event bindings: searchUser input, filterRole change, btnAddUser click, delegated clicks on .btn-edit-user/.btn-delete-user/.btn-toggle-user/.btn-password-user in usersTableBody
- Call loadUsers() on DOMContentLoaded.
- Password strength indicator logic (copied from admin.js) for the create/edit modal password field.

Per CONTEXT decisions:
- 4 roles only (Admin, Operateur, Auditeur, Observateur) — no Gestionnaire
- Initials-based avatars (no photo upload)
- Modal dialog for user edit/create
- Color-coded role tags: Admin=accent, Operateur=success, Auditeur=purple, Observateur=default
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && test -f public/users.htmx.html && test -f public/assets/css/users.css && test -f public/assets/js/pages/users.js && grep -q "data-page=\"users\"" public/users.htmx.html && grep -q "roles-explainer" public/users.htmx.html && grep -q "ag-pagination" public/users.htmx.html && grep -q "admin_users.php" public/assets/js/pages/users.js && grep -q "loadUsers" public/assets/js/pages/users.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>users.htmx.html exists with role info panel (4 roles), users table with avatar/name/email/role/status/last-login/edit, search/filter, add user button, ag-pagination; users.css has all required styles; users.js has full CRUD + search + pagination</done>
</task>

<task type="auto">
  <name>Task 2: Update sidebar, admin page, and search index</name>
  <files>public/partials/sidebar.html, public/admin.htmx.html, public/assets/js/pages/admin.js, public/assets/js/core/shell.js</files>
  <action>
**1. sidebar.html** — Update the Systeme section (lines 96-112):
- Change the first nav-item from `href="/admin.htmx.html"` to `href="/users.htmx.html"` with `data-page="users"`.
- Keep icon as icon-key (or change to icon-users for semantic clarity — use icon-users).
- Keep label "Utilisateurs".
- Add a new nav-item AFTER Utilisateurs and BEFORE Parametres:
  ```html
  <a class="nav-item" href="/admin.htmx.html" data-page="admin" data-requires-role="admin">
    <span class="nav-item-icon" aria-hidden="true"><svg class="icon"><use href="/assets/icons.svg#icon-key"></use></svg></span>
    <span class="nav-label">Administration</span>
  </a>
  ```
- Result in Systeme section: Utilisateurs (icon-users, /users.htmx.html) → Administration (icon-key, /admin.htmx.html) → Parametres (icon-settings) → Guide & FAQ (icon-info)

**2. admin.htmx.html** — Remove users tab and add compact summary:
- Remove the "Utilisateurs" tab button from the admin-tabs div (line ~195). Keep remaining tabs: Roles de seance, Politiques, Permissions, Machine a etats, Parametres, Systeme.
- Replace the entire `panel-users` div (lines 207-307) with a compact summary card:
  ```html
  <div class="card mb-4">
    <div class="card-body">
      <div class="flex items-center justify-between">
        <div>
          <div class="font-semibold">Utilisateurs</div>
          <div class="text-sm text-muted" id="adminUsersCount">-- utilisateurs</div>
        </div>
        <a href="/users.htmx.html" class="btn btn-secondary btn-sm">Gerer les utilisateurs</a>
      </div>
    </div>
  </div>
  ```
  Place this card BEFORE the admin-tabs div (as a top-level summary, always visible).
- Update the skip link from `#panel-users` to `#main-content`.
- Make "Roles de seance" the default active tab (add `active` class to that tab button and its panel).

**3. admin.js** — Stub out user functions:
- Replace `loadUsers()` with a lightweight function that only fetches user count for the compact summary:
  ```javascript
  async function loadUsers() {
    try {
      var r = await api('/api/v1/admin_users.php');
      if (r.body && r.body.ok && r.body.data) {
        var count = (r.body.data.items || []).length;
        var el = document.getElementById('adminUsersCount');
        if (el) el.textContent = count + ' utilisateur' + (count !== 1 ? 's' : '');
      }
    } catch (e) { /* silent - summary only */ }
  }
  ```
- Remove renderUsersTable(), filterAndRenderUsers(), createUser form handler, password strength logic, search/filter user handlers, and all user CRUD event delegation that references btn-edit-user, btn-delete-user, btn-toggle-user, btn-password-user. Keep the loadUsers() call in refreshAll().
- Remove the `_users` and `_allUsers` variables.
- Remove the searchUser input listener and filterRole change listener for users.

**4. shell.js** — Update SEARCH_IDX array (~line 681):
- Add entry for Users page:
  ```javascript
  { name: 'Utilisateurs', sub: 'Gestion des comptes', href: '/users.htmx.html', icon: 'users' },
  ```
- Place it before the Configuration entry.
- Keep the existing Configuration entry pointing to admin.htmx.html.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -q "users.htmx.html" public/partials/sidebar.html && grep -q "Gerer les utilisateurs" public/admin.htmx.html && grep -q "adminUsersCount" public/assets/js/pages/admin.js && grep -q "Utilisateurs.*users.htmx.html" public/assets/js/core/shell.js && ! grep -q "renderUsersTable" public/assets/js/pages/admin.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Sidebar links to /users.htmx.html under Systeme; admin page shows compact user count with link (no users tab); admin.js has only lightweight user count fetch; shell.js search index includes Utilisateurs entry</done>
</task>

</tasks>

<verification>
- Navigate to /users.htmx.html: page loads with role info panel, search/filter, users table, pagination
- Sidebar shows "Utilisateurs" link under Systeme section, navigates to /users.htmx.html
- Admin page no longer has Utilisateurs tab; shows compact "X utilisateurs" summary with link
- Cmd+K search includes "Utilisateurs" entry
- Users table: initials avatar, name, email, color-coded role tag, status badge, last login, edit button per row
- Add user button opens modal dialog
- ESLint: `npx eslint public/assets/js/pages/users.js public/assets/js/pages/admin.js` passes
</verification>

<success_criteria>
- Role info panel with 4 roles and color-coded tags (USR-01)
- Users table with avatar, name, email, role, status, last login, edit (USR-02)
- Add user button + ag-pagination (USR-03)
- Admin page cleanly separated from users management
- All existing admin functionality preserved (meeting roles, policies, permissions, states, settings, system tabs)
</success_criteria>

<output>
After completion, create `.planning/phases/12-analytics-user-management/12-02-SUMMARY.md`
</output>
