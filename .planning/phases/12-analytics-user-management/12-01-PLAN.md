---
phase: 12-analytics-user-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/analytics.htmx.html
  - public/assets/css/analytics.css
  - public/assets/js/pages/analytics-dashboard.js
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03]

must_haves:
  truths:
    - "Statistics page displays exactly 4 KPI cards: Sessions, Resolutions, Taux d'adoption, Participation with trend arrows"
    - "Trend arrows compare selected year vs previous year; hidden when year filter = Toutes"
    - "Donut chart shows Pour/Contre/Abstention vote distribution with semantic colors"
    - "Line graph shows monthly participation trends (12 data points per year)"
    - "CSV export button generates downloadable CSV of session data filtered by year"
  artifacts:
    - path: "public/analytics.htmx.html"
      provides: "Wireframe-aligned statistics page with 4 KPI cards, donut, line chart, CSV button"
    - path: "public/assets/css/analytics.css"
      provides: "CSS for KPI trend arrows, chart layout, CSV export button"
    - path: "public/assets/js/pages/analytics-dashboard.js"
      provides: "JS: year-aware trend arrows, donut/line chart rendering, CSV export"
  key_links:
    - from: "public/assets/js/pages/analytics-dashboard.js"
      to: "/api/v1/analytics.php"
      via: "fetch in loadOverview/loadParticipation/loadMotions"
      pattern: "api.*analytics\\.php"
    - from: "public/analytics.htmx.html"
      to: "public/assets/js/pages/analytics-dashboard.js"
      via: "script tag"
      pattern: "analytics-dashboard\\.js"
---

<objective>
Align the statistics page (analytics.htmx.html) with wireframe v3.19.2: restructure KPI cards to show exactly 4 metrics with year-over-year trend arrows, ensure donut chart and line graph match wireframe specs, and add CSV export.

Purpose: Complete STAT-01 (KPI cards with trends), STAT-02 (donut + line charts), STAT-03 (export button) requirements.
Output: Wireframe-aligned statistics page with trend arrows, charts, and CSV export.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-analytics-user-management/12-CONTEXT.md

@public/analytics.htmx.html
@public/assets/css/analytics.css
@public/assets/js/pages/analytics-dashboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure KPI cards and trend arrow logic</name>
  <files>public/analytics.htmx.html, public/assets/css/analytics.css, public/assets/js/pages/analytics-dashboard.js</files>
  <action>
1. **HTML (analytics.htmx.html):** The overview KPI section already has 4 cards with trend markup (kpiMeetings, kpiParticipation, kpiResolutions, kpiAdoptionRate). Verify they match wireframe labels exactly:
   - Card 1: "Seances" (id=kpiMeetings) with trend
   - Card 2: "Resolutions votees" (id=kpiResolutions) with trend
   - Card 3: "Taux d'adoption" (id=kpiAdoptionRate) with trend
   - Card 4: "Participation" (id=kpiParticipation) with trend
   Keep existing overview-card-trend divs with trend-arrow + trend-text spans.

2. **Add CSV export button** next to existing PDF button in the header actions div:
   ```html
   <button class="btn btn-secondary btn-sm" id="btnExportCsv" title="Exporter en CSV" aria-label="Exporter les donnees en CSV">
     <svg class="icon icon-text" aria-hidden="true"><use href="/assets/icons.svg#icon-download"></use></svg>
     CSV
   </button>
   ```

3. **JS (analytics-dashboard.js) — loadOverview() refactor:** Currently renders 6 cards via innerHTML. Change to render exactly 4 cards matching the static HTML structure. Instead of replacing innerHTML of overviewCards, update individual card values by ID:
   - `kpiMeetings` = totals.meetings
   - `kpiResolutions` = totals.motions
   - `kpiAdoptionRate` = adoptionRate + "%"
   - `kpiParticipation` = avgRate + "%"
   - For each trend element (kpiMeetingsTrend, kpiResolutionsTrend, kpiAdoptionTrend, kpiParticipationTrend): update the trend-arrow span with up/down arrow + percentage, add .up or .down class. When yearFilter value is "all", hide all trend divs (set hidden attribute); otherwise show them.

4. **JS — trendHtml() update:** The existing trendHtml() function returns HTML strings. Refactor to a function `updateTrend(elementId, trendValue)` that directly updates the DOM element by ID: sets the trend-arrow text (triangle + abs value + %), adds/removes .up/.down class on the parent .overview-card-trend, and toggles hidden based on whether yearFilter is "all".

5. **CSS (analytics.css):** The trend arrow styles already exist (.overview-card-trend.up, .overview-card-trend.down). Verify they use semantic tokens:
   - .up color: var(--color-success)
   - .down color: var(--color-danger)
   - Neutral: var(--color-text-muted)
   If not, update them. No new CSS needed for trends.

Use `var` keyword throughout JS (not const/let) to match codebase convention. Use escapeHtml() for any user data.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -c "kpiMeetings\|kpiResolutions\|kpiAdoptionRate\|kpiParticipation" public/analytics.htmx.html | grep -q "4\|5\|6\|7\|8" && grep -q "btnExportCsv" public/analytics.htmx.html && grep -q "updateTrend\|trendUpdate" public/assets/js/pages/analytics-dashboard.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>4 KPI cards render with year-aware trend arrows, CSV button present in header, trend arrows hidden when year=all</done>
</task>

<task type="auto">
  <name>Task 2: Chart alignment and CSV export implementation</name>
  <files>public/assets/js/pages/analytics-dashboard.js, public/assets/css/analytics.css</files>
  <action>
1. **Donut chart (motions tab):** The page already has an SVG donut (#donutFor, #donutAgainst, #donutAbstain) with legend. The JS already animates stroke-dasharray. Verify colors use semantic tokens:
   - Pour = COLORS.success (green)
   - Contre = COLORS.danger (red)
   - Abstention = COLORS.muted (per CONTEXT decision, NOT COLORS.warning)
   Update the Abstention donut segment stroke from `var(--color-warning)` to `var(--color-text-muted)` in the HTML. Update the donut legend dot for Abstention similarly. In JS, ensure the donut animation code uses COLORS.muted for abstention segments.

2. **Line graph (participation tab):** The participationChart already renders as a Chart.js line chart. Ensure it shows monthly data points (12 per year). The current code maps meeting titles as labels — change to aggregate by month:
   - Group meetings by month (Jan-Dec)
   - Average the participation rate per month
   - Labels: ['Jan', 'Fev', 'Mar', 'Avr', 'Mai', 'Jun', 'Jul', 'Aou', 'Sep', 'Oct', 'Nov', 'Dec']
   - Use COLORS.primary for the line, with fill area at 20% opacity
   This makes the chart "12 data points for how are we doing this year" per CONTEXT.

3. **CSV export implementation:** Add click handler for btnExportCsv:
   - Fetch data from the same analytics API endpoint used by loadOverview/loadParticipation
   - Build CSV string with columns: Date,Type,Titre,Participants,Quorum %,Resolutions,Taux adoption,Pour,Contre,Abstention,Statut
   - Respect current yearFilter selection (pass year param to API)
   - Generate download via Blob + URL.createObjectURL + temporary anchor click
   - Filename: `ag-vote-statistiques-{year}.csv` (or `ag-vote-statistiques-toutes.csv` for "all")
   - Use BOM prefix (\uFEFF) for Excel UTF-8 compatibility
   - Client-side generation (no new server endpoint needed)

4. **CSS:** Add `.btn-export-csv` styling if needed (likely not — uses existing btn-secondary). Ensure chart-card layout accommodates both donut SVG section and Chart.js canvases cleanly. Check `.donut-legend-dot` for Abstention color uses var(--color-text-muted).

Use `var` keyword throughout. Use escapeHtml() for any data inserted into DOM.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -q "btnExportCsv.*addEventListener\|btnExportCsv.*click" public/assets/js/pages/analytics-dashboard.js && grep -q "text/csv\|Blob\|createObjectURL" public/assets/js/pages/analytics-dashboard.js && grep -q "color-text-muted" public/analytics.htmx.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Donut uses correct semantic colors (muted for Abstention), line graph aggregates monthly, CSV export generates downloadable file respecting year filter</done>
</task>

</tasks>

<verification>
- Open analytics.htmx.html in browser: 4 KPI cards visible with trend arrows
- Change year filter: trend arrows update, hidden when "Toutes" selected
- Click "Resolutions" tab: donut shows Pour(green)/Contre(red)/Abstention(muted gray)
- Participation tab: line chart shows monthly aggregation (Jan-Dec labels)
- Click CSV button: file downloads with correct columns and year-filtered data
- ESLint: `npx eslint public/assets/js/pages/analytics-dashboard.js` passes
</verification>

<success_criteria>
- 4 KPI cards with year-over-year trend arrows (STAT-01)
- Donut chart (Pour/Contre/Abstention) + monthly line graph (STAT-02)
- CSV export button functional with year filter (STAT-03)
- All existing analytics functionality preserved (tabs, period buttons, PDF export, chart PNG export)
</success_criteria>

<output>
After completion, create `.planning/phases/12-analytics-user-management/12-01-SUMMARY.md`
</output>
