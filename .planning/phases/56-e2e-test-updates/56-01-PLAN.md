---
phase: 56-e2e-test-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/package.json
  - tests/e2e/playwright.config.js
  - tests/e2e/helpers.js
  - tests/e2e/specs/auth.spec.js
  - tests/e2e/specs/audit-regression.spec.js
  - tests/e2e/specs/vote.spec.js
  - tests/e2e/specs/mobile-viewport.spec.js
  - tests/e2e/specs/accessibility.spec.js
  - tests/e2e/specs/dashboard.spec.js
  - tests/e2e/specs/workflow-meeting.spec.js
  - tests/e2e/specs/public-display.spec.js
  - tests/e2e/specs/ux-interactions.spec.js
  - tests/e2e/specs/navigation.spec.js
  - tests/e2e/specs/operator.spec.js
  - tests/e2e/specs/archives.spec.js
  - tests/e2e/specs/report.spec.js
  - tests/e2e/specs/validate.spec.js
  - tests/e2e/specs/members.spec.js
  - tests/e2e/specs/docs.spec.js
  - tests/e2e/specs/trust.spec.js
  - tests/e2e/specs/api-security.spec.js
autonomous: true
requirements:
  - E2E-01
  - E2E-02
  - E2E-03
  - E2E-04
  - E2E-06

must_haves:
  truths:
    - "Playwright is installed with Chromium browser and can launch tests"
    - "All 18 spec files use selectors matching the current v4.3/v4.4 rebuilt pages"
    - "auth.spec.js uses #email, #password, #submitBtn (not input[name=api_key])"
    - "mobile-viewport.spec.js and accessibility.spec.js use v4.3 login selectors"
    - "playwright.config.js baseURL defaults to http://localhost:8080 for Docker stack"
  artifacts:
    - path: "tests/e2e/package.json"
      provides: "Playwright dependency declaration"
      contains: "@playwright/test"
    - path: "tests/e2e/node_modules/.package-lock.json"
      provides: "Installed Playwright"
  key_links:
    - from: "tests/e2e/playwright.config.js"
      to: "http://localhost:8080"
      via: "baseURL config"
      pattern: "baseURL.*localhost:8080"
    - from: "tests/e2e/specs/auth.spec.js"
      to: "public/login.html"
      via: "selector references"
      pattern: "#email.*#password.*#submitBtn"
---

<objective>
Install Playwright, update config to target the Docker stack at localhost:8080, and audit+fix all 18 spec files to use selectors matching the v4.3/v4.4 rebuilt pages.

Purpose: The E2E specs were written before v4.3/v4.4 ground-up page rebuilds. Many use stale selectors (e.g., `input[name="api_key"]` instead of `#email`/`#password`). Without fixing selectors, the entire E2E suite fails.

Output: All 18 spec files updated with correct selectors, Playwright installed and configured.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-e2e-test-updates/56-CONTEXT.md
@tests/e2e/playwright.config.js
@tests/e2e/helpers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright and update config for Docker stack</name>
  <files>tests/e2e/package.json, tests/e2e/playwright.config.js</files>
  <action>
1. Create `tests/e2e/package.json` with `@playwright/test` as a devDependency (latest stable, ^1.50).
2. Run `cd tests/e2e && npm install` to install Playwright.
3. Run `cd tests/e2e && npx playwright install chromium` to install the Chromium browser binary.
4. Update `tests/e2e/playwright.config.js`:
   - Change `baseURL` default from `http://localhost:8000` to `http://localhost:8080` (Docker stack port).
   - Change `webServer.url` from `http://localhost:8000/login.html` to `http://localhost:8080/login.html`.
   - Change `webServer.command` to be a no-op since Docker stack is already running: set `command: 'echo "Docker stack expected at port 8080"'`.
   - Keep all 5 projects (chromium, firefox, webkit, mobile-chrome, tablet).
   - Keep `reuseExistingServer: !process.env.CI`.
5. Verify Playwright can start by running: `cd tests/e2e && npx playwright test --list`
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php/tests/e2e && npx playwright test --list 2>&1 | head -30</automated>
  </verify>
  <done>Playwright installed, `npx playwright test --list` outputs all spec files without errors, config points to localhost:8080</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix all 18 spec files for stale selectors</name>
  <files>
    tests/e2e/specs/auth.spec.js,
    tests/e2e/specs/audit-regression.spec.js,
    tests/e2e/specs/vote.spec.js,
    tests/e2e/specs/mobile-viewport.spec.js,
    tests/e2e/specs/accessibility.spec.js,
    tests/e2e/specs/dashboard.spec.js,
    tests/e2e/specs/workflow-meeting.spec.js,
    tests/e2e/specs/public-display.spec.js,
    tests/e2e/specs/ux-interactions.spec.js,
    tests/e2e/specs/navigation.spec.js,
    tests/e2e/specs/operator.spec.js,
    tests/e2e/specs/archives.spec.js,
    tests/e2e/specs/report.spec.js,
    tests/e2e/specs/validate.spec.js,
    tests/e2e/specs/members.spec.js,
    tests/e2e/specs/docs.spec.js,
    tests/e2e/specs/trust.spec.js,
    tests/e2e/specs/api-security.spec.js
  </files>
  <action>
For each of the 18 spec files, read the file, then load the actual HTML page it targets (from `public/`) to compare selectors. Fix every stale selector.

**Methodology per spec:** Read spec -> identify all CSS selectors/IDs used -> read the corresponding HTML file(s) from `public/` -> compare -> fix mismatches.

**Known stale selectors to fix (identified from code review):**

1. **auth.spec.js** (E2E-02):
   - Replace `input[type="password"], input[name="api_key"]` with `#password`
   - Replace `button[type="submit"]` with `#submitBtn`
   - Replace API key login (`page.fill('input...', 'operator-key-2026-secret')`) with email+password login using `#email` and `#password` fields per helpers.js pattern
   - Add `#email` field fill for valid login test
   - Update invalid credentials test to use email+password format

2. **mobile-viewport.spec.js** (E2E-06):
   - Replace `input[type="password"], input[name="api_key"]` with `#password` in mobile login test
   - Replace `button[type="submit"]` with `#submitBtn`
   - Update `.bottom-nav, .mobile-nav, [data-mobile-nav]` and `.hamburger, [data-toggle="nav"], .menu-toggle` selectors by checking actual mobile nav DOM in the rebuilt pages
   - If no mobile hamburger/bottom-nav exists in v4.4, update test to check what actually exists (or mark as skip with comment)

3. **accessibility.spec.js**:
   - Replace `input[type="password"], input[name="api_key"]` with `#password`
   - Replace `button[type="submit"]` with `#submitBtn`
   - Update `.faq-question, .faq-item` by checking actual help.htmx.html DOM

4. **audit-regression.spec.js** (E2E-03):
   - Check `.app-sidebar` still exists in rebuilt pages
   - Check `.toggle-visibility .eye-open`, `.toggle-visibility .eye-closed` selectors on login page — if v4.3 uses `.field-eye` instead, update
   - Check `#togglePassword` still exists
   - Check `#kpiTotal`, `#statLive`, `#pvEmptyState`, `#pvFrame`, `#btnCloseOnboarding`, `#onboardingBanner`, `#sessionTimeoutBanner` against current HTML
   - Check `.tour-launch`, `.tour-card` against help.htmx.html

5. **vote.spec.js** (E2E-04):
   - Check `#meetingSelect, select[name="meeting_id"], ag-searchable-select` against actual vote.htmx.html DOM
   - Verify API endpoint paths (`/api/v1/ballots_cast.php`) are still correct

6. **api-security.spec.js**:
   - Verify all API endpoint paths still exist. Check each `.php` endpoint path against actual `public/api/v1/` directory.
   - If any endpoints were renamed or removed in v4.3/v4.4, update paths.

7. **All other specs** (dashboard, workflow-meeting, public-display, ux-interactions, navigation, operator, archives, report, validate, members, docs, trust):
   - Read each spec, identify selectors, cross-check against current HTML.
   - Most of these use simple patterns (overflow checks, API calls) that are likely still valid.
   - Fix any that reference stale class names or IDs.

**Important:** For each HTML file read, look for the actual IDs and classes in the DOM. The v4.3/v4.4 rebuild established consistent patterns: `#email`, `#password`, `#submitBtn` on login, `.app-shell` as main wrapper, etc.

**Do NOT change test logic or assertions** unless the selector is genuinely stale. Keep all existing test coverage intact.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php/tests/e2e && grep -rn 'input\[name="api_key"\]\|input\[type="password"\], input\[name' specs/ | wc -l</automated>
  </verify>
  <done>Zero occurrences of stale `input[name="api_key"]` selectors across all specs. All 18 specs reference IDs and classes that exist in the current v4.3/v4.4 HTML pages. auth.spec.js uses #email/#password/#submitBtn. mobile-viewport.spec.js uses v4.3 selectors.</done>
</task>

</tasks>

<verification>
1. `cd tests/e2e && npx playwright test --list` shows all 18 spec files
2. `grep -rn 'api_key' tests/e2e/specs/` returns zero matches
3. `grep -rn '#email' tests/e2e/specs/auth.spec.js` returns matches
4. `grep -rn '#submitBtn' tests/e2e/specs/auth.spec.js` returns matches
</verification>

<success_criteria>
- Playwright installed with Chromium browser
- Config targets localhost:8080 (Docker stack)
- All 18 spec files audited against current HTML pages
- Zero stale `input[name="api_key"]` selectors remain
- auth.spec.js uses #email, #password, #submitBtn
- mobile-viewport.spec.js uses v4.3 login selectors
- accessibility.spec.js uses v4.3 login selectors
</success_criteria>

<output>
After completion, create `.planning/phases/56-e2e-test-updates/56-01-SUMMARY.md`
</output>
