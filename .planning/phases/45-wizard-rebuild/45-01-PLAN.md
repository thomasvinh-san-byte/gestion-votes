---
phase: 45-wizard-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/wizard.htmx.html
  - public/assets/css/wizard.css
autonomous: true
requirements: [REB-03]

must_haves:
  truths:
    - "Each of the 4 wizard steps fits in the viewport at 1024px without vertical scrolling to reach the Suivant button"
    - "Step 1 fields type/date/time render on a single horizontal row using form-grid-3"
    - "Step 1 fields place/address render on a single horizontal row using form-grid-2"
    - "Step 2 member add form renders as a single horizontal row (name, email, voting power, add button)"
    - "Step 3 resolution add panel shows title + majority on one row, description below, add button right-aligned"
    - "The segmented pill stepper bar renders with filled/active/pending visual states and connector lines"
    - "All wizard-specific CSS uses design tokens only — dark mode works without explicit overrides"
    - "Step 4 review displays a structured summary table with Modifier links back to each step"
  artifacts:
    - path: "public/wizard.htmx.html"
      provides: "Complete wizard HTML with 900px track, 4 steps, all DOM IDs preserved"
      contains: "wiz-content"
    - path: "public/assets/css/wizard.css"
      provides: "Complete wizard CSS with slide transitions, horizontal grids, stepper refinements, error banner"
      contains: "wizSlideIn"
  key_links:
    - from: "public/wizard.htmx.html"
      to: "public/assets/js/pages/wizard.js"
      via: "DOM IDs (wizTitle, wizType, wizDate, wizTimeHH, wizTimeMM, step0-step3, btnNext0-2, btnPrev1-3, btnCreate, etc.)"
      pattern: "id=\"wiz"
    - from: "public/wizard.htmx.html"
      to: "public/assets/css/wizard.css"
      via: "CSS classes (wiz-content, wiz-step, wiz-step-item, wiz-member-add-row, etc.)"
      pattern: "class=\"wiz-"
---

<objective>
Rewrite the wizard HTML and CSS from scratch with a 900px content track, horizontal field layouts, refined stepper with connector lines, slide transition CSS, step-level error banners, and compact sections with collapsible advanced rules.

Purpose: The current wizard uses a narrow implicit track (~680px) that stacks fields vertically, causing each step to overflow the viewport. The rebuild uses horizontal grids and a wider track so every step fits at 1024px.

Output: Rewritten wizard.htmx.html and wizard.css. All DOM IDs preserved for wizard.js compatibility. JS file is NOT touched in this plan.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-wizard-rebuild/45-CONTEXT.md
@.planning/phases/45-wizard-rebuild/45-RESEARCH.md

@public/wizard.htmx.html
@public/assets/css/wizard.css
@public/assets/js/pages/wizard.js

<interfaces>
<!-- DOM IDs that wizard.js uses — these MUST all exist in the new HTML with the same IDs -->
<!-- From wizard.js getElementById calls: -->
Step containers: step0, step1, step2, step3
Stepper: wizStepper, wizStepSubtitle, stepNavCounter
Step 1 fields: wizTitle, wizType, wizDate, wizTimeHH, wizTimeMM, wizPlace, wizAddr, wizQuorum, wizDefaultMaj
Step 1 errors: errWizTitle, errWizDate, errWizTime
Step 2: wizMemberAddForm, wizMemberName, wizMemberEmail, wizMemberVp, wizMemberVpField, btnAddMemberInline, wizMembersList, wizMemberCount, wizVoixTotal, wizVotingPowerToggle, btnImportCSV, csvFileInput, csvDropZone, errStep1Members
Step 3: resoTitle, resoDesc, resoMaj, chipNonSecret, chipSecret, wizResoList, btnAddReso, resoAddTrigger, btnShowResoPanel, errStep2Reso
Step 4: wizRecap, btnCreate
Navigation: btnNext0, btnNext1, btnNext2, btnPrev1, btnPrev2, btnPrev3

<!-- CSS classes wizard.js queries: -->
.wiz-step-item (querySelectorAll for stepper update)
.wiz-snum (querySelector inside each step-item)
.wiz-template-btn (querySelectorAll for template click)
.reso-add-panel (querySelector for panel show/hide)
.member-votes (querySelectorAll for voting power toggle)
.resolution-documents[data-motion-id] (querySelector for FilePond)
.filepond-input (querySelector inside resolution docs container)
.doc-list (querySelector inside docs container)
.review-modifier (querySelectorAll in recap for edit links)

<!-- Design system classes available (from design-system.css): -->
.form-grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-field) 12px; }
.form-grid-3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-field) 12px; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite wizard.htmx.html from scratch</name>
  <files>public/wizard.htmx.html</files>
  <read_first>
    - public/wizard.htmx.html (current HTML — all DOM IDs to preserve)
    - public/assets/js/pages/wizard.js (DOM selectors — lines 56-120, 145-170, 193-260, 310-435, 710-765, 783-870, 900-960, 1003-1025)
    - public/assets/css/design-system.css (lines 1910-1950 — form-grid-2, form-grid-3 classes)
    - .planning/phases/45-wizard-rebuild/45-RESEARCH.md (patterns 1-5, anti-patterns)
  </read_first>
  <action>
Rewrite public/wizard.htmx.html completely. Keep the same head section (meta, fonts, CSS links, FilePond CSS CDN). Keep the same script section at bottom (utils.js, shared.js, shell.js, meeting-context.js, FilePond plugins, wizard.js, ag-toast, ag-popover). Keep the same sidebar, header, and footer structure.

Rewrite everything inside `<main class="app-main">`:

**Stepper bar** (id="wizStepper"):
- Keep `.wiz-progress-wrap` with 4 `.wiz-step-item[data-step="N"]` children, each containing `.wiz-snum` div and a span label.
- Same labels: Informations, Membres, Resolutions, Revision.
- First item has class `active`. wizard.js manages active/done classes — no change needed.

**Content track** (class="wiz-content"):
- `max-width: 900px; margin: 0 auto` — wider track per locked decision.
- Contains 4 `.wiz-step` divs (id="step0" through id="step3").
- Only step0 has class `active` initially. Steps 1-3 have NO `style="display:none"` — CSS `.wiz-step { display: none; }` / `.wiz-step.active { display: block; }` handles visibility. This is CRITICAL for the slide animation to work.

**Step 0 — Informations generales:**
- Error banner: `<div class="wiz-error-banner" id="errBannerStep0" hidden>` with error icon SVG and `<span id="errBannerStep0Text"></span>`.
- Required note: `.wiz-required-note` with asterisk.
- Section "Identite de la seance" with `.wiz-section-title` and icon SVG.
- Title field: full-width `.field` with id="wizTitle", id="errWizTitle" error.
- 3-column grid `<div class="form-grid-3">` containing type (wizType select), date (wizDate input), time (wizTimeHH/wizTimeMM in `.time-input-wrap`). Same field IDs, same error IDs (errWizDate, errWizTime).
- Section "Lieu" with `.wiz-section-title` and map-pin icon SVG.
- 2-column grid `<div class="form-grid-2">` with wizPlace and wizAddr fields.
- Collapsed `<details class="wiz-advanced-toggle">` for vote rules. Summary shows "Regles de vote" with hint "Quorum - Majorite par defaut" and chevron SVG. Body has `form-grid-2` with wizQuorum select and wizDefaultMaj select, plus the yellow alert about convocation deadline.
- Step navigation `.step-nav`: Retour link to dashboard, `.step-nav-counter` id="stepNavCounter" "Etape 1 sur 4", btnNext0 primary button "Suivant".
- NO `.wiz-step-body` wrapper with overflow-y: auto. The step div directly contains sections and step-nav.

**Step 1 — Participants:**
- Error banner: `<div class="wiz-error-banner" id="errBannerStep1" hidden>` with `<span id="errBannerStep1Text"></span>`.
- CSV import row: btnImportCSV button and hidden csvFileInput file input.
- CSV drop zone: `<div class="upload-zone" id="csvDropZone">` — compact horizontal style.
- Voting power toggle: `.wiz-toggle-row` with wizVotingPowerToggle checkbox.
- Member add form as SINGLE HORIZONTAL ROW: `<div class="wiz-member-add-row" id="wizMemberAddForm">` containing:
  - `.field.field--flex` with wizMemberName input
  - `.field.field--flex` with wizMemberEmail input
  - `.field.field--w-narrow` id="wizMemberVpField" style="display:none" with wizMemberVp input
  - `button.btn.btn-primary.btn-sm.wiz-member-add-btn` id="btnAddMemberInline" "+ Ajouter"
- Members list: `<div id="wizMembersList"></div>` (populated by JS).
- Total row: `.members-total-row` with wizMemberCount and wizVoixTotal spans.
- Error: `<div id="errStep1Members" class="field-error-msg">` (same text).
- Step navigation: btnPrev1, "Etape 2 sur 4", btnNext1.

**Step 2 — Resolutions:**
- Error banner: `<div class="wiz-error-banner" id="errBannerStep2" hidden>` with `<span id="errBannerStep2Text"></span>`.
- Template grid: `.wiz-template-grid` with 3 `.wiz-template-card.wiz-template-btn` buttons. Same data-template attributes (approbation-comptes, election-conseil, modification-reglement). Same icon SVGs, same titles and descriptions.
- Add resolution trigger: `<div class="reso-add-trigger" id="resoAddTrigger">` with btnShowResoPanel button.
- Add resolution panel: `<div class="reso-add-panel" style="display:none;">`. Title + majority on one row using `form-grid-2`: resoTitle input + resoMaj select. Description below full-width: resoDesc textarea (rows="2"). Secret vote chips: chipNonSecret, chipSecret in `.chips-row`. Footer with btnAddReso right-aligned.
- Resolution list: `<div id="wizResoList"></div>`.
- Inline style block for `.resolution-documents`, `.doc-card`, `.doc-list` — keep same as current (lines 411-419 of current HTML).
- Error: `<div id="errStep2Reso" class="field-error-msg">`.
- Step navigation: btnPrev2, "Etape 3 sur 4", btnNext2.

**Step 3 — Revision:**
- Success alert: same green alert "Pret a creer".
- Recap container: `<div id="wizRecap"></div>` (populated by JS — buildRecap function will need DOM to exist).
- Warning alert: same yellow alert about not launching the vote.
- Step navigation: btnPrev3, "Etape 4 sur 4", btnCreate "Creer la seance".

IMPORTANT — Do NOT use `style="display:none"` on step1, step2, step3. Use CSS class-based visibility (`display:none` on `.wiz-step`, `display:block` on `.wiz-step.active`). Step0 gets class="wiz-step active", others get class="wiz-step" only.
  </action>
  <verify>
    <automated>grep -c 'id="wizTitle"\|id="wizType"\|id="wizDate"\|id="wizTimeHH"\|id="wizTimeMM"\|id="wizPlace"\|id="wizAddr"\|id="wizQuorum"\|id="wizDefaultMaj"\|id="step0"\|id="step1"\|id="step2"\|id="step3"\|id="btnNext0"\|id="btnNext1"\|id="btnNext2"\|id="btnPrev1"\|id="btnPrev2"\|id="btnPrev3"\|id="btnCreate"\|id="wizMemberName"\|id="wizMemberEmail"\|id="wizMemberVp"\|id="wizMemberVpField"\|id="btnAddMemberInline"\|id="wizMembersList"\|id="wizRecap"\|id="wizStepper"' public/wizard.htmx.html</automated>
  </verify>
  <acceptance_criteria>
    - grep 'wiz-content' public/wizard.htmx.html returns at least 1 match
    - grep 'form-grid-3' public/wizard.htmx.html returns at least 1 match (type/date/time row)
    - grep 'form-grid-2' public/wizard.htmx.html returns at least 2 matches (place/addr row + vote rules row)
    - grep 'wiz-member-add-row' public/wizard.htmx.html returns at least 1 match
    - grep 'wiz-error-banner' public/wizard.htmx.html returns at least 3 matches (one per step 0-2)
    - grep 'wiz-advanced-toggle' public/wizard.htmx.html returns at least 1 match (collapsed details)
    - All 29 DOM IDs used by wizard.js are present (verify count >= 29)
    - No style="display:none" on step1/step2/step3 divs
    - grep 'wiz-step active' public/wizard.htmx.html returns 1 match (step0 only)
  </acceptance_criteria>
  <done>wizard.htmx.html is completely rewritten with 900px track, horizontal field grids, error banners, all DOM IDs preserved, no inline display:none on step panels</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite wizard.css from scratch</name>
  <files>public/assets/css/wizard.css</files>
  <read_first>
    - public/assets/css/wizard.css (current CSS — understand what exists)
    - public/wizard.htmx.html (the NEW HTML from Task 1 — match class names)
    - public/assets/css/design-system.css (lines 1-100 for tokens, 1910-1950 for form grids)
    - .planning/phases/45-wizard-rebuild/45-RESEARCH.md (patterns 1-5: step slot, 900px track, viewport-fit, member add row, error banner)
  </read_first>
  <action>
Rewrite public/assets/css/wizard.css completely. Use ONLY CSS custom properties (tokens) from design-system.css — never hardcode hex values. This ensures dark mode works automatically.

**Content track:**
```css
.wiz-content {
  max-width: 900px;
  margin: 0 auto;
  padding: var(--space-4);
  overflow: hidden; /* clip slide animations */
  position: relative;
}
```

**Step visibility and slide transitions:**
```css
.wiz-step {
  display: none;
  animation: wizSlideIn 220ms cubic-bezier(0.22, 1, 0.36, 1) both;
}
.wiz-step.active {
  display: block;
}
.wiz-step.slide-out {
  display: block;
  animation: wizSlideOut 180ms ease-in both;
}
@keyframes wizSlideIn {
  from { opacity: 0; transform: translateX(32px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes wizSlideOut {
  from { opacity: 1; transform: translateX(0); }
  to   { opacity: 0; transform: translateX(-32px); }
}
```

**Stepper bar** (`.wiz-progress-wrap`):
- `display: flex; justify-content: center; gap: 0; position: sticky; top: 0; z-index: 10;`
- Background: `var(--color-surface)` with subtle bottom border.
- Padding: `12px var(--space-4)`.
- `.wiz-step-item`: `display: flex; align-items: center; gap: 6px; padding: 8px 16px; font-size: 0.8125rem; font-weight: 500; color: var(--color-text-muted); cursor: pointer; position: relative; transition: color 150ms;`
- `.wiz-step-item.active`: `color: var(--color-primary); font-weight: 600;`
- `.wiz-step-item.done`: `color: var(--color-success);`
- Connector lines via `::before` on `:not(:first-child)`: `content: ''; width: 24px; height: 2px; background: var(--color-border); margin-right: 4px; flex-shrink: 0;`
- `.wiz-step-item.done::before`: `background: var(--color-success);`
- `.wiz-snum`: `width: 24px; height: 24px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 0.75rem; font-weight: 600; background: var(--color-bg-subtle); color: var(--color-text-muted); flex-shrink: 0;`
- `.wiz-step-item.active .wiz-snum`: `background: var(--color-primary); color: #fff;`
- `.wiz-step-item.done .wiz-snum`: `background: var(--color-success); color: #fff;`

**Step card and sections:**
- `.wiz-step` gets card styling: `background: var(--color-surface); border-radius: var(--radius-lg); border: 1px solid var(--color-border-subtle); padding: 0;`
- Remove ALL `overflow-y: auto` and `min-height: 0` and `flex: 1` from step body. Steps grow to content height naturally.
- `.wiz-step-body` is removed from HTML but if remnants exist in classes, ensure no overflow. If task 1 kept `.wiz-step-body`, style it as `padding: 1rem 1.5rem;` with NO overflow.
- `.wiz-section`: `margin-bottom: 1rem;`
- `.wiz-section-title`: `display: flex; align-items: center; gap: 6px; font-size: 0.8125rem; font-weight: 600; color: var(--color-text-muted); text-transform: uppercase; letter-spacing: 0.04em; margin-bottom: 0.5rem;`
- `.wiz-required-note`: `font-size: 0.75rem; color: var(--color-text-muted); margin-bottom: 0.75rem;`

**Error banner:**
```css
.wiz-error-banner {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 10px 14px;
  background: var(--color-danger-subtle);
  border: 1px solid var(--color-danger);
  border-radius: var(--radius);
  color: var(--color-danger);
  font-size: 0.875rem;
  font-weight: 500;
  margin-bottom: 1rem;
}
.wiz-error-banner[hidden] { display: none; }
```

**Member add row (single horizontal line):**
```css
.wiz-member-add-row {
  display: flex;
  align-items: flex-end;
  gap: 10px;
  padding: 12px 14px;
  background: var(--color-bg-subtle);
  border-radius: 10px;
  border: 1px solid var(--color-border-subtle);
  margin-bottom: 1rem;
}
.wiz-member-add-row .field { margin: 0; }
.wiz-member-add-row .field--flex { flex: 1; }
.wiz-member-add-row .field--w-narrow { width: 90px; flex-shrink: 0; }
.wiz-member-add-btn { flex-shrink: 0; align-self: flex-end; margin-bottom: 0; }
```

**Step navigation:**
```css
.step-nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 1.5rem;
  border-top: 1px solid var(--color-border-subtle);
  background: var(--color-bg-subtle);
  border-radius: 0 0 var(--radius-lg) var(--radius-lg);
}
.step-nav-counter {
  font-size: 0.8125rem;
  color: var(--color-text-muted);
  font-weight: 500;
}
```

**Advanced toggle (details/summary):**
- `.wiz-advanced-toggle`: `border: 1px solid var(--color-border-subtle); border-radius: var(--radius); margin-top: 0.5rem;`
- `.wiz-advanced-summary`: `display: flex; align-items: center; gap: 8px; padding: 10px 14px; font-size: 0.8125rem; font-weight: 500; cursor: pointer; list-style: none; color: var(--color-text-muted);`
- `.wiz-advanced-summary::-webkit-details-marker { display: none; }`
- `.wiz-advanced-hint`: `flex: 1; font-weight: 400; opacity: 0.7;`
- `.wiz-advanced-chevron`: `transition: transform 200ms;`
- `.wiz-advanced-toggle[open] .wiz-advanced-chevron`: `transform: rotate(180deg);`
- `.wiz-advanced-body`: `padding: 0 14px 14px;`

**Template grid:**
- `.wiz-template-grid`: `display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; margin-bottom: 1rem;`
- `.wiz-template-card`: `display: flex; align-items: flex-start; gap: 10px; padding: 12px; border: 1px solid var(--color-border-subtle); border-radius: var(--radius); cursor: pointer; background: var(--color-surface); transition: border-color 150ms, box-shadow 150ms; text-align: left;`
- `.wiz-template-card:hover`: `border-color: var(--color-primary); box-shadow: 0 0 0 1px var(--color-primary);`
- `.wiz-template-card-icon`: `width: 32px; height: 32px; display: flex; align-items: center; justify-content: center; border-radius: var(--radius-sm); background: var(--color-bg-subtle); flex-shrink: 0;`
- `.wiz-template-card-title`: `font-size: 0.8125rem; font-weight: 600; color: var(--color-text);`
- `.wiz-template-card-desc`: `font-size: 0.75rem; color: var(--color-text-muted); margin-top: 2px;`

**Resolution add panel:**
- `.reso-add-panel`: `padding: 14px; background: var(--color-bg-subtle); border-radius: var(--radius); border: 1px solid var(--color-border-subtle); margin-bottom: 1rem;`
- `.reso-add-title`: `display: flex; align-items: center; gap: 6px; font-size: 0.8125rem; font-weight: 600; margin-bottom: 0.75rem;`
- `.reso-add-footer`: `display: flex; justify-content: flex-end; margin-top: 0.5rem;`

**Upload zone:**
- `.upload-zone`: `display: flex; align-items: center; gap: 12px; padding: 12px 16px; border: 2px dashed var(--color-border); border-radius: var(--radius); margin-bottom: 0.75rem; color: var(--color-text-muted); font-size: 0.8125rem;` — compact horizontal, NOT tall vertical box.
- `.upload-zone-title`: `font-weight: 500;`

**Toggle row:**
- `.wiz-toggle-row`: `margin-bottom: 0.75rem;`
- `.toggle-label`: `display: flex; align-items: center; gap: 8px; font-size: 0.8125rem; cursor: pointer;`

**Members total row:**
- `.members-total-row`: `padding: 8px 0; border-top: 1px solid var(--color-border-subtle); margin-top: 0.5rem; font-size: 0.8125rem; color: var(--color-text-muted);`

**Chips row (secret vote toggle):**
- `.chips-row`: `display: flex; gap: 6px;`
- `.chip`: `padding: 4px 12px; border-radius: 999px; font-size: 0.75rem; font-weight: 500; border: 1px solid var(--color-border); cursor: pointer; color: var(--color-text-muted); background: transparent; transition: all 150ms;`
- `.chip.active`: `background: var(--color-primary); color: #fff; border-color: var(--color-primary);`

**Review recap (Step 4) — styled for summary table:**
- `.review-section`: `margin-bottom: 1rem; border: 1px solid var(--color-border-subtle); border-radius: var(--radius); overflow: hidden;`
- `.review-section-header`: `display: flex; justify-content: space-between; align-items: center; padding: 8px 14px; background: var(--color-bg-subtle); font-size: 0.8125rem; font-weight: 600;`
- `.review-modifier`: `font-size: 0.75rem; color: var(--color-primary); cursor: pointer; font-weight: 500; background: none; border: none;`
- `.review-row`: `display: flex; justify-content: space-between; padding: 6px 14px; font-size: 0.8125rem; border-top: 1px solid var(--color-border-subtle);`
- `.review-label`: `color: var(--color-text-muted);`
- `.review-value`: `font-weight: 500;`

**Time input:**
- `.time-input-wrap`: `display: flex; align-items: center; gap: 4px;`
- `.time-input-hh, .time-input-mm`: `width: 48px; text-align: center;`
- `.time-sep`: `font-weight: 600; color: var(--color-text-muted);`

**Responsive at 768px:**
```css
@media (max-width: 768px) {
  .wiz-template-grid { grid-template-columns: 1fr; }
  .wiz-member-add-row { flex-wrap: wrap; }
  .wiz-member-add-row .field--flex { flex: 1 1 200px; }
}
```

**Field error message** (consistent with design system):
- `.field-error-msg`: `display: none; font-size: 0.75rem; color: var(--color-danger); margin-top: 4px;`
- `.field-error-msg.visible, .field.has-error .field-error-msg`: `display: block;`
- `.field.has-error .field-input`: `border-color: var(--color-danger);`

Keep the `.wf-step` header hidden: `.wf-step { display: none; }` — consistent with current behavior.

Do NOT include any `overflow-y: auto` or `min-height: 0` on any wizard element.
  </action>
  <verify>
    <automated>grep -c 'wizSlideIn\|wizSlideOut\|wiz-content\|wiz-member-add-row\|wiz-error-banner\|wiz-template-grid\|wiz-advanced-toggle\|step-nav' public/assets/css/wizard.css</automated>
  </verify>
  <acceptance_criteria>
    - grep 'max-width: 900px' public/assets/css/wizard.css returns at least 1 match
    - grep 'wizSlideIn' public/assets/css/wizard.css returns at least 1 match
    - grep 'wizSlideOut' public/assets/css/wizard.css returns at least 1 match
    - grep 'wiz-member-add-row' public/assets/css/wizard.css returns at least 1 match
    - grep 'wiz-error-banner' public/assets/css/wizard.css returns at least 1 match
    - grep 'overflow-y' public/assets/css/wizard.css returns 0 matches (no overflow scroll)
    - grep -c '#[0-9a-fA-F]\{3,6\}' public/assets/css/wizard.css returns 2 or fewer matches (only #fff for white text on colored badges — all other colors use tokens)
    - grep 'translateX' public/assets/css/wizard.css returns at least 2 matches (slideIn + slideOut)
  </acceptance_criteria>
  <done>wizard.css is completely rewritten with 900px track, slide transitions, horizontal member add row, error banners, compact stepper with connectors, all using design tokens for dark mode parity, no overflow-y:auto anywhere</done>
</task>

</tasks>

<verification>
1. Open wizard.htmx.html in browser — page loads without CSS errors, stepper is visible, step 0 shows
2. All 4 steps have correct DOM IDs (grep confirms 29+ IDs present)
3. wizard.css has no overflow-y:auto, uses design tokens throughout
4. Slide transition keyframes are defined in CSS
5. Error banners exist in HTML for steps 0-2
6. form-grid-3 is used for type/date/time row, form-grid-2 for place/addr
</verification>

<success_criteria>
- wizard.htmx.html rewritten with 900px content track and horizontal field layouts
- wizard.css rewritten with slide transitions, refined stepper, error banners, all tokens
- All DOM IDs preserved for wizard.js compatibility (29+ IDs confirmed)
- No overflow-y:auto in any wizard CSS rule
- Dark mode works automatically via token usage
</success_criteria>

<output>
After completion, create `.planning/phases/45-wizard-rebuild/45-01-SUMMARY.md`
</output>
