---
phase: 12-page-by-page-mvp-sweep
plan: 07
type: execute
wave: 2
depends_on: []
files_modified:
  - public/assets/css/vote.css
  - tests/e2e/specs/critical-path-vote.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "vote page main content area (.vote-main) uses full width of its container without an artificial 720px cap"
    - "vote.css contains zero hex/oklch/rgba color literals — every color is a design-system token or color-mix with var(--*) bases"
    - "Each primary vote interaction (meeting select, member select, vote button wiring, confirm button, zoom toggle) is asserted by Playwright via observable DOM change"
  artifacts:
    - path: public/assets/css/vote.css
      provides: vote page styles, width caps removed
      contains: "max-width: 100%"
    - path: tests/e2e/specs/critical-path-vote.spec.js
      provides: function gate for vote page
      contains: "test.describe"
  key_links:
    - from: tests/e2e/specs/critical-path-vote.spec.js
      to: public/vote.htmx.html
      via: "page.goto('/vote.htmx.html') + locator click + assert observable"
      pattern: "vote.htmx.html"
---

<objective>
Phase 12 Wave 2 / Page 3: Sweep `/vote` so it passes the 3 MVP gates (width, design tokens, function).

Purpose: vote is the tablet-optimized voting interface (2751-line CSS, largest in the app). It has multiple artificial max-width caps: `.vote-main` at 720px and `.vote-app` at 780px. The token gate requires verifying the :root custom properties use only var(--*) bases (they do — all color-mix calls reference design tokens). The function gate extends beyond the existing critical-path-votant.spec.js (which only checks visibility).

Output: vote.css width caps removed, token compliance verified, plus a `critical-path-vote.spec.js` Playwright spec asserting primary interactions produce real observable results.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-PAGES-AUDIT.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/vote.htmx.html
@public/assets/css/vote.css
@tests/e2e/specs/critical-path-votant.spec.js
@tests/e2e/helpers.js
</context>

<interfaces>
Existing helper (extract):
```js
// tests/e2e/helpers.js
const { loginAsVoter } = require('../helpers');
// usage: await loginAsVoter(page);
```

Existing critical-path-votant.spec.js (Phase 9) covers:
- Login as voter, navigate to /vote.htmx.html
- Assert `#voteApp` visible with `data-vote-state` attribute
- Assert `#meetingSelect` visible
- Assert `#memberSelect` visible
- Assert `#voteWaitingState` visible
- Assert `#btnConfirm` exists in DOM
- Assert `#btnZoom` click toggles `aria-pressed`
BUT: no real state change asserted beyond visibility — no API call, no DOM mutation from interaction.

Vote page primary interactive IDs (from public/vote.htmx.html):
- App shell: `#voteApp` with `data-vote-state="waiting|loading|ready|voting|confirmed"`
- Meeting select: `#meetingSelect`
- Member select: `#memberSelect`
- Identity banner: `#identityBanner`, `#identityName`, `#identityMeeting`
- Zoom toggle: `#btnZoom` (aria-pressed)
- Vote buttons: `.vote-btn` (for/against/abstain/blanc)
- Confirm button: `#btnConfirm`
- Waiting state: `#voteWaitingState`
- Confirmed state: `#voteConfirmedState`
- Speech button: `#btnHand` (aria-pressed)
- Resolution counter: `#voteResolutionCounter`
- Offline banner: `#offlineBanner`
- Timer: `#voteTimer`
- Progress dots: `#voteProgressDots`

Current vote.css width issues (lines to fix):
- Line 166: `.vote-main { max-width: 720px; }` — caps main content area
- Line 680: `.vote-buttons { max-width: 480px; }` — caps vote button container
- Line 1335: `@media landscape { .vote-buttons { max-width: 480px; } }` — redundant cap
- Line 1487: `.vote-app { max-width: 780px; }` — caps entire app frame
- Line 1729: `[data-vote-state="voting"] .vote-buttons { max-width: 480px; }` — caps during voting
- Line 1976: `.blocked-overlay-inner { max-width: 520px; }` — modal overlay, KEEP (legitimate)

Decision: The vote page IS applicative (not content). Per CONTEXT.md width gate rules, applicative pages get full width. The 780px tablet frame was a design choice, but per user decision it must fill available space. Remove the caps on `.vote-app`, `.vote-main`, and `.vote-buttons`. Keep `.blocked-overlay-inner` (modal).

Current vote.css token status: zero hex/oklch/rgba literals. All :root custom properties use `color-mix(in oklch, var(--*), ...)` — compliant. The [data-theme="dark"] block overrides the same properties. Clean.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width gate — remove artificial max-width caps on vote page containers</name>
  <files>public/assets/css/vote.css</files>
  <read_first>
    - public/assets/css/vote.css lines 155-170 (.vote-main)
    - public/assets/css/vote.css lines 675-683 (.vote-buttons)
    - public/assets/css/vote.css lines 1330-1337 (landscape .vote-buttons)
    - public/assets/css/vote.css lines 1483-1495 (.vote-app tablet frame)
    - public/assets/css/vote.css lines 1725-1730 (voting state .vote-buttons)
    - public/assets/css/vote.css lines 1972-1982 (.blocked-overlay-inner)
  </read_first>
  <action>
    Fix the following max-width caps:

    1. **Line ~166**: `.vote-main { max-width: 720px; }` — Change to `max-width: 100%;`. The `margin-inline: auto` and `width: 100%` already handle centering.

    2. **Line ~680**: `.vote-buttons { max-width: 480px; }` — Change to `max-width: 100%;`. Vote buttons should fill their container.

    3. **Line ~1335**: Inside `@media (min-width: 769px) and (orientation: landscape)`, `.vote-buttons { max-width: 480px; }` — Change to `max-width: 100%;`.

    4. **Line ~1487**: `.vote-app { max-width: 780px; }` — Change to `max-width: 100%;`. The full-viewport tablet experience should use available width.

    5. **Line ~1729**: `[data-vote-state="voting"] .vote-buttons { max-width: 480px; }` — Change to `max-width: 100%;`.

    Do NOT touch:
    - Line ~1976: `.blocked-overlay-inner { max-width: 520px; }` — this is a modal/overlay, not a page container. KEEP.
    - Any `@media (max-width: ...)` breakpoint queries — those are responsive, not caps.

    After editing, run:
    ```bash
    grep -nE 'max-width:\s*[0-9]+px' public/assets/css/vote.css | grep -v '@media' | grep -v 'blocked-overlay\|modal\|popover\|tooltip'
    ```
    Expected: zero matches (all page-container caps removed, only overlay kept).
  </action>
  <verify>
    <automated>test "$(grep -nE '^\s*max-width:\s*[0-9]+px' public/assets/css/vote.css | grep -v '@media' | grep -v 'blocked-overlay\|modal\|popover\|tooltip\|overlay-inner' | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'max-width: 720px' public/assets/css/vote.css` returns 0.
    - `grep -c 'max-width: 780px' public/assets/css/vote.css` returns 0.
    - `grep -c 'max-width: 480px' public/assets/css/vote.css` returns 0.
    - `.blocked-overlay-inner { max-width: 520px; }` is still present (overlay preserved).
    - `grep -c 'max-width: 100%' public/assets/css/vote.css` returns at least 3 (replacements made).
  </acceptance_criteria>
  <done>Vote page containers (.vote-app, .vote-main, .vote-buttons) no longer have artificial width caps; only the blocked overlay modal retains its constraint.</done>
</task>

<task type="auto">
  <name>Task 2: Token gate — verify zero color literals in vote.css</name>
  <files>public/assets/css/vote.css</files>
  <read_first>
    - public/assets/css/vote.css lines 1-55 (:root and [data-theme="dark"] custom property blocks)
    - Scan the rest of the file for any raw hex/rgb outside :root
  </read_first>
  <action>
    Run the violation grep:
    ```bash
    grep -nE '#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(' public/assets/css/vote.css | grep -v '/\*' | grep -v '^\s*\*'
    ```

    Expected: zero matches. vote.css was rebuilt with design tokens. The :root block (lines 7-30) and [data-theme="dark"] block (lines 32-55) define custom properties using `color-mix(in oklch, var(--*), ...)` — these are TOKEN-BASED compositions, not raw literals.

    Also verify no raw `oklch(` calls exist outside color-mix context:
    ```bash
    grep -nE 'oklch\(' public/assets/css/vote.css | grep -v 'color-mix' | grep -v '/\*'
    ```

    Expected: zero matches.

    If any unexpected literal is found in the component styles (lines 56+), replace with the matching `var(--color-*)` or `var(--vote-*)` custom property from the :root block.
  </action>
  <verify>
    <automated>test -z "$(grep -nE '#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(' public/assets/css/vote.css | grep -v '/\*' | grep -v '^\s*\*')" && test -z "$(grep -nE 'oklch\(' public/assets/css/vote.css | grep -v 'color-mix' | grep -v '/\*')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/vote.css` returns zero non-comment lines.
    - `grep -nE 'oklch\(' public/assets/css/vote.css | grep -v 'color-mix'` returns zero non-comment lines.
    - All color values use `var(--*)` or `color-mix(in oklch, var(--*), ...)`.
  </acceptance_criteria>
  <done>vote.css verified: zero raw color literals. All 43 color-mix() calls use var(--*) bases.</done>
</task>

<task type="auto">
  <name>Task 3: Function gate — Playwright spec asserts real results for vote interactions</name>
  <files>tests/e2e/specs/critical-path-vote.spec.js</files>
  <read_first>
    - tests/e2e/specs/critical-path-votant.spec.js (existing spec — visibility assertions only)
    - tests/e2e/helpers.js (for `loginAsVoter`)
    - tests/e2e/helpers/waitForHtmxSettled.js
    - public/vote.htmx.html (full file — to know which IDs are present)
  </read_first>
  <action>
    Create a NEW spec file `tests/e2e/specs/critical-path-vote.spec.js` (separate from critical-path-votant.spec.js which tests the role login path; this tests the PAGE interactions).

    The spec must use `loginAsVoter(page)` and contain ONE `test()` tagged `@critical-path` named "vote page: selectors + zoom + app state + speech button".

    Use `test.setTimeout(120000)` and `waitUntil: 'domcontentloaded'`.

    For each PRIMARY interaction below, trigger AND assert an observable result:

    1. **App shell boot — data-vote-state attribute**
       - Navigate to `/vote.htmx.html`
       - Wait for `#voteApp` to be visible
       - Assert `#voteApp` has `data-vote-state` attribute with a value (proves JS booted and set initial state)

    2. **Meeting select — dropdown is populated**
       - Assert `#meetingSelect` is visible
       - Check if it has `<option>` children OR is a custom `<ag-searchable-select>` with real data
       - If standard select: assert `page.locator('#meetingSelect option').count()` > 0 (at least the placeholder + any meetings)
       - If custom component: assert it's visible and interactive

    3. **Member select — dropdown is populated**
       - Assert `#memberSelect` is visible
       - Same approach: assert options or custom component renders

    4. **Zoom toggle — aria-pressed flips**
       - Click `#btnZoom`
       - Assert `aria-pressed` attribute changes (from "false" to "true" or vice versa)
       - Click again to restore original state
       - Assert it flipped back (proves two-way toggle)

    5. **Speech button — hand raise interaction**
       - If `#btnHand` is visible:
         - Store initial `aria-pressed` value
         - Click `#btnHand`
         - Set up `page.waitForResponse(r => r.url().includes('speech') || r.url().includes('hand'))` OR assert `aria-pressed` flips OR assert `#speechLabel` text changes
         - If an API call was made, assert response status < 500

    6. **Offline banner — hidden in connected state**
       - Assert `#offlineBanner` is hidden (proves the online state detection is running)

    7. **Waiting state panel — renders meaningful UI**
       - Assert `#voteWaitingState` is visible (when no vote is active, the waiting state should show)
       - Assert it contains text content (not empty div)

    NOTE: We cannot trigger a real vote without an active meeting with open vote. The test proves the app BOOTS correctly, selectors POPULATE, and interactive elements RESPOND — which is the Phase 12 function gate. Full voting flow is covered by critical-path-operator.spec.js (which opens a vote) and critical-path-votant.spec.js (which navigates the role path).
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-vote.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/critical-path-vote.spec.js` exists.
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-vote.spec.js` returns at least 1.
    - `grep -c "btnZoom\|meetingSelect\|memberSelect\|voteApp\|btnHand\|offlineBanner" tests/e2e/specs/critical-path-vote.spec.js` returns at least 4 (proves multiple interactions covered).
    - `./bin/test-e2e.sh specs/critical-path-vote.spec.js` exits 0.
  </acceptance_criteria>
  <done>The function gate is provable: a Playwright run asserts app boot state, selector population, zoom toggle, speech button wiring, offline detection, and waiting state rendering.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
- `grep -c 'max-width: 720px\|max-width: 780px\|max-width: 480px' public/assets/css/vote.css` returns 0 (width gate)
- `! grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/vote.css | grep -v '/\*'` returns empty (token gate)
- `./bin/test-e2e.sh specs/critical-path-vote.spec.js` exits 0 (function gate)
</verification>

<success_criteria>
- Width gate: vote.css page containers (.vote-app, .vote-main, .vote-buttons) use full width.
- Token gate: vote.css has zero raw color literals (all color-mix calls use var(--*) bases).
- Function gate: critical-path-vote.spec.js asserts real observable results for app boot, selectors, zoom, speech, and state management.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-07-SUMMARY.md` documenting:
- Width: which selectors were capped, what the fixes were (5 caps removed)
- Tokens: verification result (clean — 43 color-mix calls all use var bases)
- Function: list of interactions covered, the run output of `./bin/test-e2e.sh specs/critical-path-vote.spec.js`
</output>
