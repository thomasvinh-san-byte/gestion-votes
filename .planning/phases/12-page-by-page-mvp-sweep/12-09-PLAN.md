---
phase: 12-page-by-page-mvp-sweep
plan: 09
type: execute
wave: 3
depends_on: []
files_modified:
  - public/assets/css/audit.css
  - tests/e2e/specs/critical-path-audit.spec.js
autonomous: true
requirements: [MVP-01, MVP-02, MVP-03]
gap_closure: false
must_haves:
  truths:
    - "audit page uses full viewport width with no artificial container limit"
    - "audit.css contains zero hex/oklch/rgba color literals — every color is a design-system token"
    - "Each primary audit interaction (filter tabs, search, sort, view toggle, row click detail, export) is asserted by Playwright via observable DOM change or API call"
  artifacts:
    - path: public/assets/css/audit.css
      provides: audit page styles, full-width verified
      contains: "width: 100%"
    - path: tests/e2e/specs/critical-path-audit.spec.js
      provides: function gate for audit page
      contains: "test.describe"
  key_links:
    - from: tests/e2e/specs/critical-path-audit.spec.js
      to: public/audit.htmx.html
      via: "page.goto('/audit.htmx.html') + locator click + assert observable"
      pattern: "audit.htmx.html"
---

<objective>
Phase 12 Wave 3 / Page 1: Sweep `/audit` so it passes the 3 MVP gates (width, design tokens, function).

Purpose: audit is the traceability journal page with filter tabs, search, sort, table/timeline views, row-click detail modal, and CSV export. v1.2-PAGES-AUDIT.md marked it as "FONCTIONNE" but noted 1-2 minor orphan IDs (probable false positive from shared.js delegation). This plan proves the visible UI is full-width, on-brand, and provably functional.

Output: audit.css verified clean (width + tokens), plus a `critical-path-audit.spec.js` Playwright spec asserting primary interactions produce real observable results.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-PAGES-AUDIT.md
@.planning/phases/12-page-by-page-mvp-sweep/12-CONTEXT.md
@public/audit.htmx.html
@public/assets/css/audit.css
@public/assets/js/pages/audit.js
@tests/e2e/specs/page-interactions.spec.js
@tests/e2e/helpers.js
</context>

<interfaces>
Existing helper (extract):
```js
// tests/e2e/helpers.js
const { loginAsAdmin } = require('../helpers');
// usage: await loginAsAdmin(page);
```

Existing CSRF pattern (from critical-path-admin.spec.js):
```js
const csrfResp = await page.request.get('/api/v1/auth_csrf.php');
const csrfBody = await csrfResp.json();
const csrfToken = csrfBody?.token || csrfBody?.data?.token || csrfBody?.csrf_token || '';
```

Existing page-interactions.spec.js coverage (lines 130-143): only tests that `#auditSearch` can be filled with a value. Does NOT assert that typing triggers a filter or that API data loads. Insufficient for function gate.

Audit page primary interactive IDs (from public/audit.htmx.html):
- Filter tabs: `#auditTypeFilter` with `.filter-tab` children (Tous, Votes, Presences, Securite, Systeme)
- Search: `#auditSearch` (input type="search")
- Sort: `#auditSort` (select: date-desc, date-asc, severity-desc, severity-asc)
- View toggle: `.view-toggle-btn[data-view="table"]`, `.view-toggle-btn[data-view="timeline"]`
- Table body: `#auditTableBody`
- Timeline: `#auditTimeline`
- Table view container: `#auditTableView`
- Timeline view container: `#auditTimelineView`
- Pagination: `#auditPagination`
- Select all checkbox: `#selectAll`
- Export all: `#btnExportAll`
- Export selection: `#btnExportSelection` (hidden until selection made)
- Detail modal: `#auditDetailModal`, `#auditDetailBackdrop`
- Close detail: `#btnCloseAuditDetail`, `#btnCloseAuditDetail2`
- Export detail: `#btnExportDetail`
- Retry button: `#auditRetryBtn`
- KPI indicators: `#kpiIntegrity`, `#kpiEvents`, `#kpiAnomalies`, `#kpiLastSession`

Current audit.css width status: `.audit-page { width: 100%; }` at line 17 — already full-width. No artificial max-width on page container. Only sub-component max-widths exist (`.audit-col-hash max-width: 160px`, `.audit-hash-cell max-width: 120px`).

Current audit.css token status: zero hex/oklch/rgba literals found. All values use `var(--*)`. Clean.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Width gate + Token gate — verify audit page is full-width and token-pure</name>
  <files>public/assets/css/audit.css</files>
  <read_first>
    - public/assets/css/audit.css (entire file, 693 lines)
  </read_first>
  <action>
    Run the width audit grep:
    ```bash
    grep -nE 'max-width:\s*[0-9]+px' public/assets/css/audit.css | grep -v '@media'
    ```

    Expected: only sub-component hits (`.audit-col-hash` line 240 at 160px, `.audit-hash-cell` line 289 at 120px). These are column-internal constraints, NOT page-container caps. No fix needed.

    Verify `.audit-page { width: 100%; }` is present (line 17).

    Run the token violation grep:
    ```bash
    grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(' public/assets/css/audit.css | grep -v '/\*' | grep -v '^\s*\*'
    ```

    Expected: zero matches. audit.css was rebuilt in v4.3 with full design-system tokens.

    If any unexpected literal is found, replace with the matching `var(--color-*)` from design-system.css.
  </action>
  <verify>
    <automated>bash -c '! grep -nE "^\s*max-width:\s*[0-9]+px" public/assets/css/audit.css | grep -v "@media" | grep -v "col-hash\|hash-cell\|popover\|tooltip\|modal\|overlay\|search-input\|sort-select" && test -z "$(grep -nE "oklch\(|#[0-9a-fA-F]{3,8}[;\s,)]|rgba?\(" public/assets/css/audit.css | grep -v "/\*" | grep -v "^\s*\*" | grep -v "color-mix")"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'max-width:\s*[0-9]+px' public/assets/css/audit.css | grep -v '@media'` returns only sub-component hits (col-hash, hash-cell), zero page-container hits.
    - `.audit-page { width: 100%; }` confirmed present.
    - `grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/audit.css` returns zero non-comment lines.
  </acceptance_criteria>
  <done>Audit page verified full-width and token-pure; no changes needed.</done>
</task>

<task type="auto">
  <name>Task 2: Function gate — Playwright spec asserts real results for audit interactions</name>
  <files>tests/e2e/specs/critical-path-audit.spec.js</files>
  <read_first>
    - tests/e2e/specs/page-interactions.spec.js (lines 130-143 — existing audit test, too shallow)
    - tests/e2e/helpers.js (for loginAsAdmin)
    - public/audit.htmx.html (full file — to know which IDs are present)
    - public/assets/js/pages/audit.js (first 80 lines — to understand API call pattern and state)
  </read_first>
  <action>
    Create a NEW spec file `tests/e2e/specs/critical-path-audit.spec.js` (do not extend page-interactions — keep page-scoped specs separate per Phase 12 methodology).

    The spec must use `loginAsAdmin(page)` and contain ONE `test()` tagged `@critical-path` named "audit: filter tabs + search + sort + view toggle + data load + detail modal".

    Use `test.setTimeout(120000)` and `waitUntil: 'domcontentloaded'`.

    For each PRIMARY interaction below, trigger AND assert an observable result:

    1. **API data load — audit table populates**
       - Navigate to `/audit.htmx.html`
       - Wait for `#auditTableBody` to be visible
       - Assert KPI `#kpiEvents` text does NOT equal the placeholder dash character (proves API loaded)
       - OR if test env has no audit events, assert the empty state in table body

    2. **Filter tab click — observable DOM change**
       - Click the "Votes" filter tab (`#auditTypeFilter .filter-tab[data-type="votes"]`)
       - Assert the clicked tab gains `.active` class
       - Assert the "Tous" tab loses `.active` class
       - Click "Tous" to restore (or assert table content changed)

    3. **Search input — filters the table**
       - Fill `#auditSearch` with `zzz-no-match-${Date.now()}`
       - Wait 500ms (debounce)
       - Assert `#auditTableBody` contains either zero data rows or an empty-state message
       - Clear the search input
       - Wait 500ms
       - Assert table repopulates (tbody has rows again or KPI is restored)

    4. **Sort select — observable change**
       - Change `#auditSort` to `date-asc`
       - Assert the select has the new value (proves select is wired)

    5. **View toggle — switch to timeline**
       - Click `.view-toggle-btn[data-view="timeline"]`
       - Assert `#auditTimelineView` is visible (not hidden)
       - Assert `#auditTableView` is hidden
       - Click `.view-toggle-btn[data-view="table"]` to restore table view

    6. **Row click — detail modal opens** (only if table has data rows)
       - Click the first data row in `#auditTableBody tr:not(:has(.spinner))`
       - Assert `#auditDetailModal` becomes visible (hidden attribute removed)
       - Assert `#detailTimestamp` text is not the placeholder dash
       - Close the modal via `#btnCloseAuditDetail`
       - Assert `#auditDetailModal` is hidden again

    If test data has no audit events, adapt: skip row-click test (wrap in conditional), filter/search assertions work on zero-item state.
  </action>
  <verify>
    <automated>./bin/test-e2e.sh specs/critical-path-audit.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/specs/critical-path-audit.spec.js` exists.
    - `grep -c "@critical-path" tests/e2e/specs/critical-path-audit.spec.js` returns at least 1.
    - `grep -c "auditSearch\|auditTypeFilter\|auditSort\|auditTimelineView\|auditDetailModal" tests/e2e/specs/critical-path-audit.spec.js` returns at least 4 (proves multiple interactions covered).
    - `./bin/test-e2e.sh specs/critical-path-audit.spec.js` exits 0.
  </acceptance_criteria>
  <done>The function gate is provable: a Playwright run asserts filter tabs, search, sort, view toggle, data loading, and detail modal — all via observable DOM changes.</done>
</task>

</tasks>

<verification>
After all tasks:
- `! grep -nE '^\s*max-width:\s*[0-9]+px' public/assets/css/audit.css | grep -v '@media' | grep -v 'col-hash\|hash-cell'` returns empty (width gate)
- `! grep -nE 'oklch\(|#[0-9a-fA-F]{3,8}|rgba?\(' public/assets/css/audit.css | grep -v '/\*' | grep -v 'color-mix'` returns empty (token gate)
- `./bin/test-e2e.sh specs/critical-path-audit.spec.js` exits 0 (function gate)
</verification>

<success_criteria>
- Width gate: audit page verified full-width (no cap to remove).
- Token gate: audit.css has zero raw color literals.
- Function gate: critical-path-audit.spec.js asserts real observable results for filter tabs, search, sort, view toggle, data load, and detail modal.
</success_criteria>

<output>
After completion, create `.planning/phases/12-page-by-page-mvp-sweep/12-09-SUMMARY.md` documenting:
- Width: verification result (already clean or what was fixed)
- Tokens: verification result (already clean or how many literals replaced)
- Function: list of interactions covered, the run output of `./bin/test-e2e.sh specs/critical-path-audit.spec.js`
</output>
