---
phase: 02-sidebar-navigation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/e2e/specs/critical-path-votant.spec.js
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03]

must_haves:
  truths:
    - "Le test E2E votant verifie que /vote et /account sont visibles pour un votant"
    - "La sidebar a 200px de large visuellement sur une page reelle"
    - "Le votant ne voit que les liens attendus dans la sidebar"
  artifacts:
    - path: "tests/e2e/specs/critical-path-votant.spec.js"
      provides: "Updated voter sidebar assertions including /vote and /account visibility"
      contains: "a[href=\"/account\"]"
  key_links:
    - from: "tests/e2e/specs/critical-path-votant.spec.js"
      to: "public/partials/sidebar.html"
      via: "Test asserts nav items added in Plan 01 are visible for voter"
      pattern: "mustBeVisible.*account"
---

<objective>
Update the E2E test for voter sidebar visibility to assert the new "Voter" and "Mon compte" nav items are visible, then run the test suite and get human verification of the visual result.

Purpose: Confirm the sidebar transformation works end-to-end -- the new nav items appear for voters, admin items stay hidden, and the 200px layout looks correct visually.
Output: Updated E2E test passing, human-verified visual confirmation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sidebar-navigation/02-CONTEXT.md
@.planning/phases/02-sidebar-navigation/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01's sidebar.html changes -->
New nav items in sidebar.html (inside <nav id="main-nav">):
- `<a class="nav-item" href="/vote" data-page="vote" data-requires-role="admin,operator,president,voter">` with label "Voter"
- `<a class="nav-item" href="/account" data-page="account">` with label "Mon compte" (no data-requires-role = visible to all)

<!-- From existing test file -->
From tests/e2e/specs/critical-path-votant.spec.js (lines 60-107):
- Test name: 'votant: sidebar hides admin-only items (POLISH-03) @critical-path'
- mustBeHidden array: checks admin/settings/members/operator/hub/users are hidden
- mustBeVisible array: currently only checks `a[href="/"]` and `a[href="/help"]`
- Voter role previously could not see /vote in main nav (it was in .sidebar-device-section)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update E2E voter sidebar test with new nav item assertions</name>
  <files>tests/e2e/specs/critical-path-votant.spec.js</files>
  <read_first>
    - tests/e2e/specs/critical-path-votant.spec.js (full file)
    - public/partials/sidebar.html (to confirm final nav item structure from Plan 01)
  </read_first>
  <action>
    In `tests/e2e/specs/critical-path-votant.spec.js`, update the test 'votant: sidebar hides admin-only items (POLISH-03) @critical-path':

    **1. Add to the `mustBeVisible` array** (currently at line 100):
    ```javascript
    const mustBeVisible = [
      'a[href="/"]',         // brand link — all roles
      'a[href="/help"]',     // Guide & FAQ — all roles
      'a[href="/vote"]',     // Voter — voter role included in data-requires-role
      'a[href="/account"]',  // Mon compte — no role restriction
      'a[href="/dashboard"]', // Tableau de bord — no role restriction
    ];
    ```

    **2. Update the `mustBeHidden` array** — verify these items are still hidden for voter:
    Keep the existing entries. Also add `a[href="/wizard"]` to mustBeHidden (it requires admin,operator):
    ```javascript
    const mustBeHidden = [
      'a[href="/users"]',
      'a[href="/admin"]',
      'a[href="/settings"]',
      'a[href="/members"]',
      'a[href="/operator"]',
      'a[href="/hub"]',
      'a[href="/wizard"]',
    ];
    ```

    **3. Do NOT change** the test structure, timeouts, or login flow. Only modify the assertion arrays.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php/tests/e2e && timeout 60 npx playwright test specs/critical-path-votant.spec.js --project=chromium 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `mustBeVisible` array contains `'a[href="/vote"]'` and `'a[href="/account"]'` and `'a[href="/dashboard"]'`
    - `mustBeHidden` array contains `'a[href="/wizard"]'`
    - Test passes on chromium
    - grep: `grep -c 'href="/account"' tests/e2e/specs/critical-path-votant.spec.js` returns >= 1
    - grep: `grep -c 'href="/vote"' tests/e2e/specs/critical-path-votant.spec.js` returns >= 1
  </acceptance_criteria>
  <done>E2E voter sidebar test asserts /vote and /account are visible, /wizard is hidden, test passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of sidebar at 200px</name>
  <what-built>
    Static 200px sidebar with always-visible labels, no pin button, Mon compte + Voter nav items for voter role, 44px touch targets, and updated E2E test.
  </what-built>
  <how-to-verify>
    1. Open the app in a browser at the dashboard page (login as admin)
    2. Verify the sidebar is 200px wide with all labels visible — no hover needed
    3. Verify there is NO pin button in the top-right of the sidebar
    4. Verify nav group headers (Preparation, Seance en direct, etc.) are visible with chevrons
    5. Verify the main content is pushed right (220px offset) with no overlap
    6. Login as a voter role user
    7. Verify the voter sees only: brand logo, Tableau de bord, Voter, Guide & FAQ, Mon compte, and the theme toggle
    8. Verify clicking "Mon compte" navigates to /account without 404
    9. Verify clicking "Voter" navigates to /vote
    10. On mobile viewport (resize browser to < 768px): verify hamburger menu still works, sidebar slides in as overlay
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. E2E test passes: `cd tests/e2e && npx playwright test specs/critical-path-votant.spec.js --project=chromium`
2. Human confirms visual appearance of 200px sidebar
3. Human confirms voter role sees correct nav items
4. Human confirms mobile hamburger still works
</verification>

<success_criteria>
- E2E voter sidebar test green with /vote and /account assertions
- Admin sees full sidebar at 200px with all labels
- Voter sees only allowed nav items
- Mobile sidebar overlay still functional
- No visual regressions on desktop layout
</success_criteria>

<output>
After completion, create `.planning/phases/02-sidebar-navigation/02-02-SUMMARY.md`
</output>
