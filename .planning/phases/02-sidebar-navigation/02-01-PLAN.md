---
phase: 02-sidebar-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
  - public/partials/sidebar.html
  - public/assets/js/core/shell.js
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03]

must_haves:
  truths:
    - "La sidebar fait 200px de large en permanence avec labels visibles"
    - "Le mecanisme pin/unpin est completement supprime (CSS, JS, HTML)"
    - "Le contenu principal est decale de 220px a gauche sans toggle JS"
    - "Un votant voit 'Voter' et 'Mon compte' dans la sidebar"
    - "Tous les nav-items et nav-groups font minimum 44px de hauteur"
    - "Les labels longs sont tronques avec ellipsis"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Sidebar 200px static, labels opacity 1, touch targets 44px, no hover/pin rules"
      contains: "--sidebar-width: 200px"
    - path: "public/partials/sidebar.html"
      provides: "No pin button, Mon compte nav item, Voter in main nav"
      contains: "Mon compte"
    - path: "public/assets/js/core/shell.js"
      provides: "No pin/unpin logic, no localStorage sidebar key, no SidebarPin export"
  key_links:
    - from: "public/assets/css/design-system.css"
      to: "public/partials/sidebar.html"
      via: ".nav-item height 44px applies to all items including new Mon compte"
      pattern: "height:\\s*44px"
    - from: "public/assets/js/core/shell.js"
      to: "public/assets/css/design-system.css"
      via: "No JS toggle of padding-left; CSS handles it statically"
      pattern: "padding-left:\\s*calc\\(200px"
---

<objective>
Transform the sidebar from a 58px rail with hover-to-expand into a permanently open 200px sidebar with always-visible labels. Remove the pin/unpin mechanism entirely. Add "Mon compte" and "Voter" nav items for voter role. Set touch targets to 44px minimum.

Purpose: Users should see navigation labels at all times without hovering -- reduces cognitive load for non-technical users (55+ target audience).
Output: Static 200px sidebar, no pin button, voter-appropriate nav items, WCAG 2.5.8 compliant touch targets.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sidebar-navigation/02-CONTEXT.md
@.planning/phases/02-sidebar-navigation/02-RESEARCH.md
@.planning/phases/02-sidebar-navigation/02-UI-SPEC.md

<interfaces>
<!-- Key CSS rules and structures the executor must understand -->

From public/assets/css/design-system.css (current state):
- Line 489: `--sidebar-width: 58px;` (root token)
- Line 490: `--sidebar-rail: 58px;` (used by .app-sidebar width)
- Line 491: `--sidebar-expanded: 252px;` (hover/pin width)
- Lines 981-994: `.app-sidebar { width: var(--sidebar-rail); transition: width .25s ...; }`
- Lines 997-1008: `.app-sidebar:hover, .app-sidebar.pinned { width: var(--sidebar-expanded); }` + pinned rules
- Lines 1011-1022: hover/pinned reveal rules for .nav-label, .nav-group-label, .nav-group-chevron, .sidebar-version, .sidebar-pin, .device-tag
- Lines 1114-1122: `.nav-label { opacity: 0; max-width: 0; font-size: 14px; }`
- Lines 1125-1143: `.nav-group { height: 20px; padding: 6px 10px 2px; }`
- Lines 1144-1157: `.nav-group::after` divider line + hover/pinned hide rules
- Lines 1160-1171: `.nav-group-label { opacity: 0; max-width: 0; font-weight: 700; }`
- Lines 1172-1181: `.nav-group-chevron { opacity: 0; }`
- Line 1075: `.nav-item { height: 42px; }` (inside larger rule block at line 1067)
- Line 1086: `.nav-item { font-size: 14px; }`
- Line 1072: `.nav-item { gap: 10px; }`
- Lines 1207-1217: rail-mode badge dot rule `.app-sidebar:not(:hover):not(.pinned) .nav-badge[...]`
- Lines 1219-1248: `.sidebar-pin` styles (entire block)
- Lines 1256-1266: `.sidebar-version { opacity: 0; }`
- Lines 1267-1279: `.device-tag { opacity: 0; max-width: 0; }`
- Lines 1282-1290: `.app-main { padding-left: calc(var(--sidebar-rail) + 22px); }`
- Line 908: `.app-shell.no-sidebar .app-main { padding-left: 22px; }`

From public/partials/sidebar.html:
- Lines 2-5: `<button class="sidebar-pin" id="sidebarPin">` (to remove)
- Lines 122-129: `.sidebar-device-section` with "Apercu votant" vote link (to restructure)
- No "Mon compte" nav item exists

From public/assets/js/core/shell.js:
- Line 24: `const PIN_KEY = 'ag-vote-sidebar-pinned';`
- Lines 29-41: `togglePin()` function
- Lines 44-50: localStorage restore block
- Lines 53-63: `bindPinButton()` function + call
- Line 203: `window.SidebarPin = { toggle: togglePin };`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS — static 200px sidebar, remove hover/pin rules, touch targets 44px</name>
  <files>public/assets/css/design-system.css</files>
  <read_first>
    - public/assets/css/design-system.css (lines 485-500 for root tokens, lines 980-1025 for sidebar rules, lines 1065-1100 for .nav-item, lines 1114-1180 for labels/groups, lines 1200-1280 for badges/pin/version/device-tag, lines 1280-1295 for .app-main, lines 3325-3375 for media queries)
    - .planning/phases/02-sidebar-navigation/02-RESEARCH.md (Pattern 1: CSS-only static sidebar)
    - .planning/phases/02-sidebar-navigation/02-UI-SPEC.md (Component Inventory section)
  </read_first>
  <action>
    Modify design-system.css with these EXACT changes:

    **1. Root tokens (line 489):**
    - Change `--sidebar-width: 58px` to `--sidebar-width: 200px`
    - Keep `--sidebar-rail: 58px` and `--sidebar-expanded: 252px` as comments only (add `/* legacy — unused */` suffix)

    **2. .app-sidebar base rule (lines 981-994):**
    - Change `width: var(--sidebar-rail)` to `width: 200px`
    - Remove the `transition: width .25s cubic-bezier(.4, 0, .2, 1);` line entirely
    - Keep `overflow: hidden` (needed for label containment)

    **3. DELETE these rule blocks entirely:**
    - Lines 996-1008: `.app-sidebar:hover, .app-sidebar.pinned` width override + `.app-sidebar.pinned` box-shadow + `.app-sidebar.pinned ~ .app-main` padding-left
    - Lines 1010-1022: All `.app-sidebar:hover .nav-label`, `.app-sidebar.pinned .nav-label`, same for .nav-group-label, .nav-group-chevron, .sidebar-version, .sidebar-pin, .device-tag reveal rules
    - Lines 1144-1157: `.nav-group::after` divider line AND the `.app-sidebar:hover .nav-group::after, .app-sidebar.pinned .nav-group::after` hide rule (divider is a rail-mode affordance, irrelevant at 200px)
    - Lines 1207-1217: `.app-sidebar:not(:hover):not(.pinned) .nav-badge[data-count]` rail-mode badge dot rule
    - Lines 1219-1248: Entire `.sidebar-pin` style block (button, :hover, .app-sidebar.pinned .sidebar-pin)
    - Lines 1243-1248: `.app-sidebar.pinned .sidebar-pin` (part of the pin block above)

    **4. .nav-label (lines 1114-1122) — replace with:**
    ```css
    .nav-label {
      opacity: 1;
      max-width: 180px;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      font-size: var(--text-base);
      font-weight: 500;
    }
    ```
    (Remove opacity:0, max-width:0, transition, font-size:14px)

    **5. .nav-group (lines 1125-1143) — update these properties:**
    - Change `height: 20px` to `height: 44px`
    - Change `padding: 6px 10px 2px` to `padding: 0 10px`
    - Keep all other properties unchanged

    **6. .nav-group-label (lines 1160-1171) — replace with:**
    ```css
    .nav-group-label {
      font-size: 12px;
      font-weight: 500;
      letter-spacing: 1.4px;
      text-transform: uppercase;
      color: rgba(255, 255, 255, .5);
      padding-left: 4px;
      opacity: 1;
      max-width: 160px;
      overflow: hidden;
      white-space: nowrap;
      text-overflow: ellipsis;
    }
    ```
    (Remove opacity:0, max-width:0, transition; change font-weight from 700 to 500)

    **7. .nav-group-chevron (lines 1172-1181) — update:**
    - Change `opacity: 0` to `opacity: 1`
    - Remove the `transition: opacity .2s .05s, transform .2s;` line (keep only `transition: transform .2s;` for the collapse rotation)

    **8. .nav-item (lines 1067-1089) — update these properties:**
    - Change `height: 42px` to `height: 44px`
    - Change `gap: 10px` to `gap: var(--gap-sm)` (8px)
    - Change `font-size: 14px` to `font-size: var(--text-base)`

    **9. .sidebar-version (lines 1256-1266) — update:**
    - Change `opacity: 0` to `opacity: 1`
    - Remove `transition: opacity .15s ease;`

    **10. .device-tag (lines 1267-1279) — update:**
    - Change `opacity: 0` to `opacity: 1`
    - Change `max-width: 0` to `max-width: 100px`
    - Remove the `transition: opacity .2s .05s, max-width .25s cubic-bezier(.4, 0, .2, 1);` line

    **11. .app-main (lines 1282-1290) — update:**
    - Change `padding-left: calc(var(--sidebar-rail) + 22px)` to `padding-left: calc(200px + 20px)`

    **12. DO NOT TOUCH the mobile media query block** at lines 3325+ — the `@media (max-width: 768px)` override keeps mobile sidebar behavior (hamburger overlay) intact.

    After all changes, verify syntax: `php -r "echo 'ok';"` is not applicable for CSS — instead, visually scan for unclosed braces by checking the file parses.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "sidebar-width: 200px" public/assets/css/design-system.css && grep -c "height: 44px" public/assets/css/design-system.css | head -1 && grep -c "opacity: 0" public/assets/css/design-system.css | head -1 && ! grep -q "\.app-sidebar:hover" public/assets/css/design-system.css && echo "HOVER_REMOVED_OK" && ! grep -q "\.app-sidebar\.pinned" public/assets/css/design-system.css && echo "PINNED_REMOVED_OK" && grep -q "padding-left: calc(200px + 20px)" public/assets/css/design-system.css && echo "MAIN_PADDING_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `--sidebar-width: 200px` present in :root
    - `.app-sidebar` has `width: 200px` with NO transition on width
    - NO `.app-sidebar:hover` rules remain (except mobile media query `.app-sidebar:hover { width: 260px }` at line ~3362 which is inside @media)
    - NO `.app-sidebar.pinned` rules remain
    - `.nav-label` has `opacity: 1`, `max-width: 180px`, `text-overflow: ellipsis`, `font-size: var(--text-base)`
    - `.nav-group` has `height: 44px`, `padding: 0 10px`
    - `.nav-group-label` has `opacity: 1`, `font-weight: 500` (not 700)
    - `.nav-group-chevron` has `opacity: 1`
    - `.nav-item` has `height: 44px`, `gap: var(--gap-sm)`, `font-size: var(--text-base)`
    - `.sidebar-version` has `opacity: 1`
    - `.device-tag` has `opacity: 1`, `max-width: 100px`
    - `.app-main` has `padding-left: calc(200px + 20px)`
    - Rail-mode badge dot rule deleted
    - `.sidebar-pin` style block deleted
    - `.nav-group::after` divider rule deleted
    - Mobile `@media (max-width: 768px)` block UNCHANGED
  </acceptance_criteria>
  <done>design-system.css has a static 200px sidebar with always-visible labels, 44px touch targets, no hover/pin expansion rules, and static 220px app-main padding</done>
</task>

<task type="auto">
  <name>Task 2: HTML + JS — remove pin button, add Mon compte, move Voter, strip pin logic from shell.js</name>
  <files>public/partials/sidebar.html, public/assets/js/core/shell.js</files>
  <read_first>
    - public/partials/sidebar.html (full file — 142 lines)
    - public/assets/js/core/shell.js (lines 1-65 for pin section, line 203 for SidebarPin export)
    - .planning/phases/02-sidebar-navigation/02-RESEARCH.md (Pattern 2: shell.js, Pattern 3: sidebar.html)
  </read_first>
  <action>
    **sidebar.html changes:**

    1. **DELETE lines 2-5** — the entire pin button block:
    ```html
    <!-- Pin button (visible on hover/pin) -->
    <button class="sidebar-pin" id="sidebarPin" ...>...</button>
    ```

    2. **Add "Voter" nav item in main nav** — insert BEFORE the Guide & FAQ link (line 113), after the Statistiques link (line 94). This places it at the bottom of main nav but before utility links:
    ```html
    <a class="nav-item" href="/vote" data-page="vote" data-requires-role="admin,operator,president,voter">
      <span class="nav-item-icon" aria-hidden="true"><svg class="icon"><use href="/assets/icons.svg#icon-smartphone"></use></svg></span>
      <span class="nav-label">Voter</span>
    </a>
    ```

    3. **Add "Mon compte" nav item** — insert AFTER Guide & FAQ link (the last nav item before </nav>):
    ```html
    <a class="nav-item" href="/account" data-page="account">
      <span class="nav-item-icon" aria-hidden="true"><svg class="icon"><use href="/assets/icons.svg#icon-user"></use></svg></span>
      <span class="nav-label">Mon compte</span>
    </a>
    ```
    No `data-requires-role` attribute — all authenticated users can access their account.

    4. **Remove the old "Apercu votant" from device section** — delete the `<a>` tag at lines 124-128 inside `.sidebar-device-section`. Keep the `.sidebar-device-section` div itself (even if empty) to preserve CSS structure, OR remove the entire div if it becomes empty. Since it only contained one link, remove the entire `.sidebar-device-section` div (lines 122-129).

    **shell.js changes:**

    1. **DELETE** `const PIN_KEY = 'ag-vote-sidebar-pinned';` (line 24)
    2. **DELETE** the entire `togglePin()` function (lines 29-41)
    3. **DELETE** the localStorage restore block (lines 44-50):
    ```javascript
    if (sidebar && localStorage.getItem(PIN_KEY) === '1') {
      sidebar.classList.add('pinned');
      const main = document.querySelector('.app-main');
      if (main) { main.style.paddingLeft = 'calc(var(--sidebar-expanded) + 22px)'; }
    }
    ```
    4. **DELETE** `bindPinButton()` function and its call (lines 53-63 + line 63 call)
    5. **DELETE** `window.SidebarPin = { toggle: togglePin };` (line 203)
    6. **UPDATE the module docblock** (lines 1-15) — remove references to "pin/expand" and "58px->252px":
    Change the description to: `shell.js - Application shell with sidebar, drawer system, and mobile navigation.`
    Remove bullet "Sidebar rail/expand (58px->252px on hover, pin to persist)" and replace with "Sidebar always-open (200px static)"

    **Keep untouched in shell.js:**
    - `updateScrollFade()` and `bindScrollFade()` — scroll indicators
    - `bindNavGroupToggle()` and `restoreNavGroupState()` — section collapse
    - `markActivePage()` — active link highlighting
    - `updateSidebarTop()` — header banner offset
    - MutationObserver for sidebar partial loading
    - All drawer, mobile nav, theme toggle, notification, search sections
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && ! grep -q "sidebarPin" public/partials/sidebar.html && echo "PIN_BUTTON_REMOVED" && grep -q "Mon compte" public/partials/sidebar.html && echo "MON_COMPTE_ADDED" && grep -q 'href="/account"' public/partials/sidebar.html && echo "ACCOUNT_LINK_OK" && grep -q 'href="/vote".*data-page="vote"' public/partials/sidebar.html && echo "VOTE_IN_MAIN_NAV" && ! grep -q "sidebar-device-section" public/partials/sidebar.html && echo "DEVICE_SECTION_REMOVED" && ! grep -q "PIN_KEY" public/assets/js/core/shell.js && echo "PIN_KEY_REMOVED" && ! grep -q "togglePin" public/assets/js/core/shell.js && echo "TOGGLE_PIN_REMOVED" && ! grep -q "SidebarPin" public/assets/js/core/shell.js && echo "SIDEBAR_PIN_EXPORT_REMOVED"</automated>
  </verify>
  <acceptance_criteria>
    - sidebar.html: No `<button class="sidebar-pin"` or `id="sidebarPin"` element
    - sidebar.html: `<a class="nav-item" href="/account" data-page="account">` with `<span class="nav-label">Mon compte</span>` exists inside `<nav>`
    - sidebar.html: `<a class="nav-item" href="/vote" data-page="vote" data-requires-role="admin,operator,president,voter">` with `<span class="nav-label">Voter</span>` exists inside `<nav>` (not in .sidebar-device-section)
    - sidebar.html: No `.sidebar-device-section` div remains (or it is empty)
    - shell.js: No `PIN_KEY` constant
    - shell.js: No `togglePin` function
    - shell.js: No `bindPinButton` function
    - shell.js: No `window.SidebarPin` export
    - shell.js: No `localStorage.getItem(PIN_KEY)` or `localStorage.setItem(PIN_KEY`
    - shell.js: `updateScrollFade`, `bindNavGroupToggle`, `markActivePage`, `updateSidebarTop` still present
  </acceptance_criteria>
  <done>Pin button removed from HTML, Mon compte + Voter added to main nav, device section removed, shell.js pin mechanism fully stripped while keeping scroll fade, nav group toggle, and active page marking</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "\.app-sidebar:hover" public/assets/css/design-system.css` should return 1 (only the mobile media query line)
2. `grep -c "\.app-sidebar\.pinned" public/assets/css/design-system.css` should return 0
3. `grep "sidebar-width" public/assets/css/design-system.css` should show `--sidebar-width: 200px`
4. `grep "Mon compte" public/partials/sidebar.html` should match
5. `grep "togglePin\|PIN_KEY\|SidebarPin" public/assets/js/core/shell.js` should return no matches
6. `grep "updateScrollFade\|bindNavGroupToggle\|markActivePage" public/assets/js/core/shell.js` should return matches (kept)
</verification>

<success_criteria>
- Sidebar renders at 200px with all labels visible (no hover needed)
- No pin button in DOM, no pin logic in JS, no pin CSS rules
- .app-main has static 220px left padding
- Nav items are 44px tall, nav groups are 44px tall
- "Voter" and "Mon compte" appear in main nav for voter role
- Mobile hamburger behavior unaffected (media query untouched)
- Labels truncate with ellipsis at container width
</success_criteria>

<output>
After completion, create `.planning/phases/02-sidebar-navigation/02-01-SUMMARY.md`
</output>
