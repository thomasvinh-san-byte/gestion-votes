---
phase: 55-coverage-target-tooling
plan: 05
type: execute
wave: 2
depends_on: ["55-03"]
files_modified:
  - tests/Unit/OperatorControllerTest.php
  - tests/Unit/AdminControllerTest.php
  - tests/Unit/AnalyticsControllerTest.php
  - tests/Unit/BallotsControllerTest.php
  - tests/Unit/AuditControllerTest.php
autonomous: true
gap_closure: true
requirements: [COV-02]

must_haves:
  truths:
    - "5 more controllers (Operator, Admin, Analytics, Ballots, Audit) have 90%+ coverage"
    - "Tests use real handle() dispatch with mocked repos, not source-level verification only"
  artifacts:
    - path: "tests/Unit/OperatorControllerTest.php"
      provides: "Execution-based tests for operator console endpoints"
    - path: "tests/Unit/AdminControllerTest.php"
      provides: "Execution-based tests for admin panel endpoints"
    - path: "tests/Unit/AnalyticsControllerTest.php"
      provides: "Execution-based tests for analytics endpoints"
    - path: "tests/Unit/BallotsControllerTest.php"
      provides: "Execution-based tests for ballot/vote endpoints"
    - path: "tests/Unit/AuditControllerTest.php"
      provides: "Execution-based tests for audit log endpoints"
  key_links:
    - from: "tests/Unit/OperatorControllerTest.php"
      to: "tests/Unit/ControllerTestCase.php"
      via: "extends ControllerTestCase"
      pattern: "extends ControllerTestCase"
---

<objective>
Rewrite 5 more large controller tests (Operator 516 lines, Admin 475, Analytics 424, Ballots 377, Audit 355 = 2147 lines) to use ControllerTestCase with RepositoryFactory injection, targeting 90%+ coverage.

Purpose: Combined with Plan 04's 5 controllers, this covers the 10 largest controllers (~70% of all controller code).
Output: 5 rewritten controller test files
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-coverage-target-tooling/55-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite OperatorController + AdminController + AnalyticsController tests</name>
  <files>tests/Unit/OperatorControllerTest.php, tests/Unit/AdminControllerTest.php, tests/Unit/AnalyticsControllerTest.php</files>
  <action>
    Rewrite all three to extend ControllerTestCase and achieve 90%+ coverage.

    read_first:
    - tests/Unit/ControllerTestCase.php
    - app/Controller/OperatorController.php (516 lines)
    - app/Controller/AdminController.php (475 lines)
    - app/Controller/AnalyticsController.php (424 lines)
    - tests/Unit/OperatorControllerTest.php (existing 1233 lines)
    - tests/Unit/AdminControllerTest.php (existing 827 lines)
    - tests/Unit/AnalyticsControllerTest.php (existing 676 lines)

    **OperatorController (516 lines, repos: attendance, ballot, meeting, meetingStats, member, motion, policy, proxy, voteToken):**
    - Operator console is the live meeting control panel
    - Endpoints: getDashboard, getMotionStatus, startVote, endVote, getVoteResults, getAttendanceSnapshot
    - Uses EventBroadcaster for SSE — check if it's a static call or instance. If static, verify test env handles it (likely no-op or needs stub)
    - Test: each endpoint with mocked repos returning realistic meeting/motion/ballot data
    - Key paths: active meeting found/not found, vote in progress, vote completed, quorum met/not met

    **AdminController (475 lines, repos: auditEvent, meeting, meetingStats, member, motion, system, user, voteToken):**
    - Admin panel with system stats, user management, audit view
    - Endpoints: getStats, listUsers, createUser, updateUser, deleteUser, getAuditLog, getSystemInfo
    - Test: role enforcement (admin only), CRUD operations, stats aggregation
    - api_require_role('admin') is stubbed as no-op in test env — but verify the source code calls it

    **AnalyticsController (424 lines, repos: aggregateReport, analytics, member):**
    - Analytics dashboard with charts/reports
    - Endpoints: getDashboard, getVoteAnalytics, getMemberAnalytics, getExportData
    - Uses ExportService for some paths — if constructed internally, handle the exception
    - Test: each endpoint with mocked analytics repo returning summary data

    Pattern for each controller:
    1. Change `extends TestCase` to `extends ControllerTestCase`
    2. Remove setUp/tearDown that duplicates base class
    3. Keep structural reflection tests
    4. Add for EACH public method:
       - Method enforcement test (wrong HTTP method -> 405)
       - Happy path test (all mocks return valid data -> 200)
       - Not-found path test (repo returns null/empty -> 404)
       - Validation error test (missing/invalid params -> 400/422)

    acceptance_criteria:
    - `php vendor/bin/phpunit --filter OperatorControllerTest` exits 0
    - `php vendor/bin/phpunit --filter AdminControllerTest` exits 0
    - `php vendor/bin/phpunit --filter AnalyticsControllerTest` exits 0
  </action>
  <verify>
    <automated>php vendor/bin/phpunit --filter "OperatorControllerTest|AdminControllerTest|AnalyticsControllerTest" --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>All three controller tests pass with execution-based tests. Each controller's public methods are exercised through handle() with mocked repos.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite BallotsController + AuditController tests</name>
  <files>tests/Unit/BallotsControllerTest.php, tests/Unit/AuditControllerTest.php</files>
  <action>
    Rewrite both to extend ControllerTestCase with 90%+ coverage.

    read_first:
    - tests/Unit/ControllerTestCase.php
    - app/Controller/BallotsController.php (377 lines)
    - app/Controller/AuditController.php (355 lines)
    - tests/Unit/BallotsControllerTest.php (existing 2009 lines)
    - tests/Unit/AuditControllerTest.php (existing 636 lines)

    **BallotsController (377 lines):**
    - Endpoints: castVote, getResults, getBallotStatus, listBallots
    - Uses VoteTokenService and EventBroadcaster
    - Test: vote casting with valid/invalid tokens, results aggregation, ballot status checks
    - Key: castVote likely validates token, checks vote is open, records ballot, broadcasts update

    **AuditController (355 lines):**
    - Endpoints: listEvents, getEventDetails, exportAudit, getFilters
    - Uses auditEvent repo, possibly analytics repo
    - Test: list with pagination, filter by type/date, export format, empty results

    Same rewrite pattern: extend ControllerTestCase, mock repos, test all endpoints.

    acceptance_criteria:
    - `php vendor/bin/phpunit --filter BallotsControllerTest` exits 0
    - `php vendor/bin/phpunit --filter AuditControllerTest` exits 0
  </action>
  <verify>
    <automated>php vendor/bin/phpunit --filter "BallotsControllerTest|AuditControllerTest" --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>Both controller tests pass with execution-based tests covering all public endpoints via mocked repos.</done>
</task>

</tasks>

<verification>
1. All 5 tests pass: `php vendor/bin/phpunit --filter "OperatorControllerTest|AdminControllerTest|AnalyticsControllerTest|BallotsControllerTest|AuditControllerTest" --no-coverage`
2. No regressions: `php vendor/bin/phpunit --testsuite Unit --no-coverage`
</verification>

<success_criteria>
- All 5 controller tests extend ControllerTestCase and pass
- Tests execute real controller methods, not just source-level verification
- Each controller targets 90%+ individual line coverage
</success_criteria>

<output>
After completion, create `.planning/phases/55-coverage-target-tooling/55-05-SUMMARY.md`
</output>
