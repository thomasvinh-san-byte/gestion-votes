---
phase: 50-secondary-pages-part-2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/users.htmx.html
  - public/assets/css/users.css
  - public/assets/js/pages/users.js
autonomous: true
requirements: [REB-07, WIRE-01, WIRE-02]

must_haves:
  truths:
    - "Users table loads with rows showing name, email, role, status"
    - "Role filter tabs filter users by role (admin, operator, auditor, viewer)"
    - "Role count badges show per-role user counts"
    - "Admin can create a new user via modal form (name, email, password, role)"
    - "Admin can edit an existing user via same modal (pre-filled fields)"
    - "Admin can deactivate/delete a user — changes reflect immediately"
    - "Search filters users by name/email"
    - "Pagination navigates through user list"
    - "Password strength indicator shows in create/edit modal"
    - "No JS console errors — all DOM IDs preserved for users.js"
  artifacts:
    - path: "public/users.htmx.html"
      provides: "Complete users page with table, role filter, create/edit modal"
      contains: "usersTableBody"
    - path: "public/assets/css/users.css"
      provides: "Users styles — table, modal, role badges, filters, responsive"
      contains: "users-table"
    - path: "public/assets/js/pages/users.js"
      provides: "Users JS — CRUD, filter, search, pagination, modal"
      contains: "admin_users"
  key_links:
    - from: "public/users.htmx.html"
      to: "public/assets/js/pages/users.js"
      via: "DOM IDs preserved for JS binding"
      pattern: "usersTableBody|usersPagination|usersCount|searchUser|btnAddUser|userModal|modalUserName|modalUserEmail|modalUserPassword|modalUserRole|modalUserId|btnSaveUser|btnCancelUser|roleCountAdmin|roleCountOperator|roleCountAuditor|roleCountViewer|roleFilter|btnRefresh|passwordStrength|modalPasswordGroup"
    - from: "public/assets/js/pages/users.js"
      to: "/api/v1/admin_users.php"
      via: "fetch calls for CRUD"
      pattern: "api.*admin_users"
---

<objective>
Ground-up rebuild of the users management page — complete HTML+CSS rewrite from scratch with v4.3 design language. Preserve all DOM IDs that users.js targets. Verify CRUD operations, role filtering, and pagination all wire correctly.

Purpose: Users page is the admin's tool for managing system access — must support create/edit/deactivate with role assignment and real-time filtering.
Output: New users.htmx.html + users.css, with users.js verified working.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-secondary-pages-part-2/50-CONTEXT.md

@public/users.htmx.html
@public/assets/css/users.css
@public/assets/js/pages/users.js
@public/assets/css/app.css
@public/dashboard.htmx.html

<interfaces>
<!-- Critical DOM IDs that users.js targets — ALL must exist in new HTML -->

Role counts:
- #roleCountAdmin — admin user count badge
- #roleCountOperator — operator user count badge
- #roleCountAuditor — auditor user count badge
- #roleCountViewer — viewer user count badge

Toolbar:
- #searchUser — search input
- #btnAddUser — add user button
- #btnRefresh — refresh button
- #roleFilter — role filter container with .filter-tab buttons
- #roleFilter .filter-tab — individual role filter tabs (JS queries active one)

Table:
- #usersTableBody — table body for user rows
- #usersCount — users count display
- #usersPagination — pagination container

User modal (create/edit):
- #userModal — modal container
- #modalUserId — hidden user ID field (empty = create, value = edit)
- #modalUserName — name input
- #modalUserEmail — email input
- #modalUserPassword — password input
- #modalUserRole — role select dropdown
- #modalPasswordGroup — password field group (hidden on edit if not changing)
- #passwordStrength — password strength indicator
- #btnSaveUser — save button
- #btnCancelUser — cancel button

Notification:
- #notif_box — notification container
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild users HTML+CSS from scratch</name>
  <files>public/users.htmx.html, public/assets/css/users.css</files>
  <read_first>
    - public/users.htmx.html (current HTML — extract all DOM IDs and structure)
    - public/assets/js/pages/users.js (JS — understand ALL getElementById/querySelector calls)
    - public/assets/css/users.css (current CSS — understand structure)
    - public/assets/css/app.css (design system tokens and utility classes)
    - public/dashboard.htmx.html (reference for v4.3 app-shell, card layout)
  </read_first>
  <action>
    Rewrite users.htmx.html from scratch with v4.3 design language:

    1. **HTML structure**: app-shell layout (sidebar, header with gradient accent bar using admin/users persona color, main). Same head pattern as dashboard.

    2. **Page title**: v4.3 pattern — .page-title with gradient bar, users icon, title "Utilisateurs", breadcrumb.

    3. **Role count badges**: Horizontal bar with 4 role badges — #roleCountAdmin, #roleCountOperator, #roleCountAuditor, #roleCountViewer. Each badge: icon + role label + count. Use shadow-sm mini-cards.

    4. **Toolbar**: #searchUser input, #btnAddUser button (primary), #btnRefresh button (secondary). #roleFilter container with .filter-tab buttons: Tous, Admin, Operateur, Auditeur, Observateur.

    5. **Users table**: Card wrapper with overflow-x-auto. Table with thead (columns: Nom, Email, Role, Statut, Actions) and #usersTableBody. JS renders rows. #usersCount display. #usersPagination below table.

    6. **User modal**: #userModal overlay. Card with header (title "Nouvel utilisateur" / "Modifier utilisateur"), body with form fields: #modalUserId (hidden), #modalUserName (text), #modalUserEmail (email), #modalPasswordGroup containing #modalUserPassword + #passwordStrength, #modalUserRole (select: admin, operator, auditor, viewer). Footer: #btnCancelUser, #btnSaveUser.

    7. **Notification**: #notif_box container for toast notifications.

    8. **Scripts**: htmx, utils, shared, shell, users.js.

    Rewrite users.css from scratch:
    - All values via design tokens
    - Role badges: inline-flex row, colored left border per role
    - Filter tabs: horizontal tab bar with active state
    - Table: card-wrapped, sticky header, hover rows, role badge colors
    - Modal: centered with backdrop, form fields with floating labels or standard labels
    - Password strength: colored bar indicator (red/yellow/green)
    - Pagination: centered nav
    - Responsive: table scrolls horizontally below 768px, modal goes full-width on mobile
    - No hardcoded hex colors
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c 'usersTableBody\|usersPagination\|usersCount\|searchUser\|btnAddUser\|userModal\|modalUserName\|modalUserEmail\|modalUserPassword\|modalUserRole\|modalUserId\|btnSaveUser\|btnCancelUser\|roleCountAdmin\|roleCountOperator\|roleCountAuditor\|roleCountViewer\|roleFilter\|btnRefresh\|passwordStrength\|modalPasswordGroup\|notif_box' public/users.htmx.html && grep -c 'var(--' public/assets/css/users.css</automated>
  </verify>
  <acceptance_criteria>
    - grep 'id="usersTableBody"' public/users.htmx.html returns match
    - grep 'id="usersPagination"' public/users.htmx.html returns match
    - grep 'id="usersCount"' public/users.htmx.html returns match
    - grep 'id="searchUser"' public/users.htmx.html returns match
    - grep 'id="btnAddUser"' public/users.htmx.html returns match
    - grep 'id="btnRefresh"' public/users.htmx.html returns match
    - grep 'id="userModal"' public/users.htmx.html returns match
    - grep 'id="modalUserId"' public/users.htmx.html returns match
    - grep 'id="modalUserName"' public/users.htmx.html returns match
    - grep 'id="modalUserEmail"' public/users.htmx.html returns match
    - grep 'id="modalUserPassword"' public/users.htmx.html returns match
    - grep 'id="modalUserRole"' public/users.htmx.html returns match
    - grep 'id="modalPasswordGroup"' public/users.htmx.html returns match
    - grep 'id="passwordStrength"' public/users.htmx.html returns match
    - grep 'id="btnSaveUser"' public/users.htmx.html returns match
    - grep 'id="btnCancelUser"' public/users.htmx.html returns match
    - grep 'id="roleCountAdmin"' public/users.htmx.html returns match
    - grep 'id="roleCountOperator"' public/users.htmx.html returns match
    - grep 'id="roleCountAuditor"' public/users.htmx.html returns match
    - grep 'id="roleCountViewer"' public/users.htmx.html returns match
    - grep 'id="roleFilter"' public/users.htmx.html returns match
    - grep 'id="notif_box"' public/users.htmx.html returns match
    - grep 'filter-tab' public/users.htmx.html returns match
    - grep 'var(--' public/assets/css/users.css returns 15+ matches
    - grep 'users.js' public/users.htmx.html returns match
  </acceptance_criteria>
  <done>Users HTML completely rewritten with v4.3 design language. Table, role filters, create/edit modal all present with correct IDs. CSS uses tokens exclusively.</done>
</task>

<task type="auto">
  <name>Task 2: Verify users JS wiring and fix broken selectors</name>
  <files>public/assets/js/pages/users.js</files>
  <read_first>
    - public/users.htmx.html (the NEW rebuilt HTML from Task 1)
    - public/assets/js/pages/users.js (current JS — check all selectors against new HTML)
  </read_first>
  <action>
    After the HTML rebuild, verify every getElementById/querySelector call in users.js matches the new HTML:

    1. Cross-reference all 19 getElementById calls against new HTML: btnAddUser, btnCancelUser, btnRefresh, btnSaveUser, modalPasswordGroup, modalUserEmail, modalUserId, modalUserName, modalUserPassword, modalUserRole, notif_box, passwordStrength, roleCountAdmin, roleCountAuditor, roleCountOperator, roleCountViewer, searchUser, userModal, usersCount, usersPagination, usersTableBody.

    2. Cross-reference class-based selectors: #roleFilter .filter-tab, querySelector('label'), #roleFilter .filter-tab.active.

    3. If any selectors are broken, update users.js to match.

    4. Verify API endpoint: /api/v1/admin_users.php exists and supports GET (list), POST (create), PUT (update), DELETE (deactivate).

    5. Verify modal open/close logic: clicking #btnAddUser opens modal with empty fields (create mode), clicking edit on a row opens modal with pre-filled fields (edit mode, #modalUserId set).

    6. Verify password strength indicator updates on keyup in #modalUserPassword.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && node -e "const fs=require('fs');const html=fs.readFileSync('public/users.htmx.html','utf8');const js=fs.readFileSync('public/assets/js/pages/users.js','utf8');const ids=[...js.matchAll(/getElementById\(['\"]([^'\"]+)['\"]\)/g)].map(m=>m[1]);const missing=ids.filter(id=>!html.includes('id=\"'+id+'\"'));if(missing.length){console.error('MISSING IDs:',missing);process.exit(1)}else{console.log('All',ids.length,'IDs found in HTML')}"</automated>
  </verify>
  <acceptance_criteria>
    - The node verification script exits 0 (all getElementById targets exist in HTML)
    - grep 'admin_users' public/assets/js/pages/users.js returns match (CRUD endpoint)
    - No syntax errors: node -c public/assets/js/pages/users.js exits 0
    - grep 'passwordStrength\|password.*strength' public/assets/js/pages/users.js returns match
  </acceptance_criteria>
  <done>All users.js selectors match rebuilt HTML. No broken DOM references. CRUD, role filtering, pagination, modal all wired. API endpoint verified.</done>
</task>

</tasks>

<verification>
1. Open users page in browser — role count badges show correct numbers
2. Users table loads with real data
3. Role filter tabs filter the table
4. Search filters by name/email
5. Click "Add user" opens modal with empty fields
6. Click edit on row opens modal with pre-filled data
7. Password strength indicator works
8. Pagination navigates
9. No JS console errors on page load
10. Dark mode toggle works
</verification>

<success_criteria>
- Users page fully rebuilt with v4.3 design language
- Table with CRUD operations functional
- Role filter tabs work
- Create/edit modal with password strength
- Pagination works
- All DOM IDs preserved — users.js binds without errors
- Dark mode works via tokens
- No JS console errors
</success_criteria>

<output>
After completion, create `.planning/phases/50-secondary-pages-part-2/50-03-SUMMARY.md`
</output>
