---
phase: 20-live-vote-flow
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - tests/Unit/OperatorControllerTest.php
  - tests/Unit/MotionsControllerTest.php
autonomous: false
requirements: [VOT-01, VOT-02, VOT-03, VOT-04]

must_haves:
  truths:
    - "Operator opens a motion and voter view shows the motion as active via SSE"
    - "Voter submits a ballot and receives confirmation; duplicate rejected"
    - "Operator closes a motion and results are displayed in proclamation modal"
    - "Meeting state machine transitions draft→scheduled→frozen→live→closed→validated execute without errors"
    - "Verdict override changes the stored decision with audit trail"
  artifacts:
    - path: "tests/Unit/OperatorControllerTest.php"
      provides: "Tests for openVote broadcast and state transitions"
    - path: "tests/Unit/MotionsControllerTest.php"
      provides: "Tests for overrideDecision endpoint validation"
  key_links:
    - from: "tests/Unit/OperatorControllerTest.php"
      to: "app/Controller/OperatorController.php"
      via: "PHPUnit test assertions"
      pattern: "openVote.*meetingStatusChanged"
    - from: "tests/Unit/MotionsControllerTest.php"
      to: "app/Controller/MotionsController.php"
      via: "PHPUnit test assertions"
      pattern: "overrideDecision"
---

<objective>
Verify the complete vote flow works end-to-end: add targeted unit tests for the new backend behavior (broadcast on frozen-to-live, verdict override validation), then human-verify the full cycle visually.

Purpose: Ensures all four VOT requirements are provably met before moving to Phase 21.

Output: New test cases for Plan 01 backend changes + human verification of the complete vote cycle.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-live-vote-flow/20-CONTEXT.md
@.planning/phases/20-live-vote-flow/20-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit tests for openVote broadcast and verdict override</name>
  <files>tests/Unit/OperatorControllerTest.php, tests/Unit/MotionsControllerTest.php</files>
  <behavior>
    - Test 1 (OperatorControllerTest): openVote when meeting is frozen transitions to live and test confirms the broadcast path is reachable (verify EventBroadcaster was invoked or status changed)
    - Test 2 (MotionsControllerTest): overrideDecision with valid params (closed motion, decision=adopted, justification) returns ok and updates decision
    - Test 3 (MotionsControllerTest): overrideDecision with missing justification returns 422
    - Test 4 (MotionsControllerTest): overrideDecision with invalid decision value returns 422
    - Test 5 (MotionsControllerTest): overrideDecision on a motion that is not yet closed returns error
  </behavior>
  <action>
    Read existing test files to understand the testing patterns used (mock setup, api call helpers, assertion style). Follow the exact same patterns.

    **OperatorControllerTest.php**: Add a test method that:
    1. Creates a meeting with status 'frozen' and at least one motion
    2. Calls the openVote endpoint
    3. Asserts the response is ok
    4. Verifies the meeting status is now 'live' in the database (or via the response data)

    **MotionsControllerTest.php**: Add test methods that:
    1. Create a meeting and motion, close the motion first (via motions_close endpoint)
    2. Call overrideDecision with decision='adopted' and justification='President tie-break'
    3. Assert response is ok and decision field is updated
    4. Test validation: missing justification returns 422, invalid decision returns 422, unclosed motion returns error

    Follow the project's test patterns exactly — use the same base class, same helper methods for creating test data, same assertion patterns.
  </action>
  <verify>
    <automated>./vendor/bin/phpunit tests/Unit/OperatorControllerTest.php tests/Unit/MotionsControllerTest.php -x</automated>
  </verify>
  <done>All new test cases pass. openVote frozen-to-live transition verified. overrideDecision endpoint validated for happy path and error cases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify complete live vote cycle</name>
  <files>n/a</files>
  <action>
    Present the verification checklist to the user for manual testing of the complete vote flow across operator console, voter view, and projection screen.
  </action>
  <verify>User confirms all 12 verification steps pass</verify>
  <done>User types "approved" confirming the full vote cycle works end-to-end</done>
  <what-built>Complete live vote flow: operator opens motion (with frozen-to-live auto-transition), voters cast ballots with confirmation modal, operator closes motion and sees results with verdict override option, projection shows participation during vote and full results after close, meeting state machine transitions work E2E</what-built>
  <how-to-verify>
    1. Open the operator console and select a meeting in 'frozen' status
    2. Click "Ouvrir le vote" on a motion — verify the confirmation modal says "Cela demarrera la seance. Continuer?"
    3. Confirm — verify the meeting transitions to 'live' and the vote opens
    4. Open the voter view (vote.htmx.html) in another tab — verify the active motion appears automatically
    5. Cast a ballot (Pour/Contre/Abstention/NSP) — verify confirmation overlay appears before submission
    6. Confirm the vote — verify "Vote enregistre" toast and buttons become disabled
    7. Try to vote again on the same motion — verify duplicate is rejected
    8. Back in operator console exec view — verify you see "X/Y votes recus" but NOT the Pour/Contre breakdown
    9. Open the projection screen (public.htmx.html) — verify it shows "Vote en cours" with participation bar, NOT the vote breakdown
    10. Close the vote in operator console — verify:
        a. Proclamation modal shows full results (Pour/Contre/Abstention counts + Adopte/Rejete verdict)
        b. Projection screen auto-reveals the full results with bar chart
        c. Voter view shows "En attente d'une resolution" (or next motion if available)
    11. In the proclamation modal, click "Modifier la decision" — verify you can override the verdict with justification
    12. Verify the override updates the displayed decision
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `./vendor/bin/phpunit tests/Unit/OperatorControllerTest.php tests/Unit/MotionsControllerTest.php -x` — all tests pass
2. Full unit suite: `./vendor/bin/phpunit --testsuite Unit --stop-on-failure`
3. Human verification of complete vote cycle
</verification>

<success_criteria>
- All new unit tests pass (openVote broadcast, overrideDecision happy path + validation)
- Full unit test suite green (no regressions)
- Human confirms: operator can open/close motions, voter can cast ballot with confirmation, projection shows correct state, verdict override works
</success_criteria>

<output>
After completion, create `.planning/phases/20-live-vote-flow/20-02-SUMMARY.md`
</output>
