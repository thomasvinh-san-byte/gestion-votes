---
phase: 20-live-vote-flow
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified: []
autonomous: false
requirements: [VOT-01, VOT-02, VOT-03, VOT-04]

must_haves:
  truths:
    - "Operator opens a motion and voter view shows the motion as active via SSE"
    - "Voter submits a ballot and receives confirmation; duplicate rejected"
    - "Operator closes a motion and results are displayed in proclamation modal"
    - "Meeting state machine transitions draft→scheduled→frozen→live→closed→validated execute without errors"
    - "Verdict override changes the stored decision with audit trail"
    - "Between votes, projection shows meeting info and agenda position"
  artifacts: []
  key_links: []
---

<objective>
Human-verify the complete vote flow works end-to-end across all views: operator console, voter view, and projection screen.

Purpose: Ensures all four VOT requirements are provably met before moving to Phase 21. Test files were already created and validated in Plan 01 Task 1 (TDD).

Output: Human verification of the complete vote cycle.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-live-vote-flow/20-CONTEXT.md
@.planning/phases/20-live-vote-flow/20-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite to confirm no regressions</name>
  <files></files>
  <action>
    Run the full PHPUnit unit test suite to confirm all tests pass after Plan 01 changes. This validates both the new tests created in Plan 01 Task 1 and all existing tests.
  </action>
  <verify>
    <automated>./vendor/bin/phpunit --testsuite Unit --stop-on-failure</automated>
  </verify>
  <done>Full unit test suite passes with zero failures including new OperatorControllerTest and MotionsControllerTest.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify complete live vote cycle</name>
  <files></files>
  <action>
    Present the verification checklist to the user for manual testing of the complete vote flow across operator console, voter view, and projection screen.
  </action>
  <verify>User confirms all 13 verification steps pass</verify>
  <done>User types "approved" confirming the full vote cycle works end-to-end</done>
  <what-built>Complete live vote flow: operator opens motion (with frozen-to-live auto-transition), voters cast ballots with confirmation modal, operator closes motion and sees results with verdict override option, projection shows participation during vote and full results after close, between-vote projection shows meeting info and agenda position, meeting state machine transitions work E2E</what-built>
  <how-to-verify>
    1. Open the operator console and select a meeting in 'frozen' status
    2. Click "Ouvrir le vote" on a motion — verify the confirmation modal says "Cela demarrera la seance. Continuer?"
    3. Confirm — verify the meeting transitions to 'live' and the vote opens
    4. Open the voter view (vote.htmx.html) in another tab — verify the active motion appears automatically (SSE auto-switch)
    5. Cast a ballot (Pour/Contre/Abstention/NSP) — verify confirmation overlay appears with choice summary before submission
    6. Confirm the vote — verify "Vote enregistre" toast, buttons become disabled, receipt displayed
    7. Try to vote again on the same motion — verify duplicate is rejected
    8. Back in operator console exec view — verify you see "X/Y votes recus" but NOT the Pour/Contre breakdown
    9. Open the projection screen (public.htmx.html) — verify it shows "Vote en cours" with participation bar, NOT the vote breakdown
    10. Close the vote in operator console — verify:
        a. Proclamation modal shows full results (Pour/Contre/Abstention counts + Adopte/Rejete verdict)
        b. Projection screen auto-reveals the full results with bar chart
        c. Voter view shows "En attente d'une resolution" (or next motion if available)
    11. In the proclamation modal, click "Modifier la decision" — verify you can override the verdict with justification
    12. Verify the override updates the displayed decision
    13. After closing the proclamation modal, verify the projection shows meeting info and agenda position (e.g., "Resolution 3/7") in the between-vote state
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `./vendor/bin/phpunit --testsuite Unit --stop-on-failure` — all tests pass
2. Human verification of complete vote cycle (13 steps)
</verification>

<success_criteria>
- Full unit test suite green (no regressions)
- Human confirms: operator can open/close motions with frozen-to-live auto-transition, voter can cast ballot with confirmation, projection shows correct states (participation during vote, results after close, meeting info between votes), verdict override works
</success_criteria>

<output>
After completion, create `.planning/phases/20-live-vote-flow/20-02-SUMMARY.md`
</output>
