---
phase: 05-shared-components
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-02, 05-03]
files_modified: []
autonomous: false
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, COMP-06, COMP-07, COMP-08, COMP-09]

must_haves:
  truths:
    - "All 9 shared components render correctly in light theme matching wireframe v3.19.2"
    - "All 9 shared components render correctly in dark theme with no visual artifacts"
    - "No hardcoded hex colors remain outside of CSS var() fallbacks across all modified files"
  artifacts: []
  key_links: []
---

<objective>
Run automated validation across all modified components and verify dark/light theme rendering.

Purpose: Cross-cutting verification ensures all three parallel plans produced consistent, token-aligned results and nothing was missed. The user decision mandates dark theme verification for every component.

Output: Verified component library ready for consumption by Phase 6+ pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-shared-components/05-CONTEXT.md
@.planning/phases/05-shared-components/05-01-SUMMARY.md
@.planning/phases/05-shared-components/05-02-SUMMARY.md
@.planning/phases/05-shared-components/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated token validation across all components</name>
  <files></files>
  <action>
Run comprehensive validation checks:

1. **Hardcoded hex scan** — Scan all modified component JS files for standalone hardcoded hex colors (not inside var() fallbacks):
```bash
for f in public/assets/js/components/ag-modal.js public/assets/js/components/ag-confirm.js public/assets/js/components/ag-toast.js public/assets/js/components/ag-badge.js public/assets/js/components/ag-mini-bar.js public/assets/js/components/ag-popover.js; do
  echo "=== $f ==="
  grep -n '#[0-9a-fA-F]\{3,6\}' "$f" | grep -v 'var(--\|icons.svg\|//'
done
```
Any matches (outside var() fallbacks) are bugs — fix them by wrapping in a CSS custom property.

2. **Deprecated token scan** — Check that no removed tokens are used:
```bash
grep -rn "tag-bg\|tag-text\|radius-md\b\|color-text-secondary" public/assets/js/components/ag-{modal,confirm,toast,badge,mini-bar,popover}.js public/assets/css/design-system.css | grep -v '//'
```
Any matches are bugs — replace with canonical Phase 4 tokens.

3. **Component registration check** — Verify all components still register:
```bash
grep "customElements.define" public/assets/js/components/ag-{modal,confirm,toast,badge,mini-bar,popover}.js
```

4. **E2E regression** — Run the full interaction test suite:
```bash
npx playwright test tests/e2e/specs/ux-interactions.spec.js --reporter=list
```

5. **Quick validation from VALIDATION.md** — Run all grep checks:
```bash
grep "color-surface-raised\|color-backdrop" public/assets/js/components/ag-modal.js
grep "variant.*danger\|variant.*warn\|variant.*info" public/assets/js/components/ag-confirm.js
grep "auto-dismiss\|5000\|8000" public/assets/js/components/ag-toast.js
grep "color-danger-subtle\|color-success-subtle" public/assets/js/components/ag-badge.js
grep "progress-bar" public/assets/css/design-system.css | head -3
grep "color-surface-raised\|shadow" public/assets/js/components/ag-popover.js
grep "session-expiry-warning" public/assets/css/design-system.css | head -3
grep "tour.*color-surface-raised" public/assets/css/design-system.css | head -3
```

If any validation fails, fix the issue in the relevant file before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && echo "=== Hex scan ===" && grep -rn '#[0-9a-fA-F]\{3,6\}' public/assets/js/components/ag-{modal,confirm,toast,badge,mini-bar,popover}.js | grep -v 'var(--\|icons.svg\|//' | wc -l && echo "=== Deprecated tokens ===" && grep -rn 'tag-bg\|tag-text\|radius-md\b\|color-text-secondary' public/assets/js/components/ag-{modal,confirm,toast,badge,mini-bar,popover}.js | wc -l && echo "=== Registration ===" && grep -c "customElements.define" public/assets/js/components/ag-{modal,confirm,toast,badge,mini-bar,popover}.js</automated>
  </verify>
  <done>Zero standalone hardcoded hex colors. Zero deprecated token references. All 6 components still register with customElements.define. E2E tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all shared components in light and dark themes</name>
  <files></files>
  <action>
Present the user with verification instructions for all 9 components in both themes. This is a human-verify checkpoint — the user must confirm visual correctness.
  </action>
  <verify>User approves visual rendering in both themes</verify>
  <done>User confirms all 9 components match wireframe v3.19.2 in both light and dark themes</done>
</task>

</tasks>

<verification>
All automated checks pass in Task 1. Human visual verification confirms wireframe alignment.
</verification>

<success_criteria>
- Zero standalone hardcoded hex colors in component files
- Zero deprecated token references
- All component registrations intact
- E2E tests pass
- User confirms visual match with wireframe in both light and dark themes
</success_criteria>

<output>
After completion, create `.planning/phases/05-shared-components/05-04-SUMMARY.md`
</output>
