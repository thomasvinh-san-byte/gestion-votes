---
phase: 03-layouts-secondaires
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - public/login.html
  - public/assets/css/login.css
autonomous: true
requirements:
  - LOGIN-01
  - LOGIN-02
user_setup: []

must_haves:
  truths:
    - "Le pattern de fond login-brand-grid est supprimé du HTML et du CSS"
    - "L'orbe animé login-orb (radial-gradient animé) est supprimé du CSS"
    - "Le login-brand-glow (gradient atténué single-layer) est conservé — différent d'un pattern"
    - "Le panneau brand expose 1 seul bénéfice : 'Vote sécurisé' (le 2e <li> de login-brand-features)"
    - "Les 2 autres <li> de login-brand-features sont supprimés"
    - "Strings françaises préservées sur Vote sécurisé et tagline"
  artifacts:
    - path: "public/login.html"
      provides: "Markup login simplifié — 0 grid pattern, 1 seule feature"
      contains: "Vote sécurisé"
    - path: "public/assets/css/login.css"
      provides: "CSS login nettoyé — 0 selector .login-orb, 0 selector .login-brand-grid"
      contains: "login-brand-glow"
  key_links:
    - from: "public/login.html"
      to: "public/assets/css/login.css"
      via: "classes .login-brand-glow conservée, .login-brand-features simplifiée"
      pattern: "login-brand-(glow|features)"
---

<objective>
Simplifier la page de login : supprimer le pattern de fond `login-brand-grid` (HTML + CSS) et l'orbe animé `login-orb` (CSS), conserver le `login-brand-glow` atténué, et réduire la liste `login-brand-features` à un seul bénéfice — `Vote sécurisé` (le 2e `<li>` actuel). Le formulaire prend ainsi plus de place visuelle.

Purpose: Refactoring UI Schoger — *"Patterns and textures belong on marketing pages, not on tool entry points where the user wants to do their job."* L'entrée tool doit minimiser le bruit visuel et concentrer l'attention sur le formulaire.
Output: `public/login.html` allégé (1 feature au lieu de 3, plus de div brand-grid) + `public/assets/css/login.css` nettoyé (plus de `.login-orb`, plus de `.login-brand-grid`).
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@CLAUDE.md

<read_first>
- public/login.html (entier — focus lignes ~28 pour `login-brand-grid` et lignes ~60-69 pour `login-brand-features` <ul><li>x3)
- public/assets/css/login.css (entier — repérer tous les sélecteurs `.login-orb`, `.login-brand-grid`, `.login-brand-glow`, `.login-brand-features`)
</read_first>

<product_decision_locked>
**LOGIN-02 (figé) :** Bénéfice retenu = **Vote sécurisé** (le 2e `<li>` de `<ul class="login-brand-features">` dans login.html lignes 60-69). Supprimer les 2 autres `<li>` (1er et 3e) intégralement. Justification produit : "Vote sécurisé" résume la promesse différenciante de l'application — les 2 autres bénéfices sont soit redondants soit secondaires en contexte d'entrée tool.

**LOGIN-01 (figé) :** Supprimer **`login-brand-grid`** (pattern de fond) et **`login-orb`** (radial-gradient animé). **Garder `login-brand-glow`** (gradient unique atténué — single subtle layer ≠ pattern). Si le glow est jugé trop intense après les autres suppressions, l'exécutant peut atténuer ses opacités/saturations (judgment call autorisé), mais ne le supprime pas.
</product_decision_locked>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Supprimer login-brand-grid et orbe + simplifier login-brand-features à 1 li (Vote sécurisé)</name>
  <files>public/login.html, public/assets/css/login.css</files>
  <read_first>
    - public/login.html (lignes 1-100 minimum — capter la structure brand panel)
    - public/assets/css/login.css (entier — chercher tous les sélecteurs visés)
  </read_first>
  <acceptance_criteria>
    - `grep -c "login-brand-grid" public/login.html` retourne 0
    - `grep -c "login-brand-grid" public/assets/css/login.css` retourne 0
    - `grep -c "login-orb" public/login.html` retourne 0
    - `grep -c "login-orb" public/assets/css/login.css` retourne 0
    - `grep -F "Vote sécurisé" public/login.html` matche
    - `grep -F "login-brand-glow" public/assets/css/login.css` matche (conservé)
    - Le `<ul class="login-brand-features">` ne contient plus qu'**1 seul** `<li>` (celui de Vote sécurisé)
    - **F4 fix lock ratio form-dominant** : `grep -F "grid-template-columns: 2fr 3fr" public/assets/css/login.css` matche (vérification baseline 2026-04-30 — ratio brand 40% / form 60% **déjà form-dominant**, conforme ROADMAP §8 "50/50 ou 40/60 form-dominant"). Si la valeur actuelle a dérivé, la restaurer à `2fr 3fr` ou `1fr 1fr`. Aucune dérive vers `3fr 2fr` (brand-dominant) tolérée.
    - Aucune autre string française dans la page n'est altérée (tagline, label form, bouton, etc.)
  </acceptance_criteria>
  <action>
1. **HTML — `public/login.html`** :
   a. Supprimer la ligne ~28 contenant `<div class="login-brand-grid" aria-hidden="true">` (et sa fermeture si la balise est sur la même ligne ou multi-ligne).
   b. Dans le bloc `<ul class="login-brand-features">` (lignes ~60-69), conserver **uniquement** le 2e `<li>` (celui qui contient "Vote sécurisé"). Supprimer le 1er et le 3e `<li>` intégralement (incluant leur icône / SVG / texte).
   c. Préserver intégralement le reste du markup : `login-brand-glow`, logo, tagline, formulaire, libellés français.

2. **CSS — `public/assets/css/login.css`** :
   a. Supprimer **tous** les sélecteurs et règles concernant `.login-brand-grid` (background-image gradient pattern, positionnement, animation éventuelle). Si une règle utilise `.login-brand-grid` parmi plusieurs sélecteurs (`.foo, .login-brand-grid, .bar`), retirer uniquement le sélecteur ciblé.
   b. Supprimer **tous** les sélecteurs et règles concernant `.login-orb` (radial-gradient animé, keyframes associées si exclusivement utilisées par l'orbe).
   c. Si des `@keyframes` sont devenues orphelines après suppression de `.login-orb`, les supprimer également.
   d. **Conserver** `.login-brand-glow` et toutes ses règles. Si le glow paraît disproportionné après la suppression des autres effets, atténuer **uniquement** ses propriétés `opacity`, `filter: blur(...)`, ou la saturation des couleurs — ne pas changer sa structure ni le supprimer. Ce point est un judgment call autorisé.
   e. Aucun nouveau padding/margin/gap numérique introduit pendant le nettoyage.

3. **Vérification finale** :
   - `grep -c "login-brand-grid" public/login.html public/assets/css/login.css` → 0 par fichier
   - `grep -c "login-orb" public/login.html public/assets/css/login.css` → 0 par fichier
   - `grep -F "login-brand-glow" public/assets/css/login.css` matche encore
   - Compter les `<li>` dans le bloc `login-brand-features` : doit être exactement 1 (Vote sécurisé)
   - Vérifier syntaxe : `php -l public/login.html` n'est pas applicable (HTML pur), mais ouvrir le fichier pour confirmer pas de balises orphelines.
  </action>
  <verify>
    <automated>[ "$(grep -c 'login-brand-grid' public/login.html)" = "0" ] && [ "$(grep -c 'login-brand-grid' public/assets/css/login.css)" = "0" ] && [ "$(grep -c 'login-orb' public/login.html)" = "0" ] && [ "$(grep -c 'login-orb' public/assets/css/login.css)" = "0" ] && grep -F "Vote sécurisé" public/login.html >/dev/null && grep -F "login-brand-glow" public/assets/css/login.css >/dev/null && grep -E "grid-template-columns:\s+(2fr 3fr|1fr 1fr)" public/assets/css/login.css >/dev/null</automated>
  </verify>
  <done>0 occurrence de `login-brand-grid` et `login-orb` dans HTML et CSS ; `login-brand-glow` préservé ; `login-brand-features` réduit à 1 `<li>` (Vote sécurisé) ; reste du markup intact.</done>
</task>

</tasks>

<verification>
- `[ "$(grep -c 'login-brand-grid' public/login.html)" = "0" ]`
- `[ "$(grep -c 'login-brand-grid' public/assets/css/login.css)" = "0" ]`
- `[ "$(grep -c 'login-orb' public/login.html)" = "0" ]`
- `[ "$(grep -c 'login-orb' public/assets/css/login.css)" = "0" ]`
- `grep -F "Vote sécurisé" public/login.html`
- `grep -F "login-brand-glow" public/assets/css/login.css`
- Compter `<li>` dans bloc `login-brand-features` : exactement 1
- Aucun string "copropriété" ou "syndic" introduit
- Tagline, logo, labels formulaire, bouton submit : strings françaises intactes
</verification>

<success_criteria>
La page `/login.html` est nettoyée : pas de pattern de fond, pas d'orbe animé, glow conservé. Le panneau brand affiche logo + tagline + 1 seul bénéfice (Vote sécurisé). Le formulaire de connexion gagne en présence visuelle. CSS débarrassé de tout sélecteur `.login-orb` et `.login-brand-grid`.
</success_criteria>

<output>
Créer `.planning/phases/03-layouts-secondaires/03-04-SUMMARY.md` après exécution. Documenter dans le SUMMARY :
- Lignes exactes supprimées dans login.html (avant/après)
- Sélecteurs CSS supprimés (liste exhaustive : `.login-orb`, `.login-brand-grid`, `@keyframes` orphelines éventuelles)
- Si le glow a été atténué : préciser quelles propriétés et avec quelles valeurs (avant/après)
- Confirmer que le 2e `<li>` (Vote sécurisé) est conservé tel quel sans modification de texte
</output>
