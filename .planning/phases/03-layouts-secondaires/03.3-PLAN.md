---
phase: 03-layouts-secondaires
plan: 3
type: execute
wave: 2
depends_on: [1]
files_modified:
  - public/dashboard.htmx.html
  - public/assets/css/dashboard.css
autonomous: true
requirements:
  - DASHBOARD-03
user_setup: []

must_haves:
  truths:
    - "Le bloc actions rapides (Créer séance, Importer membres, etc.) est positionné en bas du <main> ou .dashboard-content"
    - "Le bloc actions rapides utilise un background sunken (var(--color-bg-subtle) ou token surface-sunken équivalent)"
    - "Les actions rapides sont visuellement secondarisées par rapport au hero card et aux KPI"
    - "Le bloc actions rapides est distinct de la section shortcut-cards (15 cards organisées en plan 03.2)"
  artifacts:
    - path: "public/dashboard.htmx.html"
      provides: "Bloc actions rapides déplacé en bas du dashboard"
      contains: "actions-rapides"
    - path: "public/assets/css/dashboard.css"
      provides: "Styles sunken sur le bloc actions rapides"
      contains: "var(--color-bg-subtle)"
  key_links:
    - from: "public/assets/css/dashboard.css"
      to: "design-system.css tokens"
      via: "var(--color-bg-subtle) ou var(--surface-sunken)"
      pattern: "var\\(--(color-bg-subtle|surface-sunken)"
---

<objective>
Identifier le bloc "actions rapides" du dashboard (distinct des shortcut-cards traitées dans plan 03.2 — typiquement un groupe d'actions primaires comme "Créer séance", "Importer membres" en haut ou milieu du dashboard) et le reléguer en bas du `<main>` avec un background sunken pour le secondariser visuellement.

Purpose: Refactoring UI Schoger — les actions rapides ne doivent pas concurrencer le hero card / les KPI dans la hiérarchie visuelle. Elles restent accessibles mais perdent leur priorité visuelle.
Output: `public/dashboard.htmx.html` avec le bloc actions rapides déplacé en bas + `public/assets/css/dashboard.css` avec un style sunken sur ce bloc.
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/03-layouts-secondaires/03-01-SUMMARY.md

<read_first>
- public/dashboard.htmx.html (entier — repérer le bloc "actions rapides" : souvent une section avec titre "Actions rapides" ou "Raccourcis", contenant des boutons primaires distincts des 15 shortcut-cards)
- public/assets/css/dashboard.css (rapide — repérer les sélecteurs existants liés aux actions rapides ; identifier les tokens surface/background disponibles via grep dans design-system.css)
- public/assets/css/design-system.css (grep `--color-bg-`, `--surface-`, `--color-surface-` pour confirmer le nom du token "sunken" disponible)
</read_first>

<distinction_clé>
**Actions rapides ≠ Shortcut-cards.**
- *Shortcut-cards (15)* : grille de cards d'accès rapide (traitées en plan 03.2 avec le `<details>`).
- *Actions rapides* : bloc plus petit avec 2-5 boutons d'action primaire (créer, importer, etc.), souvent placé en haut ou milieu du dashboard avec une présence visuelle forte.

Si le markup contient une seule de ces deux structures, traiter celle qui correspond à "boutons d'action primaire" (CTA forts), pas la grille de cards.
</distinction_clé>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Identifier le bloc actions rapides et le déplacer en bas du dashboard avec background sunken</name>
  <files>public/dashboard.htmx.html, public/assets/css/dashboard.css</files>
  <read_first>
    - public/dashboard.htmx.html (lire entier, repérer le bloc actions rapides — chercher "Actions rapides", "Raccourcis", "Actions principales", ou un wrapper de boutons primaires distinct de la section shortcut-cards)
    - public/assets/css/design-system.css (grep `--color-bg-subtle`, `--surface-sunken`, `--color-surface-sunken` pour identifier le token disponible)
  </read_first>
  <acceptance_criteria>
    - Le bloc actions rapides est repositionné en fin de `<main>` ou `.dashboard-content` (après KPI, hero, agenda, et shortcut-cards)
    - Le bloc reçoit une classe ou un sélecteur qui applique un background `var(--color-bg-subtle)` ou `var(--surface-sunken)` (selon ce qui existe dans design-system.css)
    - `grep -F "var(--color-bg-subtle)" public/assets/css/dashboard.css` OR `grep -F "var(--surface-sunken)" public/assets/css/dashboard.css` matche
    - Aucun bouton d'action retiré (les CTA "Créer séance", "Importer membres", etc. restent accessibles, juste relégués)
    - Strings françaises préservées
    - Aucun chevauchement avec le travail de plan 03.2 (shortcut-cards `<details>` reste en place)
  </acceptance_criteria>
  <action>
1. Lire `public/dashboard.htmx.html` pour repérer le bloc actions rapides. Indices :
   - Section avec un titre `<h2>` ou `<h3>` du genre "Actions rapides", "Raccourcis", "Actions principales"
   - Wrapper contenant 2-5 boutons primaires (`<a class="btn btn-primary">` ou similaire) — distinct de la grille `shortcut-card`
   - Si ambigu, choisir le bloc dont les actions sont des CTA forts (créer, importer, démarrer) plutôt que des liens de navigation.
2. Couper le bloc complet (titre + wrapper + boutons) et le coller à la fin du `<main>` ou `.dashboard-content`, **après** la section shortcut-cards (qui a été réorganisée en plan 03.2). L'ordre final souhaité du dashboard :
   1. Hero card
   2. KPI (3 cards)
   3. Agenda / liste séances (avec empty state si vide)
   4. Shortcut-cards (5 visibles + `<details>` 10 repliées)
   5. **Actions rapides (sunken)**
3. Ajouter une classe `dashboard-actions-rapides` (ou similaire — adapter au nommage existant) sur le wrapper du bloc déplacé.
4. Dans `public/assets/css/dashboard.css`, ajouter les styles :
   ```css
   .dashboard-actions-rapides {
     background: var(--color-bg-subtle, #f9fafb); /* fallback si token absent ; remplacer par le nom réel disponible */
     padding: var(--space-6) var(--space-4);
     border-radius: var(--radius-md, 8px);
     margin-top: var(--space-8);
   }

   .dashboard-actions-rapides h2,
   .dashboard-actions-rapides h3 {
     font-size: var(--font-size-md, 1rem);
     font-weight: 500;
     color: var(--color-text-muted, #6b7280);
     margin-bottom: var(--space-3);
   }
   ```
   Vérifier le nom exact du token sunken dans `design-system.css` :
   - Si `--color-bg-subtle` existe → l'utiliser.
   - Sinon si `--surface-sunken` existe → l'utiliser.
   - Sinon prendre le plus proche (`--color-surface-2`, `--bg-secondary`, etc.).
5. **Aucun padding/margin/gap numérique** — tokens uniquement.
6. Préserver tous les libellés français des boutons (Créer séance, Importer, etc.).
  </action>
  <verify>
    <automated>{ grep -F "var(--color-bg-subtle)" public/assets/css/dashboard.css >/dev/null || grep -F "var(--surface-sunken)" public/assets/css/dashboard.css >/dev/null || grep -F "var(--color-surface-sunken)" public/assets/css/dashboard.css >/dev/null; } && grep -F "dashboard-actions-rapides" public/dashboard.htmx.html >/dev/null && grep -F "dashboard-actions-rapides" public/assets/css/dashboard.css >/dev/null</automated>
  </verify>
  <done>Bloc actions rapides repositionné en fin de `<main>` ; classe sunken appliquée via token design-system ; tous les CTA et libellés français préservés ; aucune interférence avec plans 03.1 et 03.2.</done>
</task>

</tasks>

<verification>
- `grep -F "dashboard-actions-rapides" public/dashboard.htmx.html`
- `grep -F "dashboard-actions-rapides" public/assets/css/dashboard.css`
- Au moins un token sunken : `grep -F "var(--color-bg-subtle)" OR "var(--surface-sunken)" public/assets/css/dashboard.css`
- Position du bloc : visuellement en bas du dashboard (vérification manuelle si needed via diff)
- Aucun string "copropriété" ou "syndic" introduit
- Le travail de plans 03.1 et 03.2 n'est pas régressé (KPI 3 cards, shortcut-cards `<details>` toujours présents)
</verification>

<success_criteria>
Le bloc actions rapides du dashboard est relégué en bas avec un background sunken via token design-system. Les CTA restent accessibles mais leur poids visuel est réduit, conformément à DASHBOARD-03 ("secondariser visuellement"). Aucune action perdue.
</success_criteria>

<output>
Créer `.planning/phases/03-layouts-secondaires/03-03-SUMMARY.md` après exécution. Documenter dans le SUMMARY :
- Le nom exact du bloc identifié (titre, classe wrapper)
- Le token sunken utilisé (nom exact tel que présent dans design-system.css)
- L'ordre final des sections du dashboard
</output>
