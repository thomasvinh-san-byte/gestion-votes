---
phase: 03-layouts-secondaires
plan: 2
subsystem: dashboard
tags: [shortcut-cards, schoger, rule-4-deviation, zero-diff, audit-spec-vs-reality]

# Dependency graph
requires:
  - plan: 03.1 (dashboard layout — confirms shortcut-cards live in `<aside class="dashboard-aside">`)
provides:
  - "Audit factuel de l'inventaire shortcut-cards : 3 réelles (vs 15 supposé par Schoger S-8 + REQUIREMENTS DASHBOARD-05 + plan 03.2)"
  - "DASHBOARD-05 livré ZERO-DIFF — 3 cards déjà ≤ 5, l'objectif Schoger 'top 5 + disclosure' est de facto atteint avant cette phase"
  - "Correction de propagation d'erreur : Schoger review + REQUIREMENTS + plan + questionnaire utilisateur tous corrigés vers la réalité du code"
affects: [DASHBOARD-05 marked Done, Schoger S-8 resolved by existing code]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Rule 4 deviation pattern : l'executor stop sur CHECKPOINT décision quand le PLAN.md contredit la réalité du code (spec-vs-reality mismatch). Préserve l'intégrité plutôt que d'inventer du markup factice."
    - "Audit anti-comptage substring : `grep -c 'shortcut-card' file` ≠ count de cards si BEM (les enfants `__icon`/`__text`/etc. matchent aussi). Pattern correct : `grep -c 'class=\"shortcut-card\"'` ou parser le markup."

key-files:
  created: []
  modified: []  # ZERO diff code — uniquement métadonnées (REQUIREMENTS, Schoger review, ce SUMMARY)

key-decisions:
  - "Option A retenue (questionnaire utilisateur 2026-04-30) : acter déjà-conforme, 0 diff code, marquer DASHBOARD-05 Done avec note d'audit"
  - "Le `<details>` 'Toutes les actions (15)' n'est PAS livré — il n'aurait rien à wrapper. Markup factice évité"
  - "Origine de l'erreur : scan Explore initial 2026-04-30 a compté `grep -c shortcut-card` = 15, qui inclut les enfants BEM `__icon` (×3), `__text` (×3), `__title` (×3), `__sub` (×3) = 12 enfants + 3 parents = 15 matches. Le scan rapportait '15 shortcut-cards' = lecture erronée du résultat grep"
  - "Propagation : Schoger review S-8 → REQUIREMENTS DASHBOARD-05 → plan 03.2 → questionnaire utilisateur (Top 5 + <details>) → toutes basées sur l'inventaire faux. Corrigées en cascade dans le commit de ce plan"

# Inventory réel (post-audit Plan 03.2)
inventory:
  shortcut_cards_count: 3
  cards:
    - label: "Créer une séance"
      href: "/wizard"
      subtitle: "Assistant en 4 étapes"
      classification: "flux quotidien fréquent — création séance"
      retained: true
    - label: "Piloter un vote"
      href: "/operator"
      subtitle: "Séance en direct"
      classification: "flux quotidien fréquent — exécution séance"
      retained: true
    - label: "Consulter le suivi"
      href: "/trust"
      subtitle: "Journal de traçabilité"
      classification: "flux quotidien fréquent — audit"
      retained: true
  cards_demoted: 0  # rien à plier
  schoger_target: "≤ 5 visibles"
  status_vs_target: "3 ≤ 5 — déjà conforme avant cette phase"

# Verification
verification:
  acceptance:
    - "REQUIREMENTS.md DASHBOARD-05 ligne 47 : `[x]` + note inventaire réel + lien vers ce SUMMARY ✓"
    - "REQUIREMENTS.md traceability table ligne 127 : `Done (n=3 already ≤5, zero-diff per Plan 03.2 audit)` ✓"
    - "v2.3-UX-REVIEW-SCHOGER.md S-8 corrigé en place + barré l'erreur ✓"
    - "Aucun fichier `public/` modifié par ce plan ✓"
    - "Aucun nouveau commit `feat(03-2):` — uniquement `docs(03-2):` pour les métadonnées ✓"

# Deviations
deviations:
  - rule: "Rule 4 (architectural deviation)"
    type: "spec-vs-reality mismatch"
    rationale: "Le PLAN 03.2 + REQUIREMENTS DASHBOARD-05 + revue Schoger S-8 supposaient unanimement 15 shortcut-cards. L'audit factuel sur `public/dashboard.htmx.html` (commit initial à HEAD) trouve 3 cards. L'executor a stop sur CHECKPOINT pour décision produit avant tout edit destructif."
    impact: "DASHBOARD-05 livré sans diff code — ce qui est le résultat attendu pour un objectif déjà atteint par le code existant. Plus honnête que d'inventer 12 cards factices ou de bidouiller un `<details>` vide."
    user_decision: "Option A retenue via AskUserQuestion 2026-04-30 ('Acter déjà-conforme, 0 diff')"

# Tests
tests:
  added: []
  modified: []
  removed: []

# Followups
followups:
  - id: "03.2-FOLLOWUP-1"
    type: "preventive process improvement"
    description: "Pour les futurs scans Explore qui informent les revues UX : utiliser `grep -c '<a class=\"shortcut-card\"' file` (selector exact) plutôt que `grep -c shortcut-card` (substring). Évite la confusion BEM enfant/parent. Documenter ce pattern dans le runbook revues UX."
    owner: "v2.4 process retrospective"
    blocking_for: "rien — leçon préventive"

# Commits
commits:
  - hash: TBD
    type: docs
    description: "complete shortcut-cards audit (zero-diff per Option A) — DASHBOARD-05 Done, Schoger S-8 corrected"
