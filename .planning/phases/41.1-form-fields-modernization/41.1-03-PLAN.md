---
phase: 41.1-form-fields-modernization
plan: 03
type: execute
wave: 2
depends_on:
  - 41.1-01
files_modified:
  - public/email-templates.htmx.html
  - public/postsession.htmx.html
  - public/operator.htmx.html
  - public/partials/operator-exec.html
autonomous: false
requirements:
  - UX-01
  - UX-02

must_haves:
  truths:
    - "Every textarea in the codebase uses form-textarea class (not form-input on textarea elements)"
    - "All 7 textarea.form-input occurrences converted to form-textarea"
    - "User confirms form fields look professional across wizard, settings, admin pages"
  artifacts:
    - path: "public/email-templates.htmx.html"
      provides: "templateBody textarea with form-textarea class"
      contains: "form-textarea"
    - path: "public/postsession.htmx.html"
      provides: "pvObservations and pvReserves textareas with form-textarea class"
      contains: "form-textarea"
    - path: "public/operator.htmx.html"
      provides: "newResolutionDesc textarea with form-textarea class"
      contains: "form-textarea"
    - path: "public/partials/operator-exec.html"
      provides: "opSecretaryNotes textarea with form-textarea class"
      contains: "form-textarea"
  key_links:
    - from: "all textarea elements"
      to: "public/assets/css/design-system.css"
      via: ".form-textarea class providing min-height + resize"
      pattern: "form-textarea"
---

<objective>
Fix remaining textarea class mismatches across all pages (email-templates, postsession, operator) and run a visual verification checkpoint for the complete form field modernization.

Purpose: Ensure every textarea in the codebase gets proper sizing via the correct class, then confirm all form changes look right in the browser.
Output: All textareas fixed, user-verified form quality.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.1-form-fields-modernization/41.1-CONTEXT.md
@.planning/phases/41.1-form-fields-modernization/41.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix remaining textarea class mismatches (4 files, 5 textareas)</name>
  <files>public/email-templates.htmx.html, public/postsession.htmx.html, public/operator.htmx.html, public/partials/operator-exec.html</files>
  <read_first>
    - public/email-templates.htmx.html (search for textarea)
    - public/postsession.htmx.html (search for textarea)
    - public/operator.htmx.html (search for textarea)
    - public/partials/operator-exec.html (search for textarea)
  </read_first>
  <action>
In each file, change `class="form-input"` to `class="form-textarea"` on textarea elements:

**File 1 — email-templates.htmx.html** (line ~130):
`<textarea class="form-input" id="templateBody"` -> `<textarea class="form-textarea" id="templateBody"`

**File 2 — postsession.htmx.html** (lines ~275, ~283):
`<textarea class="form-input" id="pvObservations"` -> `<textarea class="form-textarea" id="pvObservations"`
`<textarea class="form-input" id="pvReserves"` -> `<textarea class="form-textarea" id="pvReserves"`

**File 3 — operator.htmx.html** (line ~660):
`<textarea class="form-input" id="newResolutionDesc"` -> `<textarea class="form-textarea" id="newResolutionDesc"`

**File 4 — partials/operator-exec.html** (line ~227):
`<textarea class="form-input" id="opSecretaryNotes"` -> `<textarea class="form-textarea" id="opSecretaryNotes"`

These 5 textareas plus the 2 fixed in plan 02 (settings + admin templateBody) account for all 7 `textarea.form-input` instances found in the codebase.

Do NOT change:
- Any other attributes on these textareas (rows, placeholder, id, maxlength)
- Any JavaScript that references these textareas by ID (unchanged)
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -r 'textarea.*class="form-input"' public/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep count of textarea.form-input returns 0 (all converted)
    - All 7 textareas now use class="form-textarea"
    - No ID or attribute changes — JS compatibility preserved
  </acceptance_criteria>
  <done>Zero textarea elements use form-input class. All textareas inherit form-textarea rules (min-height: 100px, resize: vertical) from design-system.css.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual checkpoint — form field modernization across all pages</name>
  <files>none</files>
  <action>
Present the user with a verification checklist for the complete form field modernization (plans 01 + 02 + 03 combined).

What was built:
- Italic placeholders on all inputs/textareas (differentiate empty vs filled)
- Custom SVG chevron on all select dropdowns (light + dark mode)
- Grey form labels (recede behind input values, Stripe-style hierarchy)
- Wizard fields aligned with design-system (1px border, 6px radius, surface-raised bg)
- Settings SMTP fields in 2-column grid
- Admin user-create form in 2-column grid
- Number inputs constrained to 120px max-width
- All textareas with proper min-height and vertical resize
- .form-grid-2 and .form-stack utilities available

How to verify:
1. Open wizard (create session) — fields have 1px border, 6px radius, white background, italic placeholder, grey labels, custom chevron on selects, standard double-ring focus
2. Open settings -> Communication tab — SMTP fields in 2-col grid, textarea resizable, number inputs compact
3. Open admin -> Users tab — user create form has 2-column layout
4. Toggle dark mode — select chevron visible (light stroke), placeholders still italic
5. Check postsession or operator page — textareas have min-height and vertical resize handle

Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>User visual approval</verify>
  <acceptance_criteria>
    - User confirms form fields look professional and consistent across all checked pages
    - No visual regressions reported
  </acceptance_criteria>
  <done>User approves form field quality. Phase 41.1 complete.</done>
</task>

</tasks>

<verification>
After all tasks complete and checkpoint passes:
- Zero textarea.form-input instances in codebase
- All form fields across all pages share consistent visual treatment
- No JavaScript regressions
</verification>

<success_criteria>
- User approves form field quality across wizard, settings, admin, and other pages
- All 7 textarea class mismatches fixed
- Form fields look Stripe-quality: clean, compact, professional
</success_criteria>

<output>
After completion, create `.planning/phases/41.1-form-fields-modernization/41.1-03-SUMMARY.md`
</output>
