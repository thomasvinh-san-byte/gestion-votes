---
phase: 11-post-session-records
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/audit.htmx.html
  - public/assets/css/audit.css
autonomous: true
requirements: [AUD-01, AUD-02]

must_haves:
  truths:
    - "Audit page loads in the app shell with sidebar highlighting audit nav item"
    - "4 KPI cards display at top: Integrite, Evenements, Anomalies, Derniere seance"
    - "Filter pills row shows Tous/Votes/Presences/Securite/Systeme tabs"
    - "View toggle switches between Tableau and Chronologie views"
    - "Table view has checkbox column, #, Horodatage (mono), Evenement (with severity dot), Utilisateur (tag), Empreinte (mono hash)"
    - "Timeline view shows vertical connector line with severity-colored dots and event cards"
    - "Pagination controls appear below the table/timeline"
  artifacts:
    - path: "public/audit.htmx.html"
      provides: "Complete audit page HTML with app shell, KPIs, filters, table and timeline views, pagination"
      contains: "data-page=\"audit\""
    - path: "public/assets/css/audit.css"
      provides: "Audit page styles: KPI grid, filter tabs, table with severity dots, timeline with connector line"
      contains: "audit-timeline"
  key_links:
    - from: "public/audit.htmx.html"
      to: "public/assets/css/audit.css"
      via: "link rel stylesheet"
      pattern: "audit\\.css"
    - from: "public/audit.htmx.html"
      to: "public/assets/css/app.css"
      via: "app shell styles"
      pattern: "app\\.css"
---

<objective>
Create the audit page HTML structure and CSS from scratch, following wireframe v3.19.2 and CONTEXT.md decisions.

Purpose: The audit page is a new page that does not exist yet. It provides audit trail visibility with KPIs, filterable event table/timeline, and pagination. This plan creates the static HTML and CSS; Plan 03 will add the JS behavior.

Output: audit.htmx.html and audit.css with complete page structure ready for JS wiring.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-post-session-records/11-CONTEXT.md

@public/archives.htmx.html
@public/assets/css/archives.css
</context>

<interfaces>
<!-- Reusable patterns from existing codebase (confirmed in archives.htmx.html): -->

Page shell pattern:
- Skip links, .app-shell, aside.app-sidebar[data-page], header.app-header[role=banner], main.app-main#main-content[role=main], footer.app-footer[role=contentinfo]
- Script chain: utils.js -> shared.js -> shell.js -> page.js

CSS patterns to reuse from design-system.css / archives.css:
- .kpi-grid / .kpi-card / .kpi-value / .kpi-label (KPI cards)
- .filter-tabs / .filter-tab / .filter-tab.active (filter pills)
- .view-toggle / .view-toggle-btn / .view-toggle-btn.active (view toggle)
- .card / .card-header / .card-body (content containers)
- .tag / .tag-success / .tag-danger / .tag-accent / .tag-warning (badges)
- .modal / .modal-backdrop / .modal-header / .modal-body / .modal-footer (modal)
- .archives-pagination (pagination pattern — reuse as .audit-pagination)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create audit page HTML with full structure</name>
  <files>public/audit.htmx.html</files>
  <action>
Create `public/audit.htmx.html` following the exact page pattern from archives.htmx.html. Use HTML entities for accented French characters (same convention as all other pages).

**Head section:** Same as archives — favicon, meta, theme-color, fonts, app.css + audit.css, theme-init.js.

**App shell:** Skip links, .app-shell, sidebar with `data-page="audit"`, drawer (same pattern as archives).

**Header (role=banner):**
- Title: `<h1>` with shield icon + "Audit et Conformit&eacute;"
- Subtitle: "Journal de tra&ccedil;abilit&eacute;"
- Header actions: conditional "Exporter la s&eacute;lection" button (`#btnExportSelection`, initially hidden) + "Tout exporter" button (`#btnExportAll`) + drawer toggle button

**Main content:**
- 4 KPI cards in `.kpi-grid.mb-6`:
  1. Shield icon, id="kpiIntegrity", label "Int&eacute;grit&eacute;"
  2. Activity icon, id="kpiEvents", label "&Eacute;v&eacute;nements"
  3. Check-circle icon, id="kpiAnomalies", label "Anomalies"
  4. Clock icon, id="kpiLastSession", label "Derni&egrave;re s&eacute;ance"

- Filter pills row in `.filter-tabs.mb-4#auditTypeFilter`:
  - `<button class="filter-tab active" data-type="">Tous</button>`
  - `<button class="filter-tab" data-type="votes">Votes</button>`
  - `<button class="filter-tab" data-type="presences">Pr&eacute;sences</button>`
  - `<button class="filter-tab" data-type="securite">S&eacute;curit&eacute;</button>`
  - `<button class="filter-tab" data-type="systeme">Syst&egrave;me</button>`

- Card wrapper for content:
  - Card header with: search input (`#auditSearch`, placeholder "Rechercher..."), sort select (`#auditSort`), view toggle (Tableau/Chronologie buttons with grid/list icons, same `.view-toggle` pattern as archives)

  - Card body with two view containers:
    1. `#auditTableView` (visible by default):
       - `<table class="audit-table" id="auditTable">` with thead: checkbox (select-all `#selectAll`), #, Horodatage, &Eacute;v&eacute;nement, Utilisateur, Empreinte
       - `<tbody id="auditTableBody">` with loading placeholder
    2. `#auditTimelineView` (hidden):
       - `<div class="audit-timeline" id="auditTimeline">` with loading placeholder

  - Pagination: `<div class="audit-pagination" id="auditPagination"></div>`

**Event detail modal** (at end of body, before scripts):
- `.modal-backdrop#auditDetailBackdrop` (display:none)
- `.modal#auditDetailModal` with:
  - Header: "D&eacute;tail de l'&eacute;v&eacute;nement" title + close button
  - Body:
    - 2x2 metadata grid (`.audit-detail-grid`): Horodatage (`#detailTimestamp`), Cat&eacute;gorie (`#detailCategory`), Utilisateur (`#detailUser`), S&eacute;v&eacute;rit&eacute; (`#detailSeverity`) — each in a `.audit-detail-item` with label + value, background surface-alt
    - Description block (`#detailDescription`) in surface-alt
    - Full SHA-256 hash block (`#detailHash`) in surface-alt with monospace, accent color
  - Footer: "Fermer" button + "Exporter" button

**Footer:** Same app-footer as archives.

**Scripts:** utils.js, components/index.js (module), shared.js, shell.js, meeting-context.js, then `audit.js`.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && node -e "
      var fs = require('fs');
      var html = fs.readFileSync('public/audit.htmx.html','utf8');
      var checks = [
        ['file exists', true],
        ['data-page audit', html.includes('data-page=\"audit\"')],
        ['skip link', html.includes('skip-link')],
        ['role banner', html.includes('role=\"banner\"')],
        ['role main', html.includes('role=\"main\"')],
        ['role contentinfo', html.includes('role=\"contentinfo\"')],
        ['4 KPI cards', (html.match(/kpi-card/g)||[]).length >= 4],
        ['filter tabs', html.includes('auditTypeFilter')],
        ['view toggle', html.includes('view-toggle')],
        ['audit table', html.includes('auditTable')],
        ['audit timeline', html.includes('auditTimeline')],
        ['select all checkbox', html.includes('selectAll')],
        ['detail modal', html.includes('auditDetailModal')],
        ['detail hash', html.includes('detailHash')],
        ['pagination', html.includes('auditPagination')],
        ['audit.css linked', html.includes('audit.css')],
        ['audit.js linked', html.includes('audit.js')],
        ['app-footer', html.includes('app-footer')],
      ];
      var pass = checks.filter(c=>c[1]).length;
      checks.forEach(function(c){console.log((c[1]?'PASS':'FAIL')+': '+c[0])});
      console.log(pass+'/'+checks.length+' checks passed');
      process.exit(pass===checks.length?0:1);
    "
    </automated>
  </verify>
  <done>audit.htmx.html exists with complete page structure: app shell, 4 KPIs, filter pills, table+timeline views with toggle, select-all checkbox, pagination, event detail modal, correct script chain.</done>
</task>

<task type="auto">
  <name>Task 2: Create audit page CSS</name>
  <files>public/assets/css/audit.css</files>
  <action>
Create `public/assets/css/audit.css` with styles for all audit-specific elements. Reuse design-system tokens throughout — no hardcoded colors.

**Audit table styles (`.audit-table`):**
- Full width, border-collapse, font-size var(--text-sm)
- `th, td`: padding var(--space-2) var(--space-3), border-bottom 1px solid var(--color-border), text-align left
- `th`: font-weight semibold, font-size text-xs, uppercase, letter-spacing 0.5px, color text-muted, bg bg-subtle, sticky top 0
- `tbody tr:hover td`: bg bg-subtle
- `tbody tr`: cursor pointer (rows are clickable)
- Checkbox column: width 40px, text-align center
- `.audit-hash-cell`: font-family var(--font-mono), font-size var(--text-xs), color var(--color-text-muted), max-width 120px, overflow hidden, text-overflow ellipsis, white-space nowrap
- `.audit-timestamp`: font-family var(--font-mono), font-size var(--text-xs), white-space nowrap
- `.audit-event-cell`: display flex, align-items center, gap var(--space-2)
- Severity dot (`.audit-severity-dot`): width 6px, height 6px, border-radius 50%, display inline-block, flex-shrink 0
- `.audit-severity-dot.info`: background var(--color-accent, var(--color-primary))
- `.audit-severity-dot.success`: background var(--color-success)
- `.audit-severity-dot.danger`: background var(--color-danger)
- `.audit-severity-dot.warning`: background var(--color-warning)

**Audit timeline styles (`.audit-timeline`):**
- Position relative, padding-left 32px (space for connector)
- `.audit-timeline::before` (connector line): content '', position absolute, left 14px, top 0, bottom 0, width 2px, background var(--color-border)
- `.audit-timeline-item`: position relative, padding var(--space-4) 0, padding-left var(--space-4)
- `.audit-timeline-dot`: position absolute, left -22px (relative to item padding), width 10px, height 10px, border-radius 50%, border 2px solid var(--color-surface), z-index 1
- Dot colors: same severity color classes as table dots
- `.audit-timeline-content`: wrapped in `.card` with padding
- First line: `.audit-timeline-title` (font-weight semibold) + `.audit-timeline-time` (text-muted, text-xs, mono)
- Second line: `.audit-timeline-meta` with category tag + user tag + hash (text-xs, mono, muted)
- `.audit-timeline-chevron`: position absolute, right var(--space-3), top 50%, transform translateY(-50%), color text-muted, cursor pointer

**Event detail modal styles (`.audit-detail-grid`):**
- Display grid, grid-template-columns 1fr 1fr, gap var(--space-3), margin-bottom var(--space-4)
- `.audit-detail-item`: padding var(--space-3), background var(--color-bg-subtle), border-radius var(--radius)
- `.audit-detail-label`: font-size text-xs, color text-muted, text-transform uppercase, letter-spacing 0.5px, margin-bottom var(--space-1)
- `.audit-detail-value`: font-weight semibold
- `.audit-detail-description`: padding var(--space-4), background var(--color-bg-subtle), border-radius var(--radius), margin-bottom var(--space-4), font-size text-sm
- `.audit-detail-hash`: padding var(--space-3), background var(--color-bg-subtle), border-radius var(--radius), font-family var(--font-mono), font-size var(--text-xs), color var(--color-accent, var(--color-primary)), word-break break-all

**Pagination (`.audit-pagination`):**
- Reuse same pattern as archives: display flex, justify-content center, gap var(--space-2), padding var(--space-4) 0

**Header actions:** `.audit-header-actions` flex gap-2 for export buttons

**Responsive:**
- @media (max-width: 768px): audit-table hides Empreinte column (nth-child(6)), audit-detail-grid goes to 1 column, audit-timeline padding-left reduced
- @media (max-width: 640px): audit-table hides Utilisateur column too (nth-child(5))
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && node -e "
      var fs = require('fs');
      var css = fs.readFileSync('public/assets/css/audit.css','utf8');
      var checks = [
        ['audit-table styles', css.includes('.audit-table')],
        ['severity dot', css.includes('audit-severity-dot')],
        ['timeline styles', css.includes('audit-timeline')],
        ['timeline connector', css.includes('::before') || css.includes(':before')],
        ['timeline dot', css.includes('audit-timeline-dot')],
        ['detail grid', css.includes('audit-detail-grid')],
        ['detail hash', css.includes('audit-detail-hash')],
        ['font-mono usage', css.includes('font-mono')],
        ['pagination', css.includes('audit-pagination')],
        ['responsive', css.includes('@media')],
        ['uses design tokens', css.includes('var(--')],
        ['no hardcoded hex outside fallbacks', true],
      ];
      var pass = checks.filter(c=>c[1]).length;
      checks.forEach(function(c){console.log((c[1]?'PASS':'FAIL')+': '+c[0])});
      console.log(pass+'/'+checks.length+' checks passed');
      process.exit(pass===checks.length?0:1);
    "
    </automated>
  </verify>
  <done>audit.css exists with complete styles: table with severity dots and mono hash, timeline with connector line and colored dots, event detail modal with 2x2 grid and hash block, pagination, responsive breakpoints. All using design system tokens.</done>
</task>

</tasks>

<verification>
- audit.htmx.html opens in browser with correct app shell (sidebar, header, footer)
- 4 KPI cards visible at top
- Filter pills row with 5 buttons
- Table view visible by default with correct column headers
- Clicking Chronologie toggle shows timeline container
- Event detail modal exists in DOM (hidden)
- No CSS errors, all tokens resolve
</verification>

<success_criteria>
Audit page HTML and CSS fully built: complete app shell integration, 4 KPIs, filter pills, table view with checkboxes and severity dots, timeline view with connector line, event detail modal with metadata grid and hash display, pagination, responsive breakpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/11-post-session-records/11-02-SUMMARY.md`
</output>
