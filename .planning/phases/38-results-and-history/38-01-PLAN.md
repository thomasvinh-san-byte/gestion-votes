---
phase: 38-results-and-history
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/postsession.css
  - public/postsession.htmx.html
  - public/assets/js/pages/postsession.js
autonomous: true
requirements: [CORE-05]

must_haves:
  truths:
    - "Post-session stepper shows pill-shaped steps with glow on active and green checkmark on completed"
    - "ADOPTE/REJETE verdict badges on result card summaries are large, colored, and readable from across a meeting room"
    - "Each stepper step has an ag-tooltip explaining what happens at that stage"
    - "Result cards have a colored left border indicating verdict (green adopted, red rejected)"
    - "Expanded result card shows a large prominent verdict display (2.5rem)"
    - "48px spacing between major post-session sections"
  artifacts:
    - path: "public/assets/css/postsession.css"
      provides: "Pill stepper CSS, verdict prominence CSS, section spacing"
      contains: "ps-seg.*border-radius.*radius-full"
    - path: "public/postsession.htmx.html"
      provides: "ag-tooltip wrappers on stepper steps"
      contains: "ag-tooltip.*Vérifiez"
    - path: "public/assets/js/pages/postsession.js"
      provides: "data-verdict attribute on result cards"
      contains: "data-verdict"
  key_links:
    - from: "public/postsession.htmx.html"
      to: "public/assets/css/postsession.css"
      via: "ps-seg pill classes + ag-tooltip display:contents"
      pattern: "ps-stepper ag-tooltip.*display.*contents"
    - from: "public/assets/js/pages/postsession.js"
      to: "public/assets/css/postsession.css"
      via: "data-verdict attribute drives left-border color"
      pattern: "result-card\\[data-verdict"
---

<objective>
Redesign the post-session page with pill-shaped stepper, prominent verdict badges, ag-tooltip guidance, and proper section spacing. This page is the operator's post-meeting workflow — clarity of step progression and vote outcomes is critical.

Purpose: CORE-05 — Post-session stepper, result cards, archival progression, spacing, tooltips
Output: Visually transformed postsession page with pill stepper glow, large verdict badges, tooltip-wrapped steps
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-results-and-history/38-CONTEXT.md
@.planning/phases/38-results-and-history/38-RESEARCH.md

@public/assets/css/postsession.css
@public/postsession.htmx.html
@public/assets/js/pages/postsession.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Post-session stepper pill upgrade + ag-tooltip wrappers</name>
  <files>public/assets/css/postsession.css, public/postsession.htmx.html</files>
  <read_first>
    - public/assets/css/postsession.css (full file — understand current .ps-seg, .ps-stepper rules)
    - public/postsession.htmx.html (full file — find the .ps-stepper markup and all .ps-seg elements)
    - .planning/phases/38-results-and-history/38-RESEARCH.md (Pattern 1: stepper upgrade, Pattern 7: ag-tooltip)
  </read_first>
  <action>
**CSS — postsession.css: Replace flat .ps-seg with pill stepper**

BEFORE: .ps-seg has flat background, square corners, simple active coloring.
AFTER: Pill-shaped segments with rounded corners, glow on active, green checkmark on completed.

1. Replace the `.ps-seg` base rule with:
   - `border-radius: var(--radius-full)` (pill shape)
   - `border: 1.5px solid var(--color-border)`
   - `background: var(--color-surface)`
   - `color: var(--color-text-muted)`
   - `font-size: 0.8125rem; font-weight: 600`
   - `display: flex; align-items: center; justify-content: center; gap: var(--space-2)`
   - `padding: 10px 16px`
   - `transition: all var(--duration-normal) var(--ease-standard)`

2. `.ps-seg.active`:
   - `background: var(--color-primary); color: var(--color-primary-text)`
   - `border-color: var(--color-primary)`
   - `box-shadow: 0 0 0 3px var(--color-primary-glow)` (primary glow ring)
   - `font-weight: 700`

3. `.ps-seg.done` / `.ps-seg.step-complete`:
   - `background: var(--color-success-subtle); color: var(--color-success-text)`
   - `border-color: var(--color-success-border)`

4. Add `.ps-stepper ag-tooltip { display: contents; }` to prevent flex layout breakage when tooltips wrap stepper steps.

5. Ensure the `.ps-stepper` container uses `display: flex; gap: var(--space-2)` with a step connector. Add a pseudo-element line between pills if not already present, or rely on gap spacing for visual separation.

6. Keep the existing responsive rule `@media (max-width: 768px) { .ps-seg { flex: 1 1 calc(50% - 2px); } }` — ensure pill shape still works in 2x2 grid on mobile.

**HTML — postsession.htmx.html: Wrap each .ps-seg in ag-tooltip**

Find the `.ps-stepper` section. Wrap each `.ps-seg` div in `<ag-tooltip>` with descriptive French text and `position="bottom"`:

- Step 1 (Verification): "Verifiez que tous les votes sont enregistres et les resultats coherents avant de continuer"
- Step 2 (Validation): "Le president valide officiellement les resultats — action irreversible"
- Step 3 (Proces-verbal): "Generez et verifiez le proces-verbal. Le hash garantit l'integrite du document"
- Step 4 (Envoi & Archivage): "Envoyez le PV aux participants et archivez definitivement la seance"

Use proper French accented characters in the actual text attributes. See RESEARCH.md Pattern 7 for exact markup.

**Section spacing: Add 48px between major sections**

Add `margin-top: var(--space-section)` (48px) between the major post-session sections (Results, Validation, PV, Archival). Target the section container elements that separate these workflow steps.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "radius-full" public/assets/css/postsession.css && grep -c "ag-tooltip" public/postsession.htmx.html && grep -c "display: contents" public/assets/css/postsession.css</automated>
  </verify>
  <acceptance_criteria>
    - .ps-seg uses border-radius: var(--radius-full) for pill shape
    - .ps-seg.active has box-shadow glow ring
    - .ps-seg.done has green success background/border
    - .ps-stepper ag-tooltip { display: contents } prevents flex breakage
    - All 4 stepper steps wrapped in ag-tooltip with French explanatory text
    - 48px section spacing between major post-session workflow sections
  </acceptance_criteria>
  <done>Stepper visually transformed from flat segments to glowing pills with tooltips, section spacing at 48px</done>
</task>

<task type="auto">
  <name>Task 2: Result card verdict prominence + data-verdict left border</name>
  <files>public/assets/css/postsession.css, public/assets/js/pages/postsession.js</files>
  <read_first>
    - public/assets/css/postsession.css (find .result-card-verdict, .result-adopted, .result-rejected rules)
    - public/assets/js/pages/postsession.js (find renderResultCards function — locate where details.result-card is built)
    - .planning/phases/38-results-and-history/38-RESEARCH.md (Pattern 2: verdict prominence)
  </read_first>
  <action>
**CSS — postsession.css: Verdict badge prominence**

1. Upgrade `.result-card-verdict` (the summary-level badge):
   - `font-size: var(--text-base)` (up from text-sm)
   - `font-weight: 800`
   - `padding: var(--space-1) var(--space-3)`
   - `border-radius: var(--radius-full)`
   - `letter-spacing: 0.04em`

2. `.result-adopted` (summary badge):
   - `background: var(--color-success-subtle)`
   - `color: var(--color-success-text)`
   - `border: 1px solid var(--color-success-border)`

3. `.result-rejected` (summary badge):
   - `background: var(--color-danger-subtle)`
   - `color: var(--color-danger-text)`
   - `border: 1px solid var(--color-danger-border)`

4. Add left-border color coding on result card via data-verdict:
   - `.result-card[data-verdict="adopted"] { border-left: 4px solid var(--color-success); }`
   - `.result-card[data-verdict="rejected"] { border-left: 4px solid var(--color-danger); }`

5. Add/upgrade `.result-card-verdict-large` (expanded body large verdict):
   - `font-size: 2.5rem; font-weight: 900`
   - `font-family: var(--font-display, 'Bricolage Grotesque', sans-serif)`
   - `letter-spacing: -0.02em; text-align: center`
   - `padding: var(--space-6) var(--space-4)`

6. Ensure vote breakdown numbers inside expanded card use `font-family: var(--font-mono)` (JetBrains Mono) and `font-variant-numeric: tabular-nums`.

**JS — postsession.js: Add data-verdict attribute**

In the `renderResultCards()` function (or equivalent that builds result card HTML), find where `<details class="result-card">` is generated. Add the `data-verdict` attribute:

```js
data-verdict="' + (adopted ? 'adopted' : 'rejected') + '"
```

This enables the CSS left-border color coding. Do NOT change the function signature or the container parameter contract. Keep the `<details>/<summary>` pattern intact.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "data-verdict" public/assets/js/pages/postsession.js && grep "result-card\[data-verdict" public/assets/css/postsession.css | head -2</automated>
  </verify>
  <acceptance_criteria>
    - .result-card-verdict uses font-size var(--text-base) with font-weight 800
    - .result-adopted has green success background/border pill
    - .result-rejected has red danger background/border pill
    - .result-card[data-verdict="adopted"] has 4px green left border
    - .result-card[data-verdict="rejected"] has 4px red left border
    - .result-card-verdict-large is 2.5rem display font for expanded view
    - postsession.js emits data-verdict attribute on details elements
    - Vote breakdown numbers use JetBrains Mono
  </acceptance_criteria>
  <done>ADOPTE/REJETE verdicts are large, colored, pill-shaped badges readable from across a meeting room; result cards have verdict-colored left borders; expanded view shows 2.5rem display verdict</done>
</task>

</tasks>

<verification>
- Open postsession.htmx.html in browser at 1080p
- Stepper shows 4 pill-shaped steps (not flat segments)
- Active step has blue glow ring
- Completed steps show green background
- Hover each step to see ag-tooltip with French explanation
- Result cards show ADOPTE in green pill or REJETE in red pill on the summary line
- Result cards have colored left border matching verdict
- Expanding a card shows large 2.5rem verdict text
- 48px spacing between major sections
- Dark mode: all elements maintain contrast and visibility
- No JS console errors
</verification>

<success_criteria>
Post-session page is visually transformed: pill stepper with glow, prominent verdict badges, tooltip guidance on every step, proper section spacing. Before/after screenshot shows dramatic improvement.
</success_criteria>

<output>
After completion, create `.planning/phases/38-results-and-history/38-01-SUMMARY.md`
</output>
