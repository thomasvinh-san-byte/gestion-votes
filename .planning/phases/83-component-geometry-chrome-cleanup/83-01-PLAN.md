---
phase: 83-component-geometry-chrome-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
  - public/assets/css/pages.css
  - public/assets/css/analytics.css
  - public/assets/css/meetings.css
  - public/assets/css/help.css
  - public/assets/css/app.css
  - public/assets/css/audit.css
  - public/assets/css/validate.css
  - public/assets/css/email-templates.css
  - public/assets/css/hub.css
  - public/assets/css/operator.css
  - public/assets/css/wizard.css
  - public/assets/css/login.css
  - public/assets/css/members.css
  - public/assets/css/public.css
  - public/assets/css/users.css
  - public/assets/css/landing.css
  - public/assets/js/components/ag-kpi.js
  - public/assets/js/components/ag-modal.js
  - public/assets/js/components/ag-toast.js
  - public/assets/js/components/ag-vote-button.js
  - public/assets/js/components/ag-pdf-viewer.js
  - public/assets/js/components/ag-tz-picker.js
  - public/assets/js/components/ag-searchable-select.js
  - public/assets/js/components/ag-quorum-bar.js
  - public/assets/js/components/ag-pagination.js
  - public/assets/js/components/ag-tooltip.js
  - public/assets/js/components/ag-confirm.js
  - public/assets/js/components/ag-page-header.js
  - public/assets/js/components/ag-time-input.js
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03]

must_haves:
  truths:
    - "Every button, input, card, modal, and dropdown shares the same 8px corner radius via --radius-base"
    - "Adjusting --radius-base changes all components simultaneously with no per-component overrides"
    - "Shadow scale has exactly three named elevation levels: sm, md, lg"
    - "No component uses a shadow token outside the sm/md/lg vocabulary (except inner/inset/focus utilities)"
    - "Borders on cards and panels use alpha-based oklch color that adapts to light and dark backgrounds"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "--radius-base token, 3-level shadow scale, --color-border-alpha token"
      contains: "--radius-base"
    - path: "public/assets/css/pages.css"
      provides: "Updated shadow and border tokens for dashboard/page components"
    - path: "public/assets/js/components/ag-kpi.js"
      provides: "Updated radius fallback (8px)"
    - path: "public/assets/js/components/ag-modal.js"
      provides: "Updated radius and shadow fallbacks"
  key_links:
    - from: "design-system.css :root"
      to: "all per-page CSS files"
      via: "var(--shadow-sm), var(--shadow-md), var(--shadow-lg)"
      pattern: "var\\(--shadow-(sm|md|lg)\\)"
    - from: "design-system.css :root"
      to: "all Web Components"
      via: "var(--radius-base, 8px)"
      pattern: "var\\(--radius-base, 8px\\)"
    - from: "design-system.css :root"
      to: "[data-theme='dark'] block"
      via: "shadow and border-alpha tokens mirrored"
      pattern: "--color-border-alpha"
---

<objective>
Consolidate radius tokens to --radius-base (8px), reduce shadow scale from 9 to 3 named levels (sm/md/lg), and add alpha-based border token for adaptive depth.

Purpose: Uniform geometry language across all components — one radius, three shadows, adaptive borders.
Output: Updated design-system.css tokens + all per-page CSS + all Web Component fallback literals.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-component-geometry-chrome-cleanup/83-CONTEXT.md
@.planning/phases/83-component-geometry-chrome-cleanup/83-RESEARCH.md
@.planning/phases/82-token-foundation-palette-shift/82-01-SUMMARY.md

<interfaces>
<!-- Current radius tokens in design-system.css :root (lines 259-268) -->
--radius-xs:   0.1875rem;  /* 3px */
--radius-sm:   0.25rem;    /* 4px */
--radius-md:   0.375rem;   /* 6px */
--radius-lg:   0.5rem;     /* 8px */
--radius-xl:   0.75rem;    /* 12px */
--radius-full: 9999px;
--radius: var(--radius-lg);

<!-- Component alias tokens (lines 526-536) — TO BE REMOVED -->
--radius-btn:     var(--radius-md);   /* 6px */
--radius-input:   var(--radius-lg);   /* 8px */
--radius-badge:   var(--radius-full);
--radius-chip:    var(--radius-full);
--radius-card:    var(--radius-xl);   /* 12px */
--radius-panel:   var(--radius-lg);   /* 8px */
--radius-modal:   var(--radius-xl);   /* 12px */
--radius-toast:   var(--radius-lg);   /* 8px */
--radius-tooltip: var(--radius-sm);   /* 4px */
--radius-avatar:  var(--radius-full);
--radius-tag:     var(--radius-xs);   /* 3px */

<!-- Current shadow tokens in :root (lines 408-428) -->
--shadow-2xs: 0 1px 0 rgb(var(--shadow-color) / 0.04);
--shadow-xs:  0 1px 2px rgb(var(--shadow-color) / 0.06), ...;
--shadow-sm:  0 1px 3px rgb(var(--shadow-color) / 0.08), ...;
--shadow:     <unnumbered>;
--shadow-md:  0 4px 12px rgb(var(--shadow-color) / 0.10), ...;
--shadow-lg:  0 8px 24px rgb(var(--shadow-color) / 0.12), ...;
--shadow-xl:  0 16px 40px rgb(var(--shadow-color) / 0.14), ...;
--shadow-2xl: 0 24px 64px rgb(var(--shadow-color) / 0.18), ...;
--shadow-inner:    inset 0 2px 4px rgb(var(--shadow-color) / 0.05);
--shadow-inset-sm: inset 0 1px 2px rgb(var(--shadow-color) / 0.08);
--shadow-focus: ...;
--shadow-focus-danger: ...;

<!-- Dark mode shadow overrides (lines 698-714) — MUST update in same edit -->

<!-- Current border token -->
--color-border: var(--stone-400);  /* light mode, line 291 */
--color-border: oklch(0.210 0.010 78);  /* dark mode, line 605 */

<!-- Shadow rename map (locked decision):
  old --shadow-xs  → new --shadow-sm  (cards, panels — near-zero elevation)
  old --shadow-md  → new --shadow-md  (dropdowns, popovers — unchanged name)
  old --shadow-xl  → new --shadow-lg  (modals, dialogs)
  DROP: --shadow-2xs, old --shadow-sm, --shadow (unnumbered), old --shadow-lg, --shadow-2xl
  KEEP: --shadow-inner, --shadow-inset-sm, --shadow-focus, --shadow-focus-danger
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token surgery in design-system.css — radius, shadow, border-alpha</name>
  <files>public/assets/css/design-system.css</files>
  <read_first>
    public/assets/css/design-system.css (lines 258-268 for radius primitives, 406-428 for shadow tokens, 526-536 for component aliases, 695-714 for dark mode shadows, 2853-2856 for utility classes, 3792 for shadow-xs usage, 4257 for shadow-xl fallback)
  </read_first>
  <action>
**A. Radius consolidation (in :root block):**
1. Add `--radius-base: var(--radius-lg);` after the primitives block (after line 268). This gives --radius-base = 8px.
2. Update legacy alias: `--radius: var(--radius-base);` (line 268)
3. Remove the entire component alias block (lines 526-536): delete --radius-btn, --radius-input, --radius-badge, --radius-chip, --radius-card, --radius-panel, --radius-modal, --radius-toast, --radius-tooltip, --radius-avatar, --radius-tag.
4. Update all radius USAGE sites within design-system.css itself:
   - `var(--radius-btn)` at lines 1466, 1594 → `var(--radius-base)`
   - `var(--radius-card)` at lines 1642, 1675 → `var(--radius-base)` (line 1675 partial: `0 0 var(--radius-base) var(--radius-base)`)
   - `var(--radius-input)` at line 1779 → `var(--radius-base)`
   - `var(--radius-toast)` at line 2229 → `var(--radius-base)`
   - `var(--radius-xl)` at line 2114 → `var(--radius-base)`
   - `var(--radius-sm)` at lines 917, 934, 1044, 1190, 1588, 1789 → `var(--radius-base)` (these are small rounding contexts that should now share the unified 8px)
   - `var(--radius-md)` at line 1381 → `var(--radius-base)`
5. DO NOT change `border-radius: 50%` anywhere — those are intentional circles.
6. DO NOT change `var(--radius-full)` usages — those are pill shapes.

**B. Shadow scale reduction (in :root block, lines 406-428):**
1. Redefine the three surviving tokens with these EXACT values:
   ```
   --shadow-sm:  0 1px 2px rgb(var(--shadow-color) / 0.06),
                 0 1px 1px rgb(var(--shadow-color) / 0.04);
   --shadow-md:  0 4px 12px rgb(var(--shadow-color) / 0.10),
                 0 2px 4px rgb(var(--shadow-color) / 0.06);
   --shadow-lg:  0 16px 40px rgb(var(--shadow-color) / 0.14),
                 0 6px 16px rgb(var(--shadow-color) / 0.08);
   ```
   (new --shadow-sm = old --shadow-xs values; --shadow-md unchanged; new --shadow-lg = old --shadow-xl values)
2. Remove: --shadow-2xs, --shadow-xs, old --shadow (unnumbered), --shadow-2xl, and the OLD --shadow-sm and --shadow-lg lines that will be replaced.
3. Keep: --shadow-inner, --shadow-inset-sm, --shadow-focus, --shadow-focus-danger exactly as-is.

**C. Dark mode shadow overrides (lines 698-714):**
Update simultaneously to match new 3-level scale:
```
--shadow-sm:  0 1px 2px rgb(var(--shadow-color) / 0.20),
              0 1px 1px rgb(var(--shadow-color) / 0.12);
--shadow-md:  0 4px 12px rgb(var(--shadow-color) / 0.34),
              0 2px 4px rgb(var(--shadow-color) / 0.20);
--shadow-lg:  0 16px 40px rgb(var(--shadow-color) / 0.50),
              0 6px 16px rgb(var(--shadow-color) / 0.26);
```
Remove: --shadow-2xs, --shadow-xs, old --shadow-sm, old --shadow-lg, --shadow-2xl dark mode overrides.
Keep: --shadow-inner, --shadow-inset-sm as-is.

**D. Shadow usage updates within design-system.css:**
- `var(--shadow-xs)` at line 3792 → `var(--shadow-sm)`
- `var(--shadow-xl)` at lines 2115, 2184 → `var(--shadow-lg)`
- `var(--shadow-xl, 0 20px 60px rgba(0,0,0,.15))` at line 4257 → `var(--shadow-lg)`
- Utility classes (lines 2853-2856): update `.shadow-sm`, `.shadow-md`, `.shadow-lg` to reference new tokens. Remove `.shadow` (unnumbered class at line 2854 using `var(--shadow)`) — or remap it to `var(--shadow-sm)` if preferred.

**E. Alpha-based border token:**
1. In :root block, add: `--color-border-alpha: oklch(0 0 0 / 0.08);`
2. In [data-theme="dark"] block, add: `--color-border-alpha: oklch(1 1 0 / 0.08);`
3. Do NOT replace `--color-border` usages globally in this task — only define the token. Per-page CSS files will adopt it in task 2 where appropriate (structural cards/panels only).
  </action>
  <verify>
    <automated>grep -c "^\s*--shadow-2xs\|^\s*--shadow-xs\|^\s*--shadow-2xl" public/assets/css/design-system.css | grep "^0$" && grep -c "^\s*--radius-btn\|^\s*--radius-card\|^\s*--radius-modal\|^\s*--radius-tag" public/assets/css/design-system.css | grep "^0$" && grep -c "\-\-radius-base" public/assets/css/design-system.css && grep -c "\-\-color-border-alpha" public/assets/css/design-system.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "--radius-base" design-system.css returns at least 5 matches (definition + usages)
    - grep "--radius-btn\|--radius-card\|--radius-panel\|--radius-modal\|--radius-toast\|--radius-tooltip\|--radius-tag\|--radius-input" design-system.css returns 0 matches
    - grep "--shadow-2xs\|--shadow-xs\|--shadow-2xl" design-system.css returns 0 matches (definitions removed)
    - grep "--shadow-xl" design-system.css returns 0 matches (all remapped to --shadow-lg)
    - grep "--color-border-alpha" design-system.css returns at least 2 matches (light + dark)
    - grep "--shadow-inner" design-system.css still returns matches (kept)
    - grep "--shadow-focus" design-system.css still returns matches (kept)
  </acceptance_criteria>
  <done>design-system.css has --radius-base as sole component radius, 3-level shadow scale (sm/md/lg), and --color-border-alpha defined in both light and dark modes. All internal usages updated.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate tokens to per-page CSS files and Web Component fallback literals</name>
  <files>
    public/assets/css/pages.css
    public/assets/css/analytics.css
    public/assets/css/meetings.css
    public/assets/css/help.css
    public/assets/css/app.css
    public/assets/css/audit.css
    public/assets/css/validate.css
    public/assets/css/email-templates.css
    public/assets/css/hub.css
    public/assets/css/operator.css
    public/assets/css/wizard.css
    public/assets/css/login.css
    public/assets/css/members.css
    public/assets/css/public.css
    public/assets/css/users.css
    public/assets/css/landing.css
    public/assets/js/components/ag-kpi.js
    public/assets/js/components/ag-modal.js
    public/assets/js/components/ag-toast.js
    public/assets/js/components/ag-vote-button.js
    public/assets/js/components/ag-pdf-viewer.js
    public/assets/js/components/ag-tz-picker.js
    public/assets/js/components/ag-searchable-select.js
    public/assets/js/components/ag-quorum-bar.js
    public/assets/js/components/ag-pagination.js
    public/assets/js/components/ag-tooltip.js
    public/assets/js/components/ag-confirm.js
    public/assets/js/components/ag-page-header.js
    public/assets/js/components/ag-time-input.js
  </files>
  <read_first>
    public/assets/css/pages.css (lines 504, 670, 1049 for shadow usages)
    public/assets/css/audit.css (lines 437, 601 for shadow usages)
    public/assets/css/validate.css (line 88 for --shadow-2xl)
    public/assets/css/email-templates.css (line 69, and search for --shadow-2xl)
    public/assets/css/login.css (lines 60, 69, 497 for --shadow-xl)
    public/assets/css/members.css (line 1011 for --shadow-xl)
    public/assets/css/public.css (line 659 for --shadow-xl)
    public/assets/css/operator.css (line 1142 for --shadow-xl)
    public/assets/css/hub.css (lines 19, 40, 431 for hardcoded border-radius)
    public/assets/js/components/ag-kpi.js (line 54 for radius fallback)
    public/assets/js/components/ag-modal.js (lines 116, 144 for radius/shadow)
    public/assets/js/components/ag-vote-button.js (line 89)
    public/assets/js/components/ag-pdf-viewer.js (lines 124, 144, 235)
    public/assets/js/components/ag-searchable-select.js (lines 262, 317, 341, 452)
    public/assets/js/components/ag-quorum-bar.js (line 63)
    public/assets/js/components/ag-pagination.js (line 71)
    public/assets/js/components/ag-tooltip.js (line 38)
    public/assets/js/components/ag-time-input.js (line 123)
    public/assets/js/components/ag-confirm.js (line 107)
    public/assets/js/components/ag-page-header.js (line 38)
  </read_first>
  <action>
**A. Shadow token renames in per-page CSS files:**

Remap every dropped/renamed shadow token to the new 3-level vocabulary:

| File | Line | Current | Change to |
|------|------|---------|-----------|
| pages.css | 504 | `var(--shadow-xs)` | `var(--shadow-sm)` |
| audit.css | 601 | `var(--shadow-xl)` | `var(--shadow-lg)` |
| validate.css | 88 | `var(--shadow-2xl)` | `var(--shadow-lg)` |
| login.css | 60 | `var(--shadow-xl)` | `var(--shadow-lg)` |
| login.css | 69 | `var(--shadow-xl)` | `var(--shadow-lg)` |
| members.css | 1011 | `var(--shadow-xl)` | `var(--shadow-lg)` |
| public.css | 659 | `var(--shadow-xl)` | `var(--shadow-lg)` |
| operator.css | 1142 | `var(--shadow-xl)` | `var(--shadow-lg)` |

Also search for any `var(--shadow-2xl)` in email-templates.css and remap to `var(--shadow-lg)`.

Files using `var(--shadow-sm)` (analytics, meetings, help, app, hub, wizard, users, email-templates, members, pages, operator, landing) — these currently reference the OLD --shadow-sm (0 1px 3px / 0.08). After task 1, new --shadow-sm = old --shadow-xs (0 1px 2px / 0.06) which is slightly lighter. This is acceptable per research (near-zero card elevation, nearly identical). NO ACTION NEEDED on these — they already use the correct token name.

Files using `var(--shadow-md)` — unchanged token, no action needed.
Files using `var(--shadow-lg)` (landing, design-system already done) — these currently reference OLD --shadow-lg (0 8px 24px). After rename, new --shadow-lg = old --shadow-xl (0 16px 40px) which is STRONGER. Verify these are modal/dialog contexts. landing.css uses --shadow-lg on cards which is intentional (hero cards with elevation). Leave as-is.

**B. Alpha border adoption on structural cards/panels:**

In pages.css, for dashboard KPI cards (`.kpi-card`) and dashboard panels (`.dashboard-sessions`, `.dashboard-aside`), replace:
```css
border: 1px solid var(--color-border);
```
with:
```css
border: 1px solid var(--color-border-alpha);
```

Do NOT change borders on interactive elements (inputs, form fields, buttons with focus states).

**C. Web Component radius fallback updates:**

Update every Web Component Shadow DOM style to use `var(--radius-base, 8px)` instead of removed alias tokens or wrong fallback values:

| Component | Line | Current | Change to |
|-----------|------|---------|-----------|
| ag-kpi.js | 54 | `var(--radius-lg, 16px)` | `var(--radius-base, 8px)` |
| ag-modal.js | 116 | `var(--radius-modal, 12px)` | `var(--radius-base, 8px)` |
| ag-modal.js | 144 | `var(--radius-sm, 4px)` | `var(--radius-base, 8px)` |
| ag-toast.js | 95 | `var(--radius-toast, 8px)` | `var(--radius-base, 8px)` |
| ag-toast.js | 132 | `var(--radius-sm, 4px)` | `var(--radius-base, 8px)` |
| ag-vote-button.js | 89 | `var(--radius-lg, 16px)` | `var(--radius-base, 8px)` |
| ag-pdf-viewer.js | 124 | `var(--radius, 6px)` | `var(--radius-base, 8px)` |
| ag-pdf-viewer.js | 144 | `var(--radius-lg, 16px) var(--radius-lg, 16px) 0 0` | `var(--radius-base, 8px) var(--radius-base, 8px) 0 0` |
| ag-pdf-viewer.js | 235 | `var(--radius-sm, 6px)` | `var(--radius-base, 8px)` |
| ag-tz-picker.js | 66 | `var(--radius-sm, 8px)` | `var(--radius-base, 8px)` |
| ag-searchable-select.js | 262 | `var(--radius-md, 8px)` | `var(--radius-base, 8px)` |
| ag-searchable-select.js | 317 | `0 0 var(--radius-md, 8px) var(--radius-md, 8px)` | `0 0 var(--radius-base, 8px) var(--radius-base, 8px)` |
| ag-searchable-select.js | 341 | `var(--radius-sm, 6px)` | `var(--radius-base, 8px)` |
| ag-searchable-select.js | 452 | `2px` (hardcoded) | `var(--radius-base, 8px)` |
| ag-quorum-bar.js | 63 | `var(--radius-lg, 16px)` | `var(--radius-base, 8px)` |
| ag-pagination.js | 71 | `5px` (hardcoded) | `var(--radius-base, 8px)` |
| ag-tooltip.js | 38 | `5px` (hardcoded) | `var(--radius-base, 8px)` |
| ag-confirm.js | 107 | `var(--radius, 0.5rem)` | `var(--radius-base, 8px)` |
| ag-page-header.js | 38 | `2px` (hardcoded) | keep as-is (decorative accent line, not a component corner)
| ag-time-input.js | 123 | `var(--radius-sm, 8px)` | `var(--radius-base, 8px)` |

DO NOT change `border-radius: 9999px` or `border-radius: 50%` in any Web Component — those are pill/circle shapes.
Keep `var(--radius-full, 9999px)` references (ag-toast.js line 105, ag-badge.js line 60) unchanged.
Keep `var(--radius-badge, 9999px)` — leave as-is since it maps to --radius-full.

**D. Web Component shadow fallback updates:**

| Component | Line | Current | Change to |
|-----------|------|---------|-----------|
| ag-modal.js | 117 | `var(--shadow-xl, var(--shadow-lg))` | `var(--shadow-lg)` |

Search all Web Components for any remaining `--shadow-xl`, `--shadow-2xl`, `--shadow-xs` references and remap.

**E. Hardcoded border-radius in hub.css:**
Check hub.css lines 19, 40, 431 for hardcoded px values (12px, 14px, 6px). Replace with `var(--radius-base)` where they represent component corners. Keep 50% for circles.
  </action>
  <verify>
    <automated>grep -rn "shadow-xl\|shadow-2xl\|shadow-xs\|shadow-2xs" public/assets/css/*.css public/assets/js/components/*.js | grep -v "design-system.css" | grep -v "shadow-inset-sm" | wc -l | grep "^0$" && grep -rn "radius-btn\|radius-card\|radius-panel\|radius-modal\|radius-toast\|radius-tooltip\|radius-tag\|radius-input" public/assets/js/components/*.js | wc -l | grep "^0$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "shadow-xl\|shadow-2xl\|shadow-xs\|shadow-2xs" across all per-page CSS and component JS returns 0 matches
    - grep -rn "radius-btn\|radius-card\|radius-modal\|radius-toast\|radius-tooltip\|radius-tag\|radius-input" across all component JS returns 0 matches
    - grep -c "radius-base, 8px" across all component JS returns at least 15 matches
    - grep "color-border-alpha" pages.css returns at least 1 match (KPI card borders)
    - hub.css has no hardcoded 12px or 14px border-radius values on component corners
  </acceptance_criteria>
  <done>All per-page CSS files use only --shadow-sm/md/lg vocabulary. All Web Components use var(--radius-base, 8px) with correct fallback. Dashboard structural borders use --color-border-alpha. No removed token references remain anywhere outside design-system.css.</done>
</task>

</tasks>

<verification>
1. `grep -rn "shadow-2xs\|shadow-xs\b\|shadow-2xl" public/assets/css/ public/assets/js/components/` returns 0 results
2. `grep -rn "shadow-xl" public/assets/css/ public/assets/js/components/` returns 0 results
3. `grep -rn "radius-btn\|radius-card\|radius-panel\|radius-modal\|radius-toast\|radius-tooltip\|radius-tag\|radius-input" public/assets/` returns 0 results
4. `grep -c "radius-base" public/assets/css/design-system.css` returns 10+ matches
5. `grep -c "color-border-alpha" public/assets/css/design-system.css` returns at least 2
</verification>

<success_criteria>
- Single --radius-base (8px) token controls all component border-radius
- Shadow vocabulary is exactly sm/md/lg (plus inner/inset/focus utilities)
- --color-border-alpha defined in light and dark modes
- All Web Component fallback literals updated to 8px
- Zero references to removed tokens anywhere in per-page CSS or components
</success_criteria>

<output>
After completion, create `.planning/phases/83-component-geometry-chrome-cleanup/83-01-SUMMARY.md`
</output>
