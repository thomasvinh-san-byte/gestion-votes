---
phase: 83-component-geometry-chrome-cleanup
plan: 02
type: execute
wave: 2
depends_on: [83-01]
files_modified:
  - public/assets/css/pages.css
  - public/dashboard.htmx.html
  - public/assets/js/pages/dashboard.js
autonomous: true
requirements: [COMP-04]

must_haves:
  truths:
    - "Dashboard KPI cards show a shimmer animation while API data loads instead of static dashes"
    - "Dashboard session list shows shimmer placeholders while loading"
    - "Shimmer respects prefers-reduced-motion (static gray placeholder, no animation)"
    - "Once data loads, skeleton disappears and real KPI values + session cards appear"
    - "No double-render: user never sees both skeleton and real content simultaneously"
  artifacts:
    - path: "public/assets/css/pages.css"
      provides: ".skeleton-kpi class for KPI card shimmer dimensions"
      contains: "skeleton-kpi"
    - path: "public/dashboard.htmx.html"
      provides: "KPI skeleton placeholder divs with .loading class on container"
      contains: "skeleton-kpi"
    - path: "public/assets/js/pages/dashboard.js"
      provides: "Remove .loading class after successful API response"
      contains: "loading"
  key_links:
    - from: "dashboard.htmx.html"
      to: "pages.css"
      via: ".loading class toggles skeleton visibility"
      pattern: "dashboard-kpis.*loading"
    - from: "dashboard.js"
      to: "dashboard.htmx.html"
      via: "classList.remove('loading') after API success"
      pattern: "classList.*remove.*loading"
    - from: "pages.css"
      to: "design-system.css"
      via: ".skeleton base class provides shimmer animation"
      pattern: "\\.skeleton-kpi"
---

<objective>
Add skeleton shimmer loading placeholders to dashboard KPI cards and enhance existing session list skeletons. CSS-only shimmer animation with JS class toggle.

Purpose: Layout-preserving loading state eliminates the jarring spinner-to-content flash on dashboard load.
Output: Shimmer visible during API fetch, seamless swap to real content on success.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-component-geometry-chrome-cleanup/83-CONTEXT.md
@.planning/phases/83-component-geometry-chrome-cleanup/83-RESEARCH.md
@.planning/phases/83-component-geometry-chrome-cleanup/83-01-SUMMARY.md

<interfaces>
<!-- Existing skeleton infrastructure in design-system.css (lines 3526-3566) -->
.skeleton {
  background: linear-gradient(90deg,
    var(--color-bg-subtle) 25%,
    var(--color-border) 50%,
    var(--color-bg-subtle) 75%
  );
  background-size: 200% 100%;
  animation: skeleton-shimmer 1.5s ease-in-out infinite;
  border-radius: var(--radius);
}
@keyframes skeleton-shimmer {
  0%   { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

<!-- app.css line 392 — children hidden during skeleton -->
.skeleton * { visibility: hidden; }

<!-- Existing skeleton-session in pages.css (lines 1112-1118) -->
.skeleton-session { height: 60px; }

<!-- Existing session list skeleton HTML in dashboard.htmx.html (lines 142-145) -->
<div class="skeleton skeleton-session"></div> (x3)

<!-- KPI card HTML structure (dashboard.htmx.html lines 86-123) -->
<div class="dashboard-kpis">
  <ag-tooltip ...><a class="kpi-card kpi-card--1">...</a></ag-tooltip>
  <ag-tooltip ...><a class="kpi-card kpi-card--2">...</a></ag-tooltip>
  <ag-tooltip ...><a class="kpi-card kpi-card--3">...</a></ag-tooltip>
  <ag-tooltip ...><a class="kpi-card kpi-card--4">...</a></ag-tooltip>
</div>

<!-- dashboard.js loadDashboard() — sets KPI values at lines 120-127 -->
el = document.getElementById('kpiSeances'); if (el) el.textContent = upcoming.length;
el = document.getElementById('kpiEnCours'); if (el) el.textContent = live.length;
el = document.getElementById('kpiConvoc');  if (el) el.textContent = 0;
el = document.getElementById('kpiPV');      if (el) el.textContent = ...;

<!-- prefers-reduced-motion already handled globally at design-system.css line 2956 -->
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
  }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skeleton shimmer to dashboard KPI cards and session list</name>
  <files>
    public/assets/css/pages.css
    public/dashboard.htmx.html
    public/assets/js/pages/dashboard.js
  </files>
  <read_first>
    public/assets/css/pages.css (lines 1034-1120 for KPI card styles and existing skeleton-session)
    public/dashboard.htmx.html (lines 85-145 for KPI cards and session list HTML)
    public/assets/js/pages/dashboard.js (full file — understand loadDashboard flow, especially lines 96-175)
    public/assets/css/design-system.css (lines 3526-3566 for .skeleton base class)
    public/assets/css/app.css (line 392 for .skeleton * visibility)
  </read_first>
  <action>
**A. CSS — Add skeleton-kpi class and loading-state rules to pages.css:**

After the existing `.skeleton-session` block (around line 1118), add:

```css
/* Skeleton shimmer for KPI cards */
.skeleton-kpi {
  height: 88px;               /* matches kpi-card approx height */
  min-width: 0;
  border-radius: var(--radius-base);
}

/* Loading state: show skeletons, hide real content */
.dashboard-kpis.loading .kpi-card-wrapper {
  display: none;
}
.dashboard-kpis.loading .skeleton-kpi {
  display: block;
}

/* Loaded state: hide skeletons, show real content */
.dashboard-kpis:not(.loading) .skeleton-kpi {
  display: none;
}

/* Session list loading state */
.dashboard-sessions__list.loading .session-card-link {
  display: none;
}
```

The `.skeleton` base class from design-system.css already provides the shimmer gradient animation. The `prefers-reduced-motion` global rule at design-system.css line 2956 already stops the animation — the static gray background block still shows as a layout placeholder. No per-skeleton @media override needed.

**B. HTML — Add skeleton placeholders and .loading class to dashboard.htmx.html:**

1. Add `loading` class to the `.dashboard-kpis` div (line 86):
   ```html
   <div class="dashboard-kpis loading">
   ```

2. Before the first `<ag-tooltip>` inside `.dashboard-kpis` (after the opening `<div class="dashboard-kpis loading">`), add 4 skeleton placeholder divs:
   ```html
   <!-- Skeleton placeholders — removed by JS after API response -->
   <div class="skeleton skeleton-kpi" aria-hidden="true"></div>
   <div class="skeleton skeleton-kpi" aria-hidden="true"></div>
   <div class="skeleton skeleton-kpi" aria-hidden="true"></div>
   <div class="skeleton skeleton-kpi" aria-hidden="true"></div>
   ```

3. Wrap each `<ag-tooltip>` block (containing the KPI card) in a `<div class="kpi-card-wrapper">` so the CSS can hide them during loading:
   ```html
   <div class="kpi-card-wrapper">
     <ag-tooltip text="Sessions AG planifi&eacute;es..." position="bottom">
       <a href="/meetings" class="kpi-card kpi-card--1" ...>...</a>
     </ag-tooltip>
   </div>
   ```
   Repeat for all 4 KPI cards.

4. Add `loading` class to the `#prochaines` session list div (line 140):
   ```html
   <div id="prochaines" class="dashboard-sessions__list loading" aria-live="polite">
   ```

**C. JavaScript — Remove .loading class after API success in dashboard.js:**

1. After the KPI value assignments (after line 127, where `kpiPV` is set), add:
   ```javascript
   // Remove loading state — show real KPI cards, hide skeletons
   var kpisContainer = document.querySelector('.dashboard-kpis');
   if (kpisContainer) kpisContainer.classList.remove('loading');
   ```

2. After the session list `prochaines.innerHTML = ...` assignments (both the empty state at line 146 and the populated state at line 155), add:
   ```javascript
   prochaines.classList.remove('loading');
   ```
   This should go right after each `prochaines.innerHTML = ` assignment. Put it after line 146 (empty state) and after line 155 (populated state).

3. In the `showDashboardError()` function (line 179), also remove loading states so the error banner is visible:
   ```javascript
   var kpisContainer = document.querySelector('.dashboard-kpis');
   if (kpisContainer) kpisContainer.classList.remove('loading');
   var prochaines = document.getElementById('prochaines');
   if (prochaines) prochaines.classList.remove('loading');
   ```
   Add this at the start of `showDashboardError()`, before the toast call.

**Anti-patterns to avoid:**
- Do NOT add `hx-indicator` — KPI cards are JS-fetched, not HTMX
- Do NOT create a new @keyframes — reuse the existing `skeleton-shimmer` from .skeleton base class
- Do NOT add per-skeleton `@media (prefers-reduced-motion)` — the global rule handles it
- Do NOT remove the existing skeleton-session divs in the HTML — they already work for the session list
  </action>
  <verify>
    <automated>grep -c "skeleton-kpi" public/assets/css/pages.css && grep -c "skeleton-kpi" public/dashboard.htmx.html && grep "classList.remove.*loading" public/assets/js/pages/dashboard.js | head -3 && grep "dashboard-kpis loading" public/dashboard.htmx.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "skeleton-kpi" pages.css returns at least 3 matches (class definition + show/hide rules)
    - grep "skeleton-kpi" dashboard.htmx.html returns exactly 4 matches (4 KPI skeleton divs)
    - grep "dashboard-kpis loading" dashboard.htmx.html returns 1 match (initial loading class)
    - grep "classList.remove.*loading" dashboard.js returns at least 2 matches (KPI + session list)
    - grep "kpi-card-wrapper" dashboard.htmx.html returns exactly 4 matches
    - The existing 3 skeleton-session divs in #prochaines are still present
    - No new @keyframes added (reuses existing skeleton-shimmer)
  </acceptance_criteria>
  <done>Dashboard shows 4 shimmer KPI placeholders and 3 shimmer session placeholders on page load. After API response, skeletons disappear and real KPI values + session cards appear. prefers-reduced-motion shows static gray blocks. Error state also clears loading class.</done>
</task>

</tasks>

<verification>
1. `grep -c "skeleton-kpi" public/assets/css/pages.css` returns 3+
2. `grep -c "skeleton-kpi" public/dashboard.htmx.html` returns 4
3. `grep "classList.remove.*loading" public/assets/js/pages/dashboard.js` returns 2+ matches
4. `grep "dashboard-kpis loading" public/dashboard.htmx.html` returns 1 match
5. No new `@keyframes` in pages.css (reuses existing .skeleton animation)
</verification>

<success_criteria>
- Dashboard KPI area shows 4 shimmer blocks during load instead of "-" placeholder text
- Session list area shows 3 shimmer blocks during load (existing behavior enhanced)
- After API response, real values appear and skeletons disappear
- prefers-reduced-motion: static gray placeholder shown (no animation, verified by global rule)
- Error state clears loading class so error banner is visible
</success_criteria>

<output>
After completion, create `.planning/phases/83-component-geometry-chrome-cleanup/83-02-SUMMARY.md`
</output>
