---
phase: 34-quality-assurance-final-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
  - public/assets/css/analytics.css
  - public/assets/css/hub.css
  - public/assets/css/wizard.css
  - public/assets/css/landing.css
  - public/assets/css/archives.css
  - public/assets/css/report.css
  - public/assets/css/app.css
  - public/assets/css/users.css
  - public/assets/css/meetings.css
  - public/assets/css/members.css
  - public/assets/css/public.css
  - public/assets/css/admin.css
  - public/assets/css/trust.css
  - public/assets/css/operator.css
  - public/assets/css/pages.css
autonomous: true
requirements: [QA-01, QA-03, QA-05]

must_haves:
  truths:
    - "Fraunces font appears only on h1/.page-title and brand wordmark (.logo) — never on h2, section headings, card titles, or KPI values"
    - "All CSS transitions are 200ms or under — no 0.3s or 300ms values in any transition property"
    - "All interactive cards and buttons have transform on hover — not just color/shadow change alone"
    - "All pill border-radius values use var(--radius-full) token — no literal 999px or 9999px"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Global h2 font rule fixed, confirm-dialog-title fixed, transition fallback fixed"
      contains: "h2.*font-sans"
    - path: "public/assets/css/hub.css"
      provides: "3 font-display violations fixed, transition violation fixed"
    - path: "public/assets/css/archives.css"
      provides: "Hover transform added to archive cards, radius-full token applied"
  key_links:
    - from: "design-system.css h2 rule"
      to: "All pages with h2 section headings"
      via: "Global CSS cascade"
      pattern: "h2.*font-family.*font-sans"
---

<objective>
Fix all CSS-level QA violations: Fraunces font discipline (QA-03), transition duration limits (QA-05), hover transforms on interactive elements (QA-01), and border-radius token hygiene (QA-01).

Purpose: These are the highest-leverage CSS fixes — the h2 font-display removal alone affects every page in the application.
Output: All page CSS files pass grep checks for font discipline, transition limits, hover transforms, and radius tokens.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-quality-assurance-final-audit/34-CONTEXT.md
@.planning/phases/34-quality-assurance-final-audit/34-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Fraunces font discipline and transition violations</name>
  <files>
    public/assets/css/design-system.css,
    public/assets/css/analytics.css,
    public/assets/css/hub.css,
    public/assets/css/wizard.css,
    public/assets/css/landing.css
  </files>
  <read_first>
    public/assets/css/design-system.css (lines 750-770, 4875-4885, 2320-2326),
    public/assets/css/analytics.css (line 159),
    public/assets/css/hub.css (lines 65-70, 115-125, 430-440, 838-845),
    public/assets/css/wizard.css (line 183),
    public/assets/css/landing.css (lines 86, 150-155)
  </read_first>
  <action>
**Fraunces font fixes (QA-03):**

1. **design-system.css line 762** — `h2, .h2` rule: Remove `font-family: var(--font-display)`. Replace with `font-family: var(--font-sans)`. This is the single highest-impact fix — every page h2 currently renders in Fraunces.

2. **design-system.css line 4880** — `.confirm-dialog-title`: Replace `font-family: var(--font-display, 'Fraunces', serif)` with `font-family: var(--font-sans)`.

3. **analytics.css line 159** — `.overview-card-value`: Replace `font-family: 'Fraunces', serif` with `font-family: var(--font-mono)` (KPI numeric values belong in monospace).

4. **hub.css line 68** — `.hub-identity-date`: Replace `font-family: var(--font-display)` with `font-family: var(--font-sans)`.

5. **hub.css line 435** — `.hub-action-title`: Replace `font-family: var(--font-display)` with `font-family: var(--font-sans)`.

6. **hub.css line 841** — `.hub-kpi-value-num`: Replace `font-family: var(--font-display)` with `font-family: var(--font-mono)` (KPI number = monospace).

7. **wizard.css line 183** — `.wf-step`: Replace `font-family: var(--font-display)` with `font-family: var(--font-sans)`.

8. **landing.css line 152** — `.login-title`: Replace `font-family: var(--font-display, 'Fraunces', serif)` with `font-family: var(--font-sans)`. (Keep `.hero-title` at line 86 — it IS the page title equivalent on landing.)

**Acceptable exceptions (DO NOT change):**
- design-system.css line 753 — `h1, .h1` (correct)
- design-system.css line 1311 — `.page-title` (correct)
- design-system.css line 890 — `.logo` (brand wordmark)
- landing.css line 86 — `.hero-title` (page title equivalent)
- partials/sidebar.html line 14 — AG-VOTE brand wordmark (handled in plan 02 but kept as brand exception)

**public.css lines 55, 922** — `.projection-title` and `.motion-title`: Keep these (projector display is a legitimate large-screen presentation context where display font is intentional).

**Transition fixes (QA-05):**

9. **hub.css line 120** — Replace `transition: all 0.3s ease` with:
   ```css
   transition: width var(--duration-moderate) var(--ease-standard),
               opacity var(--duration-normal) var(--ease-standard);
   ```

10. **design-system.css line 2323** — Replace `transition: width var(--duration-normal, 300ms) ease` with:
    ```css
    transition: width var(--duration-normal) ease;
    ```
    (Remove the 300ms fallback — `--duration-normal` is 150ms and is always defined.)
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && echo "=== QA-03: Font violations ===" && grep -n "font-display" public/assets/css/analytics.css public/assets/css/hub.css public/assets/css/wizard.css && echo "FAIL: font-display still found" || echo "PASS: no font-display in page CSS" && echo "=== QA-03: h2 rule ===" && grep -A2 "h2.*\.h2" public/assets/css/design-system.css | grep -q "font-sans" && echo "PASS: h2 uses font-sans" || echo "FAIL: h2 not using font-sans" && echo "=== QA-05: Transition violations ===" && grep -rn "0\.3s\|300ms" public/assets/css/hub.css public/assets/css/design-system.css | grep "transition" && echo "FAIL: transition violations remain" || echo "PASS: no transition violations"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "font-display" public/assets/css/analytics.css public/assets/css/hub.css public/assets/css/wizard.css returns zero matches
    - grep -A2 "h2.*\.h2" public/assets/css/design-system.css shows font-sans not font-display
    - grep "confirm-dialog-title" public/assets/css/design-system.css -A3 shows font-sans
    - grep -rn "transition.*0\.3s\|transition.*300ms" public/assets/css/ returns zero matches
    - landing.css .login-title uses font-sans, .hero-title still uses font-display
  </acceptance_criteria>
  <done>All 8 Fraunces violations fixed (h2 global, confirm-dialog-title, analytics KPI, 3x hub, wizard step, login-title). Both transition violations fixed (hub 300ms, design-system 300ms fallback). public.css projection titles intentionally kept.</done>
</task>

<task type="auto">
  <name>Task 2: Add hover transforms and fix radius token hygiene</name>
  <files>
    public/assets/css/archives.css,
    public/assets/css/report.css,
    public/assets/css/app.css,
    public/assets/css/users.css,
    public/assets/css/meetings.css,
    public/assets/css/members.css,
    public/assets/css/public.css,
    public/assets/css/admin.css,
    public/assets/css/trust.css,
    public/assets/css/operator.css,
    public/assets/css/pages.css
  </files>
  <read_first>
    public/assets/css/archives.css (lines 17-25, 127-135, 195-200),
    public/assets/css/report.css (lines 25-30),
    public/assets/css/app.css (lines 297-305),
    public/assets/css/users.css (lines 190-200, 220-225),
    public/assets/css/meetings.css (lines 283-290, 335-340),
    public/assets/css/members.css (lines 193-197, 421-425),
    public/assets/css/public.css (lines 885-890),
    public/assets/css/admin.css (lines 304-310),
    public/assets/css/trust.css (lines 198-203, 300-305),
    public/assets/css/operator.css (lines 3490-3497),
    public/assets/css/pages.css (lines 746-750)
  </read_first>
  <action>
**Hover transform fixes (QA-01 check 6):**

Add `transform: translateY(-1px)` and include `transform` in the transition shorthand for these hover selectors:

1. **archives.css ~line 21** — `.archive-card:hover`: Add `transform: translateY(-1px);` and update transition to include `transform var(--duration-fast)`.

2. **archives.css ~line 131** — `.archive-card-enhanced:hover`: Add `transform: translateY(-1px);` and update transition to include transform.

3. **report.css ~line 27** — `.export-btn:hover`: Add `transform: translateY(-1px);` and update transition to include transform.

4. **app.css ~line 301** — `.quick-action:hover`: Add `transform: translateY(-1px);` and update transition to include transform.

**DO NOT add transform to:**
- `.admin-tab:hover` (tabs are intentionally flat)
- `.user-row:hover` (table rows use background tint only)

**Border-radius token hygiene (QA-01 check 2):**

Replace all literal `999px` and `9999px` values with `var(--radius-full)`:

5. **archives.css line 197** — `.archive-badge`: `border-radius: 999px` -> `border-radius: var(--radius-full)`
6. **trust.css line 200** — replace `999px` -> `var(--radius-full)`
7. **trust.css line 303** — replace `999px` -> `var(--radius-full)`
8. **pages.css line 748** — replace `999px` -> `var(--radius-full)`
9. **users.css line 196** — replace `9999px` -> `var(--radius-full)`
10. **users.css line 223** — replace `9999px` -> `var(--radius-full)`
11. **meetings.css line 285** — replace `9999px` -> `var(--radius-full)`
12. **meetings.css line 337** — replace `9999px` -> `var(--radius-full)`
13. **members.css line 195** — replace `9999px` -> `var(--radius-full)`
14. **members.css line 423** — replace `9999px` -> `var(--radius-full)`
15. **public.css line 887** — replace `9999px` -> `var(--radius-full)`
16. **admin.css line 306** — replace `9999px` -> `var(--radius-full)`
17. **operator.css line 3494** — replace `9999px` -> `var(--radius-full)`
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && echo "=== QA-01: Hover transforms ===" && for f in archives.css report.css app.css; do echo "--- $f ---"; grep -A5 ":hover" public/assets/css/$f | grep -q "translateY" && echo "PASS" || echo "FAIL: missing translateY"; done && echo "=== Radius tokens ===" && grep -rn "border-radius:.*999" public/assets/css/ && echo "FAIL: literal 999px still found" || echo "PASS: all radius values tokenized"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "border-radius:.*999" public/assets/css/ returns zero matches
    - grep -A5 "archive-card:hover" public/assets/css/archives.css shows translateY(-1px)
    - grep -A5 "archive-card-enhanced:hover" public/assets/css/archives.css shows translateY(-1px)
    - grep -A5 "export-btn:hover" public/assets/css/report.css shows translateY(-1px)
    - grep -A5 "quick-action:hover" public/assets/css/app.css shows translateY(-1px)
  </acceptance_criteria>
  <done>4 hover transform violations fixed (archive-card, archive-card-enhanced, export-btn, quick-action). 13 literal pill radius values replaced with var(--radius-full) across 11 CSS files.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rn "font-display" public/assets/css/ | grep -v "design-system.css" | grep -v "public.css" | grep -v "landing.css:86"` — should return zero matches (only acceptable exceptions remain)
2. `grep -rn "transition.*0\.3s\|transition.*300ms" public/assets/css/` — zero matches
3. `grep -rn "border-radius:.*999" public/assets/css/` — zero matches
4. Spot-check archives.css, report.css, app.css hover rules include translateY
</verification>

<success_criteria>
- All 8 Fraunces font violations fixed, only h1/.page-title/.logo/hero-title/projection-title retain font-display
- Both transition violations fixed (hub 300ms, design-system 300ms fallback)
- 4 hover transform violations fixed with translateY(-1px)
- 13 literal pill radius values replaced with var(--radius-full)
</success_criteria>

<output>
After completion, create `.planning/phases/34-quality-assurance-final-audit/34-01-SUMMARY.md`
</output>
