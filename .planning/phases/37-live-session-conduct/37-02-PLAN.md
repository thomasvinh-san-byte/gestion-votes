---
phase: 37-live-session-conduct
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/vote.css
  - public/vote.htmx.html
  - public/assets/js/pages/vote-ui.js
autonomous: true
requirements: [SEC-05]

must_haves:
  truths:
    - "Vote buttons are full-width stacked 1x4 with 88px height and icon + label layout"
    - "Confirmation state shows a large animated checkmark with the voter's choice and irreversibility notice"
    - "Waiting state shows a calming pulse ring animation with descriptive text"
    - "Vote buttons have immediate visual feedback on press (< 50ms perceived)"
    - "All voter states work on mobile with 72px+ touch targets and safe-area padding"
  artifacts:
    - path: "public/assets/css/vote.css"
      provides: "1x4 button layout, confirmation animation, waiting pulse ring, button press feedback"
      contains: "confirmReveal"
    - path: "public/vote.htmx.html"
      provides: "Enhanced confirmation state HTML with checkmark icon, choice text, irreversibility notice; waiting state with pulse ring"
      contains: "vote-confirmed-icon"
    - path: "public/assets/js/pages/vote-ui.js"
      provides: "Sets confirmed choice text and color when displaying confirmation state"
      contains: "confirmedChoice"
  key_links:
    - from: "public/assets/js/pages/vote-ui.js"
      to: "public/vote.htmx.html"
      via: "JS sets #confirmedChoice text content when showing confirmation state"
      pattern: "confirmedChoice"
    - from: "public/vote.htmx.html"
      to: "public/assets/css/vote.css"
      via: "New HTML elements styled by new CSS classes"
      pattern: "vote-confirmed-icon|vote-waiting-pulse"
---

<objective>
Visually redesign the Mobile Voter Ballot for focused simplicity and satisfying feedback during live voting.

Purpose: Voters use this page on their phones during live assemblies — it must be dead-simple, instant-feedback, and confidence-building. Apple Wallet simplicity with clear confirmation states.
Output: Full-width stacked vote buttons (1x4), animated confirmation state with checkmark, calming waiting state with pulse ring, enhanced motion display.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-live-session-conduct/37-CONTEXT.md
@.planning/phases/37-live-session-conduct/37-RESEARCH.md

@public/assets/css/vote.css
@public/vote.htmx.html
@public/assets/js/pages/vote-ui.js
@public/assets/js/pages/vote.js
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vote button layout and motion card enhancement</name>
  <files>public/assets/css/vote.css, public/vote.htmx.html</files>
  <read_first>
    - public/assets/css/vote.css (full file — 1813 lines, focus on .vote-buttons, .vote-btn, .vote-btn-for/against/abstain/blank, .motion-card, .motion-title, and all @media queries affecting vote-buttons)
    - public/vote.htmx.html (full file — 339 lines, focus on #vote-buttons section, .vote-btn structure, .motion-card)
    - .planning/phases/37-live-session-conduct/37-RESEARCH.md (Pattern 3 vote button stack, Before/After vote buttons, Pitfall 2 landscape tablet)
  </read_first>
  <action>
**Vote Buttons 1x4 Layout (vote.css):**
- Change `.vote-buttons` grid from `grid-template-columns: 1fr 1fr` to `grid-template-columns: 1fr` (single column).
- Set `grid-template-rows` to `repeat(auto-fill, minmax(88px, auto))` or just remove it and let items flow.
- Keep `gap: 10px; max-width: 480px; margin: 0 auto; width: 100%;`
- Change `.vote-btn` layout:
  - `min-height: 88px` (was 72px)
  - `flex-direction: row` (was column — icon above label becomes icon left of label)
  - `justify-content: flex-start` (was center)
  - `gap: 1.25rem`
  - `padding: 1.25rem 1.5rem`
  - `font-size: var(--text-xl)` (was var(--text-2xl) — slightly smaller works better in row layout)
  - `border-radius: 14px`
  - `text-align: left`
- Keep ALL existing gradient backgrounds, box-shadows, `color-mix()` active states, and ripple transforms on `.vote-btn`.
- Keep existing `aria-pressed` and `.selected` styles.

**Button Press Feedback Enhancement:**
- Add `@keyframes voteBtnPress { 0% { transform: scale(1); } 50% { transform: scale(0.96); } 100% { transform: scale(1); } }`
- Add `.vote-btn:active { animation: voteBtnPress 0.15s ease-out; }` for immediate tactile feedback.
- Keep existing `transition: transform 0.1s` if present — the keyframe animation fires on press, complements it.

**Landscape Tablet Override:**
- Find the `@media (min-width: 769px) and (orientation: landscape)` rule that currently sets `grid-template-columns: 1fr 1fr` on `.vote-buttons`.
- Change it to keep `grid-template-columns: 1fr` (1x4 on all viewports per CONTEXT.md "full-width stacked buttons" without viewport exception).
- Keep the `max-width: 480px` constraint so buttons don't stretch on wide screens.
- For very large screens (desktop, unlikely for voter page), the 480px max-width handles it.

**Motion Card Enhancement:**
- Ensure `.motion-title` uses `clamp()` fluid sizing (already present from v4.1). Verify it is prominent — if smaller than `var(--text-xl)`, increase.
- Add `.motion-number` styling if not present: `font-size: 0.75rem; color: var(--color-text-muted); font-family: var(--font-mono); margin-bottom: 0.25rem;` — this is the motion number badge. If HTML doesn't have a `.motion-number` element, add one in vote.htmx.html as `<span class="motion-number" id="motionNumber"></span>` above the motion title.

**Dark Surface Consideration:**
- Per CONTEXT.md, voter screen should default to slightly darker surface. Add: `.vote-app { --color-surface: var(--color-surface-alt, #f8f9fa); }` in light mode to give a slightly warm/cool surface. In dark mode, the existing dark surface tokens are already appropriate. This is a subtle change — not a full dark mode, just a slightly dimmer light mode.

**vote-btn-icon sizing:**
- Ensure the SVG icon inside `.vote-btn-icon` is sized for row layout: `width: 28px; height: 28px;` (was likely 32px+ in column layout).
- Keep the `.vote-btn-kbd` keyboard hint visible but styled smaller: `font-size: 0.6875rem; position: absolute; top: 0.5rem; right: 0.75rem; opacity: 0.5;`

**Dark mode:** Verify all vote button gradients work in dark mode. The existing color-mix patterns should handle it. Add any missing dark overrides for new classes.
  </action>
  <verify>
    <automated>grep -c "grid-template-columns: 1fr;" public/assets/css/vote.css && grep -c "min-height: 88px\|voteBtnPress\|flex-direction: row" public/assets/css/vote.css</automated>
  </verify>
  <acceptance_criteria>
    - .vote-buttons uses grid-template-columns: 1fr (single column, not 1fr 1fr)
    - .vote-btn has min-height 88px, flex-direction row, justify-content flex-start
    - Vote button press animation (voteBtnPress keyframes) exists
    - Landscape tablet media query no longer forces 2-column grid
    - .motion-title is prominent with clamp() fluid sizing
    - .vote-btn-icon sized at 28px for row layout
    - Dark mode gradients and button colors verified
  </acceptance_criteria>
  <done>Vote buttons render as full-width 1x4 stacked layout with 88px height, icon-left label-right, press animation, on all viewports including landscape tablet. Motion card title is prominent.</done>
</task>

<task type="auto">
  <name>Task 2: Confirmation state, waiting state, and results display</name>
  <files>public/assets/css/vote.css, public/vote.htmx.html, public/assets/js/pages/vote-ui.js</files>
  <read_first>
    - public/vote.htmx.html (focus on #voteConfirmedState, .vote-confirmed-state, .vote-waiting-state sections)
    - public/assets/css/vote.css (focus on .vote-confirmed-state, .vote-confirmed-text, .vote-waiting-state, .vote-waiting-text)
    - public/assets/js/pages/vote-ui.js (search for voteConfirmedState, showConfirmation, showWaiting, or any function that toggles the confirmed/waiting states — understand how JS shows/hides these elements)
    - public/assets/js/pages/vote.js (search for confirmed, waiting — understand the state machine)
    - .planning/phases/37-live-session-conduct/37-RESEARCH.md (Pattern 4 confirmation state, Pattern 5 waiting pulse ring, Before/After specs)
  </read_first>
  <action>
**Confirmation State HTML (vote.htmx.html):**
- Replace the current simple `<p class="vote-confirmed-text">Vote enregistre ...</p>` inside `#voteConfirmedState` with a richer structure:
  ```html
  <div class="vote-confirmed-state" id="voteConfirmedState" hidden>
    <div class="vote-confirmed-icon" aria-hidden="true">
      <svg class="icon icon-xl"><use href="/assets/icons.svg#icon-check"></use></svg>
    </div>
    <p class="vote-confirmed-choice" id="confirmedChoice"></p>
    <p class="vote-confirmed-text">Vote enregistre</p>
    <p class="vote-confirmed-irreversible">Ce vote est definitif et irreversible.</p>
  </div>
  ```
- IMPORTANT: Keep the `id="voteConfirmedState"` and `hidden` attribute — JS controls visibility via this ID.
- Check if the existing HTML already has wrapper div or if it's just a flat `<p>` — adapt accordingly. Do NOT remove any ID that JS uses.

**Confirmation State CSS (vote.css):**
- `.vote-confirmed-state`: `display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 1rem; flex: 1; padding: 2rem; animation: confirmReveal 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);`
- `.vote-confirmed-state[hidden] { display: none !important; }` — preserve hidden behavior.
- `.vote-confirmed-icon`: `width: 80px; height: 80px; border-radius: 50%; background: var(--color-success); color: var(--color-text-inverse); display: flex; align-items: center; justify-content: center;`
- `.vote-confirmed-icon .icon-xl`: `width: 40px; height: 40px;`
- `.vote-confirmed-choice`: `font-size: var(--text-2xl); font-weight: 700; margin: 0;` — color set dynamically by JS based on choice.
- `.vote-confirmed-text`: `font-size: var(--text-lg); color: var(--color-text-muted); margin: 0;`
- `.vote-confirmed-irreversible`: `font-size: var(--text-sm); color: var(--color-text-muted); opacity: 0.7; margin: 0;`
- `@keyframes confirmReveal { from { opacity: 0; transform: scale(0.7); } to { opacity: 1; transform: scale(1); } }`

**Confirmation JS (vote-ui.js):**
- Find the function that shows the confirmation state (likely sets `hidden` attribute or similar).
- Add a line that sets `document.getElementById('confirmedChoice').textContent = choiceLabel;` where choiceLabel is the French label of the vote ("Pour", "Contre", "Abstention", "Blanc").
- Add choice-color mapping: set `confirmedChoice.style.color` to the vote's color:
  - "Pour" / "for" → `var(--color-success)` (green)
  - "Contre" / "against" → `var(--color-danger)` (red)
  - "Abstention" / "abstain" → `var(--color-warning)` (amber)
  - "Blanc" / "blank" → `var(--color-text-muted)` (neutral)
- This is a MINIMAL JS change — one element text set and one style color set. Do NOT restructure the JS state machine.

**Waiting State HTML (vote.htmx.html):**
- Enhance the existing `.vote-waiting-state` content. Replace bare text with:
  ```html
  <div class="vote-waiting-state" id="voteWaitingState" hidden>
    <div class="vote-waiting-pulse" aria-hidden="true"></div>
    <p class="vote-waiting-text">En attente du prochain vote</p>
    <p class="vote-waiting-sub">La seance est en cours</p>
  </div>
  ```
- Keep the `id` and `hidden` attribute for JS control.

**Waiting State CSS (vote.css):**
- `.vote-waiting-state`: `display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 1.5rem; flex: 1; padding: 2rem;`
- `.vote-waiting-state[hidden] { display: none !important; }`
- `.vote-waiting-pulse`: `width: 48px; height: 48px; border-radius: 50%; background: var(--color-primary-subtle); animation: waitingPulse 2.5s ease-in-out infinite;`
- `@keyframes waitingPulse { 0%, 100% { transform: scale(1); opacity: 0.7; } 50% { transform: scale(1.3); opacity: 0.3; } }`
- `.vote-waiting-text`: `font-size: var(--text-lg); font-weight: 600; color: var(--color-text); margin: 0;`
- `.vote-waiting-sub`: `font-size: var(--text-sm); color: var(--color-text-muted); margin: 0;`

**Results Display Enhancement:**
- Find the results section in vote.htmx.html (likely `.vote-results` or similar).
- Ensure the ADOPTE/REJETE verdict uses large, bold, colored text: `.vote-verdict { font-size: var(--text-2xl); font-weight: 800; }`, `.vote-verdict.adopted { color: var(--color-success); }`, `.vote-verdict.rejected { color: var(--color-danger); }` — add these CSS rules if not present.
- If result bar charts exist, ensure they use the same colored segment pattern as operator's op-vote-bars.

**Dark mode:** All new classes use CSS custom properties that already have dark mode values. Verify `.vote-confirmed-icon` green background works in dark mode (use dark success token). Add `@media (prefers-color-scheme: dark)` or `.dark` overrides if the existing dark mode mechanism requires them.
  </action>
  <verify>
    <automated>grep -c "vote-confirmed-icon\|confirmReveal\|vote-waiting-pulse\|waitingPulse\|confirmedChoice" public/assets/css/vote.css public/vote.htmx.html public/assets/js/pages/vote-ui.js</automated>
  </verify>
  <acceptance_criteria>
    - vote.htmx.html has a .vote-confirmed-icon div with SVG checkmark, #confirmedChoice paragraph, and .vote-confirmed-irreversible text
    - vote.css has @keyframes confirmReveal with spring cubic-bezier
    - vote.htmx.html has .vote-waiting-pulse div inside .vote-waiting-state
    - vote.css has @keyframes waitingPulse with scale(1.3) at 50%
    - vote-ui.js sets #confirmedChoice text and color based on voter's choice
    - [hidden] rules preserve JS show/hide behavior for both states
    - Dark mode verified for confirmation icon and waiting pulse
  </acceptance_criteria>
  <done>Confirmation state shows animated 80px green checkmark circle, voter's choice in matching color, "Vote enregistre" text, and irreversibility notice. Waiting state shows calming pulse ring with "En attente du prochain vote" text. Results display has prominent verdict text.</done>
</task>

</tasks>

<verification>
- Open voter ballot on mobile viewport (375px width)
- Verify vote buttons are full-width stacked 1x4 at 88px height
- Tap a button to verify press animation
- Verify confirmation state shows animated checkmark with choice color
- Verify waiting state shows pulsing ring
- Test landscape tablet viewport — buttons remain 1-column
- Check dark mode for all states
</verification>

<success_criteria>
- Vote buttons are 1x4 full-width stacked with 88px minimum height and icon-left layout
- Button press gives immediate visual feedback via voteBtnPress animation
- Confirmation state has animated checkmark, choice text in color, and irreversibility notice
- Waiting state has calming pulse ring animation
- Results display has prominent ADOPTE/REJETE verdict
- All states work on mobile with safe-area padding
- Dark mode works for all new elements
</success_criteria>

<output>
After completion, create `.planning/phases/37-live-session-conduct/37-02-SUMMARY.md`
</output>
