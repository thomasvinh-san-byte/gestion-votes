---
phase: 39-admin-data-tables
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/members.htmx.html
  - public/assets/css/members.css
  - public/assets/js/pages/members.js
  - public/users.htmx.html
  - public/assets/css/users.css
  - public/assets/js/pages/users.js
autonomous: true
requirements: [DATA-03, DATA-04]

must_haves:
  truths:
    - "Members stats bar has elevated surface-raised background, mono values, and ag-tooltip on each stat label"
    - "Member avatars are 40px circles (not 36px squares) with role-colored ring"
    - "Member row hover-reveal action buttons have ag-tooltip explaining each action"
    - "Users role panel shows 4 cards with role badge, count, and permission summary"
    - "Users role filter uses pill buttons (not select dropdown)"
    - "User row actions are hover-reveal icon buttons with ag-tooltip (not always-visible text)"
    - "User last-login dates use JetBrains Mono"
    - "Users page has a column header row with ag-tooltip on each column"
  artifacts:
    - path: "public/assets/css/members.css"
      provides: "Stats bar elevation, avatar 40px circle, filter-chip count badge, upload zone polish"
    - path: "public/members.htmx.html"
      provides: "ag-tooltip wraps on stats bar labels"
    - path: "public/assets/js/pages/members.js"
      provides: "Filter chip count badges updated by JS, ag-tooltip on action icons"
    - path: "public/assets/css/users.css"
      provides: "Role panel cards, hover-reveal actions, pill filter tabs, mono last-login, column header row"
    - path: "public/users.htmx.html"
      provides: "Role panel card layout, filter-tab pills replacing select, users-list-header row with ag-tooltip"
    - path: "public/assets/js/pages/users.js"
      provides: "Pill filter click handler, hover-reveal icon action buttons with ag-tooltip, role count rendering"
  key_links:
    - from: "public/members.htmx.html"
      to: "public/assets/css/members.css"
      via: "stats-bar elevation classes, avatar sizing, filter-chip-count"
      pattern: "stat-item.*ag-tooltip"
    - from: "public/users.htmx.html"
      to: "public/assets/js/pages/users.js"
      via: "renderUsersTable emits hover-reveal icon buttons"
      pattern: "user-row-actions.*opacity"
---

<objective>
Redesign Members and Users pages to Linear-quality table density — elevated stats bars, circular avatars with role color, hover-reveal icon actions with tooltips, pill filter buttons, mono dates, and column header row with tooltips.

Purpose: Members and Users are both "people" admin pages with structurally similar row lists. Upgrading them together ensures consistent patterns (hover-reveal actions, role badges, ag-tooltip headers).
Output: 6 files modified — both pages dramatically improved with scannable density, discoverable actions, and self-explanatory column headers.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-admin-data-tables/39-RESEARCH.md

@public/members.htmx.html
@public/assets/css/members.css
@public/assets/js/pages/members.js
@public/users.htmx.html
@public/assets/css/users.css
@public/assets/js/pages/users.js
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Members page — stats bar elevation, circular avatars, action tooltips, filter chip counts</name>
  <files>public/members.htmx.html, public/assets/css/members.css, public/assets/js/pages/members.js</files>
  <read_first>public/members.htmx.html, public/assets/css/members.css, public/assets/js/pages/members.js, public/assets/css/design-system.css</read_first>
  <action>
**HTML changes in members.htmx.html:**
1. Stats bar: Wrap each `.stat-label` text in `<ag-tooltip>` with explanations:
   - "Total" -> `<ag-tooltip text="Nombre total de membres enregistres" position="top">Total</ag-tooltip>`
   - "Actifs" -> `<ag-tooltip text="Membres actifs pouvant voter" position="top">Actifs</ag-tooltip>`
   - "Inactifs" -> `<ag-tooltip text="Membres desactives, exclus du quorum" position="top">Inactifs</ag-tooltip>`
   - "Voix totales" -> `<ag-tooltip text="Somme des ponderations de tous les membres actifs" position="top">Voix totales</ag-tooltip>`
   - "Pouvoir moyen" -> `<ag-tooltip text="Ponderation moyenne par membre actif" position="top">Pouvoir moyen</ag-tooltip>`
   - "Couv. email" -> `<ag-tooltip text="Pourcentage de membres avec adresse email renseignee" position="top">Couv. email</ag-tooltip>`
2. Add a 16px SVG icon above each stat-value in each `.stat-item` (icon-users for Total, icon-check-circle for Actifs, icon-x-circle for Inactifs, icon-vote for Voix, icon-bar-chart for Pouvoir, icon-mail for Email). Use `<svg class="icon stat-icon" aria-hidden="true"><use href="/assets/icons.svg#icon-XXX"></use></svg>`.

**CSS changes in members.css:**
1. Stats bar elevation:
   - `.stats-bar`: change `background` to `var(--color-surface-raised)`, add `border-bottom: 1px solid var(--color-border)`, add `border-radius: var(--radius-lg)`, add `margin-bottom: var(--space-4)`, add `padding: 1.25rem 1.5rem`
   - `.stat-item`: add `gap: 0.375rem` for icon spacing
   - Add `.stat-icon { width: 16px; height: 16px; color: var(--color-text-muted); }`
   - `.stat-value`: add `font-family: var(--font-mono)` (mono numbers)
   - `.stat-item.active .stat-icon`: `color: var(--color-success)`
   - `.stat-item.inactive .stat-icon`: `color: var(--color-text-muted)`

2. Avatar circular upgrade:
   - `.member-avatar`: change from `width: 36px; height: 36px; border-radius: 9px` to `width: 40px; height: 40px; border-radius: 50%`. Add `font-weight: 700` for initials.
   - `.member-card.is-active .member-avatar`: use `box-shadow: 0 0 0 2px var(--color-success)` ring instead of just border

3. Action buttons polish:
   - `.member-action-icon`: change from `width: 30px; height: 30px` to `width: 32px; height: 32px; min-width: 32px; min-height: 32px`

4. Filter chip count badge:
   - Add `.filter-chip .count { display: inline-flex; align-items: center; justify-content: center; min-width: 20px; height: 20px; border-radius: 10px; background: var(--color-bg-subtle); font-size: 0.6875rem; font-family: var(--font-mono); padding: 0 5px; margin-left: 4px; }`
   - Add `.filter-chip.active .count { background: var(--color-primary-subtle); color: var(--color-primary); }`

5. Upload zone polish:
   - `.upload-zone`: increase `padding` to `2.5rem 1.5rem`, add `background: var(--color-bg-subtle)`
   - `.upload-zone-icon .icon`: add `color: var(--color-primary)` always (not just hover)
   - Add `.upload-csv-badge` above upload text: small badge with "CSV" text

**JS changes in members.js:**
1. In the function that renders member cards (renderMember or similar), wrap each action icon button with `<ag-tooltip text="..." position="top">`:
   - Edit icon -> `<ag-tooltip text="Modifier ce membre" position="top">`
   - Delete icon -> `<ag-tooltip text="Supprimer ce membre" position="top">`
   - Link icon -> `<ag-tooltip text="Copier le lien de vote" position="top">`

2. After filtering/rendering, update filter chip count badges. Find the filter function and after computing filtered results, add count badges:
   - Total count for "Tous" chip
   - Active count for "Actifs" chip
   - Inactive count for "Inactifs" chip
   Pattern: `chip.querySelector('.count')` or append `<span class="count">N</span>` if missing.

**Important:** Do NOT change any filter-chip class names or querySelector patterns (Pitfall 5 from RESEARCH). Add `.count` to filter-chip, not rename filter-chip to filter-tab.
  </action>
  <verify>
    <automated>grep -c "ag-tooltip" public/members.htmx.html | head -1 && grep -c "border-radius: 50%" public/assets/css/members.css | head -1 && grep -c "stat-icon" public/assets/css/members.css | head -1</automated>
  </verify>
  <acceptance_criteria>
  - Stats bar has surface-raised background with border-bottom separator
  - Each stat label wrapped in ag-tooltip with French explanation
  - Each stat has a 16px icon above the value
  - Stat values use JetBrains Mono font
  - Member avatars are 40px circles with success ring on active members
  - Action buttons are 32x32px with ag-tooltip on hover
  - Filter chips show count badges that update after filtering
  - Upload zone has subtle background, primary-color icon, increased padding
  </acceptance_criteria>
  <done>Members page stats bar is elevated and self-explanatory, avatars are circular, actions have tooltips, filter chips show counts</done>
</task>

<task type="auto">
  <name>Task 2: Users page — role distribution panel, pill filter, hover-reveal actions, column header row, mono dates</name>
  <files>public/users.htmx.html, public/assets/css/users.css, public/assets/js/pages/users.js</files>
  <read_first>public/users.htmx.html, public/assets/css/users.css, public/assets/js/pages/users.js, public/assets/css/design-system.css</read_first>
  <action>
**HTML changes in users.htmx.html:**
1. Replace the `.roles-explainer` card (lines 58-79) with a role distribution panel. Replace the entire `<div class="card roles-explainer">...</div>` with:
```html
<div class="role-distribution-panel">
  <div class="role-dist-card" data-role="admin">
    <ag-badge variant="primary">Admin</ag-badge>
    <span class="role-dist-count" id="roleCountAdmin">0</span>
    <span class="role-dist-desc">Acces complet au systeme</span>
  </div>
  <div class="role-dist-card" data-role="operator">
    <ag-badge variant="warning">Operateur</ag-badge>
    <span class="role-dist-count" id="roleCountOperator">0</span>
    <span class="role-dist-desc">Gestion des seances et votes</span>
  </div>
  <div class="role-dist-card" data-role="auditor">
    <ag-badge variant="info">Auditeur</ag-badge>
    <span class="role-dist-count" id="roleCountAuditor">0</span>
    <span class="role-dist-desc">Consultation audit et controle</span>
  </div>
  <div class="role-dist-card" data-role="viewer">
    <ag-badge variant="neutral">Observateur</ag-badge>
    <span class="role-dist-count" id="roleCountViewer">0</span>
    <span class="role-dist-desc">Acces en lecture seule</span>
  </div>
</div>
```

2. Replace the `<select id="filterRole">` (lines 88-96) with filter-tab pills:
```html
<div class="filter-tabs" id="roleFilter">
  <button class="filter-tab active" data-role="">Tous</button>
  <button class="filter-tab" data-role="admin">Admin</button>
  <button class="filter-tab" data-role="operator">Operateur</button>
  <button class="filter-tab" data-role="auditor">Auditeur</button>
  <button class="filter-tab" data-role="viewer">Observateur</button>
</div>
```
Remove the form-group-filter wrapper around the old select. Keep the search input and users-count.

3. Add a `.users-list-header` div BEFORE `#usersTableBody` inside `.table-wrapper` (Pitfall 2 — users list is divs, not table):
```html
<div class="users-list-header">
  <span class="ulh-avatar"></span>
  <span class="ulh-name"><ag-tooltip text="Nom complet de l'utilisateur" position="bottom">Utilisateur</ag-tooltip></span>
  <span class="ulh-role"><ag-tooltip text="Role systeme determinant les permissions" position="bottom">Role</ag-tooltip></span>
  <span class="ulh-status"><ag-tooltip text="Compte actif ou desactive" position="bottom">Statut</ag-tooltip></span>
  <span class="ulh-lastlogin"><ag-tooltip text="Date de la derniere connexion reussie" position="bottom">Dernier acces</ag-tooltip></span>
  <span class="ulh-actions"></span>
</div>
```

**CSS changes in users.css:**
1. Role distribution panel:
```css
.role-distribution-panel {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: var(--space-3);
  margin-bottom: var(--space-4);
}
.role-dist-card {
  background: var(--color-surface-raised);
  border: 1px solid var(--color-border-subtle);
  border-radius: var(--radius-lg);
  padding: var(--space-4);
  display: flex;
  flex-direction: column;
  gap: 0.375rem;
}
.role-dist-count {
  font-family: var(--font-mono);
  font-size: var(--text-2xl);
  font-weight: 700;
  line-height: 1;
}
.role-dist-desc {
  font-size: 0.75rem;
  color: var(--color-text-muted);
}
@media (max-width: 768px) {
  .role-distribution-panel { grid-template-columns: repeat(2, 1fr); }
}
```

2. Hover-reveal user actions:
```css
.user-row-actions {
  opacity: 0;
  transition: opacity var(--duration-fast);
}
.user-row:hover .user-row-actions {
  opacity: 1;
}
@media (hover: none) {
  .user-row-actions { opacity: 1; }
}
```

3. Last-login mono:
```css
.user-row-lastlogin {
  font-family: var(--font-mono);
}
```

4. Column header row:
```css
.users-list-header {
  display: flex;
  align-items: center;
  gap: 1rem;
  padding: 0.5rem 1rem;
  border-bottom: 1px solid var(--color-border);
  font-size: 0.75rem;
  font-weight: 600;
  color: var(--color-text-muted);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}
```
Add width rules for each `.ulh-*` span to match `.user-row` column widths (inspect user-row layout in users.css for the exact flex proportions).

5. Remove old `.roles-explainer`, `.roles-explainer-grid`, `.role-explain` CSS rules that are now unused.

**JS changes in users.js:**
1. Replace the `filterRole` select change handler with a click handler on `#roleFilter .filter-tab`:
```javascript
document.querySelectorAll('#roleFilter .filter-tab').forEach(function(btn) {
  btn.addEventListener('click', function() {
    document.querySelector('#roleFilter .filter-tab.active').classList.remove('active');
    btn.classList.add('active');
    currentRoleFilter = btn.dataset.role;
    applyFilters(); // or whatever the current filter function is named
  });
});
```

2. In `renderUsersTable` (or the equivalent render function), change the action buttons from always-visible text to icon buttons with ag-tooltip:
   - "Modifier" text button -> `<ag-tooltip text="Modifier cet utilisateur" position="top"><button class="btn btn-ghost btn-icon btn-sm user-action-btn" data-action="edit" data-id="ID"><svg class="icon" aria-hidden="true"><use href="/assets/icons.svg#icon-edit-2"></use></svg></button></ag-tooltip>`
   - "Desactiver/Activer" text button -> `<ag-tooltip text="Desactiver/Activer le compte" position="top"><button class="btn btn-ghost btn-icon btn-sm user-action-btn" data-action="toggle" data-id="ID"><svg class="icon" aria-hidden="true"><use href="/assets/icons.svg#icon-toggle-left or icon-toggle-right"></use></svg></button></ag-tooltip>`
   - "Supprimer" text button -> `<ag-tooltip text="Supprimer definitivement" position="top"><button class="btn btn-ghost btn-icon btn-sm user-action-btn" data-action="delete" data-id="ID"><svg class="icon" aria-hidden="true"><use href="/assets/icons.svg#icon-trash-2"></use></svg></button></ag-tooltip>`

3. After loading users data, populate role distribution counts:
```javascript
function updateRoleCounts(users) {
  var counts = { admin: 0, operator: 0, auditor: 0, viewer: 0 };
  users.forEach(function(u) { if (counts[u.role] !== undefined) counts[u.role]++; });
  document.getElementById('roleCountAdmin').textContent = counts.admin;
  document.getElementById('roleCountOperator').textContent = counts.operator;
  document.getElementById('roleCountAuditor').textContent = counts.auditor;
  document.getElementById('roleCountViewer').textContent = counts.viewer;
}
```
Call this function after users data loads successfully.

**Important:** Preserve all existing event delegation patterns for action buttons. If actions use event delegation on a parent container, update the selector to match the new icon button structure (data-action attribute on the button).
  </action>
  <verify>
    <automated>grep -c "role-distribution-panel" public/users.htmx.html | head -1 && grep -c "filter-tab" public/users.htmx.html | head -1 && grep -c "users-list-header" public/users.htmx.html | head -1 && grep -c "opacity: 0" public/assets/css/users.css | head -1</automated>
  </verify>
  <acceptance_criteria>
  - Role distribution panel shows 4 cards (Admin/Operateur/Auditeur/Observateur) with ag-badge, user count, and permission description
  - Role filter is pill buttons (filter-tab pattern) replacing the select dropdown
  - User actions are hover-reveal icon buttons (opacity 0 default, 1 on hover) with ag-tooltip
  - Last-login column uses JetBrains Mono font
  - Column header row exists above users list with ag-tooltip on each column name
  - Old roles-explainer card HTML and CSS removed
  - Mobile: role panel wraps to 2-column grid, actions always visible (hover:none)
  </acceptance_criteria>
  <done>Users page has role distribution panel with counts, pill filter buttons, hover-reveal icon actions with tooltips, column headers with tooltips, and mono dates</done>
</task>

</tasks>

<verification>
- Open members.htmx.html in browser: stats bar is elevated with icons and tooltips, avatars are circular, actions reveal on hover with tooltips, filter chips show count badges
- Open users.htmx.html in browser: role panel shows 4 cards with counts, filter uses pills not dropdown, actions reveal on hover as icons, column headers have tooltips, dates in mono
- Both pages: role/status badges use consistent semantic colors (admin=primary, operator=warning, member/viewer=neutral)
- Mobile viewport: actions visible (no hover needed), role panel wraps to 2 columns
</verification>

<success_criteria>
Members and Users pages are visually upgraded to Linear-quality density — every row is scannable at a glance, every action is discoverable on hover with tooltip explanation, every column header explains itself, and role/status information uses consistent semantic badges.
</success_criteria>

<output>
After completion, create `.planning/phases/39-admin-data-tables/39-01-SUMMARY.md`
</output>
