---
phase: 75-coverage-observability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/js/pages/admin.js
autonomous: true
requirements:
  - DEBT-02

must_haves:
  truths:
    - "When the KPI fetch fails, the admin page shows a visible error state on the KPI cards instead of leaving them blank"
    - "The error state uses French text visible to the user"
    - "Other KPI cards that loaded successfully still show their values (partial failure is handled per-card)"
  artifacts:
    - path: "public/assets/js/pages/admin.js"
      provides: "loadAdminKpis() catch block renders visible error fallback"
      contains: "Erreur chargement"
  key_links:
    - from: "public/assets/js/pages/admin.js loadAdminKpis()"
      to: "admin.htmx.html KPI card elements (#adminKpiMembers etc.)"
      via: "catch block sets textContent to error indicator on unfilled KPI elements"
      pattern: "adminKpiMembers|adminKpiSessions|adminKpiVotes|adminKpiActive"
---

<objective>
Replace the silent catch block in admin.js loadAdminKpis() with a visible error fallback that sets each unfilled KPI card to an error indicator with a French error message.

Purpose: When the API calls fail (network error, 500, auth expiry), users currently see the default `-` placeholder indefinitely with no indication anything went wrong. This is a silent observability failure. The fix makes the error state visible so admins know to investigate.
Output: Modified admin.js with visible KPI error handling.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
<!-- KPI DOM elements (from public/admin.htmx.html lines 62-94): -->
<!--   id="adminKpiMembers"  — .kpi-value div, default text "-" -->
<!--   id="adminKpiSessions" — .kpi-value div, default text "-" -->
<!--   id="adminKpiVotes"    — .kpi-value div, default text "-" -->
<!--   id="adminKpiActive"   — .kpi-value div, default text "-" -->

<!-- Current loadAdminKpis() structure (admin.js lines 18-79): -->
<!-- async function loadAdminKpis() {
  try {
    var results = await Promise.all([...3 api calls...]);
    // fills each KPI element if API response ok
    if (membersEl && membersRes.body && membersRes.body.ok) { membersEl.textContent = count; }
    // etc.
  } catch (e) {
    // KPI section is optional — fail silently
  }
} -->

<!-- setNotif() is available in this scope (used at line 114+) for toast messages -->
<!-- The KPI elements may still show "-" when individual API calls succeed but return ok:false -->
<!-- The catch block only fires for thrown errors (network failures, JSON parse errors, etc.) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace silent KPI catch with visible error fallback in admin.js</name>
  <files>public/assets/js/pages/admin.js</files>
  <read_first>
    - public/assets/js/pages/admin.js (lines 1-80 — the full loadAdminKpis() function)
    - public/admin.htmx.html (lines 60-94 — the KPI card HTML structure)
  </read_first>
  <action>
In admin.js, replace the catch block in loadAdminKpis() (currently lines 76-78):

FROM:
```javascript
    } catch (e) {
      // KPI section is optional — fail silently
    }
```

TO:
```javascript
    } catch (e) {
      // KPI load failed — show visible error state on cards that are still showing '-'
      var kpiIds = ['adminKpiMembers', 'adminKpiSessions', 'adminKpiVotes', 'adminKpiActive'];
      kpiIds.forEach(function(id) {
        var el = document.getElementById(id);
        if (el && el.textContent === '-') {
          el.textContent = '—';
          el.title = 'Erreur chargement indicateurs';
          el.style.color = 'var(--color-error, #DC2626)';
        }
      });
      setNotif('error', 'Erreur lors du chargement des indicateurs');
    }
```

Design rationale:
- Cards that successfully loaded keep their value (partial failure case).
- Cards still showing '-' (default) are set to '—' (em dash) with red color and a tooltip.
- setNotif('error', ...) provides a toast so the admin sees the error immediately.
- French text throughout.
- Uses CSS variable `--color-error` with fallback to #DC2626 (matches the existing error color in admin).

Do NOT change anything else in the file. Make a surgical edit to the catch block only.
  </action>
  <acceptance_criteria>
    - `grep -n "Erreur chargement indicateurs" public/assets/js/pages/admin.js` returns a result
    - `grep -n "fail silently" public/assets/js/pages/admin.js` returns no result
    - `grep -n "adminKpiMembers" public/assets/js/pages/admin.js` still returns the success-path line AND the new catch-path line
    - `node --check public/assets/js/pages/admin.js 2>&1` exits 0 (valid JS syntax)
  </acceptance_criteria>
  <verify>
    <automated>node --check /home/user/gestion_votes_php/public/assets/js/pages/admin.js && grep -c "Erreur chargement indicateurs" /home/user/gestion_votes_php/public/assets/js/pages/admin.js</automated>
  </verify>
  <done>admin.js catch block shows visible error on unfilled KPI cards and a toast notification. No silent failure. JS syntax is valid.</done>
</task>

</tasks>

<verification>
- `node --check public/assets/js/pages/admin.js` — valid JavaScript
- `grep "fail silently" public/assets/js/pages/admin.js` — no matches (silent failure removed)
- `grep "Erreur chargement indicateurs" public/assets/js/pages/admin.js` — 1 match (error message present)
- `grep "setNotif.*error" public/assets/js/pages/admin.js | head -5` — confirms toast call in catch
</verification>

<success_criteria>
When loadAdminKpis() throws (network error, unexpected response), KPI cards that did not load show '—' in red with an error tooltip, and a toast notification is displayed. The fix is surgical — only the catch block changes.
</success_criteria>

<output>
After completion, create `.planning/phases/75-coverage-observability/75-02-SUMMARY.md`
</output>
