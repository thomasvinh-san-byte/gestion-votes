---
phase: 05-js-audit-et-wiring-repair
plan: 03
type: execute
wave: 1
depends_on: ["05-01", "05-02"]
files_modified:
  - public/assets/js/pages/operator-exec.js
  - public/assets/js/pages/operator-attendance.js
  - public/assets/js/pages/operator-realtime.js
  - public/assets/js/pages/dashboard.js
  - tests/e2e/specs/vote.spec.js
  - .planning/phases/05-js-audit-et-wiring-repair/05-ID-CONTRACTS.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [WIRE-01, WIRE-02, WIRE-03, WIRE-04]

must_haves:
  truths:
    - "Aucun orphan selector critique n'est documente sans fix associe dans 05-ID-CONTRACTS.md"
    - "waitForHtmxSettled() est importe et utilise dans vote.spec.js"
    - "REQUIREMENTS.md reflète la completion de WIRE-03 et WIRE-04"
  artifacts:
    - path: "public/assets/js/pages/operator-exec.js"
      provides: "Dead code removed for execQuorumBar legacy block"
      contains: "no getElementById('execQuorumBar')"
    - path: "public/assets/js/pages/operator-attendance.js"
      provides: "Dead code removed for proxyStatGivers/proxyStatReceivers/tabCountProxies"
      contains: "no getElementById('proxyStatGivers')"
    - path: "public/assets/js/pages/operator-realtime.js"
      provides: "Dead code removed for opPresenceBadge"
      contains: "no getElementById('opPresenceBadge')"
    - path: "public/assets/js/pages/dashboard.js"
      provides: "Dead code removed for taches section"
      contains: "no getElementById('taches')"
    - path: "tests/e2e/specs/vote.spec.js"
      provides: "waitForHtmxSettled integrated after page navigation"
      contains: "waitForHtmxSettled"
    - path: ".planning/REQUIREMENTS.md"
      provides: "WIRE-03 and WIRE-04 marked complete"
      contains: "[x] **WIRE-03**"
  key_links:
    - from: "tests/e2e/specs/vote.spec.js"
      to: "tests/e2e/helpers/waitForHtmxSettled.js"
      via: "require import"
      pattern: "require.*waitForHtmxSettled"
---

<objective>
Close 3 verification gaps from Phase 05 verification:
1. Fix 9 v4.2 orphan selectors — remove dead JS code for IDs confirmed missing from HTML, correct false positives in inventory
2. Integrate waitForHtmxSettled() helper into vote.spec.js
3. Update tracking documents (REQUIREMENTS.md, 05-ID-CONTRACTS.md)

Purpose: Complete WIRE-01 through WIRE-04 requirements fully — no orphan without fix, no helper without consumer, no stale tracking.
Output: Clean JS files, wired test helper, accurate documentation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-js-audit-et-wiring-repair/05-01-SUMMARY.md
@.planning/phases/05-js-audit-et-wiring-repair/05-02-SUMMARY.md
@.planning/phases/05-js-audit-et-wiring-repair/05-VERIFICATION.md
@.planning/phases/05-js-audit-et-wiring-repair/05-ID-CONTRACTS.md

<interfaces>
<!-- Existing helper to import in vote.spec.js -->
From tests/e2e/helpers/waitForHtmxSettled.js:
```javascript
async function waitForHtmxSettled(page, timeout = 5000) { ... }
module.exports = { waitForHtmxSettled };
```

From tests/e2e/helpers.js (existing import pattern used by all specs):
```javascript
const { E2E_MOTION_1, loginAsVoter } = require('../helpers');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead JS code for confirmed orphan selectors and correct false positives in inventory</name>
  <files>
    public/assets/js/pages/operator-exec.js,
    public/assets/js/pages/operator-attendance.js,
    public/assets/js/pages/operator-realtime.js,
    public/assets/js/pages/dashboard.js,
    .planning/phases/05-js-audit-et-wiring-repair/05-ID-CONTRACTS.md
  </files>
  <read_first>
    .planning/phases/05-js-audit-et-wiring-repair/05-ID-CONTRACTS.md (lines 699-735),
    .planning/phases/05-js-audit-et-wiring-repair/05-VERIFICATION.md,
    public/assets/js/pages/operator-exec.js,
    public/assets/js/pages/operator-attendance.js,
    public/assets/js/pages/operator-realtime.js,
    public/assets/js/pages/dashboard.js,
    public/assets/js/pages/vote-ui.js (lines 65-80),
    public/assets/js/pages/admin.js (lines 218-225),
    public/assets/js/pages/settings.js (lines 164-180),
    public/vote.htmx.html (grep for cMeeting, cMember),
    public/admin.htmx.html (grep for usersPaginationInfo)
  </read_first>
  <action>
    The verification report lists 12 orphan selectors. Investigation reveals 3 categories:

    **FALSE POSITIVES — IDs exist in HTML (remove from ORPHAN table in 05-ID-CONTRACTS.md):**
    - `vote-ui.js getElementById('cMeeting')` — EXISTS at vote.htmx.html line 333: `<span id="cMeeting">—</span>`
    - `vote-ui.js getElementById('cMember')` — EXISTS at vote.htmx.html line 334: `<span id="cMember">—</span>`
    - `admin.js getElementById('usersPaginationInfo')` — EXISTS at admin.htmx.html line 146: `<span id="usersPaginationInfo">`

    **SELF-HEALING — JS creates the element if missing (move to JS-generated section in inventory):**
    - `settings.js getElementById('app_url')` — has fallback chain: `|| document.querySelector('[data-setting="app_url"]') || document.querySelector('input[name="app_url"]')`
    - `settings.js getElementById('appUrlLocalhostWarning')` — dynamically creates element if null (line 176: `warn = document.createElement('div'); warn.id = 'appUrlLocalhostWarning'`)
    - `operator-realtime.js getElementById('opPresenceBadge')` — dynamically creates element if null (line 195-196: creates span with id='opPresenceBadge')

    **TRUE ORPHANS — Remove dead JS code (HTML element was removed in v4.2, code is unreachable):**
    - `operator-exec.js getElementById('execQuorumBar')` — Remove the entire guarded block (lines ~424-427, the `if (qBar) { ... }` block). The execSpeakerTimer at line 792 is JS-GENERATED (operator-exec.js creates it in innerHTML at line 792 then queries it at line 795) — move to JS-generated section.
    - `operator-attendance.js getElementById('proxyStatGivers')` — Remove the `if (proxyStatGiversEl)` line (line 307). The element is gone; line is dead.
    - `operator-attendance.js getElementById('proxyStatReceivers')` — Remove the `if (proxyStatReceiversEl)` line (line 308). Dead code.
    - `operator-attendance.js getElementById('tabCountProxies')` — Remove the `if (tabCount)` block (lines 310-311). Tab was removed.
    - `dashboard.js getElementById('taches')` — Remove the entire `if (taches) { ... }` block (lines 166-169). Tasks section was removed from dashboard HTML.

    **For each true orphan removal:**
    1. Read full context around the dead code (10 lines before/after)
    2. Remove the getElementById call AND its guarded block
    3. Remove the variable declaration if it's only used in the removed block
    4. Do NOT remove any surrounding code that handles other functionality
    5. Run `php -l` is not relevant (JS files). Verify syntax with `node -c {file}` after each edit.

    **Update 05-ID-CONTRACTS.md:**
    1. Remove cMeeting, cMember, usersPaginationInfo from the ORPHAN table (they are OK — IDs exist in HTML)
    2. Move app_url, appUrlLocalhostWarning, opPresenceBadge to the "ORPHAN — JS-generated (acceptable)" section (they create their own elements)
    3. Move execSpeakerTimer to "ORPHAN — JS-generated (acceptable)" section (operator-exec.js creates it in innerHTML)
    4. Mark execQuorumBar, proxyStatGivers, proxyStatReceivers, tabCountProxies, taches as "FIXED — dead code removed in plan 03"
    5. Update the summary counts at the top of the Summary section
  </action>
  <verify>
    <automated>
      node -c public/assets/js/pages/operator-exec.js &&
      node -c public/assets/js/pages/operator-attendance.js &&
      node -c public/assets/js/pages/operator-realtime.js &&
      node -c public/assets/js/pages/dashboard.js &&
      ! grep -n "getElementById('execQuorumBar')" public/assets/js/pages/operator-exec.js &&
      ! grep -n "getElementById('proxyStatGivers')" public/assets/js/pages/operator-attendance.js &&
      ! grep -n "getElementById('proxyStatReceivers')" public/assets/js/pages/operator-attendance.js &&
      ! grep -n "getElementById('tabCountProxies')" public/assets/js/pages/operator-attendance.js &&
      ! grep -n "getElementById('taches')" public/assets/js/pages/dashboard.js &&
      echo "ALL ORPHAN DEAD CODE REMOVED"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep for `getElementById('execQuorumBar')` in operator-exec.js returns 0 matches
    - grep for `getElementById('proxyStatGivers')` in operator-attendance.js returns 0 matches
    - grep for `getElementById('proxyStatReceivers')` in operator-attendance.js returns 0 matches
    - grep for `getElementById('tabCountProxies')` in operator-attendance.js returns 0 matches
    - grep for `getElementById('taches')` in dashboard.js returns 0 matches
    - node -c passes for all 4 modified JS files
    - 05-ID-CONTRACTS.md ORPHAN table no longer lists cMeeting, cMember, usersPaginationInfo (false positives corrected)
    - 05-ID-CONTRACTS.md ORPHAN table no longer lists app_url, appUrlLocalhostWarning, opPresenceBadge, execSpeakerTimer (moved to JS-generated section)
    - 05-ID-CONTRACTS.md has a "FIXED" section or annotation for execQuorumBar, proxyStatGivers, proxyStatReceivers, tabCountProxies, taches
  </acceptance_criteria>
  <done>All 5 true orphan dead-code blocks removed from JS files. 3 false positives corrected in inventory. 4 self-healing/JS-generated entries reclassified. Inventory is accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate waitForHtmxSettled in vote.spec.js</name>
  <files>tests/e2e/specs/vote.spec.js</files>
  <read_first>
    tests/e2e/specs/vote.spec.js,
    tests/e2e/helpers/waitForHtmxSettled.js
  </read_first>
  <action>
    Import waitForHtmxSettled from the helpers subdirectory and use it in the vote spec.

    1. Add this import at the top of vote.spec.js (after the existing require lines):
       ```javascript
       const { waitForHtmxSettled } = require('../helpers/waitForHtmxSettled');
       ```

    2. In the "should show meeting selector" test, add a waitForHtmxSettled call after `page.waitForLoadState('networkidle')` and before the assertion:
       ```javascript
       await waitForHtmxSettled(page);
       ```
       This ensures HTMX has settled the DOM before asserting on selectors. On vote.htmx.html, HTMX IS loaded, so this will actually wait for htmx:afterSettle (or resolve after 200ms safety timeout).

    3. Verify syntax: `node -c tests/e2e/specs/vote.spec.js`
  </action>
  <verify>
    <automated>
      node -c tests/e2e/specs/vote.spec.js &&
      grep -n "waitForHtmxSettled" tests/e2e/specs/vote.spec.js &&
      grep -n "require.*helpers/waitForHtmxSettled" tests/e2e/specs/vote.spec.js &&
      echo "HELPER INTEGRATED"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep for `require('../helpers/waitForHtmxSettled')` in vote.spec.js returns 1 match
    - grep for `waitForHtmxSettled(page)` in vote.spec.js returns at least 1 match
    - node -c tests/e2e/specs/vote.spec.js passes
  </acceptance_criteria>
  <done>waitForHtmxSettled is imported and used in vote.spec.js before HTMX-dependent assertions. The helper is no longer orphaned.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md tracking for WIRE-03 and WIRE-04</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>
    Update REQUIREMENTS.md to reflect completed status:

    1. Change line 14 from `- [ ] **WIRE-03**:` to `- [x] **WIRE-03**:`
    2. Change line 15 from `- [ ] **WIRE-04**:` to `- [x] **WIRE-04**:`
    3. In the Traceability table at the bottom, change:
       - WIRE-03 status from `Pending` to `Complete`
       - WIRE-04 status from `Pending` to `Complete`
  </action>
  <verify>
    <automated>
      grep -n "\[x\] \*\*WIRE-03\*\*" .planning/REQUIREMENTS.md &&
      grep -n "\[x\] \*\*WIRE-04\*\*" .planning/REQUIREMENTS.md &&
      grep -n "WIRE-03.*Complete" .planning/REQUIREMENTS.md &&
      grep -n "WIRE-04.*Complete" .planning/REQUIREMENTS.md &&
      echo "REQUIREMENTS UPDATED"
    </automated>
  </verify>
  <acceptance_criteria>
    - Line 14 contains `[x] **WIRE-03**`
    - Line 15 contains `[x] **WIRE-04**`
    - Traceability table shows WIRE-03 as Complete
    - Traceability table shows WIRE-04 as Complete
  </acceptance_criteria>
  <done>REQUIREMENTS.md accurately tracks all 4 WIRE requirements as complete.</done>
</task>

</tasks>

<verification>
All verification gaps from 05-VERIFICATION.md are addressed:
- Gap 1 (orphan selectors): Dead code removed for true orphans, false positives corrected in inventory, self-healing patterns reclassified
- Gap 2 (waitForHtmxSettled integration): Helper imported and used in vote.spec.js
- Gap 3 (REQUIREMENTS.md tracking): WIRE-03 and WIRE-04 marked complete
</verification>

<success_criteria>
- Zero orphan selectors in 05-ID-CONTRACTS.md without either a fix or a correct reclassification
- waitForHtmxSettled imported and called in at least one spec file
- REQUIREMENTS.md shows all 4 WIRE requirements as [x] complete
- All modified JS files pass node -c syntax check
</success_criteria>

<output>
After completion, create `.planning/phases/05-js-audit-et-wiring-repair/05-03-SUMMARY.md`
</output>
