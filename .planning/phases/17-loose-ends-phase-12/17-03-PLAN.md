---
phase: 17-loose-ends-phase-12
plan: 03
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - .planning/phases/17-loose-ends-phase-12/17-AUDIT-LEDGER.md
autonomous: true
requirements: [LOOSE-03]
must_haves:
  truths:
    - "Every 'documented but not fixed' / 'TODO' / 'known issue' / 'to revisit' note in Phase 12 SUMMARY files is classified: fixed, deferred-to-v2, or promoted-to-todo"
    - "17-AUDIT-LEDGER.md exists with a table of all findings and their resolution"
    - "No Phase 12 SUMMARY contains an unresolved 'documented but not fixed' marker without a cross-reference in the ledger"
  artifacts:
    - path: .planning/phases/17-loose-ends-phase-12/17-AUDIT-LEDGER.md
      provides: "Ledger of all Phase 12 loose notes with classification and resolution link"
      contains: "| File | Note | Classification | Resolution |"
  key_links:
    - from: .planning/phases/17-loose-ends-phase-12/17-AUDIT-LEDGER.md
      to: ".planning/milestones/v1.2-phases/12-page-by-page-mvp-sweep/12-*-SUMMARY.md"
      via: "cross-reference per finding"
      pattern: "12-\\d+-SUMMARY\\.md"
---

<objective>
Audit every Phase 12 SUMMARY file for unresolved notes and produce a classification ledger. Each finding is either (a) trivially fixed inline in this plan, (b) explicitly deferred to v2 with a written note in the original SUMMARY, or (c) promoted to a standalone v1.3 todo.

Purpose: Close LOOSE-03 — ensure the milestone ships without hidden unresolved tech debt in the SUMMARY trail.

Output: `.planning/phases/17-loose-ends-phase-12/17-AUDIT-LEDGER.md` with complete findings table.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-loose-ends-phase-12/17-CONTEXT.md
@CLAUDE.md

Phase 12 SUMMARY files live under `.planning/milestones/v1.2-phases/12-page-by-page-mvp-sweep/12-*-SUMMARY.md` (21 files: 12-01 through 12-21).

LOOSE-01 (settings race) and LOOSE-02 (postsession chip) are already covered by plans 17-01 and 17-02 — they MUST NOT reappear as findings here; if grep surfaces them, mark them as "resolved by 17-01" / "resolved by 17-02" in the ledger.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep audit + ledger creation</name>
  <files>.planning/phases/17-loose-ends-phase-12/17-AUDIT-LEDGER.md</files>
  <read_first>
    - `.planning/phases/17-loose-ends-phase-12/17-CONTEXT.md` sections `<decisions>` D-07, D-08, D-09
    - Run: `ls .planning/milestones/v1.2-phases/12-page-by-page-mvp-sweep/12-*-SUMMARY.md` to confirm file list
  </read_first>
  <action>
    Use Grep tool on `.planning/milestones/v1.2-phases/12-page-by-page-mvp-sweep/` for patterns:
    - `documented but not`
    - `TODO`
    - `known issue`
    - `to revisit`
    - `not blocking`
    - `workaround`
    - `deferred`
    - `follow-up`
    - `follow up`
    - `separate concern`

    Case-insensitive. Use `output_mode: "content"` with `-n` and `-C 2` for context.

    For each hit, open the SUMMARY file, read enough context to understand the note, and classify:

    **Fix-now:** Only if the fix is a 5-minute grep/sed-level change AND touches files not already owned by other plans. Apply the fix, commit, and record the commit hash in the ledger. Examples: stale comment, dead grep reference, typo in a command.

    **Defer-to-v2:** If the issue requires non-trivial work. Append a note to the ORIGINAL SUMMARY file:
    ```
    **Status update (Phase 17-03 audit):** Deferred to v2 — see 17-AUDIT-LEDGER.md entry #N.
    ```
    Do NOT rewrite the SUMMARY; append at the bottom under a new `## Post-milestone audit` section.

    **Promote-to-todo:** If the issue is within v1.3 scope but needs its own planning. Record in the ledger with `status: promote` and list it under a `## Promoted Todos` section so the user can decide.

    **Already resolved:** If the note matches LOOSE-01 or LOOSE-02, mark `resolved by 17-01` / `resolved by 17-02`.

    **Not a real finding:** Grep false positives (e.g. "TODO" inside code examples, not an actual unresolved item). Mark `status: noise` with a short justification.

    Build `.planning/phases/17-loose-ends-phase-12/17-AUDIT-LEDGER.md` with this structure:

    ```markdown
    # Phase 17-03 — Phase 12 SUMMARY Audit Ledger

    **Date:** 2026-04-09
    **Scope:** .planning/milestones/v1.2-phases/12-page-by-page-mvp-sweep/12-*-SUMMARY.md (21 files)
    **Patterns searched:** documented but not, TODO, known issue, to revisit, not blocking, workaround, deferred, follow-up, separate concern

    ## Summary

    - Total findings: N
    - Fixed now: X
    - Deferred to v2: Y
    - Promoted to todo: Z
    - Resolved by 17-01/17-02: W
    - Noise (false positives): V

    ## Findings Table

    | # | File | Line | Note (truncated) | Classification | Resolution |
    |---|------|------|------------------|----------------|------------|
    | 1 | 12-01-SUMMARY.md | 65 | race condition in loadSettings | resolved | 17-01 |
    | 2 | 12-15-SUMMARY.md | 96 | eIDAS chip page.evaluate | resolved | 17-02 |
    | ... | ... | ... | ... | ... | ... |

    ## Fixes Applied Inline

    (list commits for fix-now items, or "none" if zero)

    ## Deferred to v2

    (list with rationale per item)

    ## Promoted Todos

    (list items that need their own plans, for user decision)

    ## Acceptance

    - [x] All 21 SUMMARY files greped
    - [x] Every finding classified
    - [x] Deferred items have back-links in their original SUMMARY
    ```

    Do NOT edit SUMMARY files other than appending the "Post-milestone audit" note for deferred items. Do NOT add findings for phases other than 12.

    Keep the ledger under 500 lines. If findings exceed 50 entries, group noise items into a single row with a count.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-loose-ends-phase-12/17-AUDIT-LEDGER.md && grep -c "^| " .planning/phases/17-loose-ends-phase-12/17-AUDIT-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/17-loose-ends-phase-12/17-AUDIT-LEDGER.md` exists
    - File contains sections: Summary, Findings Table, Fixes Applied Inline, Deferred to v2, Promoted Todos, Acceptance
    - Every row in the findings table has a non-empty Classification column
    - `grep -l "Post-milestone audit" .planning/milestones/v1.2-phases/12-page-by-page-mvp-sweep/12-*-SUMMARY.md` returns at least the files with deferred findings (or zero if no deferrals)
    - LOOSE-01 and LOOSE-02 notes are marked `resolved by 17-01` / `resolved by 17-02` in the ledger
  </acceptance_criteria>
  <done>Ledger created, all Phase 12 loose notes classified, deferred items back-linked in original SUMMARYs</done>
</task>

</tasks>

<verification>
`test -f .planning/phases/17-loose-ends-phase-12/17-AUDIT-LEDGER.md` — file exists.
`grep -n "resolved by 17-01\|resolved by 17-02" .planning/phases/17-loose-ends-phase-12/17-AUDIT-LEDGER.md` — both cross-refs present.
</verification>

<success_criteria>
- Audit ledger produced
- All findings classified
- Deferred items back-referenced in originals
- LOOSE-03 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/17-loose-ends-phase-12/17-03-SUMMARY.md` summarizing: number of findings, classification breakdown, commits for any fix-now items, user-decision-required items (if any promoted todos).
</output>
