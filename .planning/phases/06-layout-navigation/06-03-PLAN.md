---
phase: 06-layout-navigation
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - public/assets/css/design-system.css
  - public/dashboard.htmx.html
autonomous: false
requirements:
  - NAV-06

must_haves:
  truths:
    - "A skip-to-content link is present and becomes visible on Tab focus"
    - "ARIA landmark roles are applied: nav, main, header (banner), footer (contentinfo)"
    - "Skip-to-content link targets #main-content which exists on the main element"
    - "Keyboard navigation through sidebar items works correctly"
  artifacts:
    - path: "public/dashboard.htmx.html"
      provides: "Representative page with all ARIA landmarks"
      contains: "skip-link"
    - path: "public/assets/css/design-system.css"
      provides: "Skip-link focus styles"
      contains: "skip-link:focus"
  key_links:
    - from: "skip-link href=#main-content"
      to: "main#main-content"
      via: "anchor link to main content area"
      pattern: "main-content"
---

<objective>
Verify and complete ARIA accessibility landmarks across the application shell, then visually verify the full layout.

Purpose: Ensure the application meets WCAG landmark requirements with proper skip-to-content, ARIA roles, and keyboard navigation. Then verify the complete layout visually.

Output: Verified ARIA landmarks, accessibility audit pass, visual confirmation of full layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-layout-navigation/06-CONTEXT.md
@.planning/phases/06-layout-navigation/06-01-SUMMARY.md
@.planning/phases/06-layout-navigation/06-02-SUMMARY.md
@public/dashboard.htmx.html
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix ARIA landmarks and skip-link across all pages</name>
  <files>public/assets/css/design-system.css, public/dashboard.htmx.html</files>
  <action>
Run an automated audit of ARIA landmarks across all page HTML files and fix any gaps.

1. **Audit all pages** for these required landmarks:
   - `<a href="#main-content" class="skip-link">` -- skip-to-content link (should be first child of body)
   - `<a href="#main-nav" class="skip-link">` -- skip-to-nav link (second skip link)
   - `<header class="app-header" role="banner">` -- header with banner role
   - `<main class="app-main" id="main-content" role="main">` -- main with id and role
   - `<footer class="app-footer" role="contentinfo">` -- footer with contentinfo role (added in Plan 02)
   - `<nav ... aria-label="...">` -- nav elements with labels

2. **Write a validation script** that checks all `.htmx.html` files:
```bash
# For each page, check:
# - Has skip-link to #main-content
# - Has element with id="main-content"
# - Header has role="banner"
# - Main has role="main"
# - Footer has role="contentinfo"
```

3. **Fix any pages** that fail the audit. Based on existing code review:
   - Most pages already have skip-links and id="main-content"
   - Plans 01-02 added role="banner" and role="contentinfo"
   - If any page is missing a skip-link, add it as first element inside body

4. **Verify skip-link CSS** in design-system.css:
   - `.skip-link` should be `position: absolute; top: -100px` (hidden by default)
   - `.skip-link:focus` should set `top: 0` (visible on focus)
   - These styles already exist (confirmed at line 2253-2278). Verify they are intact.

5. **Ensure the sidebar nav has aria-label="Navigation principale"** -- already present in sidebar.html. No change needed unless Plan 01 accidentally removed it.

6. **Add aria-label to the mobile bottom nav** in shell.js if not already present. The current code sets `aria-label="Navigation rapide"` -- verify this is retained.
  </action>
  <verify>
    <automated>FAIL=0; for f in public/*.htmx.html; do grep -q 'skip-link' "$f" || { echo "MISSING skip-link: $f"; FAIL=1; }; grep -q 'id="main-content"' "$f" || { echo "MISSING main-content: $f"; FAIL=1; }; done; test $FAIL -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All pages pass ARIA landmark audit: skip-links present, main-content id exists, header/main/footer have correct roles, nav elements have aria-labels</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete application shell layout: sidebar with 5 sections (rail/expand/pin), header with logo/search/notifications/theme-toggle, footer with links, mobile bottom nav with 5 tabs, ARIA landmarks</what-built>
  <how-to-verify>
    1. Open http://localhost:PORT/dashboard.htmx.html in a desktop browser
    2. Verify sidebar shows as a narrow icon rail (~58px). Hover over it -- it should expand to ~252px as an overlay WITHOUT shifting the page content
    3. Click the pin button (pushpin icon near top of sidebar). Sidebar should stay expanded and page content should shift right to accommodate
    4. Verify sidebar has 5 labeled sections: Preparation, Seance en direct, Apres la seance, Controle, Systeme
    5. Verify "Guide & FAQ" item is inside the Systeme section (not in a separate Aide section)
    6. Click a section label to collapse it. Refresh the page -- collapsed state should persist
    7. Check header bar: should have logo (AG-VOTE), search trigger (Cmd+K), notification bell, and the search/notification/theme elements
    8. Press Cmd+K (or Ctrl+K) to open global search overlay
    9. Scroll down to see the footer: should show AG-VOTE logo, version, Aide link, Accessibilite link
    10. Resize browser to < 768px width. Sidebar should disappear, a bottom navigation bar should appear with 5 tabs: Dashboard, Sessions, Fiche, Operateur, Parametres
    11. Press Tab key from the top of the page. The first Tab should reveal a "Aller au contenu principal" skip-link
    12. Toggle dark/light theme and verify sidebar, header, footer all render correctly in both themes
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. ARIA audit script passes for all pages
2. Skip-link visible on Tab focus
3. Visual verification of complete layout in desktop and mobile viewports
4. Dark/light theme renders correctly for all shell elements
</verification>

<success_criteria>
- All ARIA landmarks present and correctly attributed
- Skip-to-content link works (visible on focus, navigates to main content)
- Visual layout matches wireframe: sidebar rail/expand, header with search/notifications, footer with links, mobile 5-tab bottom nav
- Both dark and light themes render shell correctly
</success_criteria>

<output>
After completion, create `.planning/phases/06-layout-navigation/06-03-SUMMARY.md`
</output>
