---
phase: 06-layout-navigation
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - public/assets/js/core/shell.js
  - public/assets/css/design-system.css
  - public/dashboard.htmx.html
  - public/meetings.htmx.html
  - public/operator.htmx.html
  - public/hub.htmx.html
  - public/wizard.htmx.html
  - public/members.htmx.html
  - public/postsession.htmx.html
  - public/archives.htmx.html
  - public/trust.htmx.html
  - public/analytics.htmx.html
  - public/admin.htmx.html
  - public/help.htmx.html
  - public/vote.htmx.html
  - public/public.htmx.html
  - public/report.htmx.html
  - public/validate.htmx.html
  - public/docs.htmx.html
  - public/email-templates.htmx.html
autonomous: true
requirements:
  - NAV-03
  - NAV-04
  - NAV-05

must_haves:
  truths:
    - "Header bar (56px) displays logo, global search (Cmd+K), notification panel, and theme toggle"
    - "Notification bell shows unread count badge and opens a dropdown panel (300-400px) with max 5-6 items and a Voir tout link"
    - "Mobile bottom nav shows 5 tabs: Dashboard, Sessions, Fiche, Operateur, Parametres"
    - "Bottom nav appears only below 768px breakpoint, sidebar hidden at that breakpoint"
    - "Footer shows logo, version, help link, and accessibility link on desktop"
    - "Footer is hidden on mobile viewports"
  artifacts:
    - path: "public/assets/js/core/shell.js"
      provides: "Mobile bottom nav with 5 correct tabs, notification panel with item limit and Voir tout link"
      contains: "mobile-bnav"
    - path: "public/dashboard.htmx.html"
      provides: "Representative page with footer element"
      contains: "app-footer"
  key_links:
    - from: "public/assets/js/core/shell.js"
      to: "all page HTML files"
      via: "JS injects mobile bottom nav into .app-shell"
      pattern: "mobile-bnav"
    - from: "public/assets/js/core/shell.js"
      to: "public/assets/css/design-system.css"
      via: "Notification bell and panel use .notif-bell, .notif-count, .notif-panel CSS"
      pattern: "notif-panel"
    - from: "all page HTML files"
      to: "public/assets/css/design-system.css"
      via: "CSS classes for footer and header"
      pattern: "app-footer"
---

<objective>
Align header content (logo, notification panel), update mobile bottom nav to 5 tabs, and add footer to all pages.

Purpose: The header needs a logo element and the notification panel must match CONTEXT.md specs (max 5-6 items, "Voir tout" link). The mobile bottom nav must match the wireframe's 5-tab specification. Every page needs a consistent footer.

Output: Updated shell.js with correct 5-tab bottom nav and aligned notification panel, footer added to all page HTML files, header logo verified, mobile-bnav CSS added.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-layout-navigation/06-CONTEXT.md
@.planning/phases/06-layout-navigation/06-01-SUMMARY.md
@public/assets/js/core/shell.js
@public/assets/css/design-system.css
@public/dashboard.htmx.html

<interfaces>
<!-- Existing notification system in shell.js (lines 560-657) -->
<!-- createNotifBell() injects .notif-bell button into .app-header -->
<!-- .notif-count span shows unread count (display:none when 0) -->
<!-- .notif-panel div is appended inside the bell button -->
<!-- renderNotifications() currently slices items to 10 — needs limit to 5-6 -->
<!-- Panel currently lacks "Voir tout" footer link -->

<!-- Existing CSS in design-system.css (lines 3245-3285) -->
<!-- .notif-bell, .notif-count, .notif-panel, .notif-item already styled -->
<!-- .notif-panel width: 340px (within 300-400px spec) -->

<!-- Mobile bottom nav CSS (line 955) -->
<!-- .mobile-bnav { display: none; } exists but needs full mobile styles -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mobile bottom nav to 5 wireframe tabs, add header logo, and add mobile-bnav CSS</name>
  <files>public/assets/js/core/shell.js, public/assets/css/design-system.css</files>
  <action>
Update the mobile bottom navigation injection in shell.js (around line 415-430) to use exactly 5 tabs matching the wireframe specification:

1. **Replace the current 4-tab `items` array** with these 5 tabs:
```javascript
const items = [
  { href: '/dashboard.htmx.html', page: 'dashboard', icon: '...home icon SVG...', label: 'Dashboard' },
  { href: '/meetings.htmx.html', page: 'meetings', icon: '...clipboard-list icon SVG...', label: 'Sessions' },
  { href: '/hub.htmx.html', page: 'hub', icon: '...file-text icon SVG...', label: 'Fiche' },
  { href: '/operator.htmx.html', page: 'operator', icon: '...play icon SVG...', label: 'Operateur' },
  { href: '/admin.htmx.html?tab=settings', page: 'parametres', icon: '...settings icon SVG...', label: 'Parametres' },
];
```
Use inline SVG icons (same pattern as current code). Use appropriate 18x18 SVGs:
- Dashboard: home icon (path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z" + polyline 9 22 9 12 15 12 15 22)
- Sessions: clipboard-list icon (rect + lines pattern)
- Fiche: file-text icon (path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" + path "M14 2v6h6")
- Operateur: play icon (polygon "5 3 19 12 5 21 5 3")
- Parametres: settings/gear icon (same as current Config item)

2. **Fix active tab detection** -- The current code reads `currentPage` from `sidebar.getAttribute('data-page')`. Ensure it also checks for the `parametres` page value to correctly highlight the Parametres tab (currently it checks `data-page` which is set per-page on the sidebar `aside` element).

3. **Add header logo** -- In the section that creates the hamburger (around line 403-411), also check if `.app-header` has a `.logo` element. If not, inject one as the first child (after hamburger if present):
```javascript
if (header && !header.querySelector('.logo')) {
  const logoEl = document.createElement('a');
  logoEl.className = 'logo';
  logoEl.href = '/dashboard.htmx.html';
  logoEl.setAttribute('aria-label', 'AG-VOTE — Accueil');
  logoEl.innerHTML = '<span class="logo-mark"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5"><path d="M9 3H5a2 2 0 0 0-2 2v4m6-6h10a2 2 0 0 1 2 2v4M9 3v18m0 0h10a2 2 0 0 0 2-2V9M9 21H5a2 2 0 0 1-2-2V9m0 0h18"/></svg></span> AG-VOTE';
  const hamburger = header.querySelector('.hamburger');
  if (hamburger) {
    hamburger.after(logoEl);
  } else {
    header.prepend(logoEl);
  }
}
```
This ensures every page gets a logo in the header even if the page HTML does not include one inline.

4. **Add mobile-bnav CSS** to design-system.css. The existing `.mobile-bnav { display: none; }` at line 955 needs to be expanded to full mobile bottom nav styles. Add after the existing rule:
```css
@media (max-width: 767.98px) {
  .mobile-bnav {
    display: flex;
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    height: 56px;
    background: var(--color-surface);
    border-top: 1px solid var(--color-border);
    z-index: var(--z-sticky, 100);
    justify-content: space-around;
    align-items: center;
    padding: 0 4px;
  }
  .mobile-bnav-btn {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 2px;
    padding: 6px 8px;
    border: none;
    background: none;
    color: var(--color-text-muted);
    font-size: 10px;
    font-weight: 600;
    text-decoration: none;
    border-radius: var(--radius-md);
    transition: color 0.15s ease;
    flex: 1;
    min-width: 0;
  }
  .mobile-bnav-btn.active {
    color: var(--color-primary);
  }
  .mobile-bnav-btn svg {
    width: 20px;
    height: 20px;
  }
}
```
This keeps the mobile-bnav CSS co-located with the JS that creates the elements, avoiding the cross-plan dependency that would occur if 06-01 authored these styles.
  </action>
  <verify>
    <automated>grep -c "mobile-bnav-btn" public/assets/js/core/shell.js | xargs -I{} test {} -ge 5 && grep -q "Dashboard" public/assets/js/core/shell.js && grep -q "Parametres" public/assets/js/core/shell.js && grep -q "logo-mark" public/assets/js/core/shell.js && grep -q "mobile-bnav-btn" public/assets/css/design-system.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Mobile bottom nav has exactly 5 tabs (Dashboard, Sessions, Fiche, Operateur, Parametres) with matching CSS, header logo injected dynamically</done>
</task>

<task type="auto">
  <name>Task 2: Align notification panel with CONTEXT.md specifications</name>
  <files>public/assets/js/core/shell.js, public/assets/css/design-system.css</files>
  <action>
The notification bell and panel already exist in shell.js (createNotifBell at line 565) and design-system.css (lines 3245-3285). Align them with the CONTEXT.md locked decision: "action-focused dropdown (not side drawer) -- bell icon with count badge, dropdown panel 300-400px wide, max 5-6 items, 'Voir tout' link at bottom."

1. **Limit displayed items to 6** -- In renderNotifications() (around line 637), change `items.slice(0, 10)` to `items.slice(0, 6)`. This caps the dropdown to max 6 items per user decision.

2. **Add "Voir tout" footer link** -- After the `.notif-list` div in the panel HTML (line 586), add a footer link. Update the notifPanel.innerHTML to include:
```javascript
notifPanel.innerHTML = '<div style="padding:12px 16px;font-size:13px;font-weight:700;border-bottom:1px solid var(--color-border-subtle,#e8e7e2);">Notifications</div>' +
  '<div class="notif-list"></div>' +
  '<a href="/admin.htmx.html?tab=notifications" class="notif-footer" style="display:block;padding:10px 16px;text-align:center;font-size:12px;font-weight:600;color:var(--color-primary);border-top:1px solid var(--color-border-subtle,#e8e7e2);text-decoration:none;">Voir tout</a>';
```

3. **Add .notif-panel max-height for scrollable list** -- In design-system.css, add to the existing `.notif-panel` rule (line 3261):
```css
.notif-list {
  max-height: 320px;
  overflow-y: auto;
}
```
This ensures the panel remains bounded even if items have long text. The panel width is already 340px (within the 300-400px spec), so no width change needed.

4. **Verify the count badge** -- The existing `.notif-count` span already functions as the count badge. Verify it shows/hides correctly (display:flex when count > 0, display:none when 0). The existing CSS at line 3253 already styles it. No changes needed unless broken.

Do NOT restructure the bell/panel architecture -- only make the targeted changes above.
  </action>
  <verify>
    <automated>grep -q "Voir tout" public/assets/js/core/shell.js && grep -q "slice(0, 6)" public/assets/js/core/shell.js && grep -q "notif-list" public/assets/css/design-system.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Notification panel limited to 6 items, "Voir tout" footer link added, scrollable list with max-height, count badge functional</done>
</task>

<task type="auto">
  <name>Task 3: Add footer element to all page HTML files</name>
  <files>public/dashboard.htmx.html, public/meetings.htmx.html, public/operator.htmx.html, public/hub.htmx.html, public/wizard.htmx.html, public/members.htmx.html, public/postsession.htmx.html, public/archives.htmx.html, public/trust.htmx.html, public/analytics.htmx.html, public/admin.htmx.html, public/help.htmx.html, public/vote.htmx.html, public/public.htmx.html, public/report.htmx.html, public/validate.htmx.html, public/docs.htmx.html, public/email-templates.htmx.html</files>
  <action>
Add a footer element to every page HTML file that does not already have one. The footer goes inside `.app-shell`, after `</main>` (closing of `.app-main`) and before the closing `</div>` of `.app-shell`.

**Footer HTML to insert:**
```html
    <footer class="app-footer" role="contentinfo">
      <a href="/dashboard.htmx.html" class="logo" style="font-size:12px;gap:6px;" aria-label="AG-VOTE">
        <span class="logo-mark" style="width:18px;height:18px;border-radius:3px;font-size:8px;">
          <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5"><path d="M9 3H5a2 2 0 0 0-2 2v4m6-6h10a2 2 0 0 1 2 2v4M9 3v18m0 0h10a2 2 0 0 0 2-2V9M9 21H5a2 2 0 0 1-2-2V9m0 0h18"/></svg>
        </span>
        AG-VOTE
      </a>
      <span class="text-muted">v3.19</span>
      <span style="flex:1"></span>
      <a href="/help.htmx.html" class="text-muted" style="text-decoration:none;">Aide</a>
      <a href="/admin.htmx.html?tab=settings#accessibilite" class="text-muted" style="text-decoration:none;">Accessibilite</a>
    </footer>
```

**Important rules:**
- Skip pages that already have an `<footer class="app-footer">` element
- Skip `public.htmx.html` (room display is full-screen, no footer per wireframe) -- but still add the HTML element with an inline `style="display:none"` so the landmark exists for ARIA
- Skip `vote.htmx.html` (voter view has its own layout) -- same treatment, add with display:none
- The footer flows with page content (not sticky/fixed)
- Use `role="contentinfo"` on the footer element for ARIA landmark

Also while editing each page, verify these ARIA attributes are present (add if missing):
- `<header class="app-header">` should have `role="banner"`
- `<main class="app-main" ... role="main">` should exist (most pages already have this)
- `<aside class="app-sidebar" ...>` -- no role change needed (aside is already landmark)
- `<nav>` elements should have `aria-label` attributes (most already do)

If a page is missing `role="banner"` on the header, add it. If missing `role="main"` on main, add it.
  </action>
  <verify>
    <automated>FAIL=0; for f in public/*.htmx.html; do grep -q 'role="contentinfo"' "$f" || { echo "MISSING contentinfo: $f"; FAIL=1; }; done; FOOTER_COUNT=$(grep -rl 'app-footer' public/*.htmx.html 2>/dev/null | wc -l); TOTAL=$(ls public/*.htmx.html | wc -l); test "$FOOTER_COUNT" -eq "$TOTAL" && test "$FAIL" -eq 0 && echo "PASS: all $TOTAL pages have footer with contentinfo role" || echo "FAIL: $FOOTER_COUNT/$TOTAL pages, or missing contentinfo roles"</automated>
  </verify>
  <done>All page HTML files have a footer with logo, version, help link, and accessibility link. Every footer has role="contentinfo". Header and main elements have correct ARIA roles.</done>
</task>

</tasks>

<verification>
1. `grep -c 'Parametres\|Dashboard\|Sessions\|Fiche\|Operateur' public/assets/js/core/shell.js` confirms 5-tab labels in bottom nav
2. `grep -q "Voir tout" public/assets/js/core/shell.js` confirms notification panel has footer link
3. `grep -q "slice(0, 6)" public/assets/js/core/shell.js` confirms 6-item limit
4. Per-page contentinfo check: `for f in public/*.htmx.html; do grep -q 'role="contentinfo"' "$f" || echo "MISSING: $f"; done`
5. `grep -rl 'app-footer' public/*.htmx.html | wc -l` matches total page count
6. `grep -l 'role="banner"' public/*.htmx.html | wc -l` confirms header ARIA role on all pages
</verification>

<success_criteria>
- Mobile bottom nav has exactly 5 tabs matching wireframe spec with CSS styles
- Notification panel capped at 6 items with "Voir tout" link per CONTEXT.md decision
- Header has logo element on all pages (injected by shell.js)
- Footer present in all page HTML files with correct content and role="contentinfo"
- Footer hidden on mobile (CSS handles this)
- ARIA roles present on header, main, and footer elements
</success_criteria>

<output>
After completion, create `.planning/phases/06-layout-navigation/06-02-SUMMARY.md`
</output>
