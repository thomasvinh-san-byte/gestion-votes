---
phase: 20.1-refonte-ui-alignement-wireframe-et-reduction-charge-mentale
plan: "03"
subsystem: ui
tags: [css, dashboard, operator, FOUC, modals, shortcut-cards, spacing]

# Dependency graph
requires:
  - phase: 20-live-vote-flow
    provides: dashboard.htmx.html and operator.htmx.html page structures
provides:
  - Dashboard as clean action launcher with prominent shortcut cards and KPIs
  - Reduced visual density with generous spacing
  - Operator page with FOUC-free load
  - Simplified modal styling (title + content + 2 buttons pattern)
  - Exec view with prominent current motion title
affects: [phase-20.1-plan-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "FOUC prevention: inline critical-tokens style in <head> before theme-init.js"
    - "Shortcut card hover lift: translateY(-2px) + box-shadow for CTA emphasis"
    - "Modal simplification: consistent var() spacing, font-weight 700 headers"
    - "Exec hierarchy: text-lg for current motion title as dominant element"

key-files:
  created: []
  modified:
    - path: public/dashboard.htmx.html
      changes: "FOUC inline style block in head"
    - path: public/assets/css/pages.css
      changes: "Shortcut cards enlarged (48px icons, hover lift), KPI card prominence, section spacing"
    - path: public/operator.htmx.html
      changes: "FOUC inline style block in head"
    - path: public/assets/css/operator.css
      changes: "Modal header/footer var() spacing, exec-vote-title promoted to text-lg, tab-content padding"
---

## What was built

Dashboard et console opérateur restructurés pour réduire la charge mentale:

1. **Dashboard action launcher** — Shortcut cards enlarged (48px icons, hover lift effect), KPI values prominent at text-3xl, generous spacing between sections via space-6 margins.

2. **Reduced visual density** — Page subtitle muted, dashboard grid spacing increased, shortcut card text sized up for CTA emphasis.

3. **Operator modals** — Consistent spacing with CSS custom properties (var(--space-N)), header font-weight 700 at text-lg, footer flex-end layout with proper gap.

4. **Exec view hierarchy** — Current motion title promoted to text-lg/700 as the dominant element. Tab content padding increased for breathing room.

5. **FOUC fix** — Both dashboard and operator pages get inline critical-tokens style block preventing flash of unstyled content.

## Self-Check: PASSED

- [x] Dashboard shortcut cards are prominent action launchers
- [x] Dashboard visual density reduced with generous spacing
- [x] Operator modals follow simple pattern (title + content + 2 buttons)
- [x] FOUC eliminated on both dashboard and operator pages
- [x] No JS or functionality broken

## Deviations

None — all plan tasks implemented as specified.

## Metrics

- Files modified: 4
- Commits: 2 (dashboard + operator)
