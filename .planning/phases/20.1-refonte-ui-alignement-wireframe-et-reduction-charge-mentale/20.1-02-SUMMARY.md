---
phase: 20.1-refonte-ui-alignement-wireframe-et-reduction-charge-mentale
plan: "02"
subsystem: ui
tags: [css, projection, gradient, typography, Fraunces, clamp, FOUC, responsive]

# Dependency graph
requires:
  - phase: 20-live-vote-flow
    provides: public.htmx.html projection screen with quorum visual bar and result bar chart
provides:
  - Projection page with gradient background (dark/light variants)
  - FOUC-free page load via inline critical-tokens style
  - Meeting title as h1 with Fraunces display font at clamp(32px,3.6vw,56px)
  - Quorum bar integrated in header (not buried in main content)
  - Responsive result bars with clamp() scaling for projectors up to 4m
affects: [phase-20.1-plan-03, any future projection page work]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "FOUC prevention: inline critical-tokens style in <head> before theme-init.js"
    - "Gradient body background with light/dark theme variants via data-theme attribute"
    - "clamp() for projector readability: min for mobile, preferred for vw-relative scaling, max for 4m screen"
    - "Header-integrated quorum bar using projection-quorum modifier class"

key-files:
  created: []
  modified:
    - public/public.htmx.html
    - public/assets/css/public.css

key-decisions:
  - "FOUC fix: inline critical-tokens style block placed between <meta charset> and theme-init.js — style applies before scripts execute"
  - "Gradient default is dark (forced by inline script); light gradient switches via [data-theme=light] selector"
  - "Quorum bar moved from main to header: added projection-quorum modifier with max-width 600px centered"
  - "meeting_title element promoted from span.meeting-title to h1.projection-title — same ID preserved for JS compatibility"
  - "Header restructured with projection-header-top flex wrapper: left (badge+counter), center (title), right (clock+controls)"
  - "decision-value and bar-percentage clamp max raised to 2.5rem for projector readability (was 2rem)"

patterns-established:
  - "Inline critical-tokens: use on every projection/full-screen page that forces a specific theme to prevent FOUC"
  - "projection-quorum modifier: apply on quorum-visual when placed in header for correct compact layout"

requirements-completed: []

# Metrics
duration: 15min
completed: 2026-03-17
---

# Phase 20.1 Plan 02: Projection Page Redesign Summary

**Gradient background with inline FOUC fix, Fraunces h1 title at clamp(32px,3.6vw,56px), quorum bar promoted to header, and full clamp() responsive scaling for 4m projectors**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-03-17T07:30:00Z
- **Completed:** 2026-03-17T07:45:00Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments

- Eliminated FOUC on projection page by inlining critical CSS tokens before theme-init.js
- Applied deep gradient background (`linear-gradient(170deg, #0B0F1A, #111827)`) with light palette variant that switches on theme toggle
- Restructured header to a 3-column flex layout — status+counter | h1 title | clock+controls
- Promoted meeting title to `h1.projection-title` with Fraunces font at `clamp(32px, 3.6vw, 56px)` — readable from back of room
- Moved quorum visual bar from main content area into header, directly below title row
- Applied clamp() to all projector-critical elements: bar wrapper height, bar columns, bar-percentage, bar-label, decision-value

## Task Commits

1. **Task 1: FOUC fix + gradient background + header restructuring** - `15455e3` (feat)

**Plan metadata:** _(docs commit pending)_

## Files Created/Modified

- `public/public.htmx.html` — FOUC inline style, h1.projection-title, restructured header with quorum bar in header
- `public/assets/css/public.css` — gradient background, .projection-header-top/.projection-title/.projection-quorum styles, clamp() scaling on bars and decision values

## Decisions Made

- `h1.projection-title` preserves the existing `id="meeting_title"` so `public.js` requires zero changes
- `.projection-quorum` modifier on `.quorum-visual` constrains max-width to 600px and reduces vertical padding to `0.4vh` — fits neatly in header without overwhelming it
- Light gradient `#F8FAFC → #EEF2FF` mirrors wireframe v3.19.2 light palette

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None — changes were already partially applied (from context gathering session). Git diff confirmed all required modifications were in place and passing verification.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Projection page fully aligned with wireframe v3.19.2 PageEcran
- All existing IDs preserved — public.js continues to work without changes
- Ready for Plan 03 (vote.htmx.html votant page refonte) or subsequent 20.1 plans

---
*Phase: 20.1-refonte-ui-alignement-wireframe-et-reduction-charge-mentale*
*Completed: 2026-03-17*
