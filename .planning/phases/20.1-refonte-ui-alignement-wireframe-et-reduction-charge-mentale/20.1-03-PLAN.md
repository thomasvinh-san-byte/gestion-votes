---
phase: 20.1-refonte-ui-alignement-wireframe-et-reduction-charge-mentale
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/dashboard.htmx.html
  - public/assets/css/pages.css
  - public/operator.htmx.html
  - public/assets/css/operator.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Dashboard is a clean action launcher — 3 shortcut cards + 4 KPIs are the primary focus, not a dense data table"
    - "Dashboard visual density is reduced — generous spacing, less visual noise from pills/badges"
    - "Operator modals are simple — title + content + 2 buttons (cancel/confirm), no complex forms"
    - "FOUC is eliminated on dashboard and operator pages"
  artifacts:
    - path: "public/dashboard.htmx.html"
      provides: "Streamlined dashboard with FOUC inline style, reduced visual clutter"
    - path: "public/assets/css/pages.css"
      provides: "Dashboard layout spacing improvements, shortcut card emphasis"
    - path: "public/operator.htmx.html"
      provides: "FOUC-preventing inline style"
    - path: "public/assets/css/operator.css"
      provides: "Simplified modal styles, reduced charge mentale for operator view"
  key_links:
    - from: "public/assets/css/pages.css"
      to: "public/dashboard.htmx.html"
      via: "CSS classes for dashboard layout and shortcut cards"
      pattern: "shortcut-card|kpi-grid|dashboard"
---

<objective>
Refonte du dashboard (lanceur d'actions) et de la console operateur (modales simples, hierarchie). Correction FOUC sur les deux pages.

Purpose: Le dashboard doit etre un lanceur d'actions qui guide l'utilisateur, pas un tableau de bord dense. La console operateur doit avoir des modales simples (titre + contenu + 2 boutons) et une hierarchie visuelle claire avec focus sur la resolution en cours.
Output: Dashboard epure avec raccourcis proeminents + KPIs, console operateur avec modales simplifiees.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.1-refonte-ui-alignement-wireframe-et-reduction-charge-mentale/20.1-CONTEXT.md
@.planning/phases/20.1-refonte-ui-alignement-wireframe-et-reduction-charge-mentale/20.1-RESEARCH.md
@public/dashboard.htmx.html
@public/assets/css/pages.css
@public/operator.htmx.html
@public/assets/css/operator.css
@ag_vote_wireframe.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard FOUC fix + action launcher layout + reduced density</name>
  <files>public/dashboard.htmx.html, public/assets/css/pages.css</files>
  <action>
1. **FOUC fix** — In `dashboard.htmx.html`, add an inline `<style id="critical-tokens">` block right after `<meta charset="utf-8">` and BEFORE `<script src="/assets/js/theme-init.js">`:
   ```html
   <style id="critical-tokens">
     :root { --color-bg: #EDECE6; --color-surface: #FAFAF7; --color-text: #151510; }
     [data-theme="dark"] { --color-bg: #0B0D10; --color-surface: #141820; --color-text: #ECF0FA; }
     html, body { background: var(--color-bg); }
   </style>
   ```

2. **Dashboard hierarchy** — The HTML structure is already close to wireframe (3 shortcuts, 4 KPIs, 2 panels). The work is CSS-only density reduction in `pages.css`:
   - Increase spacing between major sections: add `.app-main > .dashboard-grid, .app-main > .grid-2, .app-main > .grid-3 { margin-bottom: var(--space-6); }` (or increase from current spacing to `var(--space-8)` between sections).
   - Make the shortcut cards (`.shortcut-card`) more prominent: larger icon circle (48px instead of 40px), slightly larger title text, more padding.
   - KPI cards (`.kpi-card`): ensure values are prominent with `font-size: var(--text-3xl)` and reduce visual noise of labels.
   - Urgent card (`#actionUrgente`): keep it but ensure it doesn't dominate when there's no urgent action — reduce visual weight when hidden.

3. **"Moins c'est plus" approach** — In `pages.css`:
   - Reduce `.page-sub` visual weight: `color: var(--color-text-muted); font-size: var(--text-sm);`
   - Shortcut card icons should be slightly larger and more colorful to act as clear CTAs.
   - The "Tout voir" link in the sessions panel should be subtle (ghost button is already correct).

4. **Responsive** — Ensure `.grid-3` (shortcuts) stacks to `grid-template-columns: 1fr` on mobile (`max-width: 640px`). KPI grid should go 2x2 on mobile.

IMPORTANT: Do NOT modify dashboard.js or any JS files. Do NOT rename any element IDs. Do NOT change the HTML structure significantly — the goal is CSS density reduction and spacing, not restructuring.
  </action>
  <verify>
    <automated>grep -c "critical-tokens" public/dashboard.htmx.html && vendor/bin/phpunit --testsuite Unit 2>&1 | tail -1</automated>
  </verify>
  <done>Dashboard has FOUC inline style. Visual density reduced with generous spacing. Shortcut cards are prominent action launchers. KPI values are the focal point. No JS modified.</done>
</task>

<task type="auto">
  <name>Task 2: Operator FOUC fix + simplified modals + exec view hierarchy</name>
  <files>public/operator.htmx.html, public/assets/css/operator.css</files>
  <action>
1. **FOUC fix** — In `operator.htmx.html`, add the same inline `<style id="critical-tokens">` block right after `<meta charset>` and BEFORE `<script src="/assets/js/theme-init.js">`:
   ```html
   <style id="critical-tokens">
     :root { --color-bg: #EDECE6; --color-surface: #FAFAF7; --color-text: #151510; }
     [data-theme="dark"] { --color-bg: #0B0D10; --color-surface: #141820; --color-text: #ECF0FA; }
     html, body { background: var(--color-bg); }
   </style>
   ```

2. **Simplified modals** (Claude's Discretion — user said "titre + contenu essentiel + 2 boutons") — In `operator.css`:
   - Review all modal styles (`.modal`, `.modal-content`, `.modal-body`, `.modal-footer`). Ensure they follow: clear title at top, essential content in body (no complex multi-column forms), and exactly 2 action buttons (cancel/confirm) in the footer.
   - Reduce modal padding density: ensure at least `var(--space-4)` padding on all sides.
   - Modal headers should use `font-weight: 700; font-size: var(--text-lg);` — clear and direct.
   - Modal footers should use `display: flex; gap: var(--space-3); justify-content: flex-end;` with buttons at standard sizes.

3. **Exec view hierarchy** (Claude's Discretion — "focus resolution en cours, onglet Avance secondaire"):
   - In `operator.css`, make the current-motion display (`.exec-current-motion` or similar) the dominant element: larger font size for the motion title, more padding, clearer visual separation.
   - Secondary controls (advanced tab content) should have slightly muted styling: `color: var(--color-text-secondary)` for labels, less prominent borders.
   - The vote action buttons (open/close vote) should be the largest, most prominent buttons on the operator page.

4. **General operator hierarchy** — In `operator.css`:
   - Ensure tab content areas have generous padding and spacing between elements.
   - Reduce visual noise: fewer borders between list items (use spacing instead), simpler status indicators.

IMPORTANT: Do NOT modify any operator JS files (`operator-exec.js`, `operator.js`). Do NOT rename any element IDs or data attributes. Do NOT remove any functionality — this is purely visual hierarchy and density adjustment.
  </action>
  <verify>
    <automated>grep -c "critical-tokens" public/operator.htmx.html && vendor/bin/phpunit --testsuite Unit 2>&1 | tail -1</automated>
  </verify>
  <done>Operator page has FOUC inline style. Modals follow simple pattern (title + content + 2 buttons). Exec view has clear hierarchy with current resolution prominent and secondary controls subdued. No JS modified.</done>
</task>

</tasks>

<verification>
- Open `dashboard.htmx.html` in browser: 3 shortcut cards visible and prominent, 4 KPIs readable, generous spacing
- Open `operator.htmx.html` in browser: open any modal — simple layout with title + 2 buttons
- DevTools Network throttle 3G on both pages: no FOUC flash
- `vendor/bin/phpunit --testsuite Unit` passes
- No JS console errors on either page
</verification>

<success_criteria>
- Dashboard is a clean action launcher with reduced density
- Operator modals are simplified (title + content + 2 buttons)
- Operator exec view has clear hierarchy
- FOUC eliminated on both pages
- No JS or functionality broken
</success_criteria>

<output>
After completion, create `.planning/phases/20.1-refonte-ui-alignement-wireframe-et-reduction-charge-mentale/20.1-03-SUMMARY.md`
</output>
