---
phase: 51-utility-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/public.htmx.html
  - public/assets/css/public.css
  - public/assets/js/pages/public.js
  - public/report.htmx.html
  - public/assets/css/report.css
  - public/assets/js/pages/report.js
autonomous: true
requirements: [UTL-03, UTL-04, WIRE-02]

must_haves:
  truths:
    - "Public/projector page renders results in a projection-optimized layout at full screen with large typography legible from distance"
    - "Public page forces dark theme on load, shows live vote results via SSE/polling with bar chart"
    - "Public page shows meeting title, motion title, quorum bar, decision section, resolution tracker pills"
    - "Public page meeting picker overlay appears when multiple live meetings exist"
    - "Report/PV page renders a print-ready layout at 880px max-width"
    - "Report page PV preview loads in iframe from real API endpoint"
    - "Report page export links (PDF, Excel, CSV) all point to correct API URLs with meeting_id parameter"
    - "Report page email sending form submits to real API endpoint"
    - "Report page PV status timeline shows generated/validated/sent/archived steps"
    - "No JS console errors on either page — all DOM IDs preserved for public.js and report.js"
  artifacts:
    - path: "public/public.htmx.html"
      provides: "Complete projection display page — full screen, no sidebar, bar chart, quorum, waiting state, secret vote, meeting picker"
      contains: "chart_container"
    - path: "public/assets/css/public.css"
      provides: "Projection styles — full-screen layout, large typography, bar chart, dark-first, responsive"
      contains: "projection-body"
    - path: "public/report.htmx.html"
      provides: "Complete report/PV page — preview iframe, export grid, email form, PV timeline, drawer"
      contains: "pvFrame"
    - path: "public/assets/css/report.css"
      provides: "Report styles — print layout at 880px, export grid, PV timeline, preview card"
      contains: "@media print"
  key_links:
    - from: "public/public.htmx.html"
      to: "public/assets/js/pages/public.js"
      via: "DOM IDs preserved for JS binding"
      pattern: "badge|meeting_title|motion_title|motion_sub|motion_number|resolution_box|resolution_text|waiting_state|secret_block|participation_bar|participation_pct|chart_container|bar_for|bar_against|bar_abstain|bar_for_fill|bar_against_fill|bar_abstain_fill|pct_for|pct_against|pct_abstain|count_for|count_against|count_abstain|decision_value|decision_detail|quorum_value|quorum_detail|voterTotal|voterTotalText|error_box|connectionLost|meeting_picker|meeting_picker_list|clock|footer_update|sr_alert|motionCounter|motionCounterText|btnChangeMeeting|btnThemeToggle|btnFullscreen|projectionModeToggle|resolutionTracker|quorumVisual|quorumVisualValue|quorumVisualFill|quorumSeuil"
    - from: "public/public.htmx.html"
      to: "public/assets/js/core/event-stream.js"
      via: "SSE event source for live updates"
      pattern: "event-stream"
    - from: "public/report.htmx.html"
      to: "public/assets/js/pages/report.js"
      via: "DOM IDs preserved for JS binding"
      pattern: "pvFrame|pvEmptyState|pvFrameLoading|btnExportPDF|btnOpenNewTab|exportPV|exportAttendance|exportVotes|exportMotions|exportMembers|exportAudit|exportFullXlsx|exportFullXlsxWithVotes|exportAttendanceXlsx|exportVotesXlsx|exportResultsXlsx|reportToArchives|meetingTitle|meetingName|meetingContext|notValidatedWarning|notif_box|btnSendEmail|email|emailMsg"
    - from: "public/assets/js/pages/report.js"
      to: "/api/v1/*.php"
      via: "export URL construction with meeting_id"
      pattern: "api/v1/export_.*\\.php|api/v1/report\\.php|api/v1/send_pv_email\\.php"
---

<objective>
Ground-up rebuild of the Public/Projector display page and Report/PV page — complete HTML+CSS rewrite from scratch with v4.3 design language. The public page is a standalone full-screen projection display (no sidebar, dark theme forced). The report page is a print-ready PV viewer with export links. Preserve all DOM IDs that public.js and report.js target.

Purpose: The projector display must be legible from a distance during live assemblies. The report/PV page must produce clean printed output for legal archives.
Output: New public.htmx.html + public.css, new report.htmx.html + report.css, with both JS files verified working.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-utility-pages/51-CONTEXT.md

@public/public.htmx.html
@public/assets/css/public.css
@public/assets/js/pages/public.js
@public/assets/js/core/event-stream.js
@public/report.htmx.html
@public/assets/css/report.css
@public/assets/js/pages/report.js
@public/assets/css/app.css
@public/dashboard.htmx.html

<interfaces>
<!-- Critical DOM IDs that public.js targets — ALL must exist in new HTML -->

Header:
- #badge — status badge (hors seance / en cours)
- #meeting_title — h1 meeting title
- #clock — clock display
- #motionCounter — motion counter container (hidden attr)
- #motionCounterText — "N / M" text
- #btnChangeMeeting — change meeting button (hidden attr)
- #btnThemeToggle — theme toggle button
- #btnFullscreen — fullscreen button
- #projectionModeToggle — vote/result mode toggle (hidden attr)
- [data-mode="vote"] / [data-mode="result"] — mode toggle buttons

Quorum bar:
- #quorumVisual — quorum bar container (hidden attr)
- #quorumVisualValue — quorum value text
- #quorumVisualFill — fill bar (.met class)
- #quorumSeuil — threshold marker

Motion:
- #motion_number — motion number text
- #motion_title — h1 motion title
- #motion_sub — subtitle paragraph

Resolution:
- #resolution_box — resolution container
- #resolution_text — resolution text

States:
- #waiting_state — waiting state container
- #secret_block — secret vote container
- #participation_bar — participation fill bar
- #participation_pct — participation percentage text

Chart:
- #chart_container — chart wrapper
- #bar_for / #bar_against / #bar_abstain — bar items
- #bar_for_fill / #bar_against_fill / #bar_abstain_fill — bar fill divs
- #pct_for / #pct_against / #pct_abstain — percentage texts
- #count_for / #count_against / #count_abstain — count texts

Decision:
- #decision_section — decision section
- #decision_value / #decision_detail — decision display
- #quorum_value / #quorum_detail — quorum display
- #voterTotal / #voterTotalText — voter total

Other:
- #error_box — error display
- #connectionLost — connection lost banner (hidden attr)
- #meeting_picker / #meeting_picker_list — meeting picker overlay
- #footer_update — last update timestamp
- #sr_alert — screen reader alert (aria-live)
- #resolutionTracker — resolution tracker pills (hidden attr)

<!-- Critical DOM IDs that report.js targets — ALL must exist in new HTML -->

PV Preview:
- #pvFrame — preview iframe
- #pvEmptyState — empty state div
- #pvFrameLoading — loading skeleton

Export links (all are <a> tags with href set by JS):
- #btnExportPDF — PDF export link
- #btnOpenNewTab — open in new tab link
- #exportPV — PV HTML export
- #exportAttendance — attendance CSV
- #exportVotes — votes CSV
- #exportMotions — motions CSV
- #exportMembers — members CSV
- #exportAudit — audit CSV
- #exportFullXlsx — full Excel export
- #exportFullXlsxWithVotes — full Excel with votes
- #exportAttendanceXlsx — attendance Excel
- #exportVotesXlsx — votes Excel
- #exportResultsXlsx — results Excel
- #reportToArchives — archives link

Context:
- #meetingTitle — meeting title in header subtitle
- #meetingName — meeting name in context alert
- #meetingContext — meeting context alert (display toggled)
- #notValidatedWarning — validation warning alert (display toggled)
- #notif_box — notification box

Email:
- #btnSendEmail — send email button
- #email — email input
- #emailMsg — email status message

PV Timeline:
- [data-step="generated|validated|sent|archived"] — timeline step elements
- .pv-timeline-step.done — completed step class

Drawer:
- [data-drawer="infos|context"] — drawer trigger buttons
- #drawer / #drawerTitle / #drawerBody — drawer elements
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Public/Projector display page (HTML+CSS)</name>
  <read_first>
    - public/assets/js/pages/public.js (full read — every getElementById, querySelector, data-attribute, event listener)
    - public/public.htmx.html (current HTML structure)
    - public/assets/css/public.css (current styles)
    - public/assets/js/core/event-stream.js (SSE connection pattern)
    - public/assets/css/design-system.css (if exists, for token reference — fallback to app.css)
  </read_first>
  <files>public/public.htmx.html, public/assets/css/public.css</files>
  <action>
    **HTML rewrite (public.htmx.html):**
    This is a STANDALONE projection page — NO app-shell, NO sidebar, NO app-header. Structure:
    1. Head: critical-tokens inline, theme-init.js then INLINE script to force dark theme (data-theme="dark" + localStorage), viewport, design-system.css (or app.css if design-system.css not found) + public.css
    2. data-page-role="public" on html element
    3. body.projection-body (no app-shell wrapper)
    4. header.projection-header:
       - .projection-header-top: .header-left (span#badge.status-badge.off "hors seance" + span#motionCounter hidden with span#motionCounterText), h1#meeting_title.projection-title "Aucune seance", .projection-header-right (#clock div + .projection-controls with #projectionModeToggle hidden containing [data-mode] buttons, #btnChangeMeeting hidden, #btnThemeToggle, #btnFullscreen)
       - .quorum-visual.projection-quorum#quorumVisual hidden: .quorum-visual-header (label + value#quorumVisualValue), .quorum-visual-bar with .quorum-visual-fill.met#quorumVisualFill style width 0% and .quorum-seuil#quorumSeuil style left 50%
    5. main.projection-main#main-content:
       - section.motion-section: div#motion_number.motion-number, h1#motion_title.motion-title, p#motion_sub.motion-subtitle
       - div#resolution_box.resolution-box: div#resolution_text.resolution-text
       - div#waiting_state.waiting-state: animated clock SVG, h3.waiting-title "En attente", p.waiting-text "Aucune resolution ouverte"
       - div#secret_block.secret-block: lock SVG, h4.secret-title "Vote secret", p.secret-text, .participation-bar > .participation-fill#participation_bar, .participation-text with span#participation_pct
       - div#chart_container.chart-container: .bar-chart with 3 .bar-item (for/against/abstain) each having .bar-label, .bar-wrapper > .bar (fill div), .bar-value with .bar-percentage + .bar-count spans. IDs: bar_for, bar_against, bar_abstain, bar_for_fill, bar_against_fill, bar_abstain_fill, pct_for, pct_against, pct_abstain, count_for, count_against, count_abstain
       - section#decision_section.decision-section: .decision-grid with 2 .decision-card (Decision: #decision_value + #decision_detail, Quorum: #quorum_value + #quorum_detail), span#voterTotal hidden with span#voterTotalText
       - div#error_box.error-box
       - div#connectionLost.projection-connection-lost hidden
       - div#meeting_picker.meeting-picker-overlay hidden: .meeting-picker with icon, h2, p, div#meeting_picker_list
    6. div#resolutionTracker.resolution-tracker hidden
    7. footer.projection-footer: .footer-brand "AG-VOTE", span#footer_update
    8. div#sr_alert.sr-only aria-live="assertive"
    9. Hidden standard app-footer (role="contentinfo" hidden) for compatibility
    10. Scripts: event-stream.js, public.js (NO utils.js, shared.js, shell.js — standalone page)

    **CSS rewrite (public.css):**
    Rewrite from scratch. Projection-optimized:
    - .projection-body: margin 0, padding 0, min-height 100vh, display flex column, background var(--color-bg), color var(--color-text), overflow hidden
    - .projection-header: padding var(--space-4) var(--space-6), display flex column, background var(--color-surface), border-bottom 1px solid var(--color-border)
    - .projection-header-top: display flex, justify-content space-between, align-items center
    - .projection-title: font-size clamp(1.5rem, 3vw, 2.5rem), font-weight 700, text-align center, flex 1
    - .projection-main: flex 1, display flex column, justify-content center, align-items center, padding var(--space-8), text-align center
    - LARGE TYPOGRAPHY: .motion-title font-size clamp(2rem, 5vw, 4rem), .motion-number font-size 1.25rem text-secondary, .motion-subtitle font-size 1.1rem text-secondary
    - .bar-chart: width 100%, max-width 800px, display flex column, gap var(--space-4)
    - .bar-item: display flex, align-items center, gap var(--space-3)
    - .bar-label: width 100px, text-align right, font-size 1.25rem, font-weight 600
    - .bar-wrapper: flex 1, height 48px, background var(--color-surface), border-radius var(--radius-md), overflow hidden
    - .bar: height 100%, border-radius var(--radius-md), transition width 0.6s ease
    - .bar-item.for .bar: background var(--color-success, #22c55e)
    - .bar-item.against .bar: background var(--color-danger, #ef4444)
    - .bar-item.abstain .bar: background var(--color-warning, #f59e0b)
    - .bar-value: min-width 120px, font-size 1.25rem
    - .bar-percentage: font-weight 700
    - .status-badge: padding var(--space-1) var(--space-3), border-radius 9999px, font-size 0.875rem, font-weight 600. .off: bg muted. .on: bg success with pulse animation
    - .waiting-state: display flex column center, gap var(--space-4). .waiting-icon: width 80px, height 80px, opacity 0.5
    - Clock SVG animations: .clock-hand-hour rotate 360deg / 43200s linear infinite, .clock-hand-minute rotate 360deg / 3600s linear infinite, .clock-hand-second rotate 360deg / 60s linear infinite
    - .secret-block: max-width 500px, text-align center. .participation-bar: height 8px, bg surface, radius full, overflow hidden. .participation-fill: height 100%, bg accent, transition width 0.3s
    - .decision-grid: display grid, grid-template-columns repeat(2, 1fr), gap var(--space-4), max-width 600px
    - .decision-card: bg surface, padding var(--space-4), radius-lg, text-align center. .decision-value: font-size 2rem, font-weight 800
    - .quorum-visual: padding var(--space-2) var(--space-6). .quorum-visual-bar: height 8px, bg surface, radius full. .quorum-visual-fill: height 100%, radius full, transition width 0.5s. .met: bg success
    - .projection-connection-lost: position fixed, bottom 0, left 0, right 0, bg danger, color white, text-align center, padding var(--space-3), z-index 100
    - .meeting-picker-overlay: position fixed, inset 0, bg rgba(0,0,0,0.7), display flex center, z-index 200. .meeting-picker: bg surface, radius-xl, padding var(--space-8), max-width 500px
    - .resolution-tracker: position fixed, bottom var(--space-16), left 50%, transform translateX(-50%), display flex, gap var(--space-2), z-index 50
    - .projection-footer: padding var(--space-2) var(--space-6), text-align center, opacity 0.5, font-size 0.75rem
    - .projection-controls: display flex, gap var(--space-2). .projection-btn: bg transparent, border 1px solid var(--color-border), radius-md, padding var(--space-2), cursor pointer, color var(--color-text)
    - .projection-mode-toggle: display flex, bg surface, radius-md, overflow hidden. .projection-mode-btn: padding var(--space-2) var(--space-4), border none, bg transparent, cursor pointer. .active: bg accent, color white

    CRITICAL: Do NOT change public.js. The HTML must match every selector the JS uses.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && echo "=== Public page IDs ===" && for id in badge meeting_title clock motionCounter motionCounterText btnChangeMeeting btnThemeToggle btnFullscreen projectionModeToggle quorumVisual quorumVisualValue quorumVisualFill quorumSeuil motion_number motion_title motion_sub resolution_box resolution_text waiting_state secret_block participation_bar participation_pct chart_container bar_for bar_against bar_abstain bar_for_fill bar_against_fill bar_abstain_fill pct_for pct_against pct_abstain count_for count_against count_abstain decision_section decision_value decision_detail quorum_value quorum_detail voterTotal voterTotalText error_box connectionLost meeting_picker meeting_picker_list footer_update sr_alert resolutionTracker; do grep -q "id=\"$id\"" public/public.htmx.html && echo "OK: $id" || echo "MISSING: $id"; done && echo "=== CSS checks ===" && grep -c 'projection-body' public/assets/css/public.css && grep -c 'bar-chart' public/assets/css/public.css && grep -c 'clamp' public/assets/css/public.css</automated>
  </verify>
  <acceptance_criteria>
    - All 47 DOM IDs from public.js present in public.htmx.html (zero MISSING lines)
    - grep 'data-mode="vote"' public/public.htmx.html returns match
    - grep 'data-mode="result"' public/public.htmx.html returns match
    - grep 'event-stream.js' public/public.htmx.html returns match
    - grep 'public.js' public/public.htmx.html returns match
    - grep 'data-theme.*dark' public/public.htmx.html returns match (forced dark theme)
    - grep 'projection-body' public.css returns match
    - grep 'clamp' public.css returns 2+ matches (responsive typography)
    - No app-shell, no app-sidebar in public.htmx.html (standalone page)
    - CSS uses var(--color-*) tokens, no hardcoded hex except fallback values
  </acceptance_criteria>
  <done>Public/projector page fully rebuilt with projection-optimized layout. Full-screen display with large typography, bar chart, quorum bar, waiting state, secret vote, meeting picker. All 47 DOM IDs preserved for public.js. Dark theme forced. SSE wired via event-stream.js.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild Report/PV page (HTML+CSS) + verify both pages</name>
  <read_first>
    - public/assets/js/pages/report.js (full read — every getElementById, querySelector, event listener, API URL)
    - public/report.htmx.html (current HTML structure)
    - public/assets/css/report.css (current styles)
    - public/assets/js/services/meeting-context.js (meeting context service pattern)
  </read_first>
  <files>public/report.htmx.html, public/assets/css/report.css</files>
  <action>
    **HTML rewrite (report.htmx.html):**
    Rewrite from scratch using v4.3 design language. Structure:
    1. Standard head with critical-tokens, theme-init.js, fonts, app.css + report.css
    2. data-page-role="operator,president,auditor" on html element
    3. app-shell with app-sidebar (data-include-sidebar data-page="operator")
    4. app-header: left side h1 with icon-clipboard-list "Proces-verbal" + p#meetingTitle.text-sm.text-secondary "Previsualisation et export". Right side: a#btnExportPDF.btn.btn-primary (icon-file-text + "Exporter PDF"), drawer buttons for infos and context
    5. app-main#main-content > div.container:
       - div#notif_box.hidden.mb-4
       - Meeting context alert: div#meetingContext.alert.alert-info.mb-6 style display none with icon-pin + "Seance:" strong#meetingName
       - Not validated warning: div#notValidatedWarning.alert.alert-warning.mb-6 style display none
       - Email card: .card.mb-6 with card-header (h3 icon-mail "Envoyer le PV par email"), card-body with .email-form (form-group with label + input#email type=email placeholder, button#btnSendEmail.btn.btn-primary icon-send), div#emailMsg.mt-3 display none
       - PV Preview card: .card.mb-6 with card-header (h3 icon-eye "Previsualisation du PV" + a#btnOpenNewTab.btn.btn-secondary.btn-sm icon-external-link), card-body.p-0 with .pv-preview (div#pvEmptyState with SVG + title + desc, div#pvFrameLoading hidden with skeletons, iframe#pvFrame src about:blank display none)
       - PV Timeline: .pv-timeline with 4 steps (generated done, validated, sent, archived) each having .pv-timeline-step[data-step], .pv-step-dot, .pv-step-label, connected by .pv-timeline-connector
       - Full Excel export card: .card.mb-6 with card-header (h3 icon-file-spreadsheet), card-body (a#exportFullXlsx + a#exportFullXlsxWithVotes)
       - Individual exports card: .card with card-header (h3 icon-download "Exports individuels"), card-body with:
         a#pvDownloadCta.pv-download-cta (download icon + "Telecharger le PV" + "PDF" meta)
         h4 "Format Excel (.xlsx)" + .export-grid (a#exportAttendanceXlsx, a#exportVotesXlsx, a#exportResultsXlsx)
         h4 "Format CSV" + .export-grid (a#exportPV, a#exportAttendance, a#exportVotes, a#exportMotions, a#exportMembers, a#exportAudit) — each .export-btn with icon + label
       - Navigation: flex gap-2 justify-end mt-4 with a#reportToArchives.btn.btn-secondary.btn-sm icon-archive
    6. Standard mobile-footer
    7. Drawer: backdrop + aside#drawer with #drawerTitle + #drawerBody
    8. Scripts: utils.js, components/index.js, shared.js, shell.js, meeting-context.js, report.js

    **CSS rewrite (report.css):**
    Rewrite from scratch:
    - .email-form: display flex, gap var(--space-3), align-items flex-end. Responsive: stack on mobile
    - .pv-preview: min-height 400px, position relative. iframe: width 100%, min-height 600px, border none
    - .pv-empty-state: display flex column center, padding var(--space-12), text-muted. .pv-empty-state-icon: opacity 0.3
    - .pv-timeline: display flex, align-items center, justify-content center, gap 0, padding var(--space-6), flex-wrap wrap
    - .pv-timeline-step: display flex column center, position relative. .pv-step-dot: width 32px, height 32px, border-radius 50%, border 2px solid var(--color-border), bg surface, display flex center. .pv-step-label: margin-top var(--space-2), font-size 0.75rem, text-secondary
    - .pv-timeline-step.done .pv-step-dot: bg var(--color-success), border-color var(--color-success), color white (checkmark)
    - .pv-timeline-connector: width 40px, height 2px, bg var(--color-border), flex-shrink 0
    - .export-grid: display grid, grid-template-columns repeat(auto-fill, minmax(180px, 1fr)), gap var(--space-3)
    - .export-btn: display flex column center, padding var(--space-4), bg surface, border 1px solid var(--color-border), radius-lg, text-decoration none, color var(--color-text), transition. hover: shadow-sm, border-color var(--color-accent)
    - .export-btn-icon: margin-bottom var(--space-2), opacity 0.6
    - .export-btn-label: font-size 0.875rem, font-weight 500
    - .pv-download-cta: display flex, align-items center, gap var(--space-3), padding var(--space-4), bg accent/10, border 1px solid accent/30, radius-lg, margin-bottom var(--space-6), text-decoration none, color var(--color-text), font-weight 600. hover: bg accent/20
    - @media print: hide app-sidebar, app-header, app-footer, .email-form, .pv-timeline, .export-grid, .pv-download-cta, drawer, [data-drawer]. Show only .pv-preview iframe at full width. Body max-width 880px, margin auto
    - Dark mode: via tokens

    CRITICAL: Do NOT change report.js. The HTML must match every selector the JS uses.

    **After both rebuilds, verify wiring:**
    1. Check every getElementById in public.js has matching ID in public.htmx.html
    2. Check every getElementById in report.js has matching ID in report.htmx.html
    3. Verify export link IDs all present
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && echo "=== Report page IDs ===" && for id in pvFrame pvEmptyState pvFrameLoading btnExportPDF btnOpenNewTab exportPV exportAttendance exportVotes exportMotions exportMembers exportAudit exportFullXlsx exportFullXlsxWithVotes exportAttendanceXlsx exportVotesXlsx exportResultsXlsx reportToArchives meetingTitle meetingName meetingContext notValidatedWarning notif_box btnSendEmail email emailMsg; do grep -q "id=\"$id\"" public/report.htmx.html && echo "OK: $id" || echo "MISSING: $id"; done && echo "=== Report CSS ===" && grep -c '@media print' public/assets/css/report.css && grep -c 'pv-timeline' public/assets/css/report.css && grep -c 'export-grid' public/assets/css/report.css && echo "=== Data steps ===" && grep -c 'data-step=' public/report.htmx.html</automated>
  </verify>
  <acceptance_criteria>
    - All 24 report.js DOM IDs present in report.htmx.html (zero MISSING lines)
    - grep 'data-step="generated"' report.htmx.html returns match
    - grep 'data-step="validated"' report.htmx.html returns match
    - grep 'data-step="sent"' report.htmx.html returns match
    - grep 'data-step="archived"' report.htmx.html returns match
    - grep '@media print' report.css returns match (print styles exist)
    - grep '880px' report.css returns match (print max-width)
    - grep 'export-grid' report.css returns match
    - grep 'pv-timeline' report.css returns match
    - grep 'report.js' report.htmx.html returns match
    - grep 'meeting-context.js' report.htmx.html returns match
    - Report page drawer structure preserved (data-drawer="infos", data-drawer="context")
    - Public page task 1 IDs still all pass verification
  </acceptance_criteria>
  <done>Report/PV page fully rebuilt with v4.3 design language. Print-ready layout at 880px. PV preview iframe, export grid (Excel + CSV), email form, PV status timeline all structurally correct. All 24 DOM IDs preserved for report.js. Both projector and report pages pass full selector verification.</done>
</task>

</tasks>

<verification>
1. All DOM IDs from public.js exist in public.htmx.html (47 IDs)
2. All DOM IDs from report.js exist in report.htmx.html (24 IDs)
3. Public page is standalone (no app-shell, no sidebar)
4. Public page forces dark theme inline
5. Report page has @media print rules with 880px max-width
6. Both CSS files use design tokens
7. Export links in report page use <a> tags (not buttons) for href setting by JS
8. SSE script (event-stream.js) wired in public page
</verification>

<success_criteria>
- Public/projector: full-screen projection display, bar chart shows results, large typography legible from distance, dark theme, SSE/polling updates
- Report/PV: preview iframe loads, all export links point to API, email form functional, print layout at 880px clean, PV timeline displays status
- Zero JS selector mismatches between HTML and JS files
</success_criteria>

<output>
After completion, create `.planning/phases/51-utility-pages/51-02-SUMMARY.md`
</output>
