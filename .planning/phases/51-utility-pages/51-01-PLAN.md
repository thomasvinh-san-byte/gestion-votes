---
phase: 51-utility-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/help.htmx.html
  - public/assets/css/help.css
  - public/assets/js/pages/help-faq.js
  - public/email-templates.htmx.html
  - public/assets/css/email-templates.css
  - public/assets/js/pages/email-templates-editor.js
autonomous: true
requirements: [UTL-01, UTL-02, WIRE-01]

must_haves:
  truths:
    - "Help page renders all FAQ accordion sections with correct spacing and expand/collapse"
    - "Help page search field filters visible FAQ items in real time — matching items stay, non-matching hide"
    - "Help page tabs filter FAQ sections by category (all, general, operator, vote, members, security)"
    - "Help page tour cards grid renders with proper card layout and role-based visibility"
    - "Email templates page renders template grid with cards fetched from backend"
    - "Email templates editor opens with form fields and live preview iframe that updates on content change"
    - "Saved email templates persist after page reload — create and edit operations call real API"
    - "No JS console errors on either page — all DOM IDs preserved for help-faq.js and email-templates-editor.js"
  artifacts:
    - path: "public/help.htmx.html"
      provides: "Complete help/FAQ page with tour cards, search, tabs, accordion sections"
      contains: "faqSearch"
    - path: "public/assets/css/help.css"
      provides: "Help page styles — tour grid, search, tabs, FAQ accordion, responsive"
      contains: "faq-item"
    - path: "public/assets/js/pages/help-faq.js"
      provides: "Help FAQ JS — tab filtering, search filtering, accordion toggle, role visibility"
      contains: "faqSearch"
    - path: "public/email-templates.htmx.html"
      provides: "Complete email templates page with grid, editor modal, preview panel"
      contains: "templatesGrid"
    - path: "public/assets/css/email-templates.css"
      provides: "Email templates styles — grid, editor modal, preview panel, responsive"
      contains: "template-editor"
    - path: "public/assets/js/pages/email-templates-editor.js"
      provides: "Email templates JS — CRUD, preview, filter, variable insertion"
      contains: "templatesGrid"
  key_links:
    - from: "public/help.htmx.html"
      to: "public/assets/js/pages/help-faq.js"
      via: "DOM selectors preserved for JS binding"
      pattern: "faqSearch|help-tab|faq-section|faq-item|faq-question|faq-answer|data-tab|data-category|data-required-role|faqContent"
    - from: "public/email-templates.htmx.html"
      to: "public/assets/js/pages/email-templates-editor.js"
      via: "DOM IDs preserved for JS binding"
      pattern: "templatesGrid|emptyState|filterType|templateEditor|previewFrame|variablesList|templateId|templateName|templateType|templateSubject|templateBody|templateIsDefault|editorTitle|editorStatus|btnNewTemplate|btnEmptyCreate|btnCloseEditor|btnCancelEdit|btnSaveTemplate|btnCreateDefaults|btnRefreshPreview"
    - from: "public/assets/js/pages/email-templates-editor.js"
      to: "/api/v1/email_templates.php"
      via: "fetch calls for template CRUD"
      pattern: "api.*email_templates"
---

<objective>
Ground-up rebuild of the Help/FAQ page and Email Templates page — complete HTML+CSS rewrite from scratch with v4.3 design language (gradient accent bar, shadow-md cards, token-based dark mode). Preserve all DOM IDs and class selectors that help-faq.js and email-templates-editor.js target. Verify accordion, search, tabs, editor preview, and CRUD all wire correctly.

Purpose: Help/FAQ is the user's self-service support entry point. Email templates is the admin's communication customization tool. Both must match the quality standard of already-rebuilt pages.
Output: New help.htmx.html + help.css, new email-templates.htmx.html + email-templates.css, with both JS files verified working.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-utility-pages/51-CONTEXT.md

@public/help.htmx.html
@public/assets/css/help.css
@public/assets/js/pages/help-faq.js
@public/email-templates.htmx.html
@public/assets/css/email-templates.css
@public/assets/js/pages/email-templates-editor.js
@public/assets/css/app.css
@public/dashboard.htmx.html

<interfaces>
<!-- Critical DOM selectors that help-faq.js targets — ALL must exist in new HTML -->

Search:
- #faqSearch — search input field

Tabs:
- .help-tab — tab buttons with data-tab attribute
- .help-tab.active — active tab
- [data-tab="all"] — all tab (default)

FAQ sections:
- .faq-section — section containers with data-category attribute
- [data-category="general|operator|vote|members|security"] — category filter
- [data-required-role] — role-based visibility on sections and tour cards

FAQ items:
- .faq-item — individual FAQ items with data-search attribute
- .faq-question — clickable question header (toggle)
- .faq-question span — question text for search matching
- .faq-answer — collapsible answer content
- .faq-chevron — rotating chevron icon

<!-- Critical DOM IDs that email-templates-editor.js targets — ALL must exist in new HTML -->

Grid + state:
- #templatesGrid — template cards container (rendered by JS)
- #emptyState — empty state (display toggled by JS)
- #filterType — type filter select

Editor modal:
- #templateEditor — editor overlay (.active class toggles visibility)
- #previewFrame — live preview iframe
- #variablesList — available variables container (rendered by JS)

Form fields:
- #templateId — hidden input (template ID for edit)
- #templateName — name input
- #templateType — type select
- #templateSubject — subject input
- #templateBody — body textarea
- #templateIsDefault — default checkbox
- #editorTitle — editor title text
- #editorStatus — status text

Buttons:
- #btnNewTemplate — new template button
- #btnEmptyCreate — empty state create button
- #btnCloseEditor — close editor X button
- #btnCancelEdit — cancel button
- #btnSaveTemplate — save button
- #btnCreateDefaults — create defaults button
- #btnRefreshPreview — refresh preview button
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Help/FAQ page (HTML+CSS)</name>
  <read_first>
    - public/assets/js/pages/help-faq.js (full read — understand every querySelector, getElementById, data-attribute)
    - public/help.htmx.html (current HTML — extract all FAQ content text)
    - public/assets/css/help.css (current styles)
    - public/assets/css/app.css (design tokens, utility classes)
    - public/dashboard.htmx.html (reference v4.3 page-title pattern with gradient bar)
  </read_first>
  <files>public/help.htmx.html, public/assets/css/help.css</files>
  <action>
    **HTML rewrite (help.htmx.html):**
    Rewrite from scratch using v4.3 design language. Structure:
    1. Standard head with critical-tokens, theme-init.js, app.css + help.css
    2. app-shell with app-sidebar (data-include-sidebar data-page="help")
    3. NO app-header — help is a viewer page, use in-content header instead
    4. app-main.help-main with:
       - Page header: v4.3 gradient accent bar (4px top, var(--color-accent) to transparent), page icon (icon-help-circle), title "Aide & FAQ", subtitle "Trouvez rapidement des reponses a vos questions"
       - Tour cards grid section: h2.help-section-heading with icon-play, div.tour-grid with tour-card links. Each card: tour-icon (SVG), tour-body (tour-launch "Lancer", tour-name, tour-meta). Preserve ALL data-required-role attributes exactly as current HTML.
       - Search: div.help-search with search-input-wrap containing icon-search SVG and input#faqSearch.form-input.search-input-field with placeholder "Rechercher dans la FAQ..."
       - Tabs: div.help-tabs with button.help-tab elements. First tab active with data-tab="all". Other tabs: general, operator (data-required-role="admin,operator"), vote, members (data-required-role="admin,operator"), security (data-required-role="admin,auditor,assessor")
       - FAQ content: div#faqContent containing .faq-section blocks each with data-category. Each section has .faq-section-title with icon + text, and .faq-item children with data-search. Each item: .faq-question (span + .faq-chevron icon-chevron-right), .faq-answer (collapsible)
       - Preserve ALL existing FAQ content text verbatim (general, operator, vote, members, security sections with all Q&A pairs)
    5. Standard mobile-footer
    6. Scripts: components/index.js, utils.js, shared.js, shell.js, help-faq.js

    **CSS rewrite (help.css):**
    Rewrite from scratch using design tokens. Key rules:
    - .help-main: padding using var(--space-6)
    - Page header with gradient accent bar: 4px height, var(--color-accent) background, border-radius top
    - .tour-grid: CSS grid, 2 columns on desktop (repeat(auto-fill, minmax(280px, 1fr))), gap var(--space-4)
    - .tour-card: var(--color-surface) background, border-radius var(--radius-lg), shadow-md, hover:shadow-lg transition, flex row layout with tour-icon (48px, rounded, accent background) and tour-body
    - .help-search: max-width 480px, margin 0 auto var(--space-6)
    - .search-input-wrap: relative position, icon absolutely positioned left
    - .help-tabs: flex wrap, gap var(--space-2), margin-bottom var(--space-6)
    - .help-tab: pill shape (border-radius 9999px), padding var(--space-2) var(--space-4), border 1px solid var(--color-border), transition. Active: var(--color-accent) background, white text
    - .faq-section: margin-bottom var(--space-6)
    - .faq-section-title: font-semibold, flex with icon, margin-bottom var(--space-3), var(--color-text-secondary)
    - .faq-item: border-bottom 1px solid var(--color-border), overflow hidden
    - .faq-question: padding var(--space-4), cursor pointer, flex justify-between, font-medium, hover background var(--color-surface-hover)
    - .faq-chevron: transition transform 0.2s; .faq-item.open .faq-chevron: rotate(90deg)
    - .faq-answer: max-height 0, overflow hidden, transition max-height 0.3s ease; padding 0 var(--space-4)
    - .faq-item.open .faq-answer: max-height 500px, padding-bottom var(--space-4)
    - Dark mode: all via token variables, no manual overrides needed
    - Print: hide tour-grid, help-tabs, help-search

    CRITICAL: Do NOT change help-faq.js. The HTML must match every selector the JS uses.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c 'faqSearch' public/help.htmx.html && grep -c 'help-tab' public/help.htmx.html && grep -c 'faq-section' public/help.htmx.html && grep -c 'faq-item' public/help.htmx.html && grep -c 'faq-question' public/help.htmx.html && grep -c 'faq-answer' public/help.htmx.html && grep -c 'data-tab=' public/help.htmx.html && grep -c 'data-category=' public/help.htmx.html && grep -c 'data-required-role' public/help.htmx.html && grep -c 'data-search=' public/help.htmx.html && grep -c 'faq-chevron' public/help.htmx.html && grep -c 'help-faq.js' public/help.htmx.html</automated>
  </verify>
  <acceptance_criteria>
    - grep 'faqSearch' public/help.htmx.html returns match (search input ID preserved)
    - grep 'help-tab.*data-tab' public/help.htmx.html returns 6+ matches (all tabs present)
    - grep 'faq-section.*data-category' public/help.htmx.html returns 5+ matches (all sections)
    - grep 'faq-item' public/help.htmx.html returns 15+ matches (all FAQ items preserved)
    - grep 'faq-question' public/help.htmx.html returns matches
    - grep 'faq-answer' public/help.htmx.html returns matches
    - grep 'faq-chevron' public/help.htmx.html returns matches
    - grep 'data-required-role' public/help.htmx.html returns 8+ matches (role-based visibility)
    - grep 'help-faq.js' public/help.htmx.html returns match (JS wired)
    - grep 'tour-card' public/help.htmx.html returns 10 matches (all tour cards)
    - help.css uses var(--color-*) and var(--space-*) tokens throughout
    - No inline styles except where JS manages display
  </acceptance_criteria>
  <done>Help/FAQ page fully rebuilt with v4.3 design language. All FAQ content preserved verbatim. All DOM selectors match help-faq.js expectations. Tour cards, search, tabs, accordion all structurally correct.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild Email Templates page (HTML+CSS) + verify both pages</name>
  <read_first>
    - public/assets/js/pages/email-templates-editor.js (full read — every getElementById, querySelector, event listener)
    - public/email-templates.htmx.html (current HTML)
    - public/assets/css/email-templates.css (current styles)
    - public/settings.htmx.html (reference sidebar-tab pattern for editor layout)
  </read_first>
  <files>public/email-templates.htmx.html, public/assets/css/email-templates.css</files>
  <action>
    **HTML rewrite (email-templates.htmx.html):**
    Rewrite from scratch using v4.3 design language. Structure:
    1. Standard head with critical-tokens, theme-init.js, fonts, app.css + email-templates.css
    2. app-shell with app-sidebar (data-include-sidebar data-page="admin")
    3. app-header with back arrow to /admin.htmx.html, page-title "Templates Email"
    4. app-main.email-templates-main:
       - Page header zone: v4.3 gradient accent bar (4px), icon-mail, breadcrumb "Administration > Templates Email"
       - Toolbar: flex row with #filterType select (options: Tous les types, Invitation, Rappel, Confirmation, Personnalise) and buttons #btnCreateDefaults (icon-refresh + text) + #btnNewTemplate (icon-plus + text)
       - Templates grid: div#templatesGrid.templates-grid (rendered by JS)
       - Empty state: div#emptyState.empty-state.email-templates-empty (display:none) with icon-mail SVG, h2 "Aucun template", description text, #btnEmptyCreate button
    5. Standard mobile-footer
    6. Template Editor Modal (div#templateEditor.template-editor):
       - .template-editor-content > .template-editor-header (h2#editorTitle + #btnCloseEditor icon-x)
       - .template-editor-body with two panels:
         Left (.template-editor-form): hidden #templateId, form-group #templateName input, form-group #templateType select, form-group #templateSubject input, form-group textarea#templateBody rows=12, form-group checkbox #templateIsDefault, .variables-section with h4 + div#variablesList
         Right (.template-editor-preview): .preview-header (icon-eye + "Previsualisation" + #btnRefreshPreview), iframe#previewFrame.preview-frame sandbox=allow-same-origin
       - .template-editor-footer: #editorStatus span, #btnCancelEdit, #btnSaveTemplate (icon-check + "Enregistrer")
    7. Scripts: components/index.js, utils.js, shared.js, shell.js, email-templates-editor.js

    **CSS rewrite (email-templates.css):**
    Rewrite from scratch:
    - .email-templates-main: standard main padding
    - .templates-grid: CSS grid, repeat(auto-fill, minmax(320px, 1fr)), gap var(--space-4)
    - Template card styling (rendered by JS but CSS needed): .template-card with surface bg, radius-lg, shadow-sm, hover:shadow-md, padding var(--space-4)
    - .empty-state: centered flex column, padding var(--space-12)
    - .template-editor: fixed overlay, inset 0, z-index 1000, display none, background rgba(0,0,0,0.5). &.active: display flex, align-items center, justify-content center
    - .template-editor-content: max-width 1100px, width 95vw, max-height 90vh, surface bg, radius-xl, shadow-2xl, flex column
    - .template-editor-header: flex justify-between, padding var(--space-4) var(--space-6), border-bottom
    - .template-editor-body: flex row, flex 1, overflow hidden, min-height 0
    - .template-editor-form: flex 1, overflow-y auto, padding var(--space-6), min-width 0
    - .template-editor-preview: flex 1, border-left, display flex column
    - .preview-header: padding var(--space-3) var(--space-4), border-bottom, flex items-center gap
    - .preview-frame: flex 1, border none, width 100%
    - .template-editor-footer: flex justify-end items-center gap, padding var(--space-4) var(--space-6), border-top
    - .variables-section: margin-top var(--space-4), padding var(--space-3), bg surface-alt, radius-md
    - Responsive: below 768px editor body stacks vertically (flex-direction column)
    - Dark mode: via tokens

    CRITICAL: Do NOT change email-templates-editor.js. The HTML must match every selector the JS uses.

    **After both rebuilds, verify wiring:**
    1. Check that every getElementById in help-faq.js has a matching ID in help.htmx.html
    2. Check that every getElementById in email-templates-editor.js has a matching ID in email-templates.htmx.html
    3. Check that every querySelector/querySelectorAll class in both JS files has matching classes in HTML
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && echo "=== Email Templates IDs ===" && for id in templatesGrid emptyState filterType templateEditor previewFrame variablesList templateId templateName templateType templateSubject templateBody templateIsDefault editorTitle editorStatus btnNewTemplate btnEmptyCreate btnCloseEditor btnCancelEdit btnSaveTemplate btnCreateDefaults btnRefreshPreview; do grep -q "id=\"$id\"" public/email-templates.htmx.html && echo "OK: $id" || echo "MISSING: $id"; done && echo "=== Help IDs ===" && for id in faqSearch faqContent; do grep -q "id=\"$id\"" public/help.htmx.html && echo "OK: $id" || echo "MISSING: $id"; done && echo "=== Help classes ===" && for cls in help-tab faq-section faq-item faq-question faq-answer faq-chevron; do grep -q "$cls" public/help.htmx.html && echo "OK: $cls" || echo "MISSING: $cls"; done && echo "=== JS refs ===" && grep -q 'email-templates-editor.js' public/email-templates.htmx.html && echo "OK: email-templates-editor.js wired" || echo "MISSING: JS not wired"</automated>
  </verify>
  <acceptance_criteria>
    - All 21 email-templates DOM IDs present in HTML (templatesGrid, emptyState, filterType, templateEditor, previewFrame, variablesList, templateId, templateName, templateType, templateSubject, templateBody, templateIsDefault, editorTitle, editorStatus, btnNewTemplate, btnEmptyCreate, btnCloseEditor, btnCancelEdit, btnSaveTemplate, btnCreateDefaults, btnRefreshPreview)
    - grep 'template-editor' email-templates.css returns matches (editor styles exist)
    - grep 'templates-grid' email-templates.css returns matches (grid styles exist)
    - grep '.active' email-templates.css returns match (editor toggle class styled)
    - Email templates editor has two-panel layout (form left, preview right)
    - grep 'email-templates-editor.js' email-templates.htmx.html returns match
    - All help page selectors verified in Task 1 still pass
    - Both CSS files use token variables throughout, no hardcoded hex colors
  </acceptance_criteria>
  <done>Email templates page fully rebuilt with v4.3 design language. Editor modal has form+preview two-panel layout. All 21 DOM IDs preserved for email-templates-editor.js. Both help and email templates pages pass full selector verification.</done>
</task>

</tasks>

<verification>
1. All DOM IDs from help-faq.js exist in help.htmx.html
2. All DOM IDs from email-templates-editor.js exist in email-templates.htmx.html
3. All class selectors (.help-tab, .faq-section, .faq-item, .faq-question, .faq-answer, .template-editor.active) match between HTML and JS
4. Both CSS files use design tokens (var(--color-*), var(--space-*), var(--radius-*))
5. No hardcoded hex color values in CSS
6. Both pages include correct script tags for their JS files
7. FAQ content text preserved verbatim from original
</verification>

<success_criteria>
- Help/FAQ page: accordion expands/collapses, search filters items, tabs filter sections, tour cards display in grid, role-based visibility works
- Email templates page: template grid renders, editor modal opens/closes with .active class, preview iframe updates, CRUD operations wire to API
- Zero JS selector mismatches between HTML and JS files
</success_criteria>

<output>
After completion, create `.planning/phases/51-utility-pages/51-01-SUMMARY.md`
</output>
