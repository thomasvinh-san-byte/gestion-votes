---
phase: 81-fix-ux-interactivity-blocking-popups-broken-layouts-fragile-frontend-wiring
plan: 03
type: execute
wave: 2
depends_on: [81-02]
files_modified:
  - public/assets/js/pages/users.js
  - public/assets/js/pages/operator-tabs.js
  - public/assets/js/pages/members.js
  - public/assets/js/pages/meetings.js
  - public/assets/js/pages/admin.js
  - public/assets/js/pages/settings.js
  - public/assets/js/pages/postsession.js
  - public/assets/js/pages/wizard.js
  - public/assets/js/pages/hub.js
  - public/assets/js/core/shared.js
autonomous: true
requirements: [D-08, D-09, D-10]

must_haves:
  truths:
    - "Every API call has visible feedback: loading state during request, success toast or UI update on completion, error toast on failure"
    - "No fetch .catch() silently discards errors — all show AgToast.show('error', ...)"
    - "AgToast.show() is called with correct argument order (type, message, duration) everywhere"
    - "users.js no longer has a local setNotif() that bypasses AgToast"
    - "Double-submit is prevented on all form submit buttons via Shared.btnLoading()"
  artifacts:
    - path: "public/assets/js/pages/users.js"
      provides: "Global setNotif used (local override removed)"
    - path: "public/assets/js/pages/operator-tabs.js"
      provides: "Corrected AgToast.show argument order, removed ternary guards"
      contains: "AgToast.show"
  key_links:
    - from: "public/assets/js/pages/users.js"
      to: "public/assets/js/core/utils.js"
      via: "Global setNotif() function (no local shadow)"
      pattern: "setNotif"
    - from: "All page JS files"
      to: "public/assets/js/components/ag-toast.js"
      via: "AgToast.show(type, message, duration) for all API feedback"
      pattern: "AgToast\\.show"
---

<objective>
Fix toast notification wiring (users.js local setNotif override, AgToast argument order bugs), ensure every API call has visible loading/success/error feedback, and prevent double-submit on all forms.

Purpose: Eliminate silent failures (D-08), standardize feedback patterns (D-09), and prevent double-submit (D-10).
Output: Every API call across all pages shows loading state, success feedback, and error feedback. No silent failures.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-fix-ux-interactivity-blocking-popups-broken-layouts-fragile-frontend-wiring/81-CONTEXT.md
@.planning/phases/81-fix-ux-interactivity-blocking-popups-broken-layouts-fragile-frontend-wiring/81-RESEARCH.md
@.planning/phases/81-fix-ux-interactivity-blocking-popups-broken-layouts-fragile-frontend-wiring/81-UI-SPEC.md
@.planning/phases/81-fix-ux-interactivity-blocking-popups-broken-layouts-fragile-frontend-wiring/81-02-SUMMARY.md

<interfaces>
<!-- AgToast.show() — CORRECT argument order -->
AgToast.show(type, message, duration?)
// type: 'success' | 'error' | 'warning' | 'info'
// message: string (French)
// duration: number (optional, ms — default 5000 for success/info, 8000 for error/warning)

<!-- Shared.btnLoading() — button loading state -->
Shared.btnLoading(buttonElement, isLoading, loadingText?)
// isLoading: true = disable + show spinner text, false = restore original
// loadingText: optional string, default 'Chargement...'

<!-- Pattern A — Create/Delete/Long operation -->
Shared.btnLoading(btn, true);
try {
  const r = await api(url, opts);
  if (r.body?.ok) {
    AgToast.show('success', 'Message de confirmation');
    // refresh list
  } else {
    AgToast.show('error', r.body?.error || 'Une erreur est survenue', 8000);
  }
} catch (e) {
  AgToast.show('error', 'Erreur de connexion — verifiez votre connexion et reessayez', 8000);
} finally {
  Shared.btnLoading(btn, false);
}

<!-- Pattern B — Toggle/Quick action (optimistic) -->
el.classList.toggle('active');
try {
  const r = await api(url, opts);
  if (!r.body?.ok) {
    el.classList.toggle('active'); // revert
    AgToast.show('error', 'Modification non enregistree', 8000);
  }
} catch (e) {
  el.classList.toggle('active'); // revert
  AgToast.show('error', 'Modification non enregistree', 8000);
}

<!-- setNotif() in utils.js — global version, already delegates to AgToast -->
function setNotif(type, msg) { /* ... AgToast.show(type, msg) ... */ }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix toast wiring bugs — users.js local setNotif, operator-tabs.js AgToast arg order</name>
  <files>public/assets/js/pages/users.js, public/assets/js/pages/operator-tabs.js</files>
  <read_first>
    - public/assets/js/pages/users.js (local setNotif at lines 27-38, all setNotif call sites)
    - public/assets/js/pages/operator-tabs.js (search for AgToast.show — verify argument order at every call site, especially any where message comes before type)
    - public/assets/js/core/utils.js (global setNotif definition to confirm it delegates to AgToast)
    - public/assets/js/components/ag-toast.js (AgToast.show signature: type, message, duration)
  </read_first>
  <action>
    **users.js — Remove local setNotif override:**

    1. Delete the local `setNotif()` function definition at lines 27-38 (the function that writes to `#notif_box`).
    2. Delete the associated `#notif_box` DOM element creation if it exists in the page init.
    3. All existing `setNotif('error', ...)` and `setNotif('success', ...)` calls in users.js will now correctly fall through to the global `setNotif()` defined in utils.js, which delegates to `AgToast.show()`.
    4. Verify that all `setNotif` call sites in users.js use the correct argument order: `setNotif(type, message)` where type is 'success', 'error', etc.

    **operator-tabs.js — Fix AgToast argument order bugs:**

    1. Search for ALL occurrences of `AgToast.show(` in operator-tabs.js.
    2. For each call, verify the argument order is `AgToast.show(type, message, duration?)`. The CORRECT order is type first, message second.
    3. Research identified this specific bug pattern: `AgToast.show('Le fichier depasse 10 Mo', 'error')` — arguments are REVERSED. Fix to: `AgToast.show('error', 'Le fichier depasse 10 Mo')`.
    4. Search for and fix ALL instances of this reversed pattern.
    5. Also search for and remove `window.AgToast ? ... : setNotif(...)` ternary guards. AgToast is always loaded. Replace with direct `AgToast.show()` calls. If the ternary was guarding a `setNotif` fallback, just use `AgToast.show()` directly.

    **Shared.btnLoading adoption in users.js:**

    For each form submission or API call in users.js that has a trigger button:
    1. Add `Shared.btnLoading(btn, true)` immediately before the `try` block
    2. Add `Shared.btnLoading(btn, false)` in the `finally` block
    3. This applies to: save user (around line 319), delete user, toggle user

    Check if users.js form submit buttons already have double-submit prevention. If not, add it.
  </action>
  <verify>
    <automated>! grep -n "function setNotif" public/assets/js/pages/users.js && grep -c "AgToast.show\|setNotif" public/assets/js/pages/users.js | grep -v "^0$" && echo "users.js PASS" && ! grep "AgToast.show('[A-Z]" public/assets/js/pages/operator-tabs.js && echo "operator-tabs.js arg order PASS"</automated>
  </verify>
  <acceptance_criteria>
    - users.js does NOT contain `function setNotif` (local override removed)
    - users.js still contains `setNotif(` calls that now use the global version
    - operator-tabs.js does NOT contain `AgToast.show('Le fichier` (reversed args fixed)
    - operator-tabs.js does NOT contain `window.AgToast ?` ternary guards
    - All `AgToast.show(` calls in operator-tabs.js have type as first argument (one of: 'success', 'error', 'warning', 'info')
  </acceptance_criteria>
  <done>Toast wiring bugs fixed: users.js uses global setNotif->AgToast, operator-tabs.js has correct AgToast argument order, no ternary guards.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and wire missing error/loading feedback on API calls across key pages</name>
  <files>public/assets/js/pages/members.js, public/assets/js/pages/meetings.js, public/assets/js/pages/admin.js, public/assets/js/pages/settings.js, public/assets/js/pages/postsession.js, public/assets/js/pages/wizard.js, public/assets/js/pages/hub.js, public/assets/js/core/shared.js</files>
  <read_first>
    - public/assets/js/pages/members.js (all fetch/api calls — check each has try/catch with AgToast error feedback and Shared.btnLoading)
    - public/assets/js/pages/meetings.js (all fetch/api calls)
    - public/assets/js/pages/admin.js (all fetch/api calls)
    - public/assets/js/pages/settings.js (all fetch/api calls — 29 occurrences of fetch/api)
    - public/assets/js/pages/postsession.js (all fetch/api calls)
    - public/assets/js/pages/wizard.js (all fetch/api calls — step transitions)
    - public/assets/js/pages/hub.js (all fetch/api calls)
    - public/assets/js/core/shared.js (Shared.btnLoading implementation to confirm API)
  </read_first>
  <action>
    Systematically audit each page's API calls. For EVERY `await api(` or `await fetch(` or `.then(` chain:

    **Check 1 — Error handling:**
    - Does it have a `.catch()` or `try/catch` block?
    - Does the catch block call `AgToast.show('error', ...)` or `setNotif('error', ...)`?
    - If not, add: `catch (e) { AgToast.show('error', 'Erreur de connexion — verifiez votre connexion et reessayez', 8000); }`

    **Check 2 — Success feedback:**
    - Does a successful API response show a toast or update the UI visibly?
    - For create/delete operations: must show `AgToast.show('success', 'Confirmation message')`
    - For data-loading fetches (loading lists, refreshing tables): no toast needed, but must handle error case

    **Check 3 — Loading state:**
    - Does the triggering button (if any) get disabled during the request?
    - If not, add `Shared.btnLoading(btn, true)` before the API call and `Shared.btnLoading(btn, false)` in `finally`
    - This prevents double-submit (D-10)

    **Check 4 — API error response handling:**
    - Does the success path check `r.body?.ok` or equivalent?
    - If the API returns `{ ok: false, error: '...' }`, does the page show the error message?
    - If not, add: `AgToast.show('error', r.body?.error || 'Une erreur est survenue', 8000)`

    **Priority pages** (highest interaction density, audit thoroughly):
    1. members.js — 25 fetch/api calls
    2. settings.js — 29 fetch/api calls
    3. admin.js — 6 fetch/api calls
    4. wizard.js — 7 fetch/api calls (step save operations)
    5. hub.js — 18 fetch/api calls
    6. meetings.js — 3 fetch/api calls
    7. postsession.js — 2 fetch/api calls

    Use the UI-SPEC toast messages:
    - Network error: `'Erreur de connexion — verifiez votre connexion et reessayez'`
    - Server error (5xx): `'Une erreur est survenue — veuillez reessayer'`
    - Validation error (4xx): Use server message `r.body?.error || 'Erreur de validation'`
    - Save success: `'Modifications enregistrees'`
    - Delete success: `'[Entity] supprime(e)'`

    **Double-submit prevention pattern:**
    For every form submit handler, ensure the submit button is found and wrapped:
    ```js
    const btn = form.querySelector('[type="submit"], .btn-primary');
    if (btn) Shared.btnLoading(btn, true);
    try {
      // ... API call
    } finally {
      if (btn) Shared.btnLoading(btn, false);
    }
    ```

    Do NOT add loading states to data-refresh fetches (background list reloads) — only to user-initiated action buttons.
  </action>
  <verify>
    <automated>grep -c "AgToast.show\|setNotif" public/assets/js/pages/members.js public/assets/js/pages/settings.js public/assets/js/pages/admin.js public/assets/js/pages/wizard.js public/assets/js/pages/hub.js && grep -c "btnLoading" public/assets/js/pages/members.js public/assets/js/pages/settings.js public/assets/js/pages/wizard.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - members.js: every `await api(` call has a corresponding catch block with `AgToast.show('error'` or `setNotif('error'`
    - settings.js: every `await api(` call has error handling with toast feedback
    - admin.js: every `await api(` call has error handling with toast feedback
    - wizard.js: step save operations have `Shared.btnLoading` on their submit buttons
    - members.js: add/edit/delete member buttons use `Shared.btnLoading` to prevent double-submit
    - settings.js: save buttons use `Shared.btnLoading` to prevent double-submit
    - No `await api(` or `await fetch(` call exists without a catch/error handler in any of the audited files
  </acceptance_criteria>
  <done>Every API call in the 7 key pages has visible error feedback (AgToast), success feedback where appropriate, and loading/double-submit prevention via Shared.btnLoading on user-triggered actions.</done>
</task>

</tasks>

<verification>
- `grep -r "function setNotif" public/assets/js/pages/` returns 0 results (no local overrides)
- `grep -rc "AgToast.show\|setNotif" public/assets/js/pages/` shows coverage across all key pages
- `grep -rc "btnLoading" public/assets/js/pages/` shows adoption in members, settings, wizard, admin
- No `window.AgToast ?` ternary guards remain in any file
</verification>

<success_criteria>
1. users.js local setNotif removed — global version with AgToast delegation is used
2. operator-tabs.js AgToast.show calls have correct (type, message) argument order
3. Every API call in 7 key pages has error handling with toast feedback
4. Form submit buttons in members, settings, wizard use Shared.btnLoading for double-submit prevention
5. No silent API failures remain in any audited page
</success_criteria>

<output>
After completion, create `.planning/phases/81-fix-ux-interactivity-blocking-popups-broken-layouts-fragile-frontend-wiring/81-03-SUMMARY.md`
</output>
