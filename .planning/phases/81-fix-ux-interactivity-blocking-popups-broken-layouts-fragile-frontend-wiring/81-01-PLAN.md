---
phase: 81-fix-ux-interactivity-blocking-popups-broken-layouts-fragile-frontend-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
  - public/assets/css/admin.css
  - public/assets/css/settings.css
  - public/assets/css/members.css
  - public/assets/css/users.css
  - public/assets/css/meetings.css
  - public/assets/css/operator.css
  - public/assets/css/hub.css
  - public/assets/css/analytics.css
  - public/assets/css/audit.css
  - public/assets/css/postsession.css
  - public/assets/css/archives.css
  - public/assets/css/vote.css
  - public/assets/css/app.css
autonomous: true
requirements: [D-05, D-06, D-07, D-13, D-14]

must_haves:
  truths:
    - "Form sections with 4+ fields display in 2-column grid on screens wider than 768px"
    - "Full-width pages (operator, dashboard, analytics, meetings, members, audit, hub) have no max-width constraint on main content"
    - "Narrow pages (settings, admin, mon-compte, vote/ballot) use max-width centered layout"
    - "All button, badge, card, and table components use design-system token values, not ad-hoc inline values"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: ".form-grid utility class with auto-fit minmax(240px, 1fr)"
      contains: ".form-grid"
    - path: "public/assets/css/app.css"
      provides: "Per-page width strategy via .app-main CSS rules"
  key_links:
    - from: "public/assets/css/design-system.css"
      to: "per-page CSS files"
      via: ".form-grid class reused across pages"
      pattern: "form-grid"
---

<objective>
Consolidate CSS design tokens, add the `.form-grid` utility class for multi-column form layouts, and apply per-page width strategy across all page CSS files.

Purpose: Establish the CSS foundation that all other plans depend on — consistent form grids (D-05), per-page width decisions (D-06/D-07), and component variant enforcement (D-13/D-14).
Output: Updated design-system.css with form-grid utility, per-page CSS files with correct width strategy.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-fix-ux-interactivity-blocking-popups-broken-layouts-fragile-frontend-wiring/81-CONTEXT.md
@.planning/phases/81-fix-ux-interactivity-blocking-popups-broken-layouts-fragile-frontend-wiring/81-RESEARCH.md
@.planning/phases/81-fix-ux-interactivity-blocking-popups-broken-layouts-fragile-frontend-wiring/81-UI-SPEC.md

<interfaces>
<!-- Existing CSS grid classes in design-system.css (lines 1918-1947): -->
.form-grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: var(--gap-md); }
.form-grid-2 .form-group-full { grid-column: 1 / -1; }
@media (max-width: 768px) { .form-grid-2 { grid-template-columns: 1fr; } }
.form-grid-3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: var(--gap-md); }

<!-- Existing width tokens: -->
--content-narrow: 720px
--content-wide: 1200px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .form-grid utility + SSE warning banner CSS + audit design token enforcement</name>
  <files>public/assets/css/design-system.css</files>
  <read_first>
    - public/assets/css/design-system.css (current token definitions, existing .form-grid-2 / .form-grid-3 at lines 1918-1947)
    - .planning/phases/81-fix-ux-interactivity-blocking-popups-broken-layouts-fragile-frontend-wiring/81-UI-SPEC.md (form grid rule, SSE banner spec, animation contracts)
  </read_first>
  <action>
    1. Add `.form-grid` utility class in the `@layer components` section of design-system.css, near the existing `.form-grid-2` and `.form-grid-3` rules (around line 1918):

    ```css
    .form-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: var(--gap-md, 16px);
    }
    .form-grid .field-full,
    .form-grid textarea,
    .form-grid .form-group-full,
    .form-grid .upload-zone { grid-column: 1 / -1; }
    @media (max-width: 768px) {
      .form-grid { grid-template-columns: 1fr; }
    }
    ```

    2. Add `.sse-warning-banner` styles for the SSE disconnect banner (D-11, used by Plan 04):

    ```css
    .sse-warning-banner {
      background: var(--color-warning-subtle, #fef3cd);
      color: var(--color-warning, #856404);
      border: 1px solid var(--color-warning, #856404);
      border-radius: var(--radius-sm, 4px);
      padding: var(--space-3, 12px) var(--space-4, 16px);
      font-size: var(--text-sm, 13px);
      font-weight: var(--weight-semibold, 600);
      text-align: center;
      animation: slideDown var(--duration-normal, 200ms) var(--ease-out, ease-out);
    }
    @keyframes slideDown {
      from { transform: translateY(-100%); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }
    @media (prefers-reduced-motion: reduce) {
      .sse-warning-banner { animation: none; }
    }
    ```

    3. Audit existing component styles in design-system.css for ad-hoc hardcoded values. Replace any hardcoded colors, border-radius, or spacing with their corresponding CSS custom property tokens. Specifically check:
    - `.btn` variants — ensure `border-radius` uses `var(--radius-md)` not hardcoded px
    - `.badge` variants — ensure backgrounds use `var(--color-success)`, `var(--color-warning)`, etc.
    - `.card` — ensure padding uses `var(--space-card)` not hardcoded values
    - `.modal-backdrop` / `.modal` — ensure spacing uses tokens

    Do NOT change token definitions themselves. Only replace hardcoded values in component rules with existing token references.
  </action>
  <verify>
    <automated>grep -c "form-grid" public/assets/css/design-system.css | grep -v "^0$" && grep -c "sse-warning-banner" public/assets/css/design-system.css | grep -v "^0$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - design-system.css contains `.form-grid` class with `grid-template-columns: repeat(auto-fit, minmax(240px, 1fr))`
    - design-system.css contains `.form-grid .field-full` with `grid-column: 1 / -1`
    - design-system.css contains `.form-grid` responsive breakpoint at 768px
    - design-system.css contains `.sse-warning-banner` class with `var(--color-warning-subtle` background
    - design-system.css contains `@keyframes slideDown`
    - design-system.css contains `prefers-reduced-motion: reduce` media query for banner animation
  </acceptance_criteria>
  <done>`.form-grid` utility, `.sse-warning-banner`, and token enforcement audit complete in design-system.css</done>
</task>

<task type="auto">
  <name>Task 2: Apply per-page width strategy across all page CSS files</name>
  <files>public/assets/css/admin.css, public/assets/css/settings.css, public/assets/css/members.css, public/assets/css/users.css, public/assets/css/meetings.css, public/assets/css/operator.css, public/assets/css/hub.css, public/assets/css/analytics.css, public/assets/css/audit.css, public/assets/css/postsession.css, public/assets/css/archives.css, public/assets/css/vote.css, public/assets/css/app.css</files>
  <read_first>
    - public/assets/css/app.css (global layout rules, .app-main definition)
    - public/assets/css/admin.css
    - public/assets/css/settings.css
    - public/assets/css/members.css
    - public/assets/css/users.css
    - public/assets/css/meetings.css
    - public/assets/css/operator.css
    - public/assets/css/hub.css
    - public/assets/css/analytics.css
    - public/assets/css/audit.css
    - public/assets/css/postsession.css
    - public/assets/css/archives.css
    - public/assets/css/vote.css
    - .planning/phases/81-fix-ux-interactivity-blocking-popups-broken-layouts-fragile-frontend-wiring/81-UI-SPEC.md (per-page width table)
  </read_first>
  <action>
    Apply the width strategy from the UI-SPEC. For each page CSS file, ensure the main content container uses the correct width approach:

    **Full-width pages** (width: 100%, NO max-width on main content):
    - operator.css — operator console: full-width
    - hub.css — hub: full-width
    - analytics.css — analytics: full-width
    - meetings.css — meetings list: full-width
    - members.css — member list: full-width
    - audit.css — audit log: full-width
    - postsession.css — post-session: full-width
    - archives.css — archives: full-width

    For each full-width page, find the outermost content container selector (e.g., `.meetings-page`, `.members-page`, etc.) and ensure it has `width: 100%` and NO `max-width` constraint. Remove any existing `max-width` declarations on the main content wrapper.

    **Narrow pages** (max-width centered):
    - settings.css: main content container gets `max-width: var(--content-narrow, 720px); margin-inline: auto;`
    - admin.css: main content container gets `max-width: var(--content-narrow, 720px); margin-inline: auto;`
    - vote.css: main content container gets `max-width: 720px; margin-inline: auto;`

    **Special width:**
    - app.css: If there is a global `.app-main` or `.page-content` rule that constrains width, ensure it defaults to `width: 100%` so individual pages can override.

    For each file, also scan for any `form-group` containers with 4+ sibling `.form-group` elements that should get the `form-grid` class applied in the corresponding HTML partial. Note these in a comment at the top of the CSS file: `/* TODO: HTML partial needs .form-grid on form containers */`.

    Do NOT add `max-width` to any page listed as full-width.
    Do NOT remove existing page-specific styles beyond width constraints.
  </action>
  <verify>
    <automated>grep -l "max-width.*content-narrow\|max-width.*720px" public/assets/css/settings.css public/assets/css/admin.css public/assets/css/vote.css && echo "Narrow pages configured" && ! grep "max-width" public/assets/css/hub.css public/assets/css/analytics.css public/assets/css/meetings.css 2>/dev/null | grep -v "modal\|dropdown\|popover\|tooltip\|card" | head -5 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - settings.css contains `max-width: var(--content-narrow` or `max-width: 720px` on main content container
    - admin.css contains `max-width: var(--content-narrow` or `max-width: 720px` on main content container
    - vote.css contains `max-width: 720px` on main content container
    - hub.css does NOT contain `max-width` on its main content container (modals/tooltips excluded)
    - analytics.css does NOT contain `max-width` on its main content container
    - meetings.css does NOT contain `max-width` on its main content container
    - members.css does NOT contain `max-width` on its main content container
  </acceptance_criteria>
  <done>All page CSS files have correct width strategy per UI-SPEC table. Full-width pages are unconstrained, narrow pages have centered max-width.</done>
</task>

</tasks>

<verification>
- `.form-grid` class exists in design-system.css with responsive breakpoint
- `.sse-warning-banner` styles exist for Plan 04 to consume
- Each page CSS file has the correct width strategy per UI-SPEC
- No ad-hoc hardcoded values in component CSS — tokens used consistently
</verification>

<success_criteria>
1. `grep "form-grid" public/assets/css/design-system.css` returns the utility class definition
2. `grep "sse-warning-banner" public/assets/css/design-system.css` returns the banner styles
3. Narrow pages (settings, admin, vote) have max-width constraints
4. Full-width pages (operator, hub, analytics, meetings, members, audit) have no max-width on main content
</success_criteria>

<output>
After completion, create `.planning/phases/81-fix-ux-interactivity-blocking-popups-broken-layouts-fragile-frontend-wiring/81-01-SUMMARY.md`
</output>
