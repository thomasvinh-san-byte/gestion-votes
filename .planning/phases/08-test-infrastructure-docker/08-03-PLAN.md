---
phase: 08-test-infrastructure-docker
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - .planning/phases/08-test-infrastructure-docker/08-03-BASELINE.md
autonomous: false
requirements: [INFRA-03]
must_haves:
  truths:
    - "All existing E2E specs (v1.0 + v1.1 including page-interactions.spec.js and operator-e2e.spec.js) run to completion in chromium in the container"
    - "The run produces a pass/fail report in playwright-report/ readable from host"
    - "Baseline result (green or list of failures) is recorded in 08-03-BASELINE.md"
    - "Exit code from ./bin/test-e2e.sh reflects pass/fail accurately"
  artifacts:
    - path: ".planning/phases/08-test-infrastructure-docker/08-03-BASELINE.md"
      provides: "Documented baseline of which specs pass/fail in the new infra"
      contains: "BASELINE"
  key_links:
    - from: "./bin/test-e2e.sh"
      to: "playwright-report/index.html"
      via: "dual reporter (line + html)"
      pattern: "playwright-report"
---

<objective>
Execute `./bin/test-e2e.sh` to run the full existing Playwright suite in the new containerized environment, document the baseline result, and certify that INFRA-03 is achieved. If specs fail, record them — Phase 11 (FIX-01) will repair them; Phase 8 only gates on the infrastructure working, not on 100% green.

Purpose: Prove the infrastructure works end-to-end by running it on real specs. A baseline run reveals whether failures are infra bugs (→ fix in this phase) or app/spec bugs (→ deferred to Phase 11).
Output: BASELINE.md documenting the initial run + user confirmation that the report is readable.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-test-infrastructure-docker/08-CONTEXT.md
@.planning/phases/08-test-infrastructure-docker/08-01-SUMMARY.md
@.planning/phases/08-test-infrastructure-docker/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute baseline run and capture result</name>
  <files>.planning/phases/08-test-infrastructure-docker/08-03-BASELINE.md</files>
  <read_first>
    - /home/user/gestion_votes_php/bin/test-e2e.sh (from Plan 02)
    - /home/user/gestion_votes_php/docker-compose.yml (tests service from Plan 01)
  </read_first>
  <action>
    Step 1 — Ensure stack is up:
    ```bash
    cd /home/user/gestion_votes_php
    docker compose up -d app db redis
    docker compose ps
    ```
    Wait until `app` is `healthy`.

    Step 2 — Pull the Playwright image (first run only, ~1.5GB):
    ```bash
    docker pull mcr.microsoft.com/playwright:v1.59.1-jammy
    ```

    Step 3 — First dry run to list specs (validates infra without running tests):
    ```bash
    ./bin/test-e2e.sh --list 2>&1 | tee /tmp/playwright-list.txt
    LIST_EXIT=${PIPESTATUS[0]}
    ```
    Confirm LIST_EXIT=0 and that /tmp/playwright-list.txt mentions `page-interactions.spec.js` and `operator-e2e.spec.js`. If --list fails with infra errors (missing libs, cannot resolve app, permission denied), STOP and fix infra first.

    Step 4 — Full baseline run:
    ```bash
    ./bin/test-e2e.sh 2>&1 | tee /tmp/playwright-baseline.txt
    RUN_EXIT=${PIPESTATUS[0]}
    echo "Playwright exit code: $RUN_EXIT"
    ```

    Step 5 — Verify HTML report exists on host:
    ```bash
    ls -la tests/e2e/playwright-report/index.html
    ```

    Step 6 — Parse results from /tmp/playwright-baseline.txt to extract:
    - Total specs run
    - Passed count
    - Failed count (with spec names if any)
    - Skipped count
    - Total duration

    Step 7 — Write `.planning/phases/08-test-infrastructure-docker/08-03-BASELINE.md` with this structure:
    ```markdown
    # Phase 08 — Baseline E2E Run

    **Executed:** {timestamp}
    **Infrastructure:** Docker + Playwright jammy 1.59.1 (chromium only)
    **Command:** `./bin/test-e2e.sh`

    ## Summary

    - Total specs: {N}
    - Passed: {P}
    - Failed: {F}
    - Skipped: {S}
    - Duration: {D}
    - Exit code: {RUN_EXIT}

    ## Infrastructure Health

    - [x] Container starts (mcr.microsoft.com/playwright:v1.59.1-jammy pulled)
    - [x] `app` service reachable via http://app:8080 from tests container
    - [x] Playwright resolves specs and starts executing
    - [x] HTML report generated at tests/e2e/playwright-report/index.html
    - [x] Exit code propagates via `./bin/test-e2e.sh`

    ## Passing Specs ({P})
    {list of passing spec files}

    ## Failing Specs ({F})
    {for each failing spec: name, error excerpt, triage note (infra / app / spec)}

    ## Triage Verdict

    {One of:}
    - GREEN: All specs pass. INFRA-03 satisfied. Ready for Phase 9.
    - MOSTLY GREEN: Failures are app/spec bugs, not infra. INFRA-03 satisfied (infra works). Failures logged for Phase 11 (FIX-01).
    - INFRA BROKEN: Failures are infra-related (missing libs, network, permissions). Phase 8 is NOT complete — iterate on Plans 01/02.

    ## Evidence

    - Full output: /tmp/playwright-baseline.txt (on execution host)
    - HTML report: tests/e2e/playwright-report/index.html
    ```

    Step 8 — Commit BASELINE.md (do NOT commit any playwright-report/ artifacts — they're gitignored).
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && test -f .planning/phases/08-test-infrastructure-docker/08-03-BASELINE.md && test -f tests/e2e/playwright-report/index.html && grep -q "Triage Verdict" .planning/phases/08-test-infrastructure-docker/08-03-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/playwright-report/index.html` exists on host filesystem (proves volume mount works)
    - `.planning/phases/08-test-infrastructure-docker/08-03-BASELINE.md` exists
    - BASELINE.md contains sections: Summary, Infrastructure Health, Passing Specs, Failing Specs, Triage Verdict
    - BASELINE.md Triage Verdict is one of: GREEN, MOSTLY GREEN, or INFRA BROKEN
    - If verdict is INFRA BROKEN, plan 01/02 must be iterated before this plan is marked complete
    - `git status` does not show any files under playwright-report/ or test-results/ as tracked (gitignore working)
  </acceptance_criteria>
  <done>
    Baseline run executed, report generated on host, BASELINE.md documents outcome with triage. Infrastructure is proven functional (GREEN or MOSTLY GREEN).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human confirmation of report readability and triage</name>
  <files>tests/e2e/playwright-report/index.html</files>
  <read_first>
    - .planning/phases/08-test-infrastructure-docker/08-03-BASELINE.md (output from Task 1)
  </read_first>
  <what-built>
    - `tests` service in docker-compose.yml (Plan 01)
    - `bin/test-e2e.sh` wrapper (Plan 02)
    - Baseline run executed + BASELINE.md (Task 1 above)
    - HTML report at tests/e2e/playwright-report/index.html
  </what-built>
  <action>
    PAUSE for human verification. Present the BASELINE.md Triage Verdict to the user and wait for explicit approval.

    Instruct the user (verbatim):

    1. Open `.planning/phases/08-test-infrastructure-docker/08-03-BASELINE.md` and review the Triage Verdict.
    2. Open `tests/e2e/playwright-report/index.html` in a browser:
       `xdg-open tests/e2e/playwright-report/index.html`
       or: `firefox tests/e2e/playwright-report/index.html`
    3. Confirm the report loads, shows chromium project results, and you can drill into individual test results.
    4. Re-run a single spec to confirm the script forwards args correctly:
       `./bin/test-e2e.sh specs/login.spec.js`
    5. Confirm exit code propagates:
       `./bin/test-e2e.sh --grep 'this-grep-matches-nothing-XYZ'; echo "exit: $?"`
    6. Decide:
       - GREEN or MOSTLY GREEN + report readable → reply "approved"
       - INFRA BROKEN → describe what's broken, do NOT approve
  </action>
  <how-to-verify>
    1. Open `.planning/phases/08-test-infrastructure-docker/08-03-BASELINE.md` and review the Triage Verdict.
    2. Open `tests/e2e/playwright-report/index.html` in a browser (e.g. `xdg-open tests/e2e/playwright-report/index.html` or `firefox tests/e2e/playwright-report/index.html`).
    3. Confirm the report loads, shows chromium project results, and you can drill into individual test results.
    4. Re-run a single spec to confirm the script forwards args correctly:
       ```bash
       ./bin/test-e2e.sh specs/login.spec.js
       ```
    5. Confirm exit code propagates:
       ```bash
       ./bin/test-e2e.sh --grep 'this-grep-matches-nothing-XYZ'
       echo "exit: $?"
       ```
    6. Decide acceptance:
       - If verdict is GREEN or MOSTLY GREEN and report is readable → approve, Phase 8 complete
       - If verdict is INFRA BROKEN → describe what's broken, do NOT approve
  </how-to-verify>
  <verify>
    <automated>cd /home/user/gestion_votes_php && test -f tests/e2e/playwright-report/index.html</automated>
  </verify>
  <acceptance_criteria>
    - User has opened tests/e2e/playwright-report/index.html in a browser
    - User has confirmed one single-spec invocation works (`./bin/test-e2e.sh specs/login.spec.js`)
    - User has reviewed BASELINE.md Triage Verdict and either approves (GREEN/MOSTLY GREEN) or rejects (INFRA BROKEN)
  </acceptance_criteria>
  <done>
    User has typed "approved" after reviewing BASELINE.md verdict and opening the HTML report in a browser. Phase 8 is ready to close.
  </done>
  <resume-signal>Type "approved" to mark Phase 8 complete, or describe infra issues to fix</resume-signal>
</task>

</tasks>

<verification>
- BASELINE.md exists with Triage Verdict
- playwright-report/index.html exists on host
- Single-spec invocation works
- User approval received
</verification>

<success_criteria>
INFRA-03 complete: Existing E2E specs have been run through the new containerized infrastructure. Baseline is documented. User has visually confirmed the report is readable and the infrastructure is functional. Phase 8 is ready to close; any spec failures are triaged to Phase 11 (FIX-01).
</success_criteria>

<output>
After completion, create `.planning/phases/08-test-infrastructure-docker/08-03-SUMMARY.md`
</output>
