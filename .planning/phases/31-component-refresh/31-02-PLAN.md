---
phase: 31-component-refresh
plan: "02"
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - public/assets/js/components/ag-modal.js
  - public/assets/js/components/ag-toast.js
  - public/assets/js/components/ag-badge.js
  - public/assets/js/components/ag-stepper.js
autonomous: true
requirements: [CMP-05, CMP-06, CMP-07, CMP-08]

must_haves:
  truths:
    - "Inspecting ag-modal in DevTools Shadow DOM shows var(--token) for every visual property — no literal px/hex values in source"
    - "Inspecting ag-toast shows var(--toast-width) for width and inset box-shadow for accent stripe"
    - "Inspecting ag-badge shows var(--font-medium) for weight and var(--space-1)/var(--space-2) for padding"
    - "Inspecting ag-stepper shows var(--stepper-dot-size) for dot dimensions and var(--stepper-connector-height) for connector"
  artifacts:
    - path: "public/assets/js/components/ag-modal.js"
      provides: "Modal Web Component with tokenized Shadow DOM styles"
    - path: "public/assets/js/components/ag-toast.js"
      provides: "Toast Web Component with tokenized styles and inset accent"
    - path: "public/assets/js/components/ag-badge.js"
      provides: "Badge Web Component with tokenized styles"
    - path: "public/assets/js/components/ag-stepper.js"
      provides: "Stepper Web Component with 28px dots and tokenized styles"
  key_links:
    - from: "ag-modal.js Shadow DOM style"
      to: "design-system.css :root tokens"
      via: "var(--radius-modal), var(--color-backdrop), var(--shadow-focus)"
      pattern: "var\\(--radius-modal"
    - from: "ag-toast.js Shadow DOM style"
      to: "design-system.css :root tokens"
      via: "var(--toast-width), var(--toast-accent-width)"
      pattern: "var\\(--toast-width"
    - from: "ag-stepper.js Shadow DOM style"
      to: "design-system.css :root tokens"
      via: "var(--stepper-dot-size)"
      pattern: "var\\(--stepper-dot-size"
---

<objective>
Reconcile all 4 Web Component Shadow DOM styles with the design-system.css token system. Replace every hardcoded px/hex/rgb value inside ag-modal.js, ag-toast.js, ag-badge.js, and ag-stepper.js with var(--token, correct-fallback) references.

Purpose: After plan 01 established the canonical CSS spec, this plan ensures the Shadow DOM styles match — making every component debuggable in DevTools (you see var(--token-name), not a magic number).
Output: 4 Web Component JS files with zero hardcoded visual values in their Shadow DOM style blocks.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-component-refresh/31-CONTEXT.md
@.planning/phases/31-component-refresh/31-RESEARCH.md
@.planning/phases/31-component-refresh/31-01-SUMMARY.md

<interfaces>
<!-- Tokens that Web Components must consume via var() -->
<!-- All defined in design-system.css :root — plan 01 added missing ones -->

Component aliases (from COMPONENT ALIASES section):
  --radius-modal:   var(--radius-xl)    = 12px
  --radius-toast:   var(--radius-lg)    = 8px
  --radius-badge:   var(--radius-full)  = 9999px (pill)
  --radius-btn:     var(--radius-md)    = 6px
  --radius-sm:      4px
  --radius-full:    9999px

New tokens from plan 01:
  --toast-width:              356px
  --toast-accent-width:       3px
  --stepper-dot-size:         28px
  --stepper-connector-height: 2px
  --btn-height:               36px
  --input-height:             36px

Existing semantic tokens:
  --color-backdrop:         rgba(0,0,0,0.5)
  --shadow-focus:           0 0 0 2px var(--color-surface-raised), 0 0 0 4px var(--color-border-focus)
  --shadow-lg:              (shadow token)
  --shadow-xl:              (shadow token)
  --z-modal:                (z-index token)
  --space-1: 0.25rem (4px), --space-2: 0.5rem (8px), --space-3: 0.75rem (12px)
  --space-4: 1rem (16px), --space-5: 1.25rem (20px)
  --text-xs, --text-sm, --text-2xs (typography tokens)
  --font-medium: 500, --font-semibold: 600, --font-bold: 700
  --duration-fast, --ease-default, --transition-ui
  --color-surface, --color-border, --color-text, --color-text-secondary, --color-text-muted
  --color-success, --color-warning, --color-danger, --color-info
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile ag-modal.js and ag-badge.js Shadow DOM styles</name>
  <files>public/assets/js/components/ag-modal.js, public/assets/js/components/ag-badge.js</files>
  <read_first>
    - public/assets/js/components/ag-modal.js (full file — locate the template literal style block)
    - public/assets/js/components/ag-badge.js (full file — locate the template literal style block)
    - .planning/phases/31-component-refresh/31-RESEARCH.md (Inventory of Hardcoded Values section for ag-modal.js and ag-badge.js)
  </read_first>
  <action>
**ag-modal.js — Replace hardcoded values in the Shadow DOM `<style>` block:**

Find and replace each hardcoded value with the correct token + fallback chain:

| Find (in style template) | Replace with |
|---|---|
| `border-radius: var(--radius-lg, 16px)` | `border-radius: var(--radius-modal, 12px)` |
| `background: rgba(0,0,0,.45)` (backdrop) | `background: var(--color-backdrop, rgba(0,0,0,0.5))` |
| `padding: 16px` (on backdrop/wrapper) | `padding: var(--space-4, 16px)` |
| `padding: 14px 20px` (modal header .modal-h) | `padding: var(--space-4, 16px) var(--space-5, 20px)` |
| `padding: 18px 20px` (modal body .modal-b) | `padding: var(--space-5, 20px)` |
| `gap: 6px` (modal footer .modal-f) | `gap: var(--space-2, 8px)` |
| `font-size: 13px` (modal header title .modal-h .t) | `font-size: var(--text-sm, 0.875rem)` |
| `font-weight: 700` (modal header title) | `font-weight: var(--font-semibold, 600)` |
| `z-index: 100` (if present on the host/wrapper) | `z-index: var(--z-modal, 100)` |

IMPORTANT: The fallback literal MUST match the token's actual value (not the old hardcoded value). For example, `--radius-modal` resolves to 12px, so the fallback is `12px` not `16px`.

Also add focus ring to modal close button if it has a button:
```css
.modal-h button:focus-visible {
  outline: none;
  box-shadow: var(--shadow-focus, 0 0 0 2px var(--color-surface-raised, #fff), 0 0 0 4px rgba(22,80,224,0.35));
}
```

**ag-badge.js — Replace hardcoded values in the Shadow DOM `<style>` block:**

| Find (in style template) | Replace with |
|---|---|
| `padding: 3px 10px` | `padding: var(--space-1, 4px) var(--space-2, 8px)` |
| `font-weight: 700` | `font-weight: var(--font-medium, 500)` |
| `gap: 0.375rem` or `gap: 6px` | `gap: var(--space-1, 4px)` |
| `font-size: 12px` | `font-size: var(--text-xs, 0.75rem)` |
| `padding: 2px 7px` (sm variant) | `padding: 2px var(--space-1-5, 6px)` |
| `border-radius: 9999px` or `border-radius: 999px` | `border-radius: var(--radius-badge, 9999px)` |

Do NOT change any JavaScript logic, event handling, or DOM structure. Only modify the CSS inside the template literal `<style>` block.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "var(--radius-modal" public/assets/js/components/ag-modal.js | grep -q "[1-9]" && grep -c "var(--color-backdrop" public/assets/js/components/ag-modal.js | grep -q "[1-9]" && grep -c "var(--font-medium" public/assets/js/components/ag-badge.js | grep -q "[1-9]" && grep -c "var(--space-1" public/assets/js/components/ag-badge.js | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "rgba(0,0,0,.45)" ag-modal.js returns zero matches (replaced with var(--color-backdrop))
    - grep "var(--radius-modal" ag-modal.js returns at least 1 match
    - grep "var(--color-backdrop" ag-modal.js returns at least 1 match
    - grep "var(--space-4" ag-modal.js returns at least 1 match (header padding)
    - grep "var(--space-5" ag-modal.js returns at least 1 match (body padding)
    - grep "var(--z-modal" ag-modal.js returns at least 1 match
    - grep "font-weight: 700" ag-badge.js returns zero matches (replaced with var(--font-medium))
    - grep "padding: 3px 10px" ag-badge.js returns zero matches
    - grep "var(--font-medium" ag-badge.js returns at least 1 match
    - grep "var(--radius-badge" ag-badge.js returns at least 1 match
    - grep "var(--text-xs" ag-badge.js returns at least 1 match
    - grep "16px" in ag-modal.js style block returns zero standalone hardcoded instances (only as fallback inside var())
  </acceptance_criteria>
  <done>ag-modal.js Shadow DOM uses var(--radius-modal), var(--color-backdrop), var(--space-N), var(--z-modal), var(--font-semibold) for all visual properties. ag-badge.js Shadow DOM uses var(--font-medium), var(--space-1)/var(--space-2), var(--radius-badge), var(--text-xs). All fallback literals match the token definitions.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile ag-toast.js and ag-stepper.js Shadow DOM styles</name>
  <files>public/assets/js/components/ag-toast.js, public/assets/js/components/ag-stepper.js</files>
  <read_first>
    - public/assets/js/components/ag-toast.js (full file — locate the template literal style block)
    - public/assets/js/components/ag-stepper.js (full file — locate the template literal style block)
    - .planning/phases/31-component-refresh/31-RESEARCH.md (Inventory of Hardcoded Values section for ag-toast.js and ag-stepper.js, plus Pattern 5 and Pattern 6)
  </read_first>
  <action>
**ag-toast.js — Replace hardcoded values in the Shadow DOM `<style>` block:**

| Find (in style template) | Replace with |
|---|---|
| `padding: 12px 16px` | `padding: var(--space-3, 12px) var(--space-4, 16px)` |
| `max-width: 340px` and `min-width: 240px` | Remove both; add `width: var(--toast-width, 356px)` |
| `border-left: 3px solid` (on all type variants) | `box-shadow: var(--shadow-lg, 0 4px 12px rgba(0,0,0,.12)), inset var(--toast-accent-width, 3px) 0 0 {color}` — where {color} uses `var(--color-success)`, `var(--color-danger)`, `var(--color-warning)`, `var(--color-info)` respectively with appropriate literal fallbacks |
| `border-radius: 50%` (.toast-icon) | `border-radius: var(--radius-full, 9999px)` |
| `border-radius: 4px` (.toast-close) | `border-radius: var(--radius-sm, 4px)` |
| `font-size: 13px` (.toast) | `font-size: var(--text-xs, 0.75rem)` |
| `gap: 10px` | `gap: var(--space-2-5, 10px)` (if --space-2-5 does not exist, use `var(--space-3, 12px)` — check design-system.css) |
| `top: 20px; right: 20px` (JS inline positioning) | `top: var(--space-5, 20px); right: var(--space-5, 20px)` — if set via JS `style.top/right`, change the JS to use CSS custom property values or keep as-is if the container is positioned via CSS class |

Also replace any transition literals (e.g., `.3s ease`, `.15s`) with `var(--duration-fast, 150ms) var(--ease-default, ease)` or `var(--duration-normal, 200ms) var(--ease-out)` as appropriate.

IMPORTANT for toast accent conversion: When changing from `border-left` to `box-shadow: inset`, the base `.toast` rule must NOT also set a `box-shadow` that would conflict. If `.toast` currently has `box-shadow: var(--shadow-lg)`, remove it from the base rule and include it in each variant's combined shadow declaration. Or set it as the base and use `, inset ...` only on variant overrides (CSS box-shadow does not concatenate — variants must include both the outer shadow AND the inset).

Pattern for toast variants:
```css
:host([type="success"]) .toast {
  box-shadow: var(--shadow-lg, 0 4px 12px rgba(0,0,0,.12)), inset var(--toast-accent-width, 3px) 0 0 var(--color-success, #0b7a40);
}
```

For the default (no type) toast, just use the existing shadow without inset.

**ag-stepper.js — Update Shadow DOM style block to match connector-line pattern:**

Replace hardcoded values:

| Find (in style template) | Replace with |
|---|---|
| `width: 20px; height: 20px` (dot) | `width: var(--stepper-dot-size, 28px); height: var(--stepper-dot-size, 28px)` |
| `gap: 6px` (.step) | `gap: var(--space-2, 8px)` |
| `height: 2px` (connector) | `height: var(--stepper-connector-height, 2px)` |
| `font-size: 11px` (.dot and .step-label) | `font-size: var(--text-2xs, 0.6875rem)` — if --text-2xs does not exist in the token system, use `var(--text-xs, 0.75rem)` instead |
| `margin-bottom: 14px` (.stepper) | `margin-bottom: var(--space-4, 16px)` |
| `padding: 0 4px` (.step) | `padding: 0 var(--space-1, 4px)` |

Also ensure the stepper connector line for completed steps changes color:
```css
.step.done .connector { background: var(--color-success, #16a34a); }
```

Ensure active step dot uses primary color:
```css
.step.active .dot { background: var(--color-primary, #1650e0); color: var(--color-primary-text, #fff); }
```

Ensure done step dot uses success color:
```css
.step.done .dot { background: var(--color-success, #16a34a); color: var(--color-text-inverse, #fff); }
```

Do NOT change any JavaScript logic, event handling, DOM structure, or attribute observation. Only modify the CSS inside the template literal and ensure all color/size values use tokens.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "var(--toast-width" public/assets/js/components/ag-toast.js | grep -q "[1-9]" && grep -c "var(--toast-accent-width" public/assets/js/components/ag-toast.js | grep -q "[1-9]" && grep -c "var(--stepper-dot-size" public/assets/js/components/ag-stepper.js | grep -q "[1-9]" && grep -c "var(--stepper-connector-height" public/assets/js/components/ag-stepper.js | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "min-width: 240px" ag-toast.js returns zero matches (removed)
    - grep "max-width: 340px" ag-toast.js returns zero matches (removed)
    - grep "var(--toast-width" ag-toast.js returns at least 1 match
    - grep "border-left:" ag-toast.js returns zero matches (converted to inset box-shadow)
    - grep "inset var(--toast-accent-width" ag-toast.js returns at least 1 match
    - grep "var(--radius-sm" ag-toast.js returns at least 1 match (close button)
    - grep "width: 20px" ag-stepper.js returns zero matches (replaced with token)
    - grep "height: 20px" ag-stepper.js returns zero matches (replaced with token)
    - grep "var(--stepper-dot-size" ag-stepper.js returns at least 2 matches (width + height)
    - grep "var(--stepper-connector-height" ag-stepper.js returns at least 1 match
    - grep "font-size: 11px" ag-stepper.js returns zero matches (replaced with token)
    - grep "var(--color-success" ag-stepper.js returns at least 1 match (done state)
    - grep "var(--color-primary" ag-stepper.js returns at least 1 match (active state)
  </acceptance_criteria>
  <done>ag-toast.js Shadow DOM uses var(--toast-width) for fixed width, inset box-shadow for accent stripes, and tokens for all padding/font/radius values. ag-stepper.js Shadow DOM uses var(--stepper-dot-size) for 28px dots, var(--stepper-connector-height) for connector lines, and semantic color tokens for active/done states. All 4 Web Components now have zero hardcoded visual literals in their Shadow DOM style blocks.</done>
</task>

</tasks>

<verification>
After both tasks complete, for each Web Component file run:
1. `grep -P '\b\d+px\b' public/assets/js/components/ag-modal.js | grep -v 'var(' | grep -v '//'` — should return zero lines (no standalone px outside var() fallbacks)
2. Same for ag-toast.js, ag-badge.js, ag-stepper.js
3. `grep -P 'rgba?\(' public/assets/js/components/ag-modal.js | grep -v 'var(' | grep -v '//'` — should return zero lines (no standalone rgba outside var() fallbacks)
</verification>

<success_criteria>
- All 4 Web Component Shadow DOM style blocks use var(--token, fallback) for every visual property
- Every fallback literal matches the token's actual defined value in design-system.css :root
- ag-modal uses var(--radius-modal, 12px), var(--color-backdrop), var(--z-modal)
- ag-toast uses var(--toast-width, 356px), inset box-shadow for accent, var(--radius-toast, 8px)
- ag-badge uses var(--font-medium, 500), var(--radius-badge, 9999px), var(--text-xs)
- ag-stepper uses var(--stepper-dot-size, 28px), var(--stepper-connector-height, 2px)
- No JavaScript logic, event handling, or DOM structure was changed — only CSS in template literals
</success_criteria>

<output>
After completion, create `.planning/phases/31-component-refresh/31-02-SUMMARY.md`
</output>
