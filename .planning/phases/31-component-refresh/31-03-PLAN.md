---
phase: 31-component-refresh
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
  - public/assets/js/components/ag-toast.js
autonomous: true
gap_closure: true
requirements: [CMP-02, CMP-06]

must_haves:
  truths:
    - ".card-body resolves to 24px padding — the CMP-02 minimum is met"
    - "ag-toast.js [type=info] accent uses var(--color-info) — semantically aligned with design-system.css .toast-info"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: ".card-body with var(--space-card) padding"
      contains: "card-body { padding: var(--space-card)"
    - path: "public/assets/js/components/ag-toast.js"
      provides: "info toast accent using --color-info"
      contains: "inset var(--toast-accent-width, 3px) 0 0 var(--color-info"
  key_links:
    - from: "design-system.css :root"
      to: "design-system.css .card-body"
      via: "var(--space-card)"
      pattern: "space-card.*var\\(--space-6\\)"
    - from: "ag-toast.js [type=info]"
      to: "design-system.css :root --color-info"
      via: "var(--color-info)"
      pattern: "color-info"
---

<objective>
Close the two gaps identified in the Phase 31 verification report.

Gap 1 (blocker — CMP-02): `.card-body` uses `var(--space-5)` = 20px, but CMP-02 requires 24px minimum. The `--space-card` alias (24px) is already defined in `:root` at line 247. One-line fix in design-system.css.

Gap 2 (semantic alignment — CMP-06): `ag-toast.js` [type="info"] accent uses `var(--color-primary)` while `design-system.css .toast-info` uses `var(--color-info)`. Both resolve to the same hex in light mode, but the Web Component diverges from the canonical CSS class semantically. Align to `var(--color-info)`.

Purpose: Bring Phase 31 to 12/12 verified must-haves and full CMP requirements coverage.
Output: Two targeted one-line changes; no structural rewrites.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-component-refresh/31-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix card-body padding to meet CMP-02 24px minimum</name>
  <files>public/assets/css/design-system.css</files>
  <read_first>
    - public/assets/css/design-system.css (lines 1610–1625 — .card-body rule; line 247 — --space-card definition)
  </read_first>
  <action>
    In `public/assets/css/design-system.css`, find the `.card-body` rule (around line 1614–1615).

    Change:
    ```css
    .card-body {
      padding: var(--space-5);
    ```

    To:
    ```css
    .card-body {
      padding: var(--space-card);
    ```

    `--space-card` is defined at line 247 as `var(--space-6)` which resolves to 24px.

    Do NOT change any other card rules (`.card`, `.card-header`, `.card-footer`, `.card-clickable`). This is a single property change on a single selector.

    Rationale: CMP-02 specifies "24px padding minimum" for cards. `--space-card` is the semantic alias specifically created for this purpose and is already defined in `:root` but was never applied to `.card-body`.
  </action>
  <verify>
    <automated>grep -n "card-body" /home/user/gestion_votes_php/public/assets/css/design-system.css | grep "padding"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "card-body" public/assets/css/design-system.css` shows `padding: var(--space-card)` — not `var(--space-5)`
    - No other card rules are modified (verify with `git diff public/assets/css/design-system.css`)
    - `--space-card` resolves to `var(--space-6)` = 24px per line 247
  </acceptance_criteria>
  <done>`.card-body { padding: var(--space-card); }` is present in design-system.css and resolves to 24px — CMP-02 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Align ag-toast.js info accent to var(--color-info)</name>
  <files>public/assets/js/components/ag-toast.js</files>
  <read_first>
    - public/assets/js/components/ag-toast.js (lines 154–170 — the four toast type box-shadow rules)
    - public/assets/css/design-system.css (line 2116 — .toast-info canonical rule using var(--color-info))
  </read_first>
  <action>
    In `public/assets/js/components/ag-toast.js`, find the `[type="info"]` host selector rule (around line 166).

    Current state:
    ```css
    box-shadow: var(--shadow-lg, 0 20px 25px -5px rgba(0,0,0,0.1)), inset var(--toast-accent-width, 3px) 0 0 var(--color-primary, #1650E0);
    ```

    Change to:
    ```css
    box-shadow: var(--shadow-lg, 0 20px 25px -5px rgba(0,0,0,0.1)), inset var(--toast-accent-width, 3px) 0 0 var(--color-info, #1650E0);
    ```

    Also check line 168 for the `.toast-icon` rule under `[type="info"]`:
    Current:
    ```css
    :host([type="info"]) .toast-icon { background: var(--color-primary-subtle, #e8edfa); color: var(--color-primary, #1650E0); }
    ```
    Change to:
    ```css
    :host([type="info"]) .toast-icon { background: var(--color-info-subtle, #EBF0FF); color: var(--color-info, #1650E0); }
    ```

    Rationale: `design-system.css .toast-info` uses `var(--color-info)` (line 2116). The Web Component should use the same semantic token to be consistent. Both `--color-primary` and `--color-info` resolve to `#1650E0` in light mode, but `--color-info` is the semantically correct token for "info" state. Using `--color-primary` for an info toast would diverge in any theme where primary color is changed independently.

    Do NOT touch the success, warning, or danger variants.
  </action>
  <verify>
    <automated>grep -n "color-primary\|color-info" /home/user/gestion_votes_php/public/assets/js/components/ag-toast.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "color-primary" public/assets/js/components/ag-toast.js` returns zero matches (no remaining --color-primary references)
    - `grep "color-info" public/assets/js/components/ag-toast.js` shows the accent and icon rules using `var(--color-info)` and `var(--color-info-subtle)`
    - The fallback value in the var() for info accent is `#1650E0` (matches existing light mode value)
    - Success/warning/danger variants are untouched (verify with `git diff`)
  </acceptance_criteria>
  <done>`ag-toast.js` [type="info"] uses `var(--color-info)` and `var(--color-info-subtle)` — semantically aligned with `design-system.css .toast-info`. No `--color-primary` references remain.</done>
</task>

</tasks>

<verification>
After both tasks:

1. `grep "card-body" public/assets/css/design-system.css` — must show `padding: var(--space-card)`, not `var(--space-5)`
2. `grep "color-primary" public/assets/js/components/ag-toast.js` — must return zero matches
3. `grep "color-info" public/assets/js/components/ag-toast.js` — must show at least 2 matches (accent + icon)
4. `git diff --stat` — must show exactly 2 files modified (design-system.css and ag-toast.js)
</verification>

<success_criteria>
- CMP-02 card padding gap resolved: `.card-body` uses `var(--space-card)` = 24px
- CMP-06 ag-toast info accent aligned: uses `var(--color-info)` matching the canonical CSS class
- Phase 31 verification score rises from 11/12 to 12/12
- No other component rules modified
</success_criteria>

<output>
After completion, create `.planning/phases/31-component-refresh/31-03-SUMMARY.md` documenting:
- The two one-line fixes applied
- Final requirement coverage: CMP-01 through CMP-08 all SATISFIED
- Files changed: design-system.css (1 line), ag-toast.js (2 lines)
</output>
