---
phase: 30-token-foundation
plan: 04
type: execute
wave: 1
depends_on: ["30-01", "30-02", "30-03"]
files_modified:
  - public/assets/css/design-system.css
autonomous: true
gap_closure: true
requirements: [TKN-01, TKN-04, TKN-05, TKN-06]

must_haves:
  truths:
    - "Section-title weight is 600 (semibold), visually lighter than page-title at 700 (bold)"
    - "Developers can use --space-section, --space-card, --space-field as semantic spacing tokens"
    - "The :root block has three visually distinct top-level sections: PRIMITIVES, SEMANTIC, COMPONENT ALIASES"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Fixed token values for TKN-04, TKN-05, TKN-06 compliance"
      contains: "--type-section-title-weight: var(--weight-semibold)"
  key_links:
    - from: "design-system.css :root"
      to: "all page CSS"
      via: "var(--space-section), var(--space-card), var(--space-field)"
      pattern: "--space-section.*--space-12"
---

<objective>
Close verification gaps for Phase 30 — fix token values that diverge from TKN-04, TKN-05, and TKN-06 requirements, and add the missing COMPONENT ALIASES section header for TKN-01 three-layer compliance.

Purpose: The verification report found 4 gaps: section-title weight is 700 instead of 600, three semantic spacing aliases are missing, radius values diverge from spec, and the third "component aliases" layer has no distinct section header. This plan fixes all actionable gaps in a single pass.

Output: Updated design-system.css with all TKN requirement gaps closed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-token-foundation/30-VERIFICATION.md
@.planning/phases/30-token-foundation/30-01-SUMMARY.md

<interfaces>
<!-- Current token values that need fixing, from design-system.css -->

Line 531: --type-section-title-weight: var(--weight-bold);
  PROBLEM: TKN-04 requires 600 (semibold), not 700 (bold)
  FIX: change to var(--weight-semibold)

Line 192: --weight-semibold: 600;  (already defined, just not used for section-title)
Line 193: --weight-bold: 700;

Lines 237-243: Semantic padding aliases exist (--pad-md: 16px, --pad-lg: 24px, --pad-2xl: 48px)
  PROBLEM: TKN-05 requires --space-section, --space-card, --space-field names
  FIX: add three alias tokens referencing existing --space-* primitives

Lines 255-276: Radius tokens
  --radius-sm: 5px (TKN-06 says 4px for badges)
  --radius-card = --radius-lg = 8px (TKN-06 says 12px for cards)
  FIX: Change --radius-sm to 4px, change --radius-card to var(--radius-xl) = 12px

Lines 265-276: Semantic radius aliases (--radius-btn, --radius-card, etc.)
  PROBLEM: These are inside SEMANTIC section, not a distinct COMPONENT ALIASES layer
  FIX: Move component-level aliases to a new COMPONENT ALIASES section after SEMANTIC
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TKN-04 weight, TKN-05 spacing aliases, TKN-06 radius values, TKN-01 component layer</name>
  <files>public/assets/css/design-system.css</files>
  <read_first>
    - public/assets/css/design-system.css (lines 180-280 for weights/spacing/radius, lines 520-570 for typography roles, full :root structure for section headers)
    - .planning/phases/30-token-foundation/30-VERIFICATION.md (gap details)
  </read_first>
  <action>
Make four targeted changes in design-system.css :root block:

**1. TKN-04 — Fix section-title weight (line 531):**
Change:
  --type-section-title-weight: var(--weight-bold);
To:
  --type-section-title-weight: var(--weight-semibold);

This creates the required weight hierarchy: page-title=700 (bold) > section-title=600 (semibold) > card-title=600 (semibold, already correct) > body=400 (regular).

**2. TKN-05 — Add semantic spacing aliases (after line 243, after --pad-2xl):**
Add these three lines as a new sub-section:

  /* Semantic layout spacing aliases */
  --space-section: var(--space-12);  /* 48px — between major page sections */
  --space-card:    var(--space-6);   /* 24px — card internal padding */
  --space-field:   var(--space-4);   /* 16px — between form fields */

These alias the same primitive values as --pad-2xl/--pad-lg/--pad-md but with layout-role names required by TKN-05.

**3. TKN-06 — Fix radius values:**
Change --radius-sm from 5px to 4px:
  --radius-sm:   0.25rem;   /* 4px */

Change --radius-card to point to --radius-xl (12px) instead of --radius-lg (8px):
  --radius-card:    var(--radius-xl);   /* 12px */

Leave --radius-badge pointing to --radius-sm (now 4px, matching TKN-06).
Leave --radius-modal at --radius-xl (12px, already correct per TKN-06).
Note: --radius-tooltip currently also uses --radius-sm; it will now be 4px (acceptable for tooltips).

**4. TKN-01 — Add COMPONENT ALIASES section:**
After the SEMANTIC — Focus Ring section (around line 569, just before the closing `}` of :root), insert a new top-level section header:

  /* ═══════════════════════════════════════════════════════════════════════════
     COMPONENT ALIASES — Tokens tied to specific UI components
     Composed from SEMANTIC values. Use these in component CSS, not raw values.
     ═══════════════════════════════════════════════════════════════════════════ */

Then move the following existing token groups into this section (cut from their current locations within SEMANTIC sub-sections and paste under the new header):

From "SEMANTIC — Border Radius" (lines ~265-276), move the semantic radius aliases:
  --radius-btn, --radius-input, --radius-badge, --radius-chip, --radius-card, --radius-panel, --radius-modal, --radius-toast, --radius-tooltip, --radius-avatar, --radius-tag

From "SEMANTIC — Typography Roles" (lines ~521-561), move all --type-* tokens:
  --type-page-title-*, --type-section-title-*, --type-card-title-*, --type-body-*, --type-label-*, --type-caption-*, --type-badge-*, --type-mono-*

Keep raw radius values (--radius-none through --radius-full and --radius legacy alias) in SEMANTIC.
Keep raw spacing values and gap/pad aliases in SEMANTIC.
Keep the new --space-section/--space-card/--space-field in SEMANTIC (they are layout-role aliases, not component-specific).

This creates the three visible layers: PRIMITIVES (raw palettes, sizes) > SEMANTIC (colors, spacing, shadows, transitions) > COMPONENT ALIASES (radius-btn, type-page-title, etc.).
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "COMPONENT ALIASES" public/assets/css/design-system.css | grep -q "1" && grep "type-section-title-weight.*weight-semibold" public/assets/css/design-system.css && grep "\-\-space-section:" public/assets/css/design-system.css && grep "\-\-space-card:" public/assets/css/design-system.css && grep "\-\-space-field:" public/assets/css/design-system.css && grep "radius-sm.*0\.25rem.*4px" public/assets/css/design-system.css && grep "radius-card.*radius-xl.*12px" public/assets/css/design-system.css && echo "ALL GAPS CLOSED"</automated>
  </verify>
  <acceptance_criteria>
    - design-system.css contains exactly one "COMPONENT ALIASES" section header comment
    - --type-section-title-weight: var(--weight-semibold) (not --weight-bold)
    - --space-section: var(--space-12) exists with comment /* 48px */
    - --space-card: var(--space-6) exists with comment /* 24px */
    - --space-field: var(--space-4) exists with comment /* 16px */
    - --radius-sm: 0.25rem (4px, not 5px)
    - --radius-card: var(--radius-xl) with comment /* 12px */ (not var(--radius-lg))
    - :root block has three top-level section banners: PRIMITIVES, SEMANTIC, COMPONENT ALIASES
    - --type-page-title-*, --type-section-title-*, --type-card-title-*, --type-body-*, --type-label-*, --type-caption-*, --type-badge-*, --type-mono-* are inside the COMPONENT ALIASES section
    - --radius-btn, --radius-input, --radius-badge, --radius-card, --radius-modal (etc.) are inside the COMPONENT ALIASES section
  </acceptance_criteria>
  <done>All four verification gaps are closed: section-title weight=600, three spacing aliases exist, radius values match TKN-06, and the :root has three visible layer sections (PRIMITIVES, SEMANTIC, COMPONENT ALIASES)</done>
</task>

</tasks>

<verification>
1. grep "COMPONENT ALIASES" design-system.css returns exactly 1 match
2. grep "type-section-title-weight.*weight-semibold" design-system.css returns a match
3. grep for --space-section, --space-card, --space-field all return matches
4. grep "radius-sm.*0\.25rem" returns a match (4px)
5. grep "radius-card.*radius-xl" returns a match (12px)
6. The CSS file parses without syntax errors (no unclosed braces, no duplicate properties)
</verification>

<success_criteria>
- All TKN-04, TKN-05, TKN-06, and TKN-01 gaps from 30-VERIFICATION.md are resolved
- The :root block displays three distinct organizational layers
- No regressions in existing token references (all var() references still resolve)
</success_criteria>

<output>
After completion, create `.planning/phases/30-token-foundation/30-04-SUMMARY.md`
</output>
