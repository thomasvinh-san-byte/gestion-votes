---
phase: 15-analytics-users-settings-help
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/analytics.htmx.html
  - public/assets/css/analytics.css
  - public/assets/js/pages/analytics-dashboard.js
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03]

must_haves:
  truths:
    - "Statistics page displays 4 KPI cards (Séances, Résolutions votées, Taux d'adoption, Participation moy.) with trend arrows comparing to previous year"
    - "Statistics page shows a donut chart for vote distribution (Pour/Contre/Abstention) in the Résolutions tab"
    - "Statistics page shows a line graph for participation trends in the Participation tab"
    - "PDF export button triggers window.print() with a print-optimized CSS stylesheet"
    - "KPI card styling uses design system tokens: surface background, border-radius from --radius-lg, Fraunces font for values, trend arrows with success/danger colors"
    - "Print CSS hides sidebar, header, footer, tabs and shows all chart sections in a single-page layout"
  artifacts:
    - path: "public/analytics.htmx.html"
      provides: "Statistics page with 4 wireframe-aligned KPI cards, donut chart, participation graph, PDF export"
      contains: "overview-card"
    - path: "public/assets/css/analytics.css"
      provides: "Wireframe-aligned KPI card styles and print CSS for PDF export"
      contains: "@media print"
    - path: "public/assets/js/pages/analytics-dashboard.js"
      provides: "Print handler for PDF export button"
      contains: "window.print"
  key_links:
    - from: "public/assets/js/pages/analytics-dashboard.js"
      to: "public/analytics.htmx.html"
      via: "btnExportPdf click triggers window.print()"
      pattern: "btnExportPdf"
---

<objective>
Restyle the analytics page KPI cards and chart layout to match wireframe v3.19.2, and ensure PDF export works via window.print() with print CSS.

Purpose: The analytics page is functionally complete but needs wireframe alignment for KPI card styling (Fraunces font for values, proper surface/border tokens, trend arrow styling) and a print stylesheet for the PDF export button.

Output: Wireframe-aligned analytics page with working PDF export that prints all charts in a clean layout.
</objective>

## Tasks

### Task 1: Align KPI card styling with wireframe tokens
**File**: `public/assets/css/analytics.css`

Update `.overview-card` styles to match wireframe design tokens:
- Background: `var(--color-surface)`
- Border: `1px solid var(--color-border)`
- Border-radius: `var(--radius-lg)`
- `.overview-card-value`: Use `font-family: 'Fraunces', serif` for the large value, `font-size: 2rem`, `font-weight: 700`
- `.overview-card-value.primary`: color `var(--color-primary)`
- `.overview-card-value.success`: color `var(--color-success)`
- `.overview-card-trend .trend-arrow`: Add up/down/stable arrow styling
  - `.trend-up`: color `var(--color-success)`, content "↑"
  - `.trend-down`: color `var(--color-danger)`, content "↓"
  - `.trend-stable`: color `var(--color-text-muted)`, content "→"
- `.overview-card-label`: `font-size: 0.8125rem`, `color: var(--color-text-muted)`, `text-transform: uppercase`, `letter-spacing: 0.05em`

### Task 2: Add print CSS for PDF export
**File**: `public/assets/css/analytics.css`

Add `@media print` block:
```css
@media print {
  .app-sidebar, .app-header, .app-footer,
  .analytics-tabs, .charts-controls, .period-selector,
  .privacy-notice, .chart-export-btn,
  #btnExportPdf, #btnTour, [data-drawer], .drawer, .drawer-backdrop,
  .mobile-bnav { display: none !important; }

  .app-main { margin: 0; padding: 0; }
  .tab-content { display: block !important; page-break-inside: avoid; margin-bottom: 2rem; }
  .tab-content::before { content: attr(data-print-title); font-size: 1.25rem; font-weight: 700; display: block; margin-bottom: 1rem; border-bottom: 2px solid #000; padding-bottom: 0.5rem; }
  .chart-card { break-inside: avoid; margin-bottom: 1rem; }
  .overview-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1rem; margin-bottom: 2rem; }
  body { background: #fff; color: #000; }
}
```

### Task 3: Add print title data attributes to tab content divs
**File**: `public/analytics.htmx.html`

Add `data-print-title` attributes to each tab content div so they display section headers when printed:
- `#tab-participation`: `data-print-title="Participation"`
- `#tab-motions`: `data-print-title="Résolutions"`
- `#tab-timing`: `data-print-title="Temps de vote"`
- `#tab-anomalies`: `data-print-title="Anomalies"`

### Task 4: Wire up PDF export button
**File**: `public/assets/js/pages/analytics-dashboard.js`

The PDF button (`#btnExportPdf`) should already have a handler. Verify it calls `window.print()`. If not, add:
```javascript
var pdfBtn = document.getElementById('btnExportPdf');
if (pdfBtn) pdfBtn.addEventListener('click', function() { window.print(); });
```

### Verification
- Open analytics.htmx.html — 4 KPI cards show with Fraunces font values, trend arrows
- Click "PDF" button — browser print dialog opens showing all sections without sidebar/header
- Donut chart visible in Résolutions tab, line chart in Participation tab
- Dark/light theme: KPI cards use proper surface/border tokens
