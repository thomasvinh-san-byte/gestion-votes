---
phase: 42-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/trust.htmx.html
  - public/assets/js/pages/trust.js
autonomous: false
requirements:
  - FIX-01
  - FIX-02

must_haves:
  truths:
    - "Selecting a meeting on the trust page populates the motions table with real data — no empty table after selection"
    - "No TypeError in the browser console when loading a meeting that has motions"
    - "The integrity summary dashboard shows all KPI stats including Résolutions, Présents, and Bulletins"
    - "The export report correctly reports motion count, presence count, and ballot count values instead of dashes"
  artifacts:
    - path: "public/trust.htmx.html"
      provides: "Trust page HTML with kpiMotions, kpiPresent, and kpiBallots elements present"
      contains: "id=\"kpiMotions\""
    - path: "public/assets/js/pages/trust.js"
      provides: "Trust JS with null guard on kpiMotions before .textContent, and corrected integrityStatus reference"
      contains: "if (kpi) kpi.textContent"
  key_links:
    - from: "trust.js loadMotions()"
      to: "id=kpiMotions in trust.htmx.html"
      via: "document.getElementById('kpiMotions')"
      pattern: "kpiMotions"
    - from: "trust.js loadChecks() — integrityChecksEl"
      to: "id=integrityStatus in trust.htmx.html"
      via: "document.getElementById('integrityStatus')"
      pattern: "integrityStatus"
---

<objective>
Fix the one hard crash and three silent no-ops on the trust page (trust.htmx.html + trust.js) so that selecting a meeting correctly populates the motions table and all integrity KPI stats display real values.

Purpose: The trust page is a "fix now" page (not scheduled for rebuild in Phases 43-48). Its crash at trust.js:316 prevents the motions table from ever rendering, making the page non-functional for any real session audit. The three silent no-ops silently discard data the page was designed to show.
Output: trust.htmx.html gains three missing KPI stat elements; trust.js gains a null guard on the one unguarded .textContent call and a corrected element ID reference. All KPI slots display real data after a meeting is selected.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Existing .integrity-summary structure in trust.htmx.html (lines 93-110): -->
<!--   <div class="integrity-summary">                                                       -->
<!--     <div class="integrity-stat success" id="integrityStatus">                          -->
<!--       <div class="integrity-stat-value" id="kpiIntegrity">100%</div>                   -->
<!--       <div class="integrity-stat-label">Intégrité</div>                                -->
<!--     </div>                                                                              -->
<!--     <div class="integrity-stat primary" id="integrityEvents">                          -->
<!--       <div class="integrity-stat-value" id="kpiEvents">&mdash;</div>                   -->
<!--       <div class="integrity-stat-label">Événements</div>                               -->
<!--     </div>                                                                              -->
<!--     <div class="integrity-stat danger" id="integrityAnomalies">                        -->
<!--       <div class="integrity-stat-value" id="kpiAnomalies">0</div>                      -->
<!--       <div class="integrity-stat-label">Anomalies</div>                                -->
<!--     </div>                                                                              -->
<!--     <div class="integrity-stat">                                                        -->
<!--       <div class="integrity-stat-value" id="kpiLastSession">&mdash;</div>              -->
<!--       <div class="integrity-stat-label">Dernière séance</div>                          -->
<!--     </div>                                                                              -->
<!--   </div>  <-- INSERT 3 NEW STAT DIVS BEFORE THIS CLOSING TAG                          -->

<!-- trust.js crash (line 312-316):                                                         -->
<!--   const kpi = document.getElementById('kpiMotions') || null;  // kpi IS null           -->
<!--   if (body && body.ok && ...) {                                                         -->
<!--     const motions = body.data.items;                                                    -->
<!--     kpi.textContent = motions.length;  // CRASHES -- no null guard                     -->

<!-- trust.js silent no-ops (all guarded, no crash):                                        -->
<!--   line 270: getElementById('integrityChecks') -- element renamed to integrityStatus    -->
<!--   line 280: getElementById('kpiPresent') -- element missing from HTML                  -->
<!--   line 384: getElementById('kpiBallots') -- element missing from HTML                  -->
</interfaces>
</context>

<tasks>

<task type="auto" id="task-1">
  <name>Task 1: Fix trust page crash and restore missing KPI elements</name>
  <files>public/trust.htmx.html, public/assets/js/pages/trust.js</files>

  <read_first>
    - public/trust.htmx.html lines 87-121 (integrity-summary section, exact insertion point for new stat divs)
    - public/assets/js/pages/trust.js lines 260-390 (loadChecks and loadMotions functions containing all regressions)
  </read_first>

  <action>
Apply four targeted fixes. Do not touch any other code.

**Fix A — REG-01 (CRITICAL crash): Add null guard in trust.js loadMotions()**

In public/assets/js/pages/trust.js around line 316, the line:
  kpi.textContent = motions.length;
must become:
  if (kpi) kpi.textContent = motions.length;

This is the only unguarded .textContent assignment on a potentially-null element. All other
kpi* references in this file already use if (el) guards.

**Fix B — REG-04: Correct stale element ID in trust.js loadChecks()**

In public/assets/js/pages/trust.js around line 270, change:
  const integrityChecksEl = document.getElementById('integrityChecks');
to:
  const integrityChecksEl = document.getElementById('integrityStatus');

id="integrityStatus" exists at line 94 of trust.htmx.html. id="integrityChecks" was renamed
at some point without updating this reference. This fix makes the pass-rate color class update
actually apply to the visible integrity stat card.

**Fix C — REG-02 + REG-03: Add three missing KPI stat elements to trust.htmx.html**

In public/trust.htmx.html, locate the .integrity-summary div (around line 93). After the
last existing .integrity-stat child (the one with id="kpiLastSession"), and before the
closing </div> of .integrity-summary, insert:

              <div class="integrity-stat" id="integrityMotions" role="status" aria-label="Nombre de r&eacute;solutions">
                <div class="integrity-stat-value" id="kpiMotions">&mdash;</div>
                <div class="integrity-stat-label">R&eacute;solutions</div>
              </div>
              <div class="integrity-stat" id="integrityPresent" role="status" aria-label="Membres pr&eacute;sents">
                <div class="integrity-stat-value" id="kpiPresent">&mdash;</div>
                <div class="integrity-stat-label">Pr&eacute;sents</div>
              </div>
              <div class="integrity-stat" id="integrityBallots" role="status" aria-label="Total bulletins">
                <div class="integrity-stat-value" id="kpiBallots">&mdash;</div>
                <div class="integrity-stat-label">Bulletins</div>
              </div>

Use &mdash; as initial value (matches existing pattern on kpiEvents, kpiLastSession).
Do not change or reorder any existing stats. Do not add new CSS classes.
  </action>

  <verify>
    <automated>grep -n "if (kpi) kpi.textContent = motions.length" /home/user/gestion_votes_php/public/assets/js/pages/trust.js && grep -n "getElementById('integrityStatus')" /home/user/gestion_votes_php/public/assets/js/pages/trust.js && grep -n 'id="kpiMotions"' /home/user/gestion_votes_php/public/trust.htmx.html && grep -n 'id="kpiPresent"' /home/user/gestion_votes_php/public/trust.htmx.html && grep -n 'id="kpiBallots"' /home/user/gestion_votes_php/public/trust.htmx.html && echo "ALL CHECKS PASSED"</automated>
  </verify>

  <done>
    - trust.js: null guard present on kpiMotions .textContent (line ~316)
    - trust.js: getElementById targets 'integrityStatus' not 'integrityChecks' (line ~270)
    - trust.htmx.html: three new .integrity-stat elements added — id="kpiMotions", id="kpiPresent", id="kpiBallots"
    - grep verification command exits 0 with "ALL CHECKS PASSED"
  </done>

  <acceptance_criteria>
    - trust.js line ~316: `if (kpi) kpi.textContent = motions.length;` — null guard present
    - trust.js line ~270: `getElementById('integrityStatus')` — not `integrityChecks`
    - trust.htmx.html: id="kpiMotions" present inside .integrity-summary
    - trust.htmx.html: id="kpiPresent" present inside .integrity-summary
    - trust.htmx.html: id="kpiBallots" present inside .integrity-summary
    - No other code changed in either file
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify trust page motions table and KPI stats in browser</name>
  <files>public/trust.htmx.html, public/assets/js/pages/trust.js</files>

  <action>
Human verification only — no code changes. Open the trust page in browser, select a meeting
with motions, and confirm the motions table populates and no TypeError appears in console.
  </action>

  <verify>
    <automated>MISSING — no automated test infrastructure for PHP project; manual browser verification required</automated>
  </verify>

  <done>Human confirms: motions table renders data, no TypeError in console, three new KPI stat boxes visible with numbers, Intégrité card changes color based on pass rate.</done>

  <what-built>
    Four targeted fixes applied to trust.htmx.html and trust.js:
    - Null guard added on kpiMotions before .textContent (REG-01 crash fix)
    - integrityChecks reference corrected to integrityStatus (REG-04)
    - Three new KPI stat elements added: kpiMotions, kpiPresent, kpiBallots (REG-02, REG-03)
  </what-built>

  <how-to-verify>
    1. Open the trust page in browser (navigate to /trust or equivalent route)
    2. Open browser DevTools → Console tab
    3. Select any meeting from the "Séance" dropdown that has at least one motion/resolution
    4. Observe the motions table (Résolutions & Décisions section):
       - EXPECTED: Table populates with rows of resolution data
       - FAIL: Table stays empty showing "Sélectionnez une séance..."
    5. Check the integrity summary dashboard (top card):
       - EXPECTED: Three new stat boxes visible — "Résolutions", "Présents", "Bulletins" — each showing a number after loading
       - FAIL: Stat boxes missing or showing — after loading
    6. Check DevTools console:
       - EXPECTED: No TypeError (was: "Cannot set properties of null (setting 'textContent')")
       - FAIL: Any TypeError visible in console
    7. Verify the integrity stat card color updates:
       - EXPECTED: The "Intégrité" card changes color (green/yellow/red) based on pass rate after running checks
       - FAIL: Card color never changes
  </how-to-verify>

  <acceptance_criteria>
    - Motions table populates with real data after selecting a meeting with motions
    - No TypeError in browser console on any interaction with the trust page
    - All three new KPI stat boxes (Résolutions, Présents, Bulletins) visible and display numeric values
    - Intégrité stat card color reflects actual pass rate
  </acceptance_criteria>

  <resume-signal>Type "approved" if all checks pass, or describe what failed</resume-signal>
</task>

</tasks>

<verification>
After both tasks complete:
- trust.htmx.html has 7 integrity-stat elements in .integrity-summary (was 4)
- trust.js has null guard on kpiMotions .textContent assignment
- trust.js references integrityStatus (not integrityChecks)
- Browser shows no TypeError on the trust page
- Motions table renders real data
</verification>

<success_criteria>
All v4.2 regressions on "fix now" pages are eliminated:
- The only hard crash (REG-01: kpiMotions TypeError) is fixed
- The three silent no-ops (REG-02, REG-03, REG-04) are restored to functioning state
- All other "fix now" pages confirmed clean (verified during research — no changes needed)
- Phase 42 baseline is clean and Phases 43-48 can proceed from a working foundation
</success_criteria>

<output>
After completion, create .planning/phases/42-stabilization/42-01-SUMMARY.md
</output>
