---
phase: 02-refactoring-authmiddleware
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Core/Security/SessionManager.php
  - app/Core/Security/RbacEngine.php
  - app/Core/Security/AuthMiddleware.php
autonomous: true
requirements: [REFAC-01, REFAC-02]

must_haves:
  truths:
    - "AuthMiddleware.php is under 300 LOC"
    - "SessionManager.php is under 300 LOC and is a final class"
    - "RbacEngine.php is under 300 LOC and is a final class"
    - "All 20 AuthMiddlewareTest tests pass without modification"
    - "All 5 AuthMiddlewareTimeoutTest tests pass without modification"
    - "All AuthMiddleware:: static calls from 16 callers still resolve correctly"
  artifacts:
    - path: "app/Core/Security/SessionManager.php"
      provides: "Session timeout, expiry checking, DB revalidation"
      contains: "final class SessionManager"
    - path: "app/Core/Security/RbacEngine.php"
      provides: "Role checks, permissions, transitions, meeting roles"
      contains: "final class RbacEngine"
    - path: "app/Core/Security/AuthMiddleware.php"
      provides: "Thin orchestrator with delegation stubs"
      min_lines: 100
  key_links:
    - from: "app/Core/Security/AuthMiddleware.php"
      to: "app/Core/Security/SessionManager.php"
      via: "static delegation in authenticate() and getSessionTimeout()"
      pattern: "SessionManager::"
    - from: "app/Core/Security/AuthMiddleware.php"
      to: "app/Core/Security/RbacEngine.php"
      via: "static delegation in requireRole(), can(), canTransition()"
      pattern: "RbacEngine::"
    - from: "app/Core/Security/AuthMiddleware.php"
      to: "app/Core/Security/AuthMiddleware.php"
      via: "reset() chains to SessionManager::reset() + RbacEngine::reset()"
      pattern: "SessionManager::reset.*RbacEngine::reset"
---

<objective>
Extract SessionManager and RbacEngine from AuthMiddleware (871 LOC) into focused classes, each under 300 LOC. AuthMiddleware becomes a thin orchestrator with delegation stubs so zero callers need updating.

Purpose: REFAC-01 and REFAC-02 — reduce AuthMiddleware from 871 LOC to <300 LOC by extracting session management and RBAC evaluation into dedicated final classes.
Output: Three files under 300 LOC each, all existing tests green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-refactoring-authmiddleware/02-RESEARCH.md

@app/Core/Security/AuthMiddleware.php
@app/Core/Security/Permissions.php
@app/Core/Security/SessionHelper.php
@tests/Unit/AuthMiddlewareTest.php
@tests/Unit/AuthMiddlewareTimeoutTest.php

<interfaces>
<!-- Permissions.php constants and methods used by extracted classes -->
From app/Core/Security/Permissions.php:
```php
final class Permissions {
    public const HIERARCHY = [ /* admin => 100, operator => 80, ... */ ];
    public const PERMISSIONS = [ /* role => [permission, ...] */ ];
    public const TRANSITIONS = [ /* status => [role => [target_status, ...]] */ ];
    public const LABELS = [ /* system_roles, meeting_roles, meeting_statuses => [...] */ ];
    public static function getConfig(): array;
    public static function getHierarchy(): array;
    public static function getPermissions(): array;
    public static function getTransitions(): array;
    public static function getLabels(): array;
}
```

<!-- Current AuthMiddleware public static API that must be preserved as delegation stubs -->
Key static methods called externally (16 callers):
- authenticate(), requireRole(), getCurrentUser/Id/Role/TenantId()
- can(), requirePermission(), canAccessMeeting()
- canTransition(), requireTransition(), availableTransitions()
- getSessionTimeout(), setSessionTimeoutForTest()
- isEnabled(), init(), reset(), deny()
- getSystemRoles(), getSystemRoleLabels(), getMeetingRoleLabels(), getMeetingStatusLabels()
- hashApiKey(), generateApiKey(), extractApiKey(), findUserByApiKey()
- isOwner(), getAppSecret(), getDefaultTenantId()
- setCurrentUser(), getAccessLog(), logAccessAttempt()
- setMeetingContext(), getMeetingRoles(), getEffectiveRoles()
- normalizeRole(), isMeetingRole(), isSystemRole()
- getRoleLevel(), isRoleAtLeast(), getAvailablePermissions()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SessionManager and RbacEngine extracted classes</name>
  <files>app/Core/Security/SessionManager.php, app/Core/Security/RbacEngine.php</files>
  <read_first>
    - app/Core/Security/AuthMiddleware.php (full file — the source of all extracted code)
    - app/Core/Security/Permissions.php (constants referenced by RBAC methods)
    - .planning/phases/02-refactoring-authmiddleware/02-RESEARCH.md (method inventory, LOC budgets, pitfalls)
  </read_first>
  <action>
    Create two new files in `app/Core/Security/` namespace `AgVote\Core\Security`.

    **SessionManager.php** (target ~150 LOC):
    - `declare(strict_types=1);` at top
    - `final class SessionManager` with `public function __construct(?RepositoryFactory $repoFactory = null)` that stores to `private static ?RepositoryFactory $repoFactory`
    - Move constants: `DEFAULT_SESSION_TIMEOUT` (1800), `SESSION_REVALIDATE_INTERVAL` (60)
    - Move state props: `$sessionExpired`, `$cachedSessionTimeout`, `$cachedTimeoutTenantId`, `$testSessionTimeout`, `$testTimeoutTenantId`
    - Move methods (keep same signatures, change visibility to public static):
      - `getSessionTimeout(?string $tenantId = null): int` — copy lines 114-148 from AuthMiddleware
      - `setSessionTimeoutForTest(string $tenantId, ?int $seconds): void` — copy lines 156-162
      - `isSessionExpired(): bool` — returns `self::$sessionExpired`
      - `consumeSessionExpired(): bool` — get + clear flag
    - NEW extracted methods from authenticate():
      - `checkExpiry(int $lastActivity, ?string $tenantId = null): bool` — extracted from lines 373-391 of authenticate()
      - `revalidateUser(string $userId): ?array` — extracted from lines 394-427 of authenticate(), uses RepositoryFactory to get UserRepository
    - `reset(): void` — clears all static state props
    - PITFALL: `getSessionTimeout()` reads tenant_id from `$_SESSION['auth_user']['tenant_id']` directly, NOT from `AuthMiddleware::getCurrentTenantId()`. Preserve this to avoid circular dependency.

    **RbacEngine.php** (target ~250 LOC):
    - `declare(strict_types=1);` at top
    - `final class RbacEngine` with `public function __construct(?RepositoryFactory $repoFactory = null)` storing to static
    - Move constants: `SYSTEM_ROLES`, `MEETING_ROLES`, `ROLE_ALIASES`
    - Move state: `$currentMeetingId`, `$currentMeetingRoles`
    - Move methods (all public static, accept `$user` as parameter where needed instead of calling AuthMiddleware):
      - `normalizeRole(string $role): string`
      - `isMeetingRole(string $role): bool`
      - `isSystemRole(string $role): bool`
      - `setMeetingContext(?string $meetingId): void`
      - `getMeetingRoles(?array $user, ?string $meetingId = null): array`
      - `getEffectiveRoles(?array $user, ?string $meetingId = null): array`
      - `checkRole(?array $user, array $roles): bool` — core logic from requireRole() (the role evaluation, NOT the auth check/deny)
      - `can(?array $user, string $permission, ?string $meetingId = null): bool`
      - `requirePermission(string $permission, ?string $meetingId = null): bool` — calls `AuthMiddleware::authenticate()` then delegates to own `can()`... NO — this creates coupling. Instead, keep requirePermission on AuthMiddleware as a delegation stub that calls authenticate() then RbacEngine::can().
      - `canAccessMeeting(?array $user, string $meetingId): bool` — meeting access check (calls getMeetingRoles internally)
      - `canTransition(?array $user, string $from, string $to, ?string $meetingId = null): bool`
      - `availableTransitions(?array $user, string $currentStatus, ?string $meetingId = null): array`
      - `getAvailablePermissions(?array $user, ?string $meetingId = null): array`
      - `getRoleLevel(string $role): int` — delegates to Permissions::HIERARCHY
      - `isRoleAtLeast(string $role, string $minimumRole): bool`
      - Label getters: `getSystemRoles()`, `getSystemRoleLabels()`, `getMeetingRoleLabels()`, `getMeetingStatusLabels()` — thin lookups into Permissions::LABELS
    - `reset(): void` — clears `$currentMeetingId`, `$currentMeetingRoles`, `$repoFactory`
    - PITFALL: RbacEngine methods that need the current user receive `$user` as a parameter from AuthMiddleware delegation stubs. They NEVER call AuthMiddleware::getCurrentUser(). This keeps RbacEngine testable in isolation.
    - PITFALL: `getMeetingRoles()` queries DB via RepositoryFactory. Use `RepositoryFactory::getInstance()` as default when static `$repoFactory` is null.

    Run `php -l app/Core/Security/SessionManager.php && php -l app/Core/Security/RbacEngine.php` to verify syntax.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && php -l app/Core/Security/SessionManager.php && php -l app/Core/Security/RbacEngine.php && grep -c 'final class' app/Core/Security/SessionManager.php app/Core/Security/RbacEngine.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'final class SessionManager' app/Core/Security/SessionManager.php` matches
    - `grep 'final class RbacEngine' app/Core/Security/RbacEngine.php` matches
    - `grep '__construct' app/Core/Security/SessionManager.php` shows nullable DI constructor
    - `grep '__construct' app/Core/Security/RbacEngine.php` shows nullable DI constructor
    - `wc -l app/Core/Security/SessionManager.php` shows less than 300
    - `wc -l app/Core/Security/RbacEngine.php` shows less than 300
    - Both files pass `php -l` syntax check
  </acceptance_criteria>
  <done>SessionManager.php and RbacEngine.php exist as final classes under 300 LOC each with nullable DI constructors, containing all extracted methods from AuthMiddleware</done>
</task>

<task type="auto">
  <name>Task 2: Refactor AuthMiddleware to thin orchestrator with delegation stubs</name>
  <files>app/Core/Security/AuthMiddleware.php</files>
  <read_first>
    - app/Core/Security/AuthMiddleware.php (current 871 LOC file)
    - app/Core/Security/SessionManager.php (just created — need to know exact method signatures)
    - app/Core/Security/RbacEngine.php (just created — need to know exact method signatures)
    - tests/Unit/AuthMiddlewareTest.php (20 tests that must pass unchanged)
    - tests/Unit/AuthMiddlewareTimeoutTest.php (5 tests that must pass unchanged)
  </read_first>
  <action>
    Rewrite AuthMiddleware.php as a thin orchestrator. Target: <300 LOC (research estimates ~250 LOC).

    **Keep in AuthMiddleware (own methods):**
    - State: `$currentUser`, `$debug`, `$accessLog` only
    - `init(array $config = []): void`
    - `isEnabled(): bool`
    - `authenticate(): ?array` — REWRITE to:
      1. Check isEnabled, handle API key auth (keep inline)
      2. Read session, call `SessionManager::checkExpiry()` for timeout
      3. Call `SessionManager::revalidateUser()` for DB re-check
      4. Set `$currentUser` from session
      5. Orchestration only — no session timeout math, no DB queries
    - `getCurrentUser()`, `getCurrentUserId()`, `getCurrentRole()`, `getCurrentTenantId()` — keep as-is
    - API key methods: `extractApiKey()`, `findUserByApiKey()`, `generateApiKey()`, `hashApiKey()` — keep as-is
    - `deny()`, `logAuthFailure()`, `logAccessAttempt()`, `getAccessLog()` — keep as-is
    - `isOwner()`, `getAppSecret()`, `getDefaultTenantId()` — keep as-is
    - `setCurrentUser()` — keep (test helper)
    - `reset()` — MUST chain: `SessionManager::reset(); RbacEngine::reset();` then clear own state

    **Add delegation stubs (one-liner forwards):**
    - `requireRole($roles, $strict)` → check isEnabled + authenticate, then `RbacEngine::checkRole(self::$currentUser, ...)`; if check fails and strict, call `self::deny()`
    - `can($permission, $meetingId)` → `RbacEngine::can(self::$currentUser, $permission, $meetingId)`
    - `requirePermission($permission, $meetingId)` → authenticate + can + deny
    - `canAccessMeeting($meetingId)` → `RbacEngine::canAccessMeeting(self::$currentUser, $meetingId)`
    - `canTransition($from, $to, $meetingId)` → `RbacEngine::canTransition(self::$currentUser, ...)`
    - `requireTransition($from, $to, $meetingId)` → canTransition + deny
    - `availableTransitions($status, $meetingId)` → `RbacEngine::availableTransitions(self::$currentUser, ...)`
    - `getSessionTimeout($tenantId)` → `SessionManager::getSessionTimeout($tenantId)`
    - `setSessionTimeoutForTest($tenantId, $seconds)` → `SessionManager::setSessionTimeoutForTest(...)`
    - `setMeetingContext($meetingId)` → `RbacEngine::setMeetingContext($meetingId)`
    - `getMeetingRoles($user, $meetingId)` → `RbacEngine::getMeetingRoles($user, $meetingId)`
    - `getEffectiveRoles($user, $meetingId)` → `RbacEngine::getEffectiveRoles($user, $meetingId)`
    - `normalizeRole($role)` → `RbacEngine::normalizeRole($role)`
    - `isMeetingRole($role)` → `RbacEngine::isMeetingRole($role)`
    - `isSystemRole($role)` → `RbacEngine::isSystemRole($role)`
    - `getAvailablePermissions($user, $meetingId)` → `RbacEngine::getAvailablePermissions($user, $meetingId)`
    - `getRoleLevel($role)` → `RbacEngine::getRoleLevel($role)`
    - `isRoleAtLeast($role, $min)` → `RbacEngine::isRoleAtLeast($role, $min)`
    - `getSystemRoles()` → `RbacEngine::getSystemRoles()`
    - `getSystemRoleLabels()` → `RbacEngine::getSystemRoleLabels()`
    - `getMeetingRoleLabels()` → `RbacEngine::getMeetingRoleLabels()`
    - `getMeetingStatusLabels()` → `RbacEngine::getMeetingStatusLabels()`

    **Add use statements:** `use AgVote\Core\Security\SessionManager;` and `use AgVote\Core\Security\RbacEngine;` (same namespace, but explicit for clarity — or omit since same namespace).

    **CRITICAL: Preserve exact public method signatures.** Every `AuthMiddleware::methodName()` call in the 16 caller files and 2 test files must resolve. Do NOT rename, change parameter order, or remove optional parameters.

    **After rewrite, run existing tests:**
    ```
    timeout 60 php vendor/bin/phpunit tests/Unit/AuthMiddlewareTest.php tests/Unit/AuthMiddlewareTimeoutTest.php --no-coverage
    ```
    All 25 tests must pass. If failures occur, fix the delegation stubs — do NOT modify test files.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && php -l app/Core/Security/AuthMiddleware.php && timeout 60 php vendor/bin/phpunit tests/Unit/AuthMiddlewareTest.php tests/Unit/AuthMiddlewareTimeoutTest.php --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l app/Core/Security/AuthMiddleware.php` shows less than 300
    - `php -l app/Core/Security/AuthMiddleware.php` passes syntax check
    - `timeout 60 php vendor/bin/phpunit tests/Unit/AuthMiddlewareTest.php --no-coverage` shows 20 tests, 0 failures
    - `timeout 60 php vendor/bin/phpunit tests/Unit/AuthMiddlewareTimeoutTest.php --no-coverage` shows 5 tests, 0 failures
    - `grep -c 'SessionManager::' app/Core/Security/AuthMiddleware.php` shows at least 3 delegation calls
    - `grep -c 'RbacEngine::' app/Core/Security/AuthMiddleware.php` shows at least 5 delegation calls
  </acceptance_criteria>
  <done>AuthMiddleware.php is under 300 LOC, delegates to SessionManager and RbacEngine, all 25 existing tests pass without modification</done>
</task>

</tasks>

<verification>
1. `wc -l app/Core/Security/AuthMiddleware.php` < 300
2. `wc -l app/Core/Security/SessionManager.php` < 300
3. `wc -l app/Core/Security/RbacEngine.php` < 300
4. `grep 'final class' app/Core/Security/SessionManager.php app/Core/Security/RbacEngine.php` matches both
5. `timeout 60 php vendor/bin/phpunit tests/Unit/AuthMiddlewareTest.php tests/Unit/AuthMiddlewareTimeoutTest.php --no-coverage` — 25 tests, 0 failures
</verification>

<success_criteria>
- AuthMiddleware.php < 300 LOC (down from 871)
- SessionManager.php < 300 LOC, final class, nullable DI constructor
- RbacEngine.php < 300 LOC, final class, nullable DI constructor
- All 25 existing AuthMiddleware tests pass unchanged
- Zero caller files require modification
</success_criteria>

<output>
After completion, create `.planning/phases/02-refactoring-authmiddleware/02-01-SUMMARY.md`
</output>
