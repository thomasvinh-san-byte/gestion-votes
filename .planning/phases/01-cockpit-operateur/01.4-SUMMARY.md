---
phase: 01-cockpit-operateur
plan: 4
subsystem: tests
tags: [e2e, playwright, cockpit, regression-guard]

# Dependency graph
requires:
  - plan: 01.1 (ag-health-bar custom element + stylesheet — `<ag-health-bar>` must be loaded for assertions)
  - plan: 01.2 (operator-keybindings IIFE — `agShortcutsOverlay` element + L/F/?/Escape handlers)
  - plan: 01.3 (operator integration — production buttons must carry `aria-keyshortcuts` for the @integration smoke)
provides:
  - "tests/e2e/specs/cockpit-health-bar.spec.js — 8 tests covering custom-element contract, three-state colors, pulse + reduced-motion, at-risk anti-pulse, responsive stack, attribute-driven re-render, F-1 mental-model lock"
  - "tests/e2e/specs/cockpit-keyboard-shortcuts.spec.js — 7 tests covering ?+Escape overlay, L/F dispatch, input/contentEditable/modifier suppression, F-4 @integration smoke against real seeded meeting"
  - "Permanent regression guard for the 'Votes restants' label (UX critique B from Zhuo/Norman review) — the label CANNOT be reverted to 'Votants connectés' without test breakage"
affects: [phase 01 verification gate, future cockpit refactors]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Stub-first isolation pattern: most keyboard tests inject minimal `#opBtnLaunchVote` / `#opBtnCloseVote` buttons + force `window.O.currentMode='exec'` so shortcuts are testable without seeding a meeting"
    - "Real-meeting smoke pattern (F-4): single @integration test that asserts the production toggle button carries `aria-keyshortcuts=\"L\"` after Plan 01.3 wiring — gracefully `test.skip()` if `helpers/seed-meeting` helper is absent"
    - "Layout-settle pattern (F-3): `await page.waitForFunction(() => getComputedStyle(...).flexDirection === 'column', null, { timeout: 2000 })` BEFORE reading layout — prevents viewport-set race"
    - "Reduced-motion two-context pattern: spawn a second `browser.newContext({ reducedMotion: 'reduce' })` to verify CSS `@media (prefers-reduced-motion: reduce)` actually disables the animation"

key-files:
  created:
    - tests/e2e/specs/cockpit-health-bar.spec.js (8 tests, ~6.8 kB, tagged @cockpit-v2.3)
    - tests/e2e/specs/cockpit-keyboard-shortcuts.spec.js (7 tests, ~7.5 kB, includes @integration tag)
  modified: []

key-decisions:
  - "8 tests in cockpit-health-bar (not 7) — the executor split the F-1 mental-model lock into two assertions for clarity (one for the positive 'Votes restants' presence, one wrapped with the `not.toContainText('Votants connectés')` guard inside the same test). The acceptance grep `>= 7` is satisfied; the count exceeds the spec by 1 which is fine."
  - "F-4 @integration test uses `test.skip(!helper, ...)` — if the project does not yet have a `tests/e2e/helpers/seed-meeting` module, the test gracefully skips at runtime rather than failing. Documented as expected behavior; the helper is listed as future work in the project backlog (not v2.3 scope)."
  - "Task 3 (regression run on `tests/e2e/specs/critical-path-operator.spec.js`) was NOT executed in this session — see Deviations below for rationale and the manual follow-up command"

# Verification
verification:
  acceptance_criteria:
    health-bar:
      - "node --check tests/e2e/specs/cockpit-health-bar.spec.js → exit 0 ✓"
      - "grep -c 'test(' tests/e2e/specs/cockpit-health-bar.spec.js → 8 (≥ 7 required) ✓"
      - "grep 'customElements.get' → matches ✓"
      - "grep 'observedAttributes' → matches ✓"
      - "grep 'ag-health-bar-pulse' → matches ✓"
      - "grep \"reducedMotion: 'reduce'\" → matches ✓"
      - "grep 'setViewportSize' → matches ✓"
      - "grep 'waitForFunction' → matches (F-3 settle) ✓"
      - "grep \"'at-risk'\" → matches ✓"
      - "grep 'Votes restants' → matches (F-1 positive lock) ✓"
      - "grep 'Votants connectés' → matches inside `.not.toContainText` (F-1 anti-pattern lock) ✓"
      - "grep '@cockpit-v2.3' → matches ✓"
    keyboard-shortcuts:
      - "node --check tests/e2e/specs/cockpit-keyboard-shortcuts.spec.js → exit 0 ✓"
      - "grep -c 'test(' → 7 (≥ 7 required) ✓"
      - "grep \"page.keyboard.press('?')\" → matches ✓"
      - "grep \"page.keyboard.press('Escape')\" → matches ✓"
      - "grep \"page.keyboard.press('l')\" → matches ✓"
      - "grep \"page.keyboard.press('f')\" → matches ✓"
      - "grep 'contentEditable' → matches ✓"
      - "grep 'Control+l' → matches ✓"
      - "grep '@cockpit-v2.3' → matches ✓"
      - "grep '@integration' → matches (F-4 integration test) ✓"
      - "grep 'aria-keyshortcuts=\"L\"' → matches (F-4 production-button assertion) ✓"

  playwright_runs:
    actually_executed: 0
    budget: 3
    reason: "Sandbox environment used to write the specs does not have Playwright installed (`node_modules/@playwright` absent). Specs are syntactically valid via `node --check`; runtime execution must happen on a developer machine with the dev server + Playwright stack."
    manual_followup_commands:
      - "timeout 60 npx playwright test tests/e2e/specs/cockpit-health-bar.spec.js --reporter=line"
      - "timeout 60 npx playwright test tests/e2e/specs/cockpit-keyboard-shortcuts.spec.js --reporter=line"
      - "timeout 120 npx playwright test tests/e2e/specs/critical-path-operator.spec.js --reporter=line  # regression check (Task 3)"
    budget_remaining: 3

  regression_critical_path:
    spec: tests/e2e/specs/critical-path-operator.spec.js
    status: NOT_RUN
    modified: false  # verified via `git diff --name-only` — file untouched
    requires_manual: true
    note: "Per Task 3 in 01.4-PLAN.md, this regression spec must pass post-Phase 01 ship. Defer to first developer run on a Playwright-capable environment. If it fails twice consecutively on the same assertion, STOP and surface to the user — do not modify the existing spec."

# Deviations
deviations:
  - rule: "Task 3 not executed in this session"
    type: "environment limitation, not a Rule 1-4 deviation"
    rationale: "Playwright is not installed in the GSD sandbox where this executor ran. Running `npx playwright test` would require either downloading the runtime (out of scope for this plan) or running in the developer's local environment. The 3 specs (2 new + 1 regression) are listed as `manual_followup_commands` above with the exact invocations and CLAUDE.md retry rules pre-applied. The phase verification gate (gsd-verifier next step) will note this as a partial completion that requires a green Playwright run on the developer's machine before final phase sign-off."
    impact: "Plan 01.4 ships its file artifacts (the 2 new specs) but not the runtime green/red verdict. The Wave 3 acceptance is satisfied for code (specs exist + node --check pass + all UX critique fixes preserved as test contracts) but the runtime verification is parked at the user's first opportunity to run Playwright."
  - rule: "Stream idle timeout"
    type: "infrastructure interruption, not an executor decision"
    rationale: "The async agent dispatch returned with 'API Error: Stream idle timeout - partial response received' after 62 tool uses. Pre-timeout, the agent had successfully written the 2 spec files and committed each (commits e6ff174 and 5714ccc). The post-task wrap-up (SUMMARY.md + STATE.md update + ROADMAP.md update) was incomplete. The orchestrator (Claude main thread) wrote this SUMMARY.md and the state updates manually after spot-checking the disk artifacts."

# Tests
tests:
  added:
    - tests/e2e/specs/cockpit-health-bar.spec.js (8 tests, all expected to pass on a Playwright-capable env)
    - tests/e2e/specs/cockpit-keyboard-shortcuts.spec.js (7 tests, 1 with conditional skip)
  modified: []
  removed: []

# Followups
followups:
  - id: "01.4-FOLLOWUP-1"
    type: "manual verification"
    description: "Run the 3 Playwright commands listed under verification.playwright_runs.manual_followup_commands on a developer machine. Surface any failures (especially the regression check) before declaring Phase 01 fully shipped."
    owner: "developer"
    blocking_for: "Phase 01 final sign-off / `/gsd:ship`"
  - id: "01.4-FOLLOWUP-2"
    type: "test infrastructure"
    description: "Create `tests/e2e/helpers/seed-meeting.js` to enable the F-4 @integration test (currently skips at runtime). Module should expose `seedRunningMeeting()` returning `{ id, ... }` for a real seeded meeting in the test DB."
    owner: "developer"
    blocking_for: "F-4 contract being a hard regression guard rather than a soft skip"
    priority: "low — the unit-isolation tests cover the dispatch logic; the @integration test only adds a production-button affordance assertion"

# Commits
commits:
  - hash: e6ff174
    type: test
    description: "add cockpit-health-bar E2E spec — 8 tests including F-1 mental-model lock + F-3 layout settle"
  - hash: 5714ccc
    type: test
    description: "add cockpit-keyboard-shortcuts E2E spec — 7 tests including F-4 @integration smoke against production aria-keyshortcuts"
  - hash: TBD
    type: docs
    description: "complete plan 01.4 (this SUMMARY.md + STATE/ROADMAP updates)"
