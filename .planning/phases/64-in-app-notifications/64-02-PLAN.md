---
phase: 64-in-app-notifications
plan: 02
type: execute
wave: 2
depends_on: [64-01]
files_modified:
  - public/assets/js/pages/operator-exec.js
  - public/assets/js/pages/hub.js
autonomous: false
requirements: [NOTIF-03]

must_haves:
  truths:
    - "When a vote opens on the operator page, an ag-toast appears with the motion title"
    - "When a vote opens on the hub page, an ag-toast appears with the motion title"
    - "When quorum is reached, a success toast appears"
    - "When a session starts or closes, a toast appears"
  artifacts:
    - path: "public/assets/js/pages/operator-exec.js"
      provides: "SSE event forwarding to Notifications.handleSseEvent"
      contains: "Notifications.handleSseEvent"
    - path: "public/assets/js/pages/hub.js"
      provides: "SSE event forwarding to Notifications.handleSseEvent"
      contains: "Notifications.handleSseEvent"
  key_links:
    - from: "public/assets/js/pages/operator-exec.js"
      to: "window.Notifications.handleSseEvent"
      via: "onEvent callback in EventStream.connect"
      pattern: "Notifications\\.handleSseEvent"
    - from: "public/assets/js/pages/hub.js"
      to: "window.Notifications.handleSseEvent"
      via: "onEvent callback in EventStream.connect"
      pattern: "Notifications\\.handleSseEvent"
---

<objective>
Wire SSE events on operator and hub pages to trigger real-time toasts via the Notifications.handleSseEvent hook created in Plan 01, then verify the complete notification system end-to-end.

Purpose: Complete NOTIF-03 by connecting SSE event stream to the toast system on the two pages that use EventStream.connect().
Output: Real-time toasts on operator and hub pages when votes open, quorum reached, session starts/closes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-in-app-notifications/64-RESEARCH.md
@.planning/phases/64-in-app-notifications/64-01-SUMMARY.md

<interfaces>
<!-- From shell.js (after Plan 01) -->
window.Notifications = {
  fetch: fetchNotifications,
  handleSseEvent: function(type, data) { ... }  // Maps SSE event types to French toasts via Shared.showToast
};

<!-- From event-stream.js -->
EventStream.connect(meetingId, {
  onEvent: function(type, data) { ... },  // Called for every SSE event
  onConnect: function() { ... },
  onDisconnect: function() { ... }
});

<!-- SSE event types that trigger toasts -->
motion.opened   -> "Vote ouvert : {title}"  (info)
motion.closed   -> "Vote cloture"           (info)
quorum.updated  -> "Quorum atteint"         (success, only when met=true)
meeting.status_changed -> "Seance demarree" (success) or "Seance cloturee" (info)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire SSE events to toast system in operator-exec.js and hub.js</name>
  <files>public/assets/js/pages/operator-exec.js, public/assets/js/pages/hub.js</files>
  <read_first>
    - public/assets/js/pages/operator-exec.js (find EventStream.connect call and its onEvent handler)
    - public/assets/js/pages/hub.js (find EventStream.connect call and its onEvent handler)
  </read_first>
  <action>
In both `operator-exec.js` and `hub.js`, find the `EventStream.connect()` call and its `onEvent` handler.

**For each file**, add the following line inside the `onEvent` callback, AFTER any existing event handling logic (so page-specific handlers run first):

```javascript
// Forward to notification toast system
if (window.Notifications && window.Notifications.handleSseEvent) {
  window.Notifications.handleSseEvent(type, data);
}
```

This must be placed inside the existing `onEvent: function(type, data) { ... }` callback, at the END of the function body, so it does not interfere with existing page-specific SSE handling (attendance refresh, vote panel update, etc.).

**Important:** Do NOT create a new EventStream.connect() call. Do NOT replace existing onEvent logic. Only ADD the forwarding line at the end of the existing onEvent handler.

**For hub.js specifically:** If there is already a `Shared.showToast` call inside the onEvent handler (the research mentions `payload.msg` pattern at L575-576), keep it. The Notifications.handleSseEvent will also fire but with translated French messages. If the existing toast uses `payload.msg` which is undefined, it will be a no-op, so no duplicate toasts.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "Notifications.handleSseEvent" public/assets/js/pages/operator-exec.js && grep -c "Notifications.handleSseEvent" public/assets/js/pages/hub.js</automated>
  </verify>
  <acceptance_criteria>
    - grep: "Notifications.handleSseEvent" in public/assets/js/pages/operator-exec.js
    - grep: "Notifications.handleSseEvent" in public/assets/js/pages/hub.js
    - Existing onEvent handling in both files is preserved (not replaced)
  </acceptance_criteria>
  <done>Both operator-exec.js and hub.js forward SSE events to window.Notifications.handleSseEvent inside their existing onEvent handlers</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to confirm no regressions</name>
  <files>no files modified — verification only</files>
  <read_first>
    - tests/Unit/NotificationsServiceTest.php
    - tests/Unit/NotificationsControllerTest.php
  </read_first>
  <action>
Run the full PHPUnit test suite to confirm no regressions from the notification changes:

```bash
./vendor/bin/phpunit --no-coverage
```

If any test fails, diagnose and fix. The notification changes should not affect any other test because:
- NotificationsService is a new file (no existing code modified)
- shell.js changes are JS-only (no PHP impact)
- operator-exec.js and hub.js changes add one line each (no logic change)

Also verify JS syntax is valid by checking for obvious issues:
```bash
node -e "try { require('fs').readFileSync('public/assets/js/core/shell.js','utf8'); console.log('shell.js: syntax readable'); } catch(e) { console.error(e); }"
```
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && ./vendor/bin/phpunit --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Full PHPUnit suite passes with 0 failures and 0 errors
    - NotificationsServiceTest: all tests pass
    - NotificationsControllerTest: all tests pass
  </acceptance_criteria>
  <done>Full test suite green, no regressions from notification system wiring</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify notification bell and toast system in browser</name>
  <files>n/a — browser verification</files>
  <action>
Human verifies the complete notification system in browser:

What was built: Complete in-app notification system — bell badge with unread count, notification panel with French labels, mark-all-read, and SSE real-time toasts on operator and hub pages.

Steps to verify:
1. Open the app in browser, log in as operator
2. Check the bell icon in the header — it should show a badge with the unread notification count (or no badge if 0)
3. Click the bell — a panel should open listing recent notifications with French labels (e.g., "Seance demarree", "Vote ouvert") and timestamps
4. Click the bell again to close, then reopen — the badge should drop to 0 (mark-all-read triggered on open)
5. Navigate to the operator console for an active session — SSE should be connected
6. Open a vote from the operator console — an ag-toast should appear saying "Vote ouvert : [motion title]"
7. On the hub page for the same session — SSE toasts should also appear for events

Resume signal: Type "approved" or describe any issues with the notification bell, panel, or toast behavior.
  </action>
  <verify>Human visual and functional verification in browser</verify>
  <done>User confirms bell badge, notification panel, mark-read, and real-time SSE toasts all work correctly</done>
</task>

</tasks>

<verification>
- `grep "Notifications.handleSseEvent" public/assets/js/pages/operator-exec.js` — SSE wired
- `grep "Notifications.handleSseEvent" public/assets/js/pages/hub.js` — SSE wired
- `./vendor/bin/phpunit --no-coverage` — full suite green
- Browser: bell shows unread count, panel shows French labels, mark-read clears badge, SSE toasts appear
</verification>

<success_criteria>
1. SSE events on operator and hub pages trigger French toast messages via ag-toast
2. Full test suite passes with no regressions
3. Human verification confirms bell badge, panel, mark-read, and real-time toasts all work
</success_criteria>

<output>
After completion, create `.planning/phases/64-in-app-notifications/64-02-SUMMARY.md`
</output>
