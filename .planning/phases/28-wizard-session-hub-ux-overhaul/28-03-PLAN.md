---
phase: 28-wizard-session-hub-ux-overhaul
plan: 03
type: execute
wave: 2
depends_on: [28-01, 28-02]
files_modified:
  - public/assets/css/wizard.css
  - public/assets/css/hub.css
autonomous: false
requirements: [WIZ-01, WIZ-03, WIZ-04, WIZ-05, WIZ-06, WIZ-07, WIZ-08]

must_haves:
  truths:
    - "Wizard has Notion-like aesthetic: generous spacing, strong typography, subtle shadows, clean borders"
    - "Hub has Notion-like aesthetic matching the wizard visual language"
    - "Review card sections are visually distinct with section headers and borders"
    - "Template buttons are styled as ghost buttons in a row"
    - "Voting power toggle has visible switch styling"
    - "Blocked reason text in checklist is muted italic"
    - "Wizard step transition has fade animation (150ms)"
    - "Motions list items are visually clean with doc badge alignment"
  artifacts:
    - path: "public/assets/css/wizard.css"
      provides: "Notion-like wizard CSS with review card, template, toggle, inline form, fade animation styles"
      contains: "review-section"
    - path: "public/assets/css/hub.css"
      provides: "Notion-like hub CSS with blocked reason, quorum section, motions list, convocation section styles"
      contains: "hub-check-blocked"
  key_links:
    - from: "wizard.css .review-section"
      to: "wizard.js buildReviewCard() HTML output"
      via: "CSS class names matching JS-generated HTML"
      pattern: "review-section"
    - from: "hub.css .hub-check-blocked"
      to: "hub.js renderChecklist() HTML output"
      via: "CSS class names matching JS-generated HTML"
      pattern: "hub-check-blocked"
---

<objective>
Complete CSS rewrite for wizard.css and hub.css in Notion-like aesthetic — minimaliste, typographie forte, aere, peu de couleurs.

Purpose: Visual overhaul transforms both pages from functional but dense layouts into clean, airy, content-focused interfaces. The CSS rewrite uses existing design-system.css tokens and scoped selectors to avoid bleeding into other pages.

Output: Rewritten wizard.css and hub.css with all new component styles.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-wizard-session-hub-ux-overhaul/28-CONTEXT.md
@.planning/phases/28-wizard-session-hub-ux-overhaul/28-RESEARCH.md
@.planning/phases/28-wizard-session-hub-ux-overhaul/28-01-SUMMARY.md
@.planning/phases/28-wizard-session-hub-ux-overhaul/28-02-SUMMARY.md

@public/assets/css/wizard.css
@public/assets/css/hub.css
@public/assets/css/design-system.css
@public/wizard.htmx.html
@public/hub.htmx.html

<interfaces>
<!-- CSS custom properties available from design-system.css -->

Fonts:
- var(--font-sans) — Bricolage Grotesque
- var(--font-display) — Fraunces
- var(--font-mono) — JetBrains Mono

Colors (light):
- var(--color-bg) — #EDECE6
- var(--color-surface) — #FAFAF7
- var(--color-text) — #151510
- var(--color-text-muted) — muted text
- var(--color-primary) — #1650E0
- var(--color-primary-subtle) — light primary tint
- var(--color-border) — border color
- var(--color-border-subtle) — lighter border
- var(--color-success) — green
- var(--color-warning) — amber
- var(--color-danger) — red
- var(--color-danger-subtle) — light red tint

Spacing:
- var(--space-4) — 16px
- var(--space-6) — 24px
- var(--space-8) — 32px

Radii:
- var(--radius-sm) — small radius
- var(--radius-md) — medium radius
- var(--radius-lg) — large radius (12px)

Transitions:
- var(--duration-fast) — 100ms
- var(--duration-normal) — 200ms

Shadows:
- var(--shadow-sm)
- var(--shadow-md)

<!-- New CSS classes from Plan 01 (wizard.js HTML output) -->
.review-section, .review-section-header, .review-section-title, .review-modifier
.review-row, .review-label, .review-value, .review-row-block
.review-warning, .review-empty, .review-reso-list
.wiz-templates-row, .wiz-templates-label, .wiz-template-btn
.wiz-toggle-row, .toggle-label
.wiz-member-add-form, .member-votes-field

<!-- New CSS classes from Plan 02 (hub.js HTML output) -->
.hub-check-blocked
.hub-quorum-section
.hub-motions-section, .hub-motions-header, .hub-motions-title
.hub-motion-item, .hub-motion-num, .hub-motion-title
.hub-convocation-section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wizard CSS rewrite — Notion-like aesthetic with all new component styles</name>
  <read_first>
    - public/assets/css/wizard.css
    - public/assets/css/design-system.css
    - public/wizard.htmx.html
    - .planning/phases/28-wizard-session-hub-ux-overhaul/28-01-SUMMARY.md
    - .planning/phases/28-wizard-session-hub-ux-overhaul/28-RESEARCH.md
  </read_first>
  <files>public/assets/css/wizard.css</files>
  <action>
Complete rewrite of wizard.css. Keep the same file comment header pattern. ALL selectors MUST be scoped under wizard-specific classes (.wiz-*, .step-nav, .reso-*, .member-*, .review-*, .wf-step, etc.) — NEVER redefine global component classes (.btn, .card, .field-input) which are in design-system.css.

**Notion-like design principles to apply everywhere:**
- Generous padding: step body padding 2rem 2.5rem (not 1.5rem)
- Typography hierarchy: section titles 0.6875rem uppercase 800 weight letter-spacing 0.08em color var(--color-text-muted)
- Subtle shadows: box-shadow 0 1px 4px rgba(0,0,0,.04) on cards
- Clean borders: 1px solid var(--color-border) with border-radius 12px
- Font display for step titles: var(--font-display) at 1.5rem 700 weight
- Airy spacing between sections: margin-bottom 1.5rem on wiz-section
- Step transition animation: fade-in with translateY(4px)

**Sections to include (in this order):**

1. **STEPPER PROGRESS BAR** — Rewrite existing .wiz-progress-wrap, .wiz-step-item, .wiz-snum styles:
   - .wiz-progress-wrap: background var(--color-surface), border 1px solid var(--color-border), border-radius var(--radius-lg), margin-bottom 1.5rem, box-shadow 0 1px 3px rgba(0,0,0,.04), padding 4px
   - .wiz-step-item: flex 1, gap 10px, padding 12px 16px, font-size 0.8125rem (13px), font-weight 600, color var(--color-text-muted), cursor pointer, border-radius var(--radius-md), transition background 0.15s ease
   - .wiz-step-item:last-child: no border-right change (remove border-right from all items, use gap instead)
   - .wiz-step-item.done: color var(--color-success), background color-mix(in srgb, var(--color-success) 6%, transparent)
   - .wiz-step-item.active: color var(--color-primary), background var(--color-primary-subtle), font-weight 700
   - .wiz-step-item.active::after: content '', position absolute, bottom 0, left 8px, right 8px, height 3px, background var(--color-primary), border-radius 2px
   - .wiz-snum: 22px x 22px, border-radius 50%, background var(--color-bg-subtle), font-size 0.6875rem (11px), font-weight 800

2. **STEP PANEL and BODY** — .wiz-step (card), .wiz-step-body, .wf-step:
   - .wiz-step: box-shadow 0 1px 4px rgba(0,0,0,.04), border 1px solid var(--color-border), border-radius 12px, background var(--color-surface), animation wizFadeIn 150ms ease-out
   - .wiz-step-body: padding 2rem 2.5rem
   - .wf-step: font-family var(--font-display), font-size 1.375rem, font-weight 700, gap 12px, padding-bottom 1.25rem, margin-bottom 1.25rem, border-bottom 1px solid var(--color-border-subtle)
   - .wf-step-num: 28px x 28px, background var(--color-primary-subtle), color var(--color-primary), font-size 0.8125rem, font-weight 800

3. **FADE ANIMATION:** @keyframes wizFadeIn from opacity 0 translateY(4px) to opacity 1 transform none

4. **FORM LAYOUT** — .wiz-section, .wiz-section-title, field overrides:
   - .wiz-section: margin-bottom 1.5rem
   - .wiz-section-title: font-size 0.6875rem, font-weight 800, text-transform uppercase, letter-spacing 0.08em, color var(--color-text-muted), margin-bottom 0.75rem, gap 6px
   - .wiz-required-note: margin-bottom 1rem, font-size 0.8125rem
   - .field-label (scoped as .wiz-step-body .field-label): font-size 0.75rem, font-weight 700, text-transform uppercase, letter-spacing 0.04em, color var(--color-text-muted), margin-bottom 6px
   - .field-input (scoped as .wiz-step-body .field-input): padding 10px 14px, font-size 0.9375rem, border-radius 8px
   - .field-input:focus (scoped): box-shadow 0 0 0 3px var(--color-primary-subtle)
   - Keep .field-error and .field-error-msg styles

5. **STEP NAVIGATION** — .step-nav: padding-top 1.5rem, margin-top 1.5rem, border-top 1px solid var(--color-border-subtle), gap 10px, justify-content flex-end

6. **TIME INPUT** — Keep existing time-input-wrap, time-input-hh/mm, time-sep styles but increase padding to 10px 8px.

7. **MEMBER FORM and LIST** — New styles for inline form:
   - .wiz-toggle-row: padding 1rem 0, border-bottom 1px solid var(--color-border-subtle), margin-bottom 1rem
   - .toggle-label: display flex, align-items center, gap 0.75rem, font-size 0.875rem, font-weight 500, cursor pointer
   - .toggle-label input[type="checkbox"]: width 2.5rem, height 1.375rem, appearance none, background var(--color-border), border-radius 0.75rem, position relative, cursor pointer, transition background 0.2s ease
   - .toggle-label input[type="checkbox"]::after: content '', width 1rem, height 1rem, border-radius 50%, background white, position absolute, top 3px, left 3px, transition transform 0.2s ease
   - .toggle-label input[type="checkbox"]:checked: background var(--color-primary)
   - .toggle-label input[type="checkbox"]:checked::after: transform translateX(1.125rem)
   - .wiz-member-add-form: padding 1rem, background var(--color-bg-subtle), border-radius 8px, margin-bottom 1rem
   - .members-action-row: gap 8px, margin-bottom 1rem
   - .member-row: display flex, align-items center, gap 12px, padding 8px 12px, border-bottom 1px solid var(--color-border-subtle)
   - .member-row:last-child: border-bottom none
   - .member-name: flex 1, font-size 0.875rem, font-weight 500
   - .member-votes: font-size 0.8125rem, color var(--color-text-muted), font-weight 600, min-width 60px
   - .members-total-row: padding 0.75rem 0, font-size 0.875rem, font-weight 600, border-top 1px solid var(--color-border-subtle), margin-top 0.5rem

8. **TEMPLATE BUTTONS** — .wiz-templates-row: display flex, align-items center, gap 8px, flex-wrap wrap, padding 1rem 0, margin-bottom 1rem, border-bottom 1px solid var(--color-border-subtle). .wiz-templates-label: font-size 0.75rem, font-weight 700, text-transform uppercase, letter-spacing 0.04em. .wiz-template-btn:hover: background var(--color-primary-subtle), color var(--color-primary)

9. **RESOLUTION ADD PANEL** — .reso-add-panel: background var(--color-bg-subtle), border-radius 10px, padding 1.25rem 1.5rem, margin-bottom 1rem, border 1px solid var(--color-border-subtle)

10. **RESOLUTION LIST** — .reso-row: padding 1rem 0, border-bottom 1px solid var(--color-border-subtle). .reso-num: font-family var(--font-mono). .reso-title: font-weight 600, font-size 0.9375rem

11. **REVIEW CARD** — .review-section: padding 1.25rem 0, border-bottom 1px solid var(--color-border-subtle). .review-section-header: display flex, justify-content space-between. .review-section-title: 0.6875rem uppercase 800. .review-modifier: color var(--color-primary). .review-row: display flex, justify-content space-between, padding 6px 0. .review-warning: padding 10px 14px, background color-mix(in srgb, var(--color-warning) 8%, transparent), border-left 3px solid var(--color-warning)

12. **CHIPS** — .chip: padding 6px 14px, border-radius 6px, border 1px solid var(--color-border). .chip.active: background var(--color-primary), color white

13. **UPLOAD ZONE** — .upload-zone: border 2px dashed var(--color-border), border-radius 10px, padding 2rem

14. **ALERT and CONTEXT PANEL** — .ctx-panel: background var(--color-bg-subtle), border-radius 8px. .wiz-step .alert: border-radius 8px

15. **RESPONSIVE** — @media (max-width: 768px): .wiz-step-body padding 1.25rem 1rem, .wiz-step-item span display none
  </action>
  <verify>
    <automated>grep -c "review-section" public/assets/css/wizard.css && grep -c "wiz-templates-row" public/assets/css/wizard.css && grep -c "wiz-toggle-row" public/assets/css/wizard.css && grep -c "wizFadeIn" public/assets/css/wizard.css && grep -c "review-warning" public/assets/css/wizard.css && grep -c "wiz-member-add-form" public/assets/css/wizard.css && grep -c "font-display" public/assets/css/wizard.css</automated>
  </verify>
  <acceptance_criteria>
    - grep "review-section" public/assets/css/wizard.css returns at least 3 matches
    - grep "wiz-templates-row" public/assets/css/wizard.css returns at least 1 match
    - grep "wiz-toggle-row" public/assets/css/wizard.css returns at least 1 match
    - grep "wizFadeIn" public/assets/css/wizard.css returns at least 2 matches (keyframes + usage)
    - grep "review-warning" public/assets/css/wizard.css returns at least 1 match
    - grep "review-modifier" public/assets/css/wizard.css returns at least 1 match
    - grep "wiz-member-add-form" public/assets/css/wizard.css returns at least 1 match
    - grep "font-display" public/assets/css/wizard.css returns at least 1 match (Fraunces for step titles)
    - grep "2rem 2.5rem" public/assets/css/wizard.css returns at least 1 match (generous padding)
  </acceptance_criteria>
  <done>Wizard CSS fully rewritten in Notion-like aesthetic with all new component styles for review card, templates, toggle, inline form, and fade animation.</done>
</task>

<task type="auto">
  <name>Task 2: Hub CSS rewrite — Notion-like aesthetic with blocked reason, quorum, motions, convocation styles</name>
  <read_first>
    - public/assets/css/hub.css
    - public/assets/css/design-system.css
    - public/hub.htmx.html
    - .planning/phases/28-wizard-session-hub-ux-overhaul/28-02-SUMMARY.md
    - .planning/phases/28-wizard-session-hub-ux-overhaul/28-RESEARCH.md
  </read_first>
  <files>public/assets/css/hub.css</files>
  <action>
Complete rewrite of hub.css. Keep the same file comment header pattern. ALL selectors MUST be scoped under hub-specific classes (.hub-*) — NEVER redefine global component classes (.btn, .card) which are in design-system.css.

**Same Notion-like design principles as wizard.css:**
- Generous padding and spacing
- Typography hierarchy with uppercase small labels
- Subtle shadows and clean borders
- var(--font-display) for main headings

**Sections to include (in this order):**

1. **IDENTITY BANNER** — .hub-identity: padding 1.25rem 1.5rem, border-radius 12px, box-shadow 0 1px 4px rgba(0,0,0,.04). .hub-identity-date: font-family var(--font-display), font-size 1.25rem, font-weight 700

2. **HORIZONTAL STATUS BAR** — .hub-status-bar: margin-bottom 1.5rem, gap 4px. .hub-bar-segment: height 6px, transition all 0.3s ease

3. **TWO-COLUMN LAYOUT** — .hub-layout-body: gap 2rem. .hub-stepper-col: width 240px. .hub-stepper-label: 0.6875rem uppercase 800 letter-spacing 0.08em

4. **VERTICAL STEPPER** — .hub-stepper: padding 1.25rem, border-radius 12px, box-shadow 0 1px 4px rgba(0,0,0,.04). .hub-step-num: 30px x 30px. .hub-step-num.done: var(--color-success). .hub-step-num.active: var(--color-primary) with glow ring

5. **ACTION CARD** — .hub-action: padding 1.5rem, border-radius 12px, box-shadow 0 1px 4px rgba(0,0,0,.04). .hub-action-title: font-family var(--font-display), font-size 1.125rem

6. **CHECKLIST** — .hub-checklist: padding 1.25rem, border-radius 12px, box-shadow. .hub-check-item: gap 10px, padding 8px 0. .hub-check-icon: 20px circle. **NEW: .hub-check-blocked**: font-size 0.75rem, color var(--color-warning), font-style italic, font-weight 400

7. **QUORUM SECTION** — .hub-quorum-section: margin-bottom 1.25rem, padding 1.25rem, border-radius 12px, box-shadow 0 1px 4px rgba(0,0,0,.04), border 1px solid var(--color-border), background var(--color-surface). ag-quorum-bar: display block

8. **MOTIONS LIST** — .hub-motions-section: same card pattern as quorum. .hub-motions-title: 0.6875rem uppercase 800. .hub-motion-item: display flex, gap 10px. .hub-motion-num: font-family var(--font-mono), 0.75rem. .hub-motion-title: flex 1, ellipsis overflow

9. **CONVOCATION SECTION** — .hub-convocation-section: margin-bottom 1.25rem, text-align center

10. **DETAILS TOGGLE** — .hub-details-toggle: padding 0.75rem 1rem, border-radius 8px, border 1px solid var(--color-border), width 100%, flex between

11. **KPI GRID** — .hub-kpi-card: padding 1rem, border-radius 10px. .hub-kpi-value-num: font-family var(--font-display), font-size 1.5rem

12. **DOCUMENTS PANEL** — .hub-documents: padding 1rem, border-radius 10px, box-shadow

13. **WARN CARD** — .hub-warn-card: padding 1.25rem, border-radius 10px, background color-mix(in srgb, var(--color-warning) 6%, transparent)

14. **ERROR STATE** — .hub-error: padding 1.5rem, border-radius 10px

15. **FOOTER** — .hub-footer-*: minimal spacing

16. **RESPONSIVE** — @media (max-width: 768px): .hub-layout-body flex-direction column, .hub-stepper-col width 100%
  </action>
  <verify>
    <automated>grep -c "hub-check-blocked" public/assets/css/hub.css && grep -c "hub-quorum-section" public/assets/css/hub.css && grep -c "hub-motions-section" public/assets/css/hub.css && grep -c "hub-motion-item" public/assets/css/hub.css && grep -c "hub-convocation-section" public/assets/css/hub.css && grep -c "font-display" public/assets/css/hub.css && grep -c "0 1px 4px" public/assets/css/hub.css</automated>
  </verify>
  <acceptance_criteria>
    - grep "hub-check-blocked" public/assets/css/hub.css returns at least 1 match
    - grep "hub-quorum-section" public/assets/css/hub.css returns at least 1 match
    - grep "hub-motions-section" public/assets/css/hub.css returns at least 1 match
    - grep "hub-motion-item" public/assets/css/hub.css returns at least 1 match
    - grep "hub-convocation-section" public/assets/css/hub.css returns at least 1 match
    - grep "font-display" public/assets/css/hub.css returns at least 1 match
    - grep "0 1px 4px" public/assets/css/hub.css returns at least 3 matches (Notion-like subtle shadows)
    - grep "font-style: italic" public/assets/css/hub.css returns at least 1 match (blocked reason)
  </acceptance_criteria>
  <done>Hub CSS fully rewritten in Notion-like aesthetic with blocked reason, quorum, motions, convocation, and all existing component styles refreshed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — Notion-like aesthetic on wizard and hub</name>
  <files>public/wizard.htmx.html, public/hub.htmx.html</files>
  <action>
Human verifies the complete visual and functional overhaul of wizard and hub pages.

What was built: Notion-like CSS rewrite, named stepper, review card with Modifier links, motion templates, progressive disclosure toggle, hub checklist with blocked reasons, quorum bar, motions doc badges, convocation button.

Steps to verify:
1. Open /wizard.htmx.html — verify stepper shows "Informations / Membres / Resolutions / Revision"
2. Navigate through all 4 steps without adding members or resolutions — verify no blocking errors
3. On step 2, toggle "Activer la ponderation des voix" — verify weight column appears
4. On step 2, use the inline form to add a member (not window.prompt)
5. On step 3, click a template button — verify title and description pre-fill
6. Navigate to step 4 — verify sectioned review card with Modifier links per section
7. Click a Modifier link — verify it navigates back to the correct step
8. Verify the overall aesthetic is Notion-like: airy spacing, clean typography, subtle shadows
9. Open /hub.htmx.html?id=existing_session_id — verify checklist shows blocked reasons where applicable
10. Verify ag-quorum-bar renders (even if current=0)
11. Verify motions list shows document badges
12. Verify "Envoyer les convocations" button is present when applicable
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User confirms visual quality and functional correctness of both pages.</done>
</task>

</tasks>

<verification>
1. Both wizard.css and hub.css are complete rewrites with Notion-like aesthetic
2. All new CSS classes from Plan 01 and Plan 02 JS output are styled
3. No global CSS class overrides (.btn, .card, .field-input defined at root level)
4. All selectors scoped under page-specific prefixes
5. Visual checkpoint confirms the aesthetic transformation
</verification>

<success_criteria>
- Wizard and hub pages have consistent Notion-like aesthetic
- All new HTML elements from Plans 01 and 02 are properly styled
- No CSS bleeding into other pages
- Step fade animation works (150ms)
- Blocked reason text is visually distinct (muted italic amber)
- Review card sections are clearly separated
- Responsive layout works on mobile (768px breakpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/28-wizard-session-hub-ux-overhaul/28-03-SUMMARY.md`
</output>
