---
phase: 43-dashboard-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/dashboard.htmx.html
  - public/assets/css/pages.css
autonomous: true
requirements: [REB-01]

must_haves:
  truths:
    - "Dashboard HTML is a complete ground-up rewrite with horizontal-first layout"
    - "KPI row displays 4 cards filling width with icon, large mono value, uppercase label"
    - "Session list area has skeleton placeholders replaced by JS on load"
    - "Quick actions aside is 280px sticky sidebar with 3 shortcut cards"
    - "Three-depth background model is maintained (bg > surface > raised)"
    - "All 10 JS-anchored IDs are present in the new HTML"
    - "Urgent banner starts hidden and JS reveals it when live session exists"
    - "Responsive breakpoints work: 1024px aside stacks, 768px KPIs go 2-col"
  artifacts:
    - path: "public/dashboard.htmx.html"
      provides: "Complete rebuilt dashboard page"
      contains: "kpiSeances"
    - path: "public/assets/css/pages.css"
      provides: "Rewritten dashboard CSS section (lines 935-1345)"
      contains: "dashboard-content"
  key_links:
    - from: "public/dashboard.htmx.html"
      to: "public/assets/js/pages/dashboard.js"
      via: "10 element IDs: kpiSeances, kpiEnCours, kpiConvoc, kpiPV, urgentTitle, urgentSub, actionUrgente, prochaines, taches, main-content"
      pattern: "id=\"kpi"
    - from: "public/dashboard.htmx.html"
      to: "public/assets/css/design-system.css"
      via: "session-card classes preserved for JS-rendered cards"
      pattern: "session-card"
---

<objective>
Rewrite the dashboard HTML from scratch and rewrite all dashboard CSS in pages.css. The result is a horizontal-first, data-dense, Stripe-quality dashboard that preserves all 10 JS-anchored element IDs and all session-card CSS classes from design-system.css.

Purpose: REB-01 requires a complete ground-up rebuild of the dashboard page to achieve top 1% design quality.
Output: New dashboard.htmx.html and rewritten dashboard section of pages.css.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-dashboard-rebuild/43-CONTEXT.md
@.planning/phases/43-dashboard-rebuild/43-RESEARCH.md
@public/assets/js/pages/dashboard.js
@public/assets/css/design-system.css (lines 5026-5139 — session-card styles, DO NOT modify)

<interfaces>
<!-- 10 NON-NEGOTIABLE element IDs that dashboard.js queries via getElementById -->
<!-- If ANY of these are missing, the JS silently fails (null guard) and data never populates -->

kpiSeances    — JS sets .textContent = upcoming sessions count
kpiEnCours    — JS sets .textContent = live/paused sessions count
kpiConvoc     — JS sets .textContent = 0 (convocation data not in API)
kpiPV         — JS sets .textContent = closed+validated sessions count
urgentTitle   — JS sets .textContent = 'Seance en cours'
urgentSub     — JS sets .textContent = live meeting title
actionUrgente — JS sets .hidden = true when no live meeting
prochaines    — JS sets .innerHTML = session cards or ag-empty-state
taches        — JS sets .innerHTML = ag-empty-state (tasks not in API, null-guarded, CAN be removed)
main-content  — JS prepends error banner div with .dashboard-error class

<!-- Additional JS selector: .dashboard-error (querySelector for dedup) -->
<!-- Additional JS-created ID: dashboardRetryBtn (addEventListener for retry) -->

<!-- Session-card classes emitted by renderSessionCard() — defined in design-system.css, NOT pages.css -->
<!-- DO NOT redefine these in pages.css — they already exist in design-system.css lines 5026-5139 -->
session-card, session-card--live, session-card--muted
session-card-info, session-card-title, session-card-meta
session-card-date, session-card-meta-sep, session-card-cta
pulse-dot

<!-- Web components available (do not hand-roll equivalents) -->
<ag-tooltip text="..." position="bottom|left">
<ag-badge variant="live|draft|primary|success|warning|danger|info" pulse>
<ag-empty-state icon="meetings" title="..." description="..." action-label="..." action-href="...">
<ag-popover trigger="click" position="bottom" width="320">

<!-- Design tokens (from design-system.css) -->
--color-bg: #EDECE6          (page background — depth 1)
--color-surface: #FAFAF7     (card surface — depth 2)
--color-surface-raised: #FFF (KPI cards — depth 3)
--font-mono: JetBrains Mono  (KPI values, dates)
--text-4xl: 2.25rem          (KPI large numbers)
--space-card: var(--space-6) = 24px
--radius-xl: 12px            (card border radius)
--shadow-lg                   (KPI card hover shadow)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite dashboard.htmx.html from scratch</name>
  <files>public/dashboard.htmx.html</files>
  <read_first>
    - public/assets/js/pages/dashboard.js (understand ALL getElementById/querySelector calls)
    - public/dashboard.htmx.html (current structure to understand head/scripts/footer to preserve)
    - public/assets/css/design-system.css lines 5026-5139 (session-card classes to NOT duplicate)
  </read_first>
  <action>
Rewrite the entire file from scratch. Preserve the head section (critical-tokens, fonts, meta, theme-init.js), the script block at bottom, and the footer. Rewrite everything inside the app-shell between header and footer.

The new HTML structure MUST be exactly:

```html
<!doctype html>
<html lang="fr" data-page-role="viewer">
<head>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <meta charset="utf-8">
  <style id="critical-tokens">
    :root { --color-bg: #EDECE6; --color-surface: #FAFAF7; --color-text: #151510; }
    [data-theme="dark"] { --color-bg: #0B0D10; --color-surface: #141820; --color-text: #ECF0FA; }
    html, body { background: var(--color-bg); }
  </style>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="theme-color" content="#1650E0" media="(prefers-color-scheme: light)">
  <meta name="theme-color" content="#0B0F1A" media="(prefers-color-scheme: dark)">
  <meta name="description" content="Tableau de bord - AG-VOTE">
  <title>Tableau de bord &mdash; AG-VOTE</title>
  <script src="/assets/js/theme-init.js"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,300;12..96,400;12..96,500;12..96,600;12..96,700;12..96,800&family=Fraunces:opsz,wght@9..144,600;9..144,700;9..144,800&family=JetBrains+Mono:wght@400;500;600;700&display=swap">
  <link rel="stylesheet" href="/assets/css/app.css">
</head>
<body>
  <!-- Skip links -->
  <a href="#main-content" class="skip-link">Aller au contenu principal</a>
  <a href="#main-nav" class="skip-link">Aller &agrave; la navigation</a>

  <div class="app-shell">
    <aside class="app-sidebar" data-include-sidebar data-page="dashboard"></aside>

    <!-- Header -->
    <header class="app-header" role="banner">
      <div class="flex items-center gap-4">
        <div>
          <nav class="breadcrumb" aria-label="Fil d'Ariane">
            <span class="bc-item bc-current" aria-current="page">Tableau de bord</span>
          </nav>
          <h1 class="page-title">
            <span class="bar"></span>
            <svg class="icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
            Tableau de bord
          </h1>
          <p class="page-sub">Vue d'ensemble de vos assembl&eacute;es</p>
        </div>
      </div>
      <div class="flex items-center gap-2">
        <ag-popover trigger="click" position="bottom" width="320">
          <button slot="trigger" class="tour-trigger-btn" aria-label="Aide sur cette page">
            <svg class="icon icon-xs" aria-hidden="true"><use href="/assets/icons.svg#icon-help-circle"></use></svg>
            Aide
          </button>
          <div slot="content">
            <strong>Tableau de bord &mdash; Comment &ccedil;a marche</strong>
            <ul class="onboarding-tips">
              <li>Cliquez sur une s&eacute;ance pour ouvrir sa fiche</li>
              <li>Le bouton d'action change selon l'&eacute;tat de la s&eacute;ance</li>
              <li>Cr&eacute;ez une nouvelle s&eacute;ance avec le bouton en haut &agrave; droite</li>
            </ul>
          </div>
        </ag-popover>
        <a class="btn btn-primary btn-sm" href="/wizard.htmx.html">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="10"/><path d="M8 12h8"/><path d="M12 8v8"/></svg>
          Nouvelle s&eacute;ance
        </a>
      </div>
    </header>

    <main class="app-main" id="main-content" role="main">
      <div class="dashboard-content">

        <!-- Urgent Banner — hidden by default, JS reveals when live session exists -->
        <a href="/hub.htmx.html" class="dashboard-urgent" id="actionUrgente" hidden>
          <div class="dashboard-urgent__icon">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14.536 21.686a.5.5 0 0 0 .937-.024l6.5-19a.496.496 0 0 0-.635-.635l-19 6.5a.5.5 0 0 0-.024.937l7.93 3.18a2 2 0 0 1 1.112 1.11z"/><path d="m21.854 2.147-10.94 10.939"/></svg>
          </div>
          <div class="dashboard-urgent__body">
            <div class="dashboard-urgent__eyebrow">S&eacute;ance en direct</div>
            <div class="dashboard-urgent__title" id="urgentTitle">Chargement&hellip;</div>
            <div class="dashboard-urgent__sub" id="urgentSub"></div>
          </div>
          <div class="dashboard-urgent__arrow">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
          </div>
        </a>

        <!-- KPI Row — 4 cards filling width -->
        <div class="dashboard-kpis">
          <ag-tooltip text="Sessions AG planifi&eacute;es ou en pr&eacute;paration" position="bottom">
            <a href="/meetings.htmx.html" class="kpi-card kpi-card--1" aria-label="AG &agrave; venir">
              <div class="kpi-icon">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M8 2v4"/><path d="M16 2v4"/><rect width="18" height="18" x="3" y="4" rx="2"/><path d="M3 10h18"/></svg>
              </div>
              <div class="kpi-value" id="kpiSeances">-</div>
              <div class="kpi-label">AG &agrave; venir</div>
            </a>
          </ag-tooltip>
          <ag-tooltip text="Sessions en cours de vote en direct" position="bottom">
            <a href="/operator.htmx.html" class="kpi-card kpi-card--2" aria-label="En cours">
              <div class="kpi-icon">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polygon points="6 3 20 12 6 21 6 3"/></svg>
              </div>
              <div class="kpi-value" id="kpiEnCours">-</div>
              <div class="kpi-label">En cours</div>
            </a>
          </ag-tooltip>
          <ag-tooltip text="Convocations en attente d'envoi" position="bottom">
            <a href="/hub.htmx.html" class="kpi-card kpi-card--3" aria-label="Convocations en attente">
              <div class="kpi-icon">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14.536 21.686a.5.5 0 0 0 .937-.024l6.5-19a.496.496 0 0 0-.635-.635l-19 6.5a.5.5 0 0 0-.024.937l7.93 3.18a2 2 0 0 1 1.112 1.11z"/><path d="m21.854 2.147-10.94 10.939"/></svg>
              </div>
              <div class="kpi-value" id="kpiConvoc">-</div>
              <div class="kpi-label">Convoc. en attente</div>
            </a>
          </ag-tooltip>
          <ag-tooltip text="Proc&egrave;s-verbaux &agrave; finaliser et envoyer" position="bottom">
            <a href="/postsession.htmx.html" class="kpi-card kpi-card--4" aria-label="PV &agrave; envoyer">
              <div class="kpi-icon">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z"/><path d="M14 2v4a2 2 0 0 0 2 2h4"/><path d="M10 9H8"/><path d="M16 13H8"/><path d="M16 17H8"/></svg>
              </div>
              <div class="kpi-value" id="kpiPV">-</div>
              <div class="kpi-label">PV &agrave; envoyer</div>
            </a>
          </ag-tooltip>
        </div>

        <!-- Body: Sessions + Aside -->
        <div class="dashboard-body">

          <!-- Sessions list (primary content) -->
          <section class="dashboard-sessions">
            <div class="dashboard-panel-header">
              <h2 class="dashboard-panel-title">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M8 2v4"/><path d="M16 2v4"/><rect width="18" height="18" x="3" y="4" rx="2"/><path d="M3 10h18"/></svg>
                S&eacute;ances
              </h2>
              <a class="btn btn-sm btn-ghost" href="/meetings.htmx.html">
                Tout voir
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m9 18 6-6-6-6"/></svg>
              </a>
            </div>
            <div id="prochaines" class="dashboard-sessions__list" aria-live="polite">
              <!-- Skeleton placeholders — JS replaces with session cards or empty state -->
              <div class="skeleton skeleton-session"></div>
              <div class="skeleton skeleton-session"></div>
              <div class="skeleton skeleton-session"></div>
            </div>
          </section>

          <!-- Aside: Quick actions -->
          <aside class="dashboard-aside">
            <h2 class="dashboard-aside__title">Acc&egrave;s rapides</h2>
            <ag-tooltip text="Cr&eacute;er une nouvelle session en 4 &eacute;tapes" position="left">
              <a href="/wizard.htmx.html" class="shortcut-card">
                <div class="shortcut-card-icon accent">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M8 12h8"/><path d="M12 8v8"/></svg>
                </div>
                <div class="shortcut-card-text">
                  <div class="shortcut-card-title">Cr&eacute;er une s&eacute;ance</div>
                  <div class="shortcut-card-sub">Assistant en 4 &eacute;tapes</div>
                </div>
              </a>
            </ag-tooltip>
            <ag-tooltip text="Acc&eacute;der &agrave; la console op&eacute;rateur pour piloter un vote en direct" position="left">
              <a href="/operator.htmx.html" class="shortcut-card">
                <div class="shortcut-card-icon danger">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="6 3 20 12 6 21 6 3"/></svg>
                </div>
                <div class="shortcut-card-text">
                  <div class="shortcut-card-title">Piloter un vote</div>
                  <div class="shortcut-card-sub">S&eacute;ance en direct</div>
                </div>
              </a>
            </ag-tooltip>
            <ag-tooltip text="Voir le journal de tra&ccedil;abilit&eacute; et l'historique des actions" position="left">
              <a href="/trust.htmx.html" class="shortcut-card">
                <div class="shortcut-card-icon muted">
                  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 13c0 5-3.5 7.5-7.66 8.95a1 1 0 0 1-.67-.01C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.24-2.72a1.17 1.17 0 0 1 1.52 0C14.51 3.81 17 5 19 5a1 1 0 0 1 1 1z"/></svg>
                </div>
                <div class="shortcut-card-text">
                  <div class="shortcut-card-title">Consulter le suivi</div>
                  <div class="shortcut-card-sub">Journal de tra&ccedil;abilit&eacute;</div>
                </div>
              </a>
            </ag-tooltip>
          </aside>

        </div><!-- .dashboard-body -->

      </div><!-- .dashboard-content -->
    </main>

    <footer class="app-footer mobile-footer" role="contentinfo">
      <a href="/dashboard.htmx.html" class="logo" aria-label="AG-VOTE">
        <span class="logo-mark">
          <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="2.5"><path d="M9 3H5a2 2 0 0 0-2 2v4m6-6h10a2 2 0 0 1 2 2v4M9 3v18m0 0h10a2 2 0 0 0 2-2V9M9 21H5a2 2 0 0 1-2-2V9m0 0h18"/></svg>
        </span>
        AG-VOTE
      </a>
      <span class="text-muted">v3.19</span>
      <span class="mobile-footer-spacer"></span>
      <a href="/help.htmx.html" class="text-muted mobile-footer-link">Aide</a>
      <a href="/settings.htmx.html#accessibilite" class="text-muted mobile-footer-link">Accessibilit&eacute;</a>
    </footer>
  </div>

  <script src="/assets/js/core/utils.js"></script>
  <script src="/assets/js/core/shared.js"></script>
  <script src="/assets/js/core/shell.js"></script>
  <script src="/assets/js/services/meeting-context.js"></script>
  <script src="/assets/js/components/ag-empty-state.js"></script>
  <script type="module" src="/assets/js/components/ag-popover.js"></script>
  <script src="/assets/js/pages/dashboard.js"></script>
</body>
</html>
```

KEY CHANGES from current HTML:
1. Urgent banner: `hidden` attribute by default (avoids flash). Classes renamed to BEM: `dashboard-urgent`, `dashboard-urgent__icon`, `dashboard-urgent__body`, `dashboard-urgent__eyebrow`, `dashboard-urgent__title`, `dashboard-urgent__sub`, `dashboard-urgent__arrow`.
2. KPI grid: class changed from `kpi-grid` to `dashboard-kpis` (cleaner namespace). Removed `primary`, `danger`, `warning` classes from kpi-value divs (colors come from parent card variant via CSS).
3. Sessions panel: replaced `<main class="dashboard-main">` with `<section class="dashboard-sessions">`. Panel header uses `dashboard-panel-header` and `dashboard-panel-title` (h2). Removed the tasks panel (`#taches`) entirely — JS null-guards it.
4. Aside: `dashboard-aside__title` instead of `dashboard-shortcuts-title`. Removed `.card` class from shortcut cards (they have their own styling).
5. All 9 required IDs preserved: `actionUrgente`, `urgentTitle`, `urgentSub`, `kpiSeances`, `kpiEnCours`, `kpiConvoc`, `kpiPV`, `prochaines`, `main-content`. The `taches` ID is intentionally removed (JS null-guards it).

CRITICAL: The JS currently hides `actionUrgente` when no live meeting (`urgentCard.hidden = true`). The new HTML starts with `hidden` attribute. When a live meeting IS found, JS sets `urgentTitle.textContent` and `urgentSub.textContent` but does NOT remove `hidden`. This means we need a small JS update to set `actionUrgente.hidden = false` when a live meeting exists. This JS fix is handled in Plan 02 Task 1.
  </action>
  <verify>
    <automated>grep -c 'id="kpiSeances"\|id="kpiEnCours"\|id="kpiConvoc"\|id="kpiPV"\|id="urgentTitle"\|id="urgentSub"\|id="actionUrgente"\|id="prochaines"\|id="main-content"' public/dashboard.htmx.html | grep -q '^9$' && echo "All 9 IDs present" || echo "MISSING IDs"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 required element IDs present in new HTML (kpiSeances, kpiEnCours, kpiConvoc, kpiPV, urgentTitle, urgentSub, actionUrgente, prochaines, main-content)
    - #taches ID removed (JS null-guards it)
    - Urgent banner has `hidden` attribute by default
    - KPI grid has 4 ag-tooltip-wrapped cards
    - Sessions panel has skeleton placeholders
    - Aside has 3 shortcut cards wrapped in ag-tooltip
    - Head section (critical-tokens, fonts, meta) preserved exactly
    - Script block at bottom preserved exactly
    - Footer preserved exactly
    - No HTMX attributes anywhere (page is pure JS)
  </acceptance_criteria>
  <done>dashboard.htmx.html is a complete ground-up rewrite with all 9 JS-anchored IDs, horizontal-first layout, hidden urgent banner, KPI row, session list with skeletons, and sticky aside</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite dashboard CSS section in pages.css</name>
  <files>public/assets/css/pages.css</files>
  <read_first>
    - public/assets/css/pages.css (lines 935-1345 — current dashboard CSS to be replaced)
    - public/assets/css/design-system.css (lines 5026-5139 — session-card styles, DO NOT duplicate)
    - The new dashboard.htmx.html (from Task 1) to know which classes need CSS rules
  </read_first>
  <action>
Replace lines 935-1345 of pages.css (the entire dashboard section: urgent card, grid/panel layout, KPI cards, shortcut cards, session/task rows, responsive grids) with the following new CSS. Keep everything before line 935 and after line 1345 untouched.

The new CSS section to write (replacing lines 935-1345):

```css
/* =============================================================================
   DASHBOARD — Urgent Banner
   ============================================================================= */

.dashboard-urgent {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 14px 20px;
  margin-bottom: var(--space-card, 24px);
  background: color-mix(in srgb, var(--color-danger) 6%, var(--color-surface));
  border: 2px solid var(--color-danger);
  border-radius: var(--radius-xl);
  text-decoration: none;
  color: inherit;
  cursor: pointer;
  transition: box-shadow 150ms, transform 150ms;
}
.dashboard-urgent:hover {
  box-shadow: var(--shadow-md);
  transform: translateY(-1px);
}
.dashboard-urgent__icon {
  width: 48px;
  height: 48px;
  border-radius: 12px;
  background: var(--color-danger);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.dashboard-urgent__body {
  flex: 1;
  min-width: 0;
}
.dashboard-urgent__eyebrow {
  font-size: 11px;
  font-weight: 700;
  color: var(--color-danger);
  text-transform: uppercase;
  letter-spacing: 1px;
}
.dashboard-urgent__title {
  font-size: var(--text-md);
  font-weight: 800;
  color: var(--color-text);
  margin: 2px 0;
}
.dashboard-urgent__sub {
  font-size: 13px;
  color: var(--color-text-muted);
}
.dashboard-urgent__arrow {
  flex-shrink: 0;
  color: var(--color-danger);
}

/* =============================================================================
   DASHBOARD — Page subtitle
   ============================================================================= */

.app-header .page-sub {
  color: var(--color-text-muted);
  font-size: var(--text-sm);
}

/* =============================================================================
   DASHBOARD — Layout
   ============================================================================= */

.dashboard-content {
  max-width: 1200px;
  margin: 0 auto;
  padding: var(--space-4) 0;
}

/* KPI grid: 4-column */
.dashboard-kpis {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: var(--space-card, 24px);
  margin-bottom: var(--space-card, 24px);
}
.dashboard-kpis ag-tooltip {
  display: contents;
}

/* Body: sessions + aside */
.dashboard-body {
  display: grid;
  grid-template-columns: 1fr 280px;
  gap: var(--space-card, 24px);
  align-items: start;
}

/* =============================================================================
   DASHBOARD — KPI Cards
   ============================================================================= */

.kpi-card {
  text-align: left;
  padding: var(--space-6);
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
  background: var(--color-surface-raised);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  transition: var(--transition-shadow), transform var(--duration-moderate) var(--ease-standard);
  text-decoration: none;
  color: inherit;
}
.kpi-card:hover {
  transform: translateY(-3px);
  box-shadow: var(--shadow-lg);
  border-color: var(--color-border-strong);
}
.kpi-card .kpi-icon {
  width: 36px;
  height: 36px;
  border-radius: var(--radius-lg);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.kpi-card--1 .kpi-icon { background: var(--color-primary-subtle); color: var(--color-primary); }
.kpi-card--2 .kpi-icon { background: var(--color-danger-subtle); color: var(--color-danger); }
.kpi-card--3 .kpi-icon { background: var(--color-warning-subtle); color: var(--color-warning); }
.kpi-card--4 .kpi-icon { background: var(--color-accent-subtle, var(--color-primary-subtle)); color: var(--color-accent, var(--color-primary)); }

.kpi-card .kpi-value {
  font-family: var(--font-mono);
  font-size: var(--text-4xl);
  font-weight: 700;
  line-height: 1;
  letter-spacing: var(--tracking-tight);
  font-variant-numeric: tabular-nums;
  color: var(--color-text);
}
.kpi-card .kpi-label {
  font-size: var(--text-xs);
  color: var(--color-text-muted);
  font-weight: var(--font-medium);
  text-transform: uppercase;
  letter-spacing: var(--tracking-wider, 0.05em);
}

/* =============================================================================
   DASHBOARD — Sessions Panel
   ============================================================================= */

.dashboard-sessions {
  background: var(--color-surface);
  border-radius: var(--radius-xl);
  border: 1px solid var(--color-border-subtle);
  padding: var(--space-card, 24px);
}
.dashboard-panel-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--space-4, 16px);
  padding-bottom: var(--space-3, 12px);
  border-bottom: 1px solid var(--color-border-subtle);
}
.dashboard-panel-title {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: var(--text-base);
  font-weight: 700;
  color: var(--color-text);
  margin: 0;
}

/* Skeleton loading placeholders */
.skeleton-session {
  height: 60px;
  margin-bottom: 8px;
  border-radius: var(--radius-md, 8px);
}
.skeleton-session:last-child {
  margin-bottom: 0;
}

/* =============================================================================
   DASHBOARD — Aside (Quick Actions)
   ============================================================================= */

.dashboard-aside {
  position: sticky;
  top: 80px;
  background: var(--color-surface);
  border-radius: var(--radius-xl);
  border: 1px solid var(--color-border-subtle);
  padding: var(--space-card, 24px);
}
.dashboard-aside__title {
  font-size: var(--text-sm);
  font-weight: 700;
  color: var(--color-text-muted);
  text-transform: uppercase;
  letter-spacing: var(--tracking-wider, 0.05em);
  margin: 0 0 var(--space-3, 12px) 0;
}
.dashboard-aside ag-tooltip {
  display: block;
}

/* Shortcut cards */
.shortcut-card {
  padding: var(--space-3, 12px) var(--space-4, 16px);
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 12px;
  text-decoration: none;
  color: inherit;
  border-radius: var(--radius-lg);
  border: 1px solid transparent;
  transition: var(--transition-ui);
}
.shortcut-card:hover {
  background: var(--color-bg-subtle, var(--color-bg));
  border-color: var(--color-border);
}
.shortcut-card-icon {
  width: 40px;
  height: 40px;
  border-radius: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.shortcut-card-icon.accent { background: var(--color-primary); }
.shortcut-card-icon.danger { background: var(--color-danger); }
.shortcut-card-icon.muted  { background: var(--color-text-muted); }
.shortcut-card-text {
  min-width: 0;
}
.shortcut-card-title {
  font-weight: 700;
  color: var(--color-text);
  font-size: var(--text-base, 1rem);
}
.shortcut-card-sub {
  color: var(--color-text-muted);
  font-size: var(--text-sm, 0.875rem);
  margin-top: 2px;
}

/* =============================================================================
   DASHBOARD — Responsive
   ============================================================================= */

@media (max-width: 1024px) {
  .dashboard-body {
    grid-template-columns: 1fr;
  }
  .dashboard-aside {
    position: static;
  }
}
@media (max-width: 768px) {
  .dashboard-kpis {
    grid-template-columns: repeat(2, 1fr);
  }
}
```

IMPORTANT:
- Remove ALL legacy classes that are no longer used: `.session-row`, `.session-dot`, `.session-row-info`, `.session-row-title`, `.session-row-meta`, `.task-row`, `.task-row-info`, `.task-row-title`, `.task-row-sub`, `.skeleton-task`, `.grid-2`, `.grid-3`, `.kpi-grid`, `.dashboard-panel`, `.dashboard-main`, `.dashboard-shortcuts-title`, `.urgent-card`, `.urgent-card-body`, `.urgent-card-icon`, `.urgent-card-label`, `.urgent-card-title`, `.urgent-card-sub`, `.urgent-card-chevron`.
- DO NOT touch session-card styles (they live in design-system.css lines 5026-5139).
- DO NOT touch anything before line 935 or after line 1345 in pages.css.
- The `.dashboard-panel` class used by the old HTML is replaced by `.dashboard-sessions` with its own panel styles.
  </action>
  <verify>
    <automated>grep -c 'dashboard-kpis\|dashboard-urgent\|dashboard-sessions\|dashboard-aside\|shortcut-card\|kpi-card' public/assets/css/pages.css | grep -qv '^0$' && echo "New dashboard CSS present" || echo "MISSING new CSS"</automated>
  </verify>
  <acceptance_criteria>
    - Old dashboard CSS (lines 935-1345) completely replaced with new rules
    - New classes match the HTML from Task 1: dashboard-urgent, dashboard-kpis, dashboard-sessions, dashboard-aside, dashboard-panel-header, dashboard-panel-title, dashboard-aside__title
    - KPI cards use three-depth model (--color-surface-raised background)
    - ag-tooltip gets `display: contents` in dashboard-kpis grid
    - Responsive breakpoints: 1024px (body 1-col, aside static), 768px (KPIs 2-col)
    - No session-card styles duplicated (they stay in design-system.css)
    - No legacy classes remain (.session-row, .task-row, .urgent-card, etc.)
    - Shortcut cards have hover state with bg and border
    - Urgent banner styled as danger-bordered link with hover lift
  </acceptance_criteria>
  <done>pages.css dashboard section is a complete ground-up rewrite matching the new HTML structure, with three-depth backgrounds, responsive breakpoints, and zero legacy CSS</done>
</task>

</tasks>

<verification>
1. `grep -c 'id="kpiSeances"\|id="kpiEnCours"\|id="kpiConvoc"\|id="kpiPV"\|id="urgentTitle"\|id="urgentSub"\|id="actionUrgente"\|id="prochaines"\|id="main-content"' public/dashboard.htmx.html` returns 9
2. `grep -c 'dashboard-kpis\|dashboard-urgent\|dashboard-sessions' public/assets/css/pages.css` returns at least 3
3. `grep -c 'session-row\|task-row\|urgent-card-body\|kpi-grid' public/assets/css/pages.css` returns 0 (legacy removed)
4. `./vendor/bin/phpunit tests/Unit/DashboardControllerTest.php` passes (backend untouched)
</verification>

<success_criteria>
- Dashboard HTML is a complete rewrite with all 9 JS-anchored IDs
- Dashboard CSS is a complete rewrite with new class names matching new HTML
- Three-depth background model maintained
- Session-card styles in design-system.css untouched
- Responsive breakpoints functional at 1024px and 768px
- No legacy CSS classes remain in the dashboard section
</success_criteria>

<output>
After completion, create `.planning/phases/43-dashboard-rebuild/43-01-SUMMARY.md`
</output>
