---
phase: 47-hub-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/hub.htmx.html
  - public/assets/css/hub.css
autonomous: true
requirements: [REB-05]

must_haves:
  truths:
    - "Hub page renders a hero card with session title, type badge, status badge, date, place, participant count"
    - "Two-column layout below hero: vertical stepper checklist on left (280px), quorum bar + motions preview on right"
    - "Quorum bar section uses ag-quorum-bar web component with numeric display"
    - "Motions section shows count badge and first 3 motion titles with Voir tout link"
    - "Primary CTA (Ouvrir la seance) and secondary CTA (Aller a la console) are prominent in hero card"
    - "Dark mode works via tokens — no hardcoded colors except quorum bar internals"
    - "Responsive: stacks to single column at 768px"
  artifacts:
    - path: "public/hub.htmx.html"
      provides: "Complete hub page HTML with hero card, two-column layout, stepper checklist, quorum bar"
      contains: "hub-hero"
    - path: "public/assets/css/hub.css"
      provides: "All hub page styles — hero, two-column grid, stepper, quorum card, motions card, responsive"
      contains: "hub-hero"
  key_links:
    - from: "public/hub.htmx.html"
      to: "public/assets/js/pages/hub.js"
      via: "DOM IDs preserved for JS binding"
      pattern: "hubTitle|hubDate|hubPlace|hubParticipants|hubTypeTag|hubStatusTag|hubQuorumBar|hubQuorumPct|hubQuorumSection|hubMotionsList|hubMotionsSection|hubChecklist|hubMainBtn|hubConvocationSection|btnSendConvocations"
---

<objective>
Rewrite the hub page HTML from scratch and rewrite all hub CSS — hero header card, two-column body (280px checklist + fluid right), vertical stepper checklist, compact quorum bar card, motions preview card.

Purpose: Ground-up rebuild of session hub page layout and styles, following the same card-based pattern established in Phase 46 operator console rebuild.
Output: New hub.htmx.html and hub.css with complete visual structure ready for JS wiring in Plan 02.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-hub-rebuild/47-CONTEXT.md
@.planning/phases/47-hub-rebuild/47-RESEARCH.md

@public/hub.htmx.html
@public/assets/css/hub.css
@public/assets/js/pages/hub.js
@public/assets/css/app.css

<interfaces>
<!-- DOM IDs that hub.js targets — ALL must exist in new HTML (or be consciously remapped in Plan 02) -->

Identity section:
- #hubTitle — session title text
- #hubDate — date display
- #hubPlace — place display
- #hubParticipants — participant count
- #hubTypeTag — type badge (AG, AGE, etc.)
- #hubStatusTag — status badge (En preparation, En cours, etc.)

Status bar and stepper:
- #hubStatusBar — horizontal status bar container (to be REMOVED in new design)
- #hubStepper — vertical stepper container (to be REMOVED — replaced by checklist)

Action card:
- #hubAction — action card container
- #hubActionIcon — action icon wrapper
- #hubActionSvg — SVG inside icon
- #hubActionTitle — action title
- #hubActionDesc — action description
- #hubMainBtn — primary action button (KEEP as main CTA in hero)
- #hubPreviewBtn — email preview button

Checklist:
- #hubChecklist — checklist container (KEEP — new 3-item vertical stepper)

Quorum:
- #hubQuorumSection — quorum section wrapper
- #hubQuorumBar — ag-quorum-bar web component
- #hubQuorumPct — percentage display

Motions:
- #hubMotionsSection — motions section wrapper
- #hubMotionsList — motions list container

Convocation:
- #hubConvocationSection — convocation send section
- #btnSendConvocations — send button

Details:
- #hubDetailsToggle — details expand toggle (to be REMOVED — KPIs moved inline)
- #hub-details-body — details body (to be REMOVED)
- #hubKpiParticipants, #hubKpiVoix, #hubKpiResolutions, etc. — KPI values (REMOVE — simplified layout)
- #hubDocsList — documents list (REMOVE — not in new design scope)
- #btnAddDoc — add document button (REMOVE)
- #btn2eConvoc — 2e convocation button (REMOVE — per research recommendation)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite hub.htmx.html from scratch</name>
  <files>public/hub.htmx.html</files>
  <read_first>
    - public/hub.htmx.html (current HTML — understand all DOM IDs)
    - public/assets/js/pages/hub.js (ALL getElementById/querySelector calls — must preserve or consciously drop)
    - public/operator.htmx.html (reference for hero card + two-column pattern from Phase 46)
    - public/assets/css/app.css (design tokens, app-shell grid, badge classes)
  </read_first>
  <action>
Rewrite hub.htmx.html completely. Keep head section (critical-tokens, fonts, theme-init, app.css, hub.css), keep script tags at bottom, keep app-shell + sidebar + header + footer structure.

Replace ALL content inside `<main class="app-main">` with:

1. **Hero card** (class `hub-hero`):
   - Left: icon (clipboard SVG in gradient square, same pattern as old hub-identity-icon)
   - Center: title row with `#hubTitle` (h2), badges row with `#hubTypeTag` + `#hubStatusTag` (badge classes)
   - Below title: meta line with `#hubDate`, `#hubPlace`, `#hubParticipants` (icon + text pairs, middot separators)
   - Right: action buttons — `#hubMainBtn` (btn btn-primary, "Ouvrir la seance") and `#hubOperatorBtn` (btn btn-secondary, "Aller a la console", href="/operator.htmx.html")
   - Hero has accent top stripe via ::before (3px gradient)

2. **Two-column body** (class `hub-body`, CSS grid 280px 1fr):
   - **Left column** (class `hub-sidebar`):
     - Vertical stepper checklist card (class `hub-checklist-card`):
       - Header: "Prerequis" label + progress badge
       - Three checklist items, each with `data-check` attribute:
         - `data-check="convocation"`: "Convocations envoyees" — dot + label + badge + reason span
         - `data-check="quorum"`: "Quorum atteint" — dot + label + badge + reason span
         - `data-check="agenda"`: "Ordre du jour verrouille" — dot + label + badge + reason span
       - Each item: `hub-checklist-item` class with modifier `--done`, `--blocked`, `--pending`
       - Each dot: `hub-checklist-dot` with modifier matching state
       - Each has `hub-checklist-reason` span (hidden by default, shown when blocked)
     - Keep `#hubChecklist` as the container ID for JS compatibility (Plan 02 will update renderChecklist)
   - **Right column** (class `hub-main`):
     - **Quorum card** (class `hub-quorum-card`):
       - Header row: "Quorum" label + info popover + percentage `#hubQuorumPct`
       - `<ag-quorum-bar id="hubQuorumBar" current="0" required="0" total="0">` web component
       - Use `hidden` attribute (NOT `style="display:none"`) — JS removes hidden when data loads
       - Keep `#hubQuorumSection` as wrapper ID
     - **Motions card** (class `hub-motions-card`):
       - Header: "Resolutions" label + count badge
       - `#hubMotionsList` container for first 3 motions (populated by JS)
       - "Voir tout" link at bottom (href to operator with meeting_id, populated by JS)
       - Use `hidden` attribute — JS removes when motions exist
       - Keep `#hubMotionsSection` as wrapper ID
     - **Convocation send section** (class `hub-convoc-section`):
       - Keep `#hubConvocationSection` wrapper with `hidden` attribute
       - Keep `#btnSendConvocations` button

3. **REMOVE from new HTML** (not in new design):
   - `#hubStatusBar` — 6-segment horizontal progress bar (replaced by hero status badge)
   - `#hubStepper` — 6-step vertical stepper (replaced by 3-item checklist)
   - `#hubAction` / `#hubActionIcon` / `#hubActionSvg` / `#hubActionTitle` / `#hubActionDesc` — old action card (replaced by hero CTA)
   - `#hubPreviewBtn` — email preview (not in new design)
   - `#hubDetailsToggle` / `#hub-details-body` — collapsible details (KPIs removed)
   - All KPI cards (`#hubKpiParticipants`, `#hubKpiVoix`, etc.)
   - Documents section (`#hubDocsList`, `#btnAddDoc`)
   - 2e convocation section (`#btn2eConvoc`)
   - `.doc-badge` inline styles in head (not needed)

4. **Header updates**: Keep breadcrumb, keep Aide popover, keep Modifier + Seances buttons in header right area (consistent with operator rebuild pattern).

Use `hidden` attribute on sections that start hidden (hubQuorumSection, hubMotionsSection, hubConvocationSection). Do NOT use `style="display:none"`.
  </action>
  <verify>
    <automated>grep -c 'hub-hero\|hub-body\|hub-checklist-item\|hubQuorumBar\|hubMotionsList\|hubMainBtn\|hubOperatorBtn' public/hub.htmx.html | xargs test 7 -le</automated>
  </verify>
  <acceptance_criteria>
    - grep 'hub-hero' public/hub.htmx.html returns matches
    - grep 'hub-body' public/hub.htmx.html returns matches (two-column grid container)
    - grep 'hub-checklist-item' public/hub.htmx.html returns matches (3 checklist items)
    - grep 'hubQuorumBar' public/hub.htmx.html returns match (ag-quorum-bar component)
    - grep 'hubMotionsList' public/hub.htmx.html returns match
    - grep 'hubMainBtn' public/hub.htmx.html returns match (primary CTA in hero)
    - grep 'hubOperatorBtn' public/hub.htmx.html returns match (secondary CTA in hero)
    - grep 'hubTitle' public/hub.htmx.html returns match
    - grep 'hubStatusBar' public/hub.htmx.html returns NO match (removed)
    - grep 'hubStepper' public/hub.htmx.html returns NO match (removed)
    - grep 'hubAction' public/hub.htmx.html returns NO match (removed)
    - grep 'display:none' public/hub.htmx.html returns NO match (use hidden attr instead)
    - All sections that start hidden use the hidden attribute
  </acceptance_criteria>
  <done>Hub HTML is completely rewritten with hero card, two-column layout, 3-item vertical checklist, quorum bar section, motions preview section. All critical DOM IDs preserved for JS binding. No display:none, no old stepper/action-card structure.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite hub.css from scratch</name>
  <files>public/assets/css/hub.css</files>
  <read_first>
    - public/hub.htmx.html (the NEW HTML from Task 1 — read class names)
    - public/assets/css/operator.css (reference for two-panel grid, card styles from Phase 46)
    - public/assets/css/app.css (design tokens, app-shell grid, badge/button classes)
  </read_first>
  <action>
Rewrite hub.css completely. Delete all old styles. Write fresh CSS for the new HTML structure.

Sections to write:

1. **Hero card** (.hub-hero):
   - Flex layout: icon + body + actions, gap 1.25rem
   - Background: var(--color-surface), border: 1px solid var(--color-border), border-radius: 12px
   - Padding: 1.5rem 1.75rem, box-shadow: var(--shadow-md)
   - ::before accent stripe: 3px gradient (primary to primary-hover), absolute top
   - .hub-hero-icon: 52px square, gradient background (primary), border-radius 14px, centered SVG
   - .hub-hero-body: flex-1, min-width 0
   - .hub-hero-title-row: flex, align-items center, gap 0.75rem, flex-wrap wrap
   - .hub-hero-title: font-size 1.25rem, font-weight 600, color var(--color-text)
   - .hub-hero-badges: flex, gap 0.5rem
   - .hub-hero-meta: flex, align-items center, gap 0.25rem, color var(--color-text-muted), font-size 0.875rem, margin-top 0.5rem
   - .hub-hero-actions: flex, gap 0.75rem, align-items flex-start, flex-shrink 0

2. **Two-column body** (.hub-body):
   - display: grid, grid-template-columns: 280px 1fr, gap: var(--space-card, 1.5rem)
   - align-items: start, max-width: 1200px, margin: 1.5rem auto 0
   - padding: 0 var(--space-page, 1.5rem)

3. **Checklist card** (.hub-checklist-card):
   - Background: var(--color-surface), border, border-radius: 12px, padding: 1.25rem
   - .hub-checklist-header: flex, justify-content space-between, margin-bottom 1rem
   - .hub-checklist-label: font-weight 600, font-size 0.875rem
   - .hub-checklist-item: flex, align-items flex-start, gap 0.75rem, padding 0.75rem 0, border-bottom 1px solid var(--color-border-subtle)
   - Last item: no border-bottom
   - .hub-checklist-dot: 24px circle, flex-shrink 0, border 2px solid, display flex, align-items center, justify-content center
   - .hub-checklist-dot--done: background var(--color-success), border-color var(--color-success), color white
   - .hub-checklist-dot--pending: border-color var(--color-border), background transparent
   - .hub-checklist-dot--blocked: border-color var(--color-warning), background transparent
   - .hub-checklist-content: flex-1, min-width 0
   - .hub-checklist-label: font-size 0.875rem, font-weight 500
   - .hub-checklist-badge: font-size 0.75rem, padding 2px 8px, border-radius var(--radius-sm)
   - .hub-checklist-badge--done: color var(--color-success), background var(--color-success-subtle)
   - .hub-checklist-badge--blocked: color var(--color-warning), background var(--color-warning-subtle)
   - .hub-checklist-badge--pending: color var(--color-text-muted)
   - .hub-checklist-reason: display block, font-size 0.8125rem, color var(--color-text-muted), margin-top 0.25rem

4. **Quorum card** (.hub-quorum-card):
   - Same card pattern: surface background, border, border-radius 12px, padding 1.25rem
   - .hub-quorum-header: flex, justify-content space-between, align-items center, margin-bottom 0.75rem
   - .hub-quorum-label: font-weight 600, font-size 0.875rem
   - .hub-quorum-pct: font-size 1.5rem, font-weight 700, font-family var(--font-mono)
   - .hub-quorum-pct.reached: color var(--color-success)
   - .hub-quorum-pct.partial: color var(--color-warning)
   - .hub-quorum-pct.critical: color var(--color-danger)

5. **Motions card** (.hub-motions-card):
   - Same card pattern
   - .hub-motions-header: flex, justify-content space-between, align-items center, margin-bottom 0.75rem
   - .hub-motions-title: font-weight 600, font-size 0.875rem
   - .hub-motion-item: flex, align-items center, gap 0.75rem, padding 0.5rem 0, border-bottom 1px solid var(--color-border-subtle)
   - .hub-motion-num: font-size 0.75rem, font-weight 600, width 24px, height 24px, border-radius 50%, background var(--color-primary-subtle), color var(--color-primary), display flex, align-items center, justify-content center, flex-shrink 0
   - .hub-motion-title: flex 1, font-size 0.875rem, overflow hidden, text-overflow ellipsis, white-space nowrap
   - .hub-motions-footer: margin-top 0.75rem, text-align right

6. **Convocation section** (.hub-convoc-section):
   - margin-top: 1rem, padding 1rem, background var(--color-surface), border, border-radius 12px

7. **Responsive** (768px breakpoint):
   - .hub-body: grid-template-columns 1fr (single column)
   - .hub-hero: flex-direction column, align-items flex-start
   - .hub-hero-actions: width 100%, justify-content flex-start

8. **Dark mode**: All colors via tokens — no hardcoded hex values. The design token system handles dark mode automatically.

9. **Hub-specific error banner** (.hub-error):
   - padding 1rem 1.5rem, background var(--color-danger-subtle), border 1px solid var(--color-danger), border-radius 8px, margin-bottom 1rem

Do NOT include any styles for removed elements (hub-identity, hub-status-bar, hub-stepper-col, hub-action, hub-action-header, hub-details-toggle, hub-details-body, hub-kpi, hub-documents, hub-warn-card, hub-live-pair, hub-layout-body, hub-main-col).
  </action>
  <verify>
    <automated>grep -c 'hub-hero\|hub-body\|hub-checklist-card\|hub-quorum-card\|hub-motions-card\|hub-convoc-section' public/assets/css/hub.css | xargs test 6 -le</automated>
  </verify>
  <acceptance_criteria>
    - grep 'hub-hero' public/assets/css/hub.css returns matches
    - grep 'hub-body' public/assets/css/hub.css returns matches (grid-template-columns: 280px 1fr)
    - grep 'hub-checklist-card' public/assets/css/hub.css returns matches
    - grep 'hub-quorum-card' public/assets/css/hub.css returns matches
    - grep 'hub-motions-card' public/assets/css/hub.css returns matches
    - grep '768px' public/assets/css/hub.css returns match (responsive breakpoint)
    - grep 'hub-identity\|hub-status-bar\|hub-stepper-col\|hub-action-header\|hub-layout-body' public/assets/css/hub.css returns NO matches (old classes removed)
    - No hardcoded hex color values (except inside comments)
    - File is significantly shorter than old 1311 lines — target ~200-300 lines
  </acceptance_criteria>
  <done>Hub CSS is completely rewritten with hero card, two-column grid, checklist card, quorum card, motions card, responsive breakpoint. All old styles removed. All colors via design tokens.</done>
</task>

</tasks>

<verification>
- Open hub.htmx.html in browser — page renders with new layout structure (hero + two-column)
- No CSS compilation errors — all classes match between HTML and CSS
- Dark mode toggle works — all elements use token-based colors
- Responsive: resize to 768px — layout stacks to single column
</verification>

<success_criteria>
Hub page has a complete new HTML structure (hero card, two-column body, 3-item checklist, quorum bar, motions preview) and matching CSS. All critical DOM IDs preserved for JS wiring in Plan 02. No old stepper/action-card artifacts remain.
</success_criteria>

<output>
After completion, create `.planning/phases/47-hub-rebuild/47-01-SUMMARY.md`
</output>
