---
phase: 20.3-page-layout-wireframe-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/operator.htmx.html
  - public/partials/operator-exec.html
  - public/assets/css/operator.css
autonomous: true
requirements:
  - LAYOUT-OP

must_haves:
  truths:
    - "Operator page in execution mode shows a split panel: resolution content on the left (flex:1), ordre-du-jour sidebar on the right (200px)"
    - "KPI strip and progress track are pinned at the top (flex-shrink:0), not scrolling with content"
    - "All existing JS-targeted element IDs are preserved — no console errors after restructure"
    - "Operator page has zero page-level scroll at 1920x1080 — internal panels scroll independently"
  artifacts:
    - path: "public/assets/css/operator.css"
      provides: "Split panel CSS classes"
      contains: "operator-split"
    - path: "public/operator.htmx.html"
      provides: "Restructured operator layout"
      contains: "operator-split"
  key_links:
    - from: "public/operator.htmx.html"
      to: "public/assets/js/pages/operator-exec.js"
      via: "DOM IDs preserved"
      pattern: "tabVote|tabResultats|tabParole|tabPresences"
---

<objective>
Restructure the operator page from full-width tabs to a wireframe-aligned split panel layout.

Purpose: The operator console is the primary cockpit page — it must show resolution + results on the left and ordre-du-jour on the right (200px sidebar), with KPI strip and progress track pinned at top. Zero page-level scroll at 1080px.

Output: Restructured operator.htmx.html, operator-exec.html partial, and updated operator.css with split panel layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.3-page-layout-wireframe-alignment/20.3-CONTEXT.md
@.planning/phases/20.3-page-layout-wireframe-alignment/20.3-RESEARCH.md
@ag_vote_wireframe.html
@public/operator.htmx.html
@public/partials/operator-exec.html
@public/assets/css/operator.css
@public/assets/js/pages/operator-exec.js
@public/assets/js/pages/operator.js
@public/assets/js/pages/operator-tabs.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit JS selectors and restructure operator HTML to split panel</name>
  <files>public/operator.htmx.html, public/partials/operator-exec.html</files>
  <action>
FIRST: Read operator-exec.js, operator.js, operator-tabs.js, operator-motions.js, operator-parole.js to catalog ALL DOM IDs and selectors used by JS. Record this list — every ID must be preserved.

THEN: Read ag_vote_wireframe.html to understand the target operator layout structure (search for "Operator" or "Console" sections in the wireframe JSX).

THEN: Restructure operator.htmx.html and operator-exec.html:

1. In the execution view (viewExec section), wrap content in a `.operator-split` container:
   - Left panel (`.operator-left`): contains resolution card + sub-content (vote tab, results tab, etc.) — existing tab content DIVs with their IDs preserved
   - Right panel (`.operator-right`): contains ordre-du-jour list (motion/resolution list sidebar)

2. Ensure KPI strip area uses `.operator-kpi-strip` class with flex-shrink:0
3. Ensure progress track uses `.operator-progress-track` class with flex-shrink:0
4. Add `.operator-guidance` bar at the bottom of the exec view (flex-shrink:0)

CRITICAL: Do NOT rename or remove ANY existing element IDs. Only ADD new wrapper elements around existing content. The tab content DIVs (#tabVote, #tabResultats, #tabParole, #tabPresences, etc.) must keep their IDs and remain in the DOM — they are just repositioned within the split panel structure.

The setup view (viewSetup) remains unchanged — only the execution view gets the split panel.
  </action>
  <verify>
    <automated>npx eslint public/assets/js/pages/operator-exec.js public/assets/js/pages/operator.js public/assets/js/pages/operator-tabs.js public/assets/js/pages/operator-motions.js public/assets/js/pages/operator-parole.js 2>/dev/null; echo "HTML restructure done — verify IDs preserved by grepping for key IDs in both HTML and JS"</automated>
  </verify>
  <done>Operator HTML restructured with .operator-split wrapper containing left/right panels. All existing JS element IDs preserved. ESLint passes on operator JS files.</done>
</task>

<task type="auto">
  <name>Task 2: Add split panel CSS and density overrides to operator.css</name>
  <files>public/assets/css/operator.css</files>
  <action>
Add the following CSS classes to operator.css (per wireframe v3.19.2 specifications from RESEARCH.md):

1. `.operator-split` — flex container: `display: flex; gap: 10px; flex: 1; min-height: 0; overflow: hidden`
2. `.operator-left` — left panel: `flex: 1; display: flex; flex-direction: column; min-width: 0; overflow: hidden`
3. `.operator-right` — right sidebar: `width: 200px; flex-shrink: 0; display: flex; flex-direction: column; overflow: hidden`
4. `.operator-left-body` — scrollable zone: `flex: 1; overflow-y: auto; min-height: 0; padding-bottom: 8px`
5. `.operator-right-body` — scrollable zone: `flex: 1; overflow-y: auto; min-height: 0`
6. `.operator-kpi-strip` — top KPI row: `flex-shrink: 0; display: flex; gap: 6px; margin-bottom: 6px` with `.operator-kpi-strip .card { flex: 1; text-align: center; padding: 6px 4px; margin: 0; }`
7. `.operator-progress-track` — `flex-shrink: 0; display: flex; align-items: center; gap: 4px; margin-bottom: 8px`
8. `.operator-guidance` — bottom guidance bar: `flex-shrink: 0; margin-top: 8px; padding: 10px 16px; border-radius: var(--radius-lg); background: var(--color-primary-subtle); border: 1px solid var(--color-primary); display: flex; align-items: center; gap: 8px`

Also compact existing operator CSS:
- Reduce any card-body padding overrides to 14px where currently 20px+
- Ensure the overall operator page uses `display: flex; flex-direction: column; flex: 1; overflow: hidden` on the main content area so the split panel fills available space

Do NOT modify design-system.css — all changes scoped to operator.css only.
  </action>
  <verify>
    <automated>grep -c "operator-split\|operator-left\|operator-right\|operator-kpi-strip\|operator-guidance" public/assets/css/operator.css</automated>
  </verify>
  <done>operator.css contains all split panel classes matching wireframe specs. Operator page at 1920x1080 has zero page-level scroll with internal panels scrolling independently.</done>
</task>

</tasks>

<verification>
- Load operator page at 1920x1080 — zero page-level scroll
- Execution view shows split panel (left: resolution content, right: 200px sidebar)
- KPI strip and progress track pinned at top
- No JS console errors — all operator JS functions work (tab switching, vote actions, etc.)
- ESLint passes on all operator JS files
</verification>

<success_criteria>
- Operator page uses split panel layout matching wireframe v3.19.2
- All existing JS functionality preserved (no broken selectors)
- Zero page-level scroll at 1080px height
- Internal panels scroll independently when content overflows
</success_criteria>

<output>
After completion, create `.planning/phases/20.3-page-layout-wireframe-alignment/20.3-01-SUMMARY.md`
</output>
