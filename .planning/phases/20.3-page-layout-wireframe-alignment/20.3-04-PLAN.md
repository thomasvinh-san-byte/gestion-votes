---
phase: 20.3-page-layout-wireframe-alignment
plan: 04
type: execute
wave: 2
depends_on:
  - 20.3-01
  - 20.3-02
  - 20.3-03
files_modified:
  - public/assets/css/pages.css
  - public/assets/css/postsession.css
  - public/assets/css/analytics.css
  - public/assets/css/vote.css
  - public/assets/css/public.css
  - public/assets/css/admin.css
autonomous: false
requirements:
  - LAYOUT-REMAINING
  - LAYOUT-VERIFY

must_haves:
  truths:
    - "Every page in the application has been compared to its wireframe equivalent and spacing/density adjusted"
    - "Post-session, analytics, vote, public, admin, report, validate pages use wireframe-aligned density (14-16px card padding, compact headers)"
    - "Page headers across all pages use compact vertical spacing — no oversized gaps between title and content"
    - "Human has verified the 6 major cockpit pages at 1920x1080: dashboard, operator, settings, hub, wizard, vote"
  artifacts:
    - path: "public/assets/css/pages.css"
      provides: "Global page header compaction and remaining page density overrides"
      contains: "page-header"
  key_links:
    - from: "public/assets/css/pages.css"
      to: "All page HTML files"
      via: "Shared page-header and card-body density classes"
      pattern: "page-header|card-body"
---

<objective>
Complete the density pass on all remaining pages and perform human verification of all major layouts.

Purpose: Plans 01-03 cover the 5 highest-impact pages (operator, settings, wizard, dashboard, hub) and global table density. This plan handles the remaining ~16 pages with a systematic density pass, then pauses for human verification of all major cockpit pages at 1920x1080.

Output: All pages aligned with wireframe v3.19.2 density. Human-verified at target resolution.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.3-page-layout-wireframe-alignment/20.3-CONTEXT.md
@.planning/phases/20.3-page-layout-wireframe-alignment/20.3-RESEARCH.md
@.planning/phases/20.3-page-layout-wireframe-alignment/20.3-01-SUMMARY.md
@.planning/phases/20.3-page-layout-wireframe-alignment/20.3-02-SUMMARY.md
@.planning/phases/20.3-page-layout-wireframe-alignment/20.3-03-SUMMARY.md
@ag_vote_wireframe.html
@public/assets/css/pages.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Systematic density pass on all remaining pages</name>
  <files>public/assets/css/pages.css, public/assets/css/postsession.css, public/assets/css/analytics.css, public/assets/css/vote.css, public/assets/css/public.css, public/assets/css/admin.css</files>
  <action>
Perform a systematic density pass on ALL pages not yet addressed by plans 01-03. The following pages need review:

**Pages to check and compact (read each HTML + CSS, compare to wireframe):**

1. **vote.htmx.html + vote.css** — Voter page. Already aligned in 20.1 (2x2 grid). Verify density fits 1080px, reduce card padding if >16px.

2. **public.htmx.html + public.css** — Projection/public display page. Already aligned in 20.1 (gradient Fraunces). Verify density.

3. **postsession.htmx.html + postsession.css** — Post-session stepper. Check step cards, result tables, action buttons. Compact card padding to 14-16px if >20px.

4. **analytics.htmx.html + analytics.css** — Statistics page. Optimize chart container heights and card density for 1080px. Reduce graph card padding if it causes scroll.

5. **report.htmx.html + report.css** — Report page. Compact card and table layouts.

6. **validate.htmx.html + validate.css** — Validation page. Compact card padding.

7. **admin.htmx.html + admin.css** — Admin page. Compact layout.

8. **docs.htmx.html + doc.css** — Documentation page. Compact card padding.

9. **help.htmx.html + help.css** — Help page. Compact layout.

10. **trust.htmx.html + trust.css** — Trust/security page. Compact.

11. **login page** — If a login.htmx.html exists, verify its density.

12. **email-templates.htmx.html** — Compact if needed.

For EACH page, apply this checklist:
- [ ] Read the HTML structure
- [ ] Read the wireframe equivalent section (search ag_vote_wireframe.html)
- [ ] Compare card padding (target: 14-16px, reduce if >20px)
- [ ] Compare section margins (target: 14px, reduce if >20px)
- [ ] Check page header height (compact if large)
- [ ] Apply CSS overrides in the page-specific CSS file (NOT design-system.css)

ALSO: Add a global `.page-header` compaction rule in pages.css if not already present:
- Reduce `.page-header` margin-bottom from ~24px to ~14px
- Reduce `.page-header` padding if it has excessive vertical padding
- This applies to ALL pages at once

The wireframe uses ~14px card padding and 10-14px section margins. The implementation currently uses ~20px. Reduce to match wireframe where it helps fit content in 1080px.

`.app-main` padding: evaluate whether 16px 22px is appropriate or should be reduced to 14px 18px. Only change if it helps multiple pages fit. Add as a scoped override in pages.css if needed (do NOT modify design-system.css).

Do NOT modify design-system.css. All overrides go in page-specific CSS or pages.css.
  </action>
  <verify>
    <automated>grep -c "page-header" public/assets/css/pages.css && echo "Remaining pages density pass complete"</automated>
  </verify>
  <done>All ~16 remaining pages have been reviewed and density-adjusted. Card padding reduced to 14-16px where it was >20px. Page headers compacted. Section margins reduced to ~14px.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all page layouts at 1920x1080</name>
  <files>none</files>
  <action>
Present the complete list of pages modified across plans 01-04 and ask the human to verify each major cockpit page at 1920x1080 resolution.
  </action>
  <verify>Human confirms approval or provides issue list</verify>
  <done>Human has verified all major cockpit pages fit at 1920x1080 and layout matches wireframe v3.19.2</done>
  <what-built>Complete page layout alignment of ALL pages with wireframe v3.19.2. Plans 01-04 have restructured the operator (split panel), settings (sidebar nav), wizard (pinned footer), dashboard (zero-scroll density), hub (verified + compacted), all data tables (8px row padding), and all remaining pages (density pass).</what-built>
  <how-to-verify>
    Open each page at 1920x1080 and verify:
    1. **Dashboard** — Zero scroll. All sections visible: urgent banner + KPIs + panels + shortcuts.
    2. **Operator** — Split panel: resolution left, ordre-du-jour right (200px). KPI strip pinned top. Zero page scroll.
    3. **Settings** — Vertical sidebar nav left (200px), content right. Tab switching works.
    4. **Hub** — Stepper left (220px), action card + checklist right. No unnecessary scroll.
    5. **Wizard** — Each step fits in viewport. Step-nav buttons at bottom of card.
    6. **Vote** — 2x2 grid fits in viewport.
    7. **Tables** (members, meetings, audit, archives, users) — Compact rows (8px padding), more data visible.
    8. **All other pages** — Density is wireframe-aligned, no excessive whitespace.
    9. **No JS console errors** on any page.
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific pages/issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All pages reviewed against wireframe and density-adjusted
- No design-system.css modifications
- No JS logic changes
- Human verification at 1920x1080 confirms wireframe alignment
</verification>

<success_criteria>
- Every page in the application has wireframe-aligned layout density
- Cockpit pages (dashboard, operator, hub) fit in 1080px without scroll
- Human approves visual alignment at target resolution
</success_criteria>

<output>
After completion, create `.planning/phases/20.3-page-layout-wireframe-alignment/20.3-04-SUMMARY.md`
</output>
