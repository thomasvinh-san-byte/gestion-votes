---
phase: 36-session-creation-flow
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/hub.htmx.html
  - public/assets/css/hub.css
  - public/assets/js/pages/hub.js
autonomous: true
requirements: [CORE-04]

must_haves:
  truths:
    - "Hub identity banner shows session type badge and status badge alongside title"
    - "Status bar segments are 36px tall with always-visible step labels"
    - "Vertical stepper uses single connector strategy (no double lines) with clear pending dimming"
    - "Quorum bar is the hero section with large percentage number and tooltip explaining quorum rules"
    - "Checklist items show 'Fait'/'A faire' badges with card-like hover treatment"
    - "Action button uses gradient CTA pattern from Phase 35"
  artifacts:
    - path: "public/assets/css/hub.css"
      provides: "All hub visual redesign CSS"
      contains: "hub-quorum-hero"
    - path: "public/hub.htmx.html"
      provides: "Restructured HTML with badges, quorum hero, identity badges"
      contains: "hub-quorum-hero-pct"
    - path: "public/assets/js/pages/hub.js"
      provides: "Updated JS for quorum pct, type/status badges, done badges"
      contains: "hubQuorumPct"
  key_links:
    - from: "public/assets/js/pages/hub.js"
      to: "#hubQuorumPct"
      via: "renderQuorumBar() updates percentage text and color class"
      pattern: "hubQuorumPct"
    - from: "public/assets/js/pages/hub.js"
      to: "#hubTypeTag"
      via: "applySessionToDOM() populates session type badge"
      pattern: "hubTypeTag"
    - from: "public/assets/js/pages/hub.js"
      to: ".hub-check-done-badge"
      via: "renderChecklist() adds Fait badge on completed items"
      pattern: "hub-check-done-badge"
---

<objective>
Complete visual redesign of the Hub page (session management) to Notion-dashboard-quality polish.

Purpose: Transform the hub from a functional but visually flat session management page into a premium dashboard with clear visual hierarchy, the quorum bar as the hero element, checklist items with status badges, and a prominent action CTA. CORE-04 requirement.
Output: Visually redesigned hub.htmx.html, hub.css, and hub.js with dramatic before/after contrast.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-session-creation-flow/36-CONTEXT.md
@.planning/phases/36-session-creation-flow/36-RESEARCH.md
@public/hub.htmx.html
@public/assets/css/hub.css
@public/assets/js/pages/hub.js
@public/assets/css/design-system.css
@public/assets/js/components/ag-quorum-bar.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identity banner badges, status bar labels, vertical stepper cleanup, and quorum hero</name>
  <files>public/hub.htmx.html, public/assets/css/hub.css, public/assets/js/pages/hub.js</files>
  <read_first>
    - public/hub.htmx.html (identity banner structure, status bar, stepper, quorum section)
    - public/assets/css/hub.css (current styles for .hub-identity, .hub-bar-segment, .hub-step-row, .hub-quorum-section)
    - public/assets/js/pages/hub.js (applySessionToDOM(), renderStatusBar(), renderStepper(), renderQuorumBar())
    - public/assets/js/components/ag-quorum-bar.js (Shadow DOM structure -- confirm cannot pierce)
    - .planning/phases/36-session-creation-flow/36-RESEARCH.md (sections: HUB-01 identity banner, HUB-02 status bar, HUB-03 stepper, HUB-04 quorum hero, Pitfall 1 shadow DOM, Pitfall 3 double connector)
  </read_first>
  <action>
**Identity banner badges (HUB-01 from RESEARCH.md):**
- In hub.htmx.html, wrap the existing `#hubTitle` and add a badges row. Restructure `.hub-identity-info` to include:
  - A `.hub-identity-title-row` div wrapping `#hubTitle` + a `.hub-identity-badges` div containing two badge spans: `<span class="badge badge--neutral" id="hubTypeTag">-</span>` and `<span class="badge badge--info" id="hubStatusTag">En preparation</span>`
- In hub.css, add `.hub-identity-title-row { display: flex; align-items: center; gap: 12px; flex-wrap: wrap; margin-bottom: 0.25rem; }`
- In hub.js `applySessionToDOM()`, add two lines to populate: `document.getElementById('hubTypeTag').textContent = session.type_label || 'AG';` and `document.getElementById('hubStatusTag').textContent = session.status_label || session.status || 'En preparation';`

**Status bar visible labels (HUB-02 from RESEARCH.md):**
- In hub.css, override `.hub-bar-segment` height to `36px` with `display: flex; align-items: center; justify-content: center; overflow: hidden;`
- Active segment: `height: 40px`
- Override `.hub-bar-label` to always visible: `font-size: 11px !important; font-weight: 600; color: rgba(255,255,255,0.9); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 90%; padding: 0 4px;`
- Pending segment labels: `.hub-bar-segment:not(.done):not(.active) .hub-bar-label { color: var(--color-text-muted); }`
- Add `.hub-status-bar { align-items: center; gap: 2px; padding: 6px; }`

**Vertical stepper cleanup (HUB-03 from RESEARCH.md):**
- CRITICAL: Disable the `::before` pseudo-element connector: `.hub-step-row::before { display: none; }` -- use only the `.hub-step-line` div rendered by JS (avoids double connector, per Pitfall 3)
- Style `.hub-step-line`: `width: 3px; height: 24px; margin-left: 14px; border-radius: 2px; background: var(--color-border);`
- Done connector: `.hub-step-line.done { background: var(--color-success); }`
- Restyle active step indicator `.hub-step-here`: `font-size: 0.75rem; color: var(--color-primary); font-weight: 700; font-style: normal; margin-top: 2px; display: flex; align-items: center; gap: 4px;` (remove the italic "Vous etes ici" look, make it bold primary "Etape en cours")
- Pending step dimming: `.hub-step-row:not([aria-current]):not(.done) .hub-step-num { opacity: 0.45; }`

**Quorum bar hero treatment (HUB-04 from RESEARCH.md):**
- In hub.htmx.html, replace the existing `.hub-quorum-section` div with a `.hub-quorum-hero` div containing:
  - `.hub-quorum-hero-header` with `.hub-quorum-hero-left` (label "Quorum" in uppercase + `ag-popover` tooltip explaining quorum rules) and `.hub-quorum-hero-pct` span (id="hubQuorumPct", shows "--%")
  - Keep the existing `<ag-quorum-bar>` element inside
- IMPORTANT: Keep the same `id="hubQuorumSection"` on the outer div and `id="hubQuorumBar"` on the component -- JS references these IDs
- In hub.css, add all `.hub-quorum-hero` styles from RESEARCH HUB-04: card with 12px radius, shadow-md, 1.5rem padding. Hero percentage at `font-family: var(--font-mono); font-size: 2rem; font-weight: 800;` with color states `.reached/.partial/.critical`
- The label uses `.hub-quorum-hero-label`: `font-size: 0.6875rem; font-weight: 800; text-transform: uppercase; letter-spacing: 0.08em; color: var(--color-primary);`
- In hub.js `renderQuorumBar()`, add lines after bar.setAttribute calls: compute percentage `Math.round((current / total) * 100)`, set `hubQuorumPct.textContent = pct + '%'`, apply status class (reached/partial/critical) based on current vs required
- PITFALL: Do NOT try to style ag-quorum-bar internals from hub.css -- Shadow DOM prevents this. Only style the wrapper.
  </action>
  <verify>
    <automated>grep -c "hub-quorum-hero\|hub-identity-title-row\|hub-step-row::before.*display.*none\|hub-bar-segment.*36px\|hubQuorumPct\|hubTypeTag" public/assets/css/hub.css public/hub.htmx.html public/assets/js/pages/hub.js | grep -v ":0$" | wc -l | xargs test 3 -le</automated>
  </verify>
  <acceptance_criteria>
- Identity banner shows session type and status badges next to title
- Status bar segments are 36px tall with visible step labels at all times
- Vertical stepper has single connector line (no double rendering), pending steps dimmed at 0.45 opacity
- Quorum section shows large 2rem percentage number that color-codes (green/amber/red) based on quorum status
- Quorum section has ag-popover tooltip explaining quorum rules
- All existing JS functionality (step navigation, quorum updates) continues to work
  </acceptance_criteria>
  <done>Hub identity, status bar, stepper, and quorum bar are visually redesigned with clear hierarchy and information density</done>
</task>

<task type="auto">
  <name>Task 2: Checklist card items with badges, action card gradient CTA, and motions list polish</name>
  <files>public/hub.htmx.html, public/assets/css/hub.css, public/assets/js/pages/hub.js</files>
  <read_first>
    - public/hub.htmx.html (checklist section, action card, motions section)
    - public/assets/css/hub.css (current .hub-check-item, .hub-action, .hub-motion-item styles)
    - public/assets/js/pages/hub.js (renderChecklist() function, renderMotionsList() function)
    - .planning/phases/36-session-creation-flow/36-RESEARCH.md (sections: HUB-05 checklist, hub action card, motions list, Pitfall 4 inline styles)
  </read_first>
  <action>
**Checklist card items with badges (HUB-05 from RESEARCH.md):**
- In hub.css, override `.hub-check-item`: `padding: 12px 10px; border-radius: var(--radius); margin-bottom: 4px; border-bottom: none; border: 1px solid transparent;`
- Add hover: `.hub-check-item:hover { border-color: var(--color-border-subtle); background: var(--color-bg-subtle); }`
- Done item visual: `.hub-check-item.done { opacity: 1; background: var(--color-success-subtle, rgba(34,197,94,0.06)); border-color: var(--color-success-border, rgba(34,197,94,0.2)); }`
- Done icon size: `.hub-check-item.done .hub-check-icon { width: 24px; height: 24px; }`
- Enlarge "A faire" badge: `.hub-check-todo { font-size: 0.75rem; padding: 3px 10px; font-weight: 700; }`
- Add "Fait" badge class: `.hub-check-done-badge { font-size: 0.75rem; padding: 3px 10px; background: var(--color-success-subtle, rgba(34,197,94,0.06)); color: var(--color-success); border-radius: var(--radius-sm); font-weight: 700; flex-shrink: 0; }`
- Thicker checklist progress bar: `.hub-checklist-bar { height: 12px; margin-bottom: 16px; }`
- In hub.js `renderChecklist()`, find where done items are rendered and add a `<span class="hub-check-done-badge">Fait</span>` element inside done items (replacing the empty slot where "A faire" would be for pending items)

**Action card gradient CTA:**
- In hub.css, override `.hub-action .btn-primary` (or `.hub-action-btn.btn-primary`) to use the gradient pattern: `background: linear-gradient(135deg, var(--color-primary), var(--color-primary-hover)); box-shadow: 0 2px 8px var(--color-primary-glow);`
- Add hover: `transform: translateY(-1px); box-shadow: 0 4px 16px var(--color-primary-glow);`
- Give the action card elevated visual weight: `.hub-action { border: 1.5px solid var(--color-primary-subtle); box-shadow: var(--shadow-md); }` -- differentiate from checklist
- PITFALL: Do NOT override `.hub-action-icon` background via CSS -- hub.js applies inline `style.background` per step color (Pitfall 4). Only style border-radius, size, and shadow.

**Motions list polish:**
- In hub.css, add slight card treatment to `.hub-motion-item`: `padding: 12px 10px; border-radius: var(--radius); margin-bottom: 2px; border: 1px solid transparent; transition: border-color 0.15s ease, background 0.15s ease;`
- Add hover: `.hub-motion-item:hover { border-color: var(--color-border-subtle); background: var(--color-bg-subtle); }`
- Increase `.hub-motion-num` to 30x30px (from 28px)
- Increase `.doc-badge` font-size: `.doc-badge { font-size: 0.75rem; }` and `.doc-badge--has-docs { padding: 3px 10px; font-weight: 600; }`
- Increase `.hub-motions-title` to `font-size: 1rem; font-weight: 700;`

**Responsive at 768px (per CONTEXT locked decision):**
- At `@media (max-width: 768px)`, ensure the stepper stacks above content. Add: `.hub-stepper { position: static; }` and reduce quorum hero pct to `font-size: 1.5rem;`
  </action>
  <verify>
    <automated>grep -c "hub-check-done-badge\|hub-quorum-hero\|hub-action.*gradient\|hub-motion-item.*hover\|hub-checklist-bar.*12px" public/assets/css/hub.css public/assets/js/pages/hub.js | grep -v ":0$" | wc -l | xargs test 2 -le</automated>
  </verify>
  <acceptance_criteria>
- Checklist items have card-like hover treatment with visible borders
- Done checklist items show green "Fait" badge, pending items show larger "A faire" badge
- Checklist progress bar is 12px tall (not 8px)
- Action card uses gradient CTA button with hover lift
- Action card has elevated border treatment to stand out from other cards
- Motion items have hover state and slightly larger number circles
- Document badges are more readable at 12px
- Responsive layout works at 768px breakpoint
  </acceptance_criteria>
  <done>Hub checklist, action card, and motions list are polished with status badges, gradient CTA, and card hover treatments</done>
</task>

</tasks>

<verification>
- Open hub page in browser with a test session
- Verify identity banner shows type badge and status badge
- Verify status bar has visible 36px-tall segments with step labels
- Verify vertical stepper has clean single connector lines, pending steps dimmed
- Verify quorum section shows large percentage number with color coding
- Verify checklist items show "Fait"/"A faire" badges with hover treatment
- Verify action button uses gradient CTA
- Verify motions list items have hover states
- Compare before/after screenshots for visible improvement
</verification>

<success_criteria>
The hub page feels like a Notion project dashboard: clear session status, prominent quorum indicator, actionable checklist with status badges, and obvious next-action CTA. The quorum bar is the visual anchor of the page. Before/after screenshot contrast is immediately obvious.
</success_criteria>

<output>
After completion, create `.planning/phases/36-session-creation-flow/36-02-SUMMARY.md`
</output>
