---
phase: 27-copropriete-transformation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Unit/WeightedVoteRegressionTest.php
  - public/assets/js/pages/wizard.js
  - public/assets/css/wizard.css
  - public/assets/js/pages/settings.js
  - public/settings.htmx.html
  - public/admin.htmx.html
  - public/help.htmx.html
  - public/index.html
  - public/assets/js/core/shell.js
  - app/Repository/AggregateReportRepository.php
autonomous: true
requirements: [CPR-01, CPR-02, CPR-03, CPR-04, CPR-05]

must_haves:
  truths:
    - "PHPUnit weighted-vote regression test passes with members having voting_power 3 and 1 producing POUR:3, CONTRE:1"
    - "No user-facing string contains copropriete, tantiemes (as label), lot (as field), milliemes, or cle de repartition"
    - "The lot field is absent from wizard member input form"
    - "The Cles de repartition section is absent from settings page and admin page"
    - "The openKeyModal function and initDistributionKeys function are removed from settings.js"
    - "The select option value='tantiemes' is preserved but display text changed to 'Par poids de vote'"
    - "voting_power column, BallotsService, ImportService tantieme alias are untouched"
  artifacts:
    - path: "tests/Unit/WeightedVoteRegressionTest.php"
      provides: "Weighted vote regression test"
      contains: "voting_power"
    - path: "public/assets/js/pages/wizard.js"
      provides: "Wizard without lot field"
    - path: "public/assets/js/pages/settings.js"
      provides: "Settings without openKeyModal"
    - path: "public/settings.htmx.html"
      provides: "Settings without Cles de repartition section"
    - path: "public/admin.htmx.html"
      provides: "Admin without Cles de repartition section"
  key_links:
    - from: "tests/Unit/WeightedVoteRegressionTest.php"
      to: "app/Services/BallotsService.php"
      via: "PHPUnit test exercises weighted vote tally"
      pattern: "voting_power.*3.*1"
    - from: "public/settings.htmx.html"
      to: "public/assets/js/pages/settings.js"
      via: "HTML references JS functions — removing both in sync"
      pattern: "btnAddKey|distributionKeysBody"
---

<objective>
Transform all copropriete-specific vocabulary to generic AG terminology across core UI files, remove dead code (lot field, openKeyModal, Cles de repartition), and write PHPUnit regression test for weighted vote correctness.

Purpose: Make the application vocabulary-neutral so it works for any type of assembly, not just copropriete. Ensure weighted vote logic remains functionally identical via regression test.
Output: PHPUnit test file, 9 modified UI/JS/HTML/PHP files with zero copropriete-specific user-facing strings and no dead code.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-copropriete-transformation/27-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write PHPUnit weighted-vote regression test</name>
  <files>tests/Unit/WeightedVoteRegressionTest.php</files>
  <read_first>
    - tests/Unit/BallotsServiceTest.php (first 80 lines — setUp pattern, mock injection)
    - tests/Unit/VoteEngineTest.php (first 60 lines — pure logic test pattern)
    - app/Services/BallotsService.php (lines 90-220 — castBallot weight logic)
    - app/Services/VoteEngine.php (computeDecision static method signature)
    - tests/bootstrap.php (stub functions available)
  </read_first>
  <behavior>
    - Test 1: Two members with voting_power 3 and 1. Member A votes POUR (weight 3), Member B votes CONTRE (weight 1). VoteEngine::computeDecision receives forWeight=3.0, againstWeight=1.0, expressedWeight=4.0. Result majority.met is true, tallies show for:3.0, against:1.0.
    - Test 2: Same setup but both vote POUR. forWeight=4.0, againstWeight=0.0. Unanimous adoption.
    - Test 3: Equal weight members (voting_power 1 each). forWeight=1.0, againstWeight=1.0. Tie — majority NOT met (threshold 0.5 on expressed means >50% needed).
  </behavior>
  <action>
Create tests/Unit/WeightedVoteRegressionTest.php following the Pure Logic Test Pattern from TESTING.md.

File structure:
```php
<?php
declare(strict_types=1);
namespace AgVote\Tests\Unit;

use AgVote\Service\VoteEngine;
use PHPUnit\Framework\TestCase;

/**
 * Regression test for weighted vote tally correctness.
 * Ensures voting_power weighting produces correct POUR/CONTRE tallies
 * regardless of vocabulary changes (CPR-05).
 */
class WeightedVoteRegressionTest extends TestCase
{
    // =========================================================================
    // WEIGHTED VOTE TALLY TESTS
    // =========================================================================

    public function testWeightedVoteTallyPourContre(): void
    {
        // Member A: voting_power=3, votes POUR
        // Member B: voting_power=1, votes CONTRE
        $result = VoteEngine::computeDecision(
            quorumPolicy: null,
            votePolicy: ['base' => 'expressed', 'threshold' => 0.5],
            forWeight: 3.0,
            againstWeight: 1.0,
            abstainWeight: 0.0,
            expressedWeight: 4.0,
            expressedMembers: 2,
            eligibleMembers: 2,
            eligibleWeight: 4.0,
        );

        $this->assertTrue($result['majority']['met'], 'POUR:3 vs CONTRE:1 should adopt');
        $this->assertEqualsWithDelta(3.0 / 4.0, $result['majority']['ratio'], 0.0001);
    }

    public function testWeightedVoteTallyUnanimousPour(): void
    {
        // Both members vote POUR: weight 3 + weight 1 = 4
        $result = VoteEngine::computeDecision(
            quorumPolicy: null,
            votePolicy: ['base' => 'expressed', 'threshold' => 0.5],
            forWeight: 4.0,
            againstWeight: 0.0,
            abstainWeight: 0.0,
            expressedWeight: 4.0,
            expressedMembers: 2,
            eligibleMembers: 2,
            eligibleWeight: 4.0,
        );

        $this->assertTrue($result['majority']['met']);
        $this->assertEqualsWithDelta(1.0, $result['majority']['ratio'], 0.0001);
    }

    public function testEqualWeightTieNotAdopted(): void
    {
        // Two members, equal weight 1 each, one POUR one CONTRE
        $result = VoteEngine::computeDecision(
            quorumPolicy: null,
            votePolicy: ['base' => 'expressed', 'threshold' => 0.5],
            forWeight: 1.0,
            againstWeight: 1.0,
            abstainWeight: 0.0,
            expressedWeight: 2.0,
            expressedMembers: 2,
            eligibleMembers: 2,
            eligibleWeight: 2.0,
        );

        $this->assertFalse($result['majority']['met'], 'Tie (50%) should NOT meet >50% threshold');
    }
}
```

Run the test immediately after creation to confirm it passes (GREEN — these test existing behavior, not new code).
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && vendor/bin/phpunit tests/Unit/WeightedVoteRegressionTest.php --no-coverage</automated>
  </verify>
  <done>WeightedVoteRegressionTest.php exists with 3 test methods, all passing. Tests exercise VoteEngine::computeDecision with weighted inputs (3:1, 4:0, 1:1).</done>
</task>

<task type="auto">
  <name>Task 2: Vocabulary renames and dead code removal across core UI files</name>
  <files>
    public/assets/js/pages/wizard.js,
    public/assets/css/wizard.css,
    public/assets/js/pages/settings.js,
    public/settings.htmx.html,
    public/admin.htmx.html,
    public/help.htmx.html,
    public/index.html,
    public/assets/js/core/shell.js,
    app/Repository/AggregateReportRepository.php
  </files>
  <read_first>
    - public/assets/js/pages/wizard.js (lines 295-310, 335-350, 385-397 — lot field locations)
    - public/assets/css/wizard.css (lines 413-417 — .member-lot class)
    - public/assets/js/pages/settings.js (lines 385-437, 620 — initDistributionKeys + openKeyModal + call site)
    - public/settings.htmx.html (lines 98-212 — tantiemes option + majority descs + Cles section)
    - public/admin.htmx.html (lines 545-688 — distribution-keys tab + panel + majority descs)
    - public/help.htmx.html (lines 194, 299, 432-439 — copropriete/tantiemes references)
    - public/index.html (lines 291-293 — tantiemes feature)
    - public/assets/js/core/shell.js (line 683 — sidebar subtitle)
    - app/Repository/AggregateReportRepository.php (line 99 — comment)
  </read_first>
  <action>
Apply all changes in this exact order:

**A. wizard.js — Remove lot field (CPR-02)**

1. Line 301: Remove the entire `'<span class="member-lot">' + escapeHtml(m.lot || '') + '</span>' +` line from the member row HTML template.

2. Line 342: In the CSV import map function, remove `lot: r[1] ? r[1].trim() : '',` and shift subsequent fields: `email` should read from `r[1]` (was r[2]), `voix` should read from `r[2]` (was r[3]).
   BEFORE: `nom: (r[0]).trim(), name: (r[0]).trim(), lot: r[1]?.trim() || '', email: r[2]?.trim() || '', voix: r[3] ? parseInt(...)`
   AFTER: `nom: (r[0] || '').trim(), name: (r[0] || '').trim(), email: r[1] ? r[1].trim() : '', voix: r[2] ? parseInt(r[2].trim(), 10) || 1 : 1`

3. Lines 389-392: Remove the lot prompt and lot field from manual add.
   BEFORE:
   ```
   var lot  = window.prompt('Numero de lot (optionnel) :') || '';
   var voix = parseInt(window.prompt('Poids de vote (defaut : 1) :') || '1', 10) || 1;
   members.push({ nom: name.trim(), name: name.trim(), lot: lot.trim(), voix: voix });
   ```
   AFTER:
   ```
   var voix = parseInt(window.prompt('Poids de vote (d\u00e9faut : 1) :') || '1', 10) || 1;
   members.push({ nom: name.trim(), name: name.trim(), voix: voix });
   ```

**B. wizard.css — Remove .member-lot class (CPR-02)**

Delete lines 413-417 (the entire `.member-lot { ... }` block).

**C. settings.js — Remove openKeyModal and initDistributionKeys (CPR-03)**

1. Delete the entire `initDistributionKeys()` function (lines 386-404).
2. Delete the entire `openKeyModal(key)` function (lines 407-437).
3. Delete the `initDistributionKeys();` call (line 620).

**D. settings.htmx.html — Vocabulary renames + section removal (CPR-01, CPR-03)**

1. Line 104: Change display text only (preserve value attribute):
   `<option value="tantiemes">Par tanti&egrave;mes</option>` ->
   `<option value="tantiemes">Par poids de vote</option>`

2. Lines 131, 141, 146: Replace "copropri&eacute;taires" with "membres" in majority descriptions:
   - Line 131: `Majorit&eacute; des voix de tous les copropri&eacute;taires` -> `Majorit&eacute; des voix de tous les membres`
   - Line 141: `2/3 des voix de tous les copropri&eacute;taires` -> `2/3 des voix de tous les membres`
   - Line 146: `Tous les copropri&eacute;taires doivent voter pour` -> `Tous les membres doivent voter pour`

3. Lines 152-211: Delete the entire "Section 4 : Cles de repartition" card (from `<!-- Section 4 : Cles de repartition -->` through the closing `</div>` before `<!-- Section 5 : Politiques de quorum -->`).

4. Line 355: Change email template description:
   `Relance pour copropri&eacute;taires en retard de paiement` ->
   `Relance pour membres en retard de paiement`

**E. admin.htmx.html — Tab removal + vocabulary + section removal (CPR-01, CPR-03)**

1. Line 548: Delete the distribution-keys tab button entirely:
   `<button class="settings-tab" data-stab="distribution-keys" role="tab">Cl&eacute;s de r&eacute;partition</button>`

2. Lines 572, 582, 587: Replace "copropri&eacute;taires" with "membres":
   - Line 572: `Majorit&eacute; des voix de tous les copropri&eacute;taires` -> `Majorit&eacute; des voix de tous les membres`
   - Line 582: `2/3 des voix de tous les copropri&eacute;taires` -> `2/3 des voix de tous les membres`
   - Line 587: `Tous les copropri&eacute;taires doivent voter pour` -> `Tous les membres doivent voter pour`

3. Lines 629-687: Delete the entire distribution-keys settings panel (from `<!-- Cles de repartition -->` through the closing `</div>` before `<!-- Securite -->`).

4. Line 830: Change email template description:
   `Relance pour copropri&eacute;taires en retard de paiement` ->
   `Relance pour membres en retard de paiement`

**F. help.htmx.html — Vocabulary renames (CPR-01)**

1. Line 194: Replace `reunions de copropriete` with `reunions d'organisation`:
   `assemblees generales, conseils syndicaux, reunions de copropriete` ->
   `assemblees generales, conseils syndicaux, reunions d'organisation`

2. Line 299: Replace `tantiemes` with `poids de vote`:
   `somme des poids (tantiemes) des presents` ->
   `somme des poids de vote des presents`

3. Line 432: Update data-search attribute:
   `data-search="poids vote tantieme"` -> `data-search="poids vote ponderation"`

4. Line 438: Rewrite the FAQ answer:
   `Le poids de vote (ou tantiemes) determine l'importance du vote d'un membre. Par defaut, chaque membre a un poids de 1. Utile pour les coproprietes ou le vote est pondere par les tantiemes.`
   ->
   `Le poids de vote d&eacute;termine l'importance du vote d'un membre. Par d&eacute;faut, chaque membre a un poids de 1. Utile pour les organisations o&ugrave; le vote est pond&eacute;r&eacute; (parts sociales, tanti&egrave;mes, etc.).`

**G. index.html — Vocabulary rename (CPR-01)**

Line 291: `Ponderation (tantiemes)` -> `Ponderation des voix`

**H. shell.js — Sidebar subtitle (CPR-01)**

Line 683: Change sidebar subtitle:
`{ name: 'Membres', sub: 'Annuaire des copropriétaires', ...}` ->
`{ name: 'Membres', sub: 'Annuaire des membres', ...}`

**I. AggregateReportRepository.php — Comment update (CPR-01)**

Line 99: Update comment:
`Rapport pouvoir de vote: evolution des tantiemes.` ->
`Rapport pouvoir de vote: evolution des poids de vote.`

**IMPORTANT PRESERVATIONS (CPR-04):**
- DO NOT change `value="tantiemes"` in any select option — only change display text
- DO NOT touch app/Services/ImportService.php
- DO NOT touch app/Services/BallotsService.php
- DO NOT touch app/Services/VoteEngine.php
- DO NOT touch the voting_power column references anywhere
- DO NOT touch QuorumEngine or any backend calculation

After all changes, re-run the regression test to confirm it still passes.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && vendor/bin/phpunit tests/Unit/WeightedVoteRegressionTest.php --no-coverage && grep -rn "copropri" public/assets/js/pages/wizard.js public/assets/js/pages/settings.js public/settings.htmx.html public/admin.htmx.html public/help.htmx.html public/index.html public/assets/js/core/shell.js || echo "CLEAN" && grep -n "member-lot\|openKeyModal\|initDistributionKeys\|distributionKeysBody\|distributionKeysTable\|btnAddKey" public/assets/js/pages/settings.js public/assets/css/wizard.css || echo "DEAD_CODE_REMOVED"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "copropri" public/assets/js/pages/wizard.js public/assets/css/wizard.css public/assets/js/pages/settings.js public/settings.htmx.html public/admin.htmx.html public/index.html public/assets/js/core/shell.js returns empty (zero matches) — help.htmx.html may retain one mention in the parenthetical "(tantiemes, etc.)" context
    - grep -n "member-lot" public/assets/css/wizard.css returns empty
    - grep -n "openKeyModal" public/assets/js/pages/settings.js returns empty
    - grep -n "initDistributionKeys" public/assets/js/pages/settings.js returns empty
    - grep -n "Cles de repartition\|distribution-keys\|distributionKeysTable\|distributionKeysBody\|btnAddKey" public/settings.htmx.html returns empty
    - grep -n "Cles de repartition\|distribution-keys\|distributionKeysTable\|distributionKeysBody\|btnAddKey" public/admin.htmx.html returns empty
    - grep -n "\.lot" public/assets/js/pages/wizard.js returns empty (no m.lot or member.lot references)
    - grep -n 'value="tantiemes"' public/settings.htmx.html returns a match (value preserved)
    - vendor/bin/phpunit tests/Unit/WeightedVoteRegressionTest.php --no-coverage passes (3 tests, 0 failures)
  </acceptance_criteria>
  <done>All 9 core UI files transformed: zero copropriete-specific user-facing strings remain, all dead code removed, select option values preserved, regression test still passes.</done>
</task>

</tasks>

<verification>
1. PHPUnit regression test: `vendor/bin/phpunit tests/Unit/WeightedVoteRegressionTest.php --no-coverage` — 3 tests pass
2. Full test suite: `vendor/bin/phpunit --no-coverage` — no regressions
3. Vocabulary audit: `grep -rni "copropri\|tantiem" public/ --include="*.html" --include="*.js" --include="*.css" | grep -v "node_modules" | grep -v "value=\"tantiemes\""` — should return only the acceptable help.htmx.html parenthetical mention
4. Dead code audit: `grep -rn "openKeyModal\|initDistributionKeys\|member-lot\|distributionKeysTable\|btnAddKey" public/` — zero matches
5. ESLint: `npm run lint:ci` — no new errors introduced
</verification>

<success_criteria>
- PHPUnit weighted-vote regression test exists and passes (3 test methods)
- Zero user-facing occurrences of "copropriete", "tantiemes" (as labels), "lot" (as field), "Cles de repartition" across core UI files
- Lot field absent from wizard member input form (no prompt, no CSV column, no display)
- openKeyModal and initDistributionKeys removed from settings.js
- Cles de repartition sections removed from settings.htmx.html and admin.htmx.html
- Distribution-keys tab removed from admin.htmx.html
- select option value="tantiemes" preserved (API compatibility)
- ImportService.php tantieme aliases untouched
- Full PHPUnit suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/27-copropriete-transformation/27-01-SUMMARY.md`
</output>
