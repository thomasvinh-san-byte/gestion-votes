---
phase: 07-dashboard-sessions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/dashboard.htmx.html
  - public/assets/js/pages/dashboard.js
  - public/assets/css/pages.css
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]

must_haves:
  truths:
    - "Dashboard displays 4 KPI cards (AG a venir, En cours, Convocations en attente, PV a envoyer) using .kpi-card class with hover lift"
    - "Urgent action card appears only when data.urgent_action exists, hidden otherwise, styled with CSS classes not inline styles"
    - "2-column layout shows upcoming sessions list (left) and prioritized task list with color indicators (right)"
    - "3 shortcut cards (Creer seance, Piloter vote, Piste audit) render with accent icon circles and CSS classes"
  artifacts:
    - path: "public/dashboard.htmx.html"
      provides: "Dashboard page with zero inline styles on KPI, urgent, shortcut, and grid elements"
      contains: "kpi-card"
    - path: "public/assets/js/pages/dashboard.js"
      provides: "Dashboard JS with refactored rendering using CSS classes instead of inline styles"
      contains: "kpi-card"
    - path: "public/assets/css/pages.css"
      provides: "Dashboard-specific CSS classes for urgent card, session rows, task rows, shortcut cards"
      contains: "urgent-card"
  key_links:
    - from: "public/assets/js/pages/dashboard.js"
      to: "/api/v1/dashboard"
      via: "Utils.apiGet fetch"
      pattern: "api.*dashboard"
    - from: "public/dashboard.htmx.html"
      to: "public/assets/css/pages.css"
      via: "link stylesheet"
      pattern: "pages\\.css"
---

<objective>
Refactor the dashboard page to replace all inline styles with proper CSS classes, use the existing .kpi-card pattern from design-system.css, and align the layout with wireframe v3.19.2.

Purpose: The dashboard currently uses extensive inline styles for KPI cards, urgent action card, session rows, task rows, and shortcut cards. This makes it impossible to theme consistently and violates the design-system token architecture established in phases 4-6.

Output: A clean dashboard.htmx.html with zero inline styles on layout elements, pages.css with reusable dashboard classes, and refactored dashboard.js using CSS classes in rendered HTML.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@public/dashboard.htmx.html
@public/assets/js/pages/dashboard.js
@public/assets/css/pages.css
@public/assets/css/design-system.css

<interfaces>
<!-- Existing design-system KPI pattern (design-system.css lines 1934-1970) -->
.kpi-grid — display: grid, auto-fit minmax(200px, 1fr), gap var(--space-4)
.kpi-card — surface bg, border, radius-lg, padding space-5, hover: translateY(-2px) + shadow-md
.kpi-value — text-4xl, extrabold, line-height 1
.kpi-value.primary / .success / .warning / .danger — semantic color variants
.kpi-label — text-sm, text-secondary, font-medium

<!-- Existing shared utilities -->
Shared.emptyState({ icon, title, description, actionHtml }) — returns empty state HTML
Utils.apiGet(url) — fetch wrapper returning promise

<!-- Established grid patterns in app.css -->
.grid-2 — 2-column grid
.grid-3 — 3-column grid
.grid-4 — 4-column grid
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor dashboard HTML and add CSS classes to pages.css</name>
  <files>public/dashboard.htmx.html, public/assets/css/pages.css</files>
  <action>
  1. In dashboard.htmx.html, refactor the KPI row (#kpiRow):
     - Replace the current `.card.kpi` elements with proper `.kpi-card` markup from design-system.css
     - Keep the `<a>` tag wrapper (clickable links per user decision)
     - Each KPI card: `.kpi-card` class, icon at top, `.kpi-value` with semantic color class (primary for AG a venir, danger for En cours, warning for Convocations, primary for PV), `.kpi-label` for text
     - Replace `style="font-family:var(--font-mono);font-size:22px;..."` with `.kpi-value` class
     - Replace `style="font-size:11px;font-weight:700;..."` with `.kpi-label` class
     - Use `.kpi-grid` wrapper instead of `.grid-4` for the row

  2. Refactor the urgent action card (#actionUrgente):
     - Remove ALL inline styles from the urgent action card
     - In pages.css, add `.urgent-card` class: margin-bottom 14px, border 2px solid var(--color-danger), padding 14px, cursor pointer, display block, text-decoration none
     - Add `.urgent-card-body`: display flex, align-items center, gap 14px, flex-wrap nowrap
     - Add `.urgent-card-icon`: 48px circle with border-radius 12px, background var(--color-danger), flex centered
     - Add `.urgent-card-label`: font-size 12px, font-weight 700, color var(--color-danger), uppercase, letter-spacing 1px
     - Add `.urgent-card-title`: font-size 16px, font-weight 800, color var(--color-text)
     - Add `.urgent-card-sub`: font-size 13px, color var(--color-text-muted)
     - Add `.urgent-card-chevron`: flex-shrink 0

  3. Refactor the 2-column grid section:
     - Remove `style="margin-bottom:14px;"` from `.grid-2`, move to pages.css as `.dashboard-grid { margin-bottom: 14px; }`
     - Remove `style="padding:16px;"` from card elements, add `.dashboard-panel` class in pages.css
     - Remove `style="margin-bottom:8px;"` from flex-between, use gap instead
     - Remove inline skeleton heights -- add `.skeleton-session { height: 60px; }` and `.skeleton-task { height: 48px; }` to pages.css

  4. Refactor the 3 shortcut cards:
     - Remove ALL inline styles from shortcut card links
     - In pages.css, add `.shortcut-card`: padding 14px, cursor pointer, display flex, align-items center, gap 12px, text-decoration none, flex-wrap nowrap
     - Add `.shortcut-card-icon`: 40px square, border-radius 10px, flex centered, flex-shrink 0
     - Add `.shortcut-card-icon.accent` / `.shortcut-card-icon.danger` / `.shortcut-card-icon.muted` for background colors
     - Add `.shortcut-card-title`: font-weight 700, color var(--color-text), font-size 14px
     - Add `.shortcut-card-sub`: color var(--color-text-muted), font-size 13px

  5. Keep all data-tour attributes intact. Keep all IDs unchanged. Keep the existing skip-link, header, sidebar, and footer structure untouched.
  </action>
  <verify>
    <automated>grep -c 'style=' public/dashboard.htmx.html | awk '{if ($1 <= 3) print "PASS: inline styles reduced to " $1; else print "FAIL: " $1 " inline styles remain"}'</automated>
  </verify>
  <done>Dashboard HTML has zero inline styles on KPI cards, urgent card, grid sections, and shortcut cards. All styling moved to CSS classes in pages.css. All IDs, links, and data-tour attributes preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor dashboard.js to use CSS classes in rendered HTML</name>
  <files>public/assets/js/pages/dashboard.js</files>
  <action>
  1. Refactor renderSeanceRow():
     - Replace all inline style strings with CSS classes
     - In pages.css (from Task 1), add: `.session-row` (display flex, align-items center, gap 8px, padding 8px 4px, cursor pointer, flex-wrap nowrap), `.session-row:not(:last-child)` (border-bottom 1px solid var(--color-border-soft))
     - `.session-dot` (10px circle, border-radius 50%, flex-shrink 0) — color set via inline `style="background:${color}"` (dynamic, acceptable)
     - `.session-row-info` (flex 1, min-width 0)
     - `.session-row-title` (font-weight 600, color var(--color-text), font-size 14px, overflow ellipsis)
     - `.session-row-meta` (font-size 13px, color var(--color-text-muted))
     - Remove the isLast border-bottom trick — use CSS :last-child instead

  2. Refactor renderTaskRow():
     - Replace inline styles with CSS classes
     - Add to pages.css: `.task-row` (display flex, align-items center, gap 10px, padding 6px 4px, border-radius 6px, cursor pointer), `.task-row:not(:last-child)` (border-bottom 1px solid var(--color-border-soft))
     - `.task-row-title` (font-weight 600, color var(--color-text), font-size 13px)
     - `.task-row-sub` (font-size 12px, color var(--color-text-muted))
     - Add task priority color support per user decision: `.task-row[data-priority="urgent"]` (border-left 3px solid var(--color-danger)), `.task-row[data-priority="high"]` (border-left 3px solid var(--color-warning)), `.task-row[data-priority="normal"]` (border-left 3px solid var(--color-primary))
     - Remove the isLast parameter — use CSS :last-child

  3. Refactor the urgent action hide/show:
     - Change `urgentCard.style.display = 'none'` to `urgentCard.hidden = true` (semantic HTML attribute)

  4. Refactor empty state fallback strings:
     - Replace inline styled empty divs with Shared.emptyState() calls where appropriate
     - For "Aucune seance a venir" and "Aucune tache", use `.session-row` / `.task-row` wrappers with centered text

  5. Keep the IIFE pattern, var keyword for global compat, and fallback data structure unchanged. Keep all getElementById calls and element IDs unchanged.
  </action>
  <verify>
    <automated>grep -c "style=" public/assets/js/pages/dashboard.js | awk '{if ($1 <= 2) print "PASS: JS inline styles reduced to " $1; else print "FAIL: " $1 " inline style usages remain"}'</automated>
  </verify>
  <done>dashboard.js renders HTML using CSS classes instead of inline styles. Session rows use .session-row, task rows use .task-row with priority colors, urgent card uses hidden attribute. Fallback data still works.</done>
</task>

</tasks>

<verification>
1. Open dashboard.htmx.html in browser — 4 KPI cards display with correct colors, hover lift effect works
2. Urgent action card shows when data exists, hidden when no urgent action
3. 2-column layout renders sessions left, tasks right
4. 3 shortcut cards display with icon circles
5. No visual regression from inline style removal — all design tokens applied via CSS classes
6. `grep -c 'style=' public/dashboard.htmx.html` returns 3 or fewer (only footer/header utility styles acceptable)
7. `grep -c 'style=' public/assets/js/pages/dashboard.js` returns 2 or fewer (only dynamic background color on status dots)
</verification>

<success_criteria>
- Dashboard renders identically to current layout but with CSS classes instead of inline styles
- KPI cards use .kpi-card / .kpi-grid from design-system.css with semantic color variants
- Urgent card uses .urgent-card CSS class with conditional hidden attribute
- Session rows and task rows use CSS classes with :last-child borders
- Task rows support priority color indicators via data-priority attribute
- All 4 DASH requirements visually met
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-sessions/07-01-SUMMARY.md`
</output>
