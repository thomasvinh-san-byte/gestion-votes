---
phase: 07-dashboard-sessions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/meetings.htmx.html
  - public/assets/css/meetings.css
autonomous: true
requirements: [SESS-01, SESS-02, SESS-03, SESS-05]

must_haves:
  truths:
    - "Sessions page has no inline creation wizard (wizard lives at wizard.htmx.html)"
    - "Stats bar is replaced by filter pills (Toutes, A venir, En cours, Terminees) with count badges"
    - "Sessions toolbar has search bar, sort dropdown, and list/calendar view toggle"
    - "Session list items show status dot, title, date, participants, resolutions, quorum, status tag, and popover menu trigger"
    - "Empty state displays when no sessions match current filter"
  artifacts:
    - path: "public/meetings.htmx.html"
      provides: "Sessions page with filter pills, search/sort bar, session list items with full metadata, calendar container, no inline wizard"
      contains: "filter-pill"
    - path: "public/assets/css/meetings.css"
      provides: "CSS for filter pills with counts, session list items with responsive hiding, popover trigger, empty state"
      contains: "filter-pill"
  key_links:
    - from: "public/meetings.htmx.html"
      to: "public/assets/css/meetings.css"
      via: "link stylesheet"
      pattern: "meetings\\.css"
    - from: "public/meetings.htmx.html"
      to: "public/assets/js/pages/meetings.js"
      via: "script src"
      pattern: "meetings\\.js"
---

<objective>
Restructure the sessions page HTML and CSS: remove the inline creation wizard, replace the stats bar with filter pills, and design the session list item layout with full metadata and responsive behavior.

Purpose: The current meetings.htmx.html has a 250-line inline wizard that duplicates wizard.htmx.html, a stats bar that doesn't match the wireframe, and session items that lack the full metadata required by SESS-03. This plan restructures the HTML skeleton and CSS that Plan 03's JS rewrite will populate.

Output: Clean meetings.htmx.html with filter pills, search/sort toolbar, session list item template structure, and meetings.css with all required styles.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@public/meetings.htmx.html
@public/assets/css/meetings.css

<interfaces>
<!-- Existing patterns from design-system.css and shared components -->
Shared.emptyState({ icon, title, description, actionHtml }) — empty state HTML generator
ag-popover — Web component for dropdown action menus (already registered)
ag-badge — Tag/badge component with color variants (danger, success, warn, accent, purple)
.card — base card class from design-system.css
.btn, .btn-primary, .btn-ghost, .btn-sm — button classes
.form-input — input styling class

<!-- Filter tabs already exist in meetings.htmx.html (lines 357-363) -->
<!-- View toggle already exists (lines 385-393) -->
<!-- Calendar container already exists (lines 407-423) -->
<!-- Edit + Delete modals remain unchanged (lines 441-499) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure meetings.htmx.html — remove wizard, replace stats bar, add filter pills</name>
  <files>public/meetings.htmx.html</files>
  <action>
  1. REMOVE the entire inline wizard block (lines 97-354, from `<div class="create-card" id="wizardCard">` through `</div>` for wizStep5). This is ~258 lines. The wizard.htmx.html page handles creation (Phase 8 scope).

  2. REPLACE the stats bar (lines 67-92, `<div class="stats-bar">...</div>`) with filter pills:
     ```html
     <div class="filter-pills" id="filterPills" role="tablist" aria-label="Filtrer les seances">
       <button class="filter-pill active" data-filter="all" role="tab" aria-selected="true">
         Toutes <span class="filter-pill-count" id="countAll">0</span>
       </button>
       <button class="filter-pill" data-filter="upcoming" role="tab" aria-selected="false">
         A venir <span class="filter-pill-count" id="countUpcoming">0</span>
       </button>
       <button class="filter-pill" data-filter="live" role="tab" aria-selected="false">
         En cours <span class="filter-pill-count" id="countLive">0</span>
       </button>
       <button class="filter-pill" data-filter="completed" role="tab" aria-selected="false">
         Terminees <span class="filter-pill-count" id="countCompleted">0</span>
       </button>
     </div>
     ```
     Use HTML entities for accented characters (A&#768; venir, Termin&#233;es).

  3. UPDATE the existing filter-tabs block (lines 357-363) — REMOVE it entirely since filter pills above replace it.

  4. REFACTOR the header: Add breadcrumb nav matching dashboard pattern, add page-title with bar+icon+text, add "Nouvelle seance" primary button linking to /wizard.htmx.html (matching dashboard header pattern).

  5. RESTRUCTURE the meetings toolbar (lines 366-378):
     - Keep search input with id="meetingsSearch"
     - Keep sort dropdown with id="meetingsSort" — update options to: Date (recent), Date (ancien), Titre A-Z, Titre Z-A
     - Move the view toggle (list/calendar) into the toolbar row, right-aligned
     - Add a count display: `<span class="meetings-result-count" id="meetingsCount">0 seances</span>`

  6. RESTRUCTURE the meetings list container (#meetingsList):
     - Change from `.meetings-grid` to `.sessions-list` class (it's a vertical list, not a grid)
     - Keep skeleton placeholders but use `.skeleton-session-item` class

  7. KEEP unchanged: onboarding banner (but update dismiss logic — add `id="onboardingBanner"` if missing, add data attribute `data-session-driven="true"` so JS can check session count instead of just localStorage), edit modal, delete modal, drawer, calendar container, footer, all script tags.

  8. UPDATE the page CSS link: keep `meetings.css` (no change needed since that's the file).

  9. KEEP the `data-tour` attributes but remove any that reference removed wizard elements (data-tour="hub-preparation", data-tour="hub-stepper", data-tour="wizard-form"). Keep data-tour="hub-action" on onboarding banner and data-tour="hub-kpis" — move it to filter pills.
  </action>
  <verify>
    <automated>bash -c 'HTML="public/meetings.htmx.html"; echo "Wizard removed: $(grep -c "wizardCard\|wizStep\|wiz-step-panel\|wiz-form-grid\|wizStepper" "$HTML") refs (should be 0)"; echo "Filter pills: $(grep -c "filter-pill" "$HTML") refs (should be > 0)"; echo "Stats bar: $(grep -c "stats-bar\|stat-item\|stat-value\|stat-label" "$HTML") refs (should be 0)"; echo "Filter tabs old: $(grep -c "filter-tab" "$HTML") refs (should be 0)"'</automated>
  </verify>
  <done>meetings.htmx.html has no inline wizard, no stats bar, no old filter tabs. Filter pills with count spans exist. Header has breadcrumb + "Nouvelle seance" button. Sessions list uses .sessions-list container.</done>
</task>

<task type="auto">
  <name>Task 2: Write meetings.css styles for filter pills, session list items, and responsive behavior</name>
  <files>public/assets/css/meetings.css</files>
  <action>
  1. REMOVE the stats-bar CSS block (lines 12-52 in meetings.css) — no longer used.

  2. REMOVE the create-card CSS block (lines 74+) — wizard removed from this page.

  3. ADD filter pills CSS:
     - `.filter-pills`: display flex, gap 8px, padding 0 1.5rem, margin-bottom 1rem, flex-wrap wrap
     - `.filter-pill`: padding 6px 14px, border-radius var(--radius-full, 999px), border 1px solid var(--color-border), background var(--color-surface), color var(--color-text-secondary), font-size 13px, font-weight 600, cursor pointer, transition background var(--duration-fast), display inline-flex, align-items center, gap 6px
     - `.filter-pill:hover`: background var(--color-bg-subtle)
     - `.filter-pill.active`: background var(--color-primary), color white, border-color var(--color-primary)
     - `.filter-pill-count`: font-size 11px, font-weight 700, background color-mix(in srgb, currentColor 15%, transparent), padding 1px 6px, border-radius var(--radius-full), min-width 20px, text-align center
     - `.filter-pill.active .filter-pill-count`: background rgba(255,255,255,0.25)

  4. ADD session list item CSS:
     - `.sessions-list`: display flex, flex-direction column, gap 0
     - `.session-item`: display flex, align-items center, gap 12px, padding 12px 16px, background var(--color-surface), border 1px solid var(--color-border), border-radius var(--radius-md), cursor pointer, text-decoration none, transition background var(--duration-fast), margin-bottom 8px
     - `.session-item:hover`: background var(--color-bg-subtle)
     - `.session-dot`: width 10px, height 10px, border-radius 50%, flex-shrink 0
     - `.session-dot.upcoming`: background var(--color-primary)
     - `.session-dot.live`: background var(--color-danger), add pulse animation
     - `.session-dot.completed`: background var(--color-text-muted)
     - `.session-dot.draft`: background var(--color-border)
     - `.session-info`: flex 1, min-width 0
     - `.session-title`: font-weight 600, font-size 14px, color var(--color-text), overflow hidden, text-overflow ellipsis, white-space nowrap
     - `.session-meta`: font-size 12px, color var(--color-text-muted), display flex, gap 12px, flex-wrap wrap, margin-top 2px
     - `.session-meta-item`: display inline-flex, align-items center, gap 4px
     - `.session-actions`: flex-shrink 0, display flex, align-items center, gap 8px

  5. ADD responsive hiding per user decision:
     - Default (desktop): show all fields
     - `@media (max-width: 1024px)`: hide `.session-meta-item.quorum` and `.session-meta-item.resolutions` (tablet)
     - `@media (max-width: 640px)`: hide all `.session-meta` except keep date visible via `.session-meta-item.date` — show only status dot, title, date, and status tag

  6. ADD skeleton styles:
     - `.skeleton-session-item`: height 56px, border-radius var(--radius-md), margin-bottom 8px

  7. ADD meetings toolbar refinement:
     - `.meetings-toolbar`: display flex, align-items center, gap 12px, padding 0 1.5rem, margin-bottom 12px, flex-wrap wrap
     - `.meetings-result-count`: font-size 13px, color var(--color-text-muted), margin-left auto
     - `.view-toggle`: move existing styles to meetings.css if not already there

  8. ADD session empty state wrapper:
     - `.sessions-empty`: padding 2rem 1.5rem

  9. KEEP all calendar CSS unchanged (it already exists and works).

  10. KEEP existing modal, drawer, wizard step CSS that might be shared — but remove any CSS that was ONLY used by the removed wizard HTML (wiz-progress-wrap, wiz-step-item, wiz-step-dot, wiz-step-label, wiz-step-panel, wiz-form-grid, wiz-nav, wiz-deadline-alert, wiz-recap-grid, wiz-recap-card, wiz-recap-value, wiz-success) ONLY if these classes exist in meetings.css. Check first — they may be in pages.css or design-system.css instead.
  </action>
  <verify>
    <automated>bash -c 'CSS="public/assets/css/meetings.css"; echo "Filter pills: $(grep -c "filter-pill" "$CSS") rules (should be > 0)"; echo "Session item: $(grep -c "session-item\|session-dot\|session-info\|session-title\|session-meta" "$CSS") rules (should be > 0)"; echo "Stats bar removed: $(grep -c "stats-bar" "$CSS") rules (should be 0)"'</automated>
  </verify>
  <done>meetings.css has filter pill styles with active state and count badges, session list item styles with status dots and responsive hiding rules, no stats-bar CSS, no wizard-only CSS. Skeleton and empty state styles present.</done>
</task>

</tasks>

<verification>
1. meetings.htmx.html loads without console errors
2. Filter pills render as horizontal row with count badges
3. Session list container is present with skeleton placeholders
4. No wizard HTML remains on the page
5. No stats bar HTML or CSS remains
6. Calendar container structure is preserved
7. Edit and delete modals are intact
8. Responsive: quorum/resolutions hidden on tablet, minimal display on mobile
</verification>

<success_criteria>
- Inline wizard completely removed (0 references to wizardCard, wizStep, wiz-step-panel)
- Stats bar replaced by filter pills (Toutes, A venir, En cours, Terminees) with count spans
- Session list items have full metadata structure in CSS (dot, title, date, participants, resolutions, quorum, tag, actions)
- Responsive hiding rules work at tablet (1024px) and mobile (640px) breakpoints
- Header matches dashboard pattern with breadcrumb and "Nouvelle seance" button
- Page loads without errors (CSS references valid classes)
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard-sessions/07-02-SUMMARY.md`
</output>
