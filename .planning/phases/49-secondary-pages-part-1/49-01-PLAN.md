---
phase: 49-secondary-pages-part-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/postsession.htmx.html
  - public/assets/css/postsession.css
  - public/assets/js/pages/postsession.js
autonomous: true
requirements: [REB-01, WIRE-01, WIRE-02]

must_haves:
  truths:
    - "Postsession stepper shows 4 pill-shaped steps with active/done states"
    - "Step 1 loads result cards with bar charts from /api/meeting_results.php"
    - "Step 2 shows validation KPIs and validate/reject buttons that call /api/validate_results.php"
    - "Step 3 has signataires, observations, eIDAS chips, generate PV button, PDF export link"
    - "Step 4 has email send form, export list, archive button"
    - "Footer nav advances steps with Precedent/Suivant buttons"
    - "Dark mode works via design tokens — no hardcoded colors"
    - "No JS console errors — all DOM IDs preserved for postsession.js"
  artifacts:
    - path: "public/postsession.htmx.html"
      provides: "Complete postsession page with 4-step guided workflow, app-shell layout"
      contains: "ps-stepper"
    - path: "public/assets/css/postsession.css"
      provides: "Postsession styles — stepper, panels, result cards, footer nav, responsive"
      contains: "ps-stepper"
    - path: "public/assets/js/pages/postsession.js"
      provides: "Postsession JS — stepper navigation, API calls, result rendering"
      contains: "goToStep"
  key_links:
    - from: "public/postsession.htmx.html"
      to: "public/assets/js/pages/postsession.js"
      via: "DOM IDs preserved for JS binding"
      pattern: "panel-1|panel-2|panel-3|panel-4|stepper|psStepCounter|btnPrecedent|btnSuivant|resultCardsContainer|verifyAlert|btnValidate|btnReject|btnGenerateReport|btnExportPDF|pvPreview|pvHash|btnSendReport|btnArchive|completeBanner|meetingPicker|meetingPickerSelect|meetingTitle"
    - from: "public/assets/js/pages/postsession.js"
      to: "/api/meeting_results.php"
      via: "fetch calls for verification and validation"
      pattern: "api.*meeting_results|api.*validate_results|api.*generate_pv|api.*archive"
---

<objective>
Ground-up rebuild of the postsession page — complete HTML+CSS rewrite from scratch with the v4.3 design language (gradient accent bars, shadow-md cards, token-based dark mode). Preserve all DOM IDs that postsession.js targets. Verify all 4 stepper steps render and JS wires correctly.

Purpose: Postsession is the most complex secondary page (4-step stepper with result cards, validation KPIs, PV generation, email+archive). Rebuilding it first establishes the pattern for multi-step workflow pages.
Output: New postsession.htmx.html + postsession.css, with postsession.js verified working.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-secondary-pages-part-1/49-CONTEXT.md

@public/postsession.htmx.html
@public/assets/css/postsession.css
@public/assets/js/pages/postsession.js
@public/assets/css/app.css
@public/hub.htmx.html
@public/dashboard.htmx.html

<interfaces>
<!-- Critical DOM IDs that postsession.js targets — ALL must exist in new HTML -->

Stepper navigation:
- .ps-seg[data-step] — 4 step segments (1-4), JS toggles active/done classes
- #panel-1, #panel-2, #panel-3, #panel-4 — step panels (use hidden attribute)

Footer nav:
- #psStepCounter — "Etape X / 4" text
- #btnPrecedent — previous button (hidden on step 1)
- #btnSuivant — next button (disabled until conditions met)
- #psFooterNav — footer nav container

Step 1 (Verification):
- #resultCardsContainer — container for dynamically rendered result cards
- #verifyAlert — success alert (hidden initially)
- #resultsTableBody — legacy table body (hidden, backward compat)

Step 2 (Validation):
- #vkpiResolutions, #vkpiAdopted, #vkpiRejected, #vkpiParticipation — KPI values
- #wsStep-closed, #wsStep-validated, #wsStep-archived — workflow state dots
- #transitionActions — dynamic action buttons container
- #btnValidate, #btnReject — static validate/reject buttons
- #workflowState — workflow state container

Step 3 (PV):
- #sigPresident, #sigSecretary, #sigScrutateur1, #sigScrutateur2 — signataire inputs
- #pvObservations, #pvReserves — textarea fields
- #eidasChips — eIDAS chip group, .chip[data-eidas] buttons
- #btnSignPresident, #btnSignSecretary — sign buttons
- #sigCounter — signature count tag
- #btnGenerateReport — generate PV button
- #btnExportPDF — PDF export link
- #pvPreview — preview container
- #pvHashBar, #pvHash — integrity hash display

Step 4 (Send & Archive):
- #pvSummaryTitle, #pvSummaryResolutions, #pvSummaryRate — PV summary values
- #pvSummaryDownload — PDF download link
- #sendTo — recipient select, #customEmail — custom email input, #customEmailGroup — group container
- #btnSendReport — send button, #recipientCount — count display
- #exportPvPdf, #exportEmargement, #exportAttendanceCsv, #exportVotesCsv, #exportResultsCsv, #exportAuditCsv — export links
- #btnArchive — archive button
- #completeBanner — completion banner (hidden initially)
- #archiveCard — archive card container

Meeting picker:
- #meetingPicker — picker card (hidden when meeting in context)
- #meetingPickerSelect — meeting dropdown
- #meetingTitle — header subtitle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild postsession HTML+CSS from scratch</name>
  <files>public/postsession.htmx.html, public/assets/css/postsession.css</files>
  <read_first>
    - public/postsession.htmx.html (current HTML — extract all DOM IDs)
    - public/assets/js/pages/postsession.js (JS — understand ALL getElementById/querySelector calls)
    - public/assets/css/postsession.css (current CSS — understand structure)
    - public/assets/css/app.css (design system tokens and utility classes)
    - public/hub.htmx.html (reference for v4.3 hero card pattern)
    - public/dashboard.htmx.html (reference for v4.3 card layout)
  </read_first>
  <action>
    Rewrite postsession.htmx.html from scratch with the v4.3 design language:

    1. **HTML structure**: app-shell layout (sidebar, header with gradient accent bar, main, footer). Use the postsession violet accent (--persona-postsession).

    2. **Stepper bar**: Keep the 4 pill-shaped .ps-seg[data-step] segments inside #stepper. JS expects to querySelectorAll('.ps-seg') and toggle active/done classes. Keep ag-tooltip wrappers.

    3. **Step panels**: 4 sections with ids panel-1 through panel-4, using hidden attribute. Each panel gets the v4.3 card treatment (shadow-md, rounded-lg, proper spacing).

    4. **Step 1 — Verification**: #resultCardsContainer for dynamic cards, #verifyAlert success alert, hidden legacy table with #resultsTableBody. Use card with card-header + card-body pattern.

    5. **Step 2 — Validation**: .ps-validation-kpis grid with 4 KPI items (#vkpiResolutions, #vkpiAdopted, #vkpiRejected, #vkpiParticipation). Workflow state dots (#wsStep-closed, #wsStep-validated, #wsStep-archived). Irreversibility warning card. #btnValidate and #btnReject buttons.

    6. **Step 3 — PV**: Signataires card (2x2 grid: #sigPresident, #sigSecretary, #sigScrutateur1, #sigScrutateur2). Observations card (#pvObservations, #pvReserves textareas). eIDAS card (#eidasChips with .chip[data-eidas] buttons, #btnSignPresident, #btnSignSecretary, #sigCounter). Preview card (#btnGenerateReport, #btnExportPDF, #pvPreview, #pvHashBar, #pvHash).

    7. **Step 4 — Send & Archive**: PV summary card (#pvSummaryTitle, #pvSummaryResolutions, #pvSummaryRate, #pvSummaryDownload). Two-column layout: exports list (6 export links by ID) + email send card (#sendTo, #customEmail, #customEmailGroup, #btnSendReport). Archive card (#btnArchive). #completeBanner hidden.

    8. **Footer nav**: #psFooterNav with #psStepCounter, #btnPrecedent, #btnSuivant.

    9. **Meeting picker**: #meetingPicker card with #meetingPickerSelect dropdown.

    10. **Scripts**: Same script order — htmx, utils, shared, shell, meeting-context, ag-searchable-select, ag-popover, ag-tooltip, postsession.js.

    Rewrite postsession.css from scratch:
    - Use design tokens (--space-*, --shadow-*, --radius-*, --color-*) throughout
    - Stepper: sticky, pill segments with active/done states, check icon for completed
    - Panels: card-based layout with shadow-md
    - Result cards container: grid layout for result cards
    - Validation KPIs: horizontal 4-column grid
    - Workflow state: horizontal dots with connecting line
    - Signataires: 2x2 grid using gap
    - eIDAS chips: inline flex group
    - Exports layout: 2-column grid (exports left, send+archive right)
    - Footer nav: sticky bottom bar
    - Meeting picker: centered card
    - Responsive: single column below 768px
    - No hardcoded colors — all via var() tokens
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c 'panel-1\|panel-2\|panel-3\|panel-4' public/postsession.htmx.html && grep -c 'ps-stepper\|ps-seg' public/postsession.htmx.html && grep -c 'btnValidate\|btnReject\|btnGenerateReport\|btnExportPDF\|btnSendReport\|btnArchive' public/postsession.htmx.html && grep -c 'resultCardsContainer\|verifyAlert\|pvPreview\|pvHash\|completeBanner' public/postsession.htmx.html && grep -c 'var(--' public/assets/css/postsession.css</automated>
  </verify>
  <acceptance_criteria>
    - grep 'id="panel-1"' public/postsession.htmx.html returns match
    - grep 'id="panel-2"' public/postsession.htmx.html returns match
    - grep 'id="panel-3"' public/postsession.htmx.html returns match
    - grep 'id="panel-4"' public/postsession.htmx.html returns match
    - grep 'id="stepper"' public/postsession.htmx.html returns match
    - grep 'id="resultCardsContainer"' public/postsession.htmx.html returns match
    - grep 'id="btnValidate"' public/postsession.htmx.html returns match
    - grep 'id="btnGenerateReport"' public/postsession.htmx.html returns match
    - grep 'id="btnExportPDF"' public/postsession.htmx.html returns match
    - grep 'id="btnSendReport"' public/postsession.htmx.html returns match
    - grep 'id="btnArchive"' public/postsession.htmx.html returns match
    - grep 'id="pvPreview"' public/postsession.htmx.html returns match
    - grep 'id="psStepCounter"' public/postsession.htmx.html returns match
    - grep 'id="meetingPicker"' public/postsession.htmx.html returns match
    - grep 'id="completeBanner"' public/postsession.htmx.html returns match
    - grep 'var(--' public/assets/css/postsession.css returns 20+ matches (all tokens)
    - No hardcoded hex colors in postsession.css (grep '#[0-9a-fA-F]' returns 0 matches outside comments)
    - grep 'postsession.js' public/postsession.htmx.html returns match (script included)
  </acceptance_criteria>
  <done>Postsession HTML completely rewritten with v4.3 design language. All 4 step panels present with correct IDs. CSS uses design tokens exclusively. All DOM IDs that postsession.js targets are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Verify postsession JS wiring and fix broken selectors</name>
  <files>public/assets/js/pages/postsession.js</files>
  <read_first>
    - public/postsession.htmx.html (the NEW rebuilt HTML from Task 1)
    - public/assets/js/pages/postsession.js (current JS — check all selectors against new HTML)
  </read_first>
  <action>
    After the HTML rebuild, verify that every getElementById/querySelector call in postsession.js matches an element in the new HTML:

    1. Cross-reference every DOM ID used in postsession.js against the new HTML. Create a checklist of all IDs and confirm each exists.

    2. Check class-based selectors: '.ps-seg', '.chip', '.ps-seg-num' — confirm these classes exist in new HTML.

    3. If any selectors are broken due to HTML restructuring, update postsession.js to match. Common fixes:
       - Class name changes (update querySelector strings)
       - New wrapper elements (update DOM traversal)
       - Attribute changes (update getAttribute calls)

    4. Verify API endpoint paths in fetch calls are correct:
       - /api/meeting_results.php (step 1 data)
       - /api/validate_results.php (step 2 action)
       - /api/generate_pv.php or similar (step 3 PV generation)
       - /api/archive_meeting.php or similar (step 4 archive)

    5. Add error banner pattern if not present: on API failure, show a visible error message rather than silent failure. Use the ag-toast pattern (setNotif from utils.js).

    6. Ensure the meeting picker flow works: if no meeting_id in URL params, show #meetingPicker; on selection, store in meeting-context service and reload.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && node -e "const fs=require('fs');const html=fs.readFileSync('public/postsession.htmx.html','utf8');const js=fs.readFileSync('public/assets/js/pages/postsession.js','utf8');const ids=[...js.matchAll(/getElementById\(['\"]([^'\"]+)['\"]\)/g)].map(m=>m[1]);const missing=ids.filter(id=>!html.includes('id=\"'+id+'\"'));if(missing.length){console.error('MISSING IDs:',missing);process.exit(1)}else{console.log('All',ids.length,'IDs found in HTML')}"</automated>
  </verify>
  <acceptance_criteria>
    - The node verification script exits 0 (all getElementById targets exist in HTML)
    - grep 'setNotif\|AgToast' public/assets/js/pages/postsession.js returns matches (error handling present)
    - grep 'goToStep' public/assets/js/pages/postsession.js returns matches (stepper navigation works)
    - No syntax errors: node -c public/assets/js/pages/postsession.js exits 0
  </acceptance_criteria>
  <done>All postsession.js selectors match the rebuilt HTML. No broken DOM references. API calls point to real endpoints. Error handling uses toast notifications.</done>
</task>

</tasks>

<verification>
1. Open postsession page in browser — stepper shows 4 steps, step 1 active
2. Result cards area renders (or shows loading state if no meeting selected)
3. Meeting picker appears when no meeting in context
4. Navigate between steps with footer nav buttons
5. No JS console errors on page load
6. Dark mode toggle works — no hardcoded colors visible
</verification>

<success_criteria>
- Postsession page fully rebuilt with v4.3 design language
- All 4 stepper steps functional with correct content
- All DOM IDs preserved — postsession.js binds without errors
- API connections verified (validate, generate PV, archive)
- Dark mode works via tokens
- No JS console errors
</success_criteria>

<output>
After completion, create `.planning/phases/49-secondary-pages-part-1/49-01-SUMMARY.md`
</output>
