---
phase: 49-secondary-pages-part-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/analytics.htmx.html
  - public/assets/css/analytics.css
  - public/assets/js/pages/analytics-dashboard.js
autonomous: true
requirements: [REB-02, WIRE-01]

must_haves:
  truths:
    - "Analytics page has 4 KPI cards in horizontal grid (Seances, Resolutions, Taux adoption, Participation)"
    - "Period pill filter bar (7j, 30j, 90j, 1an, Tout) switches chart data"
    - "4 tab buttons (Participation, Resolutions, Temps de vote, Anomalies) switch content panels"
    - "Participation tab: hero line chart + 2-col grid (sessions by month bar chart + detail table)"
    - "Motions tab: SVG donut chart + 3-col grid of Chart.js canvases"
    - "Timing tab: hero duration chart + 2-col grid (avg duration + response time)"
    - "Anomalies tab: 2-col grid (quality indicators + sessions to verify)"
    - "Year filter dropdown in header changes data scope"
    - "Dark mode works — Chart.js colors read from CSS variables"
    - "No JS console errors — all canvas IDs and DOM IDs preserved"
  artifacts:
    - path: "public/analytics.htmx.html"
      provides: "Complete analytics page with KPI grid, period pills, 4 tabs, chart containers"
      contains: "analytics-kpi-grid"
    - path: "public/assets/css/analytics.css"
      provides: "Analytics styles — KPI grid, chart cards, filter bar, tabs, donut, responsive"
      contains: "analytics-kpi-grid"
    - path: "public/assets/js/pages/analytics-dashboard.js"
      provides: "Analytics JS — Chart.js charts, data loading, tab/filter switching"
      contains: "loadAllData"
  key_links:
    - from: "public/analytics.htmx.html"
      to: "public/assets/js/pages/analytics-dashboard.js"
      via: "DOM IDs for KPIs, canvases, tabs, filters"
      pattern: "kpiMeetings|kpiResolutions|kpiAdoptionRate|kpiParticipation|participationChart|sessionsByMonthChart|motionsChart|majorityChart|motionsTrendChart|durationChart|avgDurationChart|responseTimeChart|yearFilter|refreshBtn|btnExportPdf|btnExportCsv"
    - from: "public/assets/js/pages/analytics-dashboard.js"
      to: "/api/analytics_stats.php"
      via: "fetch calls for statistics data"
      pattern: "api.*analytics|api.*stats"
---

<objective>
Ground-up rebuild of the analytics/statistics page — complete HTML+CSS rewrite with v4.3 design language. Preserve all canvas IDs and DOM selectors that analytics-dashboard.js targets. KPI cards, Chart.js charts, SVG donut, 4 tabs, period/year filters all functional.

Purpose: Analytics is a data-dense page with charts. The rebuild applies card-based design with proper chart containers and responsive grid.
Output: New analytics.htmx.html + analytics.css, with analytics-dashboard.js verified working.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-secondary-pages-part-1/49-CONTEXT.md

@public/analytics.htmx.html
@public/assets/css/analytics.css
@public/assets/js/pages/analytics-dashboard.js
@public/assets/css/app.css
@public/dashboard.htmx.html

<interfaces>
<!-- DOM IDs that analytics-dashboard.js targets — ALL must exist in new HTML -->

KPI cards:
- #kpiMeetings — total meetings value
- #kpiResolutions — total resolutions value
- #kpiAdoptionRate — adoption rate value
- #kpiParticipation — participation rate value
- #kpiMeetingsTrend, #kpiResolutionsTrend, #kpiAdoptionTrend, #kpiParticipationTrend — trend containers with .trend-arrow and .trend-text children
- #overviewCards — KPI grid container (aria-live="polite")

Filters:
- .analytics-period-pill[data-period] — period filter buttons (7j, 30j, 90j, 1an, all)
- #yearFilter — year dropdown select
- #refreshBtn — refresh button
- #btnExportPdf — PDF export button
- #btnExportCsv — CSV export button

Tabs:
- .analytics-tab[data-tab] — tab buttons (participation, motions, timing, anomalies)
- #tab-participation, #tab-motions, #tab-timing, #tab-anomalies — tab content panels
- .tab-content — all tab panels, use .active class for visibility
- #tabBadgeAnomalies — anomalies count badge

Chart canvases (ALL required — Chart.js binds to these):
- #participationChart — participation line chart
- #sessionsByMonthChart — sessions bar chart
- #participationTable — participation detail table container
- #motionsChart — motions bar chart
- #majorityChart — majority type chart
- #motionsTrendChart — motions trend line chart
- #durationChart — vote duration chart
- #avgDurationChart — average duration bar chart
- #responseTimeChart — response time chart

SVG Donut (motions tab):
- #donutFor, #donutAgainst, #donutAbstain — SVG circle segments
- #donutTotal — center total value
- #donutForPct, #donutAgainstPct, #donutAbstainPct — legend percentages
- #donutSection — donut section container

Anomalies tab:
- #anomaliesOverview — quality indicators container
- #anomaliesMeetings — sessions to verify container

Chart export:
- .chart-export-btn[data-chart-id] — per-chart export buttons
- .chart-container — chart wrapper divs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild analytics HTML+CSS from scratch</name>
  <files>public/analytics.htmx.html, public/assets/css/analytics.css</files>
  <read_first>
    - public/analytics.htmx.html (current HTML — extract all DOM IDs and canvas elements)
    - public/assets/js/pages/analytics-dashboard.js (ALL getElementById/querySelector calls)
    - public/assets/css/analytics.css (current CSS structure)
    - public/assets/css/app.css (design system tokens)
    - public/dashboard.htmx.html (reference for v4.3 KPI card pattern)
  </read_first>
  <action>
    Rewrite analytics.htmx.html from scratch with v4.3 design language:

    1. **HTML structure**: app-shell layout (sidebar, header, main, footer). Header contains h1 with bar-chart icon, subtitle, year filter dropdown, PDF/CSV export buttons, help popover, drawer toggle.

    2. **Privacy notice**: alert-info banner about aggregated statistics.

    3. **KPI grid**: #overviewCards with 4 .kpi-card cards (kpiMeetings, kpiResolutions, kpiAdoptionRate, kpiParticipation). Each card has: colored icon container, value, label, trend with arrow+text. Use ag-tooltip wrappers. Apply v4.3 kpi-card pattern with numbered variants (kpi-card--1 through kpi-card--4).

    4. **Period filter bar**: .analytics-filter-bar with .analytics-period-pill buttons (7j, 30j, 90j, 1an, all — data-period attributes). Refresh button #refreshBtn.

    5. **Tabs**: .analytics-tabs container with 4 .analytics-tab[data-tab] buttons. Anomalies tab gets #tabBadgeAnomalies span.

    6. **Tab: Participation** (#tab-participation, .tab-content.active): Hero chart card with #participationChart canvas. 2-col charts-grid: #sessionsByMonthChart canvas + #participationTable detail container. Each chart card has header (title+subtitle+tooltip), export button, .chart-container with canvas.

    7. **Tab: Motions** (#tab-motions): SVG donut section (#donutSection) with horizontal card layout — donut SVG (#donutFor, #donutAgainst, #donutAbstain circles, #donutTotal center), vertical legend (#donutForPct, #donutAgainstPct, #donutAbstainPct). 3-col charts-grid: #motionsChart, #majorityChart, #motionsTrendChart canvases.

    8. **Tab: Timing** (#tab-timing): Hero #durationChart. 2-col grid: #avgDurationChart + #responseTimeChart.

    9. **Tab: Anomalies** (#tab-anomalies): 2-col grid: #anomaliesOverview + #anomaliesMeetings containers with skeleton loading states.

    10. **Drawer**: Standard drawer structure (#drawer, #drawerTitle, #drawerBody).

    11. **Scripts**: components/index.js (module), utils, shared, shell, meeting-context, analytics-dashboard.js. Keep chart.umd.js vendor script in head.

    Rewrite analytics.css from scratch:
    - KPI grid: 4-column grid at lg, 2-column at md, 1-column at sm. Cards use shadow-md, rounded corners, icon containers with colored backgrounds.
    - Analytics-specific KPI card styles (.an-kpi) with icon circles (.an-kpi-icon--primary/success/warning/info).
    - Filter bar: flex row with pill buttons, active state uses primary color.
    - Tabs: horizontal button row, active underline indicator.
    - Chart cards: shadow-md, rounded-lg, header with title+subtitle, .chart-container with min-height 250px.
    - Hero chart card: full-width, larger min-height (350px).
    - Charts grid: 2-col default, 3-col variant (.charts-grid--3col).
    - Donut section: horizontal card layout (SVG left, legend right), responsive stacking.
    - Skeleton states for loading.
    - Year filter: compact select in header.
    - All via design tokens — no hardcoded colors.
    - Responsive: stacking grids, scrollable tables at mobile.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c 'participationChart\|sessionsByMonthChart\|motionsChart\|majorityChart\|motionsTrendChart\|durationChart\|avgDurationChart\|responseTimeChart' public/analytics.htmx.html && grep -c 'kpiMeetings\|kpiResolutions\|kpiAdoptionRate\|kpiParticipation' public/analytics.htmx.html && grep -c 'donutFor\|donutAgainst\|donutAbstain\|donutTotal' public/analytics.htmx.html && grep -c 'analytics-period-pill\|analytics-tab\|tab-content' public/analytics.htmx.html && grep -c 'var(--' public/assets/css/analytics.css</automated>
  </verify>
  <acceptance_criteria>
    - grep 'id="participationChart"' public/analytics.htmx.html returns match
    - grep 'id="sessionsByMonthChart"' public/analytics.htmx.html returns match
    - grep 'id="motionsChart"' public/analytics.htmx.html returns match
    - grep 'id="majorityChart"' public/analytics.htmx.html returns match
    - grep 'id="motionsTrendChart"' public/analytics.htmx.html returns match
    - grep 'id="durationChart"' public/analytics.htmx.html returns match
    - grep 'id="avgDurationChart"' public/analytics.htmx.html returns match
    - grep 'id="responseTimeChart"' public/analytics.htmx.html returns match
    - grep 'id="kpiMeetings"' public/analytics.htmx.html returns match
    - grep 'id="kpiParticipation"' public/analytics.htmx.html returns match
    - grep 'id="donutFor"' public/analytics.htmx.html returns match
    - grep 'id="donutTotal"' public/analytics.htmx.html returns match
    - grep 'id="yearFilter"' public/analytics.htmx.html returns match
    - grep 'id="tab-participation"' public/analytics.htmx.html returns match
    - grep 'id="tab-motions"' public/analytics.htmx.html returns match
    - grep 'id="tab-timing"' public/analytics.htmx.html returns match
    - grep 'id="tab-anomalies"' public/analytics.htmx.html returns match
    - grep 'analytics-dashboard.js' public/analytics.htmx.html returns match
    - grep 'chart.umd.js' public/analytics.htmx.html returns match
    - grep 'var(--' public/assets/css/analytics.css returns 20+ matches
  </acceptance_criteria>
  <done>Analytics HTML completely rewritten with v4.3 design language. All 8 chart canvases present. KPI grid, period pills, 4 tabs, SVG donut all have correct IDs. CSS uses design tokens exclusively.</done>
</task>

<task type="auto">
  <name>Task 2: Verify analytics JS wiring and fix broken selectors</name>
  <files>public/assets/js/pages/analytics-dashboard.js</files>
  <read_first>
    - public/analytics.htmx.html (the NEW rebuilt HTML from Task 1)
    - public/assets/js/pages/analytics-dashboard.js (check all selectors)
  </read_first>
  <action>
    After the HTML rebuild, verify analytics-dashboard.js selectors match:

    1. Cross-reference every getElementById and querySelector call against the new HTML. Key groups:
       - 8 canvas IDs for Chart.js
       - 4 KPI value IDs + 4 trend IDs
       - 6 SVG donut IDs
       - Filter/tab selectors (.analytics-period-pill, .analytics-tab, .tab-content)
       - yearFilter, refreshBtn, btnExportPdf, btnExportCsv
       - anomaliesOverview, anomaliesMeetings, participationTable
       - tabBadgeAnomalies

    2. Check class-based selectors: '.analytics-period-pill', '.analytics-tab', '.tab-content', '.chart-container', '.chart-export-btn', '.trend-arrow', '.trend-text'.

    3. Fix any broken selectors. The JS should NOT need major changes since we preserve all IDs, but verify:
       - Tab switching: querySelectorAll('.analytics-tab') and document.getElementById('tab-' + tab.dataset.tab)
       - Period pills: querySelectorAll('.analytics-period-pill')
       - KPI trend updates: querySelector('.trend-arrow') inside trend container

    4. Verify API endpoint paths in fetch calls are correct (analytics_stats.php or similar).

    5. Ensure Chart.js color reading works — getCOLORS() reads CSS variables from :root. The new CSS must define all required color tokens or inherit from app.css.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && node -e "const fs=require('fs');const html=fs.readFileSync('public/analytics.htmx.html','utf8');const js=fs.readFileSync('public/assets/js/pages/analytics-dashboard.js','utf8');const ids=[...js.matchAll(/getElementById\(['\"]([^'\"]+)['\"]\)/g)].map(m=>m[1]);const missing=ids.filter(id=>!html.includes('id=\"'+id+'\"'));if(missing.length){console.error('MISSING IDs:',missing);process.exit(1)}else{console.log('All',ids.length,'IDs found in HTML')}"</automated>
  </verify>
  <acceptance_criteria>
    - The node verification script exits 0 (all getElementById targets exist in HTML)
    - node -c public/assets/js/pages/analytics-dashboard.js exits 0 (no syntax errors)
    - grep 'loadAllData' public/assets/js/pages/analytics-dashboard.js returns matches
    - grep 'Chart' public/assets/js/pages/analytics-dashboard.js returns matches (Chart.js usage intact)
  </acceptance_criteria>
  <done>All analytics-dashboard.js selectors match the rebuilt HTML. Chart.js bindings intact. Tab switching, period filters, and year filter all wire correctly. No broken DOM references.</done>
</task>

</tasks>

<verification>
1. Open analytics page in browser — KPI cards show with loading/dash values
2. Period pills switch active state on click
3. Tab buttons switch between Participation/Resolutions/Timing/Anomalies panels
4. Chart canvases render (or show empty state if no data)
5. SVG donut visible in Motions tab
6. Year filter dropdown works
7. No JS console errors on page load
8. Dark mode toggle works — chart colors update
</verification>

<success_criteria>
- Analytics page fully rebuilt with v4.3 design language
- All 8 Chart.js canvases present and bindable
- KPI grid, period pills, 4 tabs, SVG donut all functional
- All DOM IDs preserved — analytics-dashboard.js binds without errors
- Dark mode works — colors read from CSS variables
- No JS console errors
</success_criteria>

<output>
After completion, create `.planning/phases/49-secondary-pages-part-1/49-02-SUMMARY.md`
</output>
