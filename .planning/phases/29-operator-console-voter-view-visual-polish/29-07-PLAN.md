---
phase: 29-operator-console-voter-view-visual-polish
plan: 07
type: execute
wave: 5
depends_on: [29-01, 29-02, 29-03, 29-04, 29-05, 29-06]
files_modified:
  - public/operator.htmx.html
  - public/vote.htmx.html
  - public/postsession.htmx.html
  - public/assets/js/pages/operator-exec.js
  - public/assets/css/design-system.css
autonomous: true
requirements: [VIS-07, VIS-08]

must_haves:
  truths:
    - "Zero inline style='' attributes in production HTML (excluding CSS variable overrides via --bar-pct)"
    - "Every new :root token added in Phase 29 has a dark variant in [data-theme=dark]"
    - "Focus rings on all interactive elements have >= 3:1 contrast ratio"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Complete dark mode parity for all Phase 29 tokens"
      contains: "data-theme"
  key_links:
    - from: "design-system.css [data-theme=dark]"
      to: "all new Phase 29 tokens"
      via: "dark variant overrides"
      pattern: "data-theme.*dark"
---

<objective>
Final quality audit: eliminate inline styles from HTML, verify dark mode parity for all Phase 29 tokens, and ensure focus ring contrast compliance.

Purpose: VIS-08 measurable done criteria must be enforced before the phase can be considered complete. This plan runs last to catch any inline styles introduced by earlier plans and verify dark mode completeness.

Output: Zero inline styles in HTML, full dark parity, focus ring compliance
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-operator-console-voter-view-visual-polish/29-CONTEXT.md
@.planning/phases/29-operator-console-voter-view-visual-polish/29-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline style audit + dark mode parity verification</name>
  <files>public/operator.htmx.html, public/vote.htmx.html, public/postsession.htmx.html, public/assets/js/pages/operator-exec.js, public/assets/css/design-system.css</files>
  <read_first>public/assets/css/design-system.css, public/operator.htmx.html, public/vote.htmx.html, public/postsession.htmx.html</read_first>
  <action>
**1. Audit ALL .htmx.html files for inline style="" attributes (VIS-08):**
Run: `grep -rn 'style="' public/*.htmx.html`
For each match:
- If it sets a CSS variable (`style="--bar-pct:56%"`): ACCEPTABLE — leave it
- If it sets raw CSS properties (`style="color:red"`, `style="margin:8px"`, `style="display:none"`): MOVE to the appropriate CSS file or replace with a CSS class
- Document each finding and fix

Known violations from RESEARCH.md:
- `vote.htmx.html` line ~101-103: skeleton `style="margin:..."` -> replace with `.skeleton-spacing` class in vote.css
- `vote.htmx.html` line ~171-174: inline `<style>` block for `#btnConsultDocument` -> move to vote.css

**2. Audit JS files for element.style.property assignments (VIS-08):**
Run: `grep -rn '\.style\.' public/assets/js/pages/*.js | grep -v 'setProperty\|cssText'`
For each match:
- `element.style.property = value` -> replace with `.style.setProperty('--custom-prop', value)` or class toggle
- `element.style.setProperty('--var', value)` -> ACCEPTABLE

Known violations from RESEARCH.md:
- `operator-exec.js` line ~400-401: `partEl.style.color = ...` -> replace with class swap (e.g., add `.kpi-positive`/`.kpi-negative` class)
- `operator-exec.js` line ~419, 425: `barFill.style.width = pct + '%'` -> replace with `.style.setProperty('--bar-pct', pct + '%')`

**3. Dark mode parity audit (VIS-07):**
Read the `:root` block in design-system.css. List ALL tokens that were added in Phase 29 (the color-mix tokens from Plan 01 + any new tokens from Plans 02-06).
Then read the `[data-theme="dark"]` block. Verify EVERY Phase 29 token has a dark variant.
If any token is missing from the dark block, add it.

**4. Focus ring contrast audit (VIS-08):**
Check that design-system.css has a `:focus-visible` rule with at least 3:1 contrast. The existing design system likely has this. If focus ring styles exist but use a light color, ensure they use `var(--color-primary)` or similar high-contrast token.

Add to @layer v4 if missing:
```css
:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}
```

**5. Final inline style count verification:**
Run: `grep -rn 'style="' public/*.htmx.html | grep -v 'var(--' | wc -l`
This should return 0 (or very close to 0 — only CSS variable overrides allowed).

**CRITICAL constraints:**
- Do NOT modify any functional behavior — this is a CSS/HTML cleanup pass
- CSS variable overrides (`style="--bar-pct:X%"`) are ACCEPTABLE and should not be removed
- If an inline style is load-bearing (e.g., hidden by default), replace with a CSS class or `hidden` attribute, not just delete it
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && echo "=== Inline styles (excluding CSS vars) ===" && grep -rn 'style="' public/*.htmx.html | grep -v 'var(--' | grep -v 'style=""' | wc -l && echo "=== JS .style. assignments (excluding setProperty) ===" && grep -rn '\.style\.' public/assets/js/pages/operator-exec.js public/assets/js/pages/vote.js public/assets/js/pages/postsession.js 2>/dev/null | grep -v 'setProperty\|cssText\|style\.display\|removeProperty' | wc -l && echo "=== Dark mode token count ===" && grep -c 'color-.*tint\|color-.*shade\|surface-elevated' public/assets/css/design-system.css</automated>
  </verify>
  <acceptance_criteria>
    - Inline style count (excluding CSS variable overrides) in .htmx.html files is 0 or near 0
    - JS .style.property assignments (excluding setProperty) in operator-exec.js reduced to 0
    - All Phase 29 color-mix tokens appear in BOTH :root and [data-theme="dark"] blocks
    - grep ':focus-visible' public/assets/css/design-system.css returns >= 1 match
    - No raw style="color:", style="margin:", style="display:" in production .htmx.html files
  </acceptance_criteria>
  <done>VIS-08 inline style audit complete — zero violations. VIS-07 dark mode parity verified — all Phase 29 tokens have dark variants. Focus rings at 3:1 contrast via :focus-visible rule.</done>
</task>

</tasks>

<verification>
- `grep -rn 'style="' public/*.htmx.html | grep -v 'var(--' | wc -l` returns 0 or near 0
- All Phase 29 tokens in :root have matching entries in [data-theme="dark"]
- `:focus-visible` rule exists in design-system.css
</verification>

<success_criteria>
- Zero inline style="" attributes in production HTML (CSS variable overrides excluded)
- Every Phase 29 design token has a dark mode variant
- Focus rings have >= 3:1 contrast ratio
- All JS element.style assignments use setProperty() for CSS variables, not raw property assignment
</success_criteria>

<output>
After completion, create `.planning/phases/29-operator-console-voter-view-visual-polish/29-07-SUMMARY.md`
</output>
