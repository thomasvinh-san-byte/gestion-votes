---
phase: 29-operator-console-voter-view-visual-polish
plan: 06
type: execute
wave: 4
depends_on: [29-01]
files_modified:
  - public/assets/css/operator.css
  - public/assets/css/vote.css
  - public/assets/css/postsession.css
  - public/assets/css/archives.css
  - public/assets/css/audit.css
  - public/assets/css/analytics.css
  - public/assets/css/meetings.css
  - public/assets/css/members.css
  - public/assets/css/users.css
  - public/assets/css/settings.css
  - public/assets/css/help.css
  - public/assets/css/email-templates.css
  - public/assets/css/admin.css
  - public/assets/css/pages.css
  - public/assets/css/app.css
autonomous: true
requirements: [VIS-06]

must_haves:
  truths:
    - "All page CSS files use design tokens consistently — no raw hex colors or px values for spacing"
    - "Hover transitions on buttons and cards are 150-200ms"
    - "All pages work at 1024px+ viewport (PC-first)"
    - "Voter screen verified at 375px viewport"
  artifacts:
    - path: "public/assets/css/operator.css"
      provides: "Polished operator page CSS using design tokens"
    - path: "public/assets/css/vote.css"
      provides: "Polished voter page CSS verified at 375px"
    - path: "public/assets/css/app.css"
      provides: "Global styles with consistent hover transitions"
  key_links:
    - from: "all page CSS files"
      to: "design-system.css :root tokens"
      via: "var(--*) references"
      pattern: "var\\(--"
---

<objective>
Visual polish pass across all page CSS files — enforce consistent design tokens, sober hover transitions, and PC-first layout validation.

Purpose: Every page must feel like the same app with the "officiel et confiance" identity. This pass standardizes hover states, ensures spacing and colors use tokens (not raw values), and validates layouts at 1024px+ for admin pages and 375px for voter.

Output: All 15 page CSS files polished with consistent tokens, transitions, and layout validation
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-operator-console-voter-view-visual-polish/29-CONTEXT.md
@.planning/phases/29-operator-console-voter-view-visual-polish/29-RESEARCH.md
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global CSS polish — app.css, pages.css, and high-traffic page CSS</name>
  <files>public/assets/css/app.css, public/assets/css/pages.css, public/assets/css/operator.css, public/assets/css/vote.css, public/assets/css/postsession.css, public/assets/css/members.css, public/assets/css/admin.css</files>
  <read_first>public/assets/css/app.css, public/assets/css/pages.css, public/assets/css/operator.css, public/assets/css/vote.css, public/assets/css/postsession.css, public/assets/css/members.css, public/assets/css/admin.css</read_first>
  <action>
Perform a visual polish pass on 7 high-traffic CSS files. For each file:

**1. Audit raw values and replace with tokens:**
- Replace any raw hex colors (#fff, #000, #e5e7eb, etc.) with the closest design-system token (--color-surface, --color-text, --color-border, etc.)
- Replace any raw px spacing values (8px, 16px, 24px) with token equivalents (--space-2, --space-4, --space-6)
- Replace any raw font sizes with token equivalents (--text-sm, --text-base, --text-lg)
- Replace any raw border-radius with token equivalents (--radius-sm, --radius-md, --radius-lg)
- Exception: 0, 1px, 50%, 100% are fine as raw values

**2. Add sober hover transitions to all interactive elements:**
Ensure every `.btn`, `.card`, clickable link, and interactive element has:
```css
transition: background 150ms, color 150ms, border-color 150ms, box-shadow 150ms;
```
If a transition already exists, ensure it uses 150-200ms duration (not faster, not slower).

**3. Verify PC-first layout (VIS-06):**
- All admin page CSS should work at min-width 1024px without horizontal scroll
- Check that grid/flex layouts don't break at 1024px
- Voter CSS (vote.css) must work at 375px — verify media queries exist for mobile

**4. "Officiel et confiance" refinements:**
- Ensure card shadows use var(--shadow-sm) or var(--shadow-md) — not raw box-shadow values
- Ensure borders use var(--color-border) — not raw colors
- Ensure text colors use var(--color-text), var(--color-text-secondary) — not raw grays
- Add subtle hover lift on cards where appropriate: `transform: translateY(-1px)` on hover

**5. Specific files guidance:**
- **app.css**: Global .btn, .card, .field hover states — these propagate everywhere
- **pages.css**: Calendar, shared page components — token audit
- **operator.css**: Already heavy (4358 lines) — focus on meeting-bar, KPI strip, exec panel token consistency
- **vote.css**: Mobile verification at 375px — ensure vote buttons don't overflow
- **postsession.css**: Already small (368 lines) — quick token pass
- **members.css**: Heavy (1148 lines) — token audit for member cards and table
- **admin.css**: Token-based (995 lines) — verify consistency

**CRITICAL constraints:**
- Do NOT restructure any CSS file — only update values
- Do NOT add @layer to any page CSS file
- Do NOT remove any existing CSS rules — only modify values and add transitions
- All transitions MUST be 150-200ms (user constraint: "transitions sobres, no wow-effects")
- Preserve all existing responsive breakpoints
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && for f in app.css pages.css operator.css vote.css postsession.css members.css admin.css; do echo "=== $f ===" && grep -c 'transition:' public/assets/css/$f; done</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 7 CSS files has at least one `transition:` rule
    - grep -c '#[0-9a-fA-F]\{3,6\}' on each file shows reduction vs before (fewer raw hex)
    - No transition duration exceeds 200ms in any file
    - vote.css has @media rules for max-width 480px or similar mobile breakpoint
    - No @layer declaration in any page CSS file
  </acceptance_criteria>
  <done>7 high-traffic CSS files polished with consistent design tokens, sober 150-200ms hover transitions, and PC-first layout validation. Raw values replaced with tokens where possible.</done>
</task>

<task type="auto">
  <name>Task 2: Light CSS files polish — archives, audit, analytics, meetings, users, settings, help, email-templates</name>
  <files>public/assets/css/archives.css, public/assets/css/audit.css, public/assets/css/analytics.css, public/assets/css/meetings.css, public/assets/css/users.css, public/assets/css/settings.css, public/assets/css/help.css, public/assets/css/email-templates.css</files>
  <read_first>public/assets/css/archives.css, public/assets/css/audit.css, public/assets/css/analytics.css, public/assets/css/meetings.css, public/assets/css/users.css, public/assets/css/settings.css, public/assets/css/help.css, public/assets/css/email-templates.css</read_first>
  <action>
Perform the same visual polish pass on 8 lighter CSS files. These are smaller (239-693 lines each) and mostly token-based already, so the changes should be minimal:

**For each file:**
1. Replace any remaining raw hex colors with design-system tokens
2. Replace any raw spacing/font-size/radius values with tokens
3. Add hover transitions (150ms) on interactive elements if missing
4. Verify card borders use var(--color-border)
5. Verify text colors use var(--color-text) or var(--color-text-secondary)

**Specific notes:**
- **archives.css** (430 lines): Archive cards — token audit
- **audit.css** (372 lines): Audit log table — token audit
- **analytics.css** (693 lines): Chart containers, KPI cards — ensure consistent styling
- **meetings.css** (533 lines): Meeting cards, session list — token audit
- **users.css** (323 lines): User table — token audit
- **settings.css** (403 lines): Settings panels — token audit
- **help.css** (423 lines): Help page layout — token audit
- **email-templates.css** (239 lines): Template editor — minimal changes expected

These are mostly quick passes — if a file is already fully token-based, document that finding and move on.

**CRITICAL constraints:**
- Same as Task 1: no restructure, no @layer, no removal, 150-200ms transitions only
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && for f in archives.css audit.css analytics.css meetings.css users.css settings.css help.css email-templates.css; do echo "=== $f ===" && grep -c 'var(--' public/assets/css/$f; done</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 8 CSS files uses var(--*) tokens (grep count > 0 for each)
    - No new @layer declaration in any file
    - No transition duration exceeds 200ms
    - Each file compiles without syntax errors (no broken CSS)
  </acceptance_criteria>
  <done>8 light CSS files polished with consistent design tokens and hover transitions. All pages use the "officiel et confiance" design language uniformly.</done>
</task>

</tasks>

<verification>
- All 15 page CSS files use design tokens consistently
- `grep 'transition:' public/assets/css/*.css` shows all files have transitions
- No raw hex colors remain for common values (border, text, surface, bg)
- vote.css has mobile breakpoint at ~375-480px
</verification>

<success_criteria>
- All 15 page CSS files audited and polished
- Raw hex/px values replaced with design tokens where appropriate
- Hover transitions are 150-200ms on all interactive elements
- PC-first layout works at 1024px+ on all admin pages
- Voter layout verified at 375px
- "Officiel et confiance" identity consistent across all pages
</success_criteria>

<output>
After completion, create `.planning/phases/29-operator-console-voter-view-visual-polish/29-06-SUMMARY.md`
</output>
