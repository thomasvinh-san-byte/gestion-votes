---
phase: 04-test-infra-gsd-ergo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/specs/cockpit-keyboard-shortcuts.spec.js
  - .planning/phases/04-test-infra-gsd-ergo/04-01-SEED-MEETING-VERIFICATION.md
autonomous: false
requirements: [INFRA-V26-01]
must_haves:
  truths:
    - "Le helper `seedMeeting` fournit la fixture nécessaire au test `@integration` F-4"
    - "Le test `@integration L on real operator view reaches the production toggle button` passe sans skip"
    - "Le test passe ≥3 runs consécutifs verts en CI dev (zéro flake)"
    - "Le résultat est tracé (artefact texte) dans le phase dir"
  artifacts:
    - path: "tests/e2e/helpers/seed-meeting.js"
      provides: "seedMeeting + seedRunningMeeting helpers (déjà livré v2.4 P3, plan 03.1)"
      contains: "seedRunningMeeting"
    - path: "tests/e2e/specs/cockpit-keyboard-shortcuts.spec.js"
      provides: "Test @integration F-4 sur opBtnToggleVote/aria-keyshortcuts=L"
      contains: "@integration L on real operator view"
    - path: ".planning/phases/04-test-infra-gsd-ergo/04-01-SEED-MEETING-VERIFICATION.md"
      provides: "Procès-verbal des 3 runs CI dev (commande exécutée, sortie, durée, status)"
  key_links:
    - from: "tests/e2e/specs/cockpit-keyboard-shortcuts.spec.js"
      to: "tests/e2e/helpers/seed-meeting.js"
      via: "require('../helpers/seed-meeting').seedRunningMeeting"
      pattern: "require\\(['\"]\\.\\./helpers/seed-meeting['\"]\\)"
    - from: "tests/e2e/helpers/seed-meeting.js"
      to: "/api/v1/test/seed-meeting (TestSeedController::seedMeeting)"
      via: "request.post"
      pattern: "/api/v1/test/seed-meeting"
---

<objective>
Verrouiller la livraison **INFRA-V26-01** : prouver que le test `@integration` F-4 dans `tests/e2e/specs/cockpit-keyboard-shortcuts.spec.js` (réactivé par v2.4 P3 plan 03.1 via le helper `seedMeeting`) passe **3 runs verts consécutifs** en CI dev, et tracer le résultat dans le phase dir comme artefact d'audit.

Purpose: La fixture `seedMeeting` a été livrée par v2.4 P3 et le test `@integration` est techniquement réactivé. Mais la requirement v2.6 INFRA-V26-01 exige une **preuve de stabilité ≥3 runs verts** en CI dev — gate qui n'a jamais été exécuté car v2.4 P3 a deferred Playwright runs au dev-machine. Cette phase clôt cette dette en exécutant la gate et en archivant la preuve.

Output:
- Test inchangé (pas de modification de code source si vert) OU patch minimal documenté si flake détecté
- `04-01-SEED-MEETING-VERIFICATION.md` : transcript des 3 runs (commande, exit code, durée, ID run, ligne PASS du test ciblé)
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/gestion-votes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@tests/e2e/helpers/seed-meeting.js
@tests/e2e/specs/cockpit-keyboard-shortcuts.spec.js
@app/Controller/TestSeedController.php

<interfaces>
<!-- Helper signature livré par v2.4 P3 (plan 03.1) — à utiliser tel quel, ne pas réécrire. -->

From tests/e2e/helpers/seed-meeting.js:
```javascript
async function seedMeeting(request, { tenantId, status = 'setup', motionsCount = 0 }): Promise<string> // meetingId UUID
async function seedRunningMeeting(request, { tenantId?, motionsCount? }): Promise<{ id: string }>
const DEFAULT_TENANT_ID = 'aaaaaaaa-1111-2222-3333-444444444444';
module.exports = { seedMeeting, seedRunningMeeting, DEFAULT_TENANT_ID };
```

From app/Controller/TestSeedController.php (POST /api/v1/test/seed-meeting):
- Endpoint dev-only — gardé par EnvGuardMiddleware + guardProduction()
- Body: { tenantId, status, motionsCount } → réponse JSON { ok, data: { meeting_id } }

From tests/e2e/specs/cockpit-keyboard-shortcuts.spec.js (lignes 145-161, déjà existant) :
```javascript
test('@integration L on real operator view reaches the production toggle button', async ({ page, request }) => {
  const { seedRunningMeeting } = require('../helpers/seed-meeting');
  await loginAsOperator(page);
  const meeting = await seedRunningMeeting(request, { motionsCount: 1 });
  await page.goto(`/operator?meeting=${meeting.id}`);
  // ... assert opBtnToggleVote[aria-keyshortcuts="L"] visible + click via "L" keypress
});
```
</interfaces>

**Note importante — clarification du périmètre F-4** :
La requirement INFRA-V26-01 cite `modal-focus-trap.spec.js` comme test F-4 skippé, mais l'inspection du code montre que :
- `modal-focus-trap.spec.js` n'a aucun `@integration` skippé (il utilise `archives.htmx.html` static, pas de fixture)
- Le **vrai** test `@integration` qui dépend de `seedMeeting` est dans `cockpit-keyboard-shortcuts.spec.js` (test F-4 ligne 145, taggé `@integration`, livré par v2.4 P3 plan 03.1 — voir commentaire ligne 143-144)
- Donc INFRA-V26-01 = stabiliser ce test précis sur 3 runs CI dev

Si le run révèle un autre test `@integration` skippé non identifié, ouvrir une seed à v2.7, ne pas étendre Phase 4.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit pré-run — confirmer absence d'autres @integration skippés et état du helper</name>
  <files>(read-only) tests/e2e/specs/, tests/e2e/helpers/seed-meeting.js, app/Controller/TestSeedController.php</files>
  <action>
1. Scanner tous les specs Playwright pour `@integration`, `test.skip`, `describe.skip`, `test.fixme` :
   ```bash
   grep -rn '@integration\|test\.skip\|describe\.skip\|test\.fixme' tests/e2e/specs/ | grep -v 'CONTRAST_AUDIT\|No meeting available'
   ```
   Résultat attendu : seul `cockpit-keyboard-shortcuts.spec.js:145` (`@integration L on real operator view`) doit utiliser `seedMeeting`. Tout autre `@integration` skippé inattendu = STOP et reporter (ouvrir une seed v2.7).

2. Vérifier que le helper `tests/e2e/helpers/seed-meeting.js` existe et exporte `seedRunningMeeting` :
   ```bash
   grep -E 'module\.exports.*seedRunningMeeting' tests/e2e/helpers/seed-meeting.js
   ```
   Si absent → STOP : v2.4 P3 helper manquant, escalader avant tout.

3. Vérifier que la route `/api/v1/test/seed-meeting` est bien wirée :
   ```bash
   grep -nE 'test/seed-meeting' app/routes.php
   grep -nE 'public function seedMeeting' app/Controller/TestSeedController.php
   ```
   Les deux doivent retourner ≥1 match.

4. Vérifier la syntaxe PHP du controller (rule CLAUDE.md) :
   ```bash
   php -l app/Controller/TestSeedController.php
   ```
   Doit retourner "No syntax errors detected".
  </action>
  <verify>
    <automated>
grep -rn '@integration' tests/e2e/specs/ | grep -v 'CONTRAST_AUDIT' | tee /tmp/04-01-audit-integration.txt &amp;&amp; \
test "$(grep -c '@integration L on real operator view' /tmp/04-01-audit-integration.txt)" -ge 1 &amp;&amp; \
grep -E 'module\.exports.*seedRunningMeeting' tests/e2e/helpers/seed-meeting.js &amp;&amp; \
grep -nE 'test/seed-meeting' app/routes.php &amp;&amp; \
php -l app/Controller/TestSeedController.php
    </automated>
  </verify>
  <done>L'audit confirme : exactement 1 test `@integration` lié à seedMeeting (cockpit-keyboard-shortcuts.spec.js F-4), helper exporté, route wirée, controller syntaxe OK. Aucun autre @integration skippé inattendu.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Exécution gate — 3 runs verts consécutifs du test @integration sur stack dev</name>
  <what-built>
Le test `@integration L on real operator view reaches the production toggle button` (cockpit-keyboard-shortcuts.spec.js:145) est techniquement déjà actif depuis v2.4 P3, mais n'a jamais été exécuté en CI dev pour valider la stabilité (3 runs verts) — gate INFRA-V26-01 v2.6.

Aucun code n'est modifié à cette étape. La gate est purement runtime.
  </what-built>
  <how-to-verify>
**Préalable** : stack docker dev démarrée avec base de seed test. Suivre `tests/e2e/README.md` §2 si fresh setup.

**Commandes à exécuter dans l'ordre** (run 3x pour stabilité) :

```bash
# Run #1
cd tests/e2e
TEST_BYPASS_RATELIMIT=1 timeout 120 npx playwright test specs/cockpit-keyboard-shortcuts.spec.js --grep "@integration L on real operator view" --project=chromium --reporter=line 2>&amp;1 | tee /tmp/run1.log
echo "RUN1_EXIT=$?"

# Run #2 (immédiatement après)
TEST_BYPASS_RATELIMIT=1 timeout 120 npx playwright test specs/cockpit-keyboard-shortcuts.spec.js --grep "@integration L on real operator view" --project=chromium --reporter=line 2>&amp;1 | tee /tmp/run2.log
echo "RUN2_EXIT=$?"

# Run #3
TEST_BYPASS_RATELIMIT=1 timeout 120 npx playwright test specs/cockpit-keyboard-shortcuts.spec.js --grep "@integration L on real operator view" --project=chromium --reporter=line 2>&amp;1 | tee /tmp/run3.log
echo "RUN3_EXIT=$?"
```

**Critères d'acceptation** :
1. `RUN1_EXIT=0`, `RUN2_EXIT=0`, `RUN3_EXIT=0` (tous verts)
2. Chaque log contient la ligne `1 passed` (ou équivalent reporter line)
3. Aucun test "flaky" / retry visible dans les logs (Playwright signale les retry avec `(retry #N)`)
4. Durée par run ≤ 60s (CLAUDE.md timeout test)

**En cas d'échec** :
- Si 1/3 échoue → analyser le log, c'est un flake — STOP, ne pas committer, demander à l'utilisateur (CLAUDE.md règle "2 fois de suite").
- Si 2-3/3 échouent → bug ou régression dans le helper / endpoint / spec — STOP, escalader.

**Coller les 3 sorties + exit codes en réponse pour que la Task 3 archive l'artefact.**
  </how-to-verify>
  <resume-signal>Type "approuvé" + paste des 3 sorties (ou exit codes + lignes de résultat) pour archivage. Ou "échec: ..." pour escalader.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Archiver le procès-verbal de stabilité dans le phase dir</name>
  <files>.planning/phases/04-test-infra-gsd-ergo/04-01-SEED-MEETING-VERIFICATION.md</files>
  <action>
Créer le fichier `.planning/phases/04-test-infra-gsd-ergo/04-01-SEED-MEETING-VERIFICATION.md` avec :

```markdown
# INFRA-V26-01 — Vérification stabilité test @integration F-4

**Date** : {date du run}
**Stack** : dev-machine (docker compose, BASE_URL local)
**Spec** : `tests/e2e/specs/cockpit-keyboard-shortcuts.spec.js`
**Filtre** : `--grep "@integration L on real operator view"`
**Helper** : `tests/e2e/helpers/seed-meeting.js` (livré v2.4 P3 plan 03.1)
**Endpoint** : `POST /api/v1/test/seed-meeting` (TestSeedController, dev-only)

## Runs

| # | Commande | Exit code | Résultat | Durée |
|---|---|---|---|---|
| 1 | `npx playwright test ... --grep "@integration L on real operator view" --project=chromium` | 0 | 1 passed | {Xs} |
| 2 | idem | 0 | 1 passed | {Xs} |
| 3 | idem | 0 | 1 passed | {Xs} |

## Verdict

**3 runs verts consécutifs — gate INFRA-V26-01 satisfaite.**

Aucun retry Playwright observé. Aucune modification de code requise. Helper, endpoint et spec ont fonctionné comme livrés en v2.4 P3.

## Logs (extraits)

### Run 1
```
{coller la sortie line reporter — chemin du test + 1 passed (Xs)}
```

### Run 2
```
{idem}
```

### Run 3
```
{idem}
```

## Liens

- Helper : [`tests/e2e/helpers/seed-meeting.js`](../../../tests/e2e/helpers/seed-meeting.js)
- Spec : [`tests/e2e/specs/cockpit-keyboard-shortcuts.spec.js:145`](../../../tests/e2e/specs/cockpit-keyboard-shortcuts.spec.js)
- Plan source v2.4 P3 : [`.planning/milestones/v2.5-phases/03-test-infrastructure/03.1-PLAN.md`](../../milestones/v2.5-phases/03-test-infrastructure/03.1-PLAN.md) (si existe, sinon référencer le commit livrant 03.1)
- ROADMAP : v2.6 Phase 4 INFRA-V26-01

---
*Verified: {date} — Plan 04-01 (v2.6 Phase 4)*
```

Remplacer `{date du run}`, `{Xs}`, et coller les sorties exactes des 3 runs (Task 2). Si la sortie est verbose, garder uniquement la ligne `Running 1 test using ...` + ligne `1 passed (...)` + temps total.

**Aucune autre modification.** Pas de patch sur la spec si elle est verte 3/3.
  </action>
  <verify>
    <automated>
test -f .planning/phases/04-test-infra-gsd-ergo/04-01-SEED-MEETING-VERIFICATION.md &amp;&amp; \
grep -c '3 runs verts consécutifs' .planning/phases/04-test-infra-gsd-ergo/04-01-SEED-MEETING-VERIFICATION.md | grep -qE '^[1-9]' &amp;&amp; \
grep -cE '^\| [123] \|' .planning/phases/04-test-infra-gsd-ergo/04-01-SEED-MEETING-VERIFICATION.md | grep -q '^3$'
    </automated>
  </verify>
  <done>Le fichier `.planning/phases/04-test-infra-gsd-ergo/04-01-SEED-MEETING-VERIFICATION.md` existe, contient le verdict "3 runs verts consécutifs", et le tableau Runs a exactement 3 lignes numérotées.</done>
</task>

</tasks>

<verification>
- [ ] `grep '@integration' tests/e2e/specs/` ne révèle aucun test skippé inattendu lié à seedMeeting
- [ ] Le test `cockpit-keyboard-shortcuts.spec.js:145` (`@integration L on real operator view`) passe 3/3 runs sans retry
- [ ] `04-01-SEED-MEETING-VERIFICATION.md` est créé avec verdict + 3 lignes de runs + extraits de logs
- [ ] Aucune modification de la spec ni du helper (sinon escalader avant commit)
- [ ] Pas de régression sur les autres tests cockpit-keyboard-shortcuts (4 autres tests non-@integration restent verts dans la même run)
</verification>

<success_criteria>
1. Le test `@integration` F-4 réactivé en v2.4 P3 est vérifié stable sur 3 runs verts consécutifs en CI dev (gate INFRA-V26-01).
2. Le procès-verbal `04-01-SEED-MEETING-VERIFICATION.md` archive la preuve dans le phase dir.
3. Aucune modification de code source de production / test si la spec est verte 3/3 — ce plan est purement vérification + archivage.
4. INFRA-V26-01 est marquable "Complete" dans REQUIREMENTS.md table de traçabilité.
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-infra-gsd-ergo/04-01-SUMMARY.md` listing :
- Conclusion gate (3 runs verts ou échec + détail)
- Lien vers `04-01-SEED-MEETING-VERIFICATION.md`
- Status INFRA-V26-01 (Complete / Blocked)
- Effort réel (vs estimé) — utile pour calibrage v2.7
</output>
