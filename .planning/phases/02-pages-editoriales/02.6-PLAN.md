---
phase: 02-pages-editoriales
plan: 6
type: execute
wave: 4
depends_on: [3, 4, 5]
files_modified:
  - tests/Security/EditorialConventionsTest.php
autonomous: true
requirements:
  - EDITORIAL-01
  - EDITORIAL-05
  - EDITORIAL-09
user_setup: []

must_haves:
  truths:
    - "Test PHPUnit EditorialConventionsTest existe et passe"
    - "Le test échoue si un descendant direct de .ag-editorial reçoit text-align: center"
    - "Le test échoue si le préambule pédagogique français est absent du fichier ag-integrity-modal.js"
    - "Le test échoue si grep audit.css retourne >0 padding/margin hardcodé"
  artifacts:
    - path: "tests/Security/EditorialConventionsTest.php"
      provides: "Tests gardiens (lint) pour conventions éditoriales"
      contains: "class EditorialConventionsTest"
      min_lines: 60
  key_links:
    - from: "tests/Security/EditorialConventionsTest.php"
      to: "public/audit.htmx.html, public/trust.htmx.html, public/archives.htmx.html, public/report.htmx.html"
      via: "file_get_contents + regex scan"
      pattern: "ag-editorial"
    - from: "tests/Security/EditorialConventionsTest.php"
      to: "public/assets/js/components/ag-integrity-modal.js"
      via: "file_get_contents + assertStringContainsString"
      pattern: "Voici la preuve"
---

<objective>
Créer `tests/Security/EditorialConventionsTest.php` (PHPUnit) — test gardien qui scanne les fichiers livrés par les plans 02.1-02.5 et garantit les invariants éditoriaux. Trois assertions critiques :

1. Aucun `text-align: center` dans un descendant direct de `.ag-editorial` (EDITORIAL-01).
2. Le préambule pédagogique français VERBATIM est présent dans `ag-integrity-modal.js` (EDITORIAL-05).
3. `audit.css` ne contient pas de padding/margin hardcodé (EDITORIAL-09).

Purpose: Empêcher régression future. Ces tests sont rapides (lecture fichier + regex), pas de DOM, pas de browser.
Output: 1 fichier PHP avec 3-4 méthodes de test.
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/02-pages-editoriales/02-01-SUMMARY.md
@.planning/phases/02-pages-editoriales/02-02-SUMMARY.md
@.planning/phases/02-pages-editoriales/02-03-SUMMARY.md
@.planning/phases/02-pages-editoriales/02-05-SUMMARY.md

<read_first>
- ls tests/ (vérifier la structure ; le namespace `Tests\Unit` existe — `Tests\Security` peut être nouveau ou exister selon le projet ; adapter le namespace au composer.json)
- composer.json (vérifier autoload-dev psr-4 pour le bon namespace)
- un test existant simple, ex: tests/Unit/ErrorDictionaryTest.php (calquer le pattern)
</read_first>

<test_strategy>
Trois tests :

**Test 1 — `testAgEditorialChildrenNotCentered`** :
Pour chacun des 4 fichiers HTML (`audit`, `trust`, `archives`, `report`.htmx.html), parser le contenu (regex simple, pas de DOMDocument complet) et vérifier :
- Aucun bloc `<style>...</style>` interne ne contient `.ag-editorial > * { text-align: center }`
- Aucun élément avec `class` contenant `ag-editorial` n'a un enfant direct avec `style="text-align: center"` ou `style="text-align:center"`.
- Aucune occurrence de `text-align: center` n'apparaît à moins de 200 caractères APRÈS une ouverture `class="...ag-editorial..."` (heuristique simple — sinon DOMDocument).

Implémentation pragmatique : utiliser `DOMDocument::loadHTML()` avec libxml errors supprimés, query XPath `//*[contains(@class,'ag-editorial')]/*[contains(@style,'text-align: center') or contains(@style,'text-align:center')]`. Si la query retourne ≥ 1 noeud → fail.

**Test 2 — `testIntegrityModalContainsPedagogicalPreamble`** :
- `file_get_contents('public/assets/js/components/ag-integrity-modal.js')`
- `assertStringContainsString("Voici la preuve que ce PV n'a pas été modifié depuis le", $js)`
- `assertStringContainsString('sceau cryptographique reliant la précédente', $js)`
- `assertStringContainsString('modifier une seule virgule briserait la chaîne', $js)`

**Test 3 — `testAuditCssHasNoHardcodedSpacing`** :
- `file_get_contents('public/assets/css/audit.css')`
- `preg_match_all('/(padding|margin)(-[a-z]+)?:\s+[0-9]+(\.[0-9]+)?(px|rem|em)/', $css, $matches)`
- `assertCount(0, $matches[0], 'audit.css contains hardcoded padding/margin: ' . implode(', ', $matches[0]))`

**Test 4 (bonus) — `testNoForbiddenWordsInEditorialPages`** :
- Pour chacun des 4 fichiers HTML, `assertDoesNotMatchRegularExpression('/\b(copropriété|syndic)\b/i', $html)`.
</test_strategy>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Créer EditorialConventionsTest avec 4 méthodes</name>
  <files>tests/Security/EditorialConventionsTest.php</files>
  <read_first>
    - composer.json (autoload-dev psr-4)
    - tests/Unit/ (un test simple existant pour calquer le style)
  </read_first>
  <behavior>
    - Test 1 : un fichier HTML avec `<div class="ag-editorial"><p style="text-align: center">x</p></div>` fait échouer testAgEditorialChildrenNotCentered.
    - Test 2 : suppression du préambule de ag-integrity-modal.js fait échouer testIntegrityModalContainsPedagogicalPreamble.
    - Test 3 : ajouter `padding: 12px;` dans audit.css fait échouer testAuditCssHasNoHardcodedSpacing.
    - Test 4 : insérer "syndic" dans une page éditoriale fait échouer testNoForbiddenWordsInEditorialPages.
  </behavior>
  <action>
1. **Vérifier le namespace** : `grep -A 5 "autoload-dev" composer.json`. Si `Tests\\Security\\` n'est pas mappé, soit utiliser `Tests\\Unit\\` (chemin `tests/Unit/EditorialConventionsTest.php`), soit ajouter le mapping dans composer.json + lancer `composer dump-autoload`. **Décision pragmatique : utiliser `Tests\\Unit\\` pour ne pas toucher composer.json**, sauf si `Tests\\Security\\` existe déjà.

2. **Créer le fichier** (chemin final : `tests/Unit/EditorialConventionsTest.php` si namespace Unit, sinon `tests/Security/EditorialConventionsTest.php`) :

```php
<?php

declare(strict_types=1);

namespace Tests\Unit;

use PHPUnit\Framework\TestCase;

final class EditorialConventionsTest extends TestCase
{
    private const EDITORIAL_PAGES = [
        'audit.htmx.html',
        'trust.htmx.html',
        'archives.htmx.html',
        'report.htmx.html',
    ];

    private function publicPath(string $file): string
    {
        return dirname(__DIR__, 2) . '/public/' . $file;
    }

    private function rootPath(string $rel): string
    {
        return dirname(__DIR__, 2) . '/' . $rel;
    }

    public function testAgEditorialChildrenNotCentered(): void
    {
        foreach (self::EDITORIAL_PAGES as $page) {
            $path = $this->publicPath($page);
            $this->assertFileExists($path, "Page éditoriale manquante : $page");

            $html = file_get_contents($path);
            $this->assertNotFalse($html);

            // Charger le HTML en mode permissif (templates HTMX, pas toujours valides W3C)
            libxml_use_internal_errors(true);
            $doc = new \DOMDocument();
            // Préfixer pour libxml en UTF-8
            $doc->loadHTML('<?xml encoding="UTF-8">' . $html, LIBXML_NOERROR | LIBXML_NOWARNING);
            libxml_clear_errors();

            $xpath = new \DOMXPath($doc);
            // Tous les enfants directs d'un .ag-editorial dont l'attribut style contient text-align: center
            $query = "//*[contains(concat(' ', normalize-space(@class), ' '), ' ag-editorial ')]"
                   . "/*[contains(translate(@style, ' ', ''), 'text-align:center')]";
            $nodes = $xpath->query($query);

            $this->assertSame(
                0,
                $nodes->length,
                "$page : enfant direct de .ag-editorial avec text-align: center détecté (" . $nodes->length . " noeuds)."
            );
        }
    }

    public function testIntegrityModalContainsPedagogicalPreamble(): void
    {
        $path = $this->rootPath('public/assets/js/components/ag-integrity-modal.js');
        $this->assertFileExists($path);

        $js = file_get_contents($path);
        $this->assertNotFalse($js);

        $this->assertStringContainsString(
            "Voici la preuve que ce PV n'a pas été modifié depuis le",
            $js,
            'Préambule pédagogique EDITORIAL-05 manquant dans ag-integrity-modal.js'
        );
        $this->assertStringContainsString(
            'sceau cryptographique reliant la précédente',
            $js,
            'Métaphore "sceau cryptographique" manquante (EDITORIAL-05)'
        );
        $this->assertStringContainsString(
            'modifier une seule virgule briserait la chaîne',
            $js,
            'Phrase de fermeture du préambule manquante (EDITORIAL-05)'
        );
    }

    public function testAuditCssHasNoHardcodedSpacing(): void
    {
        $path = $this->rootPath('public/assets/css/audit.css');
        $this->assertFileExists($path);

        $css = file_get_contents($path);
        $this->assertNotFalse($css);

        $matches = [];
        preg_match_all(
            '/(padding|margin)(-[a-z]+)?:\s+[0-9]+(\.[0-9]+)?(px|rem|em)/',
            $css,
            $matches
        );

        $this->assertCount(
            0,
            $matches[0],
            'audit.css contient des padding/margin hardcodés (EDITORIAL-09) : ' . implode(', ', array_slice($matches[0], 0, 5))
        );
    }

    public function testNoForbiddenWordsInEditorialPages(): void
    {
        foreach (self::EDITORIAL_PAGES as $page) {
            $path = $this->publicPath($page);
            $html = file_get_contents($path);
            $this->assertNotFalse($html);

            $this->assertDoesNotMatchRegularExpression(
                '/\b(copropriété|syndic)\b/i',
                $html,
                "$page contient un mot interdit (copropriété ou syndic — CLAUDE.md)"
            );
        }
    }
}
```

3. **Vérifier syntaxe** : `php -l tests/Unit/EditorialConventionsTest.php`.

4. **Lancer le test ciblé** (CLAUDE.md : tests ciblés uniquement) :
```bash
timeout 60 php vendor/bin/phpunit tests/Unit/EditorialConventionsTest.php --no-coverage
```
Tous les tests doivent passer (vert).

5. Si un test échoue, **NE PAS RELANCER >2 fois**. Investiguer la cause (probablement : nom de fichier incorrect, namespace mismatch, ou un plan amont incomplet — auquel cas le test fait son boulot).

**Note Playwright E2E** : skip dans ce sandbox (Playwright non installé). Documenter dans SUMMARY un followup "ajouter spec E2E pour tester l'ouverture du modal d'intégrité (clic sur lien → modal visible → préambule lisible)".
  </action>
  <verify>
    <automated>php -l tests/Unit/EditorialConventionsTest.php && timeout 60 php vendor/bin/phpunit tests/Unit/EditorialConventionsTest.php --no-coverage</automated>
  </verify>
  <done>Fichier de test créé ; syntaxe PHP valide ; les 4 tests passent ; namespace cohérent avec composer.json autoload-dev.</done>
</task>

</tasks>

<verification>
- `php -l tests/Unit/EditorialConventionsTest.php` : No syntax errors
- `php vendor/bin/phpunit tests/Unit/EditorialConventionsTest.php --no-coverage` : OK (4/4 tests passent)
- Le test échoue si on injecte volontairement `text-align: center` dans un enfant `.ag-editorial` (vérification manuelle de la sensibilité)
</verification>

<success_criteria>
Tests gardiens en place. Toute régression future sur :
- text-align center dans .ag-editorial
- Préambule pédagogique manquant
- Padding/margin hardcodés dans audit.css
- Mot interdit (copropriété/syndic) dans les pages

…fait échouer la CI immédiatement.
</success_criteria>

<output>
Créer `.planning/phases/02-pages-editoriales/02-06-SUMMARY.md` après exécution. Y consigner :
- Namespace utilisé (Unit vs Security)
- Résultat des tests (4/4 vert)
- Followup Playwright E2E (skip → manuel ou phase ultérieure)
</output>
</content>
</invoke>