---
phase: 02-pages-editoriales
plan: 3
type: execute
wave: 2
depends_on: [1, 2]
files_modified:
  - public/audit.htmx.html
  - public/trust.htmx.html
  - public/archives.htmx.html
  - public/report.htmx.html
autonomous: true
requirements:
  - EDITORIAL-01
  - EDITORIAL-02
  - EDITORIAL-03
  - EDITORIAL-04
  - EDITORIAL-05
  - EDITORIAL-06
  - EDITORIAL-08
user_setup: []

must_haves:
  truths:
    - "Les 4 pages éditoriales chargent editorial.css et wrappent leur contenu dans .ag-editorial"
    - "audit.htmx.html charge ag-integrity-modal (JS + CSS) et expose un lien 'Vérifier l'intégrité' qui l'ouvre"
    - "Les 5 filter tabs de /audit sont auditées : 2-3 plus rares déplacées dans <details>'Plus de filtres'"
    - "Aucun text-align: center inline ou class sur enfants directs de .ag-editorial dans ces 4 fichiers"
    - "Hashes / UUIDs / numéros de résolution rendus avec class mono ou .ag-resolution-pill (pill seulement en header/list/table)"
  artifacts:
    - path: "public/audit.htmx.html"
      provides: "Page audit avec wrapper .ag-editorial + filter tabs auditées + lien intégrité"
      contains: "ag-editorial"
    - path: "public/trust.htmx.html"
      provides: "Page trust avec wrapper .ag-editorial"
      contains: "ag-editorial"
    - path: "public/archives.htmx.html"
      provides: "Page archives avec wrapper .ag-editorial"
      contains: "ag-editorial"
    - path: "public/report.htmx.html"
      provides: "Page report (PV) avec wrapper .ag-editorial + lien Vérifier intégrité"
      contains: "ag-editorial"
  key_links:
    - from: "public/audit.htmx.html"
      to: "public/assets/css/editorial.css"
      via: "<link rel=stylesheet>"
      pattern: "editorial\\.css"
    - from: "public/report.htmx.html"
      to: "ag-integrity-modal custom element"
      via: "<ag-integrity-modal> tag + script include"
      pattern: "ag-integrity-modal"
---

<objective>
Appliquer le wrapper `.ag-editorial` aux 4 pages HTML éditoriales (`/audit`, `/trust`, `/archives`, `/report`). Inclure les feuilles de style et JS livrés par 02.1 + 02.2. Pour `/audit`, auditer les 5 filter tabs et déplacer les 2-3 moins utilisées dans `<details>`. Pour `/report` (PV), ajouter le lien "Vérifier l'intégrité" qui ouvre le custom element `<ag-integrity-modal>`.

Purpose: Convertir les pages à valeur de preuve en documents éditoriaux lisibles (Fraunces serif, 720px, JetBrains Mono pour hashes).
Output: 4 fichiers HTML modifiés.
</objective>

<execution_context>
@/home/user/gestion-votes/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/02-pages-editoriales/02-01-SUMMARY.md
@.planning/phases/02-pages-editoriales/02-02-SUMMARY.md

<read_first>
- public/audit.htmx.html (lecture complète — c'est la page la plus complexe ; identifier les 5 filter tabs et le markup principal)
- public/trust.htmx.html (lecture rapide — structure)
- public/archives.htmx.html (lecture rapide)
- public/report.htmx.html (lecture rapide — identifier où s'affiche le hash d'intégrité actuellement)
- public/assets/css/editorial.css (livré par 02.1)
- public/assets/js/components/ag-integrity-modal.js (livré par 02.2)
</read_first>

<audit_filter_decision>
**EDITORIAL-02 demande nommage explicite**. Sans logs réels, décision produit jugement :

- **Tabs RETENUS en avant (3)** : `Toutes`, `Authentification`, `Vote` — flux principal de consultation auditeur.
- **Tabs PLIÉS dans `<details>` "Plus de filtres" (2)** : `Procuration`, `Configuration` — usages plus rares (audit ponctuel, pas en flux quotidien).

L'exécutant DOIT :
1. Lire les 5 tabs réels dans `public/audit.htmx.html` (les noms peuvent différer).
2. Si les noms diffèrent, choisir 2-3 candidats au pliage en justifiant 1 phrase dans le SUMMARY.
3. Garder l'attribut sémantique des tabs (data-filter / value / etc.) pour ne pas casser la logique JS.
</audit_filter_decision>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrapper .ag-editorial + includes sur trust, archives, audit, report</name>
  <files>public/audit.htmx.html, public/trust.htmx.html, public/archives.htmx.html, public/report.htmx.html</files>
  <read_first>
    - les 4 fichiers HTML cités ci-dessus (lire les sections `<head>` et le bloc `<main>` ou contenu principal)
    - public/assets/css/editorial.css
  </read_first>
  <action>
Pour chacune des 4 pages :

1. **Dans `<head>`** : ajouter `<link rel="stylesheet" href="/assets/css/editorial.css">` après les autres stylesheets globaux. Sur `audit.htmx.html` et `report.htmx.html`, ajouter aussi `<link rel="stylesheet" href="/assets/css/components/ag-integrity-modal.css">` et `<script src="/assets/js/components/ag-integrity-modal.js" defer></script>`.

2. **Wrapper le contenu principal** dans `<div class="ag-editorial">` (ou ajouter la class à un `<main>` existant). Structure recommandée :

```html
<div class="ag-editorial">
  <article class="ag-editorial-content">
    <!-- contenu existant : titres, paragraphes, listes, tableaux -->
  </article>
  <aside class="ag-editorial-sidebar">
    <!-- méta : date séance, hash intégrité (sur report), navigation interne -->
  </aside>
</div>
```

Sur `trust.htmx.html` et `archives.htmx.html`, le sidebar peut être vide ou contenir uniquement la nav interne.

3. **Audit /audit — filter tabs** : localiser le bloc des 5 tabs (probablement `<div class="audit-filter-tabs">` ou markup similaire). Garder les 3 tabs principaux (cf. `<audit_filter_decision>` ci-dessus) directement visibles. Envelopper les 2-3 autres dans :

```html
<details class="audit-filter-tabs__more">
  <summary>Plus de filtres</summary>
  <!-- tabs pliés ici, mêmes attributs data-* -->
</details>
```

4. **Hashes / UUIDs / codes** : repérer les éléments qui affichent ce type de valeur (chercher `class="hash"`, `class="uuid"`, `class="audit-id"` ou texte de format UUID/hash). Ajouter la class `ag-mono` si pas déjà stylé monospace via une class équivalente.

5. **Numéros de résolution (EDITORIAL-04)** : sur `report.htmx.html` (PV), repérer où apparaissent les numéros de résolution. **Pill `.ag-resolution-pill` UNIQUEMENT** :
   - en en-tête de section (`<h2><span class="ag-resolution-pill">R-12</span> Titre</h2>`)
   - en liste (`<li><span class="ag-resolution-pill">R-12</span>`)
   - en tableau (`<td><span class="ag-resolution-pill">R-12</span></td>`)
   - **JAMAIS inline dans un `<p>` serif** : utiliser `<code class="ag-mono">R-12</code>` à la place.

6. **Sur `report.htmx.html`** : ajouter en bas du document :

```html
<footer class="ag-integrity-footer">
  <p>
    Hash d'intégrité : <code class="ag-mono">{$hash}</code>
    <a href="#" id="verify-integrity-link">Vérifier l'intégrité</a>
  </p>
</footer>
<ag-integrity-modal hidden data-date="{$date}" data-events="{$events_json_escaped}"></ag-integrity-modal>
<script>
  document.getElementById('verify-integrity-link')?.addEventListener('click', (e) => {
    e.preventDefault();
    document.querySelector('ag-integrity-modal')?.open();
  });
</script>
```

Si `report.htmx.html` est un template HTMX statique sans variables PHP, mettre des placeholders `{$date}` / `{$events_json_escaped}` et noter dans SUMMARY que le serveur doit les remplir (ou utiliser `data-*` réelles si déjà disponibles).

7. **Vérifications post-edit pour CHAQUE fichier** :
   - `grep -F "ag-editorial" <fichier>` retourne ≥ 1 match
   - `grep -E 'text-align:\s*center' <fichier>` retourne 0 (sur les enfants directs de `.ag-editorial`)
   - `grep -iE "(copropriété|syndic)" <fichier>` retourne 0
  </action>
  <verify>
    <automated>for f in public/audit.htmx.html public/trust.htmx.html public/archives.htmx.html public/report.htmx.html; do grep -F "ag-editorial" "$f" >/dev/null || { echo "MISSING ag-editorial in $f"; exit 1; }; grep -F "editorial.css" "$f" >/dev/null || { echo "MISSING editorial.css link in $f"; exit 1; }; [ "$(grep -ciE '(copropriété|syndic)' "$f")" = "0" ] || { echo "FORBIDDEN word in $f"; exit 1; }; done && grep -F "ag-integrity-modal" public/report.htmx.html && grep -F "ag-integrity-modal" public/audit.htmx.html && grep -F "Plus de filtres" public/audit.htmx.html</automated>
  </verify>
  <done>4 fichiers wrappés ; editorial.css inclus partout ; ag-integrity-modal présent sur audit + report ; "Plus de filtres" details sur audit ; 0 mot interdit ; lien "Vérifier l'intégrité" sur report.</done>
</task>

</tasks>

<verification>
- Les 4 pages chargent `editorial.css`
- audit.htmx.html : `<details>` "Plus de filtres" présent ; 5 tabs préservés (3 visibles + 2-3 pliés)
- report.htmx.html : `<ag-integrity-modal>` présent ; lien "Vérifier l'intégrité" présent
- Aucun `text-align: center` inline dans les enfants directs de `.ag-editorial`
- 0 occurrence de `copropriété` ou `syndic` dans les 4 fichiers
</verification>

<success_criteria>
Les 4 pages adoptent visuellement le traitement éditorial. Le PV affiche un lien actionnable "Vérifier l'intégrité" qui ouvre le modal pédagogique. Les filter tabs de /audit sont triés en visible/plié.
</success_criteria>

<output>
Créer `.planning/phases/02-pages-editoriales/02-03-SUMMARY.md` après exécution. Y consigner les noms exacts des 5 tabs `/audit` et la décision (visibles vs pliés).
</output>
</content>
</invoke>