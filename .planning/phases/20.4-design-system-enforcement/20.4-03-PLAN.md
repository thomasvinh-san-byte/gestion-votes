---
phase: 20.4-design-system-enforcement
plan: 03
type: execute
wave: 2
depends_on: ["20.4-01"]
files_modified:
  - public/meetings.htmx.html
  - public/assets/css/meetings.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Meetings page fits on 1920x1080 without vertical scroll"
    - "Filter pills, search bar, and meeting cards match wireframe PageSeances"
    - "meetings.css padding override is clean (no conflicting .app-main override)"
    - "Stats bar integrates with wireframe density"
  artifacts:
    - path: "public/meetings.htmx.html"
      provides: "Meetings HTML aligned with PageSeances wireframe"
    - path: "public/assets/css/meetings.css"
      provides: "Meetings-scoped CSS aligned with wireframe"
  key_links:
    - from: "public/meetings.htmx.html"
      to: "public/assets/css/meetings.css"
      via: "meetings-specific class selectors"
      pattern: "\\.meetings-"
---

<objective>
Reconstruct the meetings page to match the PageSeances wireframe section. Align filter pills, search bar, meeting card grid, and create-session card with wireframe density.

Purpose: Meetings page is the gateway to all session workflows — second page in critical path.
Output: Meetings HTML/CSS aligned with wireframe v3.19.2 at 1920x1080.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.4-design-system-enforcement/20.4-RESEARCH.md

@ag_vote_wireframe.html
@public/meetings.htmx.html
@public/assets/css/meetings.css
@public/assets/css/pages.css
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align meetings page with PageSeances wireframe</name>
  <files>public/meetings.htmx.html, public/assets/css/meetings.css</files>
  <action>
1. Open ag_vote_wireframe.html and find `PageSeances` component. Note: filter pills row, search input, meeting cards grid, create-new-session card.

2. Open meetings.htmx.html and compare structure against wireframe.

**Padding override cleanup:**
- RESEARCH notes: `.app-main.meetings-main` has a custom padding override (`padding: 0 1.5rem 1.5rem`).
- Compare against wireframe page padding (16px 22px). If meetings needs different top padding, use a scoped `.meetings-main` selector instead of overriding `.app-main`.
- If the override is unnecessary (same effect as default), remove it.

**Filter pills + search bar:**
- Wireframe shows filter pills (Toutes, En cours, Planifiees, Terminees) in a row with a search input
- Verify HTML uses correct classes (likely `.filter-pills` or `.tab-pills`)
- Check spacing between pills and search matches wireframe

**Meeting cards grid:**
- Wireframe uses a card grid layout for session cards
- Verify gap is 10px (should be correct after Plan 01 global fix)
- Check card padding (14px per wireframe)
- Ensure `.meeting-card` hover uses inherited `.card-clickable` hover (Plan 01 removed duplicate transform)

**Create session card:**
- Wireframe shows a "+" card to create new session
- Verify styling matches other cards but with dashed border or distinct visual

**Stats bar:**
- Check if stats bar exists in wireframe; if not, ensure it doesn't add unwanted height
- If it exists, verify alignment with wireframe placement

3. Fix all discrepancies in meetings.css using SCOPED selectors. Do NOT modify design-system.css or pages.css.

4. Ensure page fits at 1920x1080 without scroll.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -c "meeting" public/meetings.htmx.html > /dev/null && echo "meetings.htmx.html has meeting classes" && test -f public/assets/css/meetings.css && echo "meetings.css exists" && echo "CHECKS PASSED — visual verification at 1920x1080 required"</automated>
  </verify>
  <done>Meetings HTML and CSS match PageSeances wireframe: filter pills, search bar, card grid with 10px gaps, create card. No conflicting padding overrides. Page fits at 1920x1080.</done>
</task>

</tasks>

<verification>
Open meetings page in browser at 1920x1080:
1. Filter pills row with search input aligned
2. Meeting cards in a grid with uniform spacing
3. Create session card visually distinct
4. No vertical scrollbar with typical content
5. No padding conflicts on .app-main
</verification>

<success_criteria>
- Meetings page matches PageSeances wireframe at 1920x1080
- No padding override conflicts
- Card grid density matches wireframe
- Scoped CSS only
</success_criteria>

<output>
After completion, create `.planning/phases/20.4-design-system-enforcement/20.4-03-SUMMARY.md`
</output>
