---
phase: 20.4-design-system-enforcement
plan: 02
type: execute
wave: 2
depends_on: ["20.4-01"]
files_modified:
  - public/dashboard.htmx.html
  - public/assets/css/pages.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Dashboard fits on 1920x1080 without vertical scroll"
    - "KPI grid uses 4 columns with 10px gap matching wireframe"
    - "Urgent card and shortcut grid match wireframe layout density"
    - "Page-header spacing matches wireframe (14px after Step 0 cleanup)"
  artifacts:
    - path: "public/dashboard.htmx.html"
      provides: "Dashboard HTML aligned with PageDashboard wireframe"
    - path: "public/assets/css/pages.css"
      provides: "Dashboard-scoped CSS fixes (scoped selectors only)"
  key_links:
    - from: "public/dashboard.htmx.html"
      to: "public/assets/css/pages.css"
      via: "dashboard-specific class selectors"
      pattern: "\\.dashboard-"
---

<objective>
Reconstruct the dashboard page to match the PageDashboard wireframe section. Fix KPI grid density, panel spacing, shortcut card layout, and ensure the page fits at 1920x1080 without unnecessary scroll.

Purpose: Dashboard is the first page after login — it sets the visual standard for the entire application.
Output: Dashboard HTML/CSS aligned with wireframe v3.19.2 at 1920x1080.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.4-design-system-enforcement/20.4-RESEARCH.md

@ag_vote_wireframe.html
@public/dashboard.htmx.html
@public/assets/css/pages.css
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align dashboard layout with PageDashboard wireframe</name>
  <files>public/dashboard.htmx.html, public/assets/css/pages.css</files>
  <action>
1. Open ag_vote_wireframe.html and find `PageDashboard` component. Note the exact layout structure: KPI grid (4 columns), grid-2 panels below, grid-3 shortcut cards at bottom.

2. Open dashboard.htmx.html and compare HTML structure against wireframe. For each section:

**KPI grid:**
- Wireframe: `.kpi-grid` with 4 columns, `gap: 10px`, each `.kpi-card` with `padding: 12px 8px`
- Check current HTML uses the same grid class structure
- In pages.css, verify `.dashboard-grid` or `.kpi-grid` scoped override uses `gap: 10px` (should be correct after Plan 01)
- If KPI cards have excessive padding, scope a fix: `.dashboard-grid .kpi-card { padding: 12px 8px; }`

**Main panels (grid-2):**
- Wireframe: Two panels side-by-side (sessions urgentes + recent activity or tasks)
- Verify the grid-2 uses `gap: 10px` (handled globally by Plan 01)
- Check card internal padding matches wireframe `14px`
- If panels have different padding, fix with scoped selectors

**Shortcut cards (grid-3):**
- Wireframe: 3-column grid of action shortcut cards
- Verify layout and gap values

**Overall density:**
- The page MUST fit at 1920x1080 without vertical scrollbar
- If sections stack too tall, reduce margins between sections (use `gap: 10px` on the parent container)
- Check `.page-content` padding is reasonable (16px 22px per wireframe)

3. For any CSS fix, use SCOPED selectors (e.g., `.dashboard-panel .card`, not global `.card`). Do NOT modify design-system.css. Only modify pages.css for dashboard-scoped rules.

4. Verify no scroll at 1920x1080 by checking total height of all sections fits within ~960px viewport (1080 - header 56px - some chrome).
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -c "dashboard" public/dashboard.htmx.html > /dev/null && echo "dashboard.htmx.html exists and contains dashboard classes" && grep -c "kpi" public/dashboard.htmx.html > /dev/null && echo "KPI section present" && echo "CHECKS PASSED — visual verification at 1920x1080 required"</automated>
  </verify>
  <done>Dashboard HTML and CSS match PageDashboard wireframe: 4-col KPI grid, grid-2 panels, grid-3 shortcuts, all with 10px gaps and wireframe padding. Page fits at 1920x1080.</done>
</task>

</tasks>

<verification>
Open dashboard page in browser at 1920x1080:
1. KPI cards in a tight 4-column row
2. Two main panels side-by-side below
3. Shortcut cards in 3-column row at bottom
4. No vertical scrollbar
5. Spacing between all sections is uniform (10px gaps)
</verification>

<success_criteria>
- Dashboard matches PageDashboard wireframe layout at 1920x1080
- No unnecessary vertical scroll
- All spacing uses wireframe values (10px gaps, 14px card padding)
- Only scoped CSS selectors used (no global leaks)
</success_criteria>

<output>
After completion, create `.planning/phases/20.4-design-system-enforcement/20.4-02-SUMMARY.md`
</output>
