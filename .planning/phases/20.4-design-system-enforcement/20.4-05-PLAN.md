---
phase: 20.4-design-system-enforcement
plan: 05
type: execute
wave: 3
depends_on: ["20.4-01"]
files_modified:
  - public/operator.htmx.html
  - public/assets/css/operator.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Operator console fits on 1920x1080 without vertical scroll (flex column chain)"
    - "Split panel layout matches PageOperator wireframe (main + 200px sidebar)"
    - "Tab content areas fill available space without overflow"
    - "No !important declarations that can be replaced by specificity"
  artifacts:
    - path: "public/operator.htmx.html"
      provides: "Operator HTML aligned with PageOperator wireframe"
    - path: "public/assets/css/operator.css"
      provides: "Operator CSS cleaned and wireframe-aligned"
  key_links:
    - from: "public/operator.htmx.html"
      to: "public/assets/css/operator.css"
      via: "operator-specific classes"
      pattern: "\\.op-|operator-"
---

<objective>
Reconstruct the operator console to match the PageOperator wireframe section. Verify the split panel layout from Phase 20.3 aligns with wireframe, clean up the 4 !important declarations in responsive rules, and ensure the most complex page in the app fits at 1920x1080.

Purpose: Operator console is the most complex page (4,358 lines CSS) — needs careful alignment.
Output: Operator HTML/CSS aligned with wireframe v3.19.2 at 1920x1080.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20.4-design-system-enforcement/20.4-RESEARCH.md

@ag_vote_wireframe.html
@public/operator.htmx.html
@public/assets/css/operator.css
@public/assets/css/design-system.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align operator console with PageOperator wireframe</name>
  <files>public/operator.htmx.html, public/assets/css/operator.css</files>
  <action>
1. Open ag_vote_wireframe.html and find `PageOperator` component. Note: split panel with main content area and 200px sidebar, tabs for different sections (attendance, motions, vote control).

2. Open operator.htmx.html and compare structure.

**Split panel layout (from Phase 20.3):**
- Per decisions: `.operator-split`/`.operator-left`/`.operator-right` are additive classes on existing `.op-split`/`.op-panel`/`.op-sidebar`
- Flex column chain: `app-main (flex col, overflow:hidden) > view-exec (flex:1, flex col) > operator-split (flex:1, min-height:0) > panels`
- Verify this chain is intact and produces zero-scroll at 1920x1080
- If any element breaks the chain (fixed height, extra margin), fix it

**Tab content areas:**
- Wireframe shows tab panels filling the main content area
- Verify tab content doesn't overflow or cause scroll within the panel
- Check padding inside tab panels matches wireframe density

**Sidebar (200px):**
- Wireframe shows a narrow sidebar with session info, quick actions
- Verify width is ~200px and content doesn't overflow

**!important cleanup:**
- RESEARCH notes 4 `!important` declarations in responsive rules for mobile border resets
- Attempt to replace each with higher-specificity selectors (e.g., `.operator-console .op-panel` instead of `.op-panel !important`)
- If a specific `!important` is genuinely needed for responsive override (media query lower specificity), keep it but add a comment explaining why

**Grid-template-rows override:**
- RESEARCH notes a complex grid-template-rows override
- Compare against wireframe and simplify if possible
- The bimodal console (setup/exec) may need different row templates — that's OK if intentional

3. Fix CSS in operator.css only. Do NOT modify design-system.css. Use operator-scoped selectors.
  </action>
  <verify>
    <automated>cd /home/user/gestion-votes && grep -c "!important" public/assets/css/operator.css | xargs -I{} echo "operator.css !important count: {}" && grep -c "operator-split\|op-split" public/operator.htmx.html | xargs -I{} echo "split panel references: {}" && echo "CHECKS PASSED — visual verification at 1920x1080 required"</automated>
  </verify>
  <done>Operator console matches PageOperator wireframe: split panel layout, tabs, sidebar. !important declarations reduced or justified. Page fits at 1920x1080 with flex column chain intact.</done>
</task>

</tasks>

<verification>
Open operator console in browser at 1920x1080:
1. Split panel visible: main content + 200px sidebar
2. Tab switching works and content fills available space
3. No vertical scrollbar on the page (inner panels may scroll)
4. Mobile responsive view still works (check at 768px width)
</verification>

<success_criteria>
- Operator matches PageOperator wireframe at 1920x1080
- Flex column chain produces zero-scroll layout
- !important declarations reduced or documented
- Split panel proportions correct
</success_criteria>

<output>
After completion, create `.planning/phases/20.4-design-system-enforcement/20.4-05-SUMMARY.md`
</output>
