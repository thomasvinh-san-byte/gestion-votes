---
phase: 41.5-analytics-page-deep-redesign
plan: 02
subsystem: ui
tags: [css, chart.js, analytics, dashboard, data-visualization]

# Dependency graph
requires:
  - phase: 41.5-01
    provides: "Analytics KPI strip, tab bar with sliding indicator, base analytics.css structure"
provides:
  - "Hero chart layout: participationChart and durationChart as full-width chart-card--hero above 2-card grids"
  - "240px horizontal donut with vertical legend beside it on desktop, stacks on mobile"
  - "3-column chart grid for motions tab at 1400px+ viewport"
  - "Striped data tables with sticky headers and col-numeric mono numerics"
  - "2-column anomaly card grid with 36px colored icon containers per severity"
  - "Donut segment hover stroke-width expansion (CSS-only)"
affects: [analytics-page, charts, data-tables]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Hero chart pattern: .chart-card--hero extracted outside .charts-grid, 360px height vs standard 280px"
    - "BEM modifier pattern: --hero and --horizontal modifiers ADD to base classes, never replace"
    - "Anomaly card severity: border-color + background-color + icon background all driven by .warning/.info/.ok class"

key-files:
  created: []
  modified:
    - public/analytics.htmx.html
    - public/assets/css/analytics.css
    - public/assets/js/pages/analytics-dashboard.js

key-decisions:
  - "Hero extraction: participationChart and durationChart moved outside charts-grid as standalone chart-card--hero — remaining 2 cards stay in grid"
  - "donut-card-main wrapper added to group title + subtitle + donut-wrapper as flex-column left side of horizontal card"
  - "Anomaly nth-child(even) hover override uses primary-subtle (not bg-subtle) so stripe + hover states are visually distinct"
  - "Anomaly icon font-size: unset overrides old text-xl emoji sizing — SVG icon() helper renders correctly inside 36x36 container"
  - "charts-grid--3col on motions tab only (3 charts benefit from 3-col); participation and timing grids remain 2-col"

patterns-established:
  - "Hero chart pattern: extract first/featured chart from grid as .chart-card--hero sibling before .charts-grid"
  - "Horizontal donut: donut-card--horizontal flex-row + donut-card-main column wrapper + donut-legend--vertical"

requirements-completed: [UX-01, UX-02]

# Metrics
duration: 12min
completed: 2026-03-20
---

# Phase 41.5 Plan 02: Analytics Deep Redesign — Content Sections Summary

**Hero charts at 360px height per tab, 240px horizontal donut with vertical mono legend, 2-col anomaly card grid with 36px severity icon containers, and striped mono-numeric data tables**

## Performance

- **Duration:** 12 min
- **Started:** 2026-03-20T10:20:00Z
- **Completed:** 2026-03-20T10:32:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Participation tab: participationChart extracted as full-width hero (360px) above 2-card grid (sessions by month + detail table)
- Timing tab: durationChart extracted as full-width hero (360px) above 2-card grid (avg duration + response time)
- Motions tab: donut section restructured to 240px horizontal layout with legend beside; charts-grid gets 3-col at 1400px+
- Data tables: alternating row stripes, sticky headers, col-numeric class applied to 3 numeric cells across 2 JS tables
- Anomaly indicators: full card treatment with 2-column grid, 36x36px colored icon containers per severity (warning/info/ok)

## Task Commits

Each task was committed atomically:

1. **Task 1: Hero chart layout + donut horizontal + 3-col breakpoint** - `2758730` (feat)
2. **Task 2: Striped data tables + anomaly card grid + col-numeric JS** - `cbbbcb8` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `public/analytics.htmx.html` - Participation/Timing hero extraction, donut horizontal restructure, motions 3col class
- `public/assets/css/analytics.css` - chart-card--hero, donut horizontal modifiers, striped tables, anomaly card grid
- `public/assets/js/pages/analytics-dashboard.js` - col-numeric on 3 numeric table cells (participation x2, anomaly x1)

## Decisions Made
- Hero extraction: participationChart and durationChart moved outside charts-grid as standalone chart-card--hero — remaining 2 cards stay in grid for balanced layout
- donut-card-main wrapper groups title + subtitle + donut-wrapper as flex-column left side of horizontal card, keeping the right side purely for the legend
- Anomaly hover uses primary-subtle (not bg-subtle) so stripe rows and hover state are visually distinct
- Anomaly icon `font-size: unset` overrides old `var(--text-xl)` emoji sizing so SVG icons render correctly inside the new 36x36 container
- charts-grid--3col applied to motions tab only — participation and timing have 2 remaining cards, no benefit from 3-col

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Analytics page deep redesign complete (41.5-01 + 41.5-02)
- All chart IDs, donut IDs, and JS rendering logic preserved — zero regressions
- Phase 41.5 complete, ready for next milestone

---
*Phase: 41.5-analytics-page-deep-redesign*
*Completed: 2026-03-20*
