---
phase: 41.5-analytics-page-deep-redesign
plan: 01
subsystem: ui
tags: [analytics, kpi, sticky, tabs, css-animation, badge]

# Dependency graph
requires: []
provides:
  - Horizontal KPI cards with icon-left/body-right layout using an-kpi classes
  - Sticky analytics filter bar with primary-fill period pills
  - Sliding ::after underline tab indicator replacing background-highlight
  - Anomaly tab badge count from loadAnomalies JS
affects: [41.5-analytics-page-deep-redesign]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - an-kpi icon-left/body-right pattern mirrors dash-kpi from Phase 41.4
    - CSS scaleX(0/1) transition for sliding tab indicator (no JS needed)
    - tab-badge:empty { display:none } pattern for conditional badge visibility

key-files:
  created: []
  modified:
    - public/analytics.htmx.html
    - public/assets/css/analytics.css
    - public/assets/js/pages/analytics-dashboard.js

key-decisions:
  - "an-kpi layout reuses same icon-left/body-right pattern established in Phase 41.4 dash-kpi"
  - "Tab sliding indicator uses ::after pseudo-element with scaleX transform — no JS required"
  - "Tab badge uses :empty CSS rule so empty textContent automatically hides badge without JS class toggle"
  - "Sticky filter bar uses top:64px matching header height constant from design system"

patterns-established:
  - "an-kpi-icon--{color} variant classes for colored icon backgrounds matching semantic intent"
  - "scaleX(0) to scaleX(1) transition on ::after pseudo-element for underline animation"

requirements-completed: [UX-01, UX-02]

# Metrics
duration: 12min
completed: 2026-03-20
---

# Phase 41.5 Plan 01: Analytics Page Deep Redesign Summary

**Horizontal KPI strip with colored icon tiles, sticky filter bar with primary-fill pills, and CSS-only sliding tab underline with anomaly badge count**

## Performance

- **Duration:** 12 min
- **Started:** 2026-03-20T10:00:00Z
- **Completed:** 2026-03-20T10:12:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- All 4 KPI cards restructured to horizontal icon-left/body-right layout with color-coded icon tiles (primary/success/warning/info)
- Analytics filter bar made sticky (top:64px) with primary-fill active period pills and Actualiser text button
- Tab navigation replaced background-highlight active state with CSS scaleX ::after underline animation
- Anomalies tab shows live red badge count computed from 6 indicator categories in loadAnomalies()

## Task Commits

Each task was committed atomically:

1. **Task 1: Horizontal KPI cards + sticky filter bar + refresh button text** - `f8dbddb` (feat)
2. **Task 2: Sliding tab indicator + tab badge count** - `f8d7157` (feat)

## Files Created/Modified

- `public/analytics.htmx.html` - KPI cards restructured with an-kpi classes, Actualiser refresh button, tabBadgeAnomalies span
- `public/assets/css/analytics.css` - Added an-kpi horizontal layout, sticky filter bar, updated pills, sliding tab indicator, tab-badge
- `public/assets/js/pages/analytics-dashboard.js` - Badge count computation added to loadAnomalies after indicators HTML render

## Decisions Made

- an-kpi layout mirrors dash-kpi pattern from Phase 41.4 for consistency across dashboard pages
- Tab indicator uses ::after pseudo-element with scaleX(0/1) — purely CSS, no JS events needed
- Badge visibility controlled by `:empty` CSS rule — empty string hides, number shows; no conditional class toggling

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## Next Phase Readiness

- Analytics page top section (KPI strip, filter bar, tabs) fully modernized
- Remaining analytics sections (charts, anomaly panel) ready for subsequent plan if needed
- No blockers or concerns

---
*Phase: 41.5-analytics-page-deep-redesign*
*Completed: 2026-03-20*
