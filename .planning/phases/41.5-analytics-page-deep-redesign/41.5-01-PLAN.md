---
phase: 41.5-analytics-page-deep-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/analytics.htmx.html
  - public/assets/css/analytics.css
  - public/assets/js/pages/analytics-dashboard.js
autonomous: true
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "KPI cards display horizontally with icon left, value/label/trend right"
    - "Filter bar sticks below header on scroll"
    - "Period pills are 36px height with primary-fill active state"
    - "Tab navigation uses sliding underline indicator instead of background highlight"
    - "Anomalies tab shows badge count of active issues"
    - "Refresh button shows icon + 'Actualiser' text"
  artifacts:
    - path: "public/assets/css/analytics.css"
      provides: "an-kpi horizontal layout, sticky filter bar, sliding tab indicator, tab badge styles"
      contains: "an-kpi-icon"
    - path: "public/analytics.htmx.html"
      provides: "Restructured KPI cards with an-kpi classes, tab badge span, refresh button with text"
      contains: "an-kpi-body"
    - path: "public/assets/js/pages/analytics-dashboard.js"
      provides: "Tab badge count update in loadAnomalies"
      contains: "tabBadgeAnomalies"
  key_links:
    - from: "public/analytics.htmx.html"
      to: "public/assets/css/analytics.css"
      via: "an-kpi, analytics-filter-bar sticky, analytics-tab::after classes"
      pattern: "an-kpi|analytics-tab"
    - from: "public/assets/js/pages/analytics-dashboard.js"
      to: "public/analytics.htmx.html"
      via: "getElementById for KPI values and tab badge"
      pattern: "tabBadgeAnomalies"
---

<objective>
Modernize the analytics page top section: horizontal KPI cards with icon/body layout, sticky filter bar with segmented pills, and sliding tab indicator with badge counts.

Purpose: Transform the analytics page from basic centered layout to Stripe-quality dashboard density and visual hierarchy, starting with the page-level navigation and KPI strip.
Output: Horizontal KPIs, sticky filter bar, modern sliding tab indicator with anomaly badge.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41.5-analytics-page-deep-redesign/41.5-CONTEXT.md
@.planning/phases/41.5-analytics-page-deep-redesign/41.5-RESEARCH.md

@public/analytics.htmx.html
@public/assets/css/analytics.css
@public/assets/js/pages/analytics-dashboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Horizontal KPI cards + sticky filter bar + refresh button text</name>
  <files>public/analytics.htmx.html, public/assets/css/analytics.css</files>
  <read_first>
    - public/analytics.htmx.html (lines 92-162 — KPI grid and filter bar)
    - public/assets/css/analytics.css (lines 100-213 — filter bar and KPI grid styles)
    - public/assets/css/analytics.css (lines 215-284 — overview card styles)
    - .planning/phases/41.5-analytics-page-deep-redesign/41.5-RESEARCH.md (Patterns 1, 4 — KPI and sticky filter)
  </read_first>
  <action>
**KPI HTML restructure (analytics.htmx.html lines 93-146):**
Wrap each KPI card's content in the horizontal layout. For each of the 4 KPI cards inside `ag-tooltip` wrappers:
- Add `an-kpi` class to the existing `.kpi-card` div
- Wrap existing `.kpi-icon` div with new `an-kpi-icon an-kpi-icon--{color}` class (primary for kpi-card--1, success for --2, warning for --3, info for --4)
- Change icon size from `icon-sm` to `icon-md`
- Wrap `.kpi-value`, `.kpi-label`, and `.overview-card-trend` inside a new `<div class="an-kpi-body">` div
- Add `an-kpi-value` class alongside existing `kpi-value`
- Add `an-kpi-label` class alongside existing `kpi-label`
- Add `an-kpi-trend` class alongside existing `overview-card-trend`
- Preserve ALL existing id attributes (kpiMeetings, kpiMeetingsTrend, kpiResolutions, kpiResolutionsTrend, kpiAdoptionRate, kpiAdoptionTrend, kpiParticipation, kpiParticipationTrend)

**KPI CSS (add to analytics.css after the .analytics-kpi-grid section ~line 158):**
- `.analytics-kpi-grid .kpi-card` override: `display: flex; flex-direction: row; align-items: center; gap: var(--space-4); padding: var(--space-4) var(--space-5); text-align: left;`
- `.an-kpi-icon`: 44x44px, border-radius md, flex center, flex-shrink 0
- Color variants: `--primary` (primary-subtle bg, primary color), `--success`, `--warning`, `--info`
- `.an-kpi-body`: flex: 1, min-width: 0, flex-column
- `.an-kpi-value`: font-mono, 1.75rem, weight 700, line-height 1, margin-bottom space-1
- `.an-kpi-label`: text-sm, weight 600, text-muted
- `.an-kpi-trend`: text-xs, weight 700, margin-top space-1

**Sticky filter bar CSS (modify existing .analytics-filter-bar ~line 102):**
- Add: `position: sticky; top: 64px; z-index: 10; background: var(--color-bg); border-bottom: 1px solid var(--color-border);`
- Keep existing flex layout

**Period pill size upgrade (modify .analytics-period-pill ~line 119):**
- Change padding from `6px 14px` to `8px 16px` (achieves ~36px height)
- Active state: change from `background: var(--color-surface); color: var(--color-primary); box-shadow: var(--shadow-sm)` to `background: var(--color-primary); color: #fff; box-shadow: none;`

**Refresh button HTML (analytics.htmx.html line 158):**
- Remove `btn-icon` class from refresh button
- Add text "Actualiser" after the SVG icon
- Add `icon-text` class to the SVG icon

**PITFALL 1:** Preserve `.analytics-kpi-grid ag-tooltip { display: contents; }` rule — this prevents grid breakage from tooltip wrappers.
**PITFALL 2:** Scope `text-align: left` override to `.analytics-kpi-grid .kpi-card` — do NOT modify design-system.css.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep -c "an-kpi-body" public/analytics.htmx.html && grep -c "an-kpi-icon" public/assets/css/analytics.css && grep "position: sticky" public/assets/css/analytics.css | head -1 && grep "Actualiser" public/analytics.htmx.html | head -1</automated>
  </verify>
  <acceptance_criteria>
    - All 4 KPI cards have an-kpi class and an-kpi-body wrapper in HTML
    - Each KPI card has an-kpi-icon with correct color variant (primary/success/warning/info)
    - CSS defines .an-kpi-icon, .an-kpi-body, .an-kpi-value, .an-kpi-label, .an-kpi-trend
    - Filter bar has position:sticky with top:64px and z-index:10
    - Period pills have 8px 16px padding; active state uses primary bg with white text
    - Refresh button shows "Actualiser" text next to icon
    - All KPI id attributes preserved (kpiMeetings, kpiResolutions, kpiAdoptionRate, kpiParticipation and their Trend siblings)
    - ag-tooltip display:contents rule preserved
  </acceptance_criteria>
  <done>KPI strip is horizontal with icon-left/body-right layout, filter bar is sticky, pills are 36px with primary fill, refresh button has text label</done>
</task>

<task type="auto">
  <name>Task 2: Sliding tab indicator + tab badge count</name>
  <files>public/analytics.htmx.html, public/assets/css/analytics.css, public/assets/js/pages/analytics-dashboard.js</files>
  <read_first>
    - public/assets/css/analytics.css (lines 457-487 — current tab styles)
    - public/analytics.htmx.html (lines 164-170 — tab markup)
    - public/assets/js/pages/analytics-dashboard.js (lines 533-570 — loadAnomalies function)
    - .planning/phases/41.5-analytics-page-deep-redesign/41.5-RESEARCH.md (Pattern 5 — sliding tabs)
  </read_first>
  <action>
**Tab CSS replacement (replace .analytics-tabs and .analytics-tab rules in analytics.css lines 459-487):**
Replace the existing tab styles entirely with the sliding underline pattern:
- `.analytics-tabs`: `display: flex; gap: 0; border-bottom: 1px solid var(--color-border); margin-bottom: var(--space-6); padding: 0; position: relative;`
- `.analytics-tab`: `padding: 0.75rem 1.25rem; border: none; background: transparent; cursor: pointer; font-size: var(--text-sm); font-weight: 500; color: var(--color-text-muted); border-radius: 0; position: relative; transition: color var(--duration-fast);`
- `.analytics-tab::after`: `content: ''; position: absolute; bottom: -1px; left: 0; right: 0; height: 2px; background: var(--color-primary); border-radius: 2px 2px 0 0; transform: scaleX(0); transition: transform var(--duration-fast) ease;`
- `.analytics-tab:hover`: `color: var(--color-text); background: transparent;`
- `.analytics-tab.active`: `color: var(--color-primary); background: transparent; font-weight: 600;`
- `.analytics-tab.active::after`: `transform: scaleX(1);`

**Tab badge CSS (add after tab rules):**
- `.tab-badge`: inline-flex center, min-width 18px, height 18px, padding 0 5px, radius-full, background danger, color #fff, font 11px weight 700, margin-left space-2, vertical-align middle
- `.tab-badge:empty`: display none

**Tab badge HTML (analytics.htmx.html line 169):**
Add `<span class="tab-badge" id="tabBadgeAnomalies"></span>` inside the Anomalies tab button, after the text "Anomalies".

**Tab badge JS (analytics-dashboard.js, inside loadAnomalies function, after line 569):**
After the `overviewContainer.innerHTML = ...` assignment (line 569), add badge count computation:
```js
var badgeEl = document.getElementById('tabBadgeAnomalies');
var issueCount = (indicators.low_participation_count > 0 ? 1 : 0) +
  (indicators.quorum_issues_count > 0 ? 1 : 0) +
  (indicators.incomplete_votes_count > 0 ? 1 : 0) +
  (indicators.high_proxy_concentration > 0 ? 1 : 0) +
  (indicators.abstention_rate > 20 ? 1 : 0) +
  (indicators.very_short_votes_count > 0 ? 1 : 0);
if (badgeEl) badgeEl.textContent = issueCount > 0 ? issueCount : '';
```

**PITFALL 8:** Tab gap must be 0 (not 0.5rem) — padding on each tab provides spacing. The `::after` underline must be flush with the container border-bottom.
  </action>
  <verify>
    <automated>cd /home/user/gestion_votes_php && grep "scaleX" public/assets/css/analytics.css | head -2 && grep "tabBadgeAnomalies" public/analytics.htmx.html | head -1 && grep "tabBadgeAnomalies" public/assets/js/pages/analytics-dashboard.js | head -1 && grep "tab-badge" public/assets/css/analytics.css | head -2</automated>
  </verify>
  <acceptance_criteria>
    - Tab styles replaced: no more background-color active state, uses ::after pseudo-element with scaleX transform
    - Tab gap is 0, padding provides separation
    - Active tab has primary color text and scaleX(1) underline
    - Hover removes background, only changes text color
    - .tab-badge CSS defined with danger background, :empty hide
    - Anomalies tab HTML contains span#tabBadgeAnomalies
    - JS loadAnomalies updates badge with issue count (1-6 categories checked)
    - Badge shows count > 0, empty string hides via CSS :empty
  </acceptance_criteria>
  <done>Tabs use sliding underline indicator with CSS-only animation, anomalies tab shows red badge with active issue count</done>
</task>

</tasks>

<verification>
- Open analytics page in browser — KPI strip shows 4 horizontal cards with colored icon left, value/label/trend right
- Scroll down — filter bar sticks below header
- Period pills are larger, active pill has solid primary background with white text
- Refresh button shows icon + "Actualiser" text
- Tab bar shows underline animation on active tab, no background highlight
- Anomalies tab shows red badge with count (if issues exist)
- All chart data still loads correctly (no JS errors in console)
- Dark mode renders correctly
</verification>

<success_criteria>
- KPI cards are horizontal (icon-left, body-right) across all 4 cards
- Filter bar is sticky on scroll
- Period pills are 36px height with primary-fill active state
- Tabs use sliding ::after underline, not background color
- Anomalies badge shows issue count
- No JavaScript errors, all existing functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/41.5-analytics-page-deep-redesign/41.5-01-SUMMARY.md`
</output>
