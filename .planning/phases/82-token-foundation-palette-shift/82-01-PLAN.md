---
phase: 82-token-foundation-palette-shift
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/css/design-system.css
autonomous: true
requirements:
  - COLOR-01
  - COLOR-02
  - COLOR-03
  - COLOR-04

must_haves:
  truths:
    - "Light mode semantic tokens reference primitive vars (var(--stone-*), var(--blue-*), etc.) instead of raw hex"
    - "All rgba() calls in semantic token declarations are converted to oklch alpha syntax"
    - "All color-mix() calls in :root use 'in oklch' not 'in srgb'"
    - "Derived hover/active tokens use color-mix(in oklch, base, black/white X%) instead of hardcoded hex"
    - "Accent token --color-accent aliases --color-purple (not --color-primary)"
  artifacts:
    - path: "public/assets/css/design-system.css"
      provides: "Light mode semantic tokens via primitive refs, rgba->oklch, color-mix srgb->oklch in :root"
      contains: "var(--stone-200)"
  key_links:
    - from: "public/assets/css/design-system.css :root semantics"
      to: "public/assets/css/design-system.css :root primitives"
      via: "var(--stone-*) / var(--blue-*) references"
      pattern: "--color-bg:\\s*var\\(--stone-200\\)"
---

<objective>
Migrate all light-mode semantic color tokens in design-system.css from hardcoded hex/rgba to oklch-native values: surface/text/border tokens reference primitives via var(), transparency tokens use oklch alpha syntax, derived tokens use color-mix(in oklch), and all 20 :root color-mix(in srgb) calls are upgraded.

Purpose: Establishes the oklch foundation so all downstream tokens (dark mode, components, critical-tokens) can build on perceptually uniform color math.
Output: design-system.css with :root semantic block fully oklch-native.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-token-foundation-palette-shift/82-CONTEXT.md
@.planning/phases/82-token-foundation-palette-shift/82-RESEARCH.md

<interfaces>
<!-- Primitive palette declarations (lines 95-135) — executor must reference these exact var names -->
From public/assets/css/design-system.css:
```css
/* Stone/Parchment palette */
--stone-50:  oklch(0.969 0.006 95);
--stone-100: oklch(0.950 0.009 95);
--stone-200: oklch(0.922 0.013 95);
--stone-300: oklch(0.893 0.017 90);
--stone-400: oklch(0.833 0.022 88);
--stone-500: oklch(0.760 0.028 85);
--stone-600: oklch(0.648 0.030 82);
--stone-700: oklch(0.530 0.025 80);
--stone-800: oklch(0.350 0.018 78);
--stone-900: oklch(0.180 0.012 75);

/* Blue/Indigo palette */
--blue-50:  oklch(0.960 0.018 265);
--blue-100: oklch(0.920 0.035 265);
--blue-200: oklch(0.850 0.060 265);
--blue-400: oklch(0.680 0.130 265);
--blue-500: oklch(0.600 0.165 265);
--blue-600: oklch(0.520 0.195 265);
--blue-700: oklch(0.440 0.190 265);
--blue-800: oklch(0.360 0.180 265);

/* Green palette */
--green-50:  oklch(0.968 0.020 155);
--green-500: oklch(0.580 0.155 155);
--green-600: oklch(0.500 0.135 155);

/* Amber palette */
--amber-50:  oklch(0.978 0.022 90);
--amber-400: oklch(0.740 0.130 68);
--amber-600: oklch(0.590 0.115 60);

/* Red palette */
--red-50:  oklch(0.975 0.015 25);
--red-500: oklch(0.600 0.185 25);
--red-600: oklch(0.510 0.175 25);

/* Purple palette */
--purple-50:  oklch(0.965 0.022 298);
--purple-500: oklch(0.580 0.175 298);
--purple-600: oklch(0.490 0.170 298);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate light-mode semantic tokens from hex/rgba to primitive refs and oklch</name>
  <files>public/assets/css/design-system.css</files>
  <read_first>
    - public/assets/css/design-system.css (lines 83-135 for primitives, lines 270-430 for :root semantics)
    - .planning/phases/82-token-foundation-palette-shift/82-RESEARCH.md (code examples section for exact mappings)
  </read_first>
  <action>
Edit lines 275-366 of design-system.css :root block. Replace each semantic token value as follows.

**Surface tokens (lines 275-281):**
```css
--color-bg:             var(--stone-200);  /* oklch(0.922 0.013 95) */
--color-bg-subtle:      var(--stone-300);  /* oklch(0.893 0.017 90) */
--color-surface-alt:    var(--stone-300);  /* oklch(0.893 0.017 90) */
--color-surface:        var(--stone-50);   /* oklch(0.969 0.006 95) */
--color-surface-raised: oklch(1 0 0);      /* white — no primitive */
--color-surface-overlay: oklch(0.969 0.006 95 / 0.95);  /* was rgba(250,250,247,0.95) */
--color-glass:           oklch(0.969 0.006 95 / 0.95);
```

**Text tokens (lines 283-289):**
```css
--color-text:          var(--stone-700);  /* oklch(0.530 0.025 80) */
--color-text-dark:     var(--stone-900);  /* oklch(0.180 0.012 75) */
--color-text-secondary: var(--stone-900);
--color-text-muted:    var(--stone-600);  /* oklch(0.648 0.030 82) */
--color-text-light:    var(--stone-500);  /* oklch(0.760 0.028 85) */
--color-text-inverse:  oklch(1 0 0);      /* white */
--color-text-disabled: var(--stone-400);  /* oklch(0.833 0.022 88) */
```

**Border tokens (lines 291-295):**
```css
--color-border:        var(--stone-400);  /* oklch(0.833 0.022 88) */
--color-border-subtle: oklch(0.912 0.015 92);  /* between stone-200 and stone-300, no exact primitive */
--color-border-strong: var(--stone-500);  /* oklch(0.760 0.028 85) */
--color-border-dash:   var(--stone-500);
--color-border-focus:  oklch(0.520 0.195 265 / 0.50);  /* was rgba(22,80,224,0.50) */
```

**Primary tokens (lines 297-303):**
```css
--color-primary:        var(--blue-600);    /* oklch(0.520 0.195 265) */
--color-primary-hover:  color-mix(in oklch, var(--color-primary) 88%, black);
--color-primary-active: color-mix(in oklch, var(--color-primary) 76%, black);
--color-primary-subtle: var(--blue-50);     /* oklch(0.960 0.018 265) */
--color-primary-muted:  oklch(0.520 0.195 265 / 0.12);  /* was rgba(22,80,224,0.12) */
--color-primary-glow:   oklch(0.520 0.195 265 / 0.12);
--color-primary-text:   oklch(1 0 0);       /* white */
```

**Success tokens (lines 305-309):**
```css
--color-success:        var(--green-600);   /* oklch(0.500 0.135 155) */
--color-success-hover:  color-mix(in oklch, var(--color-success) 88%, black);
--color-success-subtle: var(--green-50);    /* oklch(0.968 0.020 155) */
--color-success-border: oklch(0.750 0.100 155);  /* #A3E8C1 approx */
--color-success-text:   var(--green-600);
```

**Warning tokens (lines 311-315):**
```css
--color-warning:        var(--amber-600);   /* oklch(0.590 0.115 60) */
--color-warning-hover:  color-mix(in oklch, var(--color-warning) 88%, black);
--color-warning-subtle: var(--amber-50);    /* oklch(0.978 0.022 90) */
--color-warning-border: oklch(0.860 0.090 80);  /* #F5D490 approx */
--color-warning-text:   var(--amber-600);
```

**Danger tokens (lines 317-321):**
```css
--color-danger:        var(--red-600);   /* oklch(0.510 0.175 25) */
--color-danger-hover:  color-mix(in oklch, var(--color-danger) 88%, black);
--color-danger-subtle: var(--red-50);    /* oklch(0.975 0.015 25) */
--color-danger-border: oklch(0.800 0.080 25);  /* #F4BFBF approx */
--color-danger-text:   var(--red-600);
```

**Info tokens (lines 323-326):**
```css
--color-info:        var(--blue-600);
--color-info-hover:  color-mix(in oklch, var(--color-info) 88%, black);
--color-info-subtle: var(--blue-50);
--color-info-text:   var(--blue-600);
```

**Purple/Accent tokens (lines 328-334):**
```css
--color-purple:        var(--purple-600);  /* oklch(0.490 0.170 298) */
--color-purple-subtle: var(--purple-50);   /* oklch(0.965 0.022 298) */
--color-purple-border: oklch(0.720 0.100 298);  /* #C4B8F8 approx */
--color-accent:        var(--purple-600);  /* stays aliased to purple, not primary — per COLOR-03 */
--color-accent-subtle: var(--purple-50);
--color-accent-border: oklch(0.720 0.100 298);
--color-accent-text:   var(--purple-600);
```

**Neutral tokens (lines 336-339):**
```css
--color-neutral:        var(--stone-600);
--color-neutral-hover:  color-mix(in oklch, var(--color-neutral) 88%, black);
--color-neutral-subtle: var(--stone-300);
--color-neutral-text:   var(--stone-700);
```

**Overlay tokens (lines 341-342):**
```css
--color-backdrop:     oklch(0 0 0 / 0.50);      /* was rgba(0,0,0,0.5) */
--color-overlay-tint: oklch(0 0 0 / 0.04);      /* was rgba(0,0,0,0.04) */
```

**Sidebar tokens (lines 345-350):** Convert rgba to oklch alpha but keep sidebar-bg as direct oklch (intentionally cool/dark for contrast):
```css
--sidebar-bg:         oklch(0.080 0.015 260);    /* was #0C1018 — intentionally cool */
--sidebar-hover:      oklch(1 0 0 / 0.10);       /* was rgba(255,255,255,0.1) */
--sidebar-active:     oklch(0.520 0.195 265 / 0.30);  /* was rgba(22,80,224,0.3) */
--sidebar-border:     oklch(1 0 0 / 0.08);
--sidebar-text:       oklch(1 0 0 / 0.85);
--sidebar-text-hover: oklch(1 0 0);
```

**Tag tokens (lines 353-354):**
```css
--tag-bg:   var(--stone-300);
--tag-text: oklch(0.530 0.025 80);  /* same as stone-700 but direct value since --tag-text is not a stone ref historically */
```

**Derived tint tokens (lines 357-366):** Upgrade from `color-mix(in srgb` to `color-mix(in oklch`:
```css
--color-primary-tint-10: color-mix(in oklch, var(--color-primary) 10%, white);
--color-primary-tint-5:  color-mix(in oklch, var(--color-primary) 5%, white);
--color-success-tint-10: color-mix(in oklch, var(--color-success) 10%, white);
--color-success-tint-5:  color-mix(in oklch, var(--color-success) 5%, white);
--color-danger-tint-10:  color-mix(in oklch, var(--color-danger) 10%, white);
--color-danger-tint-5:   color-mix(in oklch, var(--color-danger) 5%, white);
--color-warning-tint-10: color-mix(in oklch, var(--color-warning) 10%, white);
--color-warning-tint-5:  color-mix(in oklch, var(--color-warning) 5%, white);
--color-primary-shade-10: color-mix(in oklch, var(--color-primary) 90%, black);
--color-surface-elevated: color-mix(in oklch, var(--color-surface) 97%, var(--color-primary));
```

**Persona tokens (lines 373-399):** Leave as hex — they have no oklch primitives and are Phase 84 scope (hardcoded hex cleanup). These are palette-specific decorative colors, not semantic tokens.

IMPORTANT: Do NOT touch the primitives section (lines 83-135) — the duplicate hex/oklch pattern is intentional progressive enhancement.
IMPORTANT: Do NOT touch the shadow tokens (lines 406-427) — they use rgb() with channel variables which is a different pattern (Phase 83 scope).
IMPORTANT: Do NOT rename any token names — add new alongside old if needed per STATE.md decision.
  </action>
  <verify>
    <automated>grep -n "^\s\+--color-bg:\|^\s\+--color-surface:\|^\s\+--color-text:" public/assets/css/design-system.css | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var(--stone-" public/assets/css/design-system.css` returns at least 15 (surface/text/border/neutral/tag tokens)
    - `grep -c "var(--blue-" public/assets/css/design-system.css` returns at least 6 (primary/info tokens)
    - `grep -c "var(--green-\|var(--amber-\|var(--red-\|var(--purple-" public/assets/css/design-system.css` returns at least 10
    - `grep -n "rgba(" public/assets/css/design-system.css | grep "^\s*--color-\|^\s*--sidebar-\|^\s*--color-backdrop\|^\s*--color-overlay" | grep -v shadow | wc -l` returns 0 (no rgba in semantic or sidebar tokens)
    - `grep -c "color-mix(in srgb" public/assets/css/design-system.css` returns a number less than the starting 42 (at least the 10 :root tint tokens are upgraded)
    - Lines 275-366 contain zero raw hex values (except comments) — `grep -n "^  --.*: #" public/assets/css/design-system.css | awk -F: '$1>=275 && $1<=366'` returns empty
    - `--color-accent` value is `var(--purple-600)` not `var(--blue-600)` — confirms COLOR-03 accent sparsity at token level
  </acceptance_criteria>
  <done>All :root semantic tokens (surface, text, border, primary, success, warning, danger, info, purple/accent, neutral, overlay, sidebar, tag, derived tints) use var(--primitive), oklch alpha, or color-mix(in oklch) — zero hex or rgba values remain in semantic token declarations.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade all component-level color-mix(in srgb) calls to oklch</name>
  <files>public/assets/css/design-system.css</files>
  <read_first>
    - public/assets/css/design-system.css (grep for remaining "color-mix(in srgb" to find all component-level occurrences after Task 1)
  </read_first>
  <action>
After Task 1 upgrades the 10 :root tint tokens, there remain ~32 component-level `color-mix(in srgb, ...)` calls scattered through @layer components and @layer v4 (lines ~748-5167).

Run `grep -n "color-mix(in srgb" public/assets/css/design-system.css` to find all remaining occurrences.

For each occurrence, perform a simple text replacement:
- `color-mix(in srgb,` becomes `color-mix(in oklch,`
- If the call uses `#000` or `#000000`, replace with `black`
- If the call uses `#fff` or `#ffffff`, replace with `white`

Do NOT change percentages or token references — only the color space keyword and named color keywords.

Examples of expected changes:
```css
/* Before */
background: color-mix(in srgb, var(--color-primary), #000 8%);
/* After */
background: color-mix(in oklch, var(--color-primary), black 8%);

/* Before */
border-color: color-mix(in srgb, var(--color-danger) 30%, var(--color-border));
/* After */
border-color: color-mix(in oklch, var(--color-danger) 30%, var(--color-border));
```

This is a mechanical find-and-replace. Every single `color-mix(in srgb` must become `color-mix(in oklch`. The success criterion requires ZERO srgb results in the entire file.
  </action>
  <verify>
    <automated>grep -c "color-mix(in srgb" public/assets/css/design-system.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "color-mix(in srgb" public/assets/css/design-system.css` returns exactly 0
    - `grep -c "color-mix(in oklch" public/assets/css/design-system.css` returns at least 42 (all previous srgb calls now oklch)
    - No `#000` or `#000000` appears as a color-mix argument — all replaced with `black` keyword
    - CSS file parses without syntax errors (open in browser devtools or check no unclosed braces)
  </acceptance_criteria>
  <done>Zero `color-mix(in srgb` calls remain in design-system.css. All 42+ color-mix calls use `in oklch`. Success criterion 4 from roadmap is satisfied.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -c "color-mix(in srgb" public/assets/css/design-system.css` returns 0
2. `grep -n "rgba(" public/assets/css/design-system.css | grep "^\s*--color-\|^\s*--sidebar-" | grep -v shadow` returns empty
3. `grep "var(--stone-200)" public/assets/css/design-system.css` matches --color-bg declaration
4. `grep "var(--blue-600)" public/assets/css/design-system.css` matches --color-primary declaration
5. Light mode primitives section (lines 83-135) is unchanged
</verification>

<success_criteria>
- All light-mode semantic tokens reference primitive vars or use direct oklch values
- All rgba() in semantic tokens converted to oklch alpha syntax
- All 42 color-mix(in srgb) calls upgraded to color-mix(in oklch) project-wide within design-system.css
- Derived hover/active tokens computed via color-mix rather than hardcoded hex
- No token names renamed
</success_criteria>

<output>
After completion, create `.planning/phases/82-token-foundation-palette-shift/82-01-SUMMARY.md`
</output>
